# Trezarr Quality Review — quick-260608-hdz (IMP-02b gate-level cue repair)

**Date (UTC):** 20260608_062114
**Scope:** branch `fix/gate-cue-repair` vs `main` (origin/main @ 4f753ae)
**Mode:** code-review · **Specialists:** codec-fidelity-guardian, vietnamese-linguist, pipeline-reliability-reviewer · **Re-review:** codec + linguist after fixes
**Changed files:** `trezarr/translate/engine.py`, `trezarr/translate/validate.py`, `trezarr/config.py`, `tests/translate/test_gate_repair.py`

## Summary verdict: **PASS** (after fixes) — was **BLOCK** (1 BLOCKER / 3 HIGH / 2 MEDIUM / 3 LOW on first pass)

The change adds a bounded gate-level cue-repair pass (IMP-02b): when `validate_subdoc` fails on a *repairable* check {3,9,10,11,12}, `translate_file` re-translates only the failing source cues (through the existing Pass-3 `_translate_batch`), splices, and re-validates — bounded by `gate_repair_max_attempts` (default 3). Structural checks {1,2,4,5,6,7,8} and `enable_gate_repair=False` keep the unchanged quarantine path. It is the file-gate twin of IMP-02 (the batch-PARSE correction loop) and ends the "one straggler cue quarantines all 269 cues" whack-a-mole (jobs 8/9/10).

## Findings found and fixed (RED-test-first)

### [BLOCKER] Repair directive could be echoed and bypass all 12 gate checks → ship (codec) — **CLOSED**
The directive was injected as a fake `SubLine` in `Batch.context_before` ("[CORRECTION REQUIRED] …"). The e8r/job-9 incident *proved* deepseek echoes context hints into cues; an echoed directive (bare or markerless) passed every check and would ship — strictly worse than the bug being fixed (proven empirically against the real gate). **Fix (3 layers, mirrors e8r):** (1) directive moved out of `context_before` into a RULES-block instruction via a new `build_translate_prompt(correction_directive=…)` param (instruction position is a far weaker echo vector); (2) parse-layer `_LEAKED_DIRECTIVE_RE` strips a leading echo (recover); (3) `CORRECTION_DIRECTIVE_RE` added to validate.py **Check 8** as a fail-closed backstop (catches the marker anywhere, incl. mid-cue). Re-review proved closure against the real `validate_subdoc`/`parse_numbered_response`. Residual: a *markerless English paraphrase* echo still ships — accepted LOW (triple-conditional + RULES-block vector; benign meta-text, not the original defect class; documented at engine.py:409-411).

### [HIGH] Repair quarantined on transport errors — wrong failure classification (pipeline, D-47/Pitfall B) — **RESOLVED**
`_repair_failing_cues` caught `except Exception: return None`, turning a transient `openai.APIError` (which the client re-raises) into a permanent quarantine — asymmetric with the non-repair path. **Fix:** narrowed to `except BatchValidationError: return None`; everything else (APIError transport, programming bugs) propagates, leaving the ledger `in_progress` to retry next poll. RED test: repair APIError now propagates, no quarantine artifact.

### [HIGH] Repaired cue dropped its directed pronoun → safe-default mid-conversation (linguist, MOAT) — **RESOLVED**
The repair re-translated with `pronoun_hints=None`; a Check-11 cue's 2nd-person "you" could fall to the safe-default (`bạn`) while neighbours keep the relationship pair (`muội`). **Fix:** thread `flat_attributions` + `name_to_char_id` into `_repair_failing_cues`; per failing cue, resolve speaker/addressee → char ids → `resolved_map[(spk,addr)]` → directed pair, keyed by the repair batch's local index — the *same* resolution contract Pass-3 uses. Missing attribution → unhinted safe-default floor (never a wrong directed pronoun). Re-review verified end-to-end; new `test_repair_receives_directed_pronoun_hint` goes RED when threading is removed.

### [MEDIUM] `_REPAIR_DIRECTIVE[11]` offered `huynh` (a directed term) to an unhinted fallback — **RESOLVED**
Dropped `huynh`; only non-directed/safe placeholders remain (cô / cô nương / tiền bối / trưởng lão / các hạ). All five directive values scanned — no directed kinship/seniority term remains.

### [LOW] Dead `resolved_map` param + over-claiming docstring — **RESOLVED** (param now live via FIX 3; docstring corrected).
### [LOW] Splice didn't guard `raw`/opaque pass-through cues at a failing index — **RESOLVED** (FIX 5: raw cues skipped from repair, preserved verbatim).
### [LOW] `test_moat_regression` was hollow (ran attribution-off) — **RESOLVED** (renamed to `test_repair_preserves_subline_byte_identity`; real moat coverage is the new directed-hint test).

## Confirmed clean
- **Gate stays sole arbiter:** the `while True` loop re-enters a real `validate_subdoc()` before any write; nothing ships without a clean pass; budget-bounded with quarantine-on-failure (verified `test_budget_exhausted`).
- **Concurrency/retry (Pitfall A/B):** no new `asyncio.Semaphore`; repair reuses `LLMClient._semaphore`; no per-call tenacity; 3 retry layers correctly stratified, total per-file ceiling bounded.
- **Codec byte-identity:** repaired SubLines copy index/start_tc/end_tc verbatim, replace only `.text`; envelope/separators carried forward (D-92). SRT/ASS/VTT round-trip suite green.
- **Moat files UNTOUCHED:** reconcile.py / attribute.py / bible/analyze.py — empty diff.
- **validate.py change is minimal + justified:** only `CORRECTION_DIRECTIVE_RE` added to the Check-8 OR (same precedent/tier as HINT_SCAFFOLD_RE); no check weakened.

## Tests
494 passed, 1 skipped, 3 xfailed, 52 xpassed (was 484 → +10: 5 original IMP-02b + 5 harness-fix tests). ruff clean.

## Evidence trail
`_workspace/02_codec-fidelity-guardian.md` (+RE-REVIEW), `_workspace/02_vietnamese-linguist.md` (+RE-REVIEW), `_workspace/02_pipeline-reliability-reviewer.md`, `_workspace/00_changed_files.txt`.
