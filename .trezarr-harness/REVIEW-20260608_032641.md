# Trezarr Quality Review — quick-260608-e8r (Pass-3 hint-leak strip, live job-9 fix)

**Date (UTC):** 20260608_032641
**Scope:** branch `fix/pass3-hint-leak-strip` vs `main` — commits `001c491` (RED), `53a4863` (GREEN)
**Mode:** code-review · **Specialists:** codec-fidelity-guardian, vietnamese-linguist · **Verifier:** skipped (no contested findings; both ran the suite)

## Summary verdict: **PASS** (0 BLOCKER / 0 HIGH / 0 MEDIUM / 0 LOW)

Changed files: `trezarr/translate/engine.py` (+32: `_LEAKED_HINT_RE` + one strip line in `parse_numbered_response`), `tests/translate/test_engine.py` (+4 tests). `validate.py` (Check 8), `reconcile.py`, `attribute.py`, `build_translate_prompt` hint construction — all confirmed untouched. Full suite **484 passed**, ruff clean.

## Live motivation
Server job 9 (`api.deepseek.com / deepseek-v4-pro`, reasoning-on) quarantined the whole episode: deepseek echoed the Pass-3 hint into cue 24 — `(speaker says: muội; addresses as: huynh) Chư vị tu sĩ...`. Check 8 caught it (no corrupt sidecar shipped) but quarantine-whole-file = no output. The H4 carve-out instruction had not stopped the echo.

## The fix
`_LEAKED_HINT_RE = re.compile(r'^\s*\(\s*speaker\s+says\s*:[^)]*\)\s*', re.IGNORECASE)` applied in `parse_numbered_response` AFTER `<<BR>>` restoration, BEFORE the empty-check: a leading echoed hint is stripped, recovering the cue (`… Chư vị tu sĩ...`). Mirrors the existing Pass-4 `(source:` splice-guard. A hint-only echo strips to empty → the existing empty-check raises `BatchValidationError` → IMP-02's correction loop. A source-present title card `(Phàm Nhân Tu Tiên Ký)` does not match → preserved (guards the 260607-iab win).

## Confirmed clean (test-backed)
- **Round-trip / `<<BR>>` / sentinel (codec):** strip runs on the assembled cue string post-`<<BR>>`, anchored `^`; multi-line continuations, internal `\n`, and `<<T0>>` sentinels all preserved (REPL-verified). Index/timecodes/raw untouched.
- **Layered defense by design:** the anchored strip clears the common *leading* echo; `HINT_SCAFFOLD_RE` (validate.py:67, **unanchored**, unchanged) still quarantines mid-line / continuation-line / future echoes the strip deliberately misses. Translated-label variant `(người nói:` remains documented residual risk (same stance as before) — no new gap.
- **Empty-check → IMP-02:** hint-only echo strips to "" → raises → correction loop. Cannot silently ship.
- **ReDoS-safe:** anchored, `[^)]*` bounded by first `)`; 200k-char pathological inputs <0.2ms.
- **Moat-neutral (linguist):** strips only the English instruction phrase; every Vietnamese dialogue pronoun survives byte-for-byte; reconcile/attribute/Address-Map/resolved_map untouched. Job-9 cue strictly improved — `Chư vị tu sĩ` (plural group address) is the relationally correct rendering; only the echoed hint was the defect.
- **Tests load-bearing:** all 4 call the real `parse_numbered_response`; the job-9 test also drives the real `validate_subdoc` and asserts no `GateError`.

## Evidence trail
`_workspace/02_codec-fidelity-guardian.md`, `_workspace/02_vietnamese-linguist.md`, `_workspace/00_changed_files.txt`.
