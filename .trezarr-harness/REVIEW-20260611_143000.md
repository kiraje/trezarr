# Review — fix/audit-l74-regressions (quick 260611-ru6)

**Date:** 2026-06-11 · **Verdict: PASS** (after one fix round) · **Tests:** 548 passed / 1 skipped / 3 xfailed / 52 xpassed
**Scope:** fixes for all 5 verified findings of CONSISTENCY-AUDIT-20260611_121500 (R1 transition-branch bypass, R2 episode-key NxNN + S00E00 guard, R4 gate leak classes, R3 name canonicalization, LOW vfe churn).

## Round 1 (4 specialists)

| Lane | Verdict | Key finding |
|------|---------|-------------|
| vietnamese-linguist | BLOCK→fixed | HIGH: dedup canonicalized to wrong-register (pinyin over Hán-Việt); precedence ladder itself EXACTLY correct (lock > evolution-with-terms > carried pair > safe-default), BIBLE-07 + reciprocity preserved |
| bible-consistency-auditor | BLOCK→fixed | HIGH: dedup false-merged different characters (no character FK); lock precedence, idempotency, vfe sites clean |
| codec-fidelity-guardian | PASS | 16 false-positive classes stress-tested clean; ReDoS-safe; 2 LOW (comment, by-design boundary) |
| pipeline-reliability-reviewer | BLOCK→fixed | HIGH: unanchored NxNN regex parsed resolutions (1024x768→S24E768); S00E00 guard + idempotency correct |

## Round 2 fixes (commits 3c5700b..de90f71)

1. NxNN regex anchored with digit-boundary lookarounds (mirrors library.py:51) + resolution RED tests.
2. `dedup_canonical_name_terms` write loop REMOVED (no character FK = no provably-safe rewrite; closes both HIGHs); prevention guard extended with two linkage mechanisms (script-name match + rendering match) blocking dual-row creation in both orderings. FK-linked repair documented as the future path. Live dual-row data to be repaired via the Bible API (human-override valve).
3. Atomicity LOW moot (no write loop). 4. Misleading regex comment corrected via comment-TDD.

## Verifier (final): 5 CLOSED · 0 STILL-OPEN · 1 NEW LOW

Residual LOW (non-blocking, in-code documented): a later English-only run inferring bare pinyin can still create a SEPARATE locked `Han Li` row (split symptom) — it cannot overwrite or relock the established `韩立→Hàn Lập`. Resolution = character_id FK migration (backlogged).

Artifacts: `_workspace_*/02_*.md`, `03_verdicts.md` (archived per run).
