# Trezarr Quality Review — quick-260607-iab (paren-preservation, ContextWeave benchmark fix)

**Date (UTC):** 20260607_063814
**Scope:** branch `fix/contextweave-paren-preservation` vs `main` — commits `56c2e69`, `c287b91`, `f96c125`
**Mode:** code-review · **Specialists:** codec-fidelity-guardian, vietnamese-linguist · **Verifier:** finding-verifier (two rounds)

## Summary verdict: **PASS** (1 HIGH found → fixed → re-verified; 0 open)

| Severity | Found | Open after fix |
|---|---|---|
| BLOCKER | 0 | 0 |
| HIGH | 1 | **0 (fixed in `f96c125`, verified 5/0)** |
| LOW | 1 (informational) | 0 (pre-existing coverage confirmed) |

Changed files: `trezarr/translate/engine.py` (build_translate_prompt H4 rule), `trezarr/translate/validate.py` (Check 8), `tests/translate/test_engine.py`, `tests/translate/test_validate.py`. Scope guardrail held: **`reconcile.py` / `attribute.py` / Address-Map untouched** (orchestrator-confirmed, not in diff).

## Confirmed findings

### [HIGH → FIXED] Paren-preserve rule re-opened the Pass-3 hint scaffolding-leak class
- **Where:** `engine.py` H4 rule × the per-line pronoun hint `[N] (speaker says: <self>; addresses as: <addr>) <text>` (engine.py:403); gate `validate.py` Check 8.
- **Invariant:** No internal prompt scaffolding may ship into a sidecar (blind-trust bar) — the 260604/260607 leak class. The Ep-142 audit had `(speaker says: tại hạ; addresses as: Mai cô nương)` shipping in 7 on-screen cues.
- **Evidence (finding-verifier, round 1, empirical):** the exact benchmark string + modern + recased variants **passed all 12 gate checks** via the real `validate_subdoc` (Check 8 matched only `(source:`; Check 12 skips diacritic-bearing parentheticals; Check 10 skips diacritic cues). The new "PRESERVE source parentheses" rule *increases* the echo incentive the old unqualified rule had incidentally suppressed.
- **Fix (`f96c125`), two layers:**
  - *Prompt:* H4 rule now carves the hint out as a PRIVATE pronoun instruction — never translate/echo/keep it or its parentheses.
  - *Gate:* new `HINT_SCAFFOLD_RE = re.compile(r'\(\s*speaker\s+says\s*:', re.I)` folded into Check 8 (OR with `REVIEW_SCAFFOLD_RE`) → a leaked hint quarantines.
- **Re-verification (finding-verifier, round 2, 5/0 VERIFIED):** leak now raises `check=8` (3 variants incl. exact Ep-142 string); **0 false positives** across 13 realistic Vietnamese cues; title-card win intact (`(Phàm Nhân Tu Tiên Ký)` still preserved + passes gate); `(source:` Pass-4 detection intact; full suite 475 passed.

### [LOW → informational] No new test pins model-added gloss → Check 12
- codec-fidelity-guardian PASS; the gloss→Check-12 path is already pinned by pre-existing `test_audit_followup.py:105-110` + `test_validate.py:555-565` (both green). Not a gap.

## Residual risk (accepted, documented)
A model that *translates* the hint labels to Vietnamese (`(người nói: …; xưng hô: …)`) is not matched by `HINT_SCAFFOLD_RE` — identical accepted stance to the sibling `REVIEW_SCAFFOLD_RE` (`(nguồn:` residual). Prompt-layer carve-out lowers upstream probability; verbatim-label echo is the materially likely case and is now covered both layers.

## Evidence trail
`_workspace/02_codec-fidelity-guardian.md`, `_workspace/02_vietnamese-linguist.md`, `_workspace/03_verdicts.md` (round 1), `_workspace/03_verdicts_fix.md` (round 2).
