# Trezarr Quality Review — quick-260607-o4g (IMP-02 self-correcting batch retry + local runner)

**Date (UTC):** 20260607_104746
**Scope:** branch `feat/selfcorrect-retry-local-runner` vs base `9e7237d` — commits `f3405e7` (IMP-02), `dd89d89` (runner)
**Mode:** code-review · **Specialists:** pipeline-reliability-reviewer, vietnamese-linguist · **Verifier:** skipped (no contested findings — see note)

## Summary verdict: **PASS** (0 BLOCKER / 0 HIGH / 0 MEDIUM; 2 LOW non-blocking)

Changed files: `trezarr/translate/engine.py` (Pass-3 retry → correction loop), `scripts/translate_one.py` (new dev runner), `tests/translate/test_batch_self_correct.py` (5 tests), `.gitignore`. Both specialists **ran the real test suite** (`test_batch_self_correct.py` 5/5; translate+llm+output 207 passed, 0 failures). Full suite reported by executor: **480 passed, 0 failures**.

**Motivation:** a live Ep-142 server run quarantined after 16 min on `Missing line [1] in LLM response (expected 4 lines)` — DeepSeek dropped a line from a 4-cue batch and the old tenacity loop re-sent the identical prompt with no feedback. IMP-02 (confirmed benchmark finding) makes the batch self-correct instead.

## Confirmed clean (load-bearing claims, both test-backed)

### Retry boundary — no double-retry storm (pipeline-reliability)
Tenacity fully removed (import + `@retry` decorator gone; `BatchValidationError` in no `retry_if_exception_type`). Loop bounded: `attempts = translate_batch_retry_attempts + 1` (default 3), `for attempt_num in range(attempts)`, re-raise only on `is_last_attempt`. `openai.APIError` still handled solely by the SDK (`max_retries=4`) — orthogonal to the correction trigger (a clean-200 structural defect), so SDK-retry × correction-loop is **not** multiplicative. Single concurrency gate preserved (`LLMClient._semaphore`; no second semaphore). Pass-3 stays fast (no `thinking=`).

### Quarantine / idempotency unchanged
Budget exhaustion re-raises `BatchValidationError` → same `except* BatchValidationError` → `ledger.record(status="quarantined")` path, byte-for-byte unchanged. Numbered-line + document gates remain the sole arbiters. Sentinel-integrity failure routes through the same loop — **not** a weakening: it was *already* a retryable `BatchValidationError` under the old decorator, and `reinsert_sentinels` re-gates every attempt, so a lost-real-sentinel batch cannot ship via a lucky recovery.

### Directed-pronoun moat preserved (vietnamese-linguist)
`messages[0]` (the hinted/glossary/register prompt from `build_translate_prompt`) is built once and **append-only** thereafter — never mutated, re-built, or paraphrased; re-sent verbatim each retry. `_STRUCT_CORRECTION_MSG` is structural-only (`{defect}` = count/line metadata, `{n}` = int) with **no** pronoun slot; references hints as "from my FIRST message" without quoting. Silent-flip risk is **unchanged vs the old retry** (which also re-output all N lines) and arguably stricter (the correction turn re-anchors "follow … all pronoun hints from my FIRST message"). Pass-4 self-review remains the unchanged post-Pass-3 net. No diff line touches reconcile.py / attribute.py / Address-Map / resolved_map / pronoun_hints construction.

### Tests A–E real & load-bearing
`FakeLLMClient` snapshots each `messages` list and serves scripted replies; Test A drives drop-line-[1]-of-4 → recover-on-2nd-call; D/E assert exact `call_count == retry_attempts+1` (no storm); B/C assert the moat (`"(speaker says:"` absent from the correction turn; `messages[0]` immutable). Mock matches the real `LLMClient.call(... thinking=None)` signature.

## LOW (non-blocking, dev-tool only)
1. `scripts/translate_one.py` uses the JSON `Ledger` while prod uses `LedgerSQLA` — both satisfy `LedgerProtocol`; no prod impact (runner is gitignored output, not copied into the Docker image, never imported by the daemon).
2. The runner's `MagicMock` `media_item` makes `arr_kind` truthy — harmless today (it calls `translate_file` directly), but a forward footgun if ever re-wired to cli.py's `_run_translate_for_item` (set `media_item.arr_kind = None` then).

## Note on verifier
The finding-verifier (adversarial REFUTE stage) was skipped: both specialists returned PASS with **zero candidate findings to adjudicate** (unlike the 260607-iab round, which had a contested HIGH), and both independently executed the test suite as evidence. The load-bearing claims (no double-retry; moat-immutable `messages[0]`; structural-only correction turn) are each backed by a passing deterministic test. Re-running an opus verifier on two uncontested, test-proven PASSes was judged disproportionate.

## Evidence trail
`_workspace/02_pipeline-reliability-reviewer.md`, `_workspace/02_vietnamese-linguist.md`, `_workspace/00_changed_files.txt`.
