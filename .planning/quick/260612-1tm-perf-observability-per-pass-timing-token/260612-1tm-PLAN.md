---
phase: quick-260612-1tm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trezarr/llm/metrics.py
  - trezarr/llm/client.py
  - trezarr/translate/engine.py
  - tests/llm/test_metrics.py
autonomous: true
requirements: [step0-instrumentation]

must_haves:
  truths:
    - "PassStatsCollector.record(duration_s, prompt_tokens, completion_tokens, retries) accumulates per-call data and summary() returns a PassMetrics with correct totals and call_count"
    - "PassMetrics is a frozen dataclass with fields: call_count, total_duration_s, prompt_tokens, completion_tokens, retry_count"
    - "PassStatsCollector initialized with no data returns PassMetrics with all zeros from summary()"
    - "LLMClient.call() accepts an optional collector: PassStatsCollector | None = None parameter; callers that omit it get identical behavior (zero regression)"
    - "When a collector is passed to LLMClient.call(), _call_with_fallback records: wall-clock duration of the raw SDK call (perf_counter before/after), prompt_tokens + completion_tokens from response.usage (0 if usage is None/not present), and SDK-level retry count (always 0 — SDK retries are opaque; the field is present for future use)"
    - "translate_file logs a per-pass wall-clock line at INFO level for Pass 1, 2, 3, and 4 when they run: 'pass=1 duration_s=X.XX file=<basename>'"
    - "translate_file logs a job-completion summary at INFO level with per-pass token + call aggregates: 'job_summary file=<basename> pass1_calls=N pass1_prompt_tokens=N pass1_completion_tokens=N pass1_duration_s=X.XX pass2_... pass3_... pass4_...'"
    - "The full 548-test suite remains green; no prompt, batching, concurrency, or translation behavior is changed"
    - "No schema migration, no new DB table, no Alembic revision"
  artifacts:
    - path: "trezarr/llm/metrics.py"
      provides: "PassMetrics frozen dataclass + PassStatsCollector with record() and summary()"
      exports: ["PassMetrics", "PassStatsCollector"]
    - path: "trezarr/llm/client.py"
      provides: "LLMClient.call() with optional collector= param; _call_with_fallback captures usage + duration"
    - path: "trezarr/translate/engine.py"
      provides: "per-pass perf_counter timing + collector wiring in translate_file; structured log lines"
    - path: "tests/llm/test_metrics.py"
      provides: "TDD tests: PassStatsCollector accumulation, PassMetrics zero-init, summary correctness"
  key_links:
    - from: "trezarr/llm/metrics.py:PassStatsCollector"
      to: "trezarr/llm/client.py:LLMClient._call_with_fallback"
      via: "optional collector= param threaded from call() → _call_with_fallback()"
      pattern: "collector.record(duration_s, prompt_tokens, completion_tokens"
    - from: "trezarr/llm/client.py:_call_with_fallback (Tier 2/3)"
      to: "resp.usage"
      via: "resp.usage.prompt_tokens / resp.usage.completion_tokens (None-guarded)"
      pattern: "getattr(resp.usage, 'prompt_tokens', 0) or 0"
    - from: "trezarr/translate/engine.py:translate_file"
      to: "PassStatsCollector per pass"
      via: "perf_counter() before/after each pass block; collector passed to llm_client.call()"
      pattern: "time.perf_counter()"
---

<objective>
Add lightweight log-only performance observability to the Trezarr translation pipeline.

Purpose: Step 0 of the core-quality campaign — the proving runs (Step 1) will generate
structured log lines that Step 3 can parse to answer "where does the time go" per pass.
Without this, no per-pass timing or token data exists anywhere in the pipeline.

Output:
- trezarr/llm/metrics.py — PassMetrics dataclass + PassStatsCollector accumulator
- LLMClient.call() gains optional collector= for per-call duration + token capture
- translate_file gains per-pass wall-clock logging + job-completion summary log line
- tests/llm/test_metrics.py — TDD tests for the accumulator unit

Hard constraints (from campaign spec):
- Log-only: no schema migration, no Alembic revision, no new DB table
- Zero behavior change: no prompt/batching/concurrency/translation logic altered
- 548 existing tests must stay green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-1tm-perf-observability-per-pass-timing-token/260612-1tm-PLAN.md
@.planning/STATE.md

Key codebase facts (no re-read needed):
- trezarr/llm/client.py: LLMClient.call(messages, response_model=None, model=None, thinking=None)
  returns BaseModel | str. _call_with_fallback has three tiers:
    Tier 1: parsed = await client.chat.completions.parse(...)  → usage on parsed.usage
    Tier 2: resp   = await client.chat.completions.create(...) → usage on resp.usage
    Tier 3: resp   = await client.chat.completions.create(...) → usage on resp.usage
  self._call_kwargs NEVER mutated (FIX-B invariant). effective_call_kwargs is per-call local.

- trezarr/translate/engine.py:translate_file
  Pass 1 BARRIER: analyze_file() + merge_bible_analysis() at line ~1717
  Pass 2: TaskGroup over attribute_batch() at line ~1760
  Reconcile: reconcile_attributions() at line ~1771
  Pass 3 (translate): TaskGroup over _translate_batch() at line ~1858
  Pass 4 (review): TaskGroup over _review_batch() at line ~2006
  llm_client.call() is called inside _translate_batch_inner (line ~1006) and _review_batch (line ~652)
  attribute.py:attribute_batch calls llm_client.call() at line ~301
  analyze.py:_analyze_one_chunk calls llm_client.call() at line ~487

- No perf_counter, elapsed, usage, or token logging exists anywhere in trezarr/ Python code.
- import time is not in engine.py (must be added).
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (TDD): PassStatsCollector accumulator unit</name>
  <files>tests/llm/test_metrics.py, trezarr/llm/metrics.py</files>
  <behavior>
    - Test: PassStatsCollector() starts with summary() returning PassMetrics(call_count=0, total_duration_s=0.0, prompt_tokens=0, completion_tokens=0, retry_count=0)
    - Test: after record(duration_s=1.5, prompt_tokens=100, completion_tokens=50, retries=0), summary() returns call_count=1, total_duration_s=1.5, prompt_tokens=100, completion_tokens=50, retry_count=0
    - Test: after two record() calls, summary() returns correctly accumulated totals (call_count=2, totals summed)
    - Test: PassMetrics is frozen — assignment to any field raises FrozenInstanceError
    - Test: record() with prompt_tokens=None and completion_tokens=None treats them as 0 (None-safe)
  </behavior>
  <action>
    RED: create tests/llm/test_metrics.py with the five tests above. Import from trezarr.llm.metrics.
    Mark the first test xfail(strict=False, raises=(ImportError, AssertionError)) — module does not exist yet.
    Run pytest tests/llm/test_metrics.py and confirm failures.

    GREEN: create trezarr/llm/metrics.py:

    PassMetrics: frozen=True dataclass with fields call_count: int, total_duration_s: float,
    prompt_tokens: int, completion_tokens: int, retry_count: int. All default 0.

    PassStatsCollector: plain class.
      __init__: initializes _call_count=0, _total_duration_s=0.0, _prompt_tokens=0,
        _completion_tokens=0, _retry_count=0.
      record(duration_s: float, prompt_tokens: int | None, completion_tokens: int | None,
             retries: int = 0) -> None:
        increments _call_count by 1; adds duration_s; adds (prompt_tokens or 0) and
        (completion_tokens or 0); adds retries.
      summary() -> PassMetrics: returns PassMetrics with the five accumulated totals.

    Run pytest tests/llm/test_metrics.py — all five tests must pass.
    Run full suite: pytest --tb=short -q — must stay at 548 passing.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/llm/test_metrics.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>tests/llm/test_metrics.py: 5 tests pass (no xfail). trezarr/llm/metrics.py exists. Full suite still at 548 passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire PassStatsCollector into LLMClient._call_with_fallback</name>
  <files>trezarr/llm/client.py, tests/llm/test_client_tiers.py</files>
  <behavior>
    - Test: LLMClient.call(messages, collector=collector) — after a successful Tier 2 call where resp.usage has prompt_tokens=120, completion_tokens=40, summary() shows call_count=1, prompt_tokens=120, completion_tokens=40
    - Test: LLMClient.call(messages) with no collector= still returns the same content string (zero regression)
    - Test: collector records duration_s > 0 after a real (mocked) call
  </behavior>
  <action>
    Add to trezarr/llm/client.py:

    1. Import: add `import time` at the top (alongside existing imports).
    2. Import: add `from trezarr.llm.metrics import PassStatsCollector` (or use TYPE_CHECKING
       with a string annotation if circular import risk — there is none since metrics.py has no
       trezarr imports).

    3. LLMClient.call() signature: add `collector: "PassStatsCollector | None" = None` as
       the last keyword parameter. Thread it through to _call_with_fallback as the last positional/
       keyword arg. No other change to call().

    4. LLMClient._call_with_fallback() signature: add `collector: "PassStatsCollector | None" = None`.

    5. In each tier, wrap the raw SDK call with perf_counter and capture usage:

       Tier 1 (parse):
         _t0 = time.perf_counter()
         parsed = await self._client.chat.completions.parse(...)
         _dur = time.perf_counter() - _t0
         if collector is not None:
             _u = getattr(parsed, "usage", None)
             collector.record(
                 duration_s=_dur,
                 prompt_tokens=getattr(_u, "prompt_tokens", None),
                 completion_tokens=getattr(_u, "completion_tokens", None),
             )
         -- then proceed as before with msg = parsed.choices[0].message

       Tier 2 (json_object create):
         _t0 = time.perf_counter()
         resp = await self._client.chat.completions.create(...)
         _dur = time.perf_counter() - _t0
         if collector is not None:
             _u = getattr(resp, "usage", None)
             collector.record(
                 duration_s=_dur,
                 prompt_tokens=getattr(_u, "prompt_tokens", None),
                 completion_tokens=getattr(_u, "completion_tokens", None),
             )
         -- then proceed as before with content = resp.choices[0].message.content

       Tier 3 (plain text create):
         Same pattern as Tier 2, using resp.

    CRITICAL invariants to preserve:
    - self._call_kwargs is NEVER mutated (FIX-B). Only effective_call_kwargs (local) is used.
    - The semaphore gate in call() is unchanged (async with self._semaphore).
    - Return type of call() is still BaseModel | str. The collector is a side-effect only.
    - Do NOT add perf_counter timing around the semaphore wait in call() — measure only the
      raw SDK call inside _call_with_fallback.
    - existing callers (attribute.py, analyze.py, engine.py _translate_batch_inner, _review_batch)
      all omit collector= — zero regression, they default to None.

    Add 2-3 targeted tests in tests/llm/test_client_tiers.py (or a new
    tests/llm/test_client_metrics.py) confirming the collector is populated after a mocked call.
    Mirror the existing AsyncMock patching pattern from test_client_tiers.py.

    Run: pytest tests/llm/ -v --tb=short — all existing tests must still pass.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/llm/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>LLMClient.call() accepts collector= param. _call_with_fallback records duration_s + token usage for each successful SDK call. All existing llm tests pass. No behavior change for callers that omit collector=.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-pass timing + job-completion summary in translate_file</name>
  <files>trezarr/translate/engine.py, tests/translate/test_engine.py</files>
  <behavior>
    - Test: translate_file on a minimal mock source calls logger.info with a string matching 'pass=3' and 'duration_s=' (Pass 3 always runs)
    - Test: the job_summary log line matches 'job_summary' and 'pass3_calls=' and 'file='
  </behavior>
  <action>
    In trezarr/translate/engine.py:

    1. Add `import time` to the existing imports block (alongside `import asyncio`, `import logging`, etc.).
    2. Add `from trezarr.llm.metrics import PassStatsCollector` after the trezarr imports.

    3. Inside translate_file, create four PassStatsCollector instances near the top of the
       function body (after the ledger skip checks, before the passes begin):
         _p1_col = PassStatsCollector()
         _p2_col = PassStatsCollector()
         _p3_col = PassStatsCollector()
         _p4_col = PassStatsCollector()

    4. Pass 1 timing: wrap the analyze_file + merge_bible_analysis block:
         _p1_t0 = time.perf_counter()
         analysis = await analyze_file(..., collector=None)  # analyze_file doesn't accept collector yet — see note
         await merge_bible_analysis(...)
         _p1_dur = time.perf_counter() - _p1_t0
         logger.info("pass=1 duration_s=%.2f file=%s", _p1_dur, path.name)

       NOTE: analyze_file and attribute_batch don't accept collector= (that would require modifying
       those functions). For Pass 1 and Pass 2, only wall-clock duration is captured via
       perf_counter in engine.py. Token capture for Pass 1/2 requires threading collectors
       through analyze_file/attribute_batch — deferred; log zero for those passes' token fields
       in the summary.

    5. Pass 2 timing: wrap the TaskGroup for attr_batches:
         _p2_t0 = time.perf_counter()
         async with asyncio.TaskGroup() as tg:
             attr_tasks = [...]
         _p2_dur = time.perf_counter() - _p2_t0
         logger.info("pass=2 duration_s=%.2f file=%s", _p2_dur, path.name)

       Pass 2 runs only when settings.enable_attribution is True. Add the log line only
       inside the `if settings.enable_attribution:` block after the TaskGroup.

    6. Pass 3 timing + collector: wrap the Pass-3 TaskGroup.
       The _translate_batch factory function (_make_translate_batch_fn) produces
       _translate_batch_inner which calls llm_client.call(). The collector must be threaded
       through _translate_batch → _translate_batch_inner → llm_client.call(collector=_p3_col).

       To do this without a large refactor:
         a. Add `collector: "PassStatsCollector | None" = None` to _translate_batch_inner's
            signature.
         b. In _translate_batch_inner, pass `collector=collector` to llm_client.call().
         c. Add `collector: "PassStatsCollector | None" = None` to _translate_batch's
            signature and thread it into _translate_batch_inner.
         d. In translate_file's Pass-3 TaskGroup, pass collector=_p3_col to each
            _translate_batch(b, ..., collector=_p3_col) call.

       Wrap the TaskGroup:
         _p3_t0 = time.perf_counter()
         async with asyncio.TaskGroup() as tg:
             translate_tasks = [
                 tg.create_task(_translate_batch(b, llm_client, settings, ..., collector=_p3_col))
                 ...
             ]
         batch_results = [t.result() for t in translate_tasks]
         _p3_dur = time.perf_counter() - _p3_t0
         logger.info("pass=3 duration_s=%.2f file=%s", _p3_dur, path.name)

       CRITICAL: the BatchValidationError except* block after the TaskGroup must remain
       unchanged. Move the perf_counter stop and log line to AFTER the except* handling
       completes (i.e., after the `if _batch_quarantine is not None: return _batch_quarantine`
       guard). Actually, since we need timing regardless of quarantine: record _p3_dur before
       the except* by capturing perf_counter in the try block's finally, OR simply place
       _p3_t0 before the try/except* and _p3_dur after the except* block. The timing only
       needs to be logged on success (quarantined files return early before the log line
       anyway — that path returns before the job_summary line).

    7. Pass 4 timing + collector: mirror Pass 3.
       Add `collector: "PassStatsCollector | None" = None` to _review_batch's signature.
       In _review_batch, pass `collector=collector` to llm_client.call().
       In translate_file's Pass-4 TaskGroup, pass collector=_p4_col.
       Wrap the TaskGroup:
         _p4_t0 = time.perf_counter()
         async with asyncio.TaskGroup() as tg:
             review_tasks = [tg.create_task(_review_batch(..., collector=_p4_col)) ...]
         _p4_dur = time.perf_counter() - _p4_t0
         logger.info("pass=4 duration_s=%.2f file=%s", _p4_dur, path.name)
       Place AFTER the `if _review_failed:` guard block.

    8. Job completion summary log line: add immediately before Step 10 (write_vi_sidecar),
       after the validate_subdoc loop exits cleanly:
         _p1s = _p1_col.summary()
         _p2s = _p2_col.summary()
         _p3s = _p3_col.summary()
         _p4s = _p4_col.summary()
         logger.info(
             "job_summary file=%s "
             "pass1_calls=%d pass1_prompt_tokens=%d pass1_completion_tokens=%d pass1_duration_s=%.2f "
             "pass2_calls=%d pass2_prompt_tokens=%d pass2_completion_tokens=%d pass2_duration_s=%.2f "
             "pass3_calls=%d pass3_prompt_tokens=%d pass3_completion_tokens=%d pass3_duration_s=%.2f "
             "pass4_calls=%d pass4_prompt_tokens=%d pass4_completion_tokens=%d pass4_duration_s=%.2f",
             path.name,
             _p1s.call_count, _p1s.prompt_tokens, _p1s.completion_tokens, _p1_dur if ...,
             ...
         )

       For Pass 1/2 token fields: use _p1s.prompt_tokens (will be 0 since analyze_file/
       attribute_batch don't thread the collector — correct, documents the gap for future wiring).
       Use _p1_dur / _p2_dur (local floats) for wall-clock; use _p1s.total_duration_s for
       Pass 3/4 where collectors are wired (it will equal _p3_dur ± epsilon from parallelism).
       For simplicity use _p1_dur, _p2_dur, _p3_dur, _p4_dur (local floats) uniformly.

       EDGE CASE — passes that didn't run: initialize _p1_dur = _p2_dur = _p3_dur = _p4_dur = 0.0
       at the top of translate_file (same place as _p*_col). Only assign when the pass actually
       runs. This ensures the job_summary log line always fires with zero for bypassed passes.

       Pass 1/2 only run in the Bible-aware branch (eligible_item + session_factory not None).
       Pass 2 only runs when settings.enable_attribution is True.
       Pass 4 only runs when settings.enable_self_review is True + Bible-aware.
       In passthrough mode (no eligible_item), all four duration vars stay 0.0 — the
       job_summary line still fires with zeros, which is correct (no Bible passes ran).

    INVARIANTS — do not change:
    - The except* BatchValidationError block and its quarantine path are unchanged.
    - _review_batch's "never raises" (D-59) contract is unchanged. The collector= param
      is optional with default None; existing call sites that omit it get None.
    - The _make_translate_batch_fn factory closure structure is preserved. The collector
      is NOT captured in the closure — it is passed as a parameter per call so that
      concurrent batches each write to the SAME collector (thread-safe: asyncio is
      single-threaded, so concurrent coroutines cannot interleave record() calls).
    - No new asyncio.Semaphore anywhere (Pitfall 1).
    - PassStatsCollector is not shared across files — each translate_file invocation creates
      fresh collector instances. This is per-file aggregation, not global.

    Add 2 targeted tests in tests/translate/test_engine.py using caplog or a mock logger:
    - Confirm a "pass=3 duration_s=" INFO log line is emitted for a normal translation run.
    - Confirm a "job_summary" INFO log line is emitted containing "pass3_calls=".
    Mirror the existing mock-LLM patterns in test_engine.py.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_engine.py tests/llm/ -v --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>translate_file emits per-pass timing log lines and a job_summary log line. Pass 3 and Pass 4 collectors capture token usage from LLMClient responses. Full suite at 548+ passing (new tests add to the count). Log format is grep-friendly key=value pairs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → metrics | usage field from SDK response object — None-guarded, never user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-1tm-01 | Information Disclosure | PassStatsCollector log output | accept | Log lines contain only numeric durations + token counts — no PII, no prompt content, no API keys |
| T-1tm-02 | Tampering | perf_counter timing | accept | Wall-clock is observational only; no decision logic depends on it |
| T-1tm-SC | Tampering | No new npm/pip/cargo installs | accept | metrics.py uses only stdlib (dataclasses, time) — no new dependencies |
</threat_model>

<verification>
Full test suite must stay green:
  pytest --tb=short -q (expect 548+ passing, 0 failures)

Spot-check structured log output format:
  python -c "
  from trezarr.llm.metrics import PassMetrics, PassStatsCollector
  c = PassStatsCollector()
  c.record(1.23, 100, 50)
  c.record(0.77, 80, 30)
  s = c.summary()
  assert s.call_count == 2
  assert abs(s.total_duration_s - 2.0) < 0.001
  assert s.prompt_tokens == 180
  assert s.completion_tokens == 80
  print('PassStatsCollector OK')
  "

Confirm no DB or schema files modified:
  git diff --name-only | grep -v '^#' | grep -E 'alembic|migration|\.sql' | wc -l == 0
</verification>

<success_criteria>
- trezarr/llm/metrics.py exists with PassMetrics (frozen dataclass) + PassStatsCollector
- LLMClient.call() has optional collector= param; existing callers unaffected
- translate_file emits pass=N duration_s=X.XX file=F log lines for each pass that runs
- translate_file emits a single job_summary key=value log line at job completion
- 548+ tests green; 0 failures; no Alembic migration; no behavior change
</success_criteria>

<output>
Create .planning/quick/260612-1tm-perf-observability-per-pass-timing-token/260612-1tm-SUMMARY.md when done.
</output>
