---
phase: quick-260608-e8r
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - trezarr/translate/engine.py
  - tests/translate/test_engine.py
autonomous: true
requirements: [LIVE-FIX-e8r]

must_haves:
  truths:
    - "parse_numbered_response strips a LEADING '(speaker says: …; addresses as: …)' prefix from cue text after <<BR>> restoration and before the empty-check — the job-9 incident cue '[24] (speaker says: muội; addresses as: huynh) Chư vị tu sĩ...' recovers to 'Chư vị tu sĩ...' (D-hint-strip)"
    - "A hint-only echo (no dialogue after the parens) is stripped to empty, which triggers the existing BatchValidationError empty-check, handing control to IMP-02's correction loop"
    - "A source-present title-card like '(Phàm Nhân Tu Tiên Ký)' is NOT stripped — the 260607-iab paren-preservation win is intact"
    - "validate_subdoc / Check 8 / HINT_SCAFFOLD_RE in validate.py are UNCHANGED — they remain the defense-in-depth backstop"
    - "reconcile.py, attribute.py, build_translate_prompt (pronoun hint construction), and build_review_prompt are UNCHANGED"
  artifacts:
    - path: "trezarr/translate/engine.py"
      provides: "_LEAKED_HINT_RE module-level regex + strip applied inside parse_numbered_response"
      contains: "_LEAKED_HINT_RE"
    - path: "tests/translate/test_engine.py"
      provides: "4 deterministic TDD tests for the strip"
      contains: "test_parse_strips_leading_hint_echo"
  key_links:
    - from: "parse_numbered_response (engine.py ~line 729)"
      to: "_LEAKED_HINT_RE.sub"
      via: "applied after _BR_RE.sub, before parsed[n] = text and empty-check"
      pattern: "_LEAKED_HINT_RE\\.sub"
---

<objective>
Surgically strip a LEADING echoed Pass-3 attribution hint from each cue inside
parse_numbered_response — after <<BR>> restoration, before the empty-check. This
recovers the live job-9 incident (cue 24 quarantined the whole episode) without
touching any part of the pronoun moat.

Purpose: A model that echoes `(speaker says: muội; addresses as: huynh) Chư vị tu sĩ...`
should produce `Chư vị tu sĩ...` (strip + keep dialogue), not quarantine the episode.
A model that echoes the hint with NO dialogue produces an empty cue, which the existing
empty-check converts to a BatchValidationError → IMP-02 self-correction loop retries the batch.

Output: One module-level regex (_LEAKED_HINT_RE) + one strip call inside
parse_numbered_response, plus 4 TDD tests confirming all four behaviours.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@trezarr/translate/engine.py
@trezarr/translate/validate.py
@tests/translate/test_engine.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write 4 failing tests for the leading-hint strip</name>
  <files>tests/translate/test_engine.py</files>
  <behavior>
    - Test 1 (job-9 exact): parse_numbered_response called with a 1-line batch response
      `"[24] (speaker says: muội; addresses as: huynh) Chư vị tu sĩ..."` and expected_count=1
      (reindex as [1] for the test, or build a 24-line stub — simpler: use a single-cue
      response `"[1] (speaker says: muội; addresses as: huynh) Chư vị tu sĩ..."`, expected_count=1)
      → returns `["Chư vị tu sĩ..."]` (hint stripped, Vietnamese kept).
      ALSO assert that the stripped text passes validate_subdoc without a Check 8 GateError
      (the leak was in engine output pre-gate; after strip it should be clean).
    - Test 2 (modern variant): `"[1] (speaker says: anh; addresses as: em) Em đừng đi."`, expected_count=1
      → returns `["Em đừng đi."]`.
    - Test 3 (hint-only → empty → raises): `"[1] (speaker says: ta; addresses as: ngươi)"`, expected_count=1
      → raises BatchValidationError matching "Empty/whitespace-only" (the existing empty-check
      fires after strip leaves `""`; document in the test that this is IMP-02's trigger).
    - Test 4 (preservation — title card NOT stripped): `"[1] (Phàm Nhân Tu Tiên Ký)"`, expected_count=1
      → returns `["(Phàm Nhân Tu Tiên Ký)"]` — the parens are preserved because the text
      does NOT match `\(\s*speaker\s+says\s*:`, guarding the 260607-iab paren-win.
    All four tests use the existing `pytest.importorskip("trezarr.translate.engine")` pattern
    (no bare top-level imports). Group them together following the test_parse_* cluster near
    line 718.
  </behavior>
  <action>
    Add four test functions to tests/translate/test_engine.py in a clearly labelled block
    after the existing continuation/BR tests (around line 780):

    test_parse_strips_leading_hint_echo_job9 — covers Test 1 (job-9 exact) + the
    validate_subdoc clean-pass assertion. Import parse_numbered_response and
    BatchValidationError from engine via pytest.importorskip. For the validate_subdoc
    assertion, also importorskip "trezarr.translate.validate" and build a minimal one-cue
    SubDoc with _make_doc/_make_line helpers (already at the top of test_engine.py or
    import from test_validate). Use `_make_line(index=1, text=stripped_text)` and a
    matching source line, then call validate_subdoc(trn, src, settings) and assert no
    GateError is raised (the hint was stripped before the gate sees it — no quarantine).

    test_parse_strips_leading_hint_echo_modern — Test 2, single assert.

    test_parse_hint_only_raises_empty_error — Test 3. Assert raises BatchValidationError.
    Include a comment: "stripped-to-empty hands control to IMP-02 correction loop".

    test_parse_preserves_source_paren_title_card — Test 4. Assert parens round-trip intact.
    Include a comment: "guards 260607-iab paren-preservation win".

    Run `pytest tests/translate/test_engine.py -k "hint_echo or hint_only or paren_title" -x`
    — all four MUST FAIL (RED) before proceeding to Green.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_engine.py -k "hint_echo or hint_only or paren_title" -x 2>&1 | tail -20</automated>
  </verify>
  <done>All four new tests exist and FAIL (FAILED, not ERROR/SKIP) — RED confirmed.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN + commit — add _LEAKED_HINT_RE and strip to engine.py</name>
  <files>trezarr/translate/engine.py</files>
  <behavior>
    - _LEAKED_HINT_RE is defined at module level (near the other module-level regex constants
      _NUMBERED_LINE_RE and _BR_RE, around line 636-640).
    - The strip is applied inside parse_numbered_response's final validation loop, AFTER the
      three _BR_RE sub calls (lines 726-728) and BEFORE `parsed[n] = text` and the empty-check.
    - Tests 1 and 2 pass: hint + dialogue → clean dialogue returned.
    - Test 3 passes: hint-only → empty → BatchValidationError("Empty/whitespace-only").
    - Test 4 passes: `(Phàm Nhân Tu Tiên Ký)` — does NOT match the regex, preserved unchanged.
    - validate.py (HINT_SCAFFOLD_RE, Check 8) is NOT modified.
    - reconcile.py, attribute.py, build_translate_prompt, build_review_prompt are NOT modified.
    - Full test suite stays green: `pytest tests/ -x -q`.
  </behavior>
  <action>
    Step A — define the regex near line 640 in engine.py, after _BR_RE:

      _LEAKED_HINT_RE = re.compile(
          r'^\s*\(\s*speaker\s+says\s*:[^)]*\)\s*',
          re.IGNORECASE,
      )

    Rationale for `[^)]*`: the hint parenthetical `(speaker says: X; addresses as: Y)` has no
    nested parens, so `[^)]*` stops at the closing `)` without backtracking risk. The `^`
    anchor (plus DOTALL is NOT needed — the text is already a single cue string after
    <<BR>> restoration; newlines within multi-line cues are handled, but the hint is always
    a leading prefix on the assembled text). Case-insensitive and space-tolerant, mirroring
    HINT_SCAFFOLD_RE in validate.py.

    Step B — inside parse_numbered_response, in the final `for n in range(1, expected_count + 1)`
    loop, AFTER the three `_BR_RE` sub lines (currently lines 726-728) and the
    `parsed[n] = text` assignment (currently line 729), insert the strip BEFORE that assignment:

      # Strip a LEADING echoed Pass-3 pronoun hint (job-9 incident, 260608-e8r).
      # "(speaker says: X; addresses as: Y)" is an English instruction phrase that
      # CANNOT appear in genuine Vietnamese dialogue. Stripping only the leading prefix
      # leaves the actual translation intact. If the model echoed the hint with NO dialogue,
      # the result is empty → the existing empty-check below raises BatchValidationError
      # → IMP-02's correction loop retries the batch (desired).
      # A source-present paren "(Phàm Nhân Tu Tiên Ký)" does NOT match "speaker says:"
      # so it is preserved — guards the 260607-iab paren-preservation win.
      # MOAT INVARIANT: this strip touches ONLY the echoed instruction text, not the
      # translated dialogue pronouns. validate.py Check 8 / HINT_SCAFFOLD_RE remains
      # the defense-in-depth backstop for any leak this strip does not catch.
      text = _LEAKED_HINT_RE.sub('', text)
      parsed[n] = text

    The existing `if not parsed[n].strip(): raise ...` on the next line handles the hint-only case.

    Step C — commit RED tests then GREEN impl as two separate commits:
      git commit -m "test(quick-260608-e8r): RED — 4 failing tests for leading hint-echo strip"
      git commit -m "feat(quick-260608-e8r): strip leading echoed Pass-3 hint in parse_numbered_response"

    Step D — run full suite to confirm no regressions:
      pytest tests/ -x -q
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_engine.py -k "hint_echo or hint_only or paren_title" -x -q && python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All four new tests pass (GREEN). Full suite passes with no regressions. _LEAKED_HINT_RE
    is defined at module level in engine.py. The strip is applied in parse_numbered_response
    after _BR_RE restoration, before the empty-check. validate.py is unchanged. Two commits
    recorded (RED + GREEN).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → parse_numbered_response | Untrusted text — the model can echo arbitrary instruction phrases into cue content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-e8r-01 | Tampering | parse_numbered_response cue text | mitigate | _LEAKED_HINT_RE strips the LEADING hint echo before it reaches validate_subdoc or sidecar write; Check 8 in validate.py is the backstop |
| T-e8r-02 | Spoofing | _LEAKED_HINT_RE false-positive on legitimate Vietnamese | accept | The trigger phrase "speaker says:" is English instruction text that cannot appear in Vietnamese dialogue by construction; no false-positive risk |
| T-e8r-03 | Denial of Service | hint-only echo leaves empty cue → BatchValidationError | accept (desired) | Desired behaviour: empty cue triggers the IMP-02 correction loop, which retries the batch; it does NOT quarantine the episode on first occurrence |
</threat_model>

<verification>
1. `pytest tests/translate/test_engine.py -k "hint_echo or hint_only or paren_title" -x -q` — all four new tests pass.
2. `pytest tests/ -x -q` — full suite green (no regressions introduced).
3. Manual inspection: `grep "_LEAKED_HINT_RE" trezarr/translate/engine.py` returns the definition (module level) and the strip call (inside parse_numbered_response).
4. Manual inspection: `grep -c "_LEAKED_HINT_RE\|HINT_SCAFFOLD_RE" trezarr/translate/validate.py` — validate.py unchanged (still has only HINT_SCAFFOLD_RE, no _LEAKED_HINT_RE).
5. Manual inspection: `grep -c "REVIEW_SCAFFOLD_RE\|_reject_scaffolded" trezarr/translate/engine.py` — Pass-4 splice guard is untouched.
</verification>

<success_criteria>
- Job-9 cue `[1] (speaker says: muội; addresses as: huynh) Chư vị tu sĩ...` → parse returns `["Chư vị tu sĩ..."]` — no quarantine.
- Hint-only cue `[1] (speaker says: ta; addresses as: ngươi)` → parse raises BatchValidationError("Empty/whitespace-only") → IMP-02 retries.
- Title-card `[1] (Phàm Nhân Tu Tiên Ký)` → parse returns `["(Phàm Nhân Tu Tiên Ký)"]` — paren preserved.
- validate.py Check 8 / HINT_SCAFFOLD_RE is unchanged (defense-in-depth intact).
- reconcile.py, attribute.py, build_translate_prompt, build_review_prompt — untouched.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/quick/260608-e8r-surgically-strip-a-leaked-pass-3-attribu/260608-e8r-SUMMARY.md` when done.
</output>
