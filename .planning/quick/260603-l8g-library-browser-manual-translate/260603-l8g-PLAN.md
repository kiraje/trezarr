---
phase: quick-260603-l8g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trezarr/web/routes/library.py
  - trezarr/web/app.py
  - trezarr/config.py
  - alembic/env.py
  - trezarr/web/scheduler.py
  - trezarr/web/config_writer.py
  - frontend/src/api/client.ts
  - frontend/src/pages/Library.tsx
  - frontend/src/components/AppShell.tsx
  - frontend/src/App.tsx
  - frontend/src/pages/Settings.tsx
  - tests/web/test_library_api.py
  - tests/web/test_scheduler_auto_translate.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /api/library returns sonarr series + radarr movies; each source disabled or erroring returns its section empty with a per-source error note — never 500"
    - "GET /api/library/series/{id}/episodes returns per-episode rows with status enum translated|has_source|nothing"
    - "POST /api/translate enqueues exactly one job via enqueue_job(trigger='manual'); returns 400 if no source sub exists"
    - "auto_translate_enabled=False by default; poll_and_enqueue is a no-op when this flag is False"
    - "Bug 3: saving sonarr/radarr/bazarr host+key auto-sets the corresponding *_enabled=True in config_writer"
    - "Bug 2: alembic fileConfig uses disable_existing_loggers=False so trezarr.* loggers are not silenced after migration"
    - "Library page visible in sidebar; drill-in shows episode rows with Translate button enabled for has_source items; clicking navigates to /queue"
  artifacts:
    - path: "trezarr/web/routes/library.py"
      provides: "GET /api/library, GET /api/library/series/{id}/episodes, POST /api/translate"
    - path: "frontend/src/pages/Library.tsx"
      provides: "Library browser UI with series/movie listing and episode drill-in"
    - path: "tests/web/test_library_api.py"
      provides: "pytest coverage for all three new endpoints"
    - path: "tests/web/test_scheduler_auto_translate.py"
      provides: "pytest coverage for auto_translate_enabled gate in poll_and_enqueue"
  key_links:
    - from: "trezarr/web/routes/library.py POST /api/translate"
      to: "trezarr/web/worker.py::enqueue_job"
      via: "trigger='manual'"
    - from: "trezarr/web/scheduler.py::poll_and_enqueue"
      to: "settings.auto_translate_enabled"
      via: "early-return guard before sonarr/radarr discovery blocks"
    - from: "trezarr/web/config_writer.py::write_settings_to_yaml"
      to: "sonarr_enabled / radarr_enabled / bazarr_enabled"
      via: "Bug 3 auto-set when host+key both non-empty"
---

<objective>
Library browser + manual single-item translate + auto-translate safety gate.

Purpose: Give the user a way to browse what Sonarr/Radarr knows about, pick ONE episode or
movie, and translate it as a controlled test — without the poller mass-translating the entire
library. Fixes two bugs that would make a failed test untraceable (Bug 2: silenced loggers)
and connection saves that left discovery disabled (Bug 3).

Output:
- trezarr/web/routes/library.py (three new endpoints)
- trezarr/config.py (auto_translate_enabled field)
- trezarr/web/scheduler.py (auto_translate gate)
- trezarr/web/config_writer.py (Bug 3)
- alembic/env.py (Bug 2)
- frontend/src/pages/Library.tsx + wiring in App.tsx + AppShell.tsx + Settings.tsx
- frontend/src/api/client.ts (new typed wrappers)
- tests/web/test_library_api.py
- tests/web/test_scheduler_auto_translate.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend read endpoints (GET /api/library + GET /api/library/series/{id}/episodes)</name>
  <files>trezarr/web/routes/library.py, trezarr/web/app.py</files>
  <action>
Create trezarr/web/routes/library.py with two read-only endpoints. Register the new router
in trezarr/web/app.py with prefix="/api".

GET /api/library:
- Import: build_sonarr_client, build_radarr_client, _normalize_arr_host from trezarr.arr.sonarr /
  radarr; DiscoveryError from trezarr.arr. These are deferred inside the route (PLC0415 pattern
  used throughout the codebase).
- Retrieve settings from request.app.state.settings (fall back to TrezarrSettings() as the
  existing settings.py helper does).
- If settings.sonarr_enabled: call build_sonarr_client(settings).series.get(); for each item in
  the list emit a dict with keys: kind="series", id (arr id), title, year, monitored,
  poster_url (series["images"][0]["remoteUrl"] if images else None — first image entry only;
  guard against missing list). Wrap in try/except (PyarrError, Exception) → catch any error,
  append {"source": "sonarr", "error": str(exc)} to a top-level "errors" list, set
  series=[]. If sonarr_enabled is False, series=[].
- Same pattern for radarr: kind="movie", id, title, year, monitored. For movies also resolve
  source-sub status: source_sub_found = find_source_sub(local_path, settings.source_lang_priority)
  is not None; include source_sub_found bool. Wrap in try/except same way.
- Return JSONResponse({"series": [...], "movies": [...], "errors": [...]}) with HTTP 200 always.
- Import pyarr exceptions as: `from pyarr.exceptions import PyarrError` (deferred, inside the
  function, PLC0415). Catch PyarrError first, then broad Exception as fallback (same catch
  pattern as existing discover_sonarr_items in sonarr.py).
- For movies: local_path comes from apply_path_mapping on movie.get("movieFile", {}).get("path", "")
  — mirror the radarr.py pattern. If path is empty/None, source_sub_found=False.

GET /api/library/series/{id}/episodes:
- Path param: id: int (the Sonarr arr series id).
- Gate on settings.sonarr_enabled; if disabled return 400 {"error": "sonarr_disabled"}.
- client = build_sonarr_client(settings)
- ep_files = client.episode_file.get(series_id=id) — may return list or single dict (existing
  sonarr.py lines 173-179 show the normalize-to-list pattern; replicate it: if isinstance(ep_files, dict) → ep_files = [ep_files]).
- Open a DB session via request.app.state.session_factory (fallback: skip ledger lookup if
  None/missing on app.state — the no-db path should not 500).
- For each ep_file dict:
  - raw_path = ep_file.get("path", "")
  - If not raw_path: skip.
  - local_path = apply_path_mapping(raw_path, settings.path_mappings) → Path
  - source_sub_result = find_source_sub(local_path, settings.source_lang_priority)
    (source_sub_result is tuple[Path,str]|None)
  - episode_key = derive_episode_key(None, raw_path) — pass None for media_item; the function
    falls back to parsing the subtitle/video filename. Actually: derive_episode_key takes
    (media_item, source_sub_path); pass media_item=None and source_sub_path=raw_path.
  - Determine status:
      - If source_sub_result is None → status = "nothing"
      - Else check vi sidecar: vi_path = local_path.parent / (local_path.stem + ".vi.srt");
        also check .vi.ass and .vi.vtt. If any vi sidecar exists → status = "translated"
      - Else → status = "has_source"
  - title: ep_file.get("sceneName") or ep_file.get("relativePath", "")
  - source_path: str(source_sub_result[0]) if source_sub_result else None
  - source_lang: source_sub_result[1] if source_sub_result else None
  - Emit: {episode_key, title, local_path: str(local_path), status, source_path, source_lang}
- Return JSONResponse([...list of episode dicts...], 200).
- Wrap the whole body in try/except (PyarrError, Exception) → return 502 {"error": str(exc)}.

In trezarr/web/app.py: add after the existing bible_router include:
  from trezarr.web.routes.library import router as library_router  (deferred, PLC0415 pattern)
  app.include_router(library_router, prefix="/api")
  (Place this block after line ~304 where webhook_router is registered, following the exact
  same register pattern as the other routers.)

Apply path_mappings import: `from trezarr.paths import apply_path_mapping` (deferred PLC0415).
Import find_source_sub: `from trezarr.discover.scan import find_source_sub` (deferred PLC0415).
Import derive_episode_key: `from trezarr.translate.engine import derive_episode_key` (deferred PLC0415).
Import MediaItem: not needed for the read path — local_path is built from raw path directly.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run python -c "from trezarr.web.routes.library import router; print('library router ok')"</automated>
  </verify>
  <done>
library.py imports without error; router registered in app.py; GET /api/library and
GET /api/library/series/{id}/episodes are discoverable in the FastAPI route table (uv run
python -c "from trezarr.web.app import create_app; app=create_app(); routes=[r.path for r in app.routes]; assert any('/library' in r for r in routes), routes").
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /api/translate — single-item manual enqueue</name>
  <files>trezarr/web/routes/library.py</files>
  <action>
Add POST /api/translate to trezarr/web/routes/library.py (same file as Task 1).

Request body (JSON): { kind: "series"|"movie", source_path?: str, arr_series_id?: int,
episode_id?: int }

The simplest resolution strategy: if source_path is provided directly, use it (the frontend
will pass it from the episode row which already resolved it in GET /api/library/series/{id}/episodes).
If source_path is absent, return 400 {"error": "source_path_required"}.

Steps:
1. Parse body via `await request.json()`.
2. source_path = body.get("source_path"). If falsy → return JSONResponse({"error": "source_path_required"}, 400).
3. arr_series_id = body.get("arr_series_id")  # int|None; None for movies
4. Verify source sub file actually exists on disk:
   p = Path(source_path)
   if not p.exists(): return JSONResponse({"error": "source_sub_not_found", "path": source_path}, 400)
5. session_factory = getattr(request.app.state, "session_factory", None)
6. from trezarr.web.worker import enqueue_job  (deferred PLC0415)
7. enqueued = await enqueue_job(
       session_factory=session_factory,
       source_path=source_path,
       series_id=arr_series_id,
       trigger="manual",
       media_item=None,
   )
   enqueue_job signature: enqueue_job(session_factory, source_path, series_id, trigger, media_item=None) → bool
8. Return JSONResponse({"enqueued": enqueued, "source_path": source_path}, 200).
   (enqueued=False means a job was already queued for that path — not an error, just dedup.)

No blocking/synchronous translate path. No process_one_item call here. Only enqueue_job.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run python -c "from trezarr.web.routes.library import router; routes = [r.path for r in router.routes]; assert '/translate' in routes or any('translate' in r for r in routes), routes; print('translate route ok')"</automated>
  </verify>
  <done>
POST /api/translate exists in the router; returns 400 on missing source_path (verified by test
in Task 6); uses trigger="manual" with no synchronous translate path.
  </done>
</task>

<task type="auto">
  <name>Task 3: Safety gate — auto_translate_enabled config field + poller gate + Bug 3</name>
  <files>trezarr/config.py, trezarr/web/scheduler.py, trezarr/web/config_writer.py</files>
  <action>
THREE sub-changes in one safety-gate commit:

A. trezarr/config.py — add new field after the bazarr_use_inventory line (around line 179):
   # ── Library browser / manual translate safety gate ────────────────────────
   auto_translate_enabled: bool = False
   # When False, the scheduler poller discovers items but never enqueues them.
   # The user must flip this on explicitly to allow the daemon to auto-translate
   # the whole library. This protects paid LLM endpoints from accidental mass-use.
   No DB migration needed — this is a config.yaml / pydantic-settings field only.

B. trezarr/web/scheduler.py — add an early-return guard at the top of poll_and_enqueue,
   AFTER the existing `if session_factory is None or settings is None:` guard (lines 59-62),
   BEFORE the imports and sonarr/radarr discovery blocks:

   if not getattr(settings, "auto_translate_enabled", False):
       logger.debug(
           "poll_and_enqueue: auto_translate_enabled=False — skipping auto-translate sweep"
       )
       return

   This means: browsing (GET /api/library, GET /api/library/series/{id}/episodes) and manual
   translate (POST /api/translate → enqueue_job directly) still work. Only the scheduled
   poller's automatic sweep is gated.

C. trezarr/web/config_writer.py — Bug 3: in write_settings_to_yaml, after the existing
   `existing[key] = value` line (line ~144), add an auto-enable pass:

   # Bug 3: when a *arr connection's host + api_key are both non-empty after applying
   # the patch, auto-set the corresponding *_enabled = True so discovery/listing works
   # without requiring the user to flip the toggle manually.
   for svc in ("sonarr", "radarr", "bazarr"):
       host_key = f"{svc}_host"
       api_key_key = f"{svc}_api_key"
       enabled_key = f"{svc}_enabled"
       host_val = existing.get(host_key, "")
       api_key_val = existing.get(api_key_key, "")
       # Only auto-enable; never auto-disable (the user may have explicitly disabled).
       if host_val and api_key_val and api_key_val != SENTINEL:
           if not existing.get(enabled_key, False):
               existing[enabled_key] = True
               logger.debug(
                   "write_settings_to_yaml: auto-enabled %s (host+key both set)", enabled_key
               )

   Insert this block BEFORE the `config_path = CONFIG_PATH` line (after the main patch loop ends).
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run python -c "from trezarr.config import TrezarrSettings; s=TrezarrSettings(); assert s.auto_translate_enabled is False; print('auto_translate_enabled default False ok')"</automated>
  </verify>
  <done>
TrezarrSettings().auto_translate_enabled is False; poll_and_enqueue returns immediately when
auto_translate_enabled=False (verified in Task 6 test); config_writer auto-sets *_enabled=True
when host+key are non-empty on save.
  </done>
</task>

<task type="auto">
  <name>Task 4: Bug 2 — alembic/env.py disable_existing_loggers=False</name>
  <files>alembic/env.py</files>
  <action>
In alembic/env.py, find the line:
    fileConfig(config.config_file_name)
and change it to:
    fileConfig(config.config_file_name, disable_existing_loggers=False)

This is a one-line fix. The Python logging.config.fileConfig default for disable_existing_loggers
is True, which wipes out all trezarr.* logger registrations that happened before Alembic runs
its migration at daemon startup. With disable_existing_loggers=False the trezarr.* loggers
survive the migration call.

Do NOT change any other lines in alembic/env.py. Stage only alembic/env.py for this commit.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && grep -c "disable_existing_loggers=False" alembic/env.py</automated>
  </verify>
  <done>grep -c "disable_existing_loggers=False" alembic/env.py outputs 1.</done>
</task>

<task type="auto">
  <name>Task 5: Frontend — Library page + nav + api/client.ts + Settings auto_translate toggle</name>
  <files>
    frontend/src/api/client.ts,
    frontend/src/pages/Library.tsx,
    frontend/src/components/AppShell.tsx,
    frontend/src/App.tsx,
    frontend/src/pages/Settings.tsx
  </files>
  <action>
Five sub-changes in one frontend commit:

A. frontend/src/api/client.ts — add a local helper fetchWithLongTimeout (30 s, same pattern
   as the existing fetchWithTimeout but with a 30000 ms constant) to use for library endpoints
   only. Then add typed wrappers at the end of the file:

   export interface LibrarySeriesItem {
     kind: "series";
     id: number;
     title: string;
     year: number | null;
     monitored: boolean;
     poster_url: string | null;
   }

   export interface LibraryMovieItem {
     kind: "movie";
     id: number;
     title: string;
     year: number | null;
     monitored: boolean;
     poster_url: string | null;
     source_sub_found: boolean;
   }

   export interface LibraryResponse {
     series: LibrarySeriesItem[];
     movies: LibraryMovieItem[];
     errors: { source: string; error: string }[];
   }

   export interface EpisodeRow {
     episode_key: string;
     title: string;
     local_path: string;
     status: "translated" | "has_source" | "nothing";
     source_path: string | null;
     source_lang: string | null;
   }

   export interface TranslateResponse {
     enqueued: boolean;
     source_path: string;
   }

   export async function getLibrary(): Promise<LibraryResponse> {
     const resp = await fetchWithLongTimeout("/api/library");
     if (!resp.ok) throw new Error(`GET /api/library: ${resp.status}`);
     return resp.json();
   }

   export async function getSeriesEpisodes(id: number): Promise<EpisodeRow[]> {
     const resp = await fetchWithLongTimeout(`/api/library/series/${id}/episodes`);
     if (!resp.ok) throw new Error(`GET /api/library/series/${id}/episodes: ${resp.status}`);
     return resp.json();
   }

   export async function postTranslate(
     kind: "series" | "movie",
     arrSeriesId: number | null,
     sourcePath: string,
   ): Promise<TranslateResponse> {
     const resp = await fetchWithLongTimeout("/api/translate", {
       method: "POST",
       headers: { "Content-Type": "application/json" },
       body: JSON.stringify({ kind, arr_series_id: arrSeriesId, source_path: sourcePath }),
     });
     if (!resp.ok) throw new Error(`POST /api/translate: ${resp.status}`);
     return resp.json();
   }

B. frontend/src/pages/Library.tsx — new page. Follow the existing page patterns (see
   Queue.tsx / History.tsx for header + table layout patterns). Dark theme tokens from BRIEF:
   bg-base #0f1117, bg-surface #1a1d27, bg-stripe #1e2130, border #2d3148,
   text-primary #e2e6f0, text-muted #6b7280, accent #3b82f6.

   State:
   - libraryData: LibraryResponse | null (loading, error states)
   - selectedSeries: LibrarySeriesItem | null (null = top-level list view)
   - episodes: EpisodeRow[] | null
   - episodesLoading: boolean
   - translatePending: string | null (source_path currently being translated)

   Top-level view:
   - Page header: "Library" h2 with text-[#e2e6f0].
   - If errors from the library response: render a small warning bar per error entry.
   - Section header "Series" if series.length > 0. Table with columns: Title | Year | Monitored.
     Rows use bg-stripe on alternate rows, hover:bg-[#1e2130]; clicking a row sets selectedSeries
     and loads episodes.
   - Section header "Movies" if movies.length > 0. Each row has the same columns plus a
     "Translate" button (accent blue, enabled only when source_sub_found=true). Clicking
     POSTs /api/translate and navigates to /queue via useNavigate().
   - Use lucide-react Film icon for movies section header, Tv icon for series section header.

   Episode drill-in (selectedSeries != null):
   - "Back" button (← text) sets selectedSeries=null.
   - Header shows selectedSeries.title.
   - Table columns: Episode | Title | Status | Action.
   - STATUS PILL: "Translated" = green bg-[#166534] text-[#4ade80]; "Has source" = blue
     bg-[#1e3a5f] text-[#60a5fa]; "No source" = grey bg-[#374151] text-[#9ca3af].
     Use a small inline span as the pill — do NOT import StatusBadge if its variants don't map
     cleanly (avoid forcing a bad mapping).
   - Translate button: enabled only when row.status === "has_source"; accent blue. Disabled
     otherwise (opacity-50 cursor-not-allowed). While translatePending === row.source_path,
     show "Queuing..." text.
   - On Translate click: call postTranslate("series", selectedSeries.id, row.source_path!),
     then navigate("/queue").

   Import useNavigate from react-router-dom. Use useEffect + useState. No external libs beyond
   what's already in the frontend (lucide-react is already installed per AppShell.tsx).

C. frontend/src/components/AppShell.tsx — add a Library nav item between the Queue and History
   items. Import Film from lucide-react (add to the existing lucide-react import line).
   Add:
     <NavItem
       to="/library"
       icon={<Film size={16} />}
       label="Library"
     />
   Place it between the Queue NavItem (to="/queue") and the History NavItem (to="/history").

D. frontend/src/App.tsx — add route for Library:
   Import Library from "./pages/Library" (add to existing import block).
   Add: <Route path="/library" element={<Library />} />
   Place it after the /history route and before the /bible route.

E. frontend/src/pages/Settings.tsx — add auto_translate_enabled toggle. Read the existing
   Settings.tsx to understand how boolean toggles are rendered (there are likely similar enable
   booleans for sonarr_enabled etc.). Add a new "Auto-translate" section or append to the
   Scheduler/Daemon section if one exists. Render:
   - Label: "Auto-translate eligible library on a schedule"
   - Warning sub-label (text-muted, text-xs): "When OFF, the daemon discovers items but never
     translates automatically. Manual translate via Library is always available."
   - A checkbox or toggle input bound to the auto_translate_enabled field, default unchecked.
   Save via the existing putSettings mechanism.

   Read Settings.tsx first before adding — locate where sonarr_enabled or enable_webhooks is
   rendered and follow the exact same pattern.

Verify frontend builds: cd frontend && npm run build must exit 0 with no TypeScript errors.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
npm run build exits 0; Library.tsx, AppShell.tsx, App.tsx, client.ts, Settings.tsx all compile
without TypeScript errors; Library route is reachable at /library.
  </done>
</task>

<task type="auto">
  <name>Task 6: Tests — new endpoints + auto_translate poller gate</name>
  <files>tests/web/test_library_api.py, tests/web/test_scheduler_auto_translate.py</files>
  <action>
Create two new test files. Follow the exact test patterns in tests/web/test_jobs_api.py and
tests/web/test_scheduler.py: no @pytest.mark.asyncio decorator needed (asyncio_mode=auto),
deferred imports inside each test body (PLC0415 pattern), ASGITransport(app=create_app()) for
route tests.

tests/web/test_library_api.py:

async def test_get_library_returns_200():
    """GET /api/library always returns 200 even when *arr disabled."""
    from trezarr.web.app import create_app
    from httpx import AsyncClient, ASGITransport
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.get("/api/library")
    assert resp.status_code == 200
    data = resp.json()
    assert "series" in data
    assert "movies" in data
    assert "errors" in data
    assert isinstance(data["series"], list)
    assert isinstance(data["movies"], list)

async def test_get_series_episodes_disabled_returns_400():
    """GET /api/library/series/{id}/episodes returns 400 when sonarr disabled."""
    from trezarr.web.app import create_app
    from httpx import AsyncClient, ASGITransport
    app = create_app()
    # Default settings have sonarr_enabled=False
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.get("/api/library/series/1/episodes")
    assert resp.status_code == 400
    data = resp.json()
    assert "error" in data

async def test_post_translate_missing_source_path_returns_400():
    """POST /api/translate without source_path returns 400."""
    from trezarr.web.app import create_app
    from httpx import AsyncClient, ASGITransport
    import json
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.post(
            "/api/translate",
            content=json.dumps({"kind": "series"}),
            headers={"Content-Type": "application/json"},
        )
    assert resp.status_code == 400
    data = resp.json()
    assert "error" in data

async def test_post_translate_nonexistent_path_returns_400():
    """POST /api/translate with a source_path that doesn't exist on disk returns 400."""
    from trezarr.web.app import create_app
    from httpx import AsyncClient, ASGITransport
    import json
    app = create_app()
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        resp = await client.post(
            "/api/translate",
            content=json.dumps({"kind": "series", "source_path": "/nonexistent/path.srt"}),
            headers={"Content-Type": "application/json"},
        )
    assert resp.status_code == 400
    data = resp.json()
    assert "error" in data

tests/web/test_scheduler_auto_translate.py:

async def test_poll_and_enqueue_skips_when_auto_translate_disabled():
    """poll_and_enqueue is a no-op when auto_translate_enabled=False (safety gate)."""
    from trezarr.web.scheduler import poll_and_enqueue
    from trezarr.config import TrezarrSettings

    settings = TrezarrSettings()
    assert settings.auto_translate_enabled is False, (
        "auto_translate_enabled must default to False (safety invariant)"
    )
    # Calling with real settings (auto_translate=False) and a non-None session_factory stub
    # must return without attempting discovery.
    # We verify this by passing a session_factory that would raise if called:
    class _RaisingSF:
        def __call__(self, *a, **kw):
            raise AssertionError("session_factory must not be called when auto_translate_enabled=False")
    await poll_and_enqueue(session_factory=_RaisingSF(), settings=settings)
    # If we reach here without AssertionError, the gate worked.

async def test_auto_translate_enabled_default_false():
    """TrezarrSettings.auto_translate_enabled defaults to False."""
    from trezarr.config import TrezarrSettings
    s = TrezarrSettings()
    assert s.auto_translate_enabled is False
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/web/test_library_api.py tests/web/test_scheduler_auto_translate.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
All tests in test_library_api.py and test_scheduler_auto_translate.py pass (no FAILED, no ERROR).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /api/translate | User-supplied source_path crosses here |
| settings PUT → config_writer | User-supplied patch dict; auto-enable logic based on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l8g-01 | Tampering | POST /api/translate source_path | mitigate | Path.exists() check rejects nonexistent paths; does not resolve symlinks or allow traversal since source_path is compared against existing disk state, not used for file creation |
| T-l8g-02 | Information Disclosure | GET /api/library movie local_path in response | accept | Library endpoint is local-network self-hosted service; same risk level as existing /api/queue which returns source_path |
| T-l8g-03 | Spoofing | Bug 3 auto-enable in config_writer | accept | Auto-enable only sets *_enabled=True when host+key are both non-empty; never disables; user must supply real credentials |
| T-l8g-SC | Tampering | npm/pip installs | accept | No new package installs; all deps already in lockfile |
</threat_model>

<verification>
Run the full test suite (no regressions):
  uv run pytest tests/ -x -q 2>&1 | tail -20

Verify frontend build clean:
  cd frontend && npm run build

Verify library route registered:
  uv run python -c "
from trezarr.web.app import create_app
app = create_app()
paths = [r.path for r in app.routes]
assert any('/library' in p for p in paths), paths
assert any('translate' in p for p in paths), paths
print('routes ok:', [p for p in paths if 'library' in p or 'translate' in p])
"

Verify auto_translate_enabled default:
  uv run python -c "from trezarr.config import TrezarrSettings; assert TrezarrSettings().auto_translate_enabled is False; print('ok')"

Verify Bug 2 fix:
  grep -c "disable_existing_loggers=False" alembic/env.py
</verification>

<success_criteria>
- GET /api/library → 200 with {series, movies, errors} always (never 500)
- GET /api/library/series/{id}/episodes → 400 when sonarr disabled; episode rows with status
  enum otherwise
- POST /api/translate → 400 when source_path absent or nonexistent; {enqueued, source_path}
  on valid request; only enqueue_job(trigger="manual") called, no synchronous translate path
- auto_translate_enabled=False by default; poll_and_enqueue returns immediately when flag is False
- Bug 3: config_writer auto-sets sonarr_enabled/radarr_enabled/bazarr_enabled=True when
  host+api_key both non-empty on save
- Bug 2: alembic/env.py has disable_existing_loggers=False
- Library page renders at /library; sidebar nav shows "Library" between Queue and History;
  episode drill-in shows status pills; Translate button navigates to /queue after enqueue
- Settings page shows auto_translate_enabled toggle defaulting to OFF with warning label
- All new tests pass; no regressions in existing test suite
- npm run build exits 0
</success_criteria>

<output>
Commit each logical group separately (stage ONLY the specific files listed per commit — NEVER git add -A):

Commit 1 — backend read endpoints:
  git add trezarr/web/routes/library.py trezarr/web/app.py
  git commit -m "feat(library): add GET /api/library + GET /api/library/series/{id}/episodes"

Commit 2 — POST /api/translate:
  (library.py already staged or re-add it)
  git commit -m "feat(library): add POST /api/translate for single-item manual enqueue"

Commit 3 — safety gate + Bug 3:
  git add trezarr/config.py trezarr/web/scheduler.py trezarr/web/config_writer.py
  git commit -m "feat(safety): auto_translate_enabled gate + auto-enable *_enabled on save (Bug 3)"

Commit 4 — Bug 2:
  git add alembic/env.py
  git commit -m "fix(alembic): disable_existing_loggers=False to preserve trezarr.* loggers"

Commit 5 — frontend:
  git add frontend/src/api/client.ts frontend/src/pages/Library.tsx frontend/src/components/AppShell.tsx frontend/src/App.tsx frontend/src/pages/Settings.tsx
  git commit -m "feat(ui): Library browser page + nav + auto_translate toggle in Settings"

Commit 6 — tests:
  git add tests/web/test_library_api.py tests/web/test_scheduler_auto_translate.py
  git commit -m "test(library): coverage for GET/POST library endpoints + auto_translate gate"

Create .planning/quick/260603-l8g-library-browser-manual-translate/260603-l8g-SUMMARY.md when done.
</output>
