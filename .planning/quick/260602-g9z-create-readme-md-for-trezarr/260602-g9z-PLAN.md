---
quick_id: 260602-g9z
slug: create-readme-md-for-trezarr
description: Create README.md for Trezarr
mode: quick
date: 2026-06-02
---

# Quick Task 260602-g9z: Create README.md for Trezarr

## Objective

Author a top-level `README.md` for the Trezarr repository — the automated Vietnamese
subtitle translator companion to the Bazarr/Sonarr/Radarr (*arr) self-hosted media stack —
then push to `origin/main`.

The README must be **accurate to the current codebase**, not aspirational. Trezarr is
mid-milestone (8 of 10 phases complete): SRT translation + the consistency engine + web UI
ship today; ASS/SSA + VTT multi-format (Phase 9) and per-series source selection (Phase 10)
are in progress and must be represented as roadmap, not finished features.

## Grounding (verified against the repo)

- `pyproject.toml` — Python 3.12, console script `trezarr = trezarr.cli:main`, dep set
  (openai, pydantic, pyarr, sqlalchemy[asyncio]+aiosqlite, alembic, fastapi, uvicorn,
  apscheduler, watchfiles, httpx, tenacity, charset-normalizer).
- `trezarr/cli.py` — two commands: `trezarr run --once` (one-shot pipeline) and
  `trezarr serve` (daemon + web UI). Pipeline: discover → scan → translate → write sidecar.
- `trezarr/` packages — arr, bible, db, discover, jobs, llm, output, paths, subtitles,
  translate, web (FastAPI app + APScheduler scheduler + worker).
- `Dockerfile` — multi-stage (node-builder → python:3.12-slim), `EXPOSE 6868`,
  healthcheck `/api/health`, `ENTRYPOINT entrypoint.sh`, `CMD trezarr serve`.
- `entrypoint.sh` — PUID/PGID drop-privilege (LSIO convention).
- `docker-compose.example.yml` + `config.yaml.example` — deployment + config reference.
- `frontend/` — React 19 + Vite 7 + Tailwind SPA ("trezarr-ui").
- `.planning/STATE.md` — 8/10 phases complete; Phase 9 (ASS/SSA+VTT) + Phase 10 in progress.
- No `LICENSE` file present (do not fabricate one).

## Tasks

### Task 1 — Write README.md
- **files:** `README.md`
- **action:** Create a comprehensive README with: one-line + paragraph description, the
  core-value pitch (cross-series pronoun/term consistency), feature list, how-it-works
  pipeline, quick start (Docker Compose + local `uv` dev), configuration pointer, project
  architecture map, project status/roadmap (phases), and an honest License note.
- **verify:** `README.md` exists at repo root; all referenced files/commands/ports match
  the repo (`trezarr run --once`, `trezarr serve`, port 6868, config.yaml.example).
- **done:** README renders as valid Markdown and contains no claims contradicted by the code
  (ASS/SSA + VTT framed as in-progress, not shipped).

## Out of scope
- Adding a LICENSE file (surfaced to the user as a follow-up decision).
- CONTRIBUTING.md, CHANGELOG.md, badges requiring external CI not yet configured.
