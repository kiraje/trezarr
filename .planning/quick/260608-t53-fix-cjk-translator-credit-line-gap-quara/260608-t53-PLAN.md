---
phase: quick-260608-t53
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/translate/test_validate.py
  - trezarr/translate/validate.py
autonomous: true
requirements:
  - fix-cjk-credit-line-gap

must_haves:
  truths:
    - "A fansub credit cue translated as 'Phụ đề dịch bởi: 虫二' (short CJK handle, credit keyword in source) passes Check 9 and does NOT raise GateError(check=9) — D-01 (exemption fires)"
    - "A real dialogue cue with CJK and no credit keyword (e.g. '你好 các bạn') STILL raises GateError(check=9) — moat preserved — D-01 (narrow guard)"
    - "A credit-keyword cue with a LONG CJK run (fully untranslated body, many CJK chars > threshold) STILL raises GateError(check=9) — D-01 (threshold guard)"
    - "A credit-keyword cue with zero CJK in the translated text passes Check 9 as before (no regression) — D-01"
    - "A non-credit cue with a short CJK handle (short enough to pass threshold alone) STILL raises GateError(check=9) — D-01 (both conditions required)"
    - "CREDIT_FANSUB_RE and CJK_CREDIT_EXEMPT_MAX_CJK constant exist as module-level names in validate.py near CJK_LEAK_RE (lines 81–193)"
    - "is_credit_fansub_cue() helper is module-level in validate.py, accepts (src_text, trn_text) -> bool, uses anchorless non-backtracking regex per ASVS L1 V5 comment style"
  artifacts:
    - path: trezarr/translate/validate.py
      provides: "CREDIT_FANSUB_RE regex + CJK_CREDIT_EXEMPT_MAX_CJK constant + is_credit_fansub_cue() helper inserted near line 193; Check 9 block (lines 481-489) gains an exemption branch"
      contains: "is_credit_fansub_cue"
    - path: tests/translate/test_validate.py
      provides: "Five new test functions for the credit-line exemption"
      contains: "test_gate_check9_credit_cue_passes"
  key_links:
    - from: "tests/translate/test_validate.py"
      to: "trezarr/translate/validate.py"
      via: "pytest.importorskip('trezarr.translate.validate')"
      pattern: "importorskip"
    - from: "validate.py Check 9 block"
      to: "is_credit_fansub_cue()"
      via: "conditional before GateError raise"
      pattern: "is_credit_fansub_cue"
---

<objective>
Exempt narrowly-detected fansub credit/attribution cues from validate.py Check 9
so a short untranslatable CJK proper-name handle (e.g. `虫二`) does not quarantine
the entire file, while keeping Check 9's CJK strictness intact for all real dialogue.

Purpose: Live incident — Job #6 quarantined a 437-cue Moon Knight file on cue 437
because the model correctly rendered the credit prefix into Vietnamese and left the
fansub handle in place. The moat (zero-false-positive CJK policy) must hold for
dialogue; the narrow credit-line case must not quarantine good files.

Output: Five new test cases (RED → GREEN) + module-level detector helper +
constant + a two-line exemption branch inside Check 9 in validate.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@trezarr/translate/validate.py
@tests/translate/test_validate.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Add five failing tests for credit-line Check 9 exemption</name>
  <files>tests/translate/test_validate.py</files>
  <behavior>
    - test_gate_check9_credit_cue_passes: source `字幕翻译：虫二`, translated `Phụ đề dịch bởi: 虫二` → validate_subdoc does NOT raise GateError(9). Uses _doc_with_bad_cue / _src_for_bad_cue helpers. Asserts result is None.
    - test_gate_check9_credit_long_cjk_still_quarantines: source carries `字幕翻译` (credit keyword), translated is a long fully-CJK line like `字幕翻译由团队制作完成感谢` (>8 CJK chars) → STILL raises GateError(check=9).
    - test_gate_check9_noncredit_short_cjk_still_quarantines: source is plain dialogue (no credit keyword, e.g. `Hello there friends`), translated carries a short 2-char CJK handle `虫二` → STILL raises GateError(check=9).
    - test_gate_check9_credit_no_cjk_passes: source `Translated by FanSubTeam`, translated `Phụ đề dịch bởi: FanSubTeam` (no CJK in translated) → does NOT raise (no GateError at all).
    - test_gate_check9_credit_english_keyword_cue_passes: source `Subtitles by: AB` (English credit keyword), translated `Phụ đề bởi: AB` (no CJK) → passes. (Confirms English credit path has no regression. No CJK means Check 9 would have passed anyway — documents the path is safe.)
  </behavior>
  <action>
    Append all five test functions after the existing `test_gate_check9_cjk_hangul_kana_leak` function (around line 449), following the established pattern exactly: `pytest.importorskip`, local `GateError`/`validate_subdoc` imports, docstring quoting the check number and audit tag, assertions with descriptive f-string messages.

    Use the existing `_doc_with_bad_cue`, `_src_for_bad_cue`, `_make_doc`, `_make_line`, `_VI_PAD`, and `_settings()` helpers — do not create new module-level helpers unless unavoidable.

    For the credit-cue tests, the source cue text must carry at least one credit keyword from the locked keyword set so the future helper will detect it. Use `字幕翻译：虫二` as the source in the primary RED test to mirror the live incident.

    For the long-CJK guard: construct a translated text with >8 CJK codepoints with a credit keyword in the source. Example: translated = `字幕翻译制作组辛苦啦谢谢` (12 CJK chars).

    For the non-credit short-CJK guard: source = `"Hello there friends"` (no credit keyword), translated = `"Đây là 虫二"` (short CJK, but source has no credit keyword).

    Run after writing to confirm all five tests FAIL (RED) with ImportError or AttributeError on `is_credit_fansub_cue`, or with GateError where no error is expected:
    `cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate.py -k "credit" -x --tb=short 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate.py -k "credit" -x --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Five new test functions exist in test_validate.py. Running pytest with `-k credit` shows all five collected and FAILING (not erroring on collection). The existing tests (`-k "not credit"`) still pass.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — Implement credit-line detector and Check 9 exemption in validate.py</name>
  <files>trezarr/translate/validate.py</files>
  <behavior>
    - CREDIT_FANSUB_RE matches any of the locked keyword set in the source or translated text (case-insensitive, anchorless, single alternation, no backtracking).
    - CJK_CREDIT_EXEMPT_MAX_CJK = 8 (module-level constant).
    - is_credit_fansub_cue(src_text, trn_text) -> bool: returns True iff CREDIT_FANSUB_RE matches src_text OR trn_text AND the total count of CJK/Hangul/Kana codepoints in trn_text is <= CJK_CREDIT_EXEMPT_MAX_CJK. Both conditions must hold.
    - Check 9 block: before the existing `raise GateError(...)` (line ~483), add: `if is_credit_fansub_cue(src_sl.text, text): continue`. The `src_sl` variable is already in scope per the existing loop at line 473.
  </behavior>
  <action>
    Insert the new constant, regex, and helper immediately after GLOSS_PAREN_RE (line 193), before the GateFailure dataclass, matching the existing comment density and naming style of the detector block (lines 81–193).

    CREDIT_FANSUB_RE keyword set (compile with re.IGNORECASE):
    Vietnamese: `dịch bởi`, `phụ đề`, `biên dịch`, `người dịch`, `hiệu đính`, `vietsub`, `dịch thuật`
    English: `subtitle`, `subtitles`, `translated by`, `translation by`, `sub by`, `subbed by`, `synced by`, `encoded by`, `ripped by`, `timing`
    Chinese: `字幕`, `翻译`, `翻譯`, `字幕组`, `字幕組`, `校对`, `校對`, `时间轴`, `時間軸`, `压制`, `壓制`, `后期`

    Use a single `re.compile(r"keyword1|keyword2|...", re.IGNORECASE)` pattern. Longer alternatives first to avoid shadowing (e.g. `translated by` before `translation`). The ASVS L1 V5 "no backtracking path" comment must be included — this is pure alternation of literal strings, so no catastrophic backtracking is possible; note it explicitly as: `# Anchorless alternation of literal strings — no backtracking path (ASVS L1 V5).`

    CJK codepoint count in is_credit_fansub_cue must use the existing CJK_LEAK_RE (already compiled) via `len(CJK_LEAK_RE.findall(trn_text))` — reuse the same character class, do not duplicate the range definition.

    Check 9 change is exactly two lines inserted before the `raise GateError(...)`:
    ```
    if is_credit_fansub_cue(src_sl.text, text):
        continue
    ```

    Do NOT alter Check 10, 11, or 12. Do NOT alter the `raw` skip at line 475. Do NOT alter any other part of validate_subdoc.

    After writing, run the full test suite to confirm GREEN:
    `cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate.py -x --tb=short 2>&1 | tail -30`
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate.py -x --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All tests in test_validate.py pass (including the five new credit-line tests AND all pre-existing moat tests). The live-incident cue `Phụ đề dịch bởi: 虫二` no longer raises GateError(9). Real dialogue CJK still quarantines. Long untranslated credit CJK still quarantines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subtitle content → Check 9 | translated cue text crosses into the exemption test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-t53-01 | Tampering | is_credit_fansub_cue() — keyword match too broad | mitigate | Both conditions required (keyword AND short CJK count ≤ 8); a long untranslated body exceeds threshold regardless of keyword match — validated by test_gate_check9_credit_long_cjk_still_quarantines |
| T-t53-02 | Tampering | CREDIT_FANSUB_RE — catastrophic backtracking | mitigate | Pure alternation of literal strings; no quantifiers/groups on variable-length content; ASVS L1 V5 note in comment; reviewed during coding |
| T-t53-03 | Elevation | Exemption applied to non-credit cue carrying crafted keyword phrase | mitigate | Source cue OR translated cue must carry keyword AND translated CJK count ≤ 8; dialogue with incidental credit-ish text is astronomically unlikely to also be <8 CJK chars and contain a credit keyword in source — test_gate_check9_noncredit_short_cjk_still_quarantines validates the no-keyword path |
</threat_model>

<verification>
Run all validate tests to confirm full suite GREEN after Task 2:

```
cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/ -x --tb=short
```

Confirm the live-incident pattern does not quarantine:
```python
# Smoke check (paste into python -c or a REPL after implementation)
from trezarr.translate.validate import is_credit_fansub_cue, CJK_LEAK_RE, CJK_CREDIT_EXEMPT_MAX_CJK
assert is_credit_fansub_cue("字幕翻译：虫二", "Phụ đề dịch bởi: 虫二") is True
assert is_credit_fansub_cue("Hello friends", "你好 các bạn") is False
assert is_credit_fansub_cue("字幕翻译制作团队", "字幕翻译制作团队制作感谢") is False  # long CJK
```
</verification>

<success_criteria>
- `python -m pytest tests/translate/test_validate.py -x` exits 0 with all tests passing, including the five new credit-line cases.
- `grep -n "is_credit_fansub_cue\|CREDIT_FANSUB_RE\|CJK_CREDIT_EXEMPT_MAX_CJK" trezarr/translate/validate.py` returns ≥ 3 lines (constant, regex, function definition, and usage in Check 9).
- Checks 1–8, 10–12 are UNMODIFIED (verify with `git diff trezarr/translate/validate.py` — only lines near 193 and 481–489 should change).
- Branch remains `fix/cjk-credit-line`; changes are committed.
</success_criteria>

<output>
Create `.planning/quick/260608-t53-fix-cjk-translator-credit-line-gap-quara/260608-t53-SUMMARY.md` when done.
</output>
