---
quick_id: 260604-hb4
slug: fix-orphan-sentinel-and-v11-warnings
created: 2026-06-04
status: complete
---

# Quick Task 260604-hb4: Orphan-sentinel + v1.1 warnings W1/W2/W3

Second tranche of the "fix every debt/bug" sweep (after 260604-gfl fixed the P0 crash-loop).

## Scope

1. **Orphan-sentinel (v1.0 finding #5)** — the weak model hallucinates `<<TN>>` tokens into
   untagged cues; the engine's `if smap:` guard skipped reinsert for empty maps so the orphan
   reached the document gate → whole-file quarantine → no `.vi.srt` ever produced.
2. **W1** — LIVE sidebar badge false-positive for a DISABLED *arr service (inferred from
   absence-of-error).
3. **W2** — `series_title` type drift (optional vs nullable).
4. **W3** — Bazarr `seriesid[]` param form not code-guaranteed across builds.

## Approach

- Orphan-sentinel: `reinsert_sentinels` strips orphans (tokens not in `sentinel_map`), computed
  BEFORE reinsertion so a real tag value containing a `<<TN>>`-shaped substring isn't deleted
  (codec-fidelity BLOCKER caught + fixed in review). Lost-real-sentinel still fails. Engine
  Pass-3/Pass-4 always call reinsert. (codec-fidelity-guardian: PASS)
- W1: `/api/library` returns positive `services` flags; badge = enabled AND no error.
- W2: `series_title: string | null`.
- W3: shared `_fetch_inventory_data` tries `<id>[]` then plain `<id>` on empty; redaction +
  path-mapping + read-only preserved; malformed-JSON wrapped. (arr-integration-specialist: PASS)

## Commits
- `730f062` orphan-sentinel
- `30d6b6b` W1/W2/W3

## must_haves
- truths:
  - hallucinated orphan <<TN>> in an untagged cue is stripped, not quarantined; lost real sentinel still fails; source-lookalike token never silently deleted
  - GET /api/library exposes positive services flags; a disabled service is not LIVE
  - SeriesEpisodesResponse.series_title is string|null
  - Bazarr inventory falls back from seriesid[]/radarrid[] to the plain key on empty
- artifacts: trezarr/translate/sentinel.py, trezarr/translate/engine.py, trezarr/arr/bazarr.py, trezarr/web/routes/library.py, frontend/src/api/client.ts, frontend/src/contexts/LibraryContext.tsx
