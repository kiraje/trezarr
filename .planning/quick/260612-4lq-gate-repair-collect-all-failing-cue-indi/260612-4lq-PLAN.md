---
phase: quick
plan: 260612-4lq
type: tdd
wave: 1
depends_on: []
files_modified:
  - trezarr/translate/validate.py
  - tests/translate/test_validate_collect_failing.py
autonomous: true
requirements:
  - gate-repair-budget-correctness
must_haves:
  truths:
    - "A 5-cue file with 3 CJK-leaking cues raises ONE GateError(check=9) with failing_indices=[i0,i1,i2] (all three, sorted)"
    - "A file with both check-9 and check-10 failures raises check-9 (highest priority) with all its failing indices"
    - "A file with only check-10 failures raises check-9-less GateError(check=10) carrying all failing indices"
    - "A file with a single leaking cue still raises GateError with failing_indices=[i] — single-element list behaviour is unchanged"
    - "All existing skip/exemption rules are preserved: raw-cue skip, empty-text skip, credit-fansub exemption (Check 9), diacritic skip (Check 10), laughter-token exemption (Check 10), length/diacritic skip (Check 11/12), allowlist handling"
    - "Full pytest suite (560 tests) remains green after implementation"
    - "Checks 1-8 and file-level check 3 are not touched — no logic changes outside the per-cue loop in validate_subdoc"
  artifacts:
    - path: "tests/translate/test_validate_collect_failing.py"
      provides: "RED tests for multi-cue accumulation; must FAIL before the fix and PASS after"
    - path: "trezarr/translate/validate.py"
      provides: "Accumulation loop replacing the fail-fast raises in the per-cue section (lines ~609-687)"
  key_links:
    - from: "validate.py per-cue loop"
      to: "engine.py repair loop"
      via: "GateError.failure.failing_indices list"
      pattern: "failing_indices=failing"
---

<objective>
Fix validate.py per-cue loop (checks 9-12) to accumulate ALL failing cue indices before raising,
instead of raising on the first hit. The gate-repair budget in engine.py is supposed to bound
REPAIR ROUNDS, not total leaking cues; the current fail-fast design makes any file with >3
leaking cues permanently un-repairable regardless of budget.

Purpose: IMP-02b (bounded gate-repair) works correctly for multi-cue leak events without
burning repair budget on one cue per attempt. Fixes the live MK E01 job-16 regression
(3 consecutive quarantines on CJK-leaking cues 221, 324, 325 — each burned one repair slot).

Output:
  - tests/translate/test_validate_collect_failing.py  (new RED→GREEN test file)
  - trezarr/translate/validate.py  (per-cue loop refactored to accumulate then raise once)

MOAT CONSTRAINTS — executor must NOT touch:
  - Checks 1-8 logic (structural + scaffold-leak checks)
  - The credit-fansub exemption internals (is_credit_fansub_cue / CJK_CREDIT_EXEMPT_MAX_CJK)
  - Check 3 file-level untranslated ratio (_check_untranslated)
  - Quarantine fallback semantics in engine.py
  - _repair_failing_cues (already handles multi-element lists correctly)
  - Any gate check ordering outside the per-cue section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@trezarr/translate/validate.py
@trezarr/translate/engine.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing tests for multi-cue accumulation</name>
  <files>tests/translate/test_validate_collect_failing.py</files>
  <behavior>
    - Test A — multi-CJK (check 9): Build a 5-cue SubDoc where cues 1, 3, and 4 (0-based indices 1, 3, 4) contain CJK characters. Call validate_subdoc(). Assert GateError is raised; assert failure.check == 9; assert sorted(failure.failing_indices) == [1, 3, 4]. Must FAIL on current code (raises after index 1 only).

    - Test B — mixed check 9 + check 10: Build a 4-cue SubDoc where cue 0 has a CJK leak (check 9) and cue 2 has a source-passthrough leak (check 10). Assert GateError(check=9) with failing_indices=[0]. Check-9 must take priority (highest-priority wins when multiple check types fire). Must FAIL on current code (raises on first cue encountered).

    - Test C — multi-check-10 only: Build a 4-cue SubDoc where cues 0 and 2 are verbatim English source passthroughs (no CJK, no VI diacritics, no laughter tokens). Assert GateError(check=10) with sorted(failing_indices) == [0, 2]. Must FAIL on current code.

    - Test D — single failing cue (regression): Build a 4-cue SubDoc where only cue 2 has a CJK leak. Assert GateError(check=9) with failing_indices == [2]. This documents that the single-element case still works after the refactor (existing tests already cover this, but an explicit regression test in this file guards the single-cue path). Must PASS both before and after — run it now to confirm (if it fails on current code there is a pre-existing bug, stop and surface it).

    - Test E — multi-check-11: Build a 4-cue SubDoc where cues 1 and 3 both match HONORIFIC_CAPNAME_RE (e.g. "Miss Mei, tạm biệt." and "Doctor Zhao đến rồi."). Assert GateError(check=11) with sorted(failing_indices) == [1, 3]. Must FAIL on current code.

    - Test F — multi-check-12: Build a 4-cue SubDoc where cues 0 and 2 both contain English-gloss parentheticals matching the possessive trigger (e.g. "Được rồi (Miss Mei's idea)." and "Tốt thôi (Han's brother)."). Assert GateError(check=12) with sorted(failing_indices) == [0, 2]. Must FAIL on current code.

    Run: `pytest tests/translate/test_validate_collect_failing.py -x -v` and confirm Tests A/B/C/E/F FAIL with "assert ... == [1, 3, 4]" (or similar) and Test D PASSES. That is the RED state.

    Helper note: reuse the _make_line / _make_doc / _settings pattern from test_gate_repair.py. Source SubDoc must have matching cue count. For check-10 passthrough tests, the source cues must contain the same ASCII tokens as the translated cues so `all_in_source` resolves True.
  </behavior>
  <action>
    Create tests/translate/test_validate_collect_failing.py with the six tests above.
    Import: validate_subdoc, GateError from trezarr.translate.validate; SubLine, SubDoc from
    trezarr.subtitles.model; TrezarrSettings from trezarr.config.
    Use asyncio_mode=auto (pyproject.toml global setting — no @pytest.mark.asyncio needed).
    validate_subdoc is synchronous — call it directly inside a regular (non-async) test function.
    Do not modify any production code in this task.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate_collect_failing.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>Tests A, B, C, E, F FAIL (AssertionError on failing_indices count/values). Test D PASSES. No import errors.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Refactor per-cue loop to accumulate failures then raise once</name>
  <files>trezarr/translate/validate.py</files>
  <behavior>
    After the fix, the six tests above all pass. All 560 pre-existing tests remain green.
    The accumulation logic follows strict priority ordering: if any check-9 failures exist,
    raise check-9 with all its indices; else if any check-11 failures exist, raise check-11;
    else if any check-12 failures exist, raise check-12; else raise check-10. (Ordering:
    9 > 11 > 12 > 10 — matches the left-to-right order of the current raises in the loop,
    which is the implied priority.)
  </behavior>
  <action>
    Edit the per-cue section of validate_subdoc in trezarr/translate/validate.py.
    The section to refactor is the for-loop at roughly line 601 that contains checks 9, 10, 11, 12.

    Replace the four in-loop `raise GateError(...)` calls with accumulators, then add a
    post-loop raise block. Concrete structure:

    1. Before the loop, declare four empty lists:
         _fail9: list[int] = []
         _fail10: list[int] = []
         _fail11: list[int] = []
         _fail12: list[int] = []

    2. Inside the loop, replace each `raise GateError(GateFailure(N, ..., failing_indices=[i]))`
       with `_failN.append(i)` and `continue` (so subsequent per-check tests within the same
       iteration are skipped when an earlier check already flagged this cue — mirror the current
       check ordering: 9 first, then 12 inner loop, then 11, then 10).

       Important: check 9 currently uses a `continue` to skip the credit-fansub exemption —
       that control flow must be preserved. After appending to _fail9, add `continue` to skip
       checks 10/11/12 for this cue (a CJK-leaking cue need not also be tested for passthrough).

       Check 12 is a nested inner loop over GLOSS_PAREN_RE.finditer(text). Replace its inner
       `raise` with `_fail12.append(i); break` — append once per cue (not once per match),
       then break the inner loop and `continue` the outer loop to skip check 11/10 for this cue.

       Check 11: replace `raise` with `_fail11.append(i); continue`.

       Check 10: replace `raise` with `_fail10.append(i)` (it is already at the end of the loop
       body, so no `continue` needed — the loop naturally proceeds to the next iteration).

    3. After the loop, add the priority-ordered post-loop block:
         if _fail9:
             raise GateError(GateFailure(9, f"CJK/Hangul/Kana script leaked into {len(_fail9)} cue(s): {sorted(_fail9)}", failing_indices=sorted(_fail9)))
         if _fail11:
             raise GateError(GateFailure(11, f"English honorific + name leaked into {len(_fail11)} cue(s): {sorted(_fail11)}", failing_indices=sorted(_fail11)))
         if _fail12:
             raise GateError(GateFailure(12, f"Gloss/parenthetical leaked into {len(_fail12)} cue(s): {sorted(_fail12)}", failing_indices=sorted(_fail12)))
         if _fail10:
             raise GateError(GateFailure(10, f"Source-language passthrough leak in {len(_fail10)} cue(s): {sorted(_fail10)}", failing_indices=sorted(_fail10)))

    Message format rationale: includes count + index list for log readability. Single-cue case
    (len=1) reads "leaked into 1 cue(s): [i]" — the check number and cue index are both present,
    which preserves the diagnostic utility of the old per-cue messages. Existing tests assert
    only on failure.check (not the message string), so message wording is safe to change.

    Do NOT modify:
      - Checks 1-8 (the non-per-cue blocks above the loop)
      - The `allowlist` assignment at line 600
      - The raw-cue skip (`if trn_sl.raw is not None: continue`)
      - The empty-text skip
      - The credit-fansub exemption call `is_credit_fansub_cue(src_sl.text, text)`
      - The VN diacritic early-continue for check 10
      - The ALLOWLIST_RE / SENTINEL_ONLY_RE skips for check 10
      - The interjection-token exemption for check 10
      - The `all_in_source` and `all...in allowlist` guards for check 10
      - _check_untranslated (check 3)
      - Any import or constant
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/translate/test_validate_collect_failing.py tests/translate/test_validate.py tests/translate/test_audit_followup.py tests/translate/test_gate_repair.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    All 6 new tests PASS (GREEN).
    All pre-existing validate/gate-repair/audit tests pass.
    Full suite: `python -m pytest --tb=short -q` shows >= 560 passed, 0 failed.
    ruff check trezarr/translate/validate.py shows no errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → gate | validate.py is the last line of defence before a translated SubDoc is written to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4lq-01 | Tampering | per-cue loop refactor | mitigate | Existing 560-test suite + 6 new RED→GREEN tests cover all accumulation paths; checks 1-8 untouched by design |
| T-4lq-02 | Denial of Service | accumulate-all pattern | accept | Accumulating N indices is O(N) with N bounded by SubDoc.lines length (typically < 2000); no ReDoS path introduced |
| T-4lq-03 | Tampering | priority ordering in post-loop block | mitigate | Test B explicitly asserts check-9 wins over check-10 when both fire; ordering matches current left-to-right loop order |
</threat_model>

<verification>
pytest tests/translate/test_validate_collect_failing.py -v  → 6 passed
pytest tests/translate/ -x -q                               → all translate tests pass
pytest --tb=short -q                                        → >= 560 passed, 0 failed
ruff check trezarr/translate/validate.py                    → no errors
</verification>

<success_criteria>
- validate_subdoc raises ONE GateError per validate_subdoc call (not one per failing cue)
- failing_indices in that GateError is the COMPLETE sorted list of all cues that triggered the highest-priority failing check
- engine.py repair loop receives all failing indices in one shot; a 3-cue CJK leak uses ONE repair attempt (budget=2 remaining after first attempt)
- 6 new tests GREEN; all 560 pre-existing tests GREEN
- No changes to checks 1-8, credit-fansub exemption, or engine.py
</success_criteria>

<output>
Create .planning/quick/260612-4lq-gate-repair-collect-all-failing-cue-indi/260612-4lq-SUMMARY.md when done.
</output>
