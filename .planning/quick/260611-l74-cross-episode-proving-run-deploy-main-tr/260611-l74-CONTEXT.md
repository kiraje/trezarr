# Quick Task 260611-l74: Cross-episode proving run - Context

**Gathered:** 2026-06-11
**Status:** Ready for planning

<domain>
## Task Boundary

Cross-episode proving run: deploy latest main image (carry-forward fix 260608-scy + CJK credit fix 260608-t53, merged to main 2026-06-11) to the server (192.168.5.42), snapshot the Ep-142 Bible baseline read-only, retry Moon Knight job 6 (live-verifies t53), then sequentially translate A Record of a Mortal's Journey to Immortality S06E19 then S06E20 on api.deepseek.com with the Ep-142 Bible carrying forward, then run the trezarr-quality Mode B cross-episode audit (Ep-142 + E19 + E20) plus a Bible diff against the baseline.

This is an OPERATIONAL task (server deploy + live runs + audit), not a code change. No source files are expected to change. Artifacts = run evidence, audit report, planning docs.

</domain>

<decisions>
## Implementation Decisions

### Protagonist name lock
- LEAVE UNLOCKED. This run proves the AUTO carry-forward path (the moat claim) with zero human help. Lock it as a production setting only AFTER the run, regardless of outcome.

### Run scope (REVISED 2026-06-11 after feasibility scout — user approved)
- Original E19+E20 scope INFEASIBLE: E20 has no English source anywhere (no sidecar, no embedded track, Bazarr providers return 0 results). Only E18/E19 mkvs have embedded eng; 6x14-17 and 6x20-28 are vie-only.
- NEW SCOPE: **Leg A** = Mortal's Journey S06E19 (one hop on the established Ep-142 Bible, xianxia register — extract embedded eng track to sidecar .en.srt first). **Leg B** = Moon Knight S01E01 retry (job 6, also live-verifies t53) → S01E02 → S01E03 (two-hop compounding proof on a fresh Bible; .zh.srt/.en.srt sources + human .vi.hi.srt gold refs on disk for all episodes).
- Sequencing: within Moon Knight, E01 DONE before E02 triggered, E02 DONE before E03. E19 independent (different series Bible).

### Execution channels (discovered during scout)
- NO direct SSH to 192.168.5.42 from this machine. Server commands go through the openclaw homelab agent: `openclaw agent --agent homelab --message "..."` (verified working; returns command output).
- Trezarr API at http://192.168.5.42:6868 reachable directly — jobs, retry, translate trigger, and full Bible reads (`/api/series/{id}/bible`) — use the API for Bible snapshots instead of sqlite.
- NAS media tree mounted locally at `/Volumes/Plex/mediaserver/data/media/` (= server's `/data/media/`) — read .vi.srt outputs directly; ffmpeg-extract E19's embedded eng/vie tracks from here.
- Current running image: ghcr.io/kiraje/trezarr:0e7ba38f053c690bda95ea86e1634ba7ca97ea09 (rollback target). New image: ghcr.io/kiraje/trezarr:9dbcb911500253dee954de60d9e423a18422fabd (CI green run 27333237821).

### Pass criteria (locked)
- Established directed pronoun pairs from the Ep-142 Bible remain identical across E19/E20 for recurring dyads with no relationship_event (no Vector 1 overwrite, no Vector 2 safe-default drop).
- Protagonist name: exactly 1 rendering across all three episodes.
- 0 scaffolding/hint leaks; validation gate clean (no quarantine).
- Result gates the deferred IMP-04 context-carry summarizer decision.

### Execution split
- Deploy + baseline snapshot + job triggers + monitoring: executable via bash/ssh/curl.
- The trezarr-quality Mode B audit MUST run in the main orchestrator session (it is a Skill that fans out specialist subagents; subagents cannot spawn subagents). The plan's final task is a checkpoint that returns control to the orchestrator for the audit.

### Safety constraints (non-negotiable, from project memory)
- NEVER write the live SQLite DB from the host while the daemon container runs. Bible baseline snapshot via `sqlite3 'file:...?mode=ro'` or the API.
- Reset/retry runs by deleting the `.vi.srt` sidecar + API trigger, or by the app's retry endpoint — never by editing the DB.
- Deploy by immutable SHA tag `ghcr.io/kiraje/trezarr:<sha>`, manual stop→pull→health→rollback procedure. Never force-push git; never `:main` floating tag for the pinned deploy.
- Endpoint is api.deepseek.com (healthy); json_object mode + raised timeout already configured in server .env — do not re-prompt for secrets.

</decisions>

<specifics>
## Specific Ideas

- CI on the merge commit of fix/cjk-credit-line into main (pushed 2026-06-11) produces the image to pin. Verify the GitHub Actions run is green and capture the SHA tag before deploy.
- Bible baseline snapshot must record, per recurring dyad: speaker, addressee, self_term, address_term, locked, valid_from_episode — plus character names and term_dictionary rows. This is the contract the post-run audit diffs against.
- Moon Knight job 6 retry expectation: cue 437 ships `Phụ đề dịch bởi: 虫二`, file NOT quarantined at Check 9.
- Job ordering on the daemon: trigger Moon Knight retry first (independent series, no Bible interaction with Mortal's Journey), then E19, wait DONE, then E20.

</specifics>

<canonical_refs>
## Canonical References

- .planning/quick/260608-scy-*/ (carry-forward fix: precedence ladder, Vector 1/2 definitions)
- .planning/quick/260608-t53-*/ (CJK credit-line exemption: live-verify expectation)
- Memory: cicd-ghcr-pipeline.md (deploy procedure), never-host-write-live-sqlite-docker-macos.md (DB safety), live-deployment-configured.md (env-first config)

</canonical_refs>
