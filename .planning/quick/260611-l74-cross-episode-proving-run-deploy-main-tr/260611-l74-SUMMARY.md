---
quick_id: 260611-l74
slug: cross-episode-proving-run-deploy-main-tr
status: complete
date: 2026-06-11
audit_verdict: BLOCK (drift found)
imp04_gate: CLOSED
---

# Summary — Cross-episode proving run (deploy + E19 + Moon Knight E01→E03 + Mode B audit)

## Outcome: run COMPLETE, audit verdict **BLOCK** — pronoun carry works in cue text, but two verified persistence regressions (R1 transition-branch bypass, R2 episode-key collision) corrupt established pairs at the data level. **IMP-04 stays CLOSED.**

## What was executed

1. **Deploy:** `ghcr.io/kiraje/trezarr:9dbcb911…` (merge of fix/cjk-credit-line; CI run 27333237821 green) deployed to 192.168.5.42 via the openclaw homelab agent (no direct SSH from this machine). Rollback target `0e7ba38…` preserved in `.env.bak-260611`. Healthy throughout.
2. **Scope revision (user-approved):** planned E19+E20 was infeasible — E20 has NO English source anywhere (no sidecar/embedded/Bazarr-providers=0). Revised: Leg A = E19 (embedded eng track extracted to sidecar, 167 cues; embedded vie kept as gold ref) + Leg B = Moon Knight E01→E02→E03 (sources on disk; human .vi.hi.srt gold refs).
3. **Runs:** E19 (job 12) done; MK E01 (job 6 retry) done — t53 class cleared; MK E02 (job 13) done on 3rd attempt — **2 quarantines at llm_max_concurrency=8** (empty line; mass Pass-4 failure + CJK leak), reverted to 4, then clean; MK E03 (job 14) done first try. All cue counts exact; gate held on every failure (no corrupt file ever shipped).
4. **t53 LIVE-VERIFIED:** E02 cue 579 `字幕翻译： 虫二` shipped via the credit exemption (≤8 CJK, source-side marker). E01's credit line was instead translated (`Trùng Nhị`) so the exemption wasn't needed there.
5. **Perf finding:** concurrency 8 degrades deepseek-v4-pro (empty/truncated responses; 11 Pass-4 batches failing in the same second). 4 = ceiling for this endpoint. ~30–55 min/episode is the endpoint's floor. Pass-4 review contributed ~nothing in these runs (43 batch failures in E01 → pre-review fallback) — candidate for a future skip/streamline task.
6. **Mode B audit (trezarr-quality):** linguist + bible-consistency-auditor fan-out → finding-verifier adversarial pass. **13 VERIFIED / 0 REFUTED / 1 UNCERTAIN.** Full report: `.trezarr-harness/CONSISTENCY-AUDIT-20260611_121500.md`.

## Audit verdict (criteria A–E)

Leg A: A FAIL · B FAIL · C PARTIAL · D PASS · E PASS — 50/53 baseline pairs identical, 3 flattened to classical safe-default via R1+R2.
Leg B: A FAIL · B PASS · C FAIL · D FAIL · E PASS — E02→E03 hop 40/40 clean; Steven→Khonshu corrupted E01→E02; names 2–4 renderings each; 3 new leak classes in E03.

**What held:** cue-text pronoun carry across ALL hops (Khonshu ta/ngươi stable ×3 episodes; all Leg-A visible dyads stable); all locks held; E02→E03 Bible hop byte-identical; ledger idempotent. The scy carry-forward works **when** the episode key is correct and no spurious event fires — the regressions are in the paths AROUND it.

## Verified blockers → fix backlog (ordered)

1. **R1** `reconcile.py:442-472`: transition branch sits above scy carry-forward; event with no suggested terms + no survivors → `get_safe_default` flattens established pairs (Mei→Han, Elder2→Zhou, Han→Mei name-drop, Steven→Khonshu). LATENT in this run's text — time bomb for E04+/E20+.
2. **R2** `engine.py:130-157`: `derive_episode_key` can't parse Plex `6x19` stems → `S00E00` → collides with cold-Bible events (all S00E00) → transition re-fires every run.
3. **R4** validate.py gate gaps: Check 10/12 diacritic skips admit `(No violation)`-class leaks; Check 6 misses unclosed `<<T153`.
4. **R3** dual competing locked name terms (Latin + Hán-Việt both locked) → Layla = 3 renderings; needs one canonical winner per entity.
5. LOW: vfe churn on transition/survivors upsert sites (7 pairs).
6. Re-run both legs + re-audit → then reconsider IMP-04.

## IMP-04 gate

**CLOSED** — "proving run executed 2026-06-11; carry verified in cue text but R1/R2 corrupt pairs at data level; re-open after R1+R2 fixed + clean re-run."

## Artifacts

Task dir: bible-baseline-s1.json, bible-post-run-s1.json, bible-post-e01/e02/e03-s18.json, e19-gold-vie.srt. Harness: CONSISTENCY-AUDIT-20260611_121500.md + _workspace/02_*/03_verdicts.md. Server: 4 new .vi.srt sidecars in the media tree.

## Deviations from plan

- E20 dropped / Moon Knight chain added (user-approved scope change after feasibility scout).
- Deploy via openclaw homelab agent instead of SSH (no key access from this machine).
- Bible snapshots via REST API instead of sqlite ro (safer; sqlite3 absent on server host anyway).
- Orchestrator executed tasks inline instead of gsd-executor (operational run: live server, multi-hour polling, checkpoint audit requiring main-session skill fan-out).
- Concurrency 8 experiment mid-run (user perf request) — reverted after evidence.
