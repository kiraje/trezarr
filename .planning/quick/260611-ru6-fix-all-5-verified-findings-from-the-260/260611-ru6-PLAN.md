---
phase: quick-260611-ru6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trezarr/translate/reconcile.py
  - trezarr/translate/engine.py
  - trezarr/translate/validate.py
  - trezarr/bible/analyze.py
  - trezarr/bible/store.py
  - tests/translate/test_reconcile.py
  - tests/translate/test_engine.py
  - tests/translate/test_validate.py
  - tests/bible/test_analyze_name_terms.py
autonomous: true
requirements: [audit-R1, audit-R2, audit-R4, audit-R3, audit-LOW]

must_haves:
  truths:
    - "R1 (D-01): _derive_transition_terms Step 3 returns the carried existing pair when transition.suggested terms are both None AND survivors is empty or missing — established dyads never fall to get_safe_default due to a term-less event"
    - "R1 (D-01): BIBLE-07 preserved — when a relationship_event fires AND survivors exist AND existing.terms are present, the transition branch still wins over plain carry-forward"
    - "R1 (D-01): lock precedence absolute — a locked pair continues to win before any transition check"
    - "R1 (D-01): truly-new dyads (no existing AddressMapDTO with non-None terms) still fall to get_safe_default correctly"
    - "R2 (D-02): derive_episode_key parses Plex NxNN stems (e.g. '- 6x19 -') and returns SxxExx normalized (e.g. S06E19)"
    - "R2 (D-02): S00E00 episode_key causes the transition branch to be SKIPPED entirely — carry-forward path is used, preventing cold-Bible event collision"
    - "R4 (D-03): cue '(Correct; tôi→anh is the right pair; no violation)' fails validate_subdoc (new ATTRIBUTION_META_RE)"
    - "R4 (D-03): cue '(No violation)' fails validate_subdoc (new ATTRIBUTION_META_RE)"
    - "R4 (D-03): cue '<<T153' (unclosed sentinel, no closing >>) fails validate_subdoc Check 6"
    - "R4 (D-03): legitimate Vietnamese parentheticals with diacritics still pass — gate is ONLY STRICTER not broader (regression-free)"
    - "R4 (D-03): t53 credit exemption unaffected — cue 'Phụ đề dịch bởi: 虫二' still exempted from Check 9"
    - "R4 (D-03): envelope preservation passing tests unchanged"
    - "R3 (D-04): second kfn auto-lock for a character that already has a locked name term adopts the canonical rendering — exactly ONE locked rendering per character entity"
    - "R3 (D-04): idempotent dedup function collapses existing dual-row data to single canonical locked rendering with audit trail"
    - "R3 (D-04): case-insensitive name matching preserved across Latin and CJK source form rows for the same character"
    - "LOW (D-05): transition branch upsert passes valid_from_episode=None when affirming an existing pair (terms unchanged, no genuine evolution) — vfe bumps only on genuine creation or genuine evolution"
    - "All ~581 existing tests continue to pass after each task"
  artifacts:
    - path: "trezarr/translate/reconcile.py"
      provides: "R1 fix in _derive_transition_terms + R1/LOW vfe fix in reconcile_attributions transition branch"
    - path: "trezarr/translate/engine.py"
      provides: "R2 NxNN parse + S00E00 guard in derive_episode_key"
    - path: "trezarr/translate/validate.py"
      provides: "R4 ATTRIBUTION_META_RE + unclosed-sentinel fix"
    - path: "trezarr/bible/analyze.py"
      provides: "R3 canonical-rendering dedup guard in plan_character_name_terms"
    - path: "trezarr/bible/store.py"
      provides: "R3 idempotent repair function dedup_canonical_name_terms"
    - path: "tests/translate/test_reconcile.py"
      provides: "R1 RED tests (Leg B reproduction + existing-pair-with-term-less-event)"
    - path: "tests/translate/test_engine.py"
      provides: "R2 RED tests (6x19 stem + S00E00 guard)"
    - path: "tests/translate/test_validate.py"
      provides: "R4 RED tests (3 verbatim audit cues)"
    - path: "tests/bible/test_analyze_name_terms.py"
      provides: "R3 RED tests (dual-locked-row adoption + dedup repair)"
  key_links:
    - from: "reconcile.py:_derive_transition_terms (line ~313)"
      to: "get_safe_default fallback (line ~319-321)"
      via: "Step 3 fallback — must return existing pair when terms are None"
      pattern: "if existing is not None and existing.self_term and existing.address_term"
    - from: "reconcile.py:reconcile_attributions transition branch (line ~442-472)"
      to: "_derive_transition_terms"
      via: "transition is not None path — must pass valid_from_episode=None when terms unchanged"
      pattern: "valid_from_episode=episode_key if.*else None"
    - from: "engine.py:derive_episode_key (line ~130-157)"
      to: "reconcile_attributions caller"
      via: "episode_key parameter — S00E00 must skip transition branch"
      pattern: "re.search.*[Nn](\\d{1,2})[xX](\\d{1,3})"
    - from: "validate.py:Check 6 (line ~498-506)"
      to: "SENTINEL_RE"
      via: "orphan sentinel — must match <<TN without closing >>"
      pattern: "<<T\\d+"
    - from: "analyze.py:plan_character_name_terms"
      to: "apply_human_edit_term"
      via: "Step 3.5 kfn auto-lock — must not create second locked row when canonical exists"
      pattern: "rendering_by_latin.*locked_sources"
---

<objective>
Fix all 5 verified blockers from the 260611-l74 cross-episode proving audit (BLOCK verdict, 13 VERIFIED / 0 REFUTED). The audit proved the pronoun carry-forward moat works at the cue-text level but is undermined by four DB-level or gate-level defects that will corrupt cross-episode output in forward episodes.

Purpose: Unblock the IMP-04 gate re-opening. After these fixes, both proving legs can re-run with correct episode keys, stable established-pair carry, clean gate, and single canonical name renderings.

Output: Five targeted fixes on branch fix/audit-l74-regressions, each with a RED commit then GREEN commit, full suite green after each GREEN commit. Final task reports new test count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260611-ru6-fix-all-5-verified-findings-from-the-260/260611-ru6-CONTEXT.md
@.trezarr-harness/CONSISTENCY-AUDIT-20260611_121500.md
@.trezarr-harness/_workspace/03_verdicts.md
@trezarr/translate/reconcile.py
@trezarr/translate/engine.py
@trezarr/translate/validate.py
@trezarr/bible/analyze.py
@trezarr/bible/store.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — R1: transition branch must carry forward established pair when event has no usable terms</name>
  <files>
    trezarr/translate/reconcile.py
    tests/translate/test_reconcile.py
  </files>
  <behavior>
    - RED test A: `_derive_transition_terms(transition_with_no_suggested_terms, survivors=[], existing=AddressMapDTO(self_term="tôi", address_term="ông"), addr_id=X, ...)` returns `("tôi", "ông")` NOT a safe-default tuple. Uses Leg B fixture: Steven(15)→Khonshu(21), transition event id=38 marker=S01E02 suggested=(None, None), existing tôi/ông from E01.
    - RED test B: `_derive_transition_terms(transition_with_no_suggested_terms, survivors=[], existing=AddressMapDTO(self_term="muội", address_term="huynh"), addr_id=Y, ...)` returns `("muội", "huynh")` — covers Leg A Mei→Han fixture.
    - RED test C (regression guard): `_derive_transition_terms(transition_WITH_suggested_terms, survivors=[...], existing=..., ...)` still returns the SUGGESTED terms — BIBLE-07 path not broken.
    - RED test D (regression guard): `_derive_transition_terms(..., existing=None, ...)` (no existing entry) falls to get_safe_default — truly-new dyad unchanged.
    - GREEN fix: In `_derive_transition_terms` (reconcile.py line ~319-321), replace Step 3 bare `get_safe_default(...)` with: if existing is not None and existing.self_term and existing.address_term: return (existing.self_term, existing.address_term); then fall to get_safe_default only for truly-new dyads.
    - The `existing` parameter is already threaded into `_derive_transition_terms` from the caller at line 443-451 — confirm it is forwarded correctly. If not, add it to the signature.
    - Do NOT change Step 1 (suggested terms win) or Step 2 (survivors + existing terms win). Only Step 3 changes.
    - Do NOT touch the lock branch (lines ~416-426) or the carry-forward else branch (lines ~526-540) — those are correct.
  </behavior>
  <action>
    RED COMMIT: Write the four tests above in `tests/translate/test_reconcile.py` under a new section `# ── R1: _derive_transition_terms Step-3 carry-forward (260611-ru6) ──`. Use the existing test file's SimpleNamespace/mock pattern for AddressMapDTO-like objects. Run `uv run pytest tests/translate/test_reconcile.py -k "r1_transition" -x` — must produce FAILURES for tests A/B (they will return a safe-default instead of the carried pair). Commit: `test(quick-260611-ru6): RED — R1 transition-branch carry-forward regression`.

    GREEN FIX: In `trezarr/translate/reconcile.py`, edit `_derive_transition_terms` (around line 319-321). The existing function signature is:
      `def _derive_transition_terms(transition, survivors, existing, addr_id, id_to_gender, settings, register=None) -> tuple[str, str]:`
    Replace the current Step 3 block:
      `# Step 3: Safe default (H3/B4: register-aware)`
      `addr_gender = id_to_gender.get(addr_id)`
      `return get_safe_default(addr_gender, settings, register=register)`
    With the four-step precedence ladder:
      `# Step 3: Carry forward the established pair (scy precedence: carried > safe-default).`
      `# A term-less event on an ESTABLISHED dyad must not flatten the existing pronoun pair`
      `# to a stranger safe-default — only truly-new dyads (no prior entry) use get_safe_default.`
      `if existing is not None and existing.self_term and existing.address_term:`
      `    return (existing.self_term, existing.address_term)`
      `# Step 4: Truly-new dyad (no prior entry or entry with None terms) → safe default.`
      `addr_gender = id_to_gender.get(addr_id)`
      `return get_safe_default(addr_gender, settings, register=register)`

    Verify the `existing` parameter is already in the `_derive_transition_terms` call at reconcile.py lines ~443-451 (it is, per the audit's code read). No caller changes needed.

    Run `uv run pytest tests/translate/test_reconcile.py -x` — all tests including the new R1 tests must pass.
    Run `uv run pytest -x -q` — full suite must pass.
    Commit: `fix(quick-260611-ru6): R1 — _derive_transition_terms Step-3 carries established pair on term-less event`.
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_reconcile.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All four R1 tests pass; full suite passes with zero regressions. Two atomic commits exist: RED then fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — R2: derive_episode_key parses NxNN Plex stems + guard S00E00 transition skip</name>
  <files>
    trezarr/translate/engine.py
    trezarr/translate/reconcile.py
    tests/translate/test_engine.py
    tests/translate/test_reconcile.py
  </files>
  <behavior>
    - RED test E (engine): `derive_episode_key(MI(season_number=None), "A Record of a Mortal's Journey to Immortality - 6x19 - Episode 143.en.srt")` returns `"S06E19"` (currently returns `"S00E00"`).
    - RED test F (engine): `derive_episode_key(MI(season_number=None), "Show - 1x5 - Ep.en.srt")` returns `"S01E05"` (1-digit season, 1-digit episode).
    - RED test G (engine): `derive_episode_key(MI(season_number=None), "Show - 12x103 - Title.en.srt")` returns `"S12E103"` (3-digit episode).
    - RED test H (reconcile): when `episode_key == "S00E00"`, reconcile does NOT enter the transition branch even when `_find_transition_for_pair` would return a match. Instead it uses the carry-forward path (or safe-default for new dyads). Test structure: call `reconcile_attributions` with `episode_key="S00E00"` + a relationship_event stamped `S00E00` + an established pair — the pair must emerge UNCHANGED.
    - GREEN fix engine: In `derive_episode_key` (engine.py line ~150), add a second regex match BEFORE the current `S(\d{2,})E(\d{2,})` match to catch `NxNN` form: `m_plex = re.search(r'(\d{1,2})[xX](\d{1,3})', stem)`. If `m_plex` fires and the standard `SxxExx` did not: return `f"S{int(m_plex.group(1)):02d}E{int(m_plex.group(2)):02d}"`. Keep the existing SxxExx regex as the primary (it takes priority if both would match on a stem that has both forms). Keep the existing fallback `S{season:02d}E00` unchanged for stems with neither form.
    - GREEN fix reconcile: In `reconcile_attributions` (reconcile.py), at the entry point of the transition check block (line ~435), add a guard: `if episode_key == "S00E00": # S00E00 is the cold-Bible default; skip transition branch to prevent cold-event re-firing.` If the guard fires, `continue` past the transition block — the pair proceeds to the survivors/carry-forward path. This is a single `if episode_key == "S00E00": pass` guard AROUND the `if getattr(settings,"enable_relationship_events",True):` block. Alternatively, skip only the transition sub-path with an `and episode_key != "S00E00"` condition on the outer transition if. The guard must be AFTER the lock check (lock still wins even on S00E00) and before the transition check.
  </behavior>
  <action>
    RED COMMIT: Write tests E/F/G in `tests/translate/test_engine.py` under `# ── R2: NxNN episode key (260611-ru6) ──`. Write test H in `tests/translate/test_reconcile.py` under `# ── R2: S00E00 transition skip (260611-ru6) ──`. Run `uv run pytest tests/translate/test_engine.py tests/translate/test_reconcile.py -k "r2" -x` — must produce FAILURES. Commit: `test(quick-260611-ru6): RED — R2 NxNN episode-key parse + S00E00 collision guard`.

    GREEN FIX ENGINE: In `trezarr/translate/engine.py`, inside `derive_episode_key`, after `stem = Path(source_sub_path).stem` and before the `m = re.search(r"S(\d{2,})E(\d{2,})", stem, re.IGNORECASE)` call, add the NxNN pre-check:
      `# R2: also match Plex "NxNN" or "N x NN" dash-separated stems (case-insensitive)`
      `m_plex = re.search(r'(\d{1,2})[xX](\d{1,3})', stem)`
    Then make the existing `if m:` block into `if m:` (unchanged) and add `elif m_plex:` as the second branch returning `f"S{int(m_plex.group(1)):02d}E{int(m_plex.group(2)):02d}"`. Place this BEFORE the fallback `season = getattr(...)` line. The `m_plex` branch must only fire when the primary `SxxExx` regex did NOT match (use the `elif`).

    GREEN FIX RECONCILE: In `trezarr/translate/reconcile.py`, in `reconcile_attributions`, locate the transition check (line ~435: `if getattr(settings, "enable_relationship_events", True):`). Change the condition to also skip S00E00: `if getattr(settings, "enable_relationship_events", True) and episode_key != "S00E00":`. Add a comment: `# R2: S00E00 is the cold-Bible default key — skip transition branch to prevent cold events re-firing on every run of the same series.`

    Run `uv run pytest tests/translate/test_engine.py tests/translate/test_reconcile.py -x -q` — all R2 tests pass.
    Run `uv run pytest -x -q` — full suite passes.
    Commit: `fix(quick-260611-ru6): R2 — derive_episode_key parses NxNN stems; skip transition on S00E00`.
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_engine.py tests/translate/test_reconcile.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Tests E/F/G/H pass. Full suite passes. Two atomic commits exist: RED then fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 — R4: close three gate leak classes (attribution meta-commentary + unclosed sentinel)</name>
  <files>
    trezarr/translate/validate.py
    tests/translate/test_validate.py
  </files>
  <behavior>
    - RED test I: `validate_subdoc` raises `GateError(check=8)` for a SubDoc whose cue text is exactly `Anh không đi cùng sao? \n (Correct; "tôi"→"anh" is the right pair; no violation)` (verbatim E03 cue 146 from 03_verdicts.md). Currently passes all 12 checks due to diacritic skip in Check 10/12.
    - RED test J: `validate_subdoc` raises `GateError(check=8)` for a SubDoc with cue text `Tôi sẽ đi. (No violation)` (verbatim E03 cue 147). Currently passes because Check 12 does not fire (no `'s`/honorific) and Check 10 is skipped (diacritics present).
    - RED test K: `validate_subdoc` raises `GateError(check=6)` for a SubDoc with cue text `<<T153` (verbatim E03 cue 459, raw bytes). Currently passes because `SENTINEL_RE` requires `>>` closing and `<<T153` has none; Check 10 is also skipped (0 ASCII word tokens >= 2 letters).
    - GREEN fix Class 1+2 (ATTRIBUTION_META_RE): Add a new regex to `validate.py` in the scaffold-leak section:
      `# R4: attribution meta-commentary that bypasses Check 10/12 diacritic skips (260611-ru6).`
      `# Anchored patterns for English review/attribution signals inside parentheses:`
      `#   "(No violation)" — Check 10/12 skipped because cue has VN diacritics.`
      `#   "(Correct; ...)" — same diacritic-skip bypass.`
      `#   "(no violation)" / "(correct;...)" — case-insensitive.`
      `# These phrases CANNOT appear in genuine Vietnamese dialogue (zero false-positive risk).`
      `ATTRIBUTION_META_RE = re.compile(r'\(\s*(?:no\s+violation|correct\s*;)', re.IGNORECASE)`
      Add `ATTRIBUTION_META_RE` as a fourth pattern in the Check 8 block (lines ~534-546 in `validate.py`), alongside the existing three: `or ATTRIBUTION_META_RE.search(sl.text)`. Update the Check 8 failure message to mention the new pattern.
    - GREEN fix Class 3 (unclosed sentinel): The existing `SENTINEL_RE = re.compile(r"<<T\d+>>")` only matches CLOSED sentinels. Check 6 iterates over translated cues checking for this. Add a second check for unclosed/orphan sentinels: `UNCLOSED_SENTINEL_RE = re.compile(r"<<T\d+(?!>>)")` — matches `<<T` followed by digits NOT followed by `>>`. Add this to Check 6's loop: if `UNCLOSED_SENTINEL_RE.search(sl.text)`: raise `GateError(GateFailure(6, ...))`. Also add the HTML-escaped form as a CONTEXT.md note only mandates the raw form, but add `r"&lt;&lt;T\d+"` as a secondary pattern (per D-03 which mentions both — use `|` alternation: `UNCLOSED_SENTINEL_RE = re.compile(r"<<T\d+(?!>>)|&lt;&lt;T\d+")`).
    - Regression tests: existing t53 credit exemption test (cue `Phụ đề dịch bởi: 虫二`) still passes; envelope-preservation tests still pass; all existing Check 6/8/10/12 pass tests unchanged.
  </behavior>
  <action>
    RED COMMIT: Write tests I/J/K in `tests/translate/test_validate.py` under a new section `# ── R4: gate leak classes (260611-ru6) ──`. For SubDoc construction follow the pattern in the existing test file (create source cue with Chinese text + translated cue with the leaked text; use mock Settings). Run `uv run pytest tests/translate/test_validate.py -k "r4_leak" -x` — must FAIL (all three pass today). Confirm cues 146/147 currently pass Check 8 (no scaffold match) and cue 459 passes Check 6 (SENTINEL_RE does not match unclosed form). Commit: `test(quick-260611-ru6): RED — R4 attribution-meta and unclosed-sentinel leak classes`.

    GREEN FIX: In `trezarr/translate/validate.py`:

    1. After the `CORRECTION_DIRECTIVE_RE` definition, add:
       `# R4 fix (Check 8): attribution meta-commentary leak — English review phrases that ride on`
       `# diacritic-bearing cues and bypass Check 10/12 diacritic skips (260611-ru6).`
       `# "(No violation)" and "(Correct; ...)" CANNOT appear in genuine Vietnamese dialogue.`
       `ATTRIBUTION_META_RE = re.compile(r'\(\s*(?:no\s+violation|correct\s*;)', re.IGNORECASE)`

    2. In Check 8's loop condition (lines ~535-539), add `or ATTRIBUTION_META_RE.search(sl.text)`.

    3. After `SENTINEL_RE` definition, add:
       `# R4 fix (Check 6): unclosed/orphan sentinel — <<TN without closing >> (260611-ru6).`
       `# Also catches HTML-escaped form &lt;&lt;TN (belt-and-suspenders; run output is raw).`
       `UNCLOSED_SENTINEL_RE = re.compile(r"<<T\d+(?!>>)|&lt;&lt;T\d+")`

    4. In the Check 6 loop (lines ~498-506), add a second condition for unclosed sentinels:
       `if SENTINEL_RE.search(sl.text) or UNCLOSED_SENTINEL_RE.search(sl.text):`
       (replace the current single `if SENTINEL_RE.search(sl.text):`)

    Run `uv run pytest tests/translate/test_validate.py -x -q` — all R4 tests pass, all existing tests pass.
    Run `uv run pytest -x -q` — full suite passes.
    Commit: `fix(quick-260611-ru6): R4 — ATTRIBUTION_META_RE (Check 8) + unclosed-sentinel (Check 6)`.
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_validate.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Tests I/J/K pass. All prior validate tests pass. Full suite passes. Two atomic commits exist: RED then fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 — R3: one canonical rendering per character entity (name terms)</name>
  <files>
    trezarr/bible/analyze.py
    trezarr/bible/store.py
    tests/bible/test_analyze_name_terms.py
  </files>
  <behavior>
    - RED test L (plan_character_name_terms): given `existing_terms` that contains a LOCKED term row for `Steven Grant` → `Steven Grant`, calling `plan_character_name_terms` with a `CharacterInference(original_latin_name="Steven Grant", original_script_name="史蒂文·格兰特", vietnamese_rendering="Sử Địch Văn")` returns an empty spec list (no new locked row should be planned for `史蒂文·格兰特` because the character already has a locked canonical rendering). Currently returns a spec for `史蒂文·格兰特` → `Sử Địch Văn`, creating a competing lock.
    - RED test M (plan_character_name_terms): given `existing_terms` with a locked row for `Steven Grant` → `Steven Grant` AND an inference `(original_latin_name="Steven Grant", original_script_name="史蒂文·格兰特", vietnamese_rendering="Sử Địch Văn")`, when the function DOES emit a spec for `史蒂文·格兰特`, the spec's `vietnamese_rendering` ADOPTS the canonical rendering (`Steven Grant`) not the inferred Hán-Việt. After the fix, test L above must pass (no spec emitted when canonical locked row already exists for the Latin name).
    - RED test N (dedup_canonical_name_terms): call the new `dedup_canonical_name_terms(session_factory, series_id=X)` function with a DB seeded with two locked rows: `Steven Grant`→`Steven Grant` (id 1, locked) and `史蒂文·格兰特`→`Sử Địch Văn · Cách Lan Đặc` (id 2, locked). After the call: row id 2 has `vietnamese_rendering == "Steven Grant"` (adopted the canonical) and a `BibleEvent` audit record exists for the change. Only ONE distinct `vietnamese_rendering` for that character group.
    - GREEN fix analyze.py: Extend `plan_character_name_terms` to track which characters already have a locked canonical rendering. Add a `locked_sources` set: for each term in `existing_terms`, if `locked_fields` contains `"vietnamese_rendering"`, record its `source_term.strip().lower()` in `locked_sources`. In the character loop, before appending a spec for the `script`/`source` form: also check whether the character's Latin name is already in `locked_sources` — if so, skip (the character already has a canonical locked row, a second locked row would compete). Use the same `.strip().lower()` normalization per the bible-name-matching contract.
    - GREEN fix store.py: Add a new public async function `dedup_canonical_name_terms(session_factory, series_id) -> list[BibleEventDTO]` that:
      1. Loads all TermDictionary rows for the series where `locked_fields` contains `"vietnamese_rendering"` (locked name terms).
      2. Groups them by their `vietnamese_rendering.strip().lower()` — rows with the same rendering are already consistent and need no action.
      3. For rows whose `vietnamese_rendering` differs but whose `source_term` maps to the same character (determined by checking whether one source_term appears as a `rendering_by_latin` key in the existing locked rows — the Latin form's rendering is canonical): rewrite the non-canonical row's `vietnamese_rendering` to the canonical value, emit a `BibleEvent(source="lock", ...)` audit record, and return all emitted events.
      4. Idempotent: a second call with the same data returns an empty list of events (nothing to repair).
      Note: TermDictionary has no character FK. The character linkage is implicit: the Latin source_term is the `original_latin_name` anchor; the script source_term maps to the same character via `rendering_by_latin` lookup in `plan_character_name_terms`. Use the same logic here: a locked Latin row's `source_term` == the character's Latin name; other locked rows whose `source_term` resolves to the same latin key (CJK or alternate spelling) are secondary and adopt the Latin row's rendering.
  </behavior>
  <action>
    RED COMMIT: If `tests/bible/test_analyze_name_terms.py` does not exist, create it. Write tests L/M/N under `# ── R3: canonical name rendering (260611-ru6) ──`. For test N use the existing async DB fixture pattern from `tests/bible/` (pytest-asyncio + in-memory SQLite). Run `uv run pytest tests/bible/test_analyze_name_terms.py -k "r3_canonical" -x` — must FAIL. Commit: `test(quick-260611-ru6): RED — R3 canonical name-term dedup (plan_character_name_terms + dedup_canonical)`.

    GREEN FIX analyze.py: In `plan_character_name_terms` (analyze.py line ~140), after building `rendering_by_latin` and `existing_sources`, add:
      `# R3: track characters that already have a locked name-term rendering (260611-ru6).`
      `# If the character's Latin name is in locked_sources, skip adding a competing locked row.`
      `locked_sources: set[str] = set()`
      `for t in existing_terms or []:`
      `    locked = getattr(t, "locked_fields", None) or []`
      `    if "vietnamese_rendering" in locked:`
      `        locked_sources.add((getattr(t, "source_term", "") or "").strip().lower())`
    In the character loop, the `if key in existing_sources or key in seen: continue` guard already prevents re-adding a source form that already has a term row. The additional guard needed: before `specs.append(...)`, check `if latin.lower() in locked_sources: continue` — this skips the CJK source form when the Latin form already has a locked row, preventing a competing locked row for the same character entity. Also: if the spec IS emitted (Latin form, not skipped), add its `latin.lower()` to `locked_sources` so subsequent CJK forms for the same character in the same batch are also skipped.

    GREEN FIX store.py: Add `dedup_canonical_name_terms` after the `upsert_term` function (around line ~900). Signature:
      `async def dedup_canonical_name_terms(session_factory: async_sessionmaker, *, series_id: int) -> list[BibleEventDTO]:`
    Logic:
      1. `async with session_factory() as session:` — query all TermDictionary rows for `series_id` where `locked_fields` contains `"vietnamese_rendering"`.
      2. Build two maps: `canonical_rendering: dict[str, str]` keyed by `source_term.strip().lower()` (Latin name → its rendering); `secondary_rows: list[TermDictionary]` for rows whose source_term is non-Latin (CJK/alternate).
      3. For each secondary row: look up the character's Latin key via `rendering_by_latin`-style logic — check if the secondary row's `vietnamese_rendering` differs from the Latin canonical; if it does, update the row inside a `session.begin()` transaction, emit a `BibleEvent`, append to results.
      4. Return `[BibleEventDTO.model_validate(e, from_attributes=True) for e in emitted_events]`.
    Export `dedup_canonical_name_terms` from the store module's public interface (add to `__all__` if present, or simply define at module scope).

    Run `uv run pytest tests/bible/test_analyze_name_terms.py -x -q` — all R3 tests pass.
    Run `uv run pytest -x -q` — full suite passes.
    Commit: `fix(quick-260611-ru6): R3 — canonical name rendering (plan_character_name_terms guard + dedup_canonical_name_terms)`.
  </action>
  <verify>
    <automated>uv run pytest tests/bible/test_analyze_name_terms.py -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Tests L/M/N pass. Full suite passes. Two atomic commits exist: RED then fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 — LOW vfe: transition upsert passes valid_from_episode=None when affirming unchanged pair + full suite report</name>
  <files>
    trezarr/translate/reconcile.py
    tests/translate/test_reconcile.py
  </files>
  <behavior>
    - RED test O: When the transition branch fires and `_derive_transition_terms` returns the CARRIED pair (i.e. terms are UNCHANGED from `existing.self_term`/`existing.address_term` — the case after the R1 fix where Step 3 carries forward), `upsert_address_pair` is called with `valid_from_episode=None` NOT `valid_from_episode=episode_key`. Test via mock/spy on `upsert_address_pair` or by observing the DB row's `valid_from_episode` after the call — it must NOT change from the prior episode value.
    - RED test P: When the transition branch fires and `_derive_transition_terms` returns terms DIFFERENT from existing (genuine evolution — suggested terms or survivor-derived terms), `upsert_address_pair` is called with `valid_from_episode=episode_key` (vfe is bumped). This is the existing correct behavior and must continue to work.
    - GREEN fix: In `reconcile_attributions` (reconcile.py lines ~452-463), after obtaining `new_self, new_addr = _derive_transition_terms(...)`, determine whether the result is an unchanged carry-forward: `is_unchanged = (existing is not None and new_self == existing.self_term and new_addr == existing.address_term)`. Pass `valid_from_episode=None if is_unchanged else episode_key` to `upsert_address_pair`. Add a comment: `# LOW fix (260611-ru6): vfe bumps only on genuine evolution; affirm-carry passes None.`
    - Regression: The existing `valid_from_episode=episode_key` behavior for genuine evolution (D-53 provenance bump) is preserved in test P.
    - After GREEN for this task: run `uv run pytest -x -q` for the full suite and report the NEW total test count in the commit message.
  </behavior>
  <action>
    RED COMMIT: Add tests O/P to `tests/translate/test_reconcile.py` under `# ── LOW: vfe stability on carry-affirm (260611-ru6) ──`. Test O requires mocking or a full async reconcile call with DB assertions — use the same async DB fixture pattern already present in `test_reconcile.py` for the full `reconcile_attributions` calls. Run `uv run pytest tests/translate/test_reconcile.py -k "vfe" -x` — must FAIL (test O: currently always passes `episode_key` as `valid_from_episode`). Commit: `test(quick-260611-ru6): RED — LOW vfe churn on carry-affirm path`.

    GREEN FIX: In `trezarr/translate/reconcile.py`, inside the transition branch (lines ~452-463), after `new_self, new_addr = _derive_transition_terms(...)` and before `resolved_map[pair] = (new_self, new_addr)`:

      `# LOW fix (260611-ru6): vfe bumps only on genuine term evolution.`
      `# When _derive_transition_terms carries the existing pair unchanged (Step 3 path after R1 fix),`
      `# passing valid_from_episode=episode_key would bump the provenance marker with no actual change.`
      `# is_genuine_evolution=True only when the terms actually changed from the existing entry.`
      `is_genuine_evolution = not (`
      `    existing is not None`
      `    and new_self == existing.self_term`
      `    and new_addr == existing.address_term`
      `)`
      `resolved_map[pair] = (new_self, new_addr)`
      `await upsert_address_pair(`
      `    session_factory,`
      `    series_id=series_id,`
      `    speaker_character_id=spk_id,`
      `    addressee_character_id=addr_id,`
      `    self_term=new_self,`
      `    address_term=new_addr,`
      `    valid_from_episode=episode_key if is_genuine_evolution else None,`
      `    episode_key=episode_key,`
      `    source="inference",`
      `)`

    Run `uv run pytest -x -q` — full suite must pass.
    Capture new test count: `uv run pytest --collect-only -q 2>&1 | tail -2`.
    Commit: `fix(quick-260611-ru6): LOW — vfe bumps only on genuine evolution in transition branch (N tests total)` (substitute actual count).

    Final verification step: run the full suite once more and confirm the test count is reported in the summary.
  </action>
  <verify>
    <automated>uv run pytest -x -q 2>&amp;1 | tail -8</automated>
  </verify>
  <done>Tests O/P pass. Full suite passes. New test count reported in commit message and here. Two atomic commits exist: RED then fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → production code | Audit snapshot data (verbatim cue text, JSON pair values) used as test fixtures; must not be injectable into production paths |
| validate.py regex → subtitle cue text | New ATTRIBUTION_META_RE and UNCLOSED_SENTINEL_RE applied to user-originated cue text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ru6-01 | Tampering | ATTRIBUTION_META_RE in validate.py | mitigate | Anchored patterns (no bare "correct" or "no" match); case-insensitive only within the parenthetical form; zero false-positive risk on genuine Vietnamese dialogue |
| T-ru6-02 | Tampering | UNCLOSED_SENTINEL_RE in validate.py | mitigate | Non-possessive look-ahead `(?!>>)` prevents matching closed sentinels; HTML-escaped form is belt-and-suspenders for proxy-transcoded output |
| T-ru6-03 | Denial of Service | new regex ReDoS | mitigate | Both new regexes are anchorless, non-recursive, no alternation with overlapping paths (ASVS L1 V5 compliant) |
| T-ru6-04 | Elevation of Privilege | dedup_canonical_name_terms writing to DB | mitigate | Series_id scoped; only rewrites locked rows whose rendering differs from established canonical; emits audit BibleEvent for every change; idempotent |
| T-ru6-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this fix — all changes are to existing modules only |
</threat_model>

<verification>
After all 5 tasks are complete:

1. Full suite: `uv run pytest -x -q` — must show all tests passing, new count >= 590 (581 baseline + ~10+ new R1/R2/R4/R3/LOW tests).
2. R1 regression probe: `uv run pytest tests/translate/test_reconcile.py -k "r1" -v` — confirm test names and PASS.
3. R2 regression probe: `uv run pytest tests/translate/test_engine.py -k "r2" -v` — confirm `S06E19`, `S01E05`, `S12E103` assertions.
4. R4 regression probe: `uv run pytest tests/translate/test_validate.py -k "r4" -v` — confirm cues 146/147/459 all fail correctly.
5. R3 regression probe: `uv run pytest tests/bible/test_analyze_name_terms.py -k "r3" -v` — confirm dedup + plan guard.
6. scy carry-forward contracts: `uv run pytest tests/translate/test_reconcile.py -k "carry" -v` — existing carry-forward tests must all pass unchanged.
7. t53 exemption: `uv run pytest tests/translate/test_validate.py -k "credit" -v` — the `虫二` exemption must still pass.
8. Envelope preservation: `uv run pytest -k "envelope" -v` — all envelope tests pass.
9. Commit log: `git log --oneline -15` — confirm 10 atomic commits (5 RED + 5 GREEN) on branch `fix/audit-l74-regressions`.
</verification>

<success_criteria>
- R1: `_derive_transition_terms` Step 3 returns carried pair for Leg-B Steven→Khonshu (tôi/ông) and Leg-A Mei→Han (muội/huynh) fixtures — no safe-default flatten.
- R2: `derive_episode_key("...6x19...")` == `"S06E19"`. Reconcile with `episode_key="S00E00"` skips transition branch for all pairs.
- R4: Three verbatim E03 cues (146 / 147 / 459) each raise GateError. Existing 12-check suite + t53 + envelope tests unchanged.
- R3: `plan_character_name_terms` emits no spec for CJK source form when Latin form already has locked row. `dedup_canonical_name_terms` collapses dual locked rows to canonical rendering with audit event.
- LOW: vfe is `None` when transition affirms an unchanged carry-forward pair; vfe is `episode_key` on genuine term change.
- All: Full suite passes. 10 atomic commits on branch (5 RED + 5 GREEN). New test count >= 590.
</success_criteria>

<output>
Create `.planning/quick/260611-ru6-fix-all-5-verified-findings-from-the-260/260611-ru6-SUMMARY.md` when done.
</output>
