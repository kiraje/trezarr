---
phase: quick-260603-mc3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trezarr/web/app.py
  - tests/web/test_spa_fallback.py
autonomous: true
requirements:
  - SPA-DEEPLINK-01
must_haves:
  truths:
    - "GET /library on a fresh browser tab returns 200 with index.html content (not a 404 JSON blob)"
    - "GET /bible/123 (nested SPA route) returns 200 with index.html content"
    - "GET /assets/does-not-exist.js returns 404 (missing assets fail loudly)"
    - "GET /api/nope returns 404 JSON (unknown API paths are not captured by SPA fallback)"
    - "GET / still returns 200 index.html (existing root behavior unchanged)"
    - "A real static file present in the dist returns its content (real files win over fallback)"
  artifacts:
    - path: "trezarr/web/app.py"
      provides: "SPAStaticFiles subclass + updated mount"
      contains: "class SPAStaticFiles"
    - path: "tests/web/test_spa_fallback.py"
      provides: "Pytest coverage for all five cases"
      exports: []
  key_links:
    - from: "trezarr/web/app.py"
      to: "fastapi.staticfiles.StaticFiles"
      via: "SPAStaticFiles subclass override of get_response()"
      pattern: "class SPAStaticFiles.*StaticFiles"
---

<objective>
Implement a selective SPA fallback so browser refresh and deep-links to client-side
React-Router routes (e.g. /library, /bible/123, /queue) return index.html instead of
FastAPI's {"detail":"Not Found"}.

Purpose: StaticFiles(html=True) only serves index.html for directory-style requests.
Extensionless deep-link paths like /library cause StaticFiles to 404, which FastAPI
renders as JSON — breaking any bookmark, refresh, or shared URL.

Output: SPAStaticFiles class in app.py + mount swap + five-case pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@trezarr/web/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SPAStaticFiles and swap the mount</name>
  <files>trezarr/web/app.py</files>
  <action>
    Add the SPAStaticFiles class and a _spa_dir escape hatch to create_app(), per the
    spec below. All edits must pass ruff lint + format.

    --- SPAStaticFiles class ---

    Place it at module level, BEFORE create_app(), after the existing module-level
    imports. Use the same deferred-import convention already used in the file for
    StaticFiles (the class must import StaticFiles at module scope since it subclasses
    it — add it alongside the existing `from fastapi.staticfiles import StaticFiles`
    reference, or move the import to module scope. Match the noqa: PLC0415 inline
    comment convention only if the import is inside a function; at module scope omit
    noqa).

    Class definition (place at module level, not inside create_app):

      from fastapi.staticfiles import StaticFiles
      from starlette.exceptions import HTTPException as StarletteHTTPException
      from starlette.responses import FileResponse
      from starlette.types import Scope

      class SPAStaticFiles(StaticFiles):
          """StaticFiles subclass that falls back to index.html for extensionless
          client-side routes, while preserving honest 404s for missing assets.

          Rules:
          - Real files → served normally (StaticFiles wins, no change).
          - Extensionless path that is NOT under /api or /webhook → 200 index.html.
          - Path with a file extension (e.g. .js, .css, .png) → 404 (asset missing,
            fail loudly so stale-build bugs surface).
          - Unknown /api/... or /webhook/... paths → 404 (exclusions required because
            these are extensionless but must NOT return the SPA shell).
          - Non-404 exceptions → re-raised unchanged.
          """

          async def get_response(self, path: str, scope: Scope):  # type: ignore[override]
              try:
                  return await super().get_response(path, scope)
              except StarletteHTTPException as exc:
                  if exc.status_code != 404:
                      raise
                  # Only fall back for extensionless paths outside /api and /webhook.
                  last_segment = path.rstrip("/").rsplit("/", 1)[-1]
                  has_extension = "." in last_segment
                  is_api = path.startswith("api/") or path == "api"
                  is_webhook = path.startswith("webhook/") or path == "webhook"
                  if has_extension or is_api or is_webhook:
                      raise
                  # Serve index.html with 200.
                  index_path = os.path.join(self.directory, "index.html")
                  if not os.path.exists(index_path):
                      raise
                  return FileResponse(index_path, media_type="text/html")

    Note on self.directory: StaticFiles stores the resolved directory as self.directory
    (a string). Verify this is accessible in the Starlette version in use; if it is
    stored differently (e.g. self.path or self.all_directories), adapt accordingly —
    the intent is to read from the same root the instance was constructed with.

    Note on path argument: Starlette passes path WITHOUT the leading slash to
    get_response (e.g. "library", not "/library"). The is_api and is_webhook checks
    must match the unslashed form — "api/..." not "/api/...". Verify this by reading
    StaticFiles.get_response in the installed Starlette source if there is any doubt.

    --- _spa_dir parameter on create_app ---

    Change the signature to:

      def create_app(
          settings: TrezarrSettings | None = None,
          _spa_dir: str | None = None,
      ) -> FastAPI:

    Update the docstring to note that _spa_dir is a test-only escape hatch for
    injecting a temporary static directory.

    In the body, replace:

      _static_dir = os.path.join(os.path.dirname(__file__), "static")

    with:

      _static_dir = _spa_dir if _spa_dir is not None else os.path.join(
          os.path.dirname(__file__), "static"
      )

    --- Mount swap ---

    Replace the existing mount line:

      app.mount("/", StaticFiles(directory=_static_dir, html=True), name="spa")

    with:

      app.mount("/", SPAStaticFiles(directory=_static_dir, html=True), name="spa")

    Keep html=True so the existing "/" → index.html and directory-index behavior is
    unchanged. Keep the surrounding if os.path.exists(_static_dir): guard and the
    else-branch logger.info call exactly as they are. Keep the mount as the LAST
    registration in create_app (Pitfall-E invariant).

    Do NOT touch any router registrations, the webhook route, or any other part of
    create_app.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && ruff check trezarr/web/app.py && ruff format --check trezarr/web/app.py</automated>
  </verify>
  <done>
    ruff reports no errors on app.py. SPAStaticFiles class exists at module level.
    create_app accepts _spa_dir parameter. Mount line uses SPAStaticFiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pytest coverage for SPAStaticFiles (five cases)</name>
  <files>tests/web/test_spa_fallback.py</files>
  <behavior>
    - Case 1: GET /library → 200, body == index.html sentinel content
    - Case 2: GET /bible/123 → 200, body == index.html sentinel content
    - Case 3: GET /assets/does-not-exist.js → 404 (not index.html)
    - Case 4: GET /api/nope → 404, Content-Type is application/json
    - Case 5: GET / → 200, body == index.html sentinel content
    - Case 6: GET /asset.js (file exists in tmp dir) → 200, body == file content
  </behavior>
  <action>
    Create tests/web/test_spa_fallback.py following the exact style of
    tests/web/test_library_api.py: deferred imports inside test bodies with
    # noqa: PLC0415, asyncio_mode="auto" (no @pytest.mark.asyncio decorator needed),
    AsyncClient + ASGITransport, create_app() calls.

    Shared setup pattern: define a module-level pytest fixture (or helper) that:
    1. Accepts tmp_path (pytest built-in).
    2. Creates a minimal static directory: writes tmp_path/index.html with a known
       sentinel string (e.g. "<!doctype html><title>SPA</title>"), and writes
       tmp_path/asset.js with content "console.log('ok');".
    3. Returns str(tmp_path).

    Each test calls create_app(_spa_dir=<tmp_static_dir>) to get an app wired to the
    temp static dir, then uses AsyncClient + ASGITransport for requests.

    Test functions (async def, no decorator):

      async def test_spa_deep_link_library(tmp_path):
          # Creates tmp static dir, GET /library → 200 + sentinel in body

      async def test_spa_nested_route_bible(tmp_path):
          # GET /bible/123 → 200 + sentinel in body

      async def test_missing_asset_with_extension_returns_404(tmp_path):
          # GET /assets/does-not-exist.js → 404
          # Assert response body does NOT contain the sentinel (not index.html)

      async def test_unknown_api_path_returns_404_json(tmp_path):
          # GET /api/nope → 404
          # Assert Content-Type contains "application/json"
          # Assert body does NOT contain the sentinel

      async def test_root_still_serves_index(tmp_path):
          # GET / → 200 + sentinel (regression: existing root behavior)

      async def test_real_static_file_served_directly(tmp_path):
          # GET /asset.js → 200, body == "console.log('ok');"

    Each test is self-contained (creates its own app instance). Do NOT share a single
    app fixture across tests (matches the existing test style in the repo).

    Add a module docstring explaining what is tested and noting that asyncio_mode="auto"
    is configured project-wide.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/web/test_spa_fallback.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All six test cases pass. ruff check tests/web/test_spa_fallback.py clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → StaticFiles mount | Untrusted path segment influences which file is served |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mc3-01 | Information Disclosure | SPAStaticFiles.get_response | mitigate | Extension check prevents serving index.html for missing assets; /api and /webhook exclusions prevent leaking SPA shell for unknown API paths |
| T-mc3-02 | Tampering | Path traversal via crafted path | accept | Starlette's StaticFiles parent already resolves and validates paths against the static directory root before get_response is called — path traversal is handled upstream |
| T-mc3-SC | Tampering | npm/pip installs | accept | No new packages introduced; subclasses existing installed Starlette |
</threat_model>

<verification>
Run full test suite to confirm no regressions:

  cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && python -m pytest tests/web/ -v

Confirm ruff clean on both modified files:

  ruff check trezarr/web/app.py tests/web/test_spa_fallback.py
  ruff format --check trezarr/web/app.py tests/web/test_spa_fallback.py

Manual smoke (optional, if dev server running):
  curl -s -o /dev/null -w "%{http_code}" http://localhost:6868/library   # expect 200
  curl -s -o /dev/null -w "%{http_code}" http://localhost:6868/bible/1   # expect 200
  curl -s -o /dev/null -w "%{http_code}" http://localhost:6868/assets/missing.js  # expect 404
</verification>

<success_criteria>
- SPAStaticFiles class exists in trezarr/web/app.py, subclasses StaticFiles
- Mount in create_app uses SPAStaticFiles (not StaticFiles directly)
- create_app signature accepts _spa_dir: str | None = None
- Six pytest cases all pass in tests/web/test_spa_fallback.py
- ruff clean on both files
- Pitfall-E mount ordering preserved (mount still last in create_app)
- No changes to any router, webhook route, or lifespan logic
</success_criteria>

<output>
Create .planning/quick/260603-mc3-selective-spa-fallback-for-deep-link-ref/260603-mc3-SUMMARY.md when done.
</output>
