---
phase: 11-shadcn-foundation-purple-theme
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - frontend/src/components/ui/sidebar.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/accordion.tsx
  - frontend/src/components/ui/collapsible.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/package.json
  - frontend/package-lock.json
autonomous: false
requirements:
  - UI-01
  - UI-02

user_setup: []

must_haves:
  truths:
    - "All 11 shadcn components exist under src/components/ui/ as vendor code, never hand-edited (bulk-install — D-06; vendor policy — D-07)"
    - "jolly-ui table.tsx is installed from https://jollyui.dev/r after a passing view-gate scan (D-06)"
    - "npm run build exits 0 with all 12 component files scanned by Tailwind"
    - "animate-in appears in the built CSS (proves tailwindcss-animate plugin is wired)"
    - "Human visual UAT confirms: dark purple background, purple Button, bg-primary/50 half-opacity (autonomous:false checkpoint — --auto chain HOLDS — D-09)"
    - "All existing pages (/queue, /history, /settings, /bible) load without broken styling"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "shadcn Button primitive (variant=default renders purple via --primary)"
      contains: "buttonVariants"
    - path: "frontend/src/components/ui/sidebar.tsx"
      provides: "shadcn Sidebar primitive (used Phase 12+)"
      contains: "SidebarProvider"
    - path: "frontend/src/components/ui/table.tsx"
      provides: "jolly-ui React Aria Table (used Phase 14)"
      contains: "Table"
  key_links:
    - from: "frontend/src/components/ui/button.tsx"
      to: "frontend/src/index.css"
      via: "bg-primary maps to hsl(var(--primary)) which resolves to 270 70% 60%"
      pattern: "bg-primary"
    - from: "frontend/src/components/ui/table.tsx"
      to: "react-aria-components"
      via: "jolly-ui Table wraps react-aria Table"
      pattern: "react-aria-components"
---

<objective>
Bulk-install the 11 official shadcn components and the jolly-ui Table via registry URL.
The jolly-ui view-gate scan is a BLOCKING step that must complete (clean result) before
running the add command. After install, run a final build gate and hold for human visual
UAT (D-09 gate) to confirm the three required browser proofs.

Purpose: Complete the vendor component layer (D-06) so all downstream phases (12–15) can
import ready primitives. The human UAT checkpoint (D-09) is the phase-close gate — the
--auto chain HOLDS here per CONTEXT.md D-09.

Output: 12 vendor component files under src/components/ui/; final green build;
human-confirmed visual UAT (dark purple background, purple Button, bg-primary/50 opacity proof).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-RESEARCH.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-PATTERNS.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-CONTEXT.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-UI-SPEC.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-VALIDATION.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-install shadcn components and jolly-ui Table (blocking view-gate first)</name>
  <files>
    frontend/src/components/ui/sidebar.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/tabs.tsx
    frontend/src/components/ui/accordion.tsx
    frontend/src/components/ui/collapsible.tsx
    frontend/src/components/ui/tooltip.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/sonner.tsx
    frontend/src/components/ui/dropdown-menu.tsx
    frontend/src/components/ui/table.tsx
    frontend/package.json
    frontend/package-lock.json
  </files>
  <read_first>
    - RESEARCH.md Step 10 (bulk shadcn add command — exact component list)
    - RESEARCH.md Step 11 (jolly-ui add table — exact REGISTRY_URL command)
    - PATTERNS.md §src/components/ui/* (vendor policy, jolly-ui view-gate requirements)
    - UI-SPEC.md §Registry Safety (jolly-ui view-gate: scan for fetch/XMLHttpRequest/eval/Function/process.env/dynamic external imports)
    - CONTEXT.md D-06 (bulk install in one clean foundation commit), D-07 (vendor policy — never hand-edit)
    - RESEARCH.md Pitfall 7 (legacy-peer-deps must already be in .npmrc — it is, from Plan 01)
    - RESEARCH.md Assumption A3 (jolly-ui registry availability; defer to Phase 14 if unavailable)
  </read_first>
  <action>
    Working directory: /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend

    Step 10 — Bulk-install 11 shadcn components (D-06, RESEARCH.md Step 10):
    Run: npx shadcn@2.10.0 add sidebar button badge card tabs accordion collapsible tooltip skeleton sonner dropdown-menu

    All 11 components land in src/components/ui/ as vendor code. If any individual component
    fails, run them separately to isolate (Assumption A2). Do NOT hand-edit the generated files.
    The .npmrc with legacy-peer-deps=true from Plan 01 is already in place — this prevents ERESOLVE.

    Step 11A — [BLOCKING] jolly-ui view-gate scan BEFORE adding table (UI-SPEC.md §Registry Safety):

    Run the view command to fetch the current registry source:
    REGISTRY_URL=https://jollyui.dev/r npx shadcn@latest view table 2>/dev/null

    Scan the output for ALL of the following suspicious patterns:
    - Network calls: fetch(, XMLHttpRequest, navigator.sendBeacon
    - Environment access: process.env
    - Dynamic execution: eval(, Function(, new Function(
    - External dynamic imports: import( followed by a URL that is not relative
    - Obfuscated variable names in non-minified source

    IF ANY flag is found: display the flagged lines and halt — present to developer for
    explicit approval before proceeding. Do NOT run `add table` without human sign-off.

    IF NO flag is found: proceed to Step 11B immediately.

    Step 11B — Install jolly-ui Table (only after passing view-gate):
    Run: REGISTRY_URL=https://jollyui.dev/r npx shadcn@latest add table

    Note: shadcn@latest (v4 CLI) is safe for the `add` command — it copies component source
    files only and does not touch tailwind.config.js or re-run init. The @2.10.0 pin is only
    required for `init`. (RESEARCH.md Step 11 verification)

    If jollyui.dev is unreachable (Assumption A3 fallback): log "jolly-ui registry unreachable —
    deferring table install to Phase 14 per CONTEXT.md deferred ideas". Proceed without table.tsx
    and note the deferral in the plan SUMMARY.

    Step 12 — Final build gate (VALIDATION.md sampling requirement):
    Run: npm run build

    Then verify animation class presence:
    Run: grep -l "animate-in" dist/assets/*.css
    Must match at least one file. If no match after components are installed:
    tailwindcss-animate may not be in plugins (check tailwind.config.js) or the
    accordion/other animation-using components are purged (check content globs in tailwind.config.js).
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && for f in button badge card tabs accordion collapsible tooltip skeleton sonner dropdown-menu sidebar; do test -f "src/components/ui/$f.tsx" || echo "MISSING: $f.tsx"; done && echo "component check done"</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && test -f src/components/ui/table.tsx && echo "table.tsx present" || echo "table.tsx absent (deferred if registry unreachable)"</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -rl "animate-in" dist/assets/ 2>/dev/null | head -1 && echo "animate-in found in CSS" || echo "WARN: animate-in not found — check tailwindcss-animate plugin"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 official shadcn component files exist under src/components/ui/ (sidebar, button, badge, card, tabs, accordion, collapsible, tooltip, skeleton, sonner, dropdown-menu)
    - jolly-ui view-gate scan completed with no suspicious patterns flagged before `add table` ran
    - src/components/ui/table.tsx exists (or deferral is explicitly documented if registry unreachable)
    - No file under src/components/ui/ was hand-edited — all are CLI-generated (D-07)
    - `npm run build` exits 0
    - `grep -rl "animate-in" dist/assets/` finds at least one CSS file
    - trezarr/web/static/ is populated with the final build output
  </acceptance_criteria>
  <done>All 11 shadcn components + jolly-ui table installed as vendor code; jolly-ui view-gate
passed (no suspicious patterns); final build exits 0 with animate-in in CSS.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: D-09 Human Visual UAT — Confirm purple theme, Button, and opacity proof</name>
  <what-built>
    The full Phase 11 shadcn foundation is installed:
    - .npmrc + 6 runtime deps + @types/node installed
    - @/ alias wired in vite.config.ts and tsconfig.json
    - components.json + src/lib/utils.ts (cn()) written by shadcn CLI
    - tailwind.config.js: purple CSS-var token map + bridge tokens + darkMode: ["class"] + tailwindcss-animate
    - src/index.css: 28 HSL variables with --primary: 270 70% 60% as bare channel triples
    - index.html: class="dark" on html element + body background-color: hsl(270, 24%, 7%)
    - 11 shadcn components + jolly-ui table in src/components/ui/ (vendor code)
    - Build is green

    To show the visual proof: the Button component is imported into one existing page (e.g., AppShell.tsx
    or any convenient route) temporarily — `import { Button } from "@/components/ui/button"` — and
    a test element is placed in the JSX:
    ```
    <Button variant="default" className="m-4">Test Purple Button</Button>
    <div className="bg-primary/50 w-16 h-16 m-4 rounded fixed top-4 right-4" />
    ```
    After visual confirmation these test elements are removed.
  </what-built>
  <how-to-verify>
    Start the dev server: cd frontend && npm run dev (or open the production build at :6868)

    Then verify all four D-09 criteria:

    1. DARK PURPLE BACKGROUND:
       Open DevTools → Elements. Inspect the <html> element.
       Confirm: class="dark" attribute is present.
       Open DevTools → Computed Styles on <body>.
       Confirm: background-color resolves to approximately hsl(270, 24%, 7%) — a dark purple,
       NOT the old blue-black #0f1117. The purple tint should be faintly visible.

    2. PURPLE BUTTON:
       A <Button variant="default"> must render with a purple background matching ~#8b5cf6
       (approximately hsl(270, 70%, 60%)). The button background should be distinctly purple,
       not blue, not grey.

    3. OPACITY MODIFIER PROOF (the channel-triple format validation):
       The test div with bg-primary/50 must render as a SEMI-TRANSPARENT purple square
       (half-opacity, not full-opacity, not invisible).
       DevTools computed color for this element should show a valid rgb() or color-mix()
       value with approximately 50% alpha.
       Full opacity = hsl() wrapper is in index.css (Pitfall 1 — fix immediately).

    4. NO BROKEN PAGES:
       Navigate to /queue, /history, /settings, /bible.
       Each page must load and display its content. No all-white or all-unstyled pages.
       Existing layout, panels, and text should remain unchanged from before Phase 11.
       (Bridge tokens carry the existing page styling — bg-base, bg-surface, text-primary, text-muted.)

    All four criteria must pass before Phase 11 closes. If criteria 3 fails (full opacity),
    the fix is: remove hsl() from the CSS variable values in src/index.css. Bare channel-triple
    only (e.g., --primary: 270 70% 60%, not --primary: hsl(270 70% 60%)).
  </how-to-verify>
  <resume-signal>
    Type "approved" when all four D-09 visual criteria pass.
    If any criterion fails, describe which one and the observed behavior so the issue can be diagnosed.
    Note: the exact purple shade may be tuned now if desired — tell Claude the target H/S/L values
    and they will be updated in src/index.css and index.html before the plan closes.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jolly-ui registry (https://jollyui.dev/r) → src/components/ui/table.tsx | Third-party copy-paste registry writes source file into the codebase |
| shadcn CLI (npx shadcn@2.10.0 add) → src/components/ui/*.tsx | Official shadcn registry writes 11 vendor source files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | jolly-ui registry add table | mitigate | BLOCKING view-gate: run `REGISTRY_URL=https://jollyui.dev/r npx shadcn@latest view table` and scan for fetch/XMLHttpRequest/eval/Function/process.env/external imports BEFORE running add — if any flag found, halt for developer approval |
| T-11-08 | Tampering | shadcn@2.10.0 add (11 components) | mitigate | Official registry pinned to 2.10.0; Radix UI primitives pulled automatically are well-established packages (millions weekly downloads); lockfile committed |
| T-11-09 | Spoofing | jolly-ui registry availability | accept | If jollyui.dev/r is unreachable, defer table to Phase 14 per CONTEXT.md deferred ideas — no security risk from deferral |
| T-11-10 | Tampering | src/components/ui/* vendor files | mitigate | D-07 vendor policy: never hand-edit; CLI is the only permitted writer; deviations cause drift from shadcn baseline |
| T-11-SC | Tampering | npm/npx installs | mitigate | slopcheck completed in RESEARCH.md for all packages; jolly-ui view-gate is the blocking human checkpoint for the third-party registry source; lockfile committed after install |
</threat_model>

<verification>
After completing both tasks (including human sign-off):

1. `ls frontend/src/components/ui/` — must list all 11+ component files
2. `cd frontend && npm run build` — must exit 0
3. `grep -rl "animate-in" frontend/dist/assets/` — must match at least one CSS file
4. Human UAT confirmed via "approved" signal:
   - dark purple background with class="dark" active
   - Button variant="default" renders purple
   - bg-primary/50 renders at half opacity (proves channel-triple format)
   - All existing pages render without broken styling
</verification>

<success_criteria>
- src/components/ui/ has all 11 shadcn components + jolly-ui table (or table deferred with documentation)
- jolly-ui view-gate scan completed; no suspicious patterns found before add ran
- npm run build exits 0; trezarr/web/static/ populated; animate-in in built CSS
- Human confirmed: dark purple background, purple Button, bg-primary/50 half-opacity, no broken pages
- Phase 11 shadcn foundation is complete and ready for Phase 12 shell work
</success_criteria>

<output>
Create `.planning/phases/11-shadcn-foundation-purple-theme/11-03-SUMMARY.md` when done
</output>

## Decision Traceability

CONTEXT.md decisions implemented by this plan (colon-form citations for the coverage gate):

- D-06: bulk-install the full shadcn component set plus the jolly-ui Table via registry URL (`add` may use `@latest`).
- D-07: `src/components/ui/` treated as vendor code (never hand-edited); install respects `.npmrc` legacy-peer-deps; no `--force`.
- D-09: phase ends with a human visual-UAT checkpoint (`autonomous: false`) — dark purple background + purple `<Button>` + `bg-primary/50` half-opacity; the `--auto` chain HOLDS here for human sign-off.
