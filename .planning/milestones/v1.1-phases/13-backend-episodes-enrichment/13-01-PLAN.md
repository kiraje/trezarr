---
phase: 13-backend-episodes-enrichment
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/web/test_library_api.py
  - tests/translate/test_engine.py
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "D-01 (per ARCHITECTURE.md §4): 8 new test stubs covering the season-grouped envelope shape exist in test_library_api.py and run as xfail (RED) before implementation"
    - "D-02 (per ARCHITECTURE.md §4): test_get_series_episodes_bazarr_join_key stub asserts Bazarr inventory joins on episode.id (sonarrEpisodeId), not episodeFile.id"
    - "D-03 (episode_key from ints): test_get_series_episodes_envelope_shape stub asserts episode_key field exists per episode (will verify format S{N:02d}E{N:02d} after implementation)"
    - "D-04 (total_count from statistics.episodeFileCount): test_get_library_series_count_fields stub asserts translated_count and total_count integer fields exist on series list items"
    - "D-05 (bulk translated_counts_for_series): test_translated_counts_for_series stub asserts the helper returns a dict keyed by str(series_id)"
    - "D-06 (CRITICAL — series_id population fix prerequisite): test_ledger_records_series_id_on_success stub exists in test_engine.py and runs RED before the engine fix"
    - "D-07 (audio normalization): test_audio_language_normalization stub asserts _normalize_audio_languages maps 'Korean/English' to ['ko','en']"
    - "D-08 (fail-soft contract): test_get_series_episodes_bazarr_disabled stub asserts HTTP 200 + bazarr_available=False + errors=[] when bazarr is disabled; test_get_series_episodes_bazarr_error stub asserts HTTP 200 + bazarr_available=False + errors entry when bazarr enabled but unreachable"
    - "API-01 (enriched episodes endpoint): all 5 endpoint test stubs exist (envelope shape, Bazarr disabled, Bazarr error, join key, audio normalization)"
    - "API-02 (counts on list endpoints): test_get_library_series_count_fields and test_translated_counts_for_series stubs exist"
    - "The 4 existing test_library_api.py tests (test_get_library_returns_200, test_get_series_episodes_disabled_returns_400, test_post_translate_missing_source_path_returns_400, test_post_translate_nonexistent_path_returns_400) remain GREEN throughout Wave 0"
    - "All new test stubs are xfail (strict=False) and are collected without error by pytest (no import failures at collection time)"
  artifacts:
    - path: "tests/web/test_library_api.py"
      provides: "8 new xfail stubs for API-01/API-02 enriched episodes endpoint and count fields"
      contains: "test_get_series_episodes_bazarr_disabled"
    - path: "tests/translate/test_engine.py"
      provides: "1 new xfail stub for D-06 series_id engine fix"
      contains: "test_ledger_records_series_id_on_success"
  key_links:
    - from: "tests/web/test_library_api.py::test_get_series_episodes_bazarr_join_key"
      to: "BazarrInventoryItem.arr_id == episode.id"
      via: "xfail stub documents the required join key"
      pattern: "episode\\.id"
    - from: "tests/translate/test_engine.py::test_ledger_records_series_id_on_success"
      to: "LedgerEntry.series_id"
      via: "xfail stub documents D-06 requirement"
      pattern: "series_id.*str.*arr_series_id"
---

<objective>
Wave 0 RED test scaffold for Phase 13 backend enrichment.

Purpose: Write all failing tests first — 8 new stubs in test_library_api.py and 1 stub in test_engine.py — so subsequent implementation waves can drive to GREEN. This also encodes the API contract (season-grouped envelope, fail-soft Bazarr, join key correctness, audio normalization, bulk count query) into executable assertions before a single line of implementation exists.

Output: Both test files with stubs; pytest collects all stubs without error; existing 4 tests remain green; all 9 new stubs are xfail (RED). The D-06 engine test stub lands in Wave 0 because D-06 ordering is load-bearing (series_id fix must precede translated_count work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-backend-episodes-enrichment/13-CONTEXT.md
@.planning/phases/13-backend-episodes-enrichment/13-RESEARCH.md
@.planning/phases/13-backend-episodes-enrichment/13-PATTERNS.md
@.planning/phases/13-backend-episodes-enrichment/13-VALIDATION.md
@tests/web/test_library_api.py
@tests/translate/test_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append 8 RED xfail stubs to test_library_api.py</name>
  <files>tests/web/test_library_api.py</files>
  <read_first>
    Read tests/web/test_library_api.py (the 4 existing tests) to learn the exact pattern before adding stubs. Key patterns: asyncio_mode="auto" (no @pytest.mark.asyncio), deferred imports with # noqa: PLC0415, AsyncClient + ASGITransport(app=app), create_app() with default sonarr_enabled=False/bazarr_enabled=False. Also read 13-PATTERNS.md §"tests/web/test_library_api.py" for the delta table of all 8 test names and what each must assert.
  </read_first>
  <behavior>
    - test_get_series_episodes_bazarr_disabled: asserts HTTP 200 + bazarr_available=False + errors=[] (empty list, no entry) when bazarr is disabled per D-08. Sonarr must be mocked (enabled+working) so the endpoint doesn't 400 before reaching the Bazarr branch.
    - test_get_series_episodes_bazarr_error: asserts HTTP 200 + bazarr_available=False + errors contains one entry with source="bazarr" when Bazarr is enabled but BazarrError is raised per D-08.
    - test_get_series_episodes_bazarr_join_key: asserts that when Bazarr inventory has arr_id matching episode.id (NOT episodeFile.id), the subtitle appears in the correct episode's subtitles list per D-02. Mocks both episode.get and episode_file.get with distinct IDs.
    - test_audio_language_normalization: unit test of the _normalize_audio_languages helper directly (or by calling the endpoint with mock data) — asserts "Korean/English" → ["ko","en"] and an unknown name falls back to its lowercased original per D-07.
    - test_get_series_episodes_envelope_shape: asserts response has series_id, bazarr_available, seasons list, each season has season_number and episodes list, each episode has episode_id, episode_key, audio_languages, subtitles fields per D-01/D-03.
    - test_get_library_series_count_fields: asserts each series item in GET /api/library response has translated_count (int) and total_count (int) fields per D-04/D-05/API-02.
    - test_translated_counts_for_series: unit test of translated_counts_for_series(session_factory, ids) with an in-memory SQLite DB containing mock ProcessedFile rows; asserts it returns {str(series_id): n} per D-05.
    - test_get_library_movies_count_fields: asserts each movie item has translated_count and total_count integer fields per D-04/API-02.
  </behavior>
  <action>
    Append 8 async test functions to tests/web/test_library_api.py. All 8 use @pytest.mark.xfail(strict=False) with a reason string referencing the Phase-13 plan that implements them. All follow the existing conventions: async def, deferred imports, # noqa: PLC0415, no @pytest.mark.asyncio decorator.

    For tests requiring Sonarr mocking: patch "trezarr.arr.sonarr.build_sonarr_client" to return a MagicMock whose episode.get and episode_file.get attributes are MagicMock instances returning minimal list payloads. Inject enabled settings via app.state.settings after create_app().

    For test_get_series_episodes_bazarr_disabled: set settings.sonarr_enabled=True, settings.bazarr_enabled=False. Mock build_sonarr_client so episode.get returns a minimal list (one episode dict with id, seasonNumber, episodeNumber, hasFile=False). Assert status_code==200, data["bazarr_available"]==False, data["errors"]==[].

    For test_get_series_episodes_bazarr_error: set sonarr_enabled=True, bazarr_enabled=True. Mock build_sonarr_client. Patch "trezarr.arr.bazarr.BazarrClient.from_settings" to raise BazarrError("timeout"). Assert status_code==200, bazarr_available==False, errors[0]["source"]=="bazarr".

    For test_get_series_episodes_bazarr_join_key: create two mock episodes: ep_id=10 (the target) and ep_id=11. episodeFile id=99. Bazarr inventory has arr_id=10 (not 99). Assert the episode with episode_id==10 has non-empty subtitles; episode with episode_id==11 has subtitles=[].

    For test_audio_language_normalization: import _normalize_audio_languages from trezarr.web.routes.library (it will be a module-level helper after implementation). Call it with "Korean/English" and assert result == ["ko","en"]. Also call with "Klingon" and assert result == ["klingon"] (lowercased fallback).

    For test_get_series_episodes_envelope_shape: mock Sonarr with 2 episodes in season 1. Assert response keys: series_id (int), bazarr_available (bool), seasons (list), errors (list). Assert seasons[0]["season_number"]==1, seasons[0]["episodes"] is a list, episodes[0] has episode_id, episode_key, audio_languages, subtitles fields.

    For test_get_library_series_count_fields: use create_app() with default settings (sonarr_enabled=False is fine — series list will be empty). GET /api/library. Assert data["series"] is list (may be empty). The test is xfail for now; after implementation, series items will have translated_count and total_count.

    For test_translated_counts_for_series: use aiosqlite in-memory + SQLAlchemy async to create a minimal ProcessedFile table. Insert 2 rows with series_id="42" status="done" and 1 row series_id="99" status="done". Call translated_counts_for_series(session_factory, [42, 99, 7]). Assert result == {"42": 2, "99": 1} (7 missing = not in dict, caller uses .get(k, 0)).

    For test_get_library_movies_count_fields: use create_app() with default settings. GET /api/library. Assert data["movies"] is list. xfail for count fields.
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_library_api.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    pytest collects 12 tests total (4 existing + 8 new). The 4 existing tests pass GREEN. All 8 new tests are XFAIL (not FAILED). Zero collection errors. Output includes "xfailed" count in the summary line.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append 1 RED xfail stub to test_engine.py for D-06 fix</name>
  <files>tests/translate/test_engine.py</files>
  <read_first>
    Read tests/translate/test_engine.py lines 1-80 to understand the existing test patterns: pytest.importorskip for engine_mod, async def convention, ENG-07 idempotency test structure (how it mocks ledger.check and ledger.record). Also read 13-PATTERNS.md §"tests/translate/test_engine.py" for the exact delta: the test_ledger_records_series_id_on_success function sketch and the pattern of using AsyncMock(side_effect=lambda e: recorded_entries.append(e)).
  </read_first>
  <behavior>
    - test_ledger_records_series_id_on_success: verify that when translate_file completes successfully and eligible_item is not None with arr_series_id=42, the LedgerEntry passed to ledger.record has series_id="42" (str). This is the D-06 fix test.
  </behavior>
  <action>
    Append one async test function test_ledger_records_series_id_on_success to tests/translate/test_engine.py. Mark it @pytest.mark.xfail(strict=False, reason="D-06 fix: engine.py Step 11 series_id — implemented in Phase 13 plan 02").

    The test must use pytest.importorskip("trezarr.translate.engine") to get translate_file, then set up a minimal mock environment:
    - mock_ledger: MagicMock with record=AsyncMock(side_effect=...) that captures all LedgerEntry arguments and check=AsyncMock(return_value=None)
    - mock_llm_client: MagicMock that returns a minimal valid translation response (numbered lines matching input count)
    - mock_eligible_item: MagicMock with media_item.series_id=42, arr_series_id=42 — or pass series_id via the eligible_item.media_item
    - A minimal SRT input (2 lines) written to a temp file

    After calling translate_file (or verifying the LedgerEntry at Step 11 after the fix), assert that recorded_entries contains exactly one entry and recorded_entries[0].series_id == "42".

    If the full translate_file call is too expensive to mock in a stub, use a simpler approach: patch the engine at the ledger.record call site and assert the kwarg. The xfail marker means it is acceptable to assert pytest.fail("not yet implemented") in the stub body — the important thing is the test documents the contract.

    Follow the exact deferred import pattern: all imports inside the function body with # noqa: PLC0415.
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_engine.py::test_ledger_records_series_id_on_success -xvs 2>&1 | tail -20</automated>
  </verify>
  <done>
    The test is collected and runs XFAIL (not FAILED, not ERROR). The existing engine tests remain GREEN. Full suite: uv run pytest tests/translate/test_engine.py -q shows all existing tests pass and the new one is xfailed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → library.py | Tests call the FastAPI app via ASGITransport; mock data replaces real *arr calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-W0-01 | Information Disclosure | test fixtures | accept | Test files contain no real credentials or paths; all data is synthetic mock payloads |
</threat_model>

<verification>
uv run pytest tests/web/test_library_api.py tests/translate/test_engine.py -q

Expected: 4 existing tests pass GREEN; 9 new stubs XFAIL. Zero FAILED. Zero collection errors.
</verification>

<success_criteria>
- `uv run pytest tests/web/test_library_api.py -q` shows 4 passed, 8 xfailed, 0 failed
- `uv run pytest tests/translate/test_engine.py::test_ledger_records_series_id_on_success -xvs` shows XFAIL
- All 9 new tests document the exact assertion they will check (not empty stubs)
- Existing tests remain green: no regressions
</success_criteria>

<output>
Create `.planning/phases/13-backend-episodes-enrichment/13-01-SUMMARY.md` when done
</output>
