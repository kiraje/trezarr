---
phase: 13-backend-episodes-enrichment
plan: 03
type: execute
wave: 2
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - trezarr/web/routes/library.py
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "D-01 (per ARCHITECTURE.md §4): get_series_episodes calls client.episode.get(series_id=N) alongside client.episode_file.get(series_id=N) via asyncio.to_thread + asyncio.gather; builds ep_file_by_id dict; groups episodes by seasonNumber into seasons defaultdict; returns {series_id, bazarr_available, seasons, errors} envelope; Season 0 (Specials) included"
    - "D-02 (badge shape + join key): each subtitle badge in subtitles[] carries exactly {code2, code3, hi, forced} — no path field; Bazarr inventory joins on bazarr_item.arr_id == episode.id (sonarrEpisodeId), NOT episodeFile.id"
    - "D-03 (episode_key from ints): episode_key = f'S{season_number:02d}E{episode_number:02d}' derived from authoritative Sonarr episode record ints; _episode_key_from_file is retired for this endpoint (kept in file for reference only)"
    - "D-04 (total_count from statistics.episodeFileCount): get_library series items include total_count = s.get('statistics', {}).get('episodeFileCount', 0) with safe default"
    - "D-05 (translated_count bulk query): get_library fetches session_factory from request.app.state, calls translated_counts_for_series(session_factory, series_ids), adds translated_count = t_counts.get(str(s_id), 0) to each series item"
    - "D-07 (audio normalization reusing rank.py): _normalize_audio_languages module-level helper in library.py imports _ORIG_LANG_NAME_TO_CODE2 from trezarr.source_selection.rank; handles both str (split on '/') and list input; dedupes preserving order; falls back to name.lower() for unknowns per D-07"
    - "D-08 (Bazarr fail-soft asymmetry): Bazarr disabled → bazarr_available=False, errors=[] (no entry), HTTP 200; Bazarr enabled+BazarrError → bazarr_available=False, errors[] entry with source='bazarr', HTTP 200; Sonarr disabled → HTTP 400; PyarrError → HTTP 502 (unchanged)"
    - "API-01 (enriched endpoint tests GREEN): all 5 endpoint stubs from Wave 0 turn GREEN: test_get_series_episodes_bazarr_disabled, test_get_series_episodes_bazarr_error, test_get_series_episodes_bazarr_join_key, test_audio_language_normalization, test_get_series_episodes_envelope_shape"
    - "API-02 (count fields tests GREEN): test_get_library_series_count_fields and test_get_library_movies_count_fields turn GREEN; each series item has translated_count (int) and total_count (int); each movie item has translated_count (0 or 1 from filesystem vi check) and total_count (1 if hasFile else 0)"
    - "Existing 4 tests remain GREEN: test_get_library_returns_200 (series/movies/errors keys still present), test_get_series_episodes_disabled_returns_400 (400 guard unchanged), both POST /api/translate tests (untouched endpoint)"
    - "Movie translated_count via filesystem vi check (per D-05 discretion): vi_found = any((local_path.parent / (local_path.stem + ext)).exists() for ext in ('.vi.srt','.vi.ass','.vi.vtt')); consistent with status determination in get_series_episodes"
    - "No backfill of existing ProcessedFile rows with series_id=NULL: documented in code comment; translated_count reflects future translations only"
    - "asyncio.to_thread wraps ALL pyarr calls in get_series_episodes: episode.get, episode_file.get — fixes the latent event-loop block per RESEARCH Pitfall 3"
    - "pyarr single-dict normalization applied: if isinstance(episodes_raw, dict): episodes_raw = [episodes_raw] per RESEARCH Pitfall 2"
    - "Bazarr fetch_episode_inventory param risk noted: if seriesid[] form returns empty on live Bazarr at 192.168.5.42, fall back to unbracketed form; documented in code comment per ARCHITECTURE.md §8"
  artifacts:
    - path: "trezarr/web/routes/library.py"
      provides: "Enriched get_series_episodes (season-grouped, Bazarr fail-soft, audio normalized) + get_library extended with translated_count/total_count"
      contains: "_normalize_audio_languages"
    - path: "trezarr/web/routes/library.py"
      provides: "get_library series items with translated_count and total_count"
      contains: "translated_counts_for_series"
  key_links:
    - from: "trezarr/web/routes/library.py:get_series_episodes"
      to: "trezarr/arr/bazarr.py:fetch_episode_inventory"
      via: "BazarrClient.from_settings(settings).fetch_episode_inventory(series_id) — fail-soft try/except"
      pattern: "fetch_episode_inventory"
    - from: "trezarr/web/routes/library.py:_normalize_audio_languages"
      to: "trezarr/source_selection/rank.py:_ORIG_LANG_NAME_TO_CODE2"
      via: "import inside function body (deferred PLC0415)"
      pattern: "_ORIG_LANG_NAME_TO_CODE2"
    - from: "trezarr/web/routes/library.py:get_library"
      to: "trezarr/output/ledger_sqla.py:translated_counts_for_series"
      via: "await translated_counts_for_series(session_factory, series_ids)"
      pattern: "translated_counts_for_series"
---

<objective>
Wave 2: Rewrite get_series_episodes and extend get_library in library.py.

Purpose: Deliver the full API-01 season-grouped enriched endpoint (audio languages, Bazarr subtitles, fail-soft) and the API-02 translated_count/total_count fields on list items. All 9 Wave-0 test stubs turn GREEN. This is the final implementation wave for Phase 13 backend work.

Output: library.py with a fully rewritten get_series_episodes returning the §4 contract envelope, a _normalize_audio_languages helper, and get_library extended with bulk translated-count and total-count fields for both series and movies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-backend-episodes-enrichment/13-CONTEXT.md
@.planning/phases/13-backend-episodes-enrichment/13-RESEARCH.md
@.planning/phases/13-backend-episodes-enrichment/13-PATTERNS.md
@.planning/phases/13-backend-episodes-enrichment/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite get_series_episodes with enriched season-grouped envelope</name>
  <files>trezarr/web/routes/library.py</files>
  <read_first>
    Read the full trezarr/web/routes/library.py to understand: (1) the existing get_series_episodes function (lines 152-220) that will be replaced, (2) the _episode_key_from_file helper (lines 31-65) that will be kept in the file but retired for this endpoint, (3) the get_library function (lines 70-147) to understand the session_factory access pattern at line 265 (post_translate) and the fail-soft errors[] pattern at lines 87-112. Also read 13-PATTERNS.md §get_series_episodes delta section for the exact implementation changes and 13-RESEARCH.md §"Enriched Episode Handler" for the full reference implementation (ARCHITECTURE.md §4 pattern updated for D-02 forced field).
  </read_first>
  <action>
    In trezarr/web/routes/library.py, make two changes:

    CHANGE 1 — Add module-level _normalize_audio_languages helper (before the router = APIRouter() line or immediately after imports):

    Add a module-level function `_normalize_audio_languages(raw: list | str | None) -> list[str]` that:
    - Returns [] if not raw
    - If raw is str: splits on "/" stripping whitespace
    - If raw is list: iterates items, each item split on "/" (handles both list-of-strings and list-with-slash-joined entries)
    - Dedupes preserving order using a seen set
    - Maps each name via `_ORIG_LANG_NAME_TO_CODE2.get(name.lower(), name.lower())` — unknowns fall back to lowercased original
    - Import _ORIG_LANG_NAME_TO_CODE2 from trezarr.source_selection.rank inside the function body (# noqa: PLC0415, deferred import pattern)

    CHANGE 2 — Entirely replace the get_series_episodes function body (keep the decorator and signature `@router.get("/library/series/{series_id}/episodes")` and `async def get_series_episodes(series_id: int, request: Request) -> JSONResponse:`):

    The new body follows ARCHITECTURE.md §4 + D-01/D-02/D-03/D-07/D-08 exactly:

    1. Deferred imports at top of function body: asyncio, collections.defaultdict, pathlib.Path, TrezarrSettings, build_sonarr_client, BazarrClient, BazarrError from trezarr.arr.bazarr, find_source_sub, apply_path_mapping, PyarrError — all with # noqa: PLC0415

    2. Settings access: `settings = getattr(request.app.state, "settings", None) or TrezarrSettings()`

    3. Sonarr disabled guard: return JSONResponse({"error": "sonarr_disabled"}, status_code=400) if not settings.sonarr_enabled

    4. errors: list[dict] = []

    5. Sonarr fetch with try/except PyarrError → 502:
       - client = build_sonarr_client(settings)
       - episodes_coro = asyncio.to_thread(client.episode.get, series_id=series_id)
       - ep_files_coro = asyncio.to_thread(client.episode_file.get, series_id=series_id)
       - episodes_raw, ep_files_raw = await asyncio.gather(episodes_coro, ep_files_coro)
       - Normalize both with isinstance(x, dict) → [x]
       - ep_file_by_id = {ef["id"]: ef for ef in ep_files_raw}

    6. Bazarr fail-soft block per D-08:
       - bazarr_by_ep_id: dict[int, list] = {}
       - bazarr_available = False
       - if settings.bazarr_enabled:
         - try: BazarrClient.from_settings(settings), fetch_episode_inventory(series_id)
           Add code comment: "# If this returns empty, try params={'seriesid': series_id} — ARCHITECTURE.md §8 risk"
           - On success: build bazarr_by_ep_id = {item.arr_id: [{"code2": s.code2, "code3": s.code3, "hi": s.hi, "forced": s.forced} for s in item.subtitles] for item in bazarr_items}; bazarr_available = True
         - except BazarrError as exc: logger.warning + errors.append({"source": "bazarr", "error": str(exc)}); bazarr_available stays False

    7. Season grouping loop over episodes_raw:
       - seasons: dict[int, list[dict]] = defaultdict(list)
       - For each ep: extract ep_id=ep.get("id"), ep_file_id=ep.get("episodeFileId"), has_file=bool(ep.get("hasFile", False)), ep_file=ep_file_by_id.get(ep_file_id) if ep_file_id else None
       - raw_path from ep_file.get("path","") if ep_file else ""
       - local_path = str(apply_path_mapping(raw_path, settings.path_mappings)) if raw_path else None
       - media_info = (ep_file or {}).get("mediaInfo") or {}
       - audio_raw = media_info.get("audioLanguages") or []
       - audio_languages = _normalize_audio_languages(audio_raw) — NOTE: _ORIG_LANG_NAME_TO_CODE2 is imported inside _normalize_audio_languages; no extra import here
       - season_number = ep.get("seasonNumber", 0); episode_number = ep.get("episodeNumber", 0)
       - episode_key = f"S{season_number:02d}E{episode_number:02d}"  # D-03: from ints, no regex
       - source_path = source_lang = None; status = "nothing"
       - if local_path: lp = Path(local_path); src = find_source_sub(lp, settings.source_lang_priority) if lp.exists() else None; if src: source_path=str(src[0]), source_lang=src[1]; vi_found=any(...); status="translated" if vi_found else "has_source"
       - subtitles = bazarr_by_ep_id.get(ep_id, [])
       - Append full episode dict to seasons[season_number]

    8. Return JSONResponse({
         "series_id": series_id,
         "bazarr_available": bazarr_available,
         "seasons": [{"season_number": sn, "episodes": seasons[sn]} for sn in sorted(seasons)],
         "errors": errors,
       })

    Keep _episode_key_from_file in the file (it may be called from other places or kept for reference), but do NOT call it from the new get_series_episodes.
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_library_api.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    The 5 endpoint stubs (test_get_series_episodes_bazarr_disabled, test_get_series_episodes_bazarr_error, test_get_series_episodes_bazarr_join_key, test_audio_language_normalization, test_get_series_episodes_envelope_shape) turn GREEN. All 4 existing tests remain GREEN (especially test_get_series_episodes_disabled_returns_400 — the 400 guard is still present). Zero failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend get_library with translated_count + total_count for series and movies</name>
  <files>trezarr/web/routes/library.py</files>
  <read_first>
    Re-read the current trezarr/web/routes/library.py get_library function (lines 70-147) to see: (1) the raw_series loop (lines 92-112) and the per-series dict at lines 101-108 — this is where translated_count/total_count are added, (2) the raw_movies loop (lines 115-141) and the per-movie dict at lines 133-141 — where movie counts are added, (3) the post_translate function at lines 225-277 showing the `session_factory = getattr(request.app.state, "session_factory", None)` access pattern (line 265). Also read 13-PATTERNS.md §get_library delta section for the exact two-part change pattern.
  </read_first>
  <action>
    In trezarr/web/routes/library.py get_library function, add two groups of changes:

    GROUP 1 — After the Sonarr series loop completes (after the except block on Sonarr) but inside the `if settings.sonarr_enabled:` block, add a bulk translated-counts lookup:

    ```python
    # D-05: bulk translated count — one aggregate DB query, not N queries
    session_factory = getattr(request.app.state, "session_factory", None)
    if session_factory and series_list:
        from trezarr.output.ledger_sqla import translated_counts_for_series  # noqa: PLC0415
        _s_ids = [s.get("id") for s in raw_series if s.get("id") is not None]
        t_counts = await translated_counts_for_series(session_factory, _s_ids)
    else:
        t_counts = {}
    ```

    Note: raw_series may be empty if Sonarr errored. Guard with `if series_list` to avoid the DB call when the list is already empty from a Sonarr error. If sonarr_enabled=False (list never populated), t_counts = {} (no series to count).

    Then in the per-series dict construction (the series_list.append({...}) call), add two new fields:
    - `"translated_count": t_counts.get(str(s.get("id")), 0),`
    - `"total_count": s.get("statistics", {}).get("episodeFileCount", 0),`

    But note that series_list.append happens inside the Sonarr try/except loop, and t_counts is computed after. To handle this correctly, store raw_series entries and build series_list in two passes — OR keep the current in-loop append but move the translated_count addition to a post-loop map:

    Preferred approach (least diff): collect `raw_series` first, then after the loop add translated_counts. Replace the single `series_list.append({...})` call to include placeholder 0 values first, then do a second pass to fill in t_counts. Alternatively: move the translated_count fetch to BEFORE the loop and use it during the append. Since t_counts requires series_ids from raw_series, the cleanest approach is:
    1. Fetch raw_series and build series_list without count fields (as today)
    2. After the loop, do the t_counts query
    3. For each item in series_list, set item["translated_count"] = t_counts.get(str(item["id"]), 0) and item["total_count"] = ... — but total_count needs s.get("statistics"...) from raw_series not from series_list

    Simplest correct approach: collect both raw_series and the Sonarr stats in a parallel dict `{series_id: episodeFileCount}` during the loop, then apply counts after. Specifically:
    - Add `raw_series_stats: dict[int, int] = {}` before the loop
    - Inside the loop, add: `s_id = s.get("id"); if s_id is not None: raw_series_stats[s_id] = s.get("statistics", {}).get("episodeFileCount", 0)`
    - Still append the series dict (without count fields yet) inside the loop
    - After the except block, fetch t_counts (with guard)
    - Then for each item in series_list: `s_id = item.get("id"); item["translated_count"] = t_counts.get(str(s_id), 0); item["total_count"] = raw_series_stats.get(s_id, 0)`

    GROUP 2 — In the Radarr movies loop, for each movie add the vi-sidecar filesystem check and append translated_count + total_count to the movie dict:

    After the existing `source_sub_found = source_sub_result is not None` line (or source_sub_found=False branch), add:
    ```python
    # D-05 movie translated_count: filesystem vi-sidecar check
    vi_found = False
    if raw_path and source_sub_found:
        local_path_obj = apply_path_mapping(raw_path, settings.path_mappings)
        vi_found = any(
            (local_path_obj.parent / (local_path_obj.stem + ext)).exists()
            for ext in (".vi.srt", ".vi.ass", ".vi.vtt")
        )
    ```

    Then in movies_list.append({...}) add:
    - `"translated_count": 1 if vi_found else 0,`
    - `"total_count": 1 if m.get("hasFile", False) else 0,`

    Note: local_path is already computed inside the `if raw_path:` block as `apply_path_mapping(raw_path, settings.path_mappings)`. Reuse that variable for the vi check rather than recomputing. The variable name in the current code is `local_path` (a Path object), so use it directly: `vi_found = any((local_path.parent / ...).exists() ...)`.

    No changes to the return statement or the errors/HTTP-200 contract.
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_library_api.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    test_get_library_series_count_fields and test_get_library_movies_count_fields turn GREEN. All 4 existing tests remain GREEN. All 9 Phase-13 Wave-0 stubs are now GREEN. Zero failures in test_library_api.py.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → library.py | series_id is an int path parameter; FastAPI validates type automatically (ASVS V5) |
| library.py → Bazarr | BazarrError messages use _normalize_arr_host in bazarr.py (existing T-10-02 mitigation) — API key never leaked into errors[] |
| library.py badge output | subtitle path fields stripped from response — only code2/code3/hi/forced returned (D-02 mitigation for T-13-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | get_series_episodes subtitle badges | mitigate | D-02: strip container paths; badges return only code2, code3, hi, forced — never subtitle file path |
| T-13-02 | Tampering | translated_counts_for_series (via get_library) | mitigate | SQLAlchemy parameterized .in_(str_ids) — no raw SQL; parameterized by ORM per Wave 1 plan |
| T-13-03 | Information Disclosure | BazarrError in errors[] | accept | bazarr.py already redacts API key via _normalize_arr_host (T-10-02 existing mitigation) — error message exposure is low-risk |
| T-13-04 | Tampering | series_id int path param | accept | FastAPI auto-validates int type; non-int returns 422 before handler runs (ASVS V5 satisfied) |
</threat_model>

<verification>
uv run pytest tests/web/test_library_api.py -q
uv run pytest tests/ -q

Expected: All 12 test_library_api.py tests GREEN (4 existing + 8 new). All engine tests GREEN. Full suite passing.

Manual verification (documented in VALIDATION.md, not blocking plan completion):
- Sonarr statistics.episodeFileCount presence on live instance at 192.168.5.42
- Bazarr fetch_episode_inventory seriesid[] param vs unbracketed — if empty, update fetch call in get_series_episodes to use params={"seriesid": series_id}
- After translating one episode, confirm its series translated_count increments in GET /api/library
</verification>

<success_criteria>
- GET /api/library/series/{id}/episodes returns JSON envelope: series_id (int), bazarr_available (bool), seasons (list of {season_number, episodes}), errors (list)
- Each episode in seasons has: episode_id, episode_file_id, season_number, episode_number, episode_key (SxxExx), title, monitored, has_file, local_path, source_path, source_lang, status, audio_languages (ISO-639-1 codes), subtitles (list of {code2, code3, hi, forced})
- GET /api/library/series/{id}/episodes returns HTTP 200 even when Bazarr is unreachable (bazarr_available=false)
- GET /api/library series items have translated_count (int) and total_count (int)
- GET /api/library movie items have translated_count (0 or 1) and total_count (1 if hasFile else 0)
- `uv run pytest tests/web/test_library_api.py -q` exits 0, all 12 tests pass
- `uv run pytest tests/ -q` exits 0, no regressions
</success_criteria>

<output>
Create `.planning/phases/13-backend-episodes-enrichment/13-03-SUMMARY.md` when done
</output>
