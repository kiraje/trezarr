---
phase: 12-app-shell-route-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Series.tsx
  - frontend/src/pages/SeriesDetail.tsx
  - frontend/src/pages/Movies.tsx
  - frontend/src/components/app-sidebar.tsx
  - frontend/src/components/AppShell.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "NAV-01: a full-height sidebar shell renders the Captions brand glyph plus uppercase TREZARR pill and shows a left 2px purple accent bar on the active nav item"
    - "NAV-02: the nav exposes exactly six items (Series/Movies/Queue/History/Bible/Settings), /library splits into /series plus /movies, and / redirects to /series"
    - "D-01: the sidebar uses collapsible=icon (default expanded), persists via the vendor sidebar_state cookie, and collapses to a ~48px icon rail via SidebarRail plus Cmd/Ctrl+B"
    - "D-02: the 48px TopBar is dropped and the green service-status dot moves into SidebarFooter (static server-is-serving semantics; label hidden when collapsed; NOT wired to arr state)"
    - "D-03: /library stays mounted on the untouched existing Library.tsx as a transitional route but is absent from NAV_ITEMS"
    - "D-04: a single layout route (Route path=/ element=AppShell) hosts Outlet with RELATIVE child paths and an index redirect to /series"
    - "D-05: active state is derived from useLocation prefix-match (pathname equals to, or pathname startsWith to plus slash) and navigation uses onClick navigate(to) NOT asChild plus anchor"
    - "D-06: the brand is a lucide Captions glyph tinted text-sidebar-primary plus an uppercase TREZARR pill in SidebarHeader, glyph only when collapsed, no new image asset"
  artifacts:
    - path: "frontend/src/pages/Series.tsx"
      provides: "Series stub page (heading placeholder)"
    - path: "frontend/src/pages/SeriesDetail.tsx"
      provides: "SeriesDetail stub page reading :seriesId param"
    - path: "frontend/src/pages/Movies.tsx"
      provides: "Movies stub page (heading placeholder)"
    - path: "frontend/src/components/app-sidebar.tsx"
      provides: "Hand-written AppSidebar: brand, 6 nav items, accent bar, footer status"
      contains: "data-[active=true]:border-sidebar-primary"
    - path: "frontend/src/components/AppShell.tsx"
      provides: "Thin SidebarProvider plus AppSidebar plus main Outlet layout host"
      contains: "Outlet"
    - path: "frontend/src/App.tsx"
      provides: "Layout-route table; / to /series; series, movies, series/:seriesId added; transitional library kept"
      contains: "Navigate to=/series"
  key_links:
    - from: "frontend/src/components/app-sidebar.tsx"
      to: "frontend/src/components/ui/sidebar.tsx"
      via: "SidebarMenuButton isActive prop maps to data-[active=true]"
      pattern: "data-\\[active=true\\]:border-sidebar-primary"
    - from: "frontend/src/components/AppShell.tsx"
      to: "react-router-dom Outlet"
      via: "layout-route element renders Outlet"
      pattern: "<Outlet"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/AppShell.tsx"
      via: "layout route element"
      pattern: "element=\\{<AppShell"
---

<objective>
Replace the legacy AppShell (48px TopBar plus fixed 192px flat nav, children-prop) with a shadcn
SidebarProvider plus hand-written AppSidebar plus main-Outlet layout, and migrate App.tsx to a
react-router-dom v6 layout-route table that splits /library into /series plus /movies plus
/series/:seriesId and redirects / to /series. Add three heading-only stub pages so the new routes
resolve. Every existing page (Queue, History, Settings, Bible List, Bible Editor, JobLogs) keeps
rendering inside the new full-height sidebar shell with no regressions and no new page content.

Purpose: deliver NAV-01 (full-height sidebar shell plus brand plus left purple accent bar) and NAV-02
(six-item nav, /library split, / to /series) — the navigation chrome the rest of the v1.1 milestone
builds on. NAV-03 badges, real page content, and Library.tsx deletion are LATER phases.

Output: 3 new stub pages, 1 new hand-written component (app-sidebar.tsx), AppShell.tsx replaced,
App.tsx rewired; cd frontend then npm run build exits 0 with trezarr/web/static populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-shell-route-restructure/12-CONTEXT.md
@.planning/phases/12-app-shell-route-restructure/12-RESEARCH.md
@.planning/phases/12-app-shell-route-restructure/12-PATTERNS.md
@.planning/phases/12-app-shell-route-restructure/12-UI-SPEC.md

# Vendor primitive (READ to learn the API — NEVER hand-edit this file):
@frontend/src/components/ui/sidebar.tsx
# Files being replaced/modified:
@frontend/src/components/AppShell.tsx
@frontend/src/App.tsx
# Stub-page analogs:
@frontend/src/pages/Queue.tsx
@frontend/src/pages/JobLogs.tsx
</context>

<critical_constraints>
- Vendor code is sacred: frontend/src/components/ui/sidebar.tsx is vendor copy-in code — NEVER hand-edit it. The left 2px purple accent bar (NAV-01) does NOT exist in the vendor active styling (filled-bg only — verified at sidebar.tsx:523). It MUST be composed via a className on each SidebarMenuButton in the hand-written app-sidebar.tsx.
- Build invariants (inherited Phase 11): build stays tsc -b then vite build; do NOT add base to vite.config.ts; outDir stays ../trezarr/web/static; TS is strict with noUnusedLocals/noUnusedParameters (import only what you use; SeriesDetail.tsx MUST read its useParams seriesId or omit the import).
- Dark mode: leave frontend/index.html html lang=en class=dark intact — the sidebar tokens live under the .dark scope.
- No new dependencies: zero npm install, zero shadcn add. Every primitive was vendored in Phase 11 and is verified present.
- No backend change: the existing SPAStaticFiles fallback already serves index.html for the new extensionless deep links (/series, /series/:id, /movies). Do NOT touch trezarr/web/.
- Sidebar geometry: do NOT hardcode w-48 or any fixed sidebar width — the vendor manages width via the sidebar-width CSS vars.
- Library untouched: frontend/src/pages/Library.tsx is NOT edited this phase (D-03); it stays reachable at /library only as a transitional route.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Create the three stub pages (Series, SeriesDetail, Movies)</name>
  <read_first>
    - frontend/src/pages/Queue.tsx (analog: file-header docstring plus heading block)
    - frontend/src/pages/JobLogs.tsx (analog: the useParams read pattern — import useParams then read its result)
    - .planning/phases/12-app-shell-route-restructure/12-UI-SPEC.md (Interaction Contract 8 stub-page contract; Typography Heading role 20px/600)
  </read_first>
  <files>frontend/src/pages/Series.tsx, frontend/src/pages/SeriesDetail.tsx, frontend/src/pages/Movies.tsx</files>
  <action>
    Create three NEW stub page files, each a default-exported function component rendering a single heading
    placeholder (Success Criterion 4 / UI-SPEC Interaction Contract 8). Open each with the codebase standard
    file-header JSDoc docstring citing 12-UI-SPEC.md Interaction Contract 8 and the relevant decision.

    Series.tsx: render an h1 with className text-xl font-semibold and text Series, plus an optional muted
    subline (a p with className text-muted-foreground and text Coming in Phase 14). Import NOTHING (Pitfall 4
    / strict noUnusedLocals) — it needs no imports.

    Movies.tsx: identical shape with the heading text Movies. Import nothing.

    SeriesDetail.tsx: import useParams from react-router-dom, read const seriesId from useParams() (the route
    param name is seriesId, matching series/:seriesId in Task 3), and echo it: h1 with className text-xl
    font-semibold and text Series Detail, plus a p (className text-muted-foreground) rendering Series number
    seriesId — coming in Phase 14. You MUST read seriesId (render it) — an unread useParams result fails
    noUnusedLocals. Render seriesId only as a JSX text node (auto-escaped); NEVER dangerouslySetInnerHTML
    (security: reflected-XSS sink — see threat_model T-12-01).

    Heading uses text-xl font-semibold (Heading role, 20px/600 per UI-SPEC Typography); subline uses
    text-muted-foreground (Body, muted). No data fetching, no useState/useEffect, no console errors.
  </action>
  <verify>
    <automated>cd frontend && test -f src/pages/Series.tsx && test -f src/pages/Movies.tsx && test -f src/pages/SeriesDetail.tsx && grep -q seriesId src/pages/SeriesDetail.tsx && grep -L dangerouslySetInnerHTML src/pages/SeriesDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Series.tsx exists, default-exports a component, contains text-xl font-semibold and heading text Series, and has zero import statements.
    - frontend/src/pages/Movies.tsx exists, default-exports a component, contains heading text Movies, and has zero import statements.
    - frontend/src/pages/SeriesDetail.tsx imports useParams from react-router-dom, destructures seriesId, and renders seriesId as a JSX text node (dangerouslySetInnerHTML ABSENT).
    - No stub file imports a symbol it does not use (strict noUnusedLocals/noUnusedParameters).
  </acceptance_criteria>
  <done>Three stub pages exist; SeriesDetail.tsx reads and echoes seriesId as auto-escaped text; no dangerouslySetInnerHTML anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Create the hand-written app-sidebar.tsx (brand, 6 nav items plus accent bar, footer)</name>
  <read_first>
    - frontend/src/components/ui/sidebar.tsx (vendor API: Sidebar, SidebarHeader, SidebarContent, SidebarFooter, SidebarMenu, SidebarMenuItem, SidebarMenuButton with its isActive and tooltip props, SidebarRail, SidebarTrigger; the active-state gap at line 523 where active styling is filled-bg only with no border-l; the data-collapsible group attribute on the wrapper)
    - frontend/src/components/AppShell.tsx (legacy NavItem active look at lines 43-50 = border-l-2 border-accent bg-bg-stripe; the green status dot literal plus aria-label/title at lines 64-69)
    - .planning/phases/12-app-shell-route-restructure/12-UI-SPEC.md (Interaction Contract 2/3/4/6/7: accent bar, prefix-match, locked six-item nav table, brand header, footer)
    - .planning/phases/12-app-shell-route-restructure/12-PATTERNS.md (app-sidebar.tsx section: the load-bearing accent-bar delta plus verbatim footer dot carry-over)
  </read_first>
  <files>frontend/src/components/app-sidebar.tsx</files>
  <action>
    Create a NEW hand-written component AppSidebar (named export AppSidebar) — this is the Trezarr-specific
    nav; it composes the vendor primitives, it does NOT edit them. Open with the standard file-header JSDoc
    docstring citing 12-UI-SPEC.md Interaction Contract and D-01/D-02/D-05/D-06.

    Imports: useLocation, useNavigate from react-router-dom; from lucide-react import ONLY the icons used —
    Captions (brand glyph, D-06), Tv, Film, List, History, BookOpen, Settings (the six nav icons); from
    ./ui/sidebar import Sidebar, SidebarHeader, SidebarContent, SidebarFooter, SidebarMenu, SidebarMenuItem,
    SidebarMenuButton, SidebarRail, SidebarTrigger. Strict TS: no unused imports.

    Define a NAV_ITEMS const array (locked, exactly six, in this order — NAV-02 / UI-SPEC Interaction
    Contract 4): to /series label Series icon Tv; to /movies label Movies icon Film; to /queue label Queue
    icon List; to /history label History icon History; to /bible label Bible icon BookOpen; to /settings
    label Settings icon Settings. Mark it as const. Do NOT add /library to NAV_ITEMS (D-03 — reachable but
    not in nav).

    Render Sidebar with collapsible set to icon (D-01 — icon rail; default expanded plus the sidebar_state
    cookie plus Cmd/Ctrl+B plus Sheet-on-mobile are all handled by the vendor SidebarProvider/Sidebar; do not
    re-implement). Inside:

    1. SidebarHeader (D-06 / NAV-01 brand): a flex row containing the Captions glyph at size 20 tinted with
       className text-sidebar-primary, then an uppercase TREZARR pill — a span with className containing
       font-semibold uppercase tracking-wide text-sm and the group-data collapsible-icon hidden utility, and
       text TREZARR (that hidden utility hides the pill in the collapsed rail). Optionally add a SidebarTrigger
       with className ml-auto plus the same hidden-when-collapsed utility (discretion ON for ergonomics per
       D-01, since the TopBar is gone). Languages is an approved alternate glyph for Captions (D-06) —
       finalize at the UI gate; default to Captions.

    2. SidebarContent containing SidebarMenu: map NAV_ITEMS. Read pathname once via const pathname from
       useLocation() and const navigate from useNavigate(). For each item compute const isActive equal to
       (pathname === to) OR (pathname.startsWith(to plus slash)) (D-05 prefix-match — keeps /series active on
       /series/:id and /bible active on /bible/:id). Render a SidebarMenuItem keyed by to, containing a
       SidebarMenuButton with: isActive set to the computed isActive, tooltip set to label, onClick set to a
       closure that calls navigate(to), and className set to the literal string border-l-2 border-transparent
       data-[active=true]:border-sidebar-primary. Inside the button render the Icon at size 16 and a span with
       the label.
       - THE ACCENT BAR (NAV-01 — the load-bearing delta): the className string above is mandatory and
         verbatim. The transparent default border reserves the 2px (no layout shift); the active state lights
         it sidebar-primary (purple). The vendor cn() merges it additively after the variant string, so the
         filled bg plus the purple left bar compose — reproducing the legacy border-l-2 border-accent
         bg-bg-stripe look on the new mechanism.
       - Navigation MUST use onClick calling navigate(to) — NOT asChild plus anchor/NavLink (D-05 — it fights
         the Radix Slot and breaks the data-active attribute the bar depends on).
       - tooltip set to label makes the vendor show the label on hover only when collapsed.

    3. SidebarFooter (D-02 — verbatim carry-over of the legacy green dot from AppShell.tsx:64-69): a flex row
       with a span using className h-2 w-2 rounded-full plus the green hex 22c55e background, aria-label
       Service status running, and title Service running; then a span with className text-sm
       text-muted-foreground plus the hidden-when-collapsed utility, and text Running (label hidden when
       collapsed). Keep the static server-is-serving semantics — do NOT wire it to arr connection state (that
       LIVE badge is NAV-03 / Phase 14).

    4. SidebarRail (D-01 — the draggable collapse affordance) as the last child of Sidebar.
  </action>
  <verify>
    <automated>cd frontend && grep -q border-sidebar-primary src/components/app-sidebar.tsx && grep -q collapsible src/components/app-sidebar.tsx && grep -q navigate src/components/app-sidebar.tsx && grep -L asChild src/components/app-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app-sidebar.tsx exports AppSidebar and contains the verbatim className border-l-2 border-transparent data-[active=true]:border-sidebar-primary on the nav button (the NAV-01 accent bar — vendor does not provide it).
    - Sidebar uses collapsible set to icon and includes a SidebarRail (D-01).
    - NAV_ITEMS has exactly six entries in order Series, Movies, Queue, History, Bible, Settings, each with the to/label/icon from UI-SPEC Interaction Contract 4; /library is ABSENT from NAV_ITEMS (D-03).
    - Active state uses pathname equals to OR pathname.startsWith(to plus slash); navigation is onClick navigate(to); asChild is ABSENT (D-05).
    - SidebarHeader renders the Captions glyph tinted text-sidebar-primary plus the uppercase TREZARR pill hidden when collapsed (D-06, NAV-01).
    - SidebarFooter renders the green dot (bg hex 22c55e) with aria-label Service status running plus a Running label hidden when collapsed (D-02).
    - lucide-react import list contains only the seven used icons (Captions, Tv, Film, List, History, BookOpen, Settings) — strict noUnusedLocals.
  </acceptance_criteria>
  <done>app-sidebar.tsx renders the brand, six locked nav items with the purple accent-bar className, prefix-match active state via navigate(to), the footer status dot, and the SidebarRail — all composing vendor primitives without editing them.</done>
</task>

<task type="auto">
  <name>Task 3: Replace AppShell.tsx (Outlet host) and rewire App.tsx (layout-route table)</name>
  <read_first>
    - frontend/src/components/AppShell.tsx (current children-prop shell to REPLACE: drop AppShellProps/children, the 48px header TopBar, the inline nav plus NavItem, and the w-48 width)
    - frontend/src/App.tsx (current route table to MODIFY: currently / redirects to /queue and wraps Routes in AppShell as children)
    - frontend/src/components/ui/sidebar.tsx (SidebarProvider export — the whole-document context the shell mounts)
    - .planning/phases/12-app-shell-route-restructure/12-RESEARCH.md (Code Example 1 App.tsx target shape; Code Example 2 AppShell target shape; Pitfall 3 relative child paths)
    - .planning/phases/12-app-shell-route-restructure/12-PATTERNS.md (AppShell.tsx and App.tsx sections: exact deltas)
  </read_first>
  <files>frontend/src/components/AppShell.tsx, frontend/src/App.tsx</files>
  <action>
    Two coupled edits that together make the build green. Order matters: AppShell drops the children prop,
    which would break the old App.tsx import shape — so rewire App.tsx in the same task.

    AppShell.tsx (REPLACE the whole component body; keep a file-header JSDoc docstring updated to cite
    12-UI-SPEC.md Interaction Contract 1 and D-02/D-04):
    - Delete the interface AppShellProps and the children prop; AppShell becomes a no-prop default export.
    - Delete the entire 48px header/TopBar block (current lines 61-70) — TopBar dropped (D-02). The green
      status dot now lives in app-sidebar.tsx SidebarFooter (Task 2), NOT here.
    - Delete the inline nav plus NavItem markup (current lines 27-103) — that moved into app-sidebar.tsx.
    - Delete the hardcoded w-48 width — the vendor manages width via CSS vars.
    - New body returns: SidebarProvider wrapping AppSidebar (imported from ./app-sidebar) and a main element
      with className flex-1 overflow-auto bg-background p-6 whose only child is Outlet (imported from
      react-router-dom). Do NOT wrap main in extra flex containers; SidebarProvider already renders the
      flex/min-h-svh/w-full wrapper and Sidebar reserves width (Pattern 1).
    - Imports: Outlet from react-router-dom; SidebarProvider from ./ui/sidebar; AppSidebar from ./app-sidebar.
      Strict TS: no unused imports (remove the old ReactNode, NavLink, lucide imports).

    App.tsx (MODIFY the route table — RESEARCH Code Example 1 / D-04; update the file-header docstring route
    list to reflect / to /series and the new routes):
    - Stop wrapping Routes in AppShell as children. Make AppShell the layout-route element: a Route with
      path / and element set to AppShell, containing nested child Routes that render through the Outlet.
    - Child paths are RELATIVE — DROP the leading slash (Pitfall 3): path series; path series/:seriesId; path
      movies; path queue; path history; path jobs/:id/logs; path settings; path bible; path bible/:seriesId;
      and the transitional path library. The index redirect uses a Route index whose element is Navigate to
      /series replace (the to target is ABSOLUTE — it is a nav target, not a route path). This replaces the
      old / to /queue default (D-04, NAV-02).
    - Add imports plus routes: Series (series), SeriesDetail (series/:seriesId), Movies (movies).
    - Keep all existing page imports plus routes as relative children: Queue, History, JobLogs, Settings,
      BibleList, BibleEditor.
    - Keep the transitional library route mounted on the existing Library import (D-03 — still imported, still
      routed, just not in the nav). Library.tsx is NOT edited.
    - Strict TS: every imported page must be routed (no unused imports).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - cd frontend then npm run build exits 0 (tsc -b strict clean: noUnusedLocals/noUnusedParameters; vite build clean) and trezarr/web/static is populated.
    - AppShell.tsx contains Outlet and SidebarProvider and AppSidebar; it has NO children prop, NO AppShellProps interface, NO header/TopBar element, and NO w-48.
    - App.tsx contains a layout route with element set to AppShell wrapping nested child Routes; the index route Navigate target is /series (NOT /queue); child path values have no leading slash; routes exist for series, series/:seriesId, movies, queue, history, jobs/:id/logs, settings, bible, bible/:seriesId, and library.
    - main uses className bg-background p-6 (NOT the legacy bg-bg-base px-8 py-6) and is NOT wrapped in an extra flex container or given a fixed width.
    - frontend/index.html still has html class dark (unchanged); vite.config.ts still has no base; Library.tsx is unchanged (git diff shows no edit to pages/Library.tsx).
  </acceptance_criteria>
  <done>The new layout-route shell builds green; / redirects to /series; AppShell hosts the routed page via Outlet inside the SidebarProvider; all existing pages remain routed; Library stays reachable transitionally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser URL bar to react-router | The :seriesId route param is attacker-influenceable via the URL and flows into the SeriesDetail stub heading. |
| backend (none new) | No backend code is touched this phase; the pre-existing SPAStaticFiles fallback serves index.html for the new extensionless paths unchanged. |

Per 12-RESEARCH.md Security Domain: Phase 12 is a client-side shell/routing change with NO new data flows,
auth, inputs, crypto, or backend edits. ASVS L1 categories V2/V3/V4/V5/V6/V13 are N/A or inherited-unchanged.
security_block_on is high — there are no high-severity findings in scope.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering (reflected XSS) | SeriesDetail.tsx echoing :seriesId into a heading | mitigate | Render seriesId ONLY as a JSX text node (React auto-escapes); NEVER dangerouslySetInnerHTML. Asserted in Task 1 acceptance criteria plus verify (grep -L dangerouslySetInnerHTML). |
| T-12-02 | Spoofing (open redirect) | / to target redirect in App.tsx | mitigate | The redirect target is the hardcoded literal /series (Navigate to /series replace) — not user-controlled. Asserted in Task 3 acceptance criteria (Navigate target is /series). |
| T-12-03 | Information disclosure | vendor sidebar_state cookie | accept | The vendor cookie stores only true/false (open/collapsed UI preference), no PII, no auth value. Acceptable as-is; do not store anything else in it. |
| T-12-SC | Tampering (supply chain) | npm/shadcn installs | accept (none in scope) | Phase 12 installs ZERO packages (no npm install, no shadcn add) — RESEARCH Package Legitimacy Audit: not applicable. No install task, no legitimacy checkpoint required. |
</threat_model>

<verification>
## Automated (build gate — the only automated check; no frontend test harness exists)

cd frontend then npm run build — must exit 0; tsc -b strict clean (noUnusedLocals/noUnusedParameters);
vite build clean; trezarr/web/static populated. This is the per-task and per-phase automated gate.

## Manual UAT (browser — maps to ROADMAP Success Criteria + UI-SPEC Phase-End Verification Contract)

1. SC-1 (NAV-02): visiting / redirects to /series; hard-refresh on each of /series, /movies, /queue, /history, /bible, /settings, and /series/1 loads the correct page (SPA fallback intact).
2. SC-2 (NAV-01/NAV-02): full-height sidebar on all pages; Captions glyph plus TREZARR pill; exactly six nav items in order (Series/Movies/Queue/History/Bible/Settings); the active item shows a left 2px purple accent bar (headline check — confirm the bar actually renders, not just a bg tint).
3. SC-3: Queue, History, Settings, Bible List, Bible Editor all navigable and fully functional inside the new shell, no regressions; /library still reachable directly and renders the existing Library page (D-03).
4. SC-4: /series, /series/1, /movies render a heading placeholder with no DevTools console errors.
5. D-01: Cmd/Ctrl+B and the SidebarRail collapse to the 48px icon rail (icons only, labels hidden); the accent bar still shows on the active icon; the sidebar_state cookie persists state across reload.
6. D-02: green Running dot in SidebarFooter; label hidden when collapsed.
</verification>

<success_criteria>
- cd frontend then npm run build exits 0; trezarr/web/static populated.
- NAV-01: full-height sidebar shell with Captions brand glyph plus uppercase TREZARR pill; active nav item shows a left 2px purple accent bar (composed via className on app-sidebar.tsx, not the vendor file).
- NAV-02: nav exposes exactly Series/Movies/Queue/History/Bible/Settings; / redirects to /series; /series plus /movies exist; /library reachable but not in nav.
- All six locked decisions implemented: D-01 (collapsible icon plus cookie plus rail), D-02 (TopBar dropped, footer dot), D-03 (transitional /library, untouched Library.tsx), D-04 (layout route plus Outlet plus relative paths), D-05 (useLocation prefix-match plus navigate), D-06 (Captions glyph plus TREZARR pill).
- No vendor file edited; no backend change; no new dependency; index.html class dark and vite.config.ts (no base) unchanged.
</success_criteria>

<output>
Create .planning/phases/12-app-shell-route-restructure/12-01-SUMMARY.md when done.
</output>
