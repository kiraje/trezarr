---
phase: 15-reskin-existing-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/JobLogs.tsx
  - frontend/src/components/LogViewer.tsx
  - frontend/src/components/StatusBadge.tsx
autonomous: true
requirements:
  - RSK-01

must_haves:
  truths:
    - "RSK-01/D-01/D-04: JobLogs renders loading state as three Skeleton rows (heading, summary strip, log area) when loading === true"
    - "RSK-01/D-04: JobLogs renders <div role='alert'> with className bg-destructive/10 text-destructive when error is non-null (replaces style={{backgroundColor:'#2d1515',color:'#f87171'}})"
    - "RSK-01/D-04: JobLogs renders <p className='text-sm text-muted-foreground py-8 text-center'> when entries is empty and loading is false and error is null"
    - "RSK-01/D-02: JobLogs heading is text-xl font-semibold text-foreground (was text-lg text-[#e2e6f0]); breadcrumb button uses Button variant=ghost size=sm with text-primary (was text-accent with raw focus styles)"
    - "RSK-01/D-02: Job summary strip uses Card/CardContent replacing bg-bg-surface border border-[#2d3148]; internal text uses text-muted-foreground (was text-[#6b7280])"
    - "RSK-01/D-02: StatusBadge maps done/translated to Badge with bg-emerald-600/80 text-white border-transparent; failed to Badge variant=destructive; queued/in_progress to Badge variant=secondary; quarantined to Badge with bg-amber-600/80 text-white border-transparent"
    - "RSK-01/D-02: LogViewer background uses bg-background border-border (was bg-bg-base border-[#2d3148]); timestamp spans use text-muted-foreground (was style={{color:'#6b7280'}}); scroll-to-bottom button uses Button variant=ghost size=icon"
    - "RSK-01/D-05: After this plan, JobLogs.tsx contains zero occurrences of bg-[#, text-[#, bg-bg-, text-text-, or style={{ with hex values"
  artifacts:
    - path: "frontend/src/pages/JobLogs.tsx"
      provides: "JobLogs page on shadcn primitives with loading/error/empty states"
      contains: "text-xl font-semibold"
    - path: "frontend/src/components/StatusBadge.tsx"
      provides: "Badge-based status renderer with semantic color classes"
      contains: "bg-emerald-600/80"
    - path: "frontend/src/components/LogViewer.tsx"
      provides: "Log viewer with shadcn tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "frontend/src/pages/JobLogs.tsx"
      to: "frontend/src/components/StatusBadge.tsx"
      via: "StatusBadge import"
      pattern: "StatusBadge"
    - from: "frontend/src/pages/JobLogs.tsx"
      to: "frontend/src/components/LogViewer.tsx"
      via: "LogViewer import"
      pattern: "LogViewer"
---

<objective>
Reskin JobLogs.tsx, LogViewer.tsx, and StatusBadge.tsx onto shadcn primitives and CSS-var tokens.
This is the first page in D-01's risk-sequenced order (simplest surface, fewest legacy tokens).

Purpose: Proves the token substitution pattern on a low-risk page before tackling higher-risk surfaces.
Shared components StatusBadge and LogViewer are reskinned here so subsequent pages that reuse them
inherit the clean tokens.

Output: JobLogs.tsx, StatusBadge.tsx, LogViewer.tsx — all legacy hex and bridge tokens replaced.
Build green after task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/src/pages/JobLogs.tsx
@frontend/src/components/LogViewer.tsx
@frontend/src/components/StatusBadge.tsx
@frontend/src/components/ui/badge.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin StatusBadge.tsx — map statuses to shadcn Badge variants</name>
  <files>frontend/src/components/StatusBadge.tsx</files>
  <action>
    Read StatusBadge.tsx in full before editing. Replace every legacy className and inline style with
    shadcn Badge primitives per the 15-UI-SPEC.md token map.

    Import Badge from "../components/ui/badge". Remove any raw div/span with hex colors.

    Status → Badge mapping (authoritative from UI-SPEC):
    - "done" | "translated": Badge with className="bg-emerald-600/80 text-white border-transparent"
    - "failed": Badge variant="destructive"
    - "queued" | "in_progress": Badge variant="secondary"
    - "quarantined": Badge with className="bg-amber-600/80 text-white border-transparent"
    - Any other/unknown status: Badge variant="outline" (safe default)

    The JobStatus type and component props are UNCHANGED. Only the rendered output changes.
    Keep the existing export and type definitions intact.

    Strict TS: no unused imports.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. StatusBadge.tsx contains no hex color strings. Each status maps to a
    Badge with the specified variant or className.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reskin LogViewer.tsx — bg-background, border-border, text-muted-foreground</name>
  <files>frontend/src/components/LogViewer.tsx</files>
  <action>
    Read LogViewer.tsx in full before editing. Apply the following substitutions per 15-UI-SPEC.md:

    - Container background: bg-bg-base → bg-background
    - Container border: border-[#2d3148] → border-border
    - Timestamp span: style={{color:"#6b7280"}} → className="text-muted-foreground"
    - Scroll-to-bottom button raw element: replace with Button variant="ghost" size="icon" from
      "../components/ui/button". Keep the existing onClick scroll behavior.
    - Any other text-[#6b7280] occurrences: → text-muted-foreground
    - Any other bg-[#...] occurrences: → use the appropriate CSS-var token from the UI-SPEC token map

    The log display logic, entry format, auto-scroll behavior, and all props are UNCHANGED.
    Strict TS: no unused imports. If Button is added as import, remove any old raw button element.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. LogViewer.tsx contains no hex color strings and no inline style with hex.
    Scroll-to-bottom uses Button variant="ghost" size="icon".
  </done>
</task>

<task type="auto">
  <name>Task 3: Reskin JobLogs.tsx — heading, breadcrumb, summary Card, loading/error/empty states</name>
  <files>frontend/src/pages/JobLogs.tsx</files>
  <action>
    Read JobLogs.tsx in full before editing. Apply all substitutions per 15-UI-SPEC.md §JobLogs:

    IMPORTS to add:
    - Button from "../components/ui/button"
    - Card, CardContent from "../components/ui/card"
    - Skeleton from "../components/ui/skeleton"

    BREADCRUMB: Replace raw button with Button variant="ghost" size="sm". Change
    text-accent → text-primary. Remove focus:outline-[#3b82f6] (Button applies ring automatically).
    Keep ChevronLeft icon and onClick navigate("/history").

    HEADING: text-lg font-semibold text-[#e2e6f0] → text-xl font-semibold text-foreground

    JOB SUMMARY STRIP: Replace the raw div with className="bg-bg-surface border border-[#2d3148]..."
    with Card + CardContent. Keep the flex layout inside CardContent. Replace text-[#6b7280] with
    text-muted-foreground throughout the strip.

    LOADING STATE: Replace the single <div className="text-xs text-[#6b7280] py-4">Loading…</div>
    with three Skeleton rows:
      <div className="flex flex-col gap-2">
        <Skeleton className="h-8 w-32" />
        <Skeleton className="h-10 w-full" />
        <Skeleton className="h-64 w-full" />
      </div>
    This loading block is gated on loading === true (same condition as before).

    ERROR STATE: Replace the raw div with inline style {{backgroundColor:"#2d1515",color:"#f87171"}}
    with: <div role="alert" className="px-4 py-3 text-sm rounded bg-destructive/10 text-destructive">
    {error}</div>
    (alert.tsx is not vendored in this project — use a semantic div per UI-SPEC fallback rule)

    EMPTY STATE (no entries, loading false, error null): Add after the error branch, before
    LogViewer:
      {!loading && !error && entries.length === 0 && (
        <p className="text-sm text-muted-foreground py-8 text-center">
          No log entries for this job.
        </p>
      )}
    LogViewer is still rendered (it handles its own empty display too) — add the empty message
    above it so it shows even when LogViewer renders nothing.

    Remove all remaining text-[#6b7280], focus:outline-[#3b82f6], and any other legacy tokens.
    Strict TS: no unused imports.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. JobLogs.tsx has text-xl heading, Button breadcrumb with text-primary,
    Card summary strip, Skeleton loading state, destructive error div, and empty-state paragraph.
    Zero hex strings in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → JSX render | job.source_path, job.trigger rendered as React text nodes (auto-escaped) |
| URL param jobId | Parsed with parseInt at component top; NaN/0 triggers setError("Invalid job ID") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering (XSS) | JobLogs.tsx job summary text | accept | job.source_path and trigger are JSX text nodes — React auto-escapes; no dangerouslySetInnerHTML |
| T-15-01-02 | Tampering (XSS) | LogViewer log entries | accept | Log entries rendered as text nodes — no innerHTML; styling changes do not affect this |
| T-15-01-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; all primitives vendored Phase 11 |
</threat_model>

<verification>
cd frontend && npm run build

Must: exit 0; tsc -b strict clean; vite build clean.
Spot-check: grep -n "bg-\[#\|text-\[#\|bg-bg-\|text-text-\|style={{" frontend/src/pages/JobLogs.tsx frontend/src/components/LogViewer.tsx frontend/src/components/StatusBadge.tsx
Must return: zero matches.
</verification>

<success_criteria>
- cd frontend && npm run build exits 0.
- StatusBadge uses Badge with semantic color classes per status (no raw hex divs).
- LogViewer uses bg-background, border-border, text-muted-foreground (no hex strings or inline style hex).
- JobLogs has text-xl heading, Button breadcrumb, Card summary strip, Skeleton loading, destructive error, empty-state paragraph.
- Zero hex strings in any of the three modified files.
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-01-SUMMARY.md when done.
</output>
