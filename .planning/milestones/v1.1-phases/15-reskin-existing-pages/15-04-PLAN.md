---
phase: 15-reskin-existing-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Settings.tsx
  - frontend/src/components/MaskedSecretInput.tsx
  - frontend/src/components/ConnectionTestButton.tsx
autonomous: true
requirements:
  - RSK-01

must_haves:
  truths:
    - "RSK-01/D-01/D-02: Settings heading is text-xl font-semibold text-foreground; SectionCard is replaced by Card/CardHeader/CardTitle/CardContent (no bg-bg-surface or border-[#2d3148])"
    - "RSK-01/D-02: All save buttons use Button variant=default className=w-full (was raw button with bg-accent hover:bg-[#2563eb] focus:outline-[#3b82f6]); disabled state via disabled prop on Button"
    - "RSK-01/D-02: TextField internal input uses shadcn Input component (was raw input with bg-bg-surface border-[#2d3148] focus:outline-[#3b82f6]); label uses text-xs text-muted-foreground"
    - "RSK-01/D-02: SectionHeading uses CardTitle or text-base font-semibold (was text-lg text-[#e2e6f0])"
    - "RSK-01/D-04: Settings UnreachableBanner uses className bg-amber-900/30 border border-amber-800 text-amber-400 (no inline style hex); RestartBanner uses text-amber-400 className (was style={{color:'#fbbf24'}})"
    - "RSK-01/D-04: Settings renders Skeleton section cards (3 × h-40 w-full) when settings === null and !unreachable"
    - "RSK-01/D-03: Settings.tsx uses import { toast } from 'sonner' and calls toast.success/toast.error; Toast/ToastState imports and useState removed"
    - "RSK-01/D-02: MaskedSecretInput internal input replaced with shadcn Input; eye-toggle with Button variant=ghost size=icon; no hex in component"
    - "RSK-01/D-02: ConnectionTestButton uses Button variant=outline size=sm; success state className bg-emerald-900/50 text-emerald-400 (was inline style hex); error state className bg-destructive/10 text-destructive"
    - "RSK-01/D-05: After this plan, Settings.tsx, MaskedSecretInput.tsx, and ConnectionTestButton.tsx contain zero hex strings"
    - "RSK-01/D-02: Auto-translate checkbox uses accent-primary className (was accent-[#3b82f6]); checkbox label uses text-sm text-foreground"
  artifacts:
    - path: "frontend/src/pages/Settings.tsx"
      provides: "Settings page on shadcn primitives with Skeleton loading, Sonner toast, Card sections"
      contains: "toast.success"
    - path: "frontend/src/components/MaskedSecretInput.tsx"
      provides: "Masked secret input using shadcn Input + Button"
      contains: "variant=\"ghost\""
    - path: "frontend/src/components/ConnectionTestButton.tsx"
      provides: "Connection test button on shadcn Button with semantic color classes"
      contains: "bg-emerald-900/50"
  key_links:
    - from: "frontend/src/pages/Settings.tsx"
      to: "sonner"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.success|toast\\.error"
    - from: "frontend/src/pages/Settings.tsx"
      to: "frontend/src/components/ui/card.tsx"
      via: "Card, CardHeader, CardTitle, CardContent imports"
      pattern: "CardContent"
---

<objective>
Reskin Settings.tsx, MaskedSecretInput.tsx, and ConnectionTestButton.tsx onto shadcn primitives.
Migrate Settings.tsx Toast usage to Sonner (D-03, second call-site).

Purpose: Fourth page in D-01 sequence; Settings is the most hand-rolled HTML surface outside
BibleEditor. The SectionCard → Card/CardContent swap, TextField → Input swap, and save button →
Button swap together eliminate the highest density of legacy tokens on a single page.

Output: Settings.tsx, MaskedSecretInput.tsx, ConnectionTestButton.tsx — zero legacy hex.
Build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/src/pages/Settings.tsx
@frontend/src/components/MaskedSecretInput.tsx
@frontend/src/components/ConnectionTestButton.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin MaskedSecretInput.tsx and ConnectionTestButton.tsx</name>
  <files>
    frontend/src/components/MaskedSecretInput.tsx
    frontend/src/components/ConnectionTestButton.tsx
  </files>
  <action>
    Read both files in full before editing.

    MASKEDSECRETINPUT:
    - Replace internal raw input with shadcn Input from "../components/ui/input".
    - Replace eye-toggle raw button with Button variant="ghost" size="icon" from
      "../components/ui/button". Keep the Eye/EyeOff lucide icons and masking logic.
    - Replace any border-[#2d3148], bg-bg-surface, text-[#e2e6f0], focus:outline-[#...]
      classNames with: the Input component handles styling automatically.
    - The label at the top: text-xs text-muted-foreground (was text-xs text-[#6b7280]).
    - All props (id, label, isSet, envLocked, value, onChange) are UNCHANGED.
    - Keep the "•••" placeholder display when isSet and value is empty.
    - Keep disabled behavior when envLocked is true.

    CONNECTIONTESTBUTTON:
    - Replace raw button element with Button variant="outline" size="sm" from
      "../components/ui/button". Keep disabled prop wired to the testing state.
    - Success state: replace style={{backgroundColor:"#14291e",color:"#4ade80"}} with
      className="bg-emerald-900/50 text-emerald-400" on the status message container.
    - Error state: replace style={{backgroundColor:"#2d1515",color:"#f87171"}} with
      className="bg-destructive/10 text-destructive".
    - Button text ("Test Connection" and loading text) is UNCHANGED.
    - The svc prop type, params prop, API call, and all logic are UNCHANGED.

    Strict TS: no unused imports after substitutions.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. MaskedSecretInput uses shadcn Input + Button ghost. ConnectionTestButton
    uses Button variant="outline" with semantic color classes. Zero hex in both files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reskin Settings.tsx — Card sections, Input fields, Button saves, Sonner, Skeleton</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <action>
    Read Settings.tsx in full before editing. This file has the highest token count — work through
    it systematically section by section.

    IMPORTS to add:
    - Card, CardHeader, CardTitle, CardContent from "../components/ui/card"
    - Input from "../components/ui/input"
    - Button from "../components/ui/button"
    - Skeleton from "../components/ui/skeleton"
    - { toast } from "sonner"

    IMPORTS to remove:
    - Toast from "../components/Toast"
    - type { ToastState } from "../components/Toast"

    SONNER MIGRATION (D-03):
    - Remove: const [toast, setToast] = useState<ToastState | null>(null)
    - Remove: const showToast = useCallback((t: Omit<ToastState, "id">) => { ... }, [])
    - Replace showToast({ message: "Settings saved.", variant: "success" }) with
      toast.success("Settings saved.")
    - Replace showToast({ message: "Failed to save settings. Check the server logs.", variant: "error" })
      with toast.error("Failed to save settings. Check the server logs.")
    - Remove: <Toast toast={toast} onDismiss={() => setToast(null)} /> from JSX
    - The showToast reference in saveSection must be updated: either inline the toast calls
      in saveSection directly, or keep showToast as a local non-state function that calls
      toast.success/toast.error. Either approach is fine as long as no ToastState state remains.
    - Also update the auto-translate section save which calls showToast directly.

    SECTIONCARD: Replace the SectionCard component with Card + CardContent:
      function SectionCard wraps with: <Card className="flex flex-col gap-4"><CardContent className="pt-6 flex flex-col gap-4">{children}</CardContent></Card>
    Or inline Card/CardContent at each usage — either approach is acceptable.

    SECTIONHEADING: Replace SectionHeading with CardTitle (inside a CardHeader) or a simple
    <h2 className="text-base font-semibold text-card-foreground">. Per UI-SPEC: section headings
    inside cards use text-base font-semibold. Switch from text-lg to text-base.

    TEXTFIELD: Replace the internal raw input with shadcn Input. The Input component handles
    border, background, focus ring automatically — remove the hand-rolled className on the input.
    Label: text-xs text-muted-foreground (was text-xs text-[#6b7280]).

    SAVE BUTTONS: Replace all six raw button elements with Button variant="default" className="w-full".
    Remove: bg-accent, hover:bg-[#2563eb], focus:outline-[#3b82f6] classes.
    The disabled prop wires to !{section}Dirty || {section}Saving as before.
    Button content: "{section}Saving ? 'Saving…' : 'Save Settings'" unchanged.

    UNREACHABLE BANNER: Replace style prop in UnreachableBanner with
    className="w-full mb-4 px-4 py-3 text-sm rounded bg-amber-900/30 border border-amber-800 text-amber-400"

    RESTART BANNER: Replace RestartBanner's style={{color:"#fbbf24"}} with
    className="flex items-center gap-2 text-sm text-amber-400"

    HEADING: text-lg font-semibold text-[#e2e6f0] → text-xl font-semibold text-foreground

    LOADING STATE: When settings === null && !unreachable, show:
      <div className="flex flex-col gap-4">
        <Skeleton className="h-8 w-48 mb-6" />
        <Skeleton className="h-40 w-full" />
        <Skeleton className="h-40 w-full" />
        <Skeleton className="h-40 w-full" />
      </div>
    Gate: render this block instead of the sections grid when settings is null.

    AUTO-TRANSLATE CHECKBOX: Replace accent-[#3b82f6] → accent-primary on the checkbox.
    Label text: text-sm text-[#e2e6f0] → text-sm text-foreground.
    Helper text: text-xs text-[#6b7280] → text-xs text-muted-foreground.

    PATH MAPPINGS section: Replace text-xs text-[#6b7280] → text-xs text-muted-foreground.

    All state variables, useEffect, useCallback, and API call logic are UNCHANGED.
    Strict TS: no unused imports after removing Toast/ToastState/showToast.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. Settings.tsx uses Card sections, Button saves, Skeleton loading,
    sonner toast (no Toast component), all text uses CSS-var token classes.
    Zero hex strings in Settings.tsx, MaskedSecretInput.tsx, and ConnectionTestButton.tsx.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form inputs → putSettings() | User-typed values sent to backend API; Input values are strings — no evaluation, no innerHTML |
| Sonner toast messages | Hardcoded strings ("Settings saved." / "Failed to save...") — no user input injected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering (XSS) | Settings form rendering | accept | All form values rendered by shadcn Input which uses value={...} attribute binding — React prevents injection |
| T-15-04-02 | Information Disclosure | MaskedSecretInput | accept | Masking behavior is unchanged — isSet/envLocked logic preserved; only the input element changes |
| T-15-04-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; sonner, card, input, button, skeleton all vendored Phase 11 |
</threat_model>

<verification>
cd frontend && npm run build

Must: exit 0; tsc -b strict clean; vite build clean.
Spot-check: grep -n "bg-\[#\|text-\[#\|bg-bg-\|text-text-\|style={{" frontend/src/pages/Settings.tsx frontend/src/components/MaskedSecretInput.tsx frontend/src/components/ConnectionTestButton.tsx
Must return: zero matches.
grep -n "Toast\|ToastState\|showToast" frontend/src/pages/Settings.tsx
Must return: zero matches (all legacy Toast references removed; local showToast wrapper if kept must call toast() not setToast()).
</verification>

<success_criteria>
- cd frontend && npm run build exits 0.
- Settings uses Card sections, Button saves, Input fields, Skeleton loading, Sonner toast.
- MaskedSecretInput uses shadcn Input + Button ghost for eye toggle.
- ConnectionTestButton uses Button outline with semantic color classes.
- Zero hex strings in all three files.
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-04-SUMMARY.md when done.
</output>
