---
id: 16-01
wave: 1
autonomous: true
requirement_ids: RSK-03
files_modified:
  - Dockerfile
  - docker-compose.yml
  - docker-compose.example.yml
  - .env.example
status: complete
---

<objective>
Rebuild the multi-stage production Docker image from scratch and verify the new v1.1
dashboard live on :6868 against the real *arr stack — the milestone acceptance gate.
</objective>

<tasks>
- [x] T-16-01-01 — Rebuild the multi-stage image (`docker build --no-cache`): node:20 Vite
  build → python:3.12-slim runtime; confirm EXIT 0 and that the built SPA ships in the image.
- [x] T-16-01-02 — Author `docker-compose.yml` (+ align `docker-compose.example.yml`,
  `.env.example`) mirroring the live deployment: image trezarr:local, /config + /data/media
  binds, PUID/PGID, TREZARR_LLM_* env with the API key interpolated from a gitignored .env.
- [x] T-16-01-03 — Deploy on :6868 via compose; confirm container healthy and `/api/health`
  returns 200; confirm SPA deep-link fallback serves index.html for new routes.
- [x] T-16-01-04 — Human live smoke test (8-item checklist, 16-HUMAN-UAT.md): all six nav
  routes, episode audio/subtitle badges from Bazarr, translate flow, Settings save,
  BibleEditor five tabs, SPA deep-link — confirmed in the browser by the operator.
</tasks>

<must_haves>
- truths:
  - RSK-03: production image builds with `--no-cache` and runs the v1.1 dashboard on :6868
    against the live *arr stack, with all six routes, badges, translate flow, and deep-link
    fallback confirmed working (verified: build EXIT 0 + operator smoke test passed 8/8).
</must_haves>
