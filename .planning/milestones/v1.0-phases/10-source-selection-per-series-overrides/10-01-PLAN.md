---
phase: 10-source-selection-per-series-overrides
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/arr/test_bazarr.py
  - tests/source_selection/__init__.py
  - tests/source_selection/test_rank.py
  - tests/source_selection/test_resolve.py
  - tests/discover/test_gap.py
  - tests/db/test_migration_0003.py
  - tests/llm/test_client.py
  - tests/web/test_bible_api.py
  - tests/arr/test_arr_discovery.py
autonomous: true
requirements:
  - INTG-02
  - SRC-01
  - SRC-02
  - SVC-05

must_haves:
  truths:
    - "uv run pytest tests/arr/test_bazarr.py tests/source_selection/ tests/discover/test_gap.py tests/db/test_migration_0003.py tests/llm/test_client.py tests/web/test_bible_api.py -q exits non-zero (RED suite) before any Phase-10 implementation"
    - "Every xfail stub names the D-number or requirement it guards — each test has a clear target"
    - "No existing tests are broken by adding stubs (full suite still passes on pre-existing GREEN tests)"
  artifacts:
    - path: tests/arr/test_bazarr.py
      provides: Bazarr inventory client stubs (INTG-02)
    - path: tests/source_selection/test_rank.py
      provides: SRC-02 ranking algorithm stubs (rank_sources, sort_key cases)
    - path: tests/source_selection/test_resolve.py
      provides: D-112 resolve_effective_settings stubs (SVC-05)
    - path: tests/discover/test_gap.py
      provides: D-110 Case 1.5 stubs (source-language upgrade + no-loop + foreign-vi-unchanged)
    - path: tests/db/test_migration_0003.py
      provides: Migration 0003 up/down stubs (SVC-05 storage)
    - path: tests/llm/test_client.py
      provides: D-113 per-call model override stub
    - path: tests/web/test_bible_api.py
      provides: PATCH /series/{id}/overrides stub (SVC-05 route)
    - path: tests/arr/test_arr_discovery.py
      provides: "D-108 original_language capture stubs for Sonarr + Radarr (authoritative D-108 coverage — supersedes RESEARCH.md Wave-0 references to test_sonarr.py/test_radarr.py; those files are NOT created; all D-108 stubs live here)"
  key_links:
    - from: tests/arr/test_bazarr.py
      to: trezarr/arr/bazarr.py
      via: "import BazarrClient, BazarrError, SubtitleEntry"
      pattern: "from trezarr.arr.bazarr import"
    - from: tests/source_selection/test_rank.py
      to: trezarr/source_selection/rank.py
      via: "import rank_sources, normalize_original_language"
      pattern: "from trezarr.source_selection.rank import"
    - from: tests/discover/test_gap.py
      to: trezarr/discover/gap.py
      via: "import is_eligible"
      pattern: "from trezarr.discover.gap import"
---

<objective>
Wave 0 RED test scaffold for all Phase-10 modules. Creates the failing test surface before any
implementation, ensuring the Nyquist gate is satisfied and every subsequent plan has a
deterministic verification target.

Purpose: Establish the full test surface for Phase 10 (INTG-02, SRC-01, SRC-02, SVC-05, D-108,
D-110, D-112, D-113, D-114) as xfail stubs. All stubs MUST be red (xfail XPASS-only OK once
the module exists). Existing GREEN tests must stay GREEN.

D-108 coverage note: RESEARCH.md Wave-0 list names tests/arr/test_sonarr.py and
tests/arr/test_radarr.py as separate files. These are NOT created. D-108 stubs live in
tests/arr/test_arr_discovery.py (the existing arr discovery test) as two appended stubs
(test_sonarr_captures_original_language, test_radarr_captures_original_language). This is
consistent with the existing project pattern and is the authoritative D-108 coverage location.
10-VALIDATION.md Wave-0 list reflects this decision.

Output: 8 new/extended test files; 1 new test package (tests/source_selection/).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-source-selection-per-series-overrides/10-CONTEXT.md
@.planning/phases/10-source-selection-per-series-overrides/10-RESEARCH.md
@.planning/phases/10-source-selection-per-series-overrides/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs — Bazarr client, source_selection package, gap Case 1.5</name>
  <files>
    tests/arr/test_bazarr.py,
    tests/source_selection/__init__.py,
    tests/source_selection/test_rank.py,
    tests/source_selection/test_resolve.py,
    tests/discover/test_gap.py
  </files>

  <read_first>
    - tests/arr/test_arr_discovery.py (existing xfail pattern + pytest-httpx fixture structure)
    - tests/discover/test_scan.py (existing gap/scan test patterns + EligibleItem usage)
    - tests/arr/__init__.py (confirm package exists)
    - trezarr/arr/__init__.py (DiscoveryError, _normalize_arr_host — imports the stubs will target)
    - .planning/phases/10-source-selection-per-series-overrides/10-RESEARCH.md §"Validation Architecture" and §"Deep Dive: Idempotency Under Source-Language Change"
  </read_first>

  <behavior>
    - test_bazarr.py: xfail stubs for test_fetch_episodes_parses_subtitle_entries, test_fetch_movies_parses_subtitle_entries, test_bazarr_error_on_4xx, test_bazarr_error_on_connection_failure, test_path_mapping_applied_to_subtitle_path — each imports from trezarr.arr.bazarr (ImportError triggers xfail). Recorded fixture response: episode obj with subtitles=[{code2:"ko", code3:"kor", path:"/tv/Show/S01E01.ko.srt", forced:false, hi:false}].
    - tests/source_selection/__init__.py: empty package init.
    - test_rank.py: xfail stubs for test_korean_drama (rank_sources(["en","ko"], "ko") == ["ko","en"]), test_us_show (rank_sources(["ko","en"], "en") == ["en","ko"]), test_tier_order (rank_sources(["zh","en"], None) == ["zh","en"]), test_fallback_chain, test_normalize_original_language_english, test_normalize_original_language_korean.
    - test_resolve.py: xfail stubs for test_per_series_source_override_wins, test_null_inherits_global_source, test_null_inherits_global_model, test_none_series_dto_uses_global.
    - test_gap.py: ADD (do not replace existing) stubs test_source_upgrade_eligible (Case 1.5: vi exists from ledger entry for en, new source ko → eligible=True, reason contains "richer source"), test_foreign_vi_unchanged (vi exists, no ledger entry at all → eligible=False, D-26 intact), test_no_loop_after_upgrade (after ko translation ledger recorded, same ko scan → already_done Case 2).
    - All stubs use xfail(strict=False, raises=(ImportError, AssertionError, TypeError)) matching existing project pattern.
  </behavior>

  <action>
    Create tests/source_selection/__init__.py as an empty file.

    Create tests/arr/test_bazarr.py. Mirror the import guard pattern from tests/arr/test_arr_discovery.py: wrap all imports in try/except ImportError with xfail(strict=False). Target imports: from trezarr.arr.bazarr import BazarrClient, BazarrError, SubtitleEntry, BazarrInventoryItem. Define a JSON fixture dict `EPISODE_FIXTURE` representing one episode object with subtitles=[{name:"Korean", code2:"ko", code3:"kor", path:"/tv/Show/S01E01.ko.srt", forced:False, hi:False}] matching the verified Bazarr response shape from RESEARCH.md §"Confirmed Endpoints and Response Shapes". Use pytest-httpx respx or a simple mock — prefer a conftest httpx mock that returns {"data": [EPISODE_FIXTURE]}. Five stubs, each decorated @pytest.mark.xfail(strict=False, raises=(ImportError, AssertionError, TypeError)).

    Create tests/source_selection/test_rank.py. Imports target: from trezarr.source_selection.rank import rank_sources, normalize_original_language. Six xfail stubs: (1) K-drama assertion rank_sources(["en","ko"], "ko") == ["ko","en"] per D-107 sort_key; (2) US-show assertion rank_sources(["ko","en"], "en") == ["en","ko"] per D-107 native-Tier3 gets sort_key=0.0; (3) tier-order assertion rank_sources(["zh","en"], None) == ["zh","en"] (Tier-1 beats Tier-3 with no bias); (4) fallback chain: rank_sources(["fr","en"], None) — fr gets default Tier-2, en gets Tier-3, so ["fr","en"]; (5) normalize_original_language("English") == "en"; (6) normalize_original_language("Korean") == "ko".

    Create tests/source_selection/test_resolve.py. Imports target: from trezarr.source_selection.resolve import resolve_effective_settings. Four xfail stubs: (1) series_dto with source_lang_override=["ko","en"] → source_priority==["ko","en"] wins over global; (2) series_dto with source_lang_override=None → falls back to settings.source_lang_priority; (3) series_dto with model_override="gpt-4o" → model=="gpt-4o"; (4) resolve_effective_settings(None, settings) → returns (settings.source_lang_priority, None, settings.llm_model). Use a minimal fake TrezarrSettings stub (not the real class — avoids env-var requirement in stub) or use monkeypatch. Prefer constructing a real TrezarrSettings with environment variables set via monkeypatch.

    Extend tests/discover/test_gap.py with three new xfail stubs appended at the bottom. Do NOT modify existing tests. Import from trezarr.discover.gap (already importable — gap.py exists). Import from trezarr.output.ledger_sqla (already exists). Stubs: (1) test_source_upgrade_eligible — creates a mock ledger where check("ko.srt") returns None AND check_by_output_path("vi.srt") returns a LedgerEntry(source_path="en.srt", output_path="vi.srt", status="done") — asserts is_eligible returns (True, reason_containing_"richer source"); (2) test_foreign_vi_unchanged — check("ko.srt") returns None AND check_by_output_path("vi.srt") returns None (truly foreign vi) — asserts is_eligible returns (False, _); (3) test_no_loop_after_upgrade — check("ko.srt") returns LedgerEntry(status="done"), vi_path exists — asserts is_eligible returns (False, reason_containing_"already done") (Case 2 fires). These stubs are xfail because check_by_output_path does not exist on ledger yet.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/arr/test_bazarr.py tests/source_selection/ tests/discover/test_gap.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>

  <done>
    All new stubs are xfail (XFAIL or XPASS never FAILED); existing discover/test_gap.py tests remain GREEN; tests/source_selection/ package is created and importable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED stubs — migration 0003, LLMClient model override, PATCH overrides route, original_language capture</name>
  <files>
    tests/db/test_migration_0003.py,
    tests/llm/test_client.py,
    tests/web/test_bible_api.py,
    tests/arr/test_arr_discovery.py
  </files>

  <read_first>
    - tests/db/test_migrations.py (existing migration up/down test pattern using in-memory SQLite)
    - tests/llm/test_client_tiers.py (existing LLMClient test structure + mock OpenAI client pattern)
    - tests/web/test_bible_api.py (existing Phase-8 PATCH /series/{id}/register test — the exact pattern to extend)
    - tests/arr/test_arr_discovery.py (existing sonarr/radarr discovery test — where D-108 original_language stubs go; this is the AUTHORITATIVE D-108 test file, not tests/arr/test_sonarr.py or tests/arr/test_radarr.py)
    - alembic/versions/0002_job_queue.py (migration template to understand test expectations)
  </read_first>

  <behavior>
    - test_migration_0003.py: stubs test_upgrade_adds_columns (run upgrade() then query series table schema — both columns exist and are nullable), test_downgrade_removes_columns (downgrade() then columns absent). Pattern from test_migrations.py.
    - test_client.py additions: xfail stub test_per_call_model_override — construct LLMClient with model="global-model", call with model="override-model" — mock verifies the OpenAI parse() call received model="override-model", not "global-model". Stub for test_call_uses_global_model_when_no_override.
    - test_bible_api.py additions: xfail stub test_patch_overrides_stores_source_and_model — PATCH /api/bible/series/1/overrides with body {source_lang_override:["ko","en"], model_override:"gpt-4o"} → 200 + returned DTO has correct fields. Stub for test_patch_overrides_clears_with_null.
    - test_arr_discovery.py additions: xfail stubs test_sonarr_captures_original_language and test_radarr_captures_original_language — fixture response includes originalLanguage:{id:1,name:"Korean"} and assert MediaItem.original_language == "Korean". These target existing modules (sonarr.py/radarr.py) so raises=(AssertionError,) (module imports fine, field doesn't exist yet). These two stubs are the complete D-108 Wave-0 coverage; no separate test_sonarr.py or test_radarr.py file is needed.
  </behavior>

  <action>
    Create tests/db/test_migration_0003.py. Read tests/db/test_migrations.py for the engine + Alembic config setup pattern. Create the same async in-memory SQLite engine + alembic config fixture. test_upgrade_adds_columns: run alembic upgrade to head (which includes 0003 after Plan 10-03 ships it), then inspect the series table via SQLAlchemy introspection (or a raw SELECT to check column_info) — verify "source_lang_override" and "model_override" columns exist. For now as a stub: attempt to import the migration module from alembic.versions module_0003 and call upgrade() on a test engine — xfail because the module doesn't exist yet. test_downgrade_removes_columns: run downgrade() then verify columns absent.

    Extend tests/llm/test_client.py (read file first for existing structure). Append two xfail stubs at the bottom. test_per_call_model_override: construct LLMClient(settings) where settings.llm_model="global-model", patch the internal _client.chat.completions.parse (or use the existing respx/httpx mock pattern from test_client_tiers.py), call await client.call([...], model="override-model"), assert the mock received model="override-model". test_call_uses_global_model_when_no_override: call with no model kwarg, assert mock received model="global-model". Both stubs raise xfail because call() does not accept a model kwarg yet.

    Extend tests/web/test_bible_api.py (read file first). Append two xfail stubs after the existing PATCH /register tests. test_patch_overrides_stores_source_and_model: async test with the existing app_with_db fixture (or the conftest fixture that sets up the FastAPI test client + in-memory DB). POST to /api/bible/series/1/overrides with {source_lang_override:["ko","en"], model_override:"gpt-4o"}, assert 200 and response body has source_lang_override==["ko","en"] and model_override=="gpt-4o". test_patch_overrides_clears_with_null: POST with {source_lang_override:null, model_override:null}, assert 200 and DTO shows null fields. Both raise xfail because the route doesn't exist yet.

    Extend tests/arr/test_arr_discovery.py. Append stubs test_sonarr_captures_original_language and test_radarr_captures_original_language. Each constructs a fixture response that includes originalLanguage:{id:1, name:"Korean"} in the series/movie dict. Assert the returned MediaItem has original_language == "Korean". xfail(strict=False, raises=(AssertionError, AttributeError)) because MediaItem.original_language doesn't exist yet. Note: these two stubs are the complete D-108 Wave-0 coverage — do NOT create tests/arr/test_sonarr.py or tests/arr/test_radarr.py as separate files.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/db/test_migration_0003.py tests/llm/test_client.py tests/web/test_bible_api.py tests/arr/test_arr_discovery.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>

  <done>
    All new stubs are xfail; pre-existing tests in extended files remain GREEN; uv run pytest tests/ -q shows no new FAILED tests.

    After all stubs are confirmed xfail and full suite is GREEN, update 10-VALIDATION.md frontmatter:
    set `nyquist_compliant: true` and `wave_0_complete: true`. This signals that the Nyquist gate
    is satisfied and Wave 1 plans (10-02, 10-03) may proceed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → test code | Test fixtures contain hardcoded fixture data that must not be mistaken for production paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — zero install tasks |
| T-10-01 | Information Disclosure | test fixture JSON | accept | Fixtures contain synthetic data only; no real credentials or paths in test files |
</threat_model>

<verification>
Full suite passes (existing GREEN tests not broken):
`uv run pytest tests/ -q`

New stubs are XFAIL (expected):
`uv run pytest tests/arr/test_bazarr.py tests/source_selection/ tests/discover/test_gap.py tests/db/test_migration_0003.py tests/llm/test_client.py tests/web/test_bible_api.py tests/arr/test_arr_discovery.py -v 2>&1 | grep -E "XFAIL|XPASS|FAILED|ERROR"`

Zero FAILED or ERROR lines expected.
</verification>

<success_criteria>
- tests/source_selection/ package created with __init__.py
- tests/arr/test_bazarr.py created with 5 xfail stubs covering INTG-02
- tests/source_selection/test_rank.py created with 6 xfail stubs covering SRC-01/SRC-02
- tests/source_selection/test_resolve.py created with 4 xfail stubs covering D-112
- tests/discover/test_gap.py extended with 3 xfail stubs covering D-110 Case 1.5
- tests/db/test_migration_0003.py created with 2 xfail stubs covering migration 0003
- tests/llm/test_client.py extended with 2 xfail stubs covering D-113
- tests/web/test_bible_api.py extended with 2 xfail stubs covering PATCH overrides (D-114)
- tests/arr/test_arr_discovery.py extended with 2 xfail stubs covering D-108 original_language capture (authoritative; no separate test_sonarr.py / test_radarr.py)
- uv run pytest tests/ -q exits with at most xfail results; zero FAILED
- 10-VALIDATION.md frontmatter updated: nyquist_compliant: true, wave_0_complete: true
</success_criteria>

<output>
Create `.planning/phases/10-source-selection-per-series-overrides/10-01-SUMMARY.md` when done.

After full suite is GREEN and all new stubs are xfail, update `.planning/phases/10-source-selection-per-series-overrides/10-VALIDATION.md` frontmatter:
- Set `nyquist_compliant: true`
- Set `wave_0_complete: true`
</output>
