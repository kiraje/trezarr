---
phase: 10-source-selection-per-series-overrides
plan: "02"
type: execute
wave: 1
depends_on:
  - "10-01"
files_modified:
  - trezarr/arr/bazarr.py
  - trezarr/arr/sonarr.py
  - trezarr/arr/radarr.py
  - trezarr/source_selection/__init__.py
  - trezarr/source_selection/rank.py
  - trezarr/source_selection/resolve.py
  - trezarr/output/ledger_sqla.py
  - trezarr/discover/gap.py
  - trezarr/discover/scan.py
  - trezarr/config.py
autonomous: true
requirements:
  - INTG-02
  - SRC-01
  - SRC-02
  - SVC-05

must_haves:
  truths:
    - "BazarrClient.fetch_episodes() and fetch_movies() return typed SubtitleEntry lists from Bazarr JSON (per D-103)"
    - "Bazarr-reported subtitle paths are reconciled via apply_path_mapping (D-105)"
    - "rank_sources() returns the relationally-richest source first; a Korean drama's ko beats en; a US show's en beats ko (D-107)"
    - "MediaItem carries original_language from Sonarr/Radarr originalLanguage.name (D-108)"
    - "is_eligible Case 1.5 fires when Trezarr wrote the vi sidecar from a different source — re-translate is triggered, not skipped (D-110)"
    - "D-26 (foreign vi guard) still fires when the vi sidecar was NOT written by Trezarr (check_by_output_path returns None)"
    - "No re-translation loop: after ko translation, next scan Case 2 fires (already done + hash check)"
    - "Bazarr disabled degrades to filesystem glob (D-104)"
    - "Phase-10 TrezarrSettings fields are present: source_lang_priority default updated, bazarr_use_inventory flag"
  artifacts:
    - path: trezarr/arr/bazarr.py
      provides: BazarrClient, BazarrError, SubtitleEntry, BazarrInventoryItem
      exports: ["BazarrClient", "BazarrError", "SubtitleEntry", "BazarrInventoryItem"]
    - path: trezarr/source_selection/rank.py
      provides: rank_sources(), normalize_original_language(), sort_key()
      exports: ["rank_sources", "normalize_original_language"]
    - path: trezarr/source_selection/resolve.py
      provides: resolve_effective_settings()
      exports: ["resolve_effective_settings"]
    - path: trezarr/output/ledger_sqla.py
      provides: check_by_output_path() method on LedgerSQLA
      contains: "check_by_output_path"
    - path: trezarr/discover/gap.py
      provides: Case 1.5 branch in is_eligible
      contains: "richer source"
  key_links:
    - from: trezarr/discover/gap.py
      to: trezarr/output/ledger_sqla.py
      via: "ledger.check_by_output_path(str(vi_path))"
      pattern: "check_by_output_path"
    - from: trezarr/discover/scan.py
      to: trezarr/arr/bazarr.py
      via: "BazarrClient.fetch_episodes() in select_source_for_item"
      pattern: "BazarrClient"
    - from: trezarr/source_selection/rank.py
      to: trezarr/arr/sonarr.py
      via: "normalize_original_language(media_item.original_language)"
      pattern: "normalize_original_language"
---

<objective>
Bazarr inventory + source-agnostic + relational-fidelity ranking vertical slice (INTG-02, SRC-01,
SRC-02, SVC-05). Implements the full backend source-selection path: Bazarr client → original_language
capture → rank_sources → select_source_for_item → Case 1.5 idempotency seam in gap.py.

This is the highest-regression-risk plan in the phase. The D-110 Case 1.5 seam (source-language
upgrade not blocked by foreign-vi guard) is the primary correctness target. All three D-26/D-27/
D-28 guards must still hold under the new code path.

SVC-05 coverage note: Task 2 (check_by_output_path + gap.py Case 1.5) is an SVC-05 idempotency
prerequisite — D-110 is both an SRC invariant and a required correctness condition for per-series
source overrides to function without infinite re-translation loops.

Purpose: Turn the D-104 "Bazarr is knowledge, filesystem is bytes" architecture into running code.
Output: All INTG-02/SRC-01/SRC-02/SVC-05 test stubs from Plan 10-01 turn GREEN.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-source-selection-per-series-overrides/10-CONTEXT.md
@.planning/phases/10-source-selection-per-series-overrides/10-RESEARCH.md
@.planning/phases/10-source-selection-per-series-overrides/10-PATTERNS.md
@.planning/phases/10-source-selection-per-series-overrides/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BazarrClient + source_selection package + original_language capture (INTG-02, SRC-01, SRC-02, D-108)</name>
  <files>
    trezarr/arr/bazarr.py,
    trezarr/arr/sonarr.py,
    trezarr/arr/radarr.py,
    trezarr/source_selection/__init__.py,
    trezarr/source_selection/rank.py,
    trezarr/source_selection/resolve.py,
    trezarr/config.py
  </files>

  <read_first>
    - trezarr/arr/__init__.py (full file — DiscoveryError, _normalize_arr_host — the template for BazarrError)
    - trezarr/arr/sonarr.py (full file — MediaItem dataclass, build_sonarr_client, discover_sonarr_items — the exact pattern to mirror for BazarrClient and for adding original_language to MediaItem)
    - trezarr/arr/radarr.py (lines 155–185 — the arr_metadata snapshot capture block where original_language goes)
    - trezarr/web/routes/test_connection.py (lines 172–214 — test_bazarr httpx + X-Api-Key call shape, the concrete template for BazarrClient.fetch_episodes)
    - trezarr/translate/reconcile.py (lines 1–50 — pure function module structure for rank.py)
    - trezarr/config.py (full file — find source_lang_priority (D-25 field) + bazarr_* fields; add Phase-10 group below the Phase-7 / bazarr_ fields)
    - .planning/phases/10-source-selection-per-series-overrides/10-PATTERNS.md §BazarrClient, §rank.py, §resolve.py
    - .planning/phases/10-source-selection-per-series-overrides/10-RESEARCH.md §"Deep Dive: Bazarr API" and §"Deep Dive: Relational-Fidelity Ranking"
  </read_first>

  <behavior>
    BazarrClient:
    - BazarrError(Exception) mirrors DiscoveryError
    - SubtitleEntry dataclass: code2: str, code3: str, path: str, forced: bool = False, hi: bool = False
    - BazarrInventoryItem dataclass: arr_id: int, path: str, subtitles: list[SubtitleEntry]
    - BazarrClient.__init__(settings): self._api_key = settings.bazarr_api_key.get_secret_value() (D-11), self._base_url = f"http://{_normalize_arr_host(settings.bazarr_host)}:{settings.bazarr_port}", self._host = settings.bazarr_host (for error display only)
    - fetch_episodes(sonarr_series_id: int) -> list[BazarrInventoryItem]: GET /api/episodes with params=[("seriesid[]", sonarr_series_id)], headers={"X-API-KEY": self._api_key}; wraps httpx.RequestError and HTTPStatusError in BazarrError with display_host (WR-01, D-11 — never log api_key)
    - fetch_movies(radarr_movie_id: int) -> list[BazarrInventoryItem]: same pattern, params=[("radarrid[]", radarr_movie_id)]
    - _parse_inventory_item(item: dict) -> BazarrInventoryItem: parses "sonarrEpisodeId" or "radarrId" as arr_id, "path" as path, "subtitles" list into SubtitleEntry objects
    - NO download endpoint calls (INTG-02 pure-read guarantee)

    rank.py:
    - _TIER dict with Tier-1 (zh/cmn/zho, ko/kor, ja/jpn, th/tha), Tier-2 (id/ind, ms/msa, hi/hin, ta/tam, ar/ara, tr/tur), Tier-3 (en/eng). _DEFAULT_TIER = 2.
    - _ORIG_LANG_NAME_TO_CODE2 dict: at minimum english→en, korean→ko, japanese→ja, chinese→zh, thai→th, vietnamese→vi, cantonese→zh, mandarin→zh (Pitfall 5 in RESEARCH.md), french→fr, german→de, spanish→es, portuguese→pt, italian→it, russian→ru, arabic→ar, hindi→hi, indonesian→id, malay→ms, tamil→ta, turkish→tr.
    - sort_key(lang: str, original_language: str | None) -> float: t = _TIER.get(lang.lower(), _DEFAULT_TIER); if original_language and lang.lower() == original_language.lower(): return 0.0 if t == 3 else t - 0.5; return float(t). This is the "final algorithm" from RESEARCH.md §Deep Dive Ranking.
    - rank_sources(available: list[str], original_language: str | None) -> list[str]: sorted(set(lang.lower() for lang in available), key=lambda lang: (sort_key(lang, original_language), lang))
    - normalize_original_language(name: str | None) -> str | None: return _ORIG_LANG_NAME_TO_CODE2.get(name.lower()) if name else None

    resolve.py (stub only — full wiring in Plan 10-03):
    - resolve_effective_settings(series_dto: "SeriesDTO | None", settings: "TrezarrSettings") -> tuple[list[str], str | None, str]: per D-112 pure function. If series_dto is None, return (settings.source_lang_priority, None, settings.llm_model). Else: source_priority = series_dto.source_lang_override if not None else settings.source_lang_priority; register = series_dto.register_value; model = series_dto.model_override if not None else settings.llm_model. Return (source_priority, register, model). Use TYPE_CHECKING imports only (no runtime ORM import).

    sonarr.py + radarr.py (D-108):
    - Add original_language: str | None = None field to MediaItem dataclass (after runtime field per PATTERNS.md)
    - In discover_sonarr_items: original_language=series.get("originalLanguage", {}).get("name") added to MediaItem construction
    - In discover_radarr_items: original_language=movie.get("originalLanguage", {}).get("name") added to MediaItem construction

    config.py (Phase-10 settings group):
    - Add bazarr_use_inventory: bool = True (controls whether to query Bazarr inventory or fall back to filesystem glob; D-104)
    - Confirm source_lang_priority exists (D-25) — it does; do NOT change its default (plan 10-02 handles source selection separately from config default)
  </behavior>

  <action>
    Create trezarr/arr/bazarr.py. Read trezarr/arr/__init__.py first to copy the _normalize_arr_host import and DiscoveryError pattern exactly. Read trezarr/web/routes/test_connection.py lines 172–214 to copy the URL construction pattern. The file must: (a) import _normalize_arr_host from trezarr.arr, (b) use TYPE_CHECKING for TrezarrSettings to avoid circular import, (c) define BazarrError, SubtitleEntry dataclass, BazarrInventoryItem dataclass, (d) BazarrClient class with __init__, fetch_episodes, fetch_movies, _parse_inventory_item (private). For Pitfall 2 (array params): params=[("seriesid[]", sonarr_series_id)] NOT a dict. For WR-01: use display_host = _normalize_arr_host(self._host) in error messages, never self._api_key. Both HTTP errors (HTTPStatusError) and transport errors (RequestError) map to BazarrError.

    Create trezarr/source_selection/__init__.py exporting rank_sources and resolve_effective_settings from __all__. One-line docstring.

    Create trezarr/source_selection/rank.py. No I/O, no imports beyond __future__ annotations. Full _TIER dict, full _ORIG_LANG_NAME_TO_CODE2 dict covering at minimum: english, korean, japanese, chinese, thai, vietnamese, cantonese, mandarin, french, german, spanish, portuguese, italian, russian, arabic, hindi, indonesian, malay, tamil, turkish. sort_key() and rank_sources() and normalize_original_language() as specified in behavior. Sort is deterministic: (sort_key, lang) secondary key ensures alphabetical tie-breaking within same tier.

    Create trezarr/source_selection/resolve.py. Imports: from __future__ import annotations; from typing import TYPE_CHECKING; if TYPE_CHECKING: from trezarr.bible.dto import SeriesDTO; from trezarr.config import TrezarrSettings. No ORM imports. resolve_effective_settings() as specified. Key note: access series_dto.register_value (Python attribute, not "register" — the alias from PATTERNS.md §resolve.py).

    Modify trezarr/arr/sonarr.py. Read the full file first. Find the MediaItem dataclass (around line 52–99). Add original_language: str | None = None after the runtime field (or as the last field). Find the MediaItem(...) construction in discover_sonarr_items (around lines 192–203). Add original_language=series.get("originalLanguage", {}).get("name") per PATTERNS.md §sonarr.py.

    Modify trezarr/arr/radarr.py. Same pattern. Read lines 155–185. Add original_language: str | None = None to MediaItem if MediaItem is defined in radarr.py, OR add the capture field to the construction site. If radarr re-imports MediaItem from sonarr (per Phase-3 STATE.md decision), only the construction site changes.

    Modify trezarr/config.py. Read the full file. Find the Bazarr settings section (bazarr_host, bazarr_port, bazarr_api_key, bazarr_enabled — D-76). Add bazarr_use_inventory: bool = True under a "# Phase 10" comment in the same section. Do NOT change source_lang_priority default (en remains present as fallback per D-106; the ranking algorithm operates on top of the discovery-layer selection).
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/arr/test_bazarr.py tests/source_selection/test_rank.py tests/source_selection/test_resolve.py tests/arr/test_arr_discovery.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>

  <done>
    tests/arr/test_bazarr.py XPASS or PASS (all 5 stubs); tests/source_selection/test_rank.py XPASS or PASS (6 stubs); tests/source_selection/test_resolve.py XPASS or PASS (4 stubs); tests/arr/test_arr_discovery.py original_language stubs XPASS or PASS; uv run pytest tests/ -q shows no new FAILED tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LedgerSQLA.check_by_output_path + gap.py Case 1.5 + scan.py Bazarr integration (D-110, SRC-01/02, SVC-05)</name>
  <files>
    trezarr/output/ledger_sqla.py,
    trezarr/discover/gap.py,
    trezarr/discover/scan.py
  </files>

  <read_first>
    - trezarr/output/ledger_sqla.py (full file — check() method at lines 68–86 is the exact pattern for check_by_output_path(); _row_to_entry() helper; ProcessedFile model import)
    - trezarr/discover/gap.py (full file — is_eligible() Cases 0–5, the LedgerProtocol usage, current Case 1 logic at ~lines 107–112)
    - trezarr/discover/scan.py (full file — find_source_sub(), scan_for_eligible_items(), EligibleItem dataclass, ScanStats dataclass — where to add source_upgraded field and select_source_for_item)
    - trezarr/output/_ledger_protocol.py (or find protocol class — add check_by_output_path abstract method to keep protocol correct)
    - trezarr/translate/path_mapping.py (or wherever apply_path_mapping is defined — needed for the traversal guard step in select_source_for_item)
    - trezarr/discover/scan.py or trezarr/translate/engine.py (find assert_within_media_roots or the INTG-03 traversal guard function — used in the path validation step)
    - .planning/phases/10-source-selection-per-series-overrides/10-PATTERNS.md §gap.py, §ledger_sqla.py, §scan.py
    - .planning/phases/10-source-selection-per-series-overrides/10-RESEARCH.md §"Deep Dive: Idempotency Under Source-Language Change"
  </read_first>

  <behavior>
    ledger_sqla.py:
    - Add check_by_output_path(self, output_path: str | Path) -> LedgerEntry | None method on LedgerSQLA class
    - Pattern is identical to check() but WHERE ProcessedFile.output_path == key instead of ProcessedFile.source_path == key
    - Also add check_by_output_path to the LedgerProtocol / abstract base (wherever the protocol is defined — find it by reading ledger_sqla.py imports)

    gap.py — Case 1.5 (D-110, highest regression-risk seam):
    - Current Case 1: if vi_path.exists() and entry is None: return (False, "foreign vi sidecar…")
    - New Case 1+1.5: if vi_path.exists() and entry is None: first call prior_entry = await ledger.check_by_output_path(str(vi_path)); if prior_entry is not None → return (True, "richer source available — re-translating"); else → return (False, "foreign vi sidecar…") [D-26 preserved]
    - D-27 and D-28 (Case 2, Case 3) are UNCHANGED — only Case 1 grows a sub-branch
    - No loop: after re-translation with ko, Case 2 fires (done + hash) on next scan — already tested in stub test_no_loop_after_upgrade

    scan.py — source selection extensions:
    - Add source_upgraded: int = 0 to ScanStats dataclass (after the existing counters)
    - Add source_upgraded: bool = False to EligibleItem dataclass
    - In scan_for_eligible_items reason classifier: add branch elif "richer source" in reason.lower(): stats.source_upgraded += 1 (not skipped — this is eligible; do NOT increment already_done)
    - Add select_source_for_item(media_item, settings, bazarr_inventory=None) -> str | None: implements the D-109 fallback chain: (1) per-series override (pass as arg), (2) rank_sources over Bazarr inventory union filesystem-detected langs biased by media_item.original_language, (3) global source_lang_priority, (4) first available.

    select_source_for_item — path safety (D-105, T-10-03):
    - For each SubtitleEntry in bazarr_inventory (if provided and settings.bazarr_use_inventory is True), apply path mapping before any filesystem use: mapped_path = apply_path_mapping(entry.path, settings.path_mappings). Then validate: assert_within_media_roots(mapped_path, settings.media_roots) (or the existing INTG-03 traversal guard — find the exact function name in the codebase). Discard entries whose mapped path does not exist locally (Path(mapped_path).exists() check). Only add the entry's code2 to the available-language set if the mapped path passes both the traversal guard and the exists() check. This ensures Bazarr-reported paths that are not locally accessible (path-mapping misconfiguration, container path mismatch) are silently dropped rather than causing false positives or traversal exploits.
    - Bazarr-disabled graceful degradation (D-104): if bazarr_inventory is None or settings.bazarr_use_inventory is False, skip Bazarr step entirely and fall through to filesystem glob (find_source_sub).
  </behavior>

  <action>
    Modify trezarr/output/ledger_sqla.py. Read the full file. Copy the check() method exactly, rename to check_by_output_path, change the WHERE clause to filter ProcessedFile.output_path == key. The method signature is async def check_by_output_path(self, output_path: str | Path) -> LedgerEntry | None. Find the LedgerProtocol (likely trezarr/output/_ledger_protocol.py or Protocol class in ledger_sqla.py itself) and add check_by_output_path as an abstract method with the same signature.

    Modify trezarr/discover/gap.py. Read the full file first. Locate is_eligible's Case 1 block (the vi_path.exists() and entry is None branch). Replace it with the two-step Case 1 + Case 1.5 as specified in behavior. The logger.info messages for Case 1.5 must include the prior source path (prior_entry.source_path) so the operator can see which source was previously used. CRITICAL: do NOT touch Case 2 (done + vi + hash), Case 3 (in-progress), Case 4, Case 5 — only Case 1 changes. Ensure gap.py still calls await ledger.check(source_sub_path) as before; the new call is check_by_output_path which is a secondary lookup only within Case 1.

    Modify trezarr/discover/scan.py. Read the full file. (1) Add source_upgraded: int = 0 to ScanStats after the existing fields (maintain existing frozen/dataclass pattern). (2) Add source_upgraded: bool = False to EligibleItem after the existing fields (maintain frozen=True). (3) In the reason-classifier inside scan_for_eligible_items, add the "richer source" branch before the return (eligible). (4) Add select_source_for_item function below find_source_sub.

    For select_source_for_item: import rank_sources and normalize_original_language from trezarr.source_selection.rank (lazy import inside function to avoid circular import if needed — check the existing scan.py import pattern). Import apply_path_mapping from wherever it is defined in the codebase (read trezarr/translate/path_mapping.py or grep for the function name to find the authoritative import path). Import or reference the INTG-03 traversal guard (assert_within_media_roots or equivalent — find it by reading the existing scan.py or gap.py imports for the guard function).

    In select_source_for_item, for each SubtitleEntry in bazarr_inventory: (a) call mapped_path = apply_path_mapping(entry.path, settings.path_mappings) per D-105; (b) call assert_within_media_roots(mapped_path, settings.media_roots) (the INTG-03 traversal guard — if it raises, catch and discard the entry); (c) if Path(mapped_path).exists(): add entry.code2 to the available-language set. Only entries that pass all three checks contribute to the ranked language list. This implements T-10-03 mitigation at the call site.

    The function signature: select_source_for_item(media_item, settings, bazarr_inventory: "list[BazarrInventoryItem] | None" = None, per_series_source_override: "list[str] | None" = None) -> str | None. Returns the best available source language code (2-letter) or None if no source. Uses TYPE_CHECKING for BazarrInventoryItem import if needed to avoid circular import. Return type is str | None.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/discover/test_gap.py tests/source_selection/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>

  <done>
    tests/discover/test_gap.py D-110 stubs (test_source_upgrade_eligible, test_foreign_vi_unchanged, test_no_loop_after_upgrade) all XPASS or PASS; uv run pytest tests/ -q shows no FAILED tests.

    D-26/D-27/D-28 non-regression: all pre-existing tests/discover/test_gap.py tests remain GREEN. Verified by: `uv run pytest tests/discover/test_gap.py -v` showing no new FAILED entries. The D-26 foreign-vi guard (test_foreign_vi_unchanged xpass), D-27 source-hash idempotency (Case 2), and D-28 self-output exclusion (Case 3) must all be unaffected by the Case 1.5 insertion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bazarr API → BazarrClient | Bazarr is a LAN service; its inventory response is trusted data but paths need traversal guard |
| BazarrClient → caller | BazarrError must redact the API key from all log messages |
| Bazarr-reported path → filesystem access | Bazarr paths are in Bazarr's container namespace and must be mapped before accessing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | BazarrClient error messages | mitigate | Use _normalize_arr_host(self._host) in all BazarrError messages; api_key resolved only in __init__ via SecretStr.get_secret_value() (D-11); never logged or included in exceptions |
| T-10-03 | Tampering | Bazarr-reported subtitle paths | mitigate | In select_source_for_item: for each entry.path call apply_path_mapping(entry.path, settings.path_mappings) then assert_within_media_roots(mapped_path, settings.media_roots) (INTG-03 traversal guard) and Path(mapped_path).exists() — discard entries that fail any check; per D-105 |
| T-10-04 | Elevation of Privilege | SSRF via bazarr_host configuration | mitigate | bazarr_host flows through _normalize_arr_host() (the existing choke-point from D-71); host is an operator-configured value read at startup from config, not from user input |
| T-10-05 | Tampering | rank_sources() language-code inputs from Bazarr | accept | Bazarr code2 is a 2-letter alpha code from a trusted LAN service; no SQL/command injection vector; inputs are passed through string lower() only |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
All INTG-02, SRC-01, SRC-02 stubs from Plan 10-01 turn GREEN:
`uv run pytest tests/arr/test_bazarr.py tests/source_selection/ tests/discover/test_gap.py -v`

Full suite clean:
`uv run pytest tests/ -q`

Spot-check idempotency correctness — D-110 regression trio:
`uv run pytest tests/discover/test_gap.py::test_source_upgrade_eligible tests/discover/test_gap.py::test_foreign_vi_unchanged tests/discover/test_gap.py::test_no_loop_after_upgrade -v`

Verify D-26/D-27/D-28 still intact for ALL existing gap tests:
`uv run pytest tests/discover/test_gap.py -v`
</verification>

<success_criteria>
- trezarr/arr/bazarr.py exists with BazarrClient, BazarrError, SubtitleEntry, BazarrInventoryItem; all test_bazarr.py stubs GREEN
- trezarr/source_selection/rank.py exists with rank_sources, normalize_original_language; K-drama and US-show test cases PASS
- trezarr/source_selection/resolve.py exists with resolve_effective_settings; all resolve test stubs GREEN
- MediaItem.original_language field present in sonarr.py/radarr.py; test_arr_discovery.py stubs GREEN
- LedgerSQLA.check_by_output_path exists and passes the D-110 Case 1.5 test trio
- gap.py Case 1.5 fires correctly (re-translate from richer source) and D-26 still fires for genuinely foreign vi
- scan.py ScanStats has source_upgraded counter; EligibleItem has source_upgraded flag; select_source_for_item exists with apply_path_mapping + traversal guard on all Bazarr-reported paths (T-10-03)
- config.py has bazarr_use_inventory: bool = True
- uv run pytest tests/ -q exits 0 or with only expected xfail results
</success_criteria>

<output>
Create `.planning/phases/10-source-selection-per-series-overrides/10-02-SUMMARY.md` when done.

Append the following rows to `.planning/phases/10-source-selection-per-series-overrides/10-VALIDATION.md`
Per-Task Verification Map table on completion:

| 10-02-01 | 02 | 1 | INTG-02, SRC-01, SRC-02, D-108 | T-10-02, T-10-04, T-10-05 | BazarrClient redacts api_key; rank_sources deterministic | unit | `uv run pytest tests/arr/test_bazarr.py tests/source_selection/test_rank.py tests/source_selection/test_resolve.py tests/arr/test_arr_discovery.py -v` | ❌ pre-impl | ⬜ pending |
| 10-02-02 | 02 | 1 | SRC-01, SRC-02, SVC-05, D-110 | T-10-03 | Bazarr paths traversal-guarded; D-26/D-27/D-28 non-regression | unit | `uv run pytest tests/discover/test_gap.py -v` | ❌ pre-impl | ⬜ pending |
</output>
