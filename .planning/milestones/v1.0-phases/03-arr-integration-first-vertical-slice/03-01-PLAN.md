---
phase: "03"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/arr/__init__.py
  - tests/arr/test_arr_discovery.py
  - tests/discover/__init__.py
  - tests/discover/test_scan.py
  - tests/test_paths.py
  - tests/test_cli.py
  - tests/output/test_write.py
  - tests/config/test_settings.py
autonomous: true
requirements:
  - INTG-01
  - INTG-03
  - INTG-04
  - AUTO-01
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "All Phase-3 test stubs exist and are EXPECTED-RED (xfail strict=False) before any implementation module is written"
    - "Stubs are EXPECTED xfail (recorded RED scaffolding), not true red failures — the test session must complete without unexpected errors"
    - "pyarr 6.6.x and pytest-httpx 0.36.x are installed and importable, pinned via uv constraints"
    - "The pre-existing test suite still passes (no regression from Phase 1/2); new Phase-3 stubs report as xfail (expected failures)"
  artifacts:
    - path: "tests/arr/test_arr_discovery.py"
      provides: "RED stubs for INTG-01 Sonarr+Radarr discovery"
      contains: "test_sonarr_discovers_monitored_series"
    - path: "tests/discover/test_scan.py"
      provides: "RED stubs for AUTO-01, AUTO-03, AUTO-04 gap/scan"
      contains: "test_gap_detection"
    - path: "tests/test_paths.py"
      provides: "RED stubs for INTG-03 path mapping + probe + traversal guard"
      contains: "test_path_mapping_replaces_prefix"
    - path: "tests/test_cli.py"
      provides: "RED stubs for D-30 batch-run semantics + exit code"
      contains: "test_batch_run_continues"
    - path: "tests/output/test_write.py"
      provides: "Extended with apply_permissions RED stub for INTG-04"
      contains: "test_apply_permissions"
  key_links:
    - from: "tests/arr/test_arr_discovery.py"
      to: "trezarr/arr/sonarr.py"
      via: "pytest.importorskip"
      pattern: "importorskip.*trezarr.arr.sonarr"
    - from: "tests/discover/test_scan.py"
      to: "trezarr/discover/scan.py"
      via: "pytest.importorskip"
      pattern: "importorskip.*trezarr.discover.scan"
---

## Phase Goal

**As a** self-hosted media user, **I want to** have Trezarr discover real episodes from Sonarr/Radarr and translate them to Vietnamese sidecars automatically, **so that** my media library gets consistent Vietnamese subtitles without manual work.

<objective>
*Revised 2026-06-01 to incorporate codex review feedback (see 03-REVIEWS.md).*

Install the two new dependencies (pyarr, pytest-httpx) and lay down the complete RED test surface for every Phase-3 module before any implementation is written. This is the Nyquist Wave-0 discipline the project already uses (Plan 02-01 established this pattern): the test stubs drive the implementation contract and prevent test-after-the-fact drift.

Purpose: Establish RED-first discipline; unblock all implementation waves; ensure pyarr is importable before Wave-1 code references it.

**Important distinction (per codex review):** Phase-3 test stubs are recorded as **expected xfail** (RED scaffolding marked with `@pytest.mark.xfail(strict=False)`). They are NOT true red (uncaught failures) and they are NOT silently passing tests. Each later wave is REQUIRED to remove the xfail marker on stubs covering modules it implements — see Plans 03-02 through 03-05 success criteria.

Output: pyproject.toml updated (with version constraints), uv.lock refreshed, 6 test files (2 new packages + 4 files), 1 extended test file — all stubs marked `@pytest.mark.xfail(strict=False)` and confirmed as expected xfails (not errors).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-RESEARCH.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-PATTERNS.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-REVIEWS.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pyarr + pytest-httpx (version-pinned); create test package skeletons</name>
  <files>pyproject.toml, uv.lock, tests/arr/__init__.py, tests/discover/__init__.py</files>
  <read_first>
    - pyproject.toml (full file — add pyarr to [project.dependencies] and pytest-httpx to [dependency-groups].dev exactly as existing deps are formatted; confirm Python >=3.12 constraint already present)
    - tests/output/__init__.py (exact empty-file pattern to replicate for tests/arr/__init__.py and tests/discover/__init__.py)
    - 03-RESEARCH.md §"Package Legitimacy Audit" (pyarr [OK], pytest-httpx [OK] — both approved)
    - 03-RESEARCH.md §"Standard Stack" (pyarr==6.6.0, pytest-httpx==0.36.2)
    - 03-REVIEWS.md §"03-01-PLAN.md Review" → "LOW: Dependency install versions" (pin via `>=6.6,<6.7` to prevent drift)
  </read_first>
  <behavior>
    - After install: `python -c "import pyarr; from pyarr import Sonarr, Radarr; print('ok')"` exits 0
    - After install: `python -c "import pytest_httpx; print('ok')"` exits 0
    - tests/arr/__init__.py and tests/discover/__init__.py are empty files (match tests/output/__init__.py pattern)
    - pyproject.toml records pyarr constraint as `pyarr>=6.6,<6.7` (not bare `pyarr`)
    - pyproject.toml records pytest-httpx constraint as `pytest-httpx>=0.36,<0.37` (not bare `pytest-httpx`)
  </behavior>
  <action>
    Per codex review LOW concern #16, install dependencies with explicit version constraints so future `uv lock --upgrade` cannot silently jump pyarr to 6.7.x or pytest-httpx to 0.37.x:

      uv add "pyarr>=6.6,<6.7"
      uv add --dev "pytest-httpx>=0.36,<0.37"

    Both commands update pyproject.toml (with the version specifiers shown above) and regenerate uv.lock automatically.

    Create tests/arr/__init__.py and tests/discover/__init__.py as empty files (same as tests/output/__init__.py — just a package marker with no content).

    Do NOT add aiopyarr (a different, unmaintained package). Import must be `from pyarr import Sonarr, Radarr` — NOT `SonarrAPI`/`RadarrAPI` (pyarr 6.x only — per 03-RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>uv run python -c "from pyarr import Sonarr, Radarr; import pytest_httpx; print('deps ok')"</automated>
  </verify>
  <done>pyarr importable as `from pyarr import Sonarr, Radarr`; pytest-httpx importable; pyproject.toml carries the `>=6.6,<6.7` and `>=0.36,<0.37` specifiers; both __init__.py files exist and are empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for all Phase-3 modules</name>
  <files>
    tests/arr/test_arr_discovery.py,
    tests/discover/test_scan.py,
    tests/test_paths.py,
    tests/test_cli.py,
    tests/output/test_write.py,
    tests/config/test_settings.py
  </files>
  <read_first>
    - tests/output/test_write.py (full file — the template: xfail(strict=False) stub pattern, pytest.importorskip, tmp_path, _make_minimal_doc helper; line numbers per 03-PATTERNS.md)
    - tests/output/test_ledger.py (full file — test_foreign_srt_not_clobbered pattern for gap detection stubs; lines 93-117 per 03-PATTERNS.md)
    - tests/translate/test_engine.py (full file — per-item exception pattern for test_cli stubs)
    - tests/config/test_settings.py (full file — env_override, api_key_not_logged, yaml_load patterns; lines per 03-PATTERNS.md)
    - tests/llm/test_client_tiers.py (deferred import pattern per 03-PATTERNS.md)
    - 03-RESEARCH.md §"Validation Architecture / Phase Requirements → Test Map" (all 12 test behaviors to stub)
    - 03-PATTERNS.md §"Test Pattern Assignments" (every test pattern assignment)
    - 03-CONTEXT.md §"Implementation Decisions" (D-21 through D-30 — stub tests must cite these)
    - 03-REVIEWS.md §"03-01" (MEDIUM: xfail removal policy) and §"03-02 HIGH #2" (empty media_roots refusal — stub must assert SystemExit when both *arr enabled and media_roots empty); §"03-04 HIGH #4" (drop media_path from is_eligible signature); §"03-04 HIGH #5" (EligibleItem dataclass return); §"03-04 MEDIUM #12" (deterministic same-language tiebreaker); §"03-04 MEDIUM #13" (PermissionApplyError on chmod failure); §"03-05 HIGH #6" (`_run_once` returns int); §"03-05 HIGH #7" (LLMClient conditional construction); §"03-05 MEDIUM #14" (ScanStats / widened summary fields); §"03-03 MEDIUM #15" (host normalisation tests)
  </read_first>
  <behavior>
    - tests/arr/test_arr_discovery.py stubs: test_sonarr_discovers_monitored_series, test_sonarr_filters_unmonitored, test_radarr_discovers_movies, test_radarr_skips_missing_file, test_normalize_arr_host_bare_ip, test_normalize_arr_host_full_url_with_scheme, test_normalize_arr_host_full_url_with_port, test_discovery_error_raised_on_http_error — all marked @pytest.mark.xfail(strict=False)
    - tests/discover/test_scan.py stubs: test_find_source_sub_priority, test_find_source_sub_none_when_absent, test_find_source_sub_deterministic_on_collision, test_gap_detection_no_source, test_gap_detection_eligible_new, test_gap_detection_foreign_vi_skip, test_idempotency_skip_unchanged, test_idempotency_retranslate_on_change, test_foreign_vi, test_scan_returns_eligible_item_and_scan_stats (10 stubs)
    - tests/test_paths.py stubs: test_path_mapping_replaces_prefix, test_path_mapping_no_match_passthrough, test_path_mapping_strips_trailing_slash, test_longest_prefix_wins, test_traversal_guard_raises_outside_roots, test_traversal_guard_allows_inside, test_traversal_guard_handles_nonexistent_sidecar, test_probe_unreadable_root_exits, test_probe_readable_root_passes, test_probe_warns_on_non_writable_root, test_assert_media_roots_configured_refuses_when_empty_and_arr_enabled, test_path_mappings_no_shared_default_identity, test_source_lang_priority_no_shared_default_identity (13 stubs — last 2 cover mutable-default regression per 03-REVIEWS.md HIGH #1)
    - tests/test_cli.py stubs: test_batch_run_continues_past_failure, test_run_once_returns_int_zero_on_all_success, test_run_once_returns_int_nonzero_on_any_failure, test_batch_summary_printed_with_widened_fields, test_llm_client_not_constructed_when_no_eligible, test_one_arr_failure_does_not_kill_other_arr, test_chmod_error_quarantines_item (7 stubs). All stubs assert behavior of `_run_once(config_path)` which now returns `int` — do NOT use `pytest.raises(SystemExit)` on `_run_once`; assert the returned int instead. The main()-level `SystemExit` wrap is tested separately if at all.
    - tests/output/test_write.py extension: add test_apply_permissions_calls_chown_chmod, test_apply_permissions_chown_permission_error_continues, test_apply_permissions_raises_PermissionApplyError_on_chmod_failure (3 stubs appended after existing tests — per 03-REVIEWS.md MEDIUM #13)
    - tests/config/test_settings.py extension: add test_sonarr_host_env_override, test_sonarr_api_key_not_in_repr, test_path_mappings_yaml_load (3 stubs appended)
    - All stubs use pytest.importorskip("trezarr.arr.sonarr") etc. with deferred imports inside function body — identical to test_write.py line 39 pattern
    - `uv run pytest tests/ -x -q` exits 0: pre-existing tests all pass; new stubs report as xfail (not error)
  </behavior>
  <action>
    Write six test files following the exact stub patterns established in Phase 2 (03-PATTERNS.md §"Test Pattern Assignments").

    CRITICAL CONVENTIONS (from 03-PATTERNS.md §"Shared Patterns"):
    - Every new test file starts with `from __future__ import annotations`
    - Import deferral: all `trezarr.*` imports inside each test function body via `pytest.importorskip`
    - Stub pattern: `@pytest.mark.xfail(strict=False) / def test_X(): pytest.importorskip(...)`
    - No `@pytest.mark.asyncio` decorator — `asyncio_mode = "auto"` is set in pyproject.toml
    - For tests using `httpx_mock` fixture (arr discovery): sync `def` tests, not `async def` (sync Sonarr client per 03-RESEARCH.md Pattern 1)
    - For CLI integration tests: `async def` (the CLI calls asyncio.run internally but tests call `_run_once()` directly)

    tests/arr/test_arr_discovery.py: Use `httpx_mock` fixture (from pytest-httpx). Mock `http://HOST:8989/api/v3/series` and `http://HOST:8989/api/v3/episodefile?seriesId=1` for Sonarr stubs; `http://HOST:7878/api/v3/movie` for Radarr. All stubs are xfail. Add stubs covering `_normalize_arr_host()` (per 03-REVIEWS.md MEDIUM #15): bare IP `"192.168.1.10"` → unchanged; `"http://192.168.1.10:8989"` → `"192.168.1.10"`; `"https://sonarr.example/api"` → `"sonarr.example"`. Add `test_discovery_error_raised_on_http_error` asserting that an HTTP 401 from Sonarr results in a `trezarr.arr.DiscoveryError` (per 03-REVIEWS.md MEDIUM #10).

    tests/discover/test_scan.py: Use `tmp_path` to create stub SRT files. Define a `_make_minimal_ledger(tmp_path, entries=None)` helper that creates a `Ledger` from `tmp_path / "processed_files.json"` with optional pre-populated entries. Mirror the `_make_minimal_doc` helper pattern from test_write.py. `is_eligible()` stubs MUST call with the new signature `is_eligible(source_sub_path, ledger)` (NO `media_path` argument — per 03-REVIEWS.md HIGH #4). Add `test_find_source_sub_deterministic_on_collision`: create two `Show.S01E01.en.srt` candidates in different subdirectories or via filesystem creation order; assert lexicographically-first wins (per 03-REVIEWS.md MEDIUM #12). Add `test_scan_returns_eligible_item_and_scan_stats`: assert `scan_for_eligible_items()` returns a 2-tuple `(list[EligibleItem], ScanStats)` where `EligibleItem` has fields `media_item`, `source_sub_path`, `reason`, `source_lang`, and `ScanStats` has fields `scanned`, `no_source`, `foreign_vi`, `already_done` (per 03-REVIEWS.md HIGH #5 and MEDIUM #14).

    tests/test_paths.py: Parametrize `test_path_mapping_replaces_prefix` and siblings with `@pytest.mark.parametrize` over `(api_path, remote, local, expected)` tuples. Use `tmp_path` for the readability probe tests. Use `pytest.raises(SystemExit)` for probe failure test.

    Add (per 03-REVIEWS.md HIGH #1 — mutable default isolation):
      - `test_path_mappings_no_shared_default_identity`: instantiate `TrezarrSettings()` twice; assert `s1.path_mappings is not s2.path_mappings`
      - `test_source_lang_priority_no_shared_default_identity`: instantiate `TrezarrSettings()` twice; assert `s1.source_lang_priority is not s2.source_lang_priority`

    Add (per 03-REVIEWS.md HIGH #2 — empty media_roots refusal):
      - `test_assert_media_roots_configured_refuses_when_empty_and_arr_enabled`: settings with empty `path_mappings` AND `sonarr_enabled=True` → `pytest.raises(SystemExit)` from a startup-helper named `assert_media_roots_configured(settings)` defined in `trezarr/paths.py`

    Add (per 03-REVIEWS.md MEDIUM #8):
      - `test_probe_warns_on_non_writable_root`: tmp_path read-only via `chmod(0o500)`; call `probe_media_roots([tmp_path])`; assert `caplog` records a WARNING containing "not writable" but the call does NOT raise/exit

    Add (per 03-REVIEWS.md MEDIUM #9):
      - `test_traversal_guard_handles_nonexistent_sidecar`: assert that `assert_within_media_roots(tmp_path / "new_unwritten.vi.srt", [tmp_path])` does NOT raise even though the file does not yet exist (the implementation resolves `resolved.parent` for not-yet-existing targets)

    tests/test_cli.py: Patch `trezarr.cli.discover_sonarr_items`, `trezarr.cli.discover_radarr_items`, and `trezarr.cli.translate_file` with `unittest.mock.AsyncMock` / `MagicMock`. Call `_run_once(config_path=None)` directly (not via argparse). Use `settings_factory` conftest fixture with arr hosts disabled.

    CRITICAL: `_run_once()` now returns `int` (per 03-REVIEWS.md HIGH #6). Stubs MUST assert the returned int value (`assert await _run_once(None) == 0` or `== 1`) — NOT `pytest.raises(SystemExit)`.

    Add (per 03-REVIEWS.md HIGH #7):
      - `test_llm_client_not_constructed_when_no_eligible`: patch `trezarr.cli.LLMClient` with `MagicMock`; arrange settings so `eligible` list is empty (no items eligible); call `await _run_once(None)`; assert `LLMClient.call_count == 0`

    Add (per 03-REVIEWS.md MEDIUM #10):
      - `test_one_arr_failure_does_not_kill_other_arr`: patch `discover_sonarr_items` to raise `DiscoveryError`; patch `discover_radarr_items` to return a populated list with 1 already-translated item (so no eligible); assert `_run_once()` returns `0` (Radarr succeeded, no items to translate; the Sonarr error is logged but not fatal). Conversely, both raising → return 1.

    Add (per 03-REVIEWS.md MEDIUM #13):
      - `test_chmod_error_quarantines_item`: patch `apply_permissions` to raise `PermissionApplyError`; assert n_quar increments and `_run_once()` returns 1

    Add (per 03-REVIEWS.md MEDIUM #14):
      - `test_batch_summary_printed_with_widened_fields`: assert the printed summary string contains "discovered=", "eligible=", "translated=", "translate_skipped=", "scan_skipped=", "quarantined=", "failed=" (not the old "skipped=")

    tests/output/test_write.py (extend — append only, do NOT modify existing tests):
      - Add `test_apply_permissions_calls_chown_chmod` and `test_apply_permissions_chown_permission_error_continues` using `unittest.mock.patch("os.chown", side_effect=PermissionError)` pattern.
      - Add `test_apply_permissions_raises_PermissionApplyError_on_chmod_failure`: patch `os.chmod` to raise `OSError`; assert `apply_permissions(...)` raises `trezarr.output.write.PermissionApplyError` (per 03-REVIEWS.md MEDIUM #13)
      - Deferred import of `apply_permissions` and `PermissionApplyError` via `pytest.importorskip("trezarr.output.write")`.

    tests/config/test_settings.py (extend — append only): Add three stubs following the existing `monkeypatch.setenv` / `TrezarrSettings()` / `assert` pattern.

    After writing all stubs, run the full test suite to confirm pre-existing tests still pass and all new stubs are xfail (not error-fail).
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite passes: all pre-existing Phase-1/2 tests pass; new stubs report as xfail (not error or unexpectedly-passing); zero ImportError at collection time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev deps → CI | pytest-httpx installed as dev dep; not present in production image |
| test code → src | Stubs import production modules via pytest.importorskip; no reverse dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | pyarr package install | mitigate | slopcheck [OK] verified in 03-RESEARCH.md Package Legitimacy Audit; package is approved; pinned `>=6.6,<6.7` |
| T-03-01-02 | Tampering | pytest-httpx package install | mitigate | slopcheck [OK] verified; 7.5M/month downloads; legitimate source repo; pinned `>=0.36,<0.37` |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck confirmed [OK] for all packages; no [ASSUMED] or [SUS] packages — no blocking checkpoint required |
</threat_model>

<verification>
uv run pytest tests/ -x -q — full suite passes (pre-existing tests all pass; Phase-3 stubs xfailed)
uv run python -c "from pyarr import Sonarr, Radarr; print('ok')" — exits 0
uv run python -c "import pytest_httpx; print('ok')" — exits 0
grep -E "pyarr.*>=6.6,<6.7" pyproject.toml — returns >= 1 match
grep -E "pytest-httpx.*>=0.36,<0.37" pyproject.toml — returns >= 1 match
</verification>

<success_criteria>
- pyarr 6.6.x installed and importable as `from pyarr import Sonarr, Radarr`, pinned `>=6.6,<6.7`
- pytest-httpx 0.36.x installed as dev dep, pinned `>=0.36,<0.37`
- tests/arr/__init__.py, tests/discover/__init__.py exist (empty)
- All RED stubs exist across 6 test files; `uv run pytest tests/ -x -q` shows xfailed stubs, zero errors
- No existing Phase-1/2 test regressions
- All HIGH-severity concerns from 03-REVIEWS.md affecting this plan are addressed (xfail removal policy clarified for later waves; brittle "63 passed" wording replaced with "pre-existing tests pass"; xfail-vs-true-red wording disambiguated; LOW #16 version pins applied)
</success_criteria>

<output>
Create `.planning/phases/03-arr-integration-first-vertical-slice/03-01-SUMMARY.md` when done.
</output>
