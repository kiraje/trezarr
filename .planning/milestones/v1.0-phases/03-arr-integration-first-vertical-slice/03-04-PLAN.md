---
phase: "03"
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - trezarr/discover/__init__.py
  - trezarr/discover/scan.py
  - trezarr/discover/gap.py
  - trezarr/output/write.py
autonomous: true
requirements:
  - INTG-04
  - AUTO-01
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "find_source_sub() returns the highest-priority language subtitle next to the media file, or None if absent — handles 2 and 3-letter ISO lang codes; deterministic on collision (lexicographically-first wins)"
    - "is_eligible(source_sub_path, ledger) returns (True, reason) for new items and quarantined retries; (False, reason) for already-done+unchanged and foreign vi sidecars — never clobbers a foreign vi.srt"
    - "is_eligible() correctly identifies source-sub change by comparing source bytes hash against LedgerEntry.content_hash"
    - "scan_for_eligible_items() returns (list[EligibleItem], ScanStats) — typed dataclasses, not bare tuples"
    - "apply_permissions() calls os.chown and os.chmod after write; PermissionError on chown logs a warning and continues; OSError on chmod raises PermissionApplyError (caller quarantines the item)"
    - "apply_permissions() computes file_mode = 0o666 & ~umask — never calls os.umask() globally"
    - "Every write path passes through assert_within_media_roots() before apply_permissions is called"
  artifacts:
    - path: "trezarr/discover/__init__.py"
      provides: "Package marker (empty)"
    - path: "trezarr/discover/scan.py"
      provides: "find_source_sub, scan_for_eligible_items, EligibleItem, ScanStats"
      exports: ["find_source_sub", "scan_for_eligible_items", "EligibleItem", "ScanStats"]
    - path: "trezarr/discover/gap.py"
      provides: "is_eligible"
      exports: ["is_eligible"]
    - path: "trezarr/output/write.py"
      provides: "Extended with apply_permissions and PermissionApplyError"
      exports: ["apply_permissions", "PermissionApplyError"]
  key_links:
    - from: "trezarr/discover/gap.py"
      to: "trezarr/output/ledger.py"
      via: "ledger.check() called to get LedgerEntry; Ledger.content_hash() called for hash comparison"
      pattern: "ledger.check"
    - from: "trezarr/discover/gap.py"
      to: "trezarr/output/write.py"
      via: "derive_vi_sidecar_path(source_sub_path) called to determine expected vi path"
      pattern: "derive_vi_sidecar_path"
    - from: "trezarr/cli.py"
      to: "trezarr/paths.py"
      via: "assert_within_media_roots called on result.output_path before apply_permissions in the translate loop"
      pattern: "assert_within_media_roots"
    - from: "trezarr/cli.py"
      to: "trezarr/output/write.py"
      via: "except PermissionApplyError → quarantine the item, n_quar++"
      pattern: "PermissionApplyError"
---

<objective>
*Revised 2026-06-01 to incorporate codex review feedback (see 03-REVIEWS.md).*

Implement the discovery layer (source-sub scan + gap detection) and the permission-application helper. These are the two missing pieces between *arr discovery and the translation call: scan.py finds what source subs exist, gap.py decides eligibility (idempotency + foreign-vi guard), and apply_permissions() handles PUID/PGID/UMASK after write (all per D-25, D-26, D-27, D-28, D-29).

This revision folds in codex review changes:
- HIGH #4: drop the unused `media_path` parameter from `is_eligible()` (adjacency is structurally enforced by `media_path.parent` scan)
- HIGH #5 / MEDIUM #14: replace bare-tuple return with typed `EligibleItem` dataclass + `ScanStats` dataclass for widened CLI summary
- MEDIUM #11: explicitly document the source-sub glob limitation (`.forced.srt`/`.default.en.srt` not supported in Phase 3)
- MEDIUM #12: deterministic tiebreaker on same-language collision (sort lexicographically)
- MEDIUM #13: chmod failure now raises `PermissionApplyError` so cli.py can quarantine; chown failure stays warn-and-continue
- LOW #19: move the meaningful `os.umask` grep to write.py (drop the trivially-true gap.py grep)

**Limitation (Phase 3, per 03-REVIEWS.md MEDIUM #11):** Source-sub discovery only matches `{media_stem}.{lang}.srt` where `{lang}` is a 2- or 3-letter ISO code. Suffixed variants like `Show.S01E01.forced.srt`, `Show.S01E01.default.en.srt`, or provider-tagged subs are NOT detected this phase. **Deferred:** richer source-sub matching → Phase 10 (Bazarr integration, INTG-02).

Purpose: Closes the AUTO-01 (gap detection), AUTO-03 (idempotency), AUTO-04 (self-output exclusion), and INTG-04 (permission-correct write) requirements. Together with Plan 03-03, this completes all logic the CLI wires together in Plan 03-05.
Output: trezarr/discover/__init__.py (empty), trezarr/discover/scan.py, trezarr/discover/gap.py, trezarr/output/write.py (extended with apply_permissions and PermissionApplyError).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-RESEARCH.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-PATTERNS.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-REVIEWS.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement trezarr/discover/scan.py + gap.py — source-sub discovery, EligibleItem, ScanStats, gap detection</name>
  <files>trezarr/discover/__init__.py, trezarr/discover/scan.py, trezarr/discover/gap.py</files>
  <read_first>
    - trezarr/output/write.py (full file — the structural analog: module docstring citing decisions, regex at module level, Path manipulation pattern, derive_vi_sidecar_path; per 03-PATTERNS.md §"trezarr/discover/scan.py")
    - trezarr/output/ledger.py (full file — LedgerEntry dataclass fields and statuses "done"/"quarantined"/"in_progress"; Ledger.check() and Ledger.content_hash() signatures; per 03-PATTERNS.md §"trezarr/discover/gap.py")
    - 03-RESEARCH.md §"Pattern 5: Source-Sub Discovery (D-25, D-26)" (find_source_sub algorithm + is_eligible logic — use these as the implementation blueprint)
    - 03-RESEARCH.md §"Open Question 3: 3-letter ISO-639-2 codes" (extend regex to r'^(.+?)\.([a-z]{2,3})\.srt$' per recommendation)
    - 03-RESEARCH.md §"Pitfall 5: Gap detection derives vi path from source sub path, not video path" (always call derive_vi_sidecar_path(source_sub_path), not the video path — this is also why the media_path argument is unused per 03-REVIEWS.md HIGH #4)
    - 03-RESEARCH.md §"Pitfall 7: source_sub_hash vs content_hash confusion" (decision: content_hash IS source_sub_hash; Ledger.content_hash() is the hash function to call)
    - 03-REVIEWS.md §"03-04-PLAN.md Review → HIGH #4" (drop media_path from is_eligible signature); §"HIGH #5" (EligibleItem dataclass); §"MEDIUM #11" (source-sub glob limitation); §"MEDIUM #12" (deterministic tiebreaker); §"MEDIUM #14" (ScanStats for widened summary)
    - 03-CONTEXT.md §"D-25, D-26, D-27, D-28" (locked semantics for scan and gap)
    - 03-PATTERNS.md §"trezarr/discover/scan.py" and §"trezarr/discover/gap.py" (full pattern sections)
    - 03-PATTERNS.md §"tests/discover/test_scan.py (unit, tmp_path)" (_make_minimal_ledger helper and test structure)
    - tests/discover/test_scan.py (read the RED stubs before implementing — the new dataclass-shape assertions and the no-media_path signature drive the implementation contract)
  </read_first>
  <behavior>
    - test_find_source_sub_priority: media dir has Show.S01E01.en.srt and Show.S01E01.zh.srt; lang_priority=["zh","en"] → returns ("Show.S01E01.zh.srt", "zh") (first priority wins)
    - test_find_source_sub_none_when_absent: no matching sidecar → returns None
    - test_find_source_sub_deterministic_on_collision: two .en.srt candidates created in non-alphabetical filesystem order; lang_priority=["en"]; find_source_sub returns the LEXICOGRAPHICALLY-FIRST candidate path (deterministic — per 03-REVIEWS.md MEDIUM #12)
    - test_gap_detection_no_source: source sub absent → is_eligible returns (False, reason containing "no source")
    - test_gap_detection_eligible_new: source sub present, no vi sidecar, no ledger entry → (True, "new item")
    - test_gap_detection_foreign_vi_skip: vi sidecar exists but not in ledger → (False, reason containing "foreign") — never clobbers
    - test_idempotency_skip_unchanged: vi sidecar exists, ledger entry status="done", content_hash matches source bytes → (False, "already translated")
    - test_idempotency_retranslate_on_change: vi sidecar exists, ledger entry status="done", but content_hash differs from current source bytes → (True, "source subtitle changed")
    - test_foreign_vi: vi sidecar exists, no ledger entry → (False, reason with "foreign") AND (True, ...) NOT returned
    - 3-letter lang code: file Show.S01E01.eng.srt with lang_priority=["eng"] → returned as the source sub (regex handles {2,3} letter codes)
    - is_eligible signature: `is_eligible(source_sub_path: Path, ledger: Ledger) -> tuple[bool, str]` (NO `media_path` argument — per 03-REVIEWS.md HIGH #4)
    - test_scan_returns_eligible_item_and_scan_stats: scan_for_eligible_items returns `tuple[list[EligibleItem], ScanStats]` (NOT a list of bare tuples). EligibleItem has fields media_item, source_sub_path, reason, source_lang. ScanStats has fields scanned, no_source, foreign_vi, already_done.
  </behavior>
  <action>
    Create trezarr/discover/__init__.py as empty file.

    Create trezarr/discover/scan.py:

    Module docstring citing D-25 and D-28:
    """Source-subtitle filesystem scan and eligible-item collection.

    Design decisions honoured:
      D-25  Filesystem source-sub scan with source-language priority list, default ["en"].
      D-28  Self-output exclusion via ledger provenance (gap.is_eligible handles this).

    Limitation (Phase 3, per 03-REVIEWS.md MEDIUM #11): only matches
    `{media_stem}.{lang}.srt` where {lang} is a 2 or 3-letter ISO code. Suffixed
    variants such as `Show.S01E01.forced.srt` or `Show.S01E01.default.en.srt`
    are NOT detected — those are Phase 10 (Bazarr integration, INTG-02) scope.
    """

    from __future__ import annotations
    import logging
    import re
    from dataclasses import dataclass, field
    from pathlib import Path
    from typing import TYPE_CHECKING
    if TYPE_CHECKING:
        from trezarr.arr.sonarr import MediaItem
        from trezarr.config import TrezarrSettings
        from trezarr.output.ledger import Ledger
    logger = logging.getLogger(__name__)

    Module-level compiled regex (per 03-PATTERNS.md §"Regex at module level"):
      _LANG_SIDECAR_RE = re.compile(r'^(.+?)\.([a-z]{2,3})\.srt$', re.IGNORECASE)
      (Note: {2,3} handles 3-letter ISO codes from Bazarr per 03-RESEARCH.md Open Question 3)

    NEW dataclasses (per 03-REVIEWS.md HIGH #5 and MEDIUM #14):

      @dataclass(frozen=True)
      class EligibleItem:
          """A media item gap-detection determined needs translation."""
          media_item: "MediaItem"
          source_sub_path: Path
          reason: str
          source_lang: str

      @dataclass
      class ScanStats:
          """Counts of items examined during scan, broken down by skip reason.

          Used by cli.py for the end-of-run summary (per 03-REVIEWS.md MEDIUM #14).
          """
          scanned: int = 0
          no_source: int = 0      # no source-language sidecar found next to the media file
          foreign_vi: int = 0     # a foreign .vi.srt blocks translation (D-26)
          already_done: int = 0   # ledger says done + content_hash unchanged

    `find_source_sub(media_path: Path, lang_priority: list[str]) -> tuple[Path, str] | None`:
      - media_dir = media_path.parent
      - media_stem = media_path.stem
      - Build found: dict[str, list[Path]] — lang_code → list of candidate sidecar paths
      - candidates = sorted(media_dir.glob(f"{media_stem}.*.srt"))  ← sorted() makes iteration deterministic per 03-REVIEWS.md MEDIUM #12
      - For each candidate in sorted order: match against _LANG_SIDECAR_RE; if match and stem matches media_stem: found.setdefault(lang.lower(), []).append(candidate)
      - For each lang in lang_priority order: if lang.lower() in found:
          paths = found[lang.lower()]
          if len(paths) > 1:
              logger.info("multiple %s candidates for %s; using lexicographically-first: %s",
                          lang, media_stem, paths[0])
          return (paths[0], lang.lower())   ← deterministic: first after sort wins
      - Return None if no priority match

    `scan_for_eligible_items(items: list["MediaItem"], settings: "TrezarrSettings", ledger: "Ledger") -> tuple[list[EligibleItem], ScanStats]` (per 03-REVIEWS.md HIGH #5 and MEDIUM #14):
      - Import `is_eligible` from `trezarr.discover.gap` inside the function (or via TYPE_CHECKING + lazy import)
      - eligible: list[EligibleItem] = []
      - stats = ScanStats()
      - For each MediaItem in items:
          stats.scanned += 1
          src = find_source_sub(item.local_path, settings.source_lang_priority)
          if src is None:
              stats.no_source += 1
              logger.info("no source sub found for %s", item.local_path)
              continue
          source_sub_path, source_lang = src
          ok, reason = is_eligible(source_sub_path, ledger)
          if not ok:
              if "foreign" in reason:
                  stats.foreign_vi += 1
              elif "already translated" in reason:
                  stats.already_done += 1
              continue
          eligible.append(EligibleItem(
              media_item=item,
              source_sub_path=source_sub_path,
              reason=reason,
              source_lang=source_lang,
          ))
      - return eligible, stats

    Create trezarr/discover/gap.py:

    Module docstring citing D-26, D-27, D-28:
    """Gap detection: decides whether an item needs translation.

    Design decisions honoured:
      D-26  Gap = source sub present AND no honored vi sidecar; foreign vi → skip+log, never clobber.
      D-27  content_hash IS the source-subtitle hash (SHA-256[:16] of source bytes). No new field.
      D-28  Self-output exclusion via ledger provenance — our output is in the ledger; foreign vi is not.
    """

    from __future__ import annotations
    import logging
    from pathlib import Path
    from trezarr.output.ledger import Ledger
    from trezarr.output.write import derive_vi_sidecar_path
    logger = logging.getLogger(__name__)

    `is_eligible(source_sub_path: Path, ledger: Ledger) -> tuple[bool, str]` (per 03-REVIEWS.md HIGH #4 — NO media_path parameter):
      Adjacency is structurally enforced by the caller (`scan_for_eligible_items` discovers `source_sub_path` via `media_path.parent.glob(...)`, so by construction the source sub IS adjacent to the media file — no extra check needed inside is_eligible).

      Implement the D-26 / D-20 logic (per 03-RESEARCH.md Pattern 5):
      - vi_path = derive_vi_sidecar_path(source_sub_path)   ← ALWAYS pass source_sub_path (Pitfall 5)
      - entry = ledger.check(str(source_sub_path))

      Case 1: vi_path.exists() AND entry is None:
        → (False, f"foreign vi sidecar at {vi_path} — not ours, skipping")
        → log: logger.info("foreign vi.srt at %s — skipping %s (D-26, never clobber)", vi_path, source_sub_path)

      Case 2: entry is not None AND entry.status == "done" AND vi_path.exists():
        → Compute current_hash = Ledger.content_hash(source_sub_path.read_bytes())
        → If entry.content_hash == current_hash: return (False, "already translated (source unchanged)")
        → Else: return (True, "source subtitle changed — re-translating")

      Case 3: entry is not None AND entry.status == "quarantined":
        → (True, "retrying previously quarantined item")

      Case 4: entry is not None AND entry.status == "in_progress":
        → (True, "resuming in-progress item (possibly crashed run)")

      Case 5 (default — not in ledger, or entry with no special status):
        → (True, "new item")

      D-28 (self-output exclusion): When translate_file() writes a vi sidecar, it records a LedgerEntry. Next run: vi_path exists AND entry is not None → Case 2 (skip if unchanged). The ledger provenance IS the self-exclusion mechanism in Phase 3 (no watcher yet).

    XFAIL REMOVAL POLICY (per 03-REVIEWS.md HIGH #3): As the FINAL step of this task, after all assertions pass GREEN, remove the `@pytest.mark.xfail(strict=False)` marker from every test in tests/discover/test_scan.py covering find_source_sub, is_eligible, EligibleItem, or ScanStats (all 10 stubs). Re-run `uv run pytest tests/discover/ -x -q` and confirm green.
  </action>
  <verify>
    <automated>uv run pytest tests/discover/ -x -q && uv run ruff check trezarr/discover/scan.py trezarr/discover/gap.py && ! grep -E "xfail" tests/discover/test_scan.py</automated>
  </verify>
  <done>All 10 discover stubs GREEN: find_source_sub priority + deterministic-collision logic, gap detection foreign-vi skip, idempotency skip on unchanged hash, retranslate on changed hash, quarantine retry, scan_for_eligible_items returns (list[EligibleItem], ScanStats). No xfail markers remain in tests/discover/test_scan.py. Full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend trezarr/output/write.py with apply_permissions + PermissionApplyError (D-29, INTG-04)</name>
  <files>trezarr/output/write.py</files>
  <read_first>
    - trezarr/output/write.py (full file — read every line; understand current exports, import block, logger presence/absence, module-level constants; per 03-PATTERNS.md §"trezarr/output/write.py (extend — add apply_permissions)")
    - 03-RESEARCH.md §"Pattern 7: Permission Application (D-29)" (apply_permissions implementation with os.chown PermissionError degrade, file_mode = 0o666 & ~umask, logger.warning with %s/%d format)
    - 03-RESEARCH.md §"Anti-patterns to avoid" (never call os.umask() globally — process-global, unsafe in async code)
    - 03-PATTERNS.md §"trezarr/output/write.py (extend — add apply_permissions)" (position of new function, import additions, logger add)
    - 03-PATTERNS.md §"Logger Call Format (% not f-string)" (logger.warning("chown(%s, %d, %d) failed: %s", path, puid, pgid, exc))
    - 03-CONTEXT.md §"D-29" (locked: os.chown/os.chmod in-process; PermissionError on chown → log-and-continue; constrain writes to media roots; os.umask() forbidden)
    - 03-REVIEWS.md §"03-04-PLAN.md Review → MEDIUM #13" (chmod failure raises PermissionApplyError; chown stays warn-and-continue)
    - tests/output/test_write.py (the new stubs for apply_permissions — test_apply_permissions_calls_chown_chmod, test_apply_permissions_chown_permission_error_continues, test_apply_permissions_raises_PermissionApplyError_on_chmod_failure)
    - trezarr/paths.py (just created in Plan 03-02 — apply_permissions must accept the path; assert_within_media_roots is the caller's responsibility before calling apply_permissions — the function does NOT call assert_within_media_roots internally; that guard is in cli.py before this call)
  </read_first>
  <behavior>
    - test_apply_permissions_calls_chown_chmod: apply_permissions(path, puid=1000, pgid=1000, umask=0o022) calls os.chown(path, 1000, 1000) and os.chmod(path, 0o644) (0o666 & ~0o022 = 0o644)
    - test_apply_permissions_chown_permission_error_continues: apply_permissions with os.chown patched to raise PermissionError → does NOT raise; logs a warning; continues to call os.chmod (chown warn-and-continue is intentional per D-29; Phase 7 s6 init will fix)
    - test_apply_permissions_raises_PermissionApplyError_on_chmod_failure: apply_permissions with os.chmod patched to raise OSError → RAISES trezarr.output.write.PermissionApplyError (chmod failure directly threatens INTG-04 media-server readability, so the caller must quarantine — per 03-REVIEWS.md MEDIUM #13)
    - apply_permissions(path, puid=-1, pgid=-1, umask=0o022): calls os.chown(path, -1, -1) — POSIX no-op (leaves ownership unchanged)
    - apply_permissions never calls os.umask() (process-global; forbidden per D-29)
    - file_mode = 0o666 & ~umask (not hardcoded 0o644 — derived from umask parameter)
  </behavior>
  <action>
    Extend trezarr/output/write.py — additive changes only (per 03-PATTERNS.md "New function position: apply_permissions() goes at the bottom of write.py, after write_vi_sidecar()"):

    1. Add `import logging` to the existing import block (after `import os`) if not already present.

    2. Add `logger = logging.getLogger(__name__)` at module level after existing constants (if not already present — check current file state first).

    3. Add the NEW exception class near the top of the module (after imports, before existing functions):

       class PermissionApplyError(RuntimeError):
           """Raised when apply_permissions fails to apply chmod to a written sidecar.

           Per 03-REVIEWS.md MEDIUM #13: a chmod failure leaves the sidecar potentially
           unreadable to the media server, which directly violates INTG-04. The caller
           (cli.py) catches this and quarantines the item via n_quar++. Distinct from
           a chown failure, which is non-fatal (Phase 7 s6 init will correct ownership).
           """

    4. Add the new function at the bottom of the file, after `write_vi_sidecar()`:

    `apply_permissions(path: Path, puid: int, pgid: int, umask: int) -> None`:
      Docstring citing D-29:
      """Apply PUID/PGID ownership and UMASK-derived permissions to a written sidecar.

      Design decisions honoured:
        D-29  os.chown/os.chmod in-process; PermissionError on chown (EPERM) degrades
              to log-and-continue (Phase 7 s6 init will fix). chmod failure raises
              PermissionApplyError because it directly threatens INTG-04 media-server
              readability (per 03-REVIEWS.md MEDIUM #13).
              Do NOT call os.umask() — that is process-global and unsafe in async code.
              file_mode = 0o666 & ~umask (standard POSIX convention).
              puid=-1 and pgid=-1 are POSIX no-ops (leave ownership unchanged).

      Phase 7 s6-overlay container init will apply correct ownership at startup.

      Raises:
          PermissionApplyError: when os.chmod fails. The caller must quarantine
              the item, because INTG-04 requires the sidecar be world-readable.
      """
      try:
          os.chown(path, puid, pgid)
      except PermissionError:
          logger.warning(
              "chown(%s, %d, %d) failed — process lacks CAP_CHOWN. "
              "File was written but ownership is not yet corrected. "
              "Phase 7 container init (s6-overlay) will fix this.",
              path, puid, pgid,
          )
      except OSError as exc:
          logger.warning("chown(%s) failed with unexpected error: %s", path, exc)

      file_mode = 0o666 & ~umask
      try:
          os.chmod(path, file_mode)
      except OSError as exc:
          logger.error("chmod(%s, %o) failed: %s — INTG-04 readability at risk; quarantining",
                       path, file_mode, exc)
          raise PermissionApplyError(
              f"chmod({path}, {file_mode:o}) failed: {exc}"
          ) from exc

    CRITICAL: Use %s/%d format in logger.warning/.error calls (not f-strings) — per 03-PATTERNS.md §"Logger Call Format (% not f-string)".
    CRITICAL: Do NOT call os.umask() anywhere — compute file_mode directly from the umask parameter.
    CRITICAL: chmod failure RAISES PermissionApplyError (not warn-and-continue). chown failure stays warn-and-continue. The asymmetry is intentional per 03-REVIEWS.md MEDIUM #13.

    Do NOT modify any existing functions in write.py.

    XFAIL REMOVAL POLICY (per 03-REVIEWS.md HIGH #3): As the FINAL step of this task, after all assertions pass GREEN, remove the `@pytest.mark.xfail(strict=False)` marker from the three new apply_permissions stubs in tests/output/test_write.py. Re-run `uv run pytest tests/output/test_write.py -x -q` and confirm green.
  </action>
  <verify>
    <automated>uv run pytest tests/output/test_write.py -x -q && uv run ruff check trezarr/output/write.py && ! grep -E "xfail" tests/output/test_write.py | grep -E "apply_permissions"</automated>
  </verify>
  <done>All write.py tests GREEN including the three new apply_permissions stubs. os.chown PermissionError degrades to warning+continue; os.chmod OSError raises PermissionApplyError. file_mode computed as 0o666 & ~umask. PermissionApplyError exported. xfail markers removed from the three apply_permissions stubs. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem (media dir) → scan results | Sidecar globs are constrained to media_path.parent — no traversal above the media directory |
| Ledger (provenance record) → eligibility decision | Ledger is the source of truth for "ours vs. foreign vi" — gap.py trusts it |
| apply_permissions → OS | os.chown/os.chmod on a path that must already be within media roots (caller's responsibility via assert_within_media_roots) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Foreign vi.srt clobber (D-26, Pitfall 10) | mitigate | is_eligible Case 1: vi_path.exists() AND entry is None → (False, "foreign...") + logger.info. The file is NEVER passed to translate_file() or write_vi_sidecar(). |
| T-03-04-02 | Tampering | Path traversal via glob (media_path.parent.glob) | mitigate | Glob is scoped to media_path.parent — can only list siblings of the video file; no ".." expansion possible. Results sorted lexicographically for deterministic same-language tiebreak (per 03-REVIEWS.md MEDIUM #12). |
| T-03-04-03 | Elevation of Privilege | apply_permissions called on path outside media roots | mitigate | assert_within_media_roots() is called in cli.py (Plan 03-05) BEFORE apply_permissions; the guard is at the CLI orchestration level so it applies to every write path |
| T-03-04-04 | Elevation of Privilege | os.chown PermissionError silently interpreted as success | mitigate | Explicit PermissionError catch with logger.warning explaining the situation; Phase 7 s6 init will correct ownership |
| T-03-04-05 | Tampering | chmod failure leaves sidecar unreadable to media server (INTG-04 risk) | mitigate | chmod failure raises PermissionApplyError (per 03-REVIEWS.md MEDIUM #13); cli.py quarantines the item and continues; sidecar that was written but is unreadable is recorded as quarantined in the ledger for retry |
| T-03-04-06 | Tampering | os.umask() called globally — race in async code | mitigate | Forbidden by D-29; not called anywhere; file_mode derived explicitly as 0o666 & ~umask per-call |
</threat_model>

<verification>
uv run pytest tests/discover/ tests/output/test_write.py -x -q — all GREEN
uv run pytest tests/ -x -q — full suite green
grep -v "^#" trezarr/output/write.py | grep "os.umask" — returns 0 (not called)  ← per 03-REVIEWS.md LOW #19, the meaningful umask check is here (NOT in gap.py)
grep -n "derive_vi_sidecar_path(source_sub_path" trezarr/discover/gap.py — confirms Pitfall 5 guard (source sub path used, not video path)
grep -c "PermissionApplyError" trezarr/output/write.py — returns >= 2 (class + raise statement)
grep -c "@dataclass" trezarr/discover/scan.py — returns >= 2 (EligibleItem + ScanStats)
! grep -E "media_path" trezarr/discover/gap.py — is_eligible signature does not include media_path (per 03-REVIEWS.md HIGH #4)
</verification>

<success_criteria>
- trezarr/discover/__init__.py, scan.py, gap.py all created
- find_source_sub: 2 and 3-letter ISO codes supported; priority-order match; sorted glob results for deterministic tiebreak; returns (Path, lang_str) or None
- EligibleItem (frozen) and ScanStats dataclasses defined in scan.py and exported
- scan_for_eligible_items returns `tuple[list[EligibleItem], ScanStats]` (NOT bare tuples — per 03-REVIEWS.md HIGH #5)
- is_eligible signature is `(source_sub_path, ledger)` (no media_path — per 03-REVIEWS.md HIGH #4)
- is_eligible: all 5 cases implemented (foreign vi, done+unchanged, done+changed, quarantined, new); never clobbers foreign vi; D-28 self-exclusion via ledger provenance
- derive_vi_sidecar_path called with source_sub_path in gap.py (not video path)
- Ledger.content_hash called for hash comparison (not a new hash function)
- apply_permissions: os.chown + os.chmod; chown PermissionError → log-and-continue; chmod OSError → raise PermissionApplyError (per 03-REVIEWS.md MEDIUM #13); file_mode = 0o666 & ~umask; os.umask() never called
- PermissionApplyError exported from trezarr.output.write
- All discover + write tests GREEN; xfail markers removed from tests/discover/test_scan.py and the new apply_permissions stubs in tests/output/test_write.py
- Full suite green
- All HIGH-severity concerns from 03-REVIEWS.md affecting this plan are addressed (#3 xfail removal, #4 drop media_path, #5 EligibleItem dataclass, plus MEDIUM #11, #12, #13, #14, LOW #19)
</success_criteria>

<output>
Create `.planning/phases/03-arr-integration-first-vertical-slice/03-04-SUMMARY.md` when done.
</output>
