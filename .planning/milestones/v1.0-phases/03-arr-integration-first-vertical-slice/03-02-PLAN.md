---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - trezarr/config.py
  - trezarr/paths.py
  - trezarr/output/ledger.py
autonomous: true
requirements:
  - INTG-03
  - AUTO-03

must_haves:
  truths:
    - "TrezarrSettings accepts Sonarr/Radarr connection fields, path_mappings list, source_lang_priority, and PUID/PGID/UMASK — all env and YAML loadable"
    - "path_mappings and source_lang_priority use Field(default_factory=...) so per-instance lists are independent (no shared mutable default state)"
    - "apply_path_mapping() strips trailing slashes, applies longest-prefix-first, returns passthrough on no match"
    - "assert_within_media_roots() raises ValueError when a resolved path escapes all configured roots; handles not-yet-existing target paths by resolving the parent"
    - "probe_media_roots() calls sys.exit with a clear error when any root is unreadable, missing, or not executable; logs a WARNING (does NOT exit) when a root is not writable"
    - "assert_media_roots_configured() refuses startup when path_mappings is empty AND any *arr discovery is enabled"
    - "LedgerEntry.content_hash docstring explicitly documents it as the source-subtitle hash (D-27)"
    - "SecretStr fields sonarr_api_key and radarr_api_key do not appear in str(settings) or repr(settings)"
  artifacts:
    - path: "trezarr/paths.py"
      provides: "Path-mapping layer: apply_path_mapping, assert_within_media_roots, probe_media_roots, build_media_roots, assert_media_roots_configured"
      exports: ["apply_path_mapping", "assert_within_media_roots", "probe_media_roots", "build_media_roots", "assert_media_roots_configured", "PathMapping"]
    - path: "trezarr/config.py"
      provides: "Extended TrezarrSettings with all Phase-3 fields"
      contains: "sonarr_host"
    - path: "trezarr/output/ledger.py"
      provides: "LedgerEntry.content_hash documented as source-sub hash (D-27)"
      contains: "source-subtitle"
  key_links:
    - from: "trezarr/paths.py"
      to: "trezarr/config.py"
      via: "PathMapping Pydantic model imported from paths.py into config.py path_mappings field"
      pattern: "from trezarr.paths import PathMapping"
    - from: "trezarr/arr/sonarr.py"
      to: "trezarr/paths.py"
      via: "apply_path_mapping called on every EpisodeFile path"
      pattern: "apply_path_mapping"
    - from: "trezarr/cli.py"
      to: "trezarr/paths.py"
      via: "assert_media_roots_configured(settings) called before probe_media_roots"
      pattern: "assert_media_roots_configured"
---

<objective>
*Revised 2026-06-01 to incorporate codex review feedback (see 03-REVIEWS.md).*

Implement the configuration foundation (per D-21 through D-29) and the path-mapping layer (per D-23, D-24, D-29) that every other Phase-3 module depends on, plus the minimal ledger docstring clarification (D-27). This is the pure-data / pure-transform layer: no external API calls, no filesystem side effects beyond path arithmetic.

Purpose: Establish the config contract and path-resolution contract that sonarr.py, radarr.py, scan.py, gap.py, and cli.py all consume. These are the shared foundation all Wave-2 and Wave-3 modules build on. Codex review HIGH #1, HIGH #2, MEDIUM #8, MEDIUM #9 changes (mutable-default safety, empty-roots refusal, writable probe, traversal-guard for nonexistent sidecars) are folded in here.
Output: trezarr/paths.py (new), trezarr/config.py (extended), trezarr/output/ledger.py (docstring only).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-RESEARCH.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-PATTERNS.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-REVIEWS.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TrezarrSettings with Phase-3 config fields</name>
  <files>trezarr/config.py</files>
  <read_first>
    - trezarr/config.py (full file — read every line; understand the existing field layout, section-comment style, model_config, SecretStr pattern, _yaml_file override; lines are cited in 03-PATTERNS.md §"trezarr/config.py (extend existing)")
    - 03-PATTERNS.md §"trezarr/config.py (extend existing)" (SecretStr field pattern lines 48-49; field group comment pattern; env_nested_delimiter note)
    - 03-RESEARCH.md §"Pattern 9: New TrezarrSettings Fields" (exact field names, types, defaults — MUST match exactly EXCEPT for the default expression: use Field(default_factory=...) per 03-REVIEWS.md HIGH #1)
    - 03-CONTEXT.md §"D-22, D-23, D-25, D-29" (locked field semantics)
    - 03-REVIEWS.md §"03-02-PLAN.md Review → HIGH #1" (mutable default fix: Field(default_factory=...) for path_mappings and source_lang_priority)
    - tests/config/test_settings.py (read before touching config.py — the new stubs assert specific env var names and repr behavior; the test_path_mappings_no_shared_default_identity stub asserts per-instance list isolation)
  </read_first>
  <behavior>
    - test_sonarr_host_env_override: `TREZARR_SONARR_HOST=192.168.1.10` → `settings.sonarr_host == "192.168.1.10"`
    - test_sonarr_api_key_not_in_repr: `sonarr_api_key` SecretStr value must not appear in `str(settings)` or `repr(settings)` or `settings.model_dump()`
    - test_path_mappings_yaml_load: `path_mappings: [{remote: /tv, local: /data/tv}]` YAML → `settings.path_mappings[0].remote == "/tv"` and `.local == "/data/tv"`
    - test_path_mappings_no_shared_default_identity: `TrezarrSettings().path_mappings is not TrezarrSettings().path_mappings` (two instances → two distinct list objects, per 03-REVIEWS.md HIGH #1)
    - test_source_lang_priority_no_shared_default_identity: `TrezarrSettings().source_lang_priority is not TrezarrSettings().source_lang_priority`
    - `settings.puid` defaults to -1; `settings.pgid` defaults to -1; `settings.umask` defaults to 0o022 (18 decimal)
    - `settings.source_lang_priority` defaults to a fresh list `["en"]` (not a shared module-level list)
    - `settings.sonarr_enabled` defaults to False; `settings.radarr_enabled` defaults to False
  </behavior>
  <action>
    Extend trezarr/config.py by adding the following field groups after the existing Phase-2 fields, following the exact `# ── Section Name (D-XX) ──` comment style already present (per 03-PATTERNS.md §"Field group comment pattern"):

    Import `Field` from `pydantic` (alongside the existing pydantic imports) and `PathMapping` from `trezarr.paths`. Add the imports near the top of config.py (after existing imports). `PathMapping` is a `pydantic.BaseModel` subclass (not a dataclass) so pydantic-settings can deserialize the JSON array from the env var `TREZARR_PATH_MAPPINGS`.

    Field groups to add (per D-22, D-23, D-25, D-29, 03-RESEARCH.md Pattern 9, and 03-REVIEWS.md HIGH #1):

    Section `# ── *arr connection (D-22) ──`:
      sonarr_host: str = ""
      sonarr_port: int = 8989
      sonarr_api_key: SecretStr = SecretStr("")  # NEVER logged; SecretStr masks in repr/str
      sonarr_enabled: bool = False
      radarr_host: str = ""
      radarr_port: int = 7878
      radarr_api_key: SecretStr = SecretStr("")  # NEVER logged; SecretStr masks in repr/str
      radarr_enabled: bool = False

    Section `# ── Path mapping (D-23) ──`:
      path_mappings: list[PathMapping] = Field(default_factory=list)
      (Env: TREZARR_PATH_MAPPINGS='[{"remote":"/tv","local":"/data/tv"}]' as JSON array string)
      (CRITICAL: per 03-REVIEWS.md HIGH #1 — do NOT use literal `[]` as the default. Pydantic v2 generally copies model defaults, but the reviewer flagged this for safety and consistency. Use Field(default_factory=list).)

    Section `# ── Source-language priority (D-25) ──`:
      source_lang_priority: list[str] = Field(default_factory=lambda: ["en"])
      (CRITICAL: per 03-REVIEWS.md HIGH #1 — Field(default_factory=lambda: ["en"]), NOT literal `["en"]`. Each instance gets a fresh list.)

    Section `# ── Permissions (D-29) ──`:
      puid: int = -1
      pgid: int = -1
      umask: int = 0o022

    CRITICAL: sonarr_api_key and radarr_api_key MUST be `SecretStr` — not `str`. Never store the resolved value. The resolve happens only in `build_sonarr_client()` / `build_radarr_client()` per 03-PATTERNS.md §"SecretStr for API Keys".

    CRITICAL: Do NOT call `os.umask()` anywhere — that is process-global and unsafe in async code. The umask field in settings is just the int value; `apply_permissions()` will compute `0o666 & ~settings.umask` when applying (03-RESEARCH.md Pattern 7).

    Circular import note: `PathMapping` is defined in `trezarr/paths.py` (created in plan 03-02 Task 2 below), but config.py imports it at module level. Since paths.py will be a pure-stdlib module with no trezarr imports, the import chain is: config.py → paths.py (stdlib only) — no cycle. (Codex LOW #18 considered: PathMapping could live in config.py for ownership cleanliness, but moving it would force paths.py to import config.py for the type, creating an import cycle. Keep current placement.)

    After adding fields, update the `settings_factory` in tests/conftest.py (if it exists) to add safe defaults for the new arr fields. Read conftest.py first to see if a fixture needs updating.

    XFAIL REMOVAL POLICY (per 03-REVIEWS.md HIGH #3): As the FINAL step of this task, after all assertions pass GREEN, remove the `@pytest.mark.xfail(strict=False)` marker from the three config stubs added in Plan 03-01 (test_sonarr_host_env_override, test_sonarr_api_key_not_in_repr, test_path_mappings_yaml_load) plus the two mutable-default stubs (test_path_mappings_no_shared_default_identity, test_source_lang_priority_no_shared_default_identity). After removal, re-run `uv run pytest tests/config/ -x -q` and confirm all five are now plain green tests (no xfail, no xpassed).
  </action>
  <verify>
    <automated>uv run pytest tests/config/ -x -q && uv run ruff check trezarr/config.py && ! grep -E "xfail" tests/config/test_settings.py | grep -E "sonarr_host_env_override|sonarr_api_key_not_in_repr|path_mappings_yaml_load|no_shared_default_identity"</automated>
  </verify>
  <done>tests/config/ stubs pass GREEN; xfail markers removed from the five config stubs covered by this task; `str(settings)` does not contain the sonarr_api_key raw value; two TrezarrSettings() instances do NOT share `path_mappings` or `source_lang_priority` list identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement trezarr/paths.py + ledger docstring clarification</name>
  <files>trezarr/paths.py, trezarr/output/ledger.py</files>
  <read_first>
    - trezarr/output/write.py (full file — the structural analog: module docstring citing decisions, from __future__ import annotations, pure-function pattern, defensive normalization; lines cited in 03-PATTERNS.md §"trezarr/paths.py (utility, transform)")
    - trezarr/output/ledger.py (full file — LedgerEntry dataclass fields; find content_hash field to add the docstring clarification per D-27; current lines 46-47 per 03-PATTERNS.md)
    - 03-RESEARCH.md §"Pattern 3: Path-Mapping Algorithm" (apply_path_mapping algorithm with trailing-slash normalization)
    - 03-RESEARCH.md §"Pattern 4: Startup Readability Probe (D-24)" (probe_media_roots implementation)
    - 03-RESEARCH.md §"Pattern 3" assertion (assert_within_media_roots — path-traversal guard)
    - 03-RESEARCH.md §"Pattern 6: Ledger Extension for D-27" (decision: content_hash IS the source-sub hash; add docstring, no new field)
    - 03-PATTERNS.md §"trezarr/paths.py (utility, transform)" (exact import block, docstring format, defensive normalization)
    - 03-REVIEWS.md §"03-02-PLAN.md Review → HIGH #2" (empty-media_roots refusal); §"MEDIUM #8" (writable probe is a WARN, not exit); §"MEDIUM #9" (resolve nonexistent target via parent)
    - tests/test_paths.py (read before implementing — ensures implementation matches the stub assertions exactly)
  </read_first>
  <behavior>
    - test_path_mapping_replaces_prefix: PathMapping(remote="/tv", local="/data/media/tv") + api_path="/tv/Show/S1/ep.mkv" → Path("/data/media/tv/Show/S1/ep.mkv")
    - test_path_mapping_no_match_passthrough: no matching mapping → Path(api_path) unchanged
    - test_path_mapping_strips_trailing_slash: remote="/tv/" (trailing slash) + api_path="/tv/Show/ep.mkv" → correct replacement (slash stripped before comparison)
    - test_longest_prefix_wins: mappings=[PathMapping(remote="/tv/Show", local="/data/A"), PathMapping(remote="/tv", local="/data/B")] + api_path="/tv/Show/S1/ep.mkv" → /data/A/S1/ep.mkv (longer prefix wins)
    - test_traversal_guard_raises_outside_roots: resolved path /etc/passwd with media_roots=[Path("/data")] → raises ValueError
    - test_traversal_guard_allows_inside: resolved /data/tv/Show.mkv with media_roots=[Path("/data")] → no exception
    - test_traversal_guard_handles_nonexistent_sidecar: `assert_within_media_roots(tmp_path / "new_unwritten.vi.srt", [tmp_path])` → no exception (target does not exist; implementation resolves `resolved.parent` for nonexistent paths)
    - test_probe_unreadable_root_exits: probe_media_roots([nonexistent_path]) → pytest.raises(SystemExit)
    - test_probe_readable_root_passes: probe_media_roots([tmp_path]) → no exception (tmp_path exists and is readable)
    - test_probe_warns_on_non_writable_root: tmp_path with mode 0o500 (read+execute, no write) → probe_media_roots does NOT exit; emits a WARNING log line containing "not writable"
    - test_assert_media_roots_configured_refuses_when_empty_and_arr_enabled: TrezarrSettings(path_mappings=[], sonarr_enabled=True) → pytest.raises(SystemExit) from assert_media_roots_configured(settings)
  </behavior>
  <action>
    Create trezarr/paths.py as a new file. Follow 03-PATTERNS.md §"trezarr/paths.py (utility, transform)" exactly:

    1. Module docstring citing D-23, D-24, D-29 (per decision-citation docstrings pattern).
    2. `from __future__ import annotations` as first line.
    3. Imports: os, sys, logging, from pydantic import BaseModel, pathlib.Path, typing.Sequence, typing.TYPE_CHECKING.

    Define `PathMapping` as `pydantic.BaseModel` with fields `remote: str` and `local: str`.

    Implement these five functions:

    `apply_path_mapping(api_path: str, mappings: Sequence[PathMapping]) -> Path` (per 03-RESEARCH.md Pattern 3 / A2):
      - Strip trailing slash from both api_path and each mapping.remote before comparison
      - Sort mappings by descending len(mapping.remote) at entry (longest prefix wins)
      - For each sorted mapping: if normalized api_path.startswith(remote_norm): return Path(mapping.local.rstrip("/") + api_path[len(remote_norm):])
      - If no match: return Path(api_path) (passthrough)
      - Do NOT lowercase (Linux filesystems are case-sensitive)

    `assert_within_media_roots(resolved: Path, media_roots: Sequence[Path]) -> None` (per 03-REVIEWS.md MEDIUM #9):
      - Use `resolved.resolve(strict=False)` so paths that do not yet exist (newly-written sidecars) are resolved symbolically rather than raising
      - If the resolved target itself does not exist, also check `resolved.parent.resolve(strict=False)` and use whichever resolves successfully against the configured roots
      - For each root: try resolved_abs.relative_to(root.resolve()); return (inside root — OK)
      - If none match: raise ValueError with clear message including the resolved path and the configured roots
      - This is the D-29 path-traversal guard — every write path must pass through this before any write
      - Add a docstring example demonstrating that calling this on a `tmp_path / "new.vi.srt"` that has NOT been created does not raise; this is critical because `apply_permissions` is called AFTER `write_vi_sidecar` writes the file, but we call `assert_within_media_roots` BEFORE the write completes in some code paths

    `build_media_roots(settings: "TrezarrSettings") -> list[Path]`:
      - Extract all `local` values from settings.path_mappings as Path objects
      - Return deduplicated list of Path objects
      - This is what probe_media_roots() and assert_within_media_roots() receive

    `probe_media_roots(media_roots: Sequence[Path]) -> None` (per 03-REVIEWS.md MEDIUM #8):
      - For each root: check root.exists() AND root.is_dir() AND os.access(root, os.R_OK | os.X_OK)
      - Mandatory-failure conditions: missing, not a directory, or not (readable AND executable). Collect all such errors and `sys.exit()` with a clear actionable multi-line message naming each bad root and the remedy ("Verify your path_mappings and volume mounts match the *arr stack.")
      - Soft-warn condition: if a root passes the mandatory check but fails `os.access(root, os.W_OK)`, emit `logger.warning("media root %s is not writable; sidecar writes will fail until permissions are fixed (Phase 7 PUID/PGID may legitimately make this read-only at probe time)", root)`. Do NOT exit on this — Phase 7 PUID/PGID may legitimately make the host-side path read-only at probe time.
      - This calls sys.exit() — the startup probe must fail closed for mandatory conditions (D-24)

    `assert_media_roots_configured(settings: "TrezarrSettings") -> None` (NEW — per 03-REVIEWS.md HIGH #2):
      - If `settings.path_mappings` is empty AND (`settings.sonarr_enabled` OR `settings.radarr_enabled`):
        - sys.exit() with a multi-line message: "No path_mappings configured but at least one *arr discovery is enabled. Per D-29, writes must be constrained to configured media roots; an empty media_roots set would silently disable the traversal guard. Configure TREZARR_PATH_MAPPINGS or disable sonarr_enabled/radarr_enabled."
      - If both *arr disabled AND path_mappings is empty: log INFO "no media roots configured and no *arr discovery enabled — passthrough OK (nothing will be written)" and return.
      - Otherwise return silently.
      - This is the formal enforcement of D-29 against the empty-mappings edge case the reviewer flagged. cli.py (Plan 03-05) MUST call this BEFORE probe_media_roots.

    TYPE_CHECKING guard for TrezarrSettings in build_media_roots and assert_media_roots_configured signatures (per 03-PATTERNS.md §"TYPE_CHECKING import guard").

    THEN extend trezarr/output/ledger.py:
    - Find `content_hash` field in `LedgerEntry` (currently has no docstring per SUMMARY)
    - Add a field docstring / comment: `# Source-subtitle content hash (SHA-256[:16] of source bytes). This IS the source-sub hash — not the vi-sidecar hash. D-27.`
    - Do NOT add a new `source_sub_hash` field — content_hash already serves this purpose and adding a second field would create the divergence risk described in 03-RESEARCH.md Pitfall 7. Document = "content_hash IS source_sub_hash" decision in the comment.
    - No other changes to ledger.py.

    XFAIL REMOVAL POLICY (per 03-REVIEWS.md HIGH #3): As the FINAL step of this task, after all assertions pass GREEN, remove the `@pytest.mark.xfail(strict=False)` marker from every test in tests/test_paths.py that covers a function defined in trezarr/paths.py (all 10 path stubs). After removal, re-run `uv run pytest tests/test_paths.py -x -q` and confirm all are now plain green tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_paths.py tests/config/ -x -q && uv run ruff check trezarr/paths.py trezarr/output/ledger.py && ! grep -E "xfail" tests/test_paths.py</automated>
  </verify>
  <done>All 10 test_paths.py stubs pass GREEN; xfail markers removed from all tests/test_paths.py tests. All config stubs pass GREEN. `trezarr/paths.py` exists with apply_path_mapping, assert_within_media_roots, probe_media_roots, build_media_roots, assert_media_roots_configured, and PathMapping exported. LedgerEntry.content_hash has the D-27 source-sub hash comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *arr API response → local filesystem | API-returned paths are treated as untrusted until path-mapped and validated within media roots |
| config env/YAML → settings fields | pydantic-settings validates types; SecretStr prevents accidental logging of API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | apply_path_mapping → write path | mitigate | assert_within_media_roots() called on every resolved path before any write; raises ValueError on escape (D-29); handles nonexistent targets via resolve(strict=False) + parent resolution |
| T-03-02-02 | Information Disclosure | sonarr_api_key / radarr_api_key in logs | mitigate | Both fields are SecretStr; .get_secret_value() called only inside build_sonarr_client() / build_radarr_client() in the arr package — never stored as str on settings |
| T-03-02-03 | Tampering | Path-traversal via empty or dangerously-broad path_mappings | mitigate | assert_media_roots_configured() refuses startup when path_mappings is empty AND any *arr is enabled (per 03-REVIEWS.md HIGH #2); assert_within_media_roots() validates each resolved WRITE target is inside a configured local root; probe_media_roots() verifies roots are readable+executable at startup |
| T-03-02-04 | Denial of Service | probe_media_roots fails closed on unreadable root | accept | By design (D-24) — failing closed is correct; the error message is actionable |
</threat_model>

<verification>
uv run pytest tests/test_paths.py tests/config/ -x -q — all pass GREEN; no xfail remaining
uv run pytest tests/ -x -q — full suite green (no regressions)
grep -n "source-subtitle" trezarr/output/ledger.py — shows D-27 comment on content_hash field
grep -v "^#" trezarr/paths.py | grep -c "assert_within_media_roots" — returns >= 1 (function defined)
grep -v "^#" trezarr/paths.py | grep -c "assert_media_roots_configured" — returns >= 1 (function defined)
grep -E "Field\(default_factory" trezarr/config.py | grep -c -E "path_mappings|source_lang_priority" — returns 2
! grep -E "xfail" tests/test_paths.py — no xfail markers on path tests
</verification>

<success_criteria>
- trezarr/paths.py created with PathMapping, apply_path_mapping, assert_within_media_roots (handles nonexistent targets), probe_media_roots (warns on non-writable; exits on non-readable), build_media_roots, assert_media_roots_configured
- Longest-prefix matching: sorting by descending len(remote) before iteration
- Trailing-slash normalization in apply_path_mapping
- sys.exit() called with clear message when probe fails on mandatory conditions (D-24 fail-fast)
- assert_media_roots_configured refuses startup when path_mappings empty AND any *arr enabled (03-REVIEWS.md HIGH #2)
- All 10 test_paths.py stubs GREEN; xfail markers removed from tests/test_paths.py
- TrezarrSettings has all 14 new Phase-3 fields; SecretStr for both API keys; path_mappings and source_lang_priority use Field(default_factory=...)
- LedgerEntry.content_hash has D-27 docstring ("source-subtitle content hash")
- All tests/config/ stubs GREEN; xfail markers removed from the 5 config stubs covered here
- Full suite remains green
- All HIGH-severity concerns from 03-REVIEWS.md affecting this plan are addressed (#1 mutable defaults, #2 empty-roots refusal, #3 xfail removal on this plan's tests)
</success_criteria>

<output>
Create `.planning/phases/03-arr-integration-first-vertical-slice/03-02-SUMMARY.md` when done.
</output>
