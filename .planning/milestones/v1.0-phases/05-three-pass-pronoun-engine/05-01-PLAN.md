---
phase: 05-three-pass-pronoun-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/translate/test_analyze.py
  - tests/translate/test_attribute.py
  - tests/translate/test_reconcile.py
  - tests/translate/test_pronoun_engine.py
  - tests/bible/test_address_map.py
  - tests/translate/__init__.py
autonomous: true
requirements: [ENG-04, BIBLE-03, PRON-01, PRON-02, PRON-03]

must_haves:
  truths:
    - "Five new test files exist in tests/translate/ and tests/bible/ containing xfail-marked RED stubs covering every row in 05-VALIDATION.md (including test_tier3_endpoint_degrades_gracefully for D-47 Tier-3 graceful degrade — ENG-04/PRON-03)"
    - "All stubs fail with xfail(strict=False) — not ImportError — so the suite stays green at Wave 0 commit"
    - "tests/bible/conftest.py re-exports session_factory from tests/db/conftest.py so address-map tests have DB access"
  artifacts:
    - path: "tests/translate/test_analyze.py"
      provides: "ENG-04 RED stubs: test_pass1_runs_before_pass3, test_pass1_failure_quarantines"
      contains: "pytest.mark.xfail"
    - path: "tests/translate/test_attribute.py"
      provides: "PRON-01 RED stubs: test_attribution_parsing, test_unmatched_name_safe_default"
      contains: "pytest.mark.xfail"
    - path: "tests/translate/test_reconcile.py"
      provides: "PRON-03 / Success #3/#4 RED stubs: test_low_confidence_safe_default, test_high_confidence_uses_address_map, test_reciprocal_coherence, test_below_threshold_ignores_address_map"
      contains: "pytest.mark.xfail"
    - path: "tests/translate/test_pronoun_engine.py"
      provides: "PRON-02 integration RED stubs: test_pronoun_hint_in_prompt, test_pronoun_consistency_within_episode"
      contains: "pytest.mark.xfail"
    - path: "tests/bible/test_address_map.py"
      provides: "BIBLE-03 RED stubs: test_upsert_address_pair, test_locked_pair_not_overwritten"
      contains: "pytest.mark.xfail"
  key_links:
    - from: "tests/bible/test_address_map.py"
      to: "tests/bible/conftest.py session_factory"
      via: "fixture import"
      pattern: "session_factory"
    - from: "tests/translate/test_pronoun_engine.py"
      to: "trezarr.translate.engine.translate_file"
      via: "xfail import stub"
      pattern: "translate_file"
---

<objective>
Create the Wave 0 test scaffold — five RED stub files that establish the Nyquist-compliant test surface for all Phase-5 behavioral requirements. Every row in 05-VALIDATION.md § Per-Task Verification Map must have a corresponding xfail-marked stub that can be turned GREEN by subsequent implementation plans. The suite must pass at the end of this plan (all stubs marked xfail(strict=False)).

Purpose: Without these stubs, later plans have no test surface to validate against. All five implementation plans (02-06) write production code to turn these stubs GREEN. Having the stubs first prevents "write code then write tests to match" drift.

Output: Five new test files + an __init__.py confirming the tests/translate/ package, all xfail, suite exits 0.
</objective>

<execution_context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/PROJECT.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/ROADMAP.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-VALIDATION.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs — translate/ package stubs (test_analyze, test_attribute, test_reconcile, test_pronoun_engine)</name>

  <read_first>
  - tests/translate/ directory listing (check for __init__.py)
  - tests/bible/conftest.py (see how session_factory is re-exported from tests/db/conftest.py — mirror exactly)
  - tests/db/conftest.py (lines 1-80 — db_engine + session_factory fixture pattern with pytest_asyncio.fixture)
  - tests/translate/test_engine.py (see existing xfail stub pattern: @pytest.mark.xfail(strict=False, reason="...", raises=...) — Phase-1 established this convention)
  - .planning/phases/05-three-pass-pronoun-engine/05-VALIDATION.md (§ Per-Task Verification Map — authoritative list of all test IDs to stub)
  </read_first>

  <behavior>
  - test_analyze.py: stubs for test_pass1_runs_before_pass3 (xfail, imports trezarr.bible.analyze), test_pass1_failure_quarantines (xfail, imports BibleAnalysisError)
  - test_attribute.py: stubs for test_attribution_parsing (xfail, imports LineAttribution from trezarr.translate.attribute), test_unmatched_name_safe_default (xfail)
  - test_reconcile.py: stubs for test_low_confidence_safe_default (xfail), test_high_confidence_uses_address_map (xfail), test_reciprocal_coherence (xfail), test_below_threshold_ignores_address_map (xfail) — all import from trezarr.translate.reconcile
  - test_pronoun_engine.py: stubs for test_pronoun_hint_in_prompt (xfail, imports build_translate_prompt), test_pronoun_consistency_within_episode (xfail, imports translate_file), test_tier3_endpoint_degrades_gracefully (xfail, imports translate_file — D-47 Tier-3 graceful degrade)
  - All stubs use xfail(strict=False, reason="...", raises=ImportError) so they pass at Wave 0 (modules don't exist yet)
  - tests/translate/__init__.py must exist (empty file) if not already present
  </behavior>

  <action>
  Check whether tests/translate/__init__.py exists. If not, create it (empty). Create tests/translate/test_analyze.py, tests/translate/test_attribute.py, tests/translate/test_reconcile.py, and tests/translate/test_pronoun_engine.py.

  Each file follows the exact xfail stub pattern from tests/translate/test_engine.py Phase-1 convention:
  - Module-level docstring explaining what this file will test
  - Imports: `import pytest` (and `pytest_asyncio` where async fixtures are needed); all `trezarr.*` imports inside the test function body under the xfail wrapper
  - Each stub: `@pytest.mark.xfail(strict=False, reason="[module] not yet implemented", raises=ImportError)` then `def test_<name>(): ...` or `async def test_<name>():`

  CRITICAL: Do NOT import trezarr.bible.analyze, trezarr.translate.attribute, trezarr.translate.reconcile, or their symbols at module level — those modules don't exist yet and would break collection. Import them inside the test function body only.

  test_analyze.py stubs (per ENG-04 rows in 05-VALIDATION.md):
  - test_pass1_runs_before_pass3: xfail, body imports `from trezarr.bible.analyze import analyze_file`
  - test_pass1_failure_quarantines: xfail, body imports `from trezarr.bible.analyze import BibleAnalysisError`

  test_attribute.py stubs (per PRON-01 rows):
  - test_attribution_parsing: xfail, body imports `from trezarr.translate.attribute import LineAttribution, BatchAttribution`
  - test_unmatched_name_safe_default: xfail, body imports `from trezarr.translate.attribute import attribute_batch`

  test_reconcile.py stubs (per PRON-03 + Success #3/#4 rows):
  - test_low_confidence_safe_default: xfail, body imports `from trezarr.translate.reconcile import reconcile_attributions, get_safe_default`
  - test_high_confidence_uses_address_map: xfail, body imports `from trezarr.translate.reconcile import reconcile_attributions`
  - test_reciprocal_coherence: xfail, body imports `from trezarr.translate.reconcile import KINSHIP_RECIPROCAL`
  - test_below_threshold_ignores_address_map: xfail, body imports `from trezarr.translate.reconcile import get_safe_default`

  test_pronoun_engine.py stubs (per PRON-02 rows + D-47 Tier-3 coverage):
  - test_pronoun_hint_in_prompt: xfail, body imports `from trezarr.translate.engine import build_translate_prompt`
  - test_pronoun_consistency_within_episode: xfail, body imports `from trezarr.translate.engine import translate_file`
  - test_tier3_endpoint_degrades_gracefully: xfail, body imports `from trezarr.translate.engine import translate_file` (D-47: Tier-3 endpoint → translate_file completes, result status == "done", valid .vi.srt written, no quarantine)
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/translate/test_analyze.py tests/translate/test_attribute.py tests/translate/test_reconcile.py tests/translate/test_pronoun_engine.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>

  <done>All four new test files collected by pytest with zero collection errors; all stubs xfail (not ERROR or FAILED); suite exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test stubs — bible/ address-map stubs (test_address_map, conftest extension)</name>

  <read_first>
  - tests/bible/conftest.py (current content — check if session_factory is already re-exported from tests/db/conftest.py)
  - tests/db/conftest.py (db_engine + session_factory fixture definitions)
  - tests/bible/test_merge_inferred.py (closest analog for DB-backed bible test stubs — xfail pattern with session_factory fixture arg)
  - .planning/phases/05-three-pass-pronoun-engine/05-VALIDATION.md (§ BIBLE-03 rows for test IDs)
  </read_first>

  <behavior>
  - tests/bible/conftest.py: if session_factory fixture is not already re-exported from tests/db/conftest.py, add it. The address-map test needs it for in-memory SQLite address_map INSERT/SELECT.
  - tests/bible/test_address_map.py: two xfail stubs — test_upsert_address_pair (xfail, imports upsert_address_pair from trezarr.bible.store) and test_locked_pair_not_overwritten (xfail, same import). Both are async.
  - Stubs use raises=ImportError or raises=(ImportError, AttributeError) because upsert_address_pair doesn't exist yet in store.py
  </behavior>

  <action>
  Read tests/bible/conftest.py. If `session_factory` fixture is already present (re-exported from tests/db/conftest.py), do not modify it. If it is absent, add the re-export following the exact pattern from tests/db/conftest.py: `@pytest_asyncio.fixture async def session_factory(db_engine): return async_sessionmaker(db_engine, expire_on_commit=False)`.

  Create tests/bible/test_address_map.py with:
  - Module docstring: "Tests for Address Map store functions (BIBLE-03): upsert_address_pair, test_locked_pair_not_overwritten."
  - `import pytest` and `import pytest_asyncio` at module level.
  - CRITICAL: Do NOT import `from trezarr.bible.store import upsert_address_pair` at module level — import inside each test body.
  - test_upsert_address_pair: `@pytest.mark.xfail(strict=False, reason="upsert_address_pair not yet in store.py", raises=(ImportError, AttributeError))` then `async def test_upsert_address_pair(session_factory): from trezarr.bible.store import upsert_address_pair; assert False, "stub"`
  - test_locked_pair_not_overwritten: same xfail decorator, `async def test_locked_pair_not_overwritten(session_factory): from trezarr.bible.store import upsert_address_pair; assert False, "stub"`
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/bible/test_address_map.py -v --tb=short 2>&1 | tail -15</automated>
  </verify>

  <done>tests/bible/test_address_map.py collects without errors; both stubs xfail (not ERROR); `uv run pytest -x -q` exits 0 (full suite still green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production import | xfail stubs import production modules inside test bodies; a collection-time import error would break the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | test stub imports at module level | mitigate | All trezarr.* imports inside test function body only; xfail(raises=ImportError) prevents collection failures |
| T-05-W0-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Wave 0; no install tasks; residual risk is LOW |

Residual risk after mitigations: LOW. No auth surface, no new packages, no data written.
</threat_model>

<verification>
Full suite check after both tasks:
`uv run pytest -x -q` exits 0 — all existing tests still pass; new stubs xfail.
Confirm 10 new stubs collected across 5 files:
`uv run pytest tests/translate/test_analyze.py tests/translate/test_attribute.py tests/translate/test_reconcile.py tests/translate/test_pronoun_engine.py tests/bible/test_address_map.py --collect-only -q | grep "test session starts" -A 50 | grep "test_" | wc -l` should show 11+
</verification>

<success_criteria>
- Five new test files collected by pytest with no collection errors
- All 10+ stubs report xfail (not FAILED, not ERROR)
- `uv run pytest -x -q` exits 0 (full suite green with new stubs)
- Every row in 05-VALIDATION.md § Per-Task Verification Map has a concrete stub test function matching the automated command
</success_criteria>

<output>
Create `.planning/phases/05-three-pass-pronoun-engine/05-01-SUMMARY.md` when done.
</output>
