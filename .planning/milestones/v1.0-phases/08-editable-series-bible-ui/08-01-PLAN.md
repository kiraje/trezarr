---
phase: 08-editable-series-bible-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/bible/test_human_edit.py
  - tests/web/test_bible_api.py
  - tests/translate/test_reconcile.py
autonomous: true
requirements:
  - BIBLE-08
  - BIBLE-09

must_haves:
  truths:
    - "tests/bible/test_human_edit.py exists with xfail stubs for all BIBLE-08/09 store-layer behaviors (Character + Term + Register + AddressMap)"
    - "tests/web/test_bible_api.py exists with xfail stubs for all BIBLE-08 API-layer behaviors and the D-39 boundary check"
    - "tests/translate/test_reconcile.py contains a xfail stub for test_kinship_reciprocal_bac_chau (D-90 H1 regression)"
    - "All stubs report xfail (not FAILED, not ERROR) — pytest collects all three files cleanly"
  artifacts:
    - path: "tests/bible/test_human_edit.py"
      provides: "RED stubs: test_lock_state_in_dto, test_locked_field_survives_merge, test_locked_fields_persisted, test_bible_event_emitted_on_lock, test_locked_pair_survives_reconcile, test_locked_pair_propagates_to_next_episode, test_locked_term_survives_merge, test_locked_register_survives"
    - path: "tests/web/test_bible_api.py"
      provides: "RED stubs: test_get_series_list, test_get_series_bible, test_lock_address_map_missing_term_rejected, test_write_acquires_series_lock, test_get_field_history, test_bible_route_does_not_import_sqla, test_delete_address_pair, test_delete_term"
    - path: "tests/translate/test_reconcile.py"
      provides: "Appended stub: test_kinship_reciprocal_bac_chau"
  key_links:
    - from: "tests/bible/test_human_edit.py"
      to: "trezarr.bible.store.apply_human_edit_character"
      via: "xfail import stub targeting Plan 08-02"
      pattern: "pytest.mark.xfail.*ImportError"
    - from: "tests/web/test_bible_api.py"
      to: "trezarr.web.routes.bible"
      via: "xfail import stub targeting Plan 08-03"
      pattern: "pytest.mark.xfail.*ImportError"
    - from: "tests/translate/test_reconcile.py"
      to: "trezarr.translate.reconcile.KINSHIP_RECIPROCAL"
      via: "appended stub for D-90 bác/cháu pairs"
      pattern: "test_kinship_reciprocal_bac_chau"
---

## Phase Goal

**As an** operator, **I want to** have a full RED test suite for the Bible editor before any implementation exists, **so that** every subsequent plan has automated verification feedback from the first commit.

<objective>
Create the Wave-0 RED test scaffold for Phase 8 per 08-VALIDATION.md. Three files receive stubs — two new test files and one existing reconcile test file extended. All stubs use xfail markers with raises=(ImportError, AssertionError, TypeError) so pytest collects them cleanly against not-yet-created modules.

Purpose: Nyquist-compliant feedback surface — no 3 consecutive plan waves will lack automated verification. Plans 02-04 turn these RED stubs GREEN incrementally.
Output: tests/bible/test_human_edit.py, tests/web/test_bible_api.py (both new), and an appended stub in tests/translate/test_reconcile.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-editable-series-bible-ui/08-VALIDATION.md
@.planning/phases/08-editable-series-bible-ui/08-PATTERNS.md
@tests/bible/test_address_map.py
@tests/web/test_settings_api.py
@tests/translate/test_reconcile.py
@tests/bible/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/bible/test_human_edit.py with all BIBLE-08/09 store-layer xfail stubs</name>
  <files>tests/bible/test_human_edit.py</files>
  <read_first>
    - tests/bible/test_address_map.py:1-85 — exact file header pattern, `_create_series`/`_create_characters` helpers, `async def test_*` stub pattern; copy these helpers verbatim into test_human_edit.py
    - tests/bible/conftest.py — confirms session_factory fixture re-export from tests/db/conftest.py; no new fixture infra needed
    - .planning/phases/08-editable-series-bible-ui/08-VALIDATION.md §Wave 0 Requirements and §Per-Task Verification Map — exact test function names and docstrings for every row
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §tests/bible/test_human_edit.py — file header docstring and helper pattern (lines 873-910)
  </read_first>
  <behavior>
    - test_lock_state_in_dto: applies a human edit lock and asserts locked_fields appears non-empty in the returned DTO
    - test_locked_field_survives_merge: locks character.gender="male", then calls merge_inferred with gender="female", asserts gender is still "male"
    - test_locked_fields_persisted: locks a field, reloads from DB in a fresh session, asserts locked_fields contains the field (verifies JSON dirty-tracking reassignment, not silent loss)
    - test_bible_event_emitted_on_lock: locks a field, queries bible_event table, asserts a row with source="lock" exists for that entity+field
    - test_locked_pair_survives_reconcile: creates + locks an address pair, calls reconcile_attributions with zero attributions for that pair, asserts pair is still in resolved_map
    - test_locked_pair_propagates_to_next_episode: creates + locks an address pair in episode N, simulates episode N+1 with no attributions, asserts pair is still applied (carry-forward + lock combo — BIBLE-09 criterion 3)
    - test_locked_term_survives_merge: locks a TermDictionary row via apply_human_edit_term, then calls merge_inferred with a contradicting value, asserts the term's vietnamese_rendering is unchanged (BIBLE-08 c1: all four entity types lockable)
    - test_locked_register_survives: locks the series register field via apply_human_edit_series, then calls merge_inferred / re-analysis, asserts register value is unchanged (BIBLE-08 c1: all four entity types lockable)
  </behavior>
  <action>
    Create tests/bible/test_human_edit.py. Begin with the exact file header from 08-PATTERNS.md §tests/bible/test_human_edit.py (the docstring covering BIBLE-08 criterion 2, BIBLE-09 criterion 3, and asyncio_mode=auto note). Import only stdlib + pytest at module level; all trezarr imports go inside xfail test bodies.

    Copy `_create_series` and `_create_characters` helpers verbatim from tests/bible/test_address_map.py:22-52 — these use `get_or_create_series` and `upsert_character` and are the exact fixture pattern.

    Write 8 xfail test stubs in this order:
    1. test_lock_state_in_dto — xfail(raises=(ImportError, AssertionError, TypeError), strict=False, reason="apply_human_edit_character not yet created — Plan 08-02"); docstring: "BIBLE-08 c1: locked_fields visible in DTO response"
    2. test_locked_field_survives_merge — xfail same raises/strict; docstring: "BIBLE-08 c2: human-locked character field not overwritten by subsequent merge_inferred with contradicting value"
    3. test_locked_fields_persisted — xfail same; docstring: "BIBLE-08 c2: JSON dirty-tracking — locked_fields persisted correctly (reassign-not-append pattern, D-80)"
    4. test_bible_event_emitted_on_lock — xfail same; docstring: "BIBLE-08 c2: apply_human_edit emits BibleEvent(source='lock') in same transaction (D-32)"
    5. test_locked_pair_survives_reconcile — xfail same; docstring: "BIBLE-09 c3: edit→lock→re-analyze: locked pair survives reconcile_attributions (reconcile.py:267-277)"
    6. test_locked_pair_propagates_to_next_episode — xfail same; docstring: "BIBLE-09 c3: edit→lock→next episode: locked pair still applied (BIBLE-06 carry-forward + lock combo)"
    7. test_locked_term_survives_merge — xfail(raises=(ImportError, AssertionError, TypeError), strict=False, reason="apply_human_edit_term not yet created — Plan 08-02"); docstring: "BIBLE-08 c1: locking a Term via apply_human_edit_term; locked vietnamese_rendering survives merge_inferred (all four entity types lockable)"
    8. test_locked_register_survives — xfail same reason applies to apply_human_edit_series; docstring: "BIBLE-08 c1: locking series register via apply_human_edit_series; locked register_value survives re-analysis (all four entity types lockable, register_value alias per CR-02)"

    Each stub body: one trezarr import inside the body (to trigger ImportError on xfail), then `assert False` as the placeholder assertion.

    asyncio_mode=auto is project-wide (pyproject.toml) — no @pytest.mark.asyncio decorators needed. All test functions are `async def`.
  </action>
  <verify>
    <automated>uv run pytest tests/bible/test_human_edit.py -v --tb=short 2>&1 | grep -E "XFAIL|PASSED|FAILED|ERROR|test session"</automated>
  </verify>
  <done>8 xfail stubs in tests/bible/test_human_edit.py; all report xfail in pytest output.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/web/test_bible_api.py (API stubs) and append D-90 stub to tests/translate/test_reconcile.py</name>
  <files>tests/web/test_bible_api.py, tests/translate/test_reconcile.py</files>
  <read_first>
    - tests/web/test_settings_api.py:1-64 — `create_app()` + `httpx.AsyncClient(ASGITransport(...))` pattern; exact imports and async test structure to mirror
    - tests/translate/test_reconcile.py — existing file content; append only after the last existing test; do NOT overwrite
    - .planning/phases/08-editable-series-bible-ui/08-VALIDATION.md §Per-Task Verification Map — exact test names: test_get_series_list, test_get_series_bible, test_lock_address_map_missing_term_rejected, test_write_acquires_series_lock, test_get_field_history, test_bible_route_does_not_import_sqla, test_kinship_reciprocal_bac_chau
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §tests/web/test_bible_api.py (lines 913-940) — D-39 boundary test using inspect.getsource
  </read_first>
  <action>
    Create tests/web/test_bible_api.py. Begin with `from __future__ import annotations` and a module docstring citing BIBLE-08, D-39, D-81, D-82, D-83, D-87. All trezarr imports inside test bodies (xfail pattern).

    Write 8 xfail stubs (all xfail(raises=(ImportError, AssertionError, TypeError), strict=False)):
    1. test_get_series_list — reason="trezarr.web.routes.bible not yet created — Plan 08-03"; docstring: "BIBLE-08 c1: GET /api/series returns list; bounded (LIMIT 500 per security domain)"
    2. test_get_series_bible — same reason; docstring: "BIBLE-08 c1: GET /api/series/{id}/bible returns full Bible with characters/address_map/terms/register"
    3. test_lock_address_map_missing_term_rejected — same reason; docstring: "BIBLE-08 c2 / T-08-01: lock AddressMap with empty address_term → HTTP 422 (reconcile.py:275 non-None guard, D-87)"
    4. test_write_acquires_series_lock — same reason; docstring: "D-81: UI write during Pass-1 inference acquires per-series asyncio.Lock (no interleave)"
    5. test_get_field_history — same reason; docstring: "D-82 / T-08-02: GET /api/series/{id}/bible/{entity_type}/{entity_id}/history returns BibleEventDTOs (LIMIT 100)"
    6. test_bible_route_does_not_import_sqla — same reason; docstring: "D-39: bible route module must not import SQLAlchemy models or sqlalchemy directly"
    7. test_delete_address_pair — same reason; docstring: "D-83: DELETE /api/series/{id}/address-map/{aid} removes the leaf row and returns {deleted: id}; calls delete_address_pair store function (no inline SQLAlchemy in route, D-39)"
    8. test_delete_term — same reason; docstring: "D-83: DELETE /api/series/{id}/terms/{tid} removes the leaf row and returns {deleted: id}; calls delete_term store function (no inline SQLAlchemy in route, D-39)"

    The test_bible_route_does_not_import_sqla stub body: import `inspect`, import `trezarr.web.routes.bible`, call `inspect.getsource(bible_module)`, assert `"from trezarr.bible.models" not in src` and `"from sqlalchemy" not in src`.

    The test_delete_address_pair stub body: inside xfail, import delete_address_pair from trezarr.bible.store (the store function Plan 02 creates), then assert False.

    The test_delete_term stub body: inside xfail, import delete_term from trezarr.bible.store, then assert False.

    Append to tests/translate/test_reconcile.py (do NOT overwrite — append after the last existing test):
    - test_kinship_reciprocal_bac_chau — xfail(raises=(AssertionError,), strict=False, reason="KINSHIP_RECIPROCAL bác/cháu gaps not yet filled — Plan 08-02 D-90"); docstring: "D-90 H1 fix: bác/cháu, chú/cháu, cô/cháu, thầy/em, tao/mày round-trip in KINSHIP_RECIPROCAL; junior-only attribution on bác/cháu does not overwrite locked pair"; body: from trezarr.translate.reconcile import KINSHIP_RECIPROCAL, assert ("bác","cháu") in KINSHIP_RECIPROCAL and ("cháu","bác") in KINSHIP_RECIPROCAL
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_bible_api.py tests/translate/test_reconcile.py::test_kinship_reciprocal_bac_chau -v --tb=short 2>&1 | grep -E "XFAIL|PASSED|FAILED|ERROR|test session"</automated>
  </verify>
  <done>9 xfail stubs across 2 files; all report xfail cleanly. (8 in test_bible_api.py + 1 appended to test_reconcile.py.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → application modules | Tests import trezarr modules; xfail stubs expect ImportError on not-yet-created modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — zero install tasks |

Note: This is a test-scaffold-only plan. The substantive security threats (input validation, entity_type allowlist, LIMIT bounds) are registered in Plans 02-03 where the actual implementation code lives.
</threat_model>

<verification>
uv run pytest tests/bible/test_human_edit.py tests/web/test_bible_api.py tests/translate/test_reconcile.py::test_kinship_reciprocal_bac_chau -v --tb=short
</verification>

<success_criteria>
17 xfail stubs across 3 files: 8 in tests/bible/test_human_edit.py, 8 in tests/web/test_bible_api.py, 1 appended to tests/translate/test_reconcile.py. pytest reports 0 FAILED, 0 ERROR, all stubs xfail. The test suite grows by 17 tests without breaking any existing passing tests.
</success_criteria>

<output>
Create `.planning/phases/08-editable-series-bible-ui/08-01-SUMMARY.md` when done.
</output>
