---
phase: 08-editable-series-bible-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/App.tsx
  - frontend/src/components/AppShell.tsx
  - frontend/src/pages/BibleList.tsx
  - frontend/src/pages/BibleEditor.tsx
  - frontend/src/components/LockBadge.tsx
  - frontend/src/components/LockToggleButton.tsx
  - frontend/src/components/FieldHistoryPanel.tsx
  - frontend/src/components/PronounCombo.tsx
  - frontend/src/components/ReciprocalSuggestionPanel.tsx
autonomous: false
requirements:
  - BIBLE-08
  - BIBLE-09

must_haves:
  truths:
    - "Navigating to /bible shows a list of all series with character/term counts and register value (BIBLE-08 criterion 1)"
    - "Clicking a series opens /bible/:id with Characters, Address Map, Terms, Register tabs — all fields visible with LockBadge provenance state"
    - "Editing a character field and saving calls PATCH; the LockBadge updates from the API response without page reload"
    - "Locking an Address-Map pair with an empty term shows the hard-block error inline (D-87); the Lock button is disabled"
    - "The reciprocal suggestion panel appears when both self_term and address_term are changed (D-85); confirming sends two PATCH calls"
    - "The pronoun term picker shows Vietnamese options served by GET /api/pronouns, not a static list embedded in TypeScript (D-86)"
    - "The Bible NavItem in AppShell is no longer disabled — active nav link shows accent color on /bible pages (D-84)"
    - "FieldHistoryPanel expands inline below the row on Clock icon click — not a modal (UI-SPEC note 9)"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "SeriesListItem, CharacterDTO, AddressMapDTO, TermDTO, BibleEventDTO, SeriesBibleDTO, PronounsResponse interfaces + getSeriesList, getSeriesBible, patchCharacter, patchAddressMapPair, addCharacter, addTerm, addAddressMapPair, deleteTerm, deleteAddressMapPair, getFieldHistory, getPronouns, patchRegister wrappers"
    - path: "frontend/src/pages/BibleList.tsx"
      provides: "Series list page: fetch-on-mount, table with Name/Characters/Terms/Register columns, row-click nav to /bible/:id, empty/error/loading states"
    - path: "frontend/src/pages/BibleEditor.tsx"
      provides: "Series Bible editor page: fetch-on-mount (getSeriesBible + getPronouns in parallel), BibleTabBar, Characters/AddressMap/Terms/Register sections, inline row edit, save/discard/lock/unlock flows, toast feedback"
    - path: "frontend/src/components/LockBadge.tsx"
      provides: "locked/inference provenance badge — 4px dot + text label, LOCK_CONFIG palette from StatusBadge done/queued colors"
    - path: "frontend/src/components/LockToggleButton.tsx"
      provides: "32×32 icon-only Lock/Unlock button with aria-label; disabled state when hard-block active"
    - path: "frontend/src/components/FieldHistoryPanel.tsx"
      provides: "Inline expansion panel below row; renders BibleEventDTO timeline; max-height 320px overflow-y scroll"
    - path: "frontend/src/components/PronounCombo.tsx"
      provides: "select-first combo: dropdown from terms prop + 'custom…' escape hatch; not a free-text input by default (D-86 typo guard)"
    - path: "frontend/src/components/ReciprocalSuggestionPanel.tsx"
      provides: "Inline reciprocal suggestion for Address-Map pair editing; pre-filled from KINSHIP_RECIPROCAL lookup; confirm/skip flow (D-85)"
  key_links:
    - from: "frontend/src/pages/BibleEditor.tsx"
      to: "/api/series/{id}/bible"
      via: "getSeriesBible(seriesId) in useEffect on mount"
      pattern: "getSeriesBible"
    - from: "frontend/src/components/PronounCombo.tsx"
      to: "/api/pronouns"
      via: "getPronouns() in BibleEditor mount; passed as `terms` prop to PronounCombo"
      pattern: "getPronouns"
    - from: "frontend/src/pages/BibleEditor.tsx"
      to: "/api/series/{id}/address-map/{aid}"
      via: "patchAddressMapPair({self_term, address_term, lock}) on Save pair"
      pattern: "patchAddressMapPair"
    - from: "frontend/src/components/ReciprocalSuggestionPanel.tsx"
      to: "kinship_reciprocal from PronounsResponse"
      via: "kinship_reciprocal[`${self_term}|${address_term}`] lookup in BibleEditor edit state"
      pattern: "kinship_reciprocal"
---

## Phase Goal

**As an** operator, **I want to** open the Series Bible in the web UI, see all characters/address pairs/terms/register with their provenance, edit any field, lock it, and see the locked state persist — **so that** the human override valve is fully functional and I can correct pronoun pairs before they affect the next episode translation.

<objective>
Build the React SPA layer on top of the proven store + REST API from Plans 02-03. All 10 files are additions or modifications to the existing Phase-7 SPA — no Phase-7 components are changed. The SPA uses functional-first patterns, reuses AppShell/StatusBadge/Toast/api/client.ts, and delivers the full BIBLE-08 criterion 1 user-visible surface.

Purpose: After this plan a real user can navigate to /bible, view all series, open a Bible, edit a field, lock it, confirm the LockBadge updates, and see the history timeline drill-down. BIBLE-08 criteria 1 and 2 are both verifiable in the browser. The human-verify checkpoint explicitly closes BIBLE-08 criterion 1.
Output: 10 modified/new frontend files; human-verify checkpoint confirms the UI contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-editable-series-bible-ui/08-CONTEXT.md
@.planning/phases/08-editable-series-bible-ui/08-PATTERNS.md
@.planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md
@.planning/phases/08-editable-series-bible-ui/08-RESEARCH.md
@frontend/src/App.tsx
@frontend/src/api/client.ts
@frontend/src/components/AppShell.tsx
@frontend/src/components/StatusBadge.tsx
@frontend/src/components/Toast.tsx
@frontend/src/components/MaskedSecretInput.tsx
@frontend/src/pages/Settings.tsx
@frontend/src/pages/Queue.tsx
@.planning/phases/08-editable-series-bible-ui/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client types + wrappers, new components (LockBadge, LockToggleButton, PronounCombo, FieldHistoryPanel, ReciprocalSuggestionPanel)</name>
  <files>
    frontend/src/api/client.ts,
    frontend/src/components/LockBadge.tsx,
    frontend/src/components/LockToggleButton.tsx,
    frontend/src/components/PronounCombo.tsx,
    frontend/src/components/FieldHistoryPanel.tsx,
    frontend/src/components/ReciprocalSuggestionPanel.tsx
  </files>
  <read_first>
    - frontend/src/api/client.ts (full file) — existing fetchWithTimeout helper, interface pattern (lines 38-56), existing section structure; add Bible wrappers after the last existing "// ── Retry" section
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §client.ts (lines 459-607) — exact interface definitions (SeriesListItem, CharacterDTO, AddressMapDTO, TermDTO, BibleEventDTO, SeriesBibleDTO, PronounsResponse) and wrappers (getSeriesList, getSeriesBible, patchCharacter, getPronouns, getFieldHistory); add patchAddressMapPair, patchRegister, addCharacter, addTerm, addAddressMapPair, deleteTerm, deleteAddressMapPair following the same pattern
    - frontend/src/components/StatusBadge.tsx (full file) — LOCK_CONFIG is derived from STATUS_CONFIG done/queued palette; copy the h-5/px-1.5/4px-dot structure verbatim per UI-SPEC; LOCK_CONFIG.locked uses #14291e/#4ade80/#22c55e (StatusBadge "done"); LOCK_CONFIG.inference uses #1e293b/#94a3b8/#64748b (StatusBadge "queued")
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §LockBadge.tsx (lines 793-822) — exact component code; note "Never dot-only — always include text label (accessibility contract)"
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §LockToggleButton — props: {state, onToggle, disabled?}; 32×32 w-8 h-8; lucide Lock/Unlock icons; aria-label "Lock this field" / "Unlock this field"; hover turns text-accent; disabled opacity-50 cursor-not-allowed
    - frontend/src/components/MaskedSecretInput.tsx (full file) — mode-switch pattern (useState(showTyped)) to adapt for PronounCombo's customMode switch; input styling class (lines 36, 80-92) to copy verbatim
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §PronounCombo.tsx (lines 829-870) — exact select/custom-mode component code; the __custom__ sentinel option; autoFocus on text input; "Done" text link clears custom mode
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §FieldHistoryPanel — inline expansion (not modal), max-height 320px overflow-y scroll; Clock icon trigger; each event row: timestamp + source badge + old→new; most-recent first; empty state: "No history for this field." 12px muted
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §ReciprocalSuggestionPanel — bordered inset card; heading "Reciprocal pair (auto-suggested)"; PronounCombo inputs pre-filled; "Confirm reciprocal" (accent) + "Skip — set manually later" (muted); null reciprocal → "No known reciprocal…" + Dismiss; panel auto-dismisses on row nav
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §No Analog Found (lines 1040-1048) — FieldHistoryPanel, ReciprocalSuggestionPanel, LockToggleButton have partial analogs; use UI-SPEC.md §Component Inventory as the spec
  </read_first>
  <action>
    In frontend/src/api/client.ts: add a new `// ── Bible ───` section after the last existing section. Define interfaces: SeriesListItem (id, arr_kind, arr_instance, arr_series_id, register: string|null [NOT register_value — CR-02 alias], locked_fields), CharacterDTO, AddressMapDTO, TermDTO, BibleEventDTO, SeriesBibleDTO, PronounsResponse per 08-PATTERNS.md §client.ts. Add async wrappers: getSeriesList, getSeriesBible, patchCharacter, patchAddressMapPair(seriesId, addressMapId, payload: {self_term, address_term, lock?}), patchRegister(seriesId, payload: {value, lock?}), addCharacter, addTerm, addAddressMapPair, deleteTerm, deleteAddressMapPair, getFieldHistory, getPronouns. Each wrapper: fetchWithTimeout → resp.ok check → resp.json(). Use the exact template from §client.ts lines 463-484.

    Create frontend/src/components/LockBadge.tsx: copy StatusBadge structure exactly per 08-PATTERNS.md §LockBadge.tsx. LockState type "locked" | "inference". LOCK_CONFIG with locked=(#14291e, #4ade80, #22c55e, "Locked") and inference=(#1e293b, #94a3b8, #64748b, "Inference"). Same span structure: h-5 px-1.5 rounded text-xs font-normal + inner 4px dot + text label. Never dot-only.

    Create frontend/src/components/LockToggleButton.tsx: functional component with props `{state: "locked" | "inference", onToggle: () => void, disabled?: boolean}`. 32×32 button (`className="w-8 h-8 flex items-center justify-center rounded"`). State "locked": render lucide `Unlock` icon (14px, text-text-muted); aria-label "Unlock this field". State "inference": render lucide `Lock` icon (14px, text-text-muted); aria-label "Lock this field". On hover: text-accent (hover:text-accent). disabled prop: opacity-50 cursor-not-allowed. Focus ring: `focus:outline-none focus:ring-2 focus:ring-[#3b82f6] focus:ring-offset-2`. onClick calls onToggle (when not disabled).

    Create frontend/src/components/PronounCombo.tsx per 08-PATTERNS.md §PronounCombo.tsx (lines 829-870). Props: `{value: string, onChange: (v: string) => void, terms: string[], placeholder?: string}`. Internal `customMode` state initialized to `!terms.includes(value) && value !== ""`. When customMode=true: `<input type="text">` with autoFocus + "Done" text link. When customMode=false: `<select>` with terms options + `<option value="__custom__">custom…</option>` at bottom. Input/select styling: copy exact className from MaskedSecretInput.tsx input class ("h-9 w-full bg-bg-surface border border-[#2d3148] rounded px-2 text-sm text-[#e2e6f0] focus:outline-[#3b82f6] focus:outline-2 focus:outline-offset-2").

    Create frontend/src/components/FieldHistoryPanel.tsx: functional component with props `{seriesId: number, entityType: string, entityId: number, field?: string, onClose: () => void}`. Fetches `getFieldHistory(seriesId, entityType, entityId, field)` on mount (useEffect with cancellation guard from 08-PATTERNS.md §Shared Patterns). Renders a div card: `bg-bg-surface border border-[#2d3148] rounded p-4` with `max-h-80 overflow-y-auto`. Each event row: `<div className="flex gap-2 text-xs py-1">` — timestamp (text-text-muted, HH:MM DD MMM format) + source badge (muted pill: inference/lock/import/system) + `old → new` values (text-text-primary). Empty state: `<p className="text-xs text-text-muted">No history for this field.</p>`. Loading state: a single muted "Loading…" row.

    Create frontend/src/components/ReciprocalSuggestionPanel.tsx: functional component with props `{forwardSelfTerm: string, forwardAddressTerm: string, reciprocal: {self_term: string, address_term: string} | null, onConfirm: (rec: {self_term: string, address_term: string}) => void, onDismiss: () => void}`. Renders as a bordered inset card (`bg-bg-surface border border-[#2d3148] rounded p-4 mt-2`). Heading: "Reciprocal pair (auto-suggested)" in 12px semibold. If reciprocal non-null: two PronounCombo inputs (pre-filled, editable) + "Confirm reciprocal" button (text-accent, 12px) + "Skip — set manually later" text link (text-text-muted, 12px). If reciprocal null: "No known reciprocal for this pair. Set the reverse pair manually." (12px muted) + "Dismiss" text link. Panel has `role="complementary"`. Focus is moved to the heading on mount (useEffect + ref).
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>API client types and wrappers added. LockBadge, LockToggleButton, PronounCombo, FieldHistoryPanel, ReciprocalSuggestionPanel components created. TypeScript clean. `grep -c "register:" frontend/src/api/client.ts` returns at least 1; `grep -c "register_value" frontend/src/api/client.ts` returns 0.</done>
</task>

<task type="auto">
  <name>Task 2a: BibleList page + App.tsx routes + AppShell Bible nav wiring</name>
  <files>
    frontend/src/pages/BibleList.tsx,
    frontend/src/App.tsx,
    frontend/src/components/AppShell.tsx
  </files>
  <read_first>
    - frontend/src/pages/Queue.tsx (full file) — BibleList.tsx analog: fetch-on-mount pattern (lines 28-53), unreachable banner, table rendering with h-10 rows bg-bg-stripe alternating border-b border-[#2d3148], cursor-pointer hover:bg-[#22263a], useNavigate row-click nav
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §BibleList.tsx (lines 667-710) — table row pattern with useNavigate; Queue.tsx load pattern adapted for getSeriesList
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §BibleList Page — columns: Series(50%)/Characters(15%)/Terms(15%)/Register(20%); empty/loading/error states exact copy text from §Copywriting Contract
    - frontend/src/App.tsx (full file) — existing routes block; find /bible placeholder (lines 33-41) to replace with real BibleList/BibleEditor routes; import pattern
    - frontend/src/components/AppShell.tsx (full file) — NavItem with disabled prop (lines 93-99); remove disabled from Bible NavItem
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §App.tsx (lines 611-636) — exact placeholder replacement and new route additions
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §AppShell.tsx (lines 639-663) — exact change: remove `disabled` prop from Bible NavItem
  </read_first>
  <action>
    Create frontend/src/pages/BibleList.tsx: fetch getSeriesList() on mount with cancellation guard (cancelled flag in useEffect cleanup). States: series (SeriesListItem[]), loading (boolean), unreachable (boolean). Copy UnreachableBanner component from Queue.tsx (same amber banner, exact className). Loading state: heading "Series Bible" (text-lg font-semibold text-text-primary) + 3 skeleton rows (h-10 bg-bg-surface animate-none muted placeholder blocks). Empty state: heading "No series in Bible" + 12px muted body text per Copywriting Contract. Table structure: `<table className="w-full">` with thead (Series/Characters/Terms/Register columns per UI-SPEC widths) and tbody. Each row: h-10, border-b border-[#2d3148], cursor-pointer hover:bg-[#22263a], alternating bg-bg-stripe, onClick navigates to /bible/${series.id} via useNavigate. Columns: series arr_kind+arr_series_id as identifier / character count / term count / register value or "—" (muted).

    In frontend/src/App.tsx: replace the /bible placeholder `<Route>` (lines 33-41) with imports and routes for BibleList and BibleEditor (BibleEditor will be imported in Task 2b but import the placeholder route structure now so routes are consistent):
    - Add `import BibleList from "./pages/BibleList"` at top
    - Add `import BibleEditor from "./pages/BibleEditor"` at top (will be the real component after Task 2b; acceptable to add import now since Task 2b creates the file)
    - Replace placeholder with: `<Route path="/bible" element={<BibleList />} />` and `<Route path="/bible/:seriesId" element={<BibleEditor />} />`

    In frontend/src/components/AppShell.tsx: remove `disabled` prop from the Bible NavItem (lines 93-99). No other changes.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>BibleList.tsx created. App.tsx routes updated (BibleList route active; BibleEditor route added). AppShell disabled prop removed from Bible NavItem. `npm run build` passes clean. `grep -c "BibleList\|BibleEditor" frontend/src/App.tsx` returns at least 2. `grep -c "disabled" frontend/src/components/AppShell.tsx` returns 0 (Bible NavItem block).</done>
</task>

<task type="auto">
  <name>Task 2b: BibleEditor — Characters section + Register section</name>
  <files>
    frontend/src/pages/BibleEditor.tsx
  </files>
  <read_first>
    - frontend/src/pages/Settings.tsx (full file) — BibleEditor.tsx analog: load-on-mount with Promise.all (lines 112-134), SectionCard primitive (lines 42-48), TextField primitive (lines 64-96), Toast usage (lines 116-120), dirty-state tracking, Save button disable when no changes
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §BibleEditor.tsx (lines 715-787) — tab state `type Tab = "characters" | "address_map" | "terms" | "register"`, Promise.all for getSeriesBible + getPronouns, SectionCard/TextField reuse
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §BibleEditor Page — Characters/Register sections; inline row editing pattern; LockBadge placement; hard-block on lock with empty term (D-87 client-side: disable LockToggleButton before request)
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Interaction & State Contracts — edit lifecycle (one row in edit at a time; unsaved-changes prompt); lock toggle lifecycle; warn-but-allow validation conditions
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Copywriting Contract — every CTA exact text: "Save character" / "Discard changes" / "Add Character" / "Save Register" / "Discard"
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Implementation Notes — notes 1-11 are all constraints; read all 11 before implementing
  </read_first>
  <action>
    Create frontend/src/pages/BibleEditor.tsx with the Characters tab section and Register tab section fully implemented. The Address Map and Terms sections (Task 2c) are rendered as `<div className="text-sm text-text-muted p-4">Loading section…</div>` stubs that Task 2c replaces — do NOT use any TODO comment or placeholder text that implies incomplete work; the stub divs are silent and functional within the tab structure.

    Page skeleton: fetch getSeriesBible(seriesId) and getPronouns() in Promise.all on mount. Tab state: useState<"characters" | "address_map" | "terms" | "register">("characters"). BibleTabBar: `<nav role="tablist">` with 4 `<button role="tab" aria-selected={...}>` — 40px height, 14px text-text-muted inactive / text-text-primary + border-b-2 border-[#3b82f6] active. Header breadcrumb + series title. Toast: copy Settings.tsx showToast pattern exactly. Edit state: `editingId: number | null` to track which row is in edit mode.

    Characters section (full implementation):
    - Table with columns: Name/Gender/Age/Role/Lock/History/Actions per UI-SPEC widths
    - Row view mode: text values + LockBadge (state derived from locked_fields.length > 0 ? "locked" : "inference") + Clock icon (triggers FieldHistoryPanel inline below row) + Edit icon (pencil, text-text-muted)
    - Row edit mode (when editingId === char.id): input fields for name/gender/age/role; LockToggleButton; "Save character" and "Discard changes" buttons
    - On save: call patchCharacter(seriesId, charId, {field, value, lock}); on success update local state from response DTO; show toast "Changes saved."
    - On lock toggle: call patchCharacter with lock=!isLocked and current stored value; update LockBadge from response
    - Clock icon: toggle inline FieldHistoryPanel below the row (not a modal); pass entityType="character", entityId=char.id
    - No Delete icon for characters (D-83)
    - "Add Character" button below table: inline form row with name input + Gender select + save/discard

    Register section (full implementation):
    - Single SectionCard with label "Register value"
    - TextField (copy Settings.tsx TextField primitive) with current register value; placeholder "e.g. formal, intimate, historical"
    - LockBadge (locked if "register_value" in series.locked_fields) + LockToggleButton inline right of label
    - "This value may be overwritten on the next episode analysis. Lock to pin it." note (12px muted, blue-tinted bg-[#1e3a5f] text-[#60a5fa] strip) when NOT locked
    - FieldHistoryPanel trigger (Clock icon) for register history; pass entityType="series", entityId=seriesId, field="register_value"
    - "Save Register" button full-width accent; calls patchRegister(seriesId, {value: currentValue, lock: isLocked})
    - Toast on success: "Register saved."

    Address Map tab and Terms tab: silent stub divs that Task 2c replaces.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>BibleEditor.tsx created with Characters and Register sections fully implemented. Address Map and Terms sections are silent stubs. `npm run build` passes clean. `grep -c "register_value" frontend/src/pages/BibleEditor.tsx` returns 0 (CR-02: SPA uses response.register, not response.register_value, for display — ORM field name "register_value" appears only in the patchRegister call payload field key).</done>
</task>

<task type="auto">
  <name>Task 2c: BibleEditor — Address Map section + Terms section (most complex — lock guard, reciprocal panel, delete confirm)</name>
  <files>
    frontend/src/pages/BibleEditor.tsx
  </files>
  <read_first>
    - frontend/src/pages/BibleEditor.tsx (current state after Task 2b) — read the full file; identify where the Address Map stub div and Terms stub div are; replace each with the full section
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §BibleEditor Page — Address Map and Terms sections full spec
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Interaction & State Contracts — reciprocal trigger (fires when BOTH self_term and address_term changed); two PATCH calls on confirm reciprocal; delete confirm inline flow; warn-but-allow conditions
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Copywriting Contract — "Save pair" / "Add Pair" / "Keep pair" / "Save term" / "Add Term" / "Keep term" / "Discard" — must use exact text
    - .planning/phases/08-editable-series-bible-ui/08-UI-SPEC.md §Implementation Notes — note 3 (lock guard D-87), note 5 (reciprocal trigger), note 8 (LockBadge whole-pair), note 11 ("Lock to pin" strip for unlocked pairs in edit mode)
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §BibleEditor.tsx (lines 715-787) — edit state per-row, PronounCombo integration, ReciprocalSuggestionPanel trigger
  </read_first>
  <action>
    Replace the Address Map stub div and Terms stub div in BibleEditor.tsx with full section implementations.

    Address Map section:
    - Table with columns: Speaker/Addressee/Self term/Address term/Lock/Rel. Events/History/Actions per UI-SPEC widths
    - Row view mode: Speaker name + Addressee name + self_term text + address_term text + LockBadge (locked if "self_term" in pair.locked_fields or "address_term" in pair.locked_fields) + relationship events count badge (read-only, D-88) + Clock history icon + Edit icon
    - Row edit mode: PronounCombo for self_term (terms=pronounsData.self_terms) + PronounCombo for address_term (terms=pronounsData.address_terms)
    - D-87 client-side hard-block: if lock=true and (self_term.trim()==="" or address_term.trim()===""): LockToggleButton disabled (opacity-50) + inline `<p aria-live="polite" className="text-xs text-[#f87171]">Both terms must be non-empty before locking a pair.</p>` below the row in edit mode
    - ReciprocalSuggestionPanel trigger: fires when BOTH self_term AND address_term have been modified from their stored values. Look up pronounsData.kinship_reciprocal[`${selfTerm}|${addressTerm}`] → if key exists, pass {self_term, address_term} as reciprocal prop; if not, pass null. Panel appears below the row being edited.
    - On reciprocal confirm: send two PATCH calls in sequence — patchAddressMapPair for the forward pair, then patchAddressMapPair for the reverse pair (look up reverse pair id from bible data, or show toast "Pair saved, but reverse pair update failed. Set it manually." if not found — D-85 spec). Dismiss panel after both calls.
    - "Lock to pin" info strip (D-88 warning): for unlocked pairs in edit mode, show `<div className="bg-[#1e3a5f] text-[#60a5fa] text-xs px-2 py-1 rounded mt-1">Locking is the only way to pin this pair against future analysis runs.</div>`
    - Delete Trash2 icon: visible on unlocked pairs only; inline confirm row replaces action buttons: `<span>Delete this pair?</span> <button>Delete pair</button> <button>Keep pair</button>`. On confirm: call deleteAddressMapPair(seriesId, pair.id); remove from local state; toast "Pair deleted."
    - "Add Pair" button below table: character picker selects (two `<select>` from characters list) + PronounCombo for each term; calls addAddressMapPair; auto-opens reciprocal panel after add
    - Save pair: calls patchAddressMapPair(seriesId, pair.id, {self_term, address_term, lock}); on success update local state; toast "Pair saved."

    Terms section:
    - Table with columns: Source term/Vietnamese rendering/Category/Lock/History/Delete per UI-SPEC
    - Case-sensitivity note below heading: `<p className="text-xs text-text-muted italic mb-2">Terms are case-sensitive. A typo in the source term will appear as a different term.</p>`
    - Row view mode: source_term + vietnamese_rendering + category + LockBadge + Clock history icon + Trash2 delete icon
    - Row edit mode: `<input>` for vietnamese_rendering + `<select>` for category ("proper noun"/"title"/"place"/"jargon") + LockToggleButton + "Save term" + "Discard" buttons
    - On save: call patchTerm (use patchCharacter pattern adapted for terms; the store function is apply_human_edit_term routed through PATCH /api/series/{id}/terms/{tid}); update local state; toast "Term saved."
    - On lock toggle: call patchTerm with lock=!isLocked and current stored value; update LockBadge
    - Delete Trash2: for ALL terms (locked or unlocked — unlike address pairs, locked terms can still be deleted per D-83 leaf-row rule); inline confirm: "Delete this term? [Delete term] [Keep term]". On confirm: call deleteTerm(seriesId, term.id); remove from local state; toast "Term deleted."
    - "Add Term" button below table: two inputs (source_term, vietnamese_rendering) + category select; calls addTerm; toast "Term added."
    - Clock history icon: toggle inline FieldHistoryPanel; entityType="term_dictionary", entityId=term.id
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>BibleEditor.tsx Address Map and Terms sections fully implemented (stubs replaced). `npm run build` passes clean. All four tabs functional: Characters/AddressMap/Terms/Register. Lock guard, reciprocal panel, and delete confirm flows present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase-8 Bible editor UI: /bible Series list → /bible/:id editor with Characters/Address Map/Terms/Register tabs, inline row edit, LockBadge provenance, LockToggleButton, FieldHistoryPanel, PronounCombo, ReciprocalSuggestionPanel. Backend: store writers for all four entity types (Character, AddressMap, Term, Series-register), safe delete functions, REST router, KINSHIP_RECIPROCAL H1 fix, KNOWN_PRONOUN_TERMS constants served by /api/pronouns.

    Approving this checkpoint closes BIBLE-08 criterion 1 ("view + edit + provenance visible in the UI for all four entity types").
  </what-built>
  <how-to-verify>
    Prerequisites: Run `trezarr serve` (or `uv run uvicorn trezarr.web.app:create_app --factory --port 6868`) and open http://localhost:6868.

    1. Navigate to http://localhost:6868/bible — confirm "Series Bible" page heading appears, Bible NavItem is active (accent color, not grayed out).

    2. If no series exist: confirm empty state "No series in Bible" + helper body text appears (not a blank page or error).

    3. If a series exists (process one episode first or seed via `trezarr run --once`):
       a. Click the series row → /bible/:id opens with Characters tab active.
       b. Confirm header shows series identifier and "← Bible" breadcrumb.
       c. Confirm BibleTabBar shows Characters / Address Map / Terms / Register tabs; clicking each switches sections.
       d. Characters tab: confirm table shows Name/Gender/Age/Role columns with LockBadge ("Inference" for all rows initially) and Clock history icon. Confirm no Delete icon appears on any character row.
       e. Click Edit icon on a character → row enters edit mode (inputs appear). Change Gender from "male" to "female". Click "Save character". Confirm toast "Changes saved." and row exits edit mode.
       f. Click LockToggleButton on same character → confirm LockBadge changes to "Locked" and toast "Field locked."
       g. Click Clock icon → FieldHistoryPanel expands inline below the row (not a modal). Confirm at least one history entry appears with "lock" source badge and the field value change. Click Clock again → panel collapses.
       h. Address Map tab: confirm pairs table shows Speaker/Addressee/Self term/Address term columns with PronounCombo-style view (not free-text). Confirm LockBadge and relationship event count badge.
       i. Click Edit on an address pair → row enters edit mode with PronounCombo dropdowns for self_term and address_term. Confirm the dropdown options are populated from the API (not static TypeScript values). Change both terms. Confirm ReciprocalSuggestionPanel appears below the row (with suggestion if pair is in KINSHIP_RECIPROCAL, or "No known reciprocal…" if not).
       j. Clear address_term, then verify LockToggleButton is disabled (opacity-50) and "Both terms must be non-empty before locking a pair." error appears inline.
       k. Fill both terms. Try locking → confirms lock succeeds and toast "Pair saved."
       l. Terms tab: confirm terms table with Source term/Vietnamese rendering/Category/LockBadge/History/Delete columns. Confirm case-sensitivity note below heading. Click Delete on a term → inline confirm "Delete this term? [Delete term] [Keep term]" appears. Click "Keep term" → nothing happens (safety).
       m. Register tab: confirm single SectionCard with "Register value" TextField, LockBadge, LockToggleButton, "Save Register" button. Confirm "This value may be overwritten…" note when not locked.

    4. Navigate away and back to /bible/:id → confirm data reloads correctly (no stale state).

    5. Navigate to /bible → confirm Bible NavItem is highlighted (accent border-b) in the sidebar.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass. Describe any issues if verification fails so Claude can fix them. Approval closes BIBLE-08 criterion 1.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI /api | All Bible write requests cross HTTP; server-side validation is the authoritative gate (client-side validation is UX-only) |
| TypeScript interface → JSON API | CR-02 alias: TS interface uses `register: string|null`, never `register_value`; mismatch silently breaks the Register section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | PronounCombo — custom text input could inject arbitrary values | accept | Custom values are stripped (.trim()) and validated server-side against MERGEABLE_FIELDS; the browser-only guard is UX (D-86 typo prevention); server is authoritative |
| T-08-09 | Information Disclosure | FieldHistoryPanel — exposes old_value/new_value of every edit | accept | Single-user/trusted-LAN deployment (D-78); all history is intentional audit trail (D-32); no PII in Bible fields |
| T-08-10 | Tampering | Two-PATCH reciprocal confirm — forward pair saved but reverse fails silently | accept | Toast "Pair saved, but reverse pair update failed. Set it manually." informs user; forward pair is the operator's primary intent; no rollback attempted (D-85 spec) |
| T-08-11 | Denial of Service | Repeated FieldHistoryPanel opens triggering many GET /history calls | accept | GET /history is read-only and LIMIT 100; no session or server state at risk; single-user deployment |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase; npm build uses existing package.json |
</threat_model>

<verification>
cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build
uv run pytest -x -q
</verification>

<success_criteria>
Vite build passes clean (0 errors) after each of Tasks 2a, 2b, 2c. Full Python test suite green (0 failures). Human-verify checkpoint approved: /bible shows series list; /bible/:id shows Characters/AddressMap/Terms/Register tabs with inline edit, lock/unlock, LockBadge, FieldHistoryPanel, PronounCombo (dropdown terms from API not hardcoded), ReciprocalSuggestionPanel all functional. CR-02 alias correct (register, not register_value) in SPA. BIBLE-08 criteria 1+2 verified in the browser (checkpoint approval closes criterion 1). BIBLE-09 criterion 3 verified by the Phase-02 test suite.
</success_criteria>

<output>
Create `.planning/phases/08-editable-series-bible-ui/08-04-SUMMARY.md` when done.
</output>
