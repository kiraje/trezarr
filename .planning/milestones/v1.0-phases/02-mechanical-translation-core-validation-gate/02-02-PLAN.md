---
phase: 02-mechanical-translation-core-validation-gate
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - trezarr/translate/__init__.py
  - trezarr/translate/sentinel.py
  - trezarr/translate/batching.py
  - trezarr/translate/validate.py
  - trezarr/output/__init__.py
  - trezarr/output/write.py
  - trezarr/config.py
autonomous: true
requirements: [ENG-02, ENG-03, ENG-06, FMT-05]

must_haves:
  truths:
    - "Given a SubDoc with multiple SubLines spanning a 2s+ time gap, batch_subdoc() splits at the gap boundary — the two groups are in separate Batch objects"
    - "Given a cue whose text contains <i>word</i>, extract_sentinels returns cleaned text with <<T0>> and sentinel_map {<<T0>>: <i>word</i>}; reinsert_sentinels restores the original and returns integrity_ok=True"
    - "Given a translated SubDoc with one wrong cue count, validate_subdoc raises GateError with failure.check == 1"
    - "Given a translated SubDoc with all 7 checks passing, validate_subdoc returns None (no exception)"
    - "write_vi_sidecar(doc, 'Show.S01E01.en.srt') creates 'Show.S01E01.vi.srt' atomically and the output file decodes as valid UTF-8"
    - "TrezarrSettings accepts all 8 new Phase-2 fields and their defaults match the values in 02-RESEARCH.md Q1: translate_chars_per_token=3.5, translate_max_cues_per_batch=50, translate_scene_gap_ms=2000, translate_context_lines_k=3, translate_batch_retry_attempts=2, translate_vi_diacritic_ratio=0.70"
  artifacts:
    - path: "trezarr/translate/__init__.py"
      provides: "translate package marker (empty)"
    - path: "trezarr/translate/sentinel.py"
      provides: "extract_sentinels(), reinsert_sentinels() — tag protection per D-12"
      exports: ["extract_sentinels", "reinsert_sentinels", "TAG_RE"]
    - path: "trezarr/translate/batching.py"
      provides: "batch_subdoc() — token-budget batch packer + context window per D-14/D-15"
      exports: ["Batch", "batch_subdoc"]
    - path: "trezarr/translate/validate.py"
      provides: "validate_subdoc() — 7-check pre-write gate per D-16/D-17"
      exports: ["validate_subdoc", "GateError", "GateFailure"]
    - path: "trezarr/output/__init__.py"
      provides: "output package marker (empty)"
    - path: "trezarr/output/write.py"
      provides: "write_vi_sidecar() — atomic UTF-8 sidecar write per D-19"
      exports: ["write_vi_sidecar"]
    - path: "trezarr/config.py"
      provides: "8 new Phase-2 TrezarrSettings fields (translate_* prefix)"
      contains: "translate_chars_per_token"
  key_links:
    - from: "trezarr/translate/batching.py"
      to: "trezarr/subtitles/model.py"
      via: "SubLine.start_tc / end_tc parsed to ms using _TC_RE-equivalent regex"
      pattern: "from trezarr.subtitles.model import"
    - from: "trezarr/output/write.py"
      to: "trezarr/subtitles/srt.py"
      via: "write_srt() reused as serializer with forced encoding='utf-8'"
      pattern: "from trezarr.subtitles.srt import write_srt"
    - from: "trezarr/translate/validate.py"
      to: "trezarr/subtitles/model.py"
      via: "SubDoc.lines iterated for all 7 checks"
      pattern: "from trezarr.subtitles.model import SubDoc"
---

## Phase Goal

**As a** Phase 3 integration, **I want to** call `translate_file(path, settings)` and receive a `TranslationResult`, **so that** a discovered SRT is translated and written as a Vietnamese sidecar automatically.

<objective>
Implement the pure-Python transformation layer: sentinel protection (D-12), token-budget batch packing with scene-gap detection (D-14, D-15), the 7-check validation gate (D-16, D-17), and atomic UTF-8 sidecar write (D-19). Extend TrezarrSettings with all Phase-2 configuration knobs. These are all deterministic, async-free transforms that require no LLM calls — they can be fully tested with mocked data.

Purpose: Deliver the validate-and-write half of the pipeline. After this plan, a correctly assembled translated SubDoc can be written as a sidecar and a bad one is caught before any disk write happens. Phase 3 (plan 02-03) wires the LLM calls on top of this foundation.
Output: 6 new files (2 package inits + sentinel.py + batching.py + validate.py + write.py) + 1 modified file (config.py).
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-CONTEXT.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md
@trezarr/config.py
@trezarr/subtitles/model.py
@trezarr/subtitles/srt.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sentinel.py, batching.py, and config.py Phase-2 fields</name>
  <files>
    trezarr/translate/__init__.py
    trezarr/translate/sentinel.py
    trezarr/translate/batching.py
    trezarr/config.py
  </files>

  <read_first>
    - trezarr/subtitles/model.py — SubDoc dataclass fields (lines, encoding, line_ending, separators, leading, trailer); SubLine fields (index, start_tc, end_tc, text, raw); confirm SubLine is a plain mutable @dataclass
    - trezarr/subtitles/srt.py — _TC_RE regex pattern (lines ~46-50) for timecode format "HH:MM:SS,mmm" handling both comma and period separators; _parse_block pattern (linear scan with accumulated state); warnings.warn usage for non-fatal anomalies
    - trezarr/config.py — existing field block structure, comment style "# ── Section header (D-NN) ──", where to insert new fields (after llm_context_window)
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md — "trezarr/translate/sentinel.py" section (full sentinel extract/reinsert implementation patterns); "trezarr/translate/batching.py" section (Batch dataclass, batch_subdoc() greedy-walk pattern, _make_batch helper, _gap_ms helper); "trezarr/config.py" section (exact field names and defaults)
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md — Q1 token budget formula, Q2 batch-packing algorithm, Q4 sentinel format and TAG_RE regex
  </read_first>

  <behavior>
    - sentinel: extract_sentinels("Hello <i>world</i>") returns ("Hello <<T0>>", {"<<T0>>": "<i>world</i>"})
    - sentinel: extract_sentinels("No tags here") returns ("No tags here", {})
    - sentinel: extract_sentinels("<b>a</b> and <i>b</i>") returns text with <<T0>> and <<T1>> (two distinct keys)
    - sentinel: reinsert_sentinels("Hello <<T0>>", {"<<T0>>": "<i>world</i>"}) returns ("<i>world</i>" restored, True)
    - sentinel: reinsert_sentinels("Hello", {"<<T0>>": "<i>world</i>"}) returns (original text, False) — sentinel missing
    - sentinel: reinsert_sentinels("<<T0>> still here", {}) returns (text, False) — orphan sentinel detected
    - sentinel: extract_sentinels("{\an8}text") extracts the ASS override tag using TAG_RE matching {\...} pattern
    - batching: batch_subdoc with 60 SubLines (no gaps, each 10 chars) and translate_max_cues_per_batch=50 → 2 batches (50 + 10)
    - batching: batch_subdoc with a 2500ms gap between lines 3 and 4, translate_scene_gap_ms=2000 → split at gap (2 batches: [0..2] and [3..end])
    - batching: every Batch.context_before has len <= translate_context_lines_k and every Batch.context_after has len <= translate_context_lines_k
    - config: TrezarrSettings() has translate_chars_per_token=3.5, translate_max_cues_per_batch=50, translate_scene_gap_ms=2000, translate_context_lines_k=3, translate_batch_retry_attempts=2, translate_vi_diacritic_ratio=0.70, translate_quarantine_dir="/config/quarantine", translate_ledger_path="/config/processed_files.json"
  </behavior>

  <action>
    Create trezarr/translate/__init__.py as an empty file (zero bytes, matching trezarr/subtitles/__init__.py — no exports, no __all__).

    Create trezarr/translate/sentinel.py. Module docstring referencing D-12. Header: `from __future__ import annotations` then `import re`. Define TAG_RE = re.compile(r'(<[^>]+>|\{\\[^}]+\})') — this regex matches both SRT inline tags (<i>, <b>, <u>, <font ...>) and inline ASS override tags ({\anX}, {\an8}, etc.) per D-12. Implement extract_sentinels(text: str) -> tuple[str, dict[str, str]]: uses a closure counter + TAG_RE.sub(replacer, text); sentinel keys follow the format "<<T{counter}>>" (double angle brackets, integer index per Q4 RESEARCH). Implement reinsert_sentinels(text: str, sentinel_map: dict[str, str]) -> tuple[str, bool]: replaces each key in sentinel_map back into text using str.replace(key, original, 1); checks for any remaining <<T\d+>> pattern via re.search(r'<<T\d+>>', text) — if found, returns (text, False); if any sentinel_map key is missing from text, returns (text, False). Both functions match the PATTERNS.md "trezarr/translate/sentinel.py" code excerpt exactly — read that section before writing.

    Create trezarr/translate/batching.py. Module docstring referencing D-14, D-15. Header: `from __future__ import annotations`, then dataclass, field, re, warnings, TYPE_CHECKING imports, then `from trezarr.subtitles.model import SubDoc, SubLine` and TYPE_CHECKING guard for TrezarrSettings. Define a _TC_PARSE_RE local regex that handles the HH:MM:SS[,.]mmm timecode format matching both comma and period separators (reuse the same pattern from trezarr/subtitles/srt.py — do NOT import _TC_RE directly as it is private; replicate the pattern locally in batching.py). Implement _tc_to_ms(tc: str) -> int that converts a verbatim timecode string to milliseconds. Implement _gap_ms(prev: SubLine, cur: SubLine) -> int using _tc_to_ms(cur.start_tc) - _tc_to_ms(prev.end_tc). Define @dataclass class Batch with fields: cues: list[SubLine] = field(default_factory=list), context_before: list[SubLine] = field(default_factory=list), context_after: list[SubLine] = field(default_factory=list). Implement _compute_budget_chars(settings) -> int using the formula from Q1: (settings.llm_context_window * (1 - settings.translate_overhead_fraction)) / (1 + settings.translate_output_expansion) * settings.translate_chars_per_token. Implement _make_batch(cues: list[SubLine], all_lines: list[SubLine], next_idx: int, settings) -> Batch that attaches context_before (K lines before the first cue in cues, from all_lines) and context_after (K lines after the last cue, from all_lines), capped at translate_context_lines_k. Implement batch_subdoc(doc: SubDoc, settings) -> list[Batch]: greedy-walk per Q2 algorithm — accumulates cues into current_batch until (a) scene gap >= translate_scene_gap_ms triggers an emit, or (b) char budget or max-cue cap would be exceeded. A single cue that alone exceeds budget emits as a one-cue batch with warnings.warn(UserWarning) per the PATTERNS.md warning pattern. Never split a cue across batches (D-14). Read PATTERNS.md "trezarr/translate/batching.py" section in full before writing — replicate the exact greedy-walk structure shown there.

    Extend trezarr/config.py: read the current file fully before editing. Insert 8 new fields after the existing llm_context_window field, using the comment style "# ── Phase 2: Batching (D-14) ──────────────────────────────────────────────" matching the established section-header convention. Fields and defaults from PATTERNS.md "trezarr/config.py" section and RESEARCH.md Q1: translate_chars_per_token: float = 3.5, translate_overhead_fraction: float = 0.30, translate_output_expansion: float = 1.40, translate_max_cues_per_batch: int = 50, translate_scene_gap_ms: int = 2000, translate_context_lines_k: int = 3 (separate section "# ── Phase 2: Context window (D-15)"), translate_batch_retry_attempts: int = 2 (separate section "# ── Phase 2: Retry + quarantine (D-18)"), translate_vi_diacritic_ratio: float = 0.70 (separate section "# ── Phase 2: Validation gate (D-17)"), translate_quarantine_dir: str = "/config/quarantine", translate_ledger_path: str = "/config/processed_files.json" (separate section "# ── Phase 2: Output paths (D-18, D-20)"). Do NOT modify __init__, settings_customise_sources, or model_config — add only the new field declarations.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && uv run pytest tests/translate/test_batching.py tests/translate/test_sentinel.py -x -q 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - uv run pytest tests/translate/test_batching.py tests/translate/test_sentinel.py -x -q: all 12 tests pass (5 batching + 7 sentinel), 0 skipped, 0 failures
    - uv run python -c "from trezarr.config import TrezarrSettings; s = TrezarrSettings(); print(s.translate_chars_per_token, s.translate_max_cues_per_batch, s.translate_vi_diacritic_ratio)" prints "3.5 50 0.7" (or "3.5 50 0.70")
    - grep -n "translate_quarantine_dir" trezarr/config.py returns exactly 1 line (the field declaration)
    - grep -n "translate_ledger_path" trezarr/config.py returns exactly 1 line
    - No "from __future__ import annotations" is missing from sentinel.py or batching.py (grep confirms it is the first line of each)
    - reinsert_sentinels on a round-trip (extract then reinsert same map) returns integrity_ok=True for all tested inputs
    - A 51-SubLine SubDoc with no gaps and translate_max_cues_per_batch=50 produces exactly 2 Batch objects from batch_subdoc()
    - uv run pytest -q exits 0 (full suite including Phase 1 tests)
  </acceptance_criteria>

  <done>sentinel.py, batching.py, and the config.py Phase-2 field block are implemented and all 12 corresponding test stubs pass (GREEN). The full test suite remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validate.py and write.py</name>
  <files>
    trezarr/translate/validate.py
    trezarr/output/__init__.py
    trezarr/output/write.py
  </files>

  <read_first>
    - trezarr/subtitles/model.py — SubDoc, SubLine fields (exact names: index, start_tc, end_tc, text); SubDoc constructor args
    - trezarr/subtitles/srt.py — write_srt(doc, path) function signature and behavior; how it encodes using doc.encoding; SubDoc constructor args (lines, encoding, line_ending, separators, leading, trailer)
    - trezarr/config.py — confirm translate_vi_diacritic_ratio field name (just added in Task 1)
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md — "trezarr/translate/validate.py" section (GateFailure dataclass, GateError class, validate_subdoc 7-check implementation, VN_DIACRITIC_RE, ALLOWLIST_RE, SENTINEL_RE, _check_untranslated, _check_monotonic helpers); "trezarr/output/write.py" section (write_vi_sidecar, _LANG_CODE_RE, NamedTemporaryFile + os.replace pattern)
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md — Q5 all 7 checks including the VN_DIACRITIC_RE = re.compile(r'[Ḁ-ỿ]') range and the ALLOWLIST_RE; Q6 atomic write pattern and sidecar naming logic
  </read_first>

  <behavior>
    - validate: validate_subdoc with translated.lines count != source.lines count raises GateError(check=1)
    - validate: translated SubLine with text="  " (whitespace-only) raises GateError(check=2)
    - validate: translated doc where all lines are byte-identical to source English (no VI diacritics, not on allowlist) raises GateError(check=3)
    - validate: lines that are all-punctuation/digits (e.g. "123", "...") are excluded from VI ratio denominator (do not contribute to check 3 failure)
    - validate: translated SubLine where start_tc != source start_tc raises GateError(check=4)
    - validate: two adjacent lines where parsed start_ms[1] < parsed end_ms[0] raises GateError(check=5)
    - validate: translated SubLine.text containing "<<T0>>" raises GateError(check=6)
    - validate: translated SubLine.text containing chr(0xD800) (lone surrogate) raises GateError(check=7)
    - validate: correctly translated Vietnamese SubDoc (diacritics on > 70% of non-allowlist lines, same cue count/timecodes, no sentinels) returns None
    - write: write_vi_sidecar(doc, "/path/to/Show.S01E01.en.srt") returns Path(".../Show.S01E01.vi.srt")
    - write: write_vi_sidecar(doc, "/path/to/Show.S01E01.srt") returns Path(".../Show.S01E01.vi.srt") (no lang code in stem)
    - write: after write_vi_sidecar completes, no .tmp file remains in dest.parent
    - write: output file content decodes as UTF-8 regardless of SubDoc.encoding input
  </behavior>

  <action>
    Create trezarr/output/__init__.py as an empty zero-byte file (matching trezarr/llm/__init__.py — no exports).

    Create trezarr/translate/validate.py. Module docstring referencing D-16, D-17. Header: `from __future__ import annotations`, then `import re`, `from dataclasses import dataclass`, `from trezarr.subtitles.model import SubDoc`, `if TYPE_CHECKING:` guard for TrezarrSettings. Define:
    - VN_DIACRITIC_RE = re.compile(r'[Ḁ-ỿ]') — U+1E00-U+1EFF Latin Extended Additional block (per Q5 RESEARCH.md; this range is specific to Vietnamese vs. French/Spanish)
    - ALLOWLIST_RE = re.compile(r'^[\W\d\s♪♫…\.]+$') — per Q5, lines matching this are "legitimately unchanged" and excluded from the VI ratio denominator
    - SENTINEL_RE = re.compile(r'<<T\d+>>') — backstop check for orphan sentinels (check 6)
    - _TC_PARSE_RE — same pattern as in batching.py for timecode-to-ms conversion in _check_monotonic
    - @dataclass class GateFailure with fields: check: int, reason: str, failing_indices: list[int] | None = None
    - class GateError(Exception) with __init__(self, failure: GateFailure) storing self.failure = failure
    - _tc_to_ms(tc: str) -> int — convert HH:MM:SS,mmm or HH:MM:SS.mmm to milliseconds
    - _check_untranslated(translated: SubDoc, source: SubDoc, settings) — per-line allowlist + per-file VI diacritic ratio check; raise GateError(GateFailure(3, ...)) if ratio < settings.translate_vi_diacritic_ratio; failing_indices = list of untranslated line indices
    - _check_monotonic(doc: SubDoc) — walk lines checking start_ms < end_ms per cue and start_ms[i] >= end_ms[i-1]; raise GateError(GateFailure(5, ...))
    - validate_subdoc(translated: SubDoc, source: SubDoc, settings) -> None — calls all 7 checks in order (fail-fast, stop at first failure). Read PATTERNS.md "trezarr/translate/validate.py" section for the exact check ordering and assertion patterns. Do NOT check translation quality (pronoun choices, semantic meaning) — structural integrity only per phase boundary in 02-CONTEXT.md.

    Create trezarr/output/write.py. Module docstring referencing D-19. Header: `from __future__ import annotations`, then `import os`, `import re`, `import tempfile`, `from pathlib import Path`, `from trezarr.subtitles.model import SubDoc`, `from trezarr.subtitles.srt import write_srt`. Define:
    - _LANG_CODE_RE = re.compile(r'\.[a-z]{2}$', re.IGNORECASE) — matches ".en", ".ja", ".fr" etc. at end of stem
    - write_vi_sidecar(doc: SubDoc, media_path: str | Path) -> Path: resolve media_path as a Path; strip any 2-letter language-code suffix from stem using _LANG_CODE_RE (e.g. "Show.S01E01.en" → "Show.S01E01"); dest = media_path.parent / (stem + '.vi.srt'). Atomic write using NamedTemporaryFile(suffix='.tmp', dir=dest.parent, delete=False) — dir=dest.parent is MANDATORY to prevent cross-device link failure (Pitfall 3, RESEARCH.md). Create a new SubDoc with encoding='utf-8' (forced, per D-19) and all other fields copied from doc; call write_srt(doc_out, tmp_path); then os.replace(tmp_path, dest) (POSIX-atomic rename). Use try/finally to unlink tmp_path on any exception before os.replace completes. Path(media_path).resolve() is called at entry to ensure the path is absolute. Read PATTERNS.md "trezarr/output/write.py" section for the exact pattern including the finally-unlink structure.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && uv run pytest tests/translate/test_validate.py tests/output/test_write.py -x -q 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - uv run pytest tests/translate/test_validate.py tests/output/test_write.py -x -q: all 13 tests pass (9 validate + 4 write), 0 failures
    - uv run pytest tests/translate/ tests/output/ -x -q: all 36 tests pass (batching + sentinel + validate + write, plus engine and ledger still skipping because engine.py / ledger.py don't exist yet)
    - validate_subdoc raises GateError for each of the 7 distinct failure modes (each test function covers one check number)
    - No .tmp file remains in dest.parent after write_vi_sidecar completes (test_atomic_write_crash_safety passes)
    - write_vi_sidecar output file opens with open(dest, encoding='utf-8') without UnicodeDecodeError
    - grep -n "dir=dest.parent" trezarr/output/write.py returns 1 line (the NamedTemporaryFile call)
    - grep -n "os.replace" trezarr/output/write.py returns 1 line
    - uv run pytest -q exits 0 (full suite including Phase 1 tests)
  </acceptance_criteria>

  <done>validate.py implements all 7 gate checks with GateError/GateFailure; write.py implements atomic UTF-8 sidecar write with correct naming and NamedTemporaryFile(dir=dest.parent). All 22 new test stubs in test_validate.py + test_write.py are GREEN. Full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → validate_subdoc | LLM output is untrusted; validate_subdoc is the V5 Input Validation control point — every translated cue text must pass all 7 checks before any write |
| write_vi_sidecar → filesystem | Path.resolve() ensures absolute path; dir=dest.parent prevents cross-device atomic failure; NamedTemporaryFile+os.replace prevents partial writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | LLM response cue text | mitigate | validate_subdoc() is the ASVS V5 control — all 7 checks run before write_vi_sidecar is called; any tampered/injected content that alters cue count, timecodes, or encoding is caught |
| T-02-02-02 | Tampering | LLM prompt injection via subtitle content | mitigate | sentinel extraction (D-12) replaces tags with opaque <<TN>> tokens before sending text to LLM; numbered-line protocol (D-13 implemented in Plan 03) means raw text is parsed structurally, never executed |
| T-02-02-03 | Tampering | File write — partial/corrupt sidecar | mitigate | NamedTemporaryFile(dir=dest.parent) + os.replace() = POSIX-atomic rename; crash before os.replace leaves a .tmp file in dest.parent, never a partially-visible dest; finally block unlinks tmp on exception |
| T-02-02-04 | Tampering | Path traversal via media_path arg | mitigate | Path(media_path).resolve() called at entry to write_vi_sidecar; full media-root enforcement deferred to Phase 3, but absolute-path validation is in place here |
| T-02-02-05 | Denial of Service | Single oversized cue exceeds token budget | accept | batch_subdoc emits oversized cues as one-cue batches with UserWarning; LLM call may time out but the pipeline does not crash — timeout handling is at the LLMClient layer (Phase 1) |
| T-02-SC | Tampering | No new package installs in this plan | accept | Plan 02 uses stdlib only (re, os, tempfile, dataclasses, hashlib); tenacity is added in Plan 03 |
</threat_model>

<verification>
uv run pytest tests/translate/test_batching.py tests/translate/test_sentinel.py tests/translate/test_validate.py tests/output/test_write.py -x -q: all 22 tests pass.
uv run pytest tests/translate/ tests/output/ -x -q: 22 pass, remaining engine/ledger stubs still skip.
uv run pytest -q: full suite green including Phase 1 codec + LLM client tests.
</verification>

<success_criteria>
The pure-Python transform layer is complete and tested: sentinel extraction/reinsertion round-trips correctly, batch_subdoc() splits on scene gaps and respects token budgets, validate_subdoc() catches all 7 failure modes and passes clean output, write_vi_sidecar() writes atomically with correct UTF-8 encoding and sidecar naming. TrezarrSettings has all 8 Phase-2 fields. Phase 3 (plan 02-03) can now wire the engine on top of these primitives.
</success_criteria>

<output>
Create `.planning/phases/02-mechanical-translation-core-validation-gate/02-02-SUMMARY.md` when done.
</output>
