---
phase: 11-shadcn-foundation-purple-theme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/.npmrc
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/components.json
  - frontend/src/lib/utils.ts
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "npm install exits 0 with all 6 runtime deps + @types/node in package.json (bulk-install set — D-06; vendor deps via legacy-peer-deps, no --force — D-07)"
    - "@/ alias resolves in both vite.config.ts (resolve.alias) and tsconfig.json (paths); no base set; outDir preserved (D-08)"
    - "shadcn init writes components.json and src/lib/utils.ts with cn() function"
    - "frontend/.npmrc exists with legacy-peer-deps=true BEFORE any npm/npx call"
    - "cd frontend && npm run build exits 0 after alias + init (pre-theme baseline)"
  artifacts:
    - path: "frontend/.npmrc"
      provides: "legacy-peer-deps=true (prevents ERESOLVE on cmdk/react-day-picker)"
      contains: "legacy-peer-deps=true"
    - path: "frontend/components.json"
      provides: "shadcn project config (style, aliases, CSS variable mode)"
      contains: "new-york"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() utility combining clsx + tailwind-merge"
      exports: ["cn"]
    - path: "frontend/vite.config.ts"
      provides: "@/ alias via resolve.alias; outDir unchanged; no base field"
      contains: "path.resolve(__dirname"
    - path: "frontend/tsconfig.json"
      provides: "@/ alias via baseUrl + paths"
      contains: "@/*"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/"
      via: "resolve.alias @"
      pattern: "resolve.*alias"
    - from: "frontend/tsconfig.json"
      to: "frontend/src/*"
      via: "paths @/*"
      pattern: "@/\\*.*src/\\*"
    - from: "frontend/src/lib/utils.ts"
      to: "clsx + tailwind-merge"
      via: "cn() import"
      pattern: "twMerge.*clsx"
---

<objective>
Install the shadcn/ui prerequisites on the existing Tailwind v3.4 / React 19 / Vite 7 SPA:
create .npmrc with legacy-peer-deps=true, install 6 runtime deps + @types/node, wire the @/ path
alias in both vite.config.ts and tsconfig.json, and run npx shadcn@2.10.0 init to produce
components.json and src/lib/utils.ts (the cn() utility). The wave ends with a green
npm run build confirming the pre-theme baseline is stable.

Purpose: Establish the foundational tooling so Plan 02 (theme tokens) and Plan 03 (component
bulk-install) can land on a known-good build. Per D-07, .npmrc MUST precede every npm/npx call.
Per D-08, both config files are updated in the same commit — partial alias causes asymmetric
tsc/vite failures (Pitfall 6).

Output: .npmrc, package.json (with new deps), vite.config.ts (with @/ alias), tsconfig.json
(with baseUrl + paths), components.json, src/lib/utils.ts.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-RESEARCH.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-PATTERNS.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .npmrc, install runtime deps + @types/node, wire @/ alias in both configs</name>
  <files>
    frontend/.npmrc
    frontend/package.json
    frontend/package-lock.json
    frontend/vite.config.ts
    frontend/tsconfig.json
  </files>
  <read_first>
    - RESEARCH.md Steps 1–5 (verbatim commands and exact file content for all five files)
    - PATTERNS.md §.npmrc, §vite.config.ts, §tsconfig.json (current state snapshots + exact replacements)
    - CONTEXT.md D-07 (legacy-peer-deps timing constraint), D-08 (@/ alias dual-config rule)
  </read_first>
  <action>
    Working directory: /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend

    Step 1 — Create frontend/.npmrc (D-07/Pitfall 7 — MUST be first, before any npm/npx):
    Write the file with the comment explaining why legacy-peer-deps=true is needed (cmdk and
    react-day-picker declare peer dep ranges excluding React 19.0 stable). Do NOT use --force
    as a substitute. Full file content is in RESEARCH.md Step 1 and PATTERNS.md §.npmrc.

    Step 2 — Install 6 runtime dependencies (RESEARCH.md Step 2):
    Run: npm install class-variance-authority@^0.7.1 clsx@^2.1.1 tailwind-merge@^3.6.0
    tailwindcss-animate@^1.0.7 react-aria-components@^1.18.0 sonner@^2.0.7

    Step 3 — Install @types/node dev dependency (RESEARCH.md Step 3 / D-08):
    Run: npm install -D @types/node

    Step 4 — Replace vite.config.ts (D-08/Pitfall 6):
    Write the exact replacement from RESEARCH.md Step 4 / PATTERNS.md §vite.config.ts.
    Critical constraints: add `import path from "path"` as first import; add `resolve: { alias:
    { "@": path.resolve(__dirname, "./src") } }` block inside defineConfig; do NOT add a `base`
    field; preserve `build.outDir: "../trezarr/web/static"` and `build.emptyOutDir: true`
    exactly; preserve the FastAPI StaticFiles comment.

    Step 5 — Update tsconfig.json (D-08/Pitfall 6 — same commit as vite.config.ts):
    Write the exact replacement from RESEARCH.md Step 5 / PATTERNS.md §tsconfig.json.
    Add `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }` at the end of compilerOptions.
    All existing options (strict, noUnusedLocals, noUnusedParameters, noEmit, moduleResolution:
    bundler, etc.) MUST be preserved verbatim. "include": ["src"] must not change.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c "legacy-peer-deps=true" .npmrc && node -e "const p=require('./package.json'); const deps=['class-variance-authority','clsx','tailwind-merge','tailwindcss-animate','react-aria-components','sonner']; deps.forEach(d=>{ if(!p.dependencies[d]) throw new Error('missing: '+d) }); if(!p.devDependencies['@types/node']) throw new Error('missing @types/node'); console.log('all deps present')"</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c "path.resolve" vite.config.ts && grep -c "@/" tsconfig.json && grep -c "base" vite.config.ts | grep -v "^[^0]"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/.npmrc contains `legacy-peer-deps=true` (grep exits 0)
    - All 6 runtime deps appear in package.json dependencies; @types/node in devDependencies
    - vite.config.ts contains `import path from "path"` and `path.resolve(__dirname, "./src")`
    - vite.config.ts has NO `base:` field; `outDir: "../trezarr/web/static"` is unchanged
    - tsconfig.json contains `"baseUrl": "."` and `"@/*": ["./src/*"]`
    - All pre-existing tsconfig.json compilerOptions are present (strict, noUnusedLocals, etc.)
    - `grep base vite.config.ts` returns zero occurrences of a `base:` key (comment lines excluded)
  </acceptance_criteria>
  <done>All 6 runtime deps + @types/node are in package.json; .npmrc has legacy-peer-deps=true;
vite.config.ts has @/ alias via resolve.alias with no base field; tsconfig.json has baseUrl + paths.</done>
</task>

<task type="auto">
  <name>Task 2: Run shadcn@2.10.0 init and verify pre-theme build is green</name>
  <files>
    frontend/components.json
    frontend/src/lib/utils.ts
  </files>
  <read_first>
    - RESEARCH.md Step 6 (exact prompt answers for shadcn init: New York, Zinc, CSS variables yes)
    - PATTERNS.md §components.json (expected content), §src/lib/utils.ts (expected cn() content)
    - CONTEXT.md D-06 (NEVER run shadcn@latest init — pins to 2.10.0 only)
    - RESEARCH.md Pitfall 2 (shadcn@latest init emits Tailwind v4 config — DO NOT USE)
    - VALIDATION.md §Per-Task Verification Map row 1 (@/ alias + cn() + components.json)
  </read_first>
  <action>
    Working directory: /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend

    Step 6 — Run shadcn init pinned to 2.10.0 (D-06 — CRITICAL: NEVER use @latest for init):
    Run: npx shadcn@2.10.0 init

    Answer the interactive prompts as follows (from RESEARCH.md Step 6):
    - Style: New York
    - Base color: Zinc
    - CSS variables for colors: yes
    - Global CSS file: src/index.css
    - Tailwind config: tailwind.config.js
    - Components alias: @/components
    - Utils alias: @/lib/utils

    This writes components.json and src/lib/utils.ts. It also writes a generated :root block
    into src/index.css and modifies tailwind.config.js — these generated changes will be
    REPLACED wholesale in Plan 02. Do NOT treat the generated index.css or tailwind.config.js
    as final output.

    NOTE: If `npx shadcn@2.10.0 init` is non-interactive (CI mode), pass prompts via flags or
    create components.json manually first — check PATTERNS.md §components.json for the exact
    expected content.

    Step 7 — Verify pre-theme baseline build:
    Run: npm run build
    This runs tsc -b && vite build. The generated shadcn init output may cause the theme to
    look different from the final state (that is expected — theme tokens land in Plan 02).
    What matters here is that the build exits 0 with the @/ alias resolving correctly.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && test -f components.json && test -f src/lib/utils.ts && echo "files present"</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c "twMerge" src/lib/utils.ts && grep -c "new-york" components.json</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists containing "new-york" style and "zinc" baseColor and cssVariables: true
    - src/lib/utils.ts exists and exports a `cn()` function importing from clsx and tailwind-merge
    - `npm run build` exits 0 (tsc -b clean, vite build clean, trezarr/web/static/ populated)
    - No TypeScript errors on @/ imports (confirms alias is wired in both configs)
  </acceptance_criteria>
  <done>components.json and src/lib/utils.ts are written by shadcn CLI; build exits 0 confirming
@/ alias resolves in both Vite and tsc; pre-theme baseline is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package install introduces third-party code into the build |
| shadcn CLI (npx) → local file writes | CLI writes vendor source files (components.json, src/lib/utils.ts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | npm install (6 runtime deps + @types/node) | mitigate | All packages in RESEARCH.md Package Legitimacy Audit table — VERIFIED from npm registry 2026-06-03; lockfile committed after install |
| T-11-02 | Tampering | npx shadcn@2.10.0 init | mitigate | Pinned to exact version 2.10.0 (not @latest); only writes local config files (components.json, src/lib/utils.ts, src/index.css modifications); no network calls after download |
| T-11-03 | Spoofing | npx version resolution | mitigate | Explicit `@2.10.0` pin prevents pulling a different version; using @latest for init would pull shadcn 4.x which breaks Tailwind v3 pipeline |
| T-11-SC | Tampering | npm/npx installs | mitigate | Package Legitimacy Audit completed in RESEARCH.md — all packages VERIFIED (no [ASSUMED]/[SUS]); lockfile committed; no blocking human checkpoint required |
</threat_model>

<verification>
After completing both tasks:

1. `cd frontend && grep -c "legacy-peer-deps=true" .npmrc` — must return 1
2. `cd frontend && cat package.json | grep class-variance-authority` — must show version
3. `cd frontend && grep "path.resolve" vite.config.ts` — must match
4. `cd frontend && grep '@/"' tsconfig.json` — must match @/* paths entry
5. `cd frontend && test -f components.json && test -f src/lib/utils.ts && echo ok`
6. `cd frontend && npm run build` — must exit 0
</verification>

<success_criteria>
- frontend/.npmrc exists with `legacy-peer-deps=true` before any shadcn invocation
- package.json has 6 new runtime deps and @types/node in devDependencies
- vite.config.ts: `@/` alias via resolve.alias; no `base` field; outDir unchanged
- tsconfig.json: `baseUrl: "."` and `"@/*": ["./src/*"]` in compilerOptions
- components.json present with style: new-york, baseColor: zinc, cssVariables: true
- src/lib/utils.ts present with cn() exported
- `npm run build` exits 0 (pre-theme baseline green)
</success_criteria>

<output>
Create `.planning/phases/11-shadcn-foundation-purple-theme/11-01-SUMMARY.md` when done
</output>

## Decision Traceability

CONTEXT.md decisions implemented by this plan (colon-form citations for the coverage gate):

- D-06: shadcn `init` pinned to `2.10.0` (never `@latest`); foundation install in one commit.
- D-07: `.npmrc` `legacy-peer-deps=true` created BEFORE the first npm/npx call; no `--force`; `src/components/ui/` is vendor code.
- D-08: `@/` alias wired in BOTH `vite.config.ts` (resolve.alias) and `tsconfig.json` (baseUrl + paths); `base` NOT set; `outDir`/`emptyOutDir` preserved; build script stays `tsc -b && vite build`.
