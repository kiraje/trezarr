---
phase: 11-shadcn-foundation-purple-theme
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/index.css
  - frontend/index.html
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "tailwind.config.js has darkMode: [\"class\"] at top level and tailwindcss-animate in plugins"
    - "Bridge tokens bg-base/bg-surface/bg-stripe/text-primary/text-muted stay as literal hex; colliding keys border/accent/destructive replaced wholesale (D-05)"
    - "Colliding keys border/accent/destructive are replaced wholesale with hsl(var(--x)) entries"
    - "src/index.css declares all 28 CSS variables as bare H S% L% channel triples (no hsl() wrapper) (D-02)"
    - "--primary is 270 70% 60% (the D-01 anchor; --ring and --sidebar-primary track it — D-03)"
    - "index.html has class=\"dark\" on <html> and body background-color updated to purple-tinted hsl(270, 24%, 7%) — not blue-black #0f1117 (D-04)"
    - "npm run build exits 0 after these three file replacements"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "CSS-var token map + bridge tokens + darkMode class + animate plugin"
      contains: "darkMode"
    - path: "frontend/src/index.css"
      provides: "Purple HSL :root block — 28 CSS variables in bare H S% L% format"
      contains: "--primary: 270 70% 60%"
    - path: "frontend/index.html"
      provides: "class=\"dark\" on <html>; purple background-color on body"
      contains: "class=\"dark\""
  key_links:
    - from: "frontend/index.html"
      to: "frontend/tailwind.config.js"
      via: "class=dark triggers darkMode: [class] selector"
      pattern: "class=\"dark\""
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/index.css"
      via: "hsl(var(--primary)) maps to bare 270 70% 60%"
      pattern: "hsl\\(var\\(--primary\\)\\)"
    - from: "frontend/src/index.css"
      to: "browser computed styles"
      via: "--primary channel-triple enables bg-primary/50 opacity modifier"
      pattern: "--primary: 270 70% 60%"
---

<objective>
Replace the three files that carry the visual theme: tailwind.config.js (wholesale replacement
with bridge + shadcn CSS-var token map + darkMode + animate plugin), src/index.css (wholesale
replacement with purple HSL :root block — all 28 variables as bare H S% L% channel triples),
and index.html (two surgical edits: add class="dark" to <html>, update body background-color).

Purpose: Establish the purple dark-mode theme (D-01, D-02, D-04, D-05) and verify the build
stays green (UI-01). After this plan, the app root will show a dark purple background when
opened in a browser. The bridge tokens keep all existing pages functional (D-05). Component
vendor code lands in Plan 03.

Output: Updated tailwind.config.js, src/index.css, index.html. Green build with themed CSS.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-RESEARCH.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-PATTERNS.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-CONTEXT.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-UI-SPEC.md
@.planning/phases/11-shadcn-foundation-purple-theme/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace tailwind.config.js and src/index.css with purple HSL token system</name>
  <files>
    frontend/tailwind.config.js
    frontend/src/index.css
  </files>
  <read_first>
    - RESEARCH.md Step 7 (exact tailwind.config.js replacement — copy verbatim, do not summarize)
    - RESEARCH.md Step 8 (exact src/index.css replacement — copy verbatim, do not summarize)
    - PATTERNS.md §tailwind.config.js (critical rules: darkMode, bridge tokens, colliding keys, plugins)
    - PATTERNS.md §src/index.css (critical rules: channel-triple format, no hsl() in variables)
    - CONTEXT.md D-02 (bare H S% L% — the #1 silent breakage if violated)
    - CONTEXT.md D-05 (bridge tokens stay, colliding keys replaced wholesale)
    - RESEARCH.md Pitfall 1 (HSL channel-triple format), Pitfall 3 (token namespace collisions)
    - RESEARCH.md Pitfall 4 (darkMode: ["class"] required), Pitfall 5 (tailwindcss-animate missing)
    - UI-SPEC.md §Purple HSL Token Table (all 28 variables with starting values)
  </read_first>
  <action>
    Working directory: /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend

    Step 7 — Wholesale replace frontend/tailwind.config.js (D-02, D-05, per RESEARCH.md Step 7):

    Write the complete file from RESEARCH.md Step 7 exactly. Key invariants:
    - Top-level `darkMode: ["class"]` MUST be present (Pitfall 4 — without it, .dark selector does nothing)
    - Bridge tokens preserved as literal hex strings (DO NOT convert to CSS variables):
      "bg-base": "#0f1117", "bg-surface": "#1a1d27", "bg-stripe": "#1e2130",
      "text-primary": "#e2e6f0", "text-muted": "#6b7280"
    - Colliding keys from old config (border, accent, destructive) replaced wholesale with
      hsl(var(--x)) entries — never leave both old and new definitions for the same key
    - sidebar token block required (sidebar.DEFAULT through sidebar.ring) — needed by shadcn sidebar.tsx
    - accordion keyframes block required (accordion-down, accordion-up)
    - plugins: [require("tailwindcss-animate")] — if CommonJS require fails at build time,
      switch to ESM import: add `import tailwindcssAnimate from "tailwindcss-animate"` at top
      and replace plugins entry with [tailwindcssAnimate]
    - The hsl() wrapper is applied HERE in tailwind.config.js only — never in index.css (D-02)

    Step 8 — Wholesale replace frontend/src/index.css (D-01, D-02, D-03, D-04, per RESEARCH.md Step 8):

    Write the complete file from RESEARCH.md Step 8 exactly. Key invariants:
    - Three @tailwind directives FIRST before @layer base (base, components, utilities)
    - ALL 28 CSS variable values are bare H S% L% channel triples — NO hsl() wrapper (D-02/Pitfall 1)
      CORRECT: --primary: 270 70% 60%;
      WRONG:   --primary: hsl(270 70% 60%);   /* silently breaks bg-primary/50 */
    - --primary: 270 70% 60% is the locked anchor (D-01)
    - --background: 270 24% 7% (purple-tinted dark, not blue-black — D-04)
    - --ring: 270 70% 60% (tracks --primary — D-03)
    - --sidebar-primary: 270 70% 60% and --sidebar-ring: 270 70% 60% (track --primary — D-03)
    - No .dark { } block needed — Trezarr is dark-only; :root IS the dark theme
    - `* { @apply border-border; }` and `body { @apply bg-background text-foreground; }` inside @layer base
    - Use the exact 28-variable token table from RESEARCH.md Step 8 / UI-SPEC.md §Purple HSL Token Table
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c 'darkMode.*class' tailwind.config.js</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c 'tailwindcss-animate' tailwind.config.js</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c '"bg-base"' tailwind.config.js</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c '\-\-primary: 270 70% 60%' src/index.css</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c 'hsl(270' src/index.css | grep -w 0 && echo "PASS: no hsl() wrapper in index.css CSS vars" || echo "FAIL: hsl() found in index.css CSS variable declarations"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js: `darkMode: ["class"]` present at top level (grep exits with count >= 1)
    - tailwind.config.js: `tailwindcss-animate` in plugins array
    - tailwind.config.js: all 5 bridge tokens present as literal hex strings ("bg-base", "bg-surface", "bg-stripe", "text-primary", "text-muted")
    - tailwind.config.js: sidebar token block present (sidebar.DEFAULT through sidebar.ring)
    - src/index.css: `--primary: 270 70% 60%` present (bare channel-triple, no hsl() wrapper)
    - src/index.css: `--background: 270 24% 7%` present
    - src/index.css: zero occurrences of `hsl(` inside CSS variable value declarations in :root
      (the hsl() wrapper belongs only in tailwind.config.js — grep for `hsl(` in the :root block must return 0)
    - All 28 variables from UI-SPEC.md §Purple HSL Token Table are present in src/index.css
  </acceptance_criteria>
  <done>tailwind.config.js has bridge tokens + CSS-var token map + darkMode + animate plugin;
src/index.css has all 28 purple HSL variables as bare channel triples with --primary: 270 70% 60%.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html (class="dark" + purple background) and verify green build</name>
  <files>
    frontend/index.html
  </files>
  <read_first>
    - RESEARCH.md Step 9 (exact edits to index.html: class="dark" on html, background-color update)
    - PATTERNS.md §index.html (current state snapshot + two surgical edits required)
    - CONTEXT.md D-04 (purple-tinted background — not blue-black), D-09 (visual UAT proof)
    - RESEARCH.md Pitfall 4 (class="dark" on html is required alongside darkMode: ["class"])
    - VALIDATION.md §Per-Task Verification Map
  </read_first>
  <action>
    Working directory: /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend

    Step 9 — Two surgical edits to frontend/index.html (D-04/Pitfall 4):

    Edit 1: Change `<html lang="en">` to `<html lang="en" class="dark">`
    Edit 2: Change `background-color: #0f1117;` to `background-color: hsl(270, 24%, 7%);`

    The `color: #e2e6f0` may optionally be updated to `hsl(270, 10%, 90%)` but is not required
    for the D-09 gate. Font stack, <title>, script tag, <div id="root"> are all preserved as-is.

    Rationale: darkMode: ["class"] in tailwind.config.js only activates when the <html> element
    carries class="dark". Without this attribute, all shadcn components render in light mode
    (white backgrounds). The inline background-color update prevents a flash of the old blue-black
    #0f1117 before the SPA hydrates (D-04).

    Step 10 — Verify build with complete theme (VALIDATION.md sampling rate):
    Run: npm run build (= tsc -b && vite build)
    This is the build-gate sample for this wave. Failure modes:
    - "Cannot find module @/lib/utils" → alias not wired (Plan 01 issue)
    - PostCSS errors → shadcn@latest was used for init instead of 2.10.0 (check components.json)
    - TypeScript errors on vendor files → check that skipLibCheck: true is in tsconfig.json

    After successful build, verify animation classes are present:
    Run: grep -l "animate-in" dist/assets/*.css
    Must match at least one file — if no match, tailwindcss-animate is not in plugins or
    shadcn components are not yet installed (latter is expected — Plan 03 adds them).
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c 'class="dark"' index.html</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && grep -c 'hsl(270, 24%, 7%)' index.html</automated>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - index.html: `class="dark"` attribute present on the `<html>` element
    - index.html: `background-color: hsl(270, 24%, 7%)` replaces the old `#0f1117`
    - index.html: no other content changed (font stack, title, script, div#root all intact)
    - `npm run build` exits 0 (tsc -b + vite build both clean)
    - trezarr/web/static/ is populated with build output
  </acceptance_criteria>
  <done>index.html has class="dark" on html and purple background-color; npm run build exits 0
with the themed CSS output in trezarr/web/static/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tailwind.config.js → browser CSS | Token values flow from config through PostCSS into shipped CSS |
| src/index.css :root block → browser | CSS variable values are runtime-resolved by the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | tailwind.config.js bridge tokens | accept | Bridge hex tokens are styling-only; no PII or secrets; low-value target |
| T-11-05 | Tampering | tailwind.config.js token collision (border/accent/destructive) | mitigate | Wholesale replacement in one atomic commit per RESEARCH.md Pitfall 3 — no partial merge left behind |
| T-11-06 | Denial of Service | tailwindcss-animate missing from plugins | mitigate | tailwindcss-animate required in plugins block; verified via `grep tailwindcss-animate tailwind.config.js` acceptance criterion |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan (Plan 01 handled installs); file edits only |
</threat_model>

<verification>
After completing both tasks:

1. `grep 'darkMode.*class' frontend/tailwind.config.js` — must match
2. `grep '"bg-base"' frontend/tailwind.config.js` — must match (bridge token preserved)
3. `grep -- '--primary: 270 70% 60%' frontend/src/index.css` — must match
4. `grep 'hsl(' frontend/src/index.css` | count must be 0 (no hsl() inside :root variable values)
5. `grep 'class="dark"' frontend/index.html` — must match
6. `grep 'hsl(270, 24%, 7%)' frontend/index.html` — must match
7. `cd frontend && npm run build` — must exit 0
</verification>

<success_criteria>
- tailwind.config.js: darkMode ["class"], bridge tokens, CSS-var token map, sidebar block, accordion keyframes, tailwindcss-animate plugin — all present
- src/index.css: all 28 purple HSL variables in bare H S% L% format; --primary: 270 70% 60%
- index.html: class="dark" on html element; background-color: hsl(270, 24%, 7%)
- npm run build exits 0 with trezarr/web/static/ populated
</success_criteria>

<output>
Create `.planning/phases/11-shadcn-foundation-purple-theme/11-02-SUMMARY.md` when done
</output>

## Decision Traceability

CONTEXT.md decisions implemented by this plan (colon-form citations for the coverage gate):

- D-01: `--primary: 270 70% 60%` locked purple anchor.
- D-02: all CSS variables are bare channel-triple `H S% L%` in `index.css`; the `hsl(var(--x))` wrapper lives only in `tailwind.config.js`; proven by `bg-primary/50` half-opacity.
- D-03: `--ring` and `--sidebar-primary` (and `--sidebar-ring`) track `--primary`.
- D-04: `--background` is purple-tinted dark (`270 24% 7%`), not blue-black `#0f1117`; `index.html` body background updated to match.
- D-05: legacy bridge tokens (`bg-base`/`bg-surface`/`bg-stripe`/`text-primary`/`text-muted`) stay literal hex; colliding keys (`border`/`accent`/`destructive`) replaced wholesale in one commit.
