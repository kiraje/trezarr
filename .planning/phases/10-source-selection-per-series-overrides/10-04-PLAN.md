---
phase: 10-source-selection-per-series-overrides
plan: "04"
type: execute
wave: 2
depends_on:
  - "10-02"
  - "10-03"
files_modified:
  - frontend/src/pages/BibleEditor.tsx
  - frontend/src/api/client.ts
autonomous: false
requirements:
  - SVC-05

must_haves:
  truths:
    - "BibleEditor has an 'Overrides' fifth tab after 'Register'"
    - "OverridesSection renders Source Language Preference (chip list), Model Override (text field), and Register (inline reduced RegisterSection)"
    - "Saving source_lang_override and model_override calls PATCH /api/bible/series/{id}/overrides and updates the in-memory bible DTO"
    - "Provenance badges show whether each field is inherited from global config or per-series overridden"
    - "Clear source override and Clear model override links immediately persist null overrides"
    - "source_lang_override chips validate 2-letter codes client-side before appending"
    - "Save Overrides button is disabled when no dirty changes; shows 'Saving…' during in-flight PATCH"
    - "Register sub-section in Overrides tab calls existing patchRegister (not the new overrides endpoint)"
  artifacts:
    - path: frontend/src/pages/BibleEditor.tsx
      provides: OverridesSection component + fifth 'Overrides' tab
      contains: "OverridesSection"
    - path: frontend/src/api/client.ts
      provides: patchSeriesOverrides() wrapper + SeriesOverridesRequest interface + DTO extensions
      contains: "patchSeriesOverrides"
  key_links:
    - from: frontend/src/pages/BibleEditor.tsx
      to: frontend/src/api/client.ts
      via: "patchSeriesOverrides(seriesId, payload)"
      pattern: "patchSeriesOverrides"
    - from: frontend/src/api/client.ts
      to: trezarr/web/routes/bible.py
      via: "PATCH /api/bible/series/{id}/overrides"
      pattern: "/overrides"
---

<objective>
Per-series Overrides UI — vertical slice that makes SVC-05 visible and usable. Adds the "Overrides"
fifth tab to BibleEditor, implementing the SourcePriorityEditor, model override text field, and
inline register sub-section as specified in 10-UI-SPEC.md.

Purpose: Complete the SVC-05 user story end-to-end: user opens BibleEditor → Overrides tab →
sets source language preference and/or model override → saves → next translation honors the override.
Output: Overrides tab visible in BibleEditor; PATCH /overrides called correctly; provenance badges
show inherited vs. overridden state. Followed by a human-verify checkpoint.

Phase Goal: As a power user, I want to set per-series source language preference, register, and
model overrides, so that Trezarr uses the relationally-richest source and my preferred model for
specific series without changing the global defaults.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-source-selection-per-series-overrides/10-CONTEXT.md
@.planning/phases/10-source-selection-per-series-overrides/10-UI-SPEC.md
@.planning/phases/10-source-selection-per-series-overrides/10-PATTERNS.md
@.planning/phases/10-source-selection-per-series-overrides/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: client.ts extensions + BibleEditor Overrides tab (D-114, SVC-05)</name>
  <files>
    frontend/src/api/client.ts,
    frontend/src/pages/BibleEditor.tsx
  </files>

  <read_first>
    - frontend/src/api/client.ts (full file — patchRegister wrapper at lines 309–321 is the exact template for patchSeriesOverrides; SeriesListItem interface and SeriesBibleDTO interface — where to add the two new fields; fetchWithTimeout usage pattern)
    - frontend/src/pages/BibleEditor.tsx (full file — Tab union type, tab array, tab content switch/conditional, RegisterSection component ~lines 1936–2055, SectionCard local component ~lines 56–97, TextField local component, showToast usage, setBible pattern, the existing section component props pattern)
    - .planning/phases/10-source-selection-per-series-overrides/10-UI-SPEC.md (full spec — component inventory, field contracts, copywriting contract, interaction contracts, accessibility contract, state inventory)
    - .planning/phases/10-source-selection-per-series-overrides/10-PATTERNS.md §client.ts and §BibleEditor.tsx
  </read_first>

  <action>
    Modify frontend/src/api/client.ts. Read the full file first. (1) Extend SeriesListItem interface: add source_lang_override: string[] | null; add model_override: string | null. (2) Extend SeriesBibleDTO interface: same two fields. (3) Add SeriesOverridesRequest interface: { source_lang_override: string[] | null; model_override: string | null }. (4) Add patchSeriesOverrides(seriesId: number, payload: SeriesOverridesRequest): Promise<SeriesBibleDTO> — copy patchRegister structure exactly, URL is /api/bible/series/${seriesId}/overrides, method PATCH. (5) Export all new additions from the file (they should be inline exports matching the file's existing export style).

    Modify frontend/src/pages/BibleEditor.tsx. Read the full file first to understand the exact locations. Make the following changes:

    (a) Tab union type: add "overrides" to Tab type union. Find the type definition and extend it.

    (b) Tab array: add ["overrides", "Overrides"] as [Tab, string] to the end of the tab array.

    (c) Tab content: add {activeTab === "overrides" && <OverridesSection seriesId={seriesId} bible={bible} setBible={setBible} showToast={showToast} />} after the register tab block.

    (d) Create SourcePriorityEditor local component (scoped to BibleEditor.tsx — not a shared component). Props: value: string[], globalDefault: string[], onChange: (v: string[] | null) => void, disabled?: boolean. Renders: ordered chip list of language codes; each chip has the code text, remove button (aria-label="Remove {code} from source priority", × icon, text-text-muted hover text-[#f87171]); chip styles: h-7 px-2 gap-2 bg-[#1e293b] text-xs text-text-primary border border-[#2d3148] rounded. Add-input: <input type="text" placeholder="+ Add" className="h-7 w-16 bg-bg-surface border border-[#2d3148] rounded px-2 text-xs text-text-primary" aria-label="Add language code" />. On Enter/blur: validate /^[a-z]{2}$/ — show inline <p role="alert" className="text-xs text-[#f87171]">Must be a 2-letter ISO-639-1 code (e.g. ko, zh, en)</p> if invalid; else append to list and clear input; ignore duplicates silently. Focus ring on all interactive elements: focus:outline-[#3b82f6] focus:outline-2 focus:outline-offset-2.

    (e) Create OverridesSection local component. Props interface: seriesId: number, bible: SeriesBibleDTO, setBible: React.Dispatch<React.SetStateAction<SeriesBibleDTO | null>>, showToast: (t: Omit<ToastState, "id">) => void.

    State: sourcePriority: string[] (initialized bible.source_lang_override ?? []), modelValue: string (initialized bible.model_override ?? ""), saving: boolean (false), globalSettings: SettingsResponse | null (null).

    useEffect on mount: getSettings().then(setGlobalSettings).catch(() => {}).

    Dirty: JSON.stringify(sourcePriority) !== JSON.stringify(bible.source_lang_override ?? []) || modelValue !== (bible.model_override ?? "").

    handleSave CRITICAL SAVE BOUNDARY (per D-111 + UI-SPEC register-vs-overrides boundary): handleSave calls patchSeriesOverrides(seriesId, {source_lang_override: sourcePriority.length > 0 ? sourcePriority : null, model_override: modelValue.trim() || null}) with source_lang_override and model_override ONLY. Register is EXCLUDED from this payload — patchSeriesOverrides must never include a register field. The register sub-section has its own independent save path via patchRegister(); the "Save Overrides" button must NOT call any register endpoint. On success: setBible(prev => prev ? {...prev, source_lang_override: ..., model_override: ...} : prev); showToast({message:"Overrides saved.", variant:"success"}); on error: showToast({message:"Failed to save overrides. Check the server logs.", variant:"error"}); finally: setSaving(false).

    handleClearSource: immediate call to patchSeriesOverrides with {source_lang_override: null, model_override: modelValue.trim() || null}; update setBible; showToast.
    handleClearModel: same with {source_lang_override: sourcePriority.length > 0 ? sourcePriority : null, model_override: null}.

    Provenance badges per 10-UI-SPEC.md: "Inherited from global config: {codes}" (text-text-muted) vs "Per-series override active" (text-accent).

    Render inside SectionCard: heading "Overrides", sub-text "Per-series overrides take priority over global config. Leave blank to inherit the global default.", then three sections: Source Language Preference, Model Override, Register.

    Register sub-section: inline reduced copy of RegisterSection logic (registerValue state, handleSaveRegister calling patchRegister directly — NOT patchSeriesOverrides, LockBadge + LockToggleButton with the existing lock pattern, advisory strip if unlocked). This sub-section is entirely independent of the overrides save path.

    "Save Overrides" primary button: disabled={!dirty || saving}, text saving ? "Saving…" : "Save Overrides", bg-accent, white text, calls handleSave. This button controls source_lang_override + model_override only — never register.

    Copywriting must match 10-UI-SPEC.md §Copywriting Contract exactly: "Source language preference", "Model override", "Register", "Clear source override", "Clear model override", "Save Overrides", "Saving…", "Overrides saved.", "Failed to save overrides. Check the server logs.", "Must be a 2-letter ISO-639-1 code (e.g. ko, zh, en)", "Inherited from global config: {codes}", "Per-series override active", "Inherited: {globalModel}", "This value may be overwritten on the next episode analysis. Lock to pin it."

    Import patchSeriesOverrides (and getSettings if not already imported) from ../api/client at the top of BibleEditor.tsx where other client.ts imports are.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>
    npm run build exits 0 (no TypeScript errors); BibleEditor.tsx contains "OverridesSection"; client.ts contains "patchSeriesOverrides"; frontend/dist/ is built successfully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 10 complete — BibleEditor "Overrides" tab is live. The full source-selection and
    per-series overrides system is wired end-to-end:
    - Bazarr inventory client reads source-language subtitle lists per series
    - rank_sources() selects the relationally-richest source (zh/ko/ja/th preferred for East-Asian content)
    - Per-series source_lang_override, model_override overrides stored in SQLite and resolved per-item
    - BibleEditor "Overrides" tab: source priority chip list, model override text field, register section
    - PATCH /api/bible/series/{id}/overrides wired end-to-end
    - Save Overrides only touches source_lang_override and model_override; register saves via its own independent patchRegister path
  </what-built>
  <how-to-verify>
    1. Start the service: uv run trezarr serve (or docker compose up)
    2. Navigate to the Bible editor for any series: http://localhost:6868 → Series list → select a series → BibleEditor
    3. Confirm the "Overrides" tab appears as the fifth tab after "Register"
    4. Click "Overrides" — verify three sections render: Source Language Preference, Model Override, Register
    5. Source Language Preference: type "ko" in the add input, press Enter — verify "ko" chip appears with × button
    6. Type "xx" — verify the inline error: "Must be a 2-letter ISO-639-1 code (e.g. ko, zh, en)"
    7. Type "en", press Enter — verify "en" chip appended after "ko"
    8. Click "Save Overrides" — verify success toast "Overrides saved." and button momentarily shows "Saving…"
    9. Refresh the page, navigate back to same series Overrides tab — verify ko and en chips persist
    10. Click "Clear source override" — verify chips cleared and provenance badge shows "Inherited from global config: {codes}"
    11. Model Override: type "gpt-4o-mini", save — verify toast and persistence after refresh
    12. Register section: change register value, verify it goes through patchRegister (NOT the overrides endpoint) — check server logs for PATCH /api/bible/series/{id}/register vs /overrides. The "Save Overrides" button must NOT trigger any /register call; the Register sub-section must have its own independent save control.
    13. If Bazarr is configured and has a series with both ko and en subs: run a discovery scan and confirm in server logs that the ko source is selected over en for that series
  </how-to-verify>
  <resume-signal>Type "approved" when the Overrides tab works end-to-end, or describe issues for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH /overrides | source_lang_override and model_override enter from the browser; server validates |
| React state → API call | Client-side validation rejects invalid codes before PATCH; server also validates (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | source_lang_override chip input — client-side validation | mitigate | Client validates /^[a-z]{2}$/ before appending to list AND before PATCH; server validates same regex (defense in depth, ASVS V5 L1) |
| T-10-11 | Tampering | model_override free-text — length | mitigate | Client enforces 256-char length limit with inline error per UI-SPEC; server Pydantic str type accepts any non-null string (model ID validation is not possible without endpoint-specific knowledge — accepted per RESEARCH.md open question 3) |
| T-10-12 | Information Disclosure | provenance badge shows global config model name | accept | This is intentional: the operator sees the global model in the "Inherited: {globalModel}" badge so they know what they'd be overriding. The model name is not a secret (it is operator-configured and visible in Settings already). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed — pure component authoring against existing React 19 + Vite 7 + Tailwind + lucide-react |
</threat_model>

<verification>
TypeScript build clean:
`cd frontend && npm run build`

Full test suite still clean:
`uv run pytest tests/ -q`

Human UAT items (in checkpoint above) confirm SVC-05 visually and functionally.
</verification>

<success_criteria>
- BibleEditor has a fifth "Overrides" tab visible in the tab bar
- OverridesSection renders all three sub-fields: Source Language Preference, Model Override, Register
- SourcePriorityEditor validates 2-letter codes and shows inline error on invalid input
- "Save Overrides" button submits PATCH /api/bible/series/{id}/overrides with source_lang_override and model_override ONLY — register is excluded from this payload
- Register sub-section uses patchRegister independently (not the overrides endpoint); the two save paths are fully separate
- Clear links immediately persist null overrides
- Provenance badges correctly show inherited vs. active override state
- npm run build exits 0 with no TypeScript errors
- Human verifier confirms all 13 verification steps pass or reports specific issues
</success_criteria>

<output>
Create `.planning/phases/10-source-selection-per-series-overrides/10-04-SUMMARY.md` when done.

Append the following rows to `.planning/phases/10-source-selection-per-series-overrides/10-VALIDATION.md`
Per-Task Verification Map table on completion:

| 10-04-01 | 04 | 2 | SVC-05, D-114 | T-10-10, T-10-11, T-10-12 | Save Overrides excludes register; client validates 2-letter codes | build | `cd frontend && npm run build` | ❌ pre-impl | ⬜ pending |
| 10-04-02 | 04 | 2 | SVC-05 | — | End-to-end Overrides tab functional | human-UAT | (checkpoint above) | N/A | ⬜ pending |
</output>
