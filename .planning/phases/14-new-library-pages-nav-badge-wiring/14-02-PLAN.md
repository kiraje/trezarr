---
phase: 14-new-library-pages-nav-badge-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/src/components/app-sidebar.tsx
  - frontend/src/pages/NotFound.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "WR-01/NAV-03 (per app-sidebar.tsx): SidebarContent wraps SidebarMenu in a SidebarGroup element (adds p-2 inset, fixes flush-wall rendering per WR-01 from 14-CARRYFORWARD-REVIEW.md)"
    - "NAV-03/D-09 (per app-sidebar.tsx): Series and Movies SidebarMenuItem rows display a count badge (purple, bg-[hsl(var(--primary))]) showing the number of items with translated_count < total_count; badge auto-hides when count === 0 (renders nothing)"
    - "NAV-03/D-10 (per app-sidebar.tsx): Series and Movies SidebarMenuItem rows display a LIVE badge (emerald, bg-emerald-600) when the backing *arr service is connected per the D-10 derivation rule; badge hidden in collapsed icon mode via group-data-[collapsible=icon]:hidden"
    - "WR-03 (per App.tsx + NotFound.tsx): navigating to any unmatched path (e.g. /nonexistent) renders the NotFound page with heading 'Page not found' and a 'Go to Series' button; the catch-all Route path='*' is the last child of the layout route"
    - "IN-01 (per useParams guard note): this plan does not implement SeriesDetail but records the guard contract — SeriesDetail.tsx in plan 05 uses useParams<{ seriesId: string }>() with explicit isNaN guard before fetch"
  artifacts:
    - path: "frontend/src/components/app-sidebar.tsx"
      provides: "Sidebar with SidebarGroup wrapper + LIVE badge + count badge wired to LibraryContext"
      contains: "SidebarGroup"
    - path: "frontend/src/pages/NotFound.tsx"
      provides: "Minimal 404 page per WR-03"
      contains: "Page not found"
    - path: "frontend/src/App.tsx"
      provides: "Catch-all Route path='*' added as last child of layout route"
      contains: "NotFound"
  key_links:
    - from: "frontend/src/components/app-sidebar.tsx"
      to: "frontend/src/contexts/LibraryContext.tsx"
      via: "useLibraryContext() + derived helpers"
      pattern: "useLibraryContext"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/NotFound.tsx"
      via: "Route path='*' catch-all"
      pattern: "NotFound"
---

<objective>
Wire the sidebar count + LIVE badges (NAV-03), add the SidebarGroup wrapper (WR-01 carry-forward),
create the 404 NotFound page (WR-03), and add the catch-all route to App.tsx.

Purpose: Sidebar badges are the only nav-visible NAV-03 requirement. They read from LibraryContext
(populated by Series/Movies pages in plans 03-04). The 404 page is a small, self-contained addition
that belongs here since App.tsx is already being touched.

Output: Updated app-sidebar.tsx with SidebarGroup + badge slots; new NotFound.tsx; App.tsx with
catch-all route. Build stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-CONTEXT.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-UI-SPEC.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-CARRYFORWARD-REVIEW.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-01-SUMMARY.md
@frontend/src/components/app-sidebar.tsx
@frontend/src/App.tsx
@frontend/src/components/ui/sidebar.tsx
@frontend/src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SidebarGroup wrapper + LIVE/count badges to app-sidebar.tsx</name>
  <files>frontend/src/components/app-sidebar.tsx</files>
  <action>
    Read frontend/src/components/app-sidebar.tsx and frontend/src/components/ui/sidebar.tsx
    (to verify SidebarGroup export name) before editing.

    Make the following changes to app-sidebar.tsx:

    1. IMPORTS: Add SidebarGroup to the import list from ./ui/sidebar. Add Badge from ./ui/badge.
       Add useLibraryContext, getSeriesLiveBadge, getMoviesLiveBadge, getSeriesNeedsViCount,
       getMoviesNeedsViCount from ../contexts/LibraryContext.

    2. SIDEBARGROUP WRAPPER (WR-01): Wrap the existing SidebarMenu in a SidebarGroup:
         <SidebarContent>
           <SidebarGroup>
             <SidebarMenu>
               {NAV_ITEMS.map(...)}
             </SidebarMenu>
           </SidebarGroup>
         </SidebarContent>
       SidebarGroup adds the p-2 inset that prevents the nav ul from rendering flush to panel walls.

    3. CONTEXT CONSUMPTION: Inside the AppSidebar component body, call:
         const { libraryData } = useLibraryContext();
       Then derive:
         const seriesCount = getSeriesNeedsViCount(libraryData);
         const moviesCount = getMoviesNeedsViCount(libraryData);
         const seriesLive = getSeriesLiveBadge(libraryData);
         const moviesLive = getMoviesLiveBadge(libraryData);

    4. NAV ITEM BADGE SLOTS: Update the SidebarMenuButton render. The current render is:
         <Icon size={16} />
         <span>{label}</span>
       Update to:
         <Icon size={16} />
         <span className="flex-1">{label}</span>
         {/* Badge slot — only for /series and /movies rows */}
         {(to === "/series" || to === "/movies") && (
           <span className="flex items-center gap-1">
             {(to === "/series" ? seriesLive : moviesLive) && (
               <Badge
                 className="ml-0 text-xs bg-emerald-600 text-white border-transparent
                            group-data-[collapsible=icon]:hidden"
                 aria-label="Service connected"
               >
                 LIVE
               </Badge>
             )}
             {(to === "/series" ? seriesCount : moviesCount) > 0 && (
               <Badge
                 className="ml-0 text-xs bg-[hsl(var(--primary))] text-primary-foreground
                            border-transparent group-data-[collapsible=icon]:hidden"
                 aria-label={`${to === "/series" ? seriesCount : moviesCount} items need Vietnamese translation`}
               >
                 {to === "/series" ? seriesCount : moviesCount}
               </Badge>
             )}
           </span>
         )}

    5. UPDATE DOCSTRING: Add a note referencing D-09/D-10 badge wiring and WR-01 SidebarGroup.

    IMPORTANT: The badge classes use Tailwind opacity notation — bg-[hsl(var(--primary))] works
    for the count badge. The LIVE badge uses bg-emerald-600 (solid, per UI-SPEC §Badge Color Token
    Mapping). Do NOT add /80 to the nav count badge (it should be solid for legibility at small size).

    The group-data-[collapsible=icon]:hidden on the badge span (or on each Badge) hides the badges
    when the sidebar is collapsed to icon rail (no room for text labels). The SidebarMenuButton
    already applies data-collapsible to the parent Sidebar group wrapper.

    Strict TS: all new imports must be used. The to variable in the map callback is already
    narrowly typed from NAV_ITEMS as const — the === "/series" comparison is safe.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. app-sidebar.tsx contains SidebarGroup, imports Badge and useLibraryContext,
    and renders badge slots for /series and /movies rows. The SidebarMenu is nested inside SidebarGroup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NotFound.tsx and add catch-all route to App.tsx (WR-03)</name>
  <files>frontend/src/pages/NotFound.tsx, frontend/src/App.tsx</files>
  <action>
    STEP A — Create frontend/src/pages/NotFound.tsx:
    Minimal 404 page per 14-UI-SPEC.md §Loading/Empty/Error States §404/NotFound:

      import { useNavigate } from "react-router-dom";
      import { Button } from "../components/ui/button";

      export default function NotFound() {
        const navigate = useNavigate();
        return (
          <div className="flex flex-col items-center justify-center h-64 gap-4">
            <h1 className="text-xl font-semibold">Page not found</h1>
            <p className="text-muted-foreground">
              The page you're looking for doesn't exist.
            </p>
            <Button variant="outline" onClick={() => navigate('/series')}>
              Go to Series
            </Button>
          </div>
        );
      }

    No data fetching. No useEffect. Strict TS: import only what is used.

    STEP B — Update frontend/src/App.tsx:
    Read App.tsx first. Add:
      1. Import NotFound from "./pages/NotFound" at the top import block.
      2. Add <Route path="*" element={<NotFound />} /> as the LAST child inside the layout
         route (after the /library route). The catch-all must be last or it shadows other routes.
      3. Update the file-header docstring route list to include: /nonexistent → NotFound (catch-all).

    Do NOT move, reorder, or remove any existing routes. The /library transitional route stays
    (removed in plan 06). The navigate target in the index redirect remains /series.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. frontend/src/pages/NotFound.tsx exists with "Page not found" heading
    and "Go to Series" Button. App.tsx imports NotFound and has a Route path="*" as last child
    of the layout route.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL bar → App.tsx routing | Catch-all Route path="*" catches unmatched paths; NotFound renders static content only (no URL reflection into DOM) |
| LibraryContext → sidebar | Badge counts derived from fetched library data; no user-controlled input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering (reflected XSS) | NotFound.tsx | mitigate | Page renders only static copy ("Page not found", "The page you're looking for doesn't exist.") — no URL/pathname is reflected into the DOM; React auto-escapes JSX text |
| T-14-02-02 | Spoofing (open redirect) | NotFound.tsx navigate('/series') | mitigate | Navigate target is the hardcoded literal '/series', not user-controlled |
| T-14-02-SC | Tampering (supply chain) | npm/shadcn installs | accept (none) | Zero new packages this plan — Badge and Button were vendored in Phase 11; SidebarGroup is from the Phase-11/12 sidebar.tsx vendor drop |
</threat_model>

<verification>
cd frontend && npm run build

Must: exit 0; tsc -b strict; vite build clean; trezarr/web/static populated.
</verification>

<success_criteria>
- cd frontend && npm run build exits 0.
- app-sidebar.tsx wraps SidebarMenu in SidebarGroup (WR-01 fixed).
- app-sidebar.tsx renders LIVE badge (emerald) and count badge (purple) on /series and /movies rows, reading from useLibraryContext(); badges hidden when count=0 or not live.
- frontend/src/pages/NotFound.tsx exists with heading "Page not found" and "Go to Series" button.
- App.tsx has Route path="*" as the last child of the layout route, importing NotFound.
</success_criteria>

<output>
Create .planning/phases/14-new-library-pages-nav-badge-wiring/14-02-SUMMARY.md when done.
</output>
