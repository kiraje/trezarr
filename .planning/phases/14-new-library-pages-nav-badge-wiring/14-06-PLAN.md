---
phase: 14-new-library-pages-nav-badge-wiring
plan: 06
type: execute
wave: 4
depends_on:
  - 14-02
  - 14-03
  - 14-04
  - 14-05
files_modified:
  - frontend/src/pages/Library.tsx
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-05
  - NAV-03

must_haves:
  truths:
    - "D-11 (per Library.tsx): frontend/src/pages/Library.tsx is DELETED — the file no longer exists in the repository"
    - "D-11 (per App.tsx): the 'import Library from ./pages/Library' import and '<Route path=library' route are REMOVED from App.tsx; navigating to /library now hits the catch-all NotFound route"
    - "D-11 (per client.ts): the @deprecated EpisodeRow, LibrarySeriesItem, LibraryMovieItem interfaces and the old LibraryResponse interface are REMOVED from client.ts; only the new Phase-13 types remain"
    - "LIB-01/LIB-02/LIB-03/LIB-05/NAV-03 (build gate): cd frontend && npm run build exits 0 after all deletions — the codebase has zero references to Library.tsx, the old EpisodeRow type, or the /library route"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Route table with /library route removed; catch-all NotFound from plan 02 retained"
      contains: "NotFound"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/NotFound.tsx"
      via: "Route path='*' catch-all (added plan 02) now also catches /library"
      pattern: "NotFound"
---

<objective>
Complete the P12→P14 handoff: delete Library.tsx, remove the transitional /library route and
import from App.tsx, and remove the now-dead @deprecated types (EpisodeRow, old LibrarySeriesItem,
old LibraryMovieItem, old LibraryResponse) from client.ts.

Purpose: This is the final cleanup step that D-11 requires. It MUST run after all three new pages
(Series, Movies, SeriesDetail) are functional and verified, so it is Wave 4. After this plan,
/library returns the 404 NotFound page (catch-all from plan 02).

Output: Library.tsx deleted; App.tsx cleaned; client.ts cleaned; build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-CONTEXT.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-02-SUMMARY.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-03-SUMMARY.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-04-SUMMARY.md
@.planning/phases/14-new-library-pages-nav-badge-wiring/14-05-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Library.tsx, remove /library route from App.tsx, remove dead types from client.ts</name>
  <files>frontend/src/pages/Library.tsx, frontend/src/App.tsx, frontend/src/api/client.ts</files>
  <action>
    Read App.tsx and client.ts before editing. Do NOT read Library.tsx (about to be deleted).

    STEP A — Verify preconditions before deleting:
    Run: cd frontend && npm run build
    Confirm it exits 0. If it does not, STOP and report — the prior plans have an error that
    must be fixed before Library.tsx can be safely removed.

    STEP B — Delete Library.tsx:
    Delete the file frontend/src/pages/Library.tsx using a shell rm command:
      rm frontend/src/pages/Library.tsx
    This file is being fully replaced by Series.tsx + SeriesDetail.tsx + Movies.tsx.

    STEP C — Remove Library import + route from App.tsx:
    Edit frontend/src/App.tsx to:
    1. Remove the line: import Library from "./pages/Library"; // transitional (D-03) — removed Phase 14
    2. Remove the route: <Route path="library" element={<Library />} /> {/* transitional, not in nav (D-03) */}
    3. Update the file-header JSDoc docstring: remove "/library → Library page (transitional route...)"
       from the route list; add "/library → NotFound (catch-all, deleted Phase 14)" as a note.
    4. Do NOT touch any other route, import, or the LibraryProvider wrapper added in plan 01.

    STEP D — Remove deprecated types from client.ts:
    Edit frontend/src/api/client.ts to remove:
    - The @deprecated LibrarySeriesItem interface
    - The @deprecated LibraryMovieItem interface
    - The @deprecated LibraryResponse (old, without translated_count/total_count) interface
    - The @deprecated EpisodeRow interface
    Also remove:
    - getLibrary() return type annotation should now ONLY reference the new LibraryResponse
      (already done in plan 01, but confirm the @deprecated one is gone)
    - getSeriesEpisodes() return type should now ONLY reference SeriesEpisodesResponse
      (confirm the old EpisodeRow[] signature is gone)

    Check that postTranslate() is still present and unchanged.

    STEP E — Build gate:
    Run: cd frontend && npm run build
    If it fails, a file still references Library or the old types. Grep for remaining references:
      grep -r "Library\|EpisodeRow\|LibrarySeriesItem\|LibraryMovieItem" frontend/src/
    Fix any found references (there should be none after the above steps — only the new types
    from plan 01 remain).

    STEP F — Update App.tsx file header comment to remove the transitional /library note and
    confirm the route list is accurate after deletion.

    Strict TS: after cleanup, no @deprecated interfaces exist in client.ts. All imports in
    App.tsx must be used (noUnusedLocals — Library is now deleted so its import must be removed).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. frontend/src/pages/Library.tsx does not exist (file deleted).
    App.tsx has no Library import and no /library route. client.ts has no EpisodeRow,
    LibrarySeriesItem, LibraryMovieItem, or old LibraryResponse interface. The /library path
    now resolves to the catch-all NotFound page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /library URL → catch-all NotFound | Previously rendered Library.tsx content; now renders static NotFound — no data exposure change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Information Disclosure | Library.tsx deletion | accept | Library.tsx contained no secrets; its deletion removes dead code, not sensitive data |
| T-14-06-SC | Tampering (supply chain) | npm/shadcn installs | accept (none) | Zero new packages this plan — pure deletion + cleanup |
</threat_model>

<verification>
# Verify Library.tsx is deleted:
test ! -f frontend/src/pages/Library.tsx && echo "DELETED OK" || echo "STILL EXISTS"

# Verify no remaining references:
grep -r "Library\b" frontend/src/App.tsx || echo "No Library references in App.tsx"
grep -r "EpisodeRow\|LibrarySeriesItem\|LibraryMovieItem" frontend/src/api/client.ts || echo "No deprecated types in client.ts"

# Build gate (the definitive check):
cd frontend && npm run build
</verification>

<success_criteria>
- cd frontend && npm run build exits 0 (tsc -b strict + vite build clean).
- frontend/src/pages/Library.tsx does NOT exist.
- frontend/src/App.tsx has no "Library" import and no path="library" route.
- frontend/src/api/client.ts has no EpisodeRow, LibrarySeriesItem, LibraryMovieItem (deprecated ones removed; new SeriesItem/MovieItem remain).
- Navigating to /library (via the catch-all route) renders NotFound ("Page not found").
- The six real nav routes still work: /series, /movies, /series/:id, /queue, /history, /settings.
</success_criteria>

<output>
Create .planning/phases/14-new-library-pages-nav-badge-wiring/14-06-SUMMARY.md when done.
</output>
