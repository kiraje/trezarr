---
phase: 06-relationship-evolution-self-review
plan: 02
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - trezarr/bible/models.py
  - trezarr/bible/dto.py
  - trezarr/bible/store.py
  - trezarr/bible/analyze.py
  - trezarr/translate/reconcile.py
  - trezarr/config.py
autonomous: true
requirements:
  - BIBLE-07

must_haves:
  truths:
    - "A relationship shift detected in Pass-1 is written as a relationship_event row in the DB (BIBLE-07-A)"
    - "load_series_bible returns the events list in SeriesBibleDTO (BIBLE-07-B)"
    - "reconcile_attributions applies a logged transition's terms and bumps valid_from_episode; without a transition, the prior pair carries forward unchanged (BIBLE-07-C)"
    - "A locked address_map entry wins over a transition (BIBLE-07-D / D-34)"
    - "No transition + no high-confidence survivors → safe default (BIBLE-07-E; Phase-5 invariant preserved)"
    - "Case-insensitive name matching in merge_bible_analysis for relationship events (BIBLE-07-F / CR-01)"
    - "No new asyncio.Semaphore anywhere (D-06 / Pitfall 1)"
    - "No SQLAlchemy imports outside trezarr/bible/store.py (D-39 / Pitfall 5)"
    - "All 226 pre-existing tests remain green"
  artifacts:
    - path: "trezarr/bible/models.py"
      provides: "Series.relationship_events ORM relationship + RelationshipEvent.series back-reference"
      contains: "relationship_events"
    - path: "trezarr/bible/dto.py"
      provides: "RelationshipEventDTO + SeriesBibleDTO.relationship_events field"
      contains: "RelationshipEventDTO"
    - path: "trezarr/bible/store.py"
      provides: "record_relationship_event writer + load_series_bible extended with selectinload(Series.relationship_events)"
      contains: "record_relationship_event"
    - path: "trezarr/bible/analyze.py"
      provides: "RelationshipEventInference model, BibleAnalysis.relationship_events field, _build_analysis_prompt episode_key param, merge_bible_analysis Step 5"
      contains: "RelationshipEventInference"
    - path: "trezarr/translate/reconcile.py"
      provides: "_find_transition_for_pair, _derive_transition_terms, transition-precedence branch in reconcile_attributions (per D-54)"
      contains: "_find_transition_for_pair"
    - path: "trezarr/config.py"
      provides: "Phase-6-A TrezarrSettings fields: enable_relationship_events, relationship_event_min_confidence"
      contains: "enable_relationship_events"
  key_links:
    - from: "trezarr/bible/analyze.py merge_bible_analysis Step 5"
      to: "trezarr/bible/store.py record_relationship_event"
      via: "await record_relationship_event(...)"
      pattern: "record_relationship_event"
    - from: "trezarr/bible/store.py load_series_bible"
      to: "trezarr/bible/models.py Series.relationship_events"
      via: "selectinload(Series.relationship_events)"
      pattern: "selectinload.*relationship_events"
    - from: "trezarr/translate/reconcile.py reconcile_attributions"
      to: "trezarr/bible/store.py upsert_address_pair"
      via: "await upsert_address_pair(..., valid_from_episode=episode_key)"
      pattern: "valid_from_episode"
---

<objective>
Wave 1 — Relationship Evolution vertical slice (BIBLE-07 end-to-end).

Lights up the empty `relationship_event` table (created in Phase 4, D-31) with all the wiring needed for pronoun-pair evolution across episodes: ORM relationship on `Series`, a `RelationshipEventDTO`, a store writer, a `load_series_bible` extension, `BibleAnalysis` extended with `RelationshipEventInference`, the `_build_analysis_prompt` `episode_key` parameter, a new `merge_bible_analysis` Step 5, and the transition-precedence branch in `reconcile_attributions`.

Success criterion #1 ("intentional, not silent drift") and success criterion #3 ("consistent across a logged change, auditable") are both structural after this plan — they are enforced by code, not prompts.

Purpose: BIBLE-07 delivered end-to-end so that relationship transitions from Pass-1 are persisted and applied correctly in reconciliation.
Output: 6 modified production files; BIBLE-07-A through F tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relationship-evolution-self-review/06-CONTEXT.md
@.planning/phases/06-relationship-evolution-self-review/06-RESEARCH.md
@.planning/phases/06-relationship-evolution-self-review/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bible store foundation — models.py ORM relationship, dto.py DTO + SeriesBibleDTO, store.py writer + load extension, config.py Phase-6-A fields</name>
  <files>
    trezarr/bible/models.py,
    trezarr/bible/dto.py,
    trezarr/bible/store.py,
    trezarr/config.py
  </files>

  <read_first>
    Read each file ONCE in a single pass and extract all needed patterns before writing:

    1. `trezarr/bible/models.py` lines 85–215 — the `Series` class (current relationships: `characters`, `terms`, `address_maps` at lines 87–95) and `RelationshipEvent` class (lines 187–213). Extract: the exact `relationship` import at the top; the `Mapped[list["..."]]` type annotation pattern; the `back_populates` / `cascade` pattern from `address_maps`. PATTERNS.md §models.py shows the two additions verbatim.

    2. `trezarr/bible/dto.py` lines 120–215 — `AddressMapDTO` (lines 120–146 for from_attributes ConfigDict pattern), `SeriesBibleDTO` (lines 180–215 for the field list and safe-default `[]` pattern). PATTERNS.md §dto.py shows `RelationshipEventDTO` and the `relationship_events: list[RelationshipEventDTO] = []` field addition.

    3. `trezarr/bible/store.py` lines 1–80 (imports + module docstring), lines 173–230 (`load_series_bible` — the `stmt.options(selectinload(...))` block and `SeriesBibleDTO` construction), lines 870–929 (`upsert_address_pair` — the public transaction-owner pattern to mirror for `record_relationship_event`), lines 581–697 (`_upsert_address_pair_in_session` — the SELECT+INSERT single-txn pattern). PATTERNS.md §store.py shows all additions.

    4. `trezarr/config.py` lines 120–150 — the Phase-5 settings group comment header + field names. PATTERNS.md §config.py shows the exact Phase-6-A block to append.
  </read_first>

  <behavior>
    models.py:
    - Series class gets `relationship_events: Mapped[list["RelationshipEvent"]] = relationship(back_populates="series", cascade="all, delete-orphan")` after `address_maps`.
    - RelationshipEvent class gets `series: Mapped["Series"] = relationship(back_populates="relationship_events")`.

    dto.py:
    - `RelationshipEventDTO` added after `AddressMapDTO`: `model_config = ConfigDict(from_attributes=True)`, fields `id`, `series_id`, `character_a_id`, `character_b_id`, `episode_marker`, `description: str | None = None`, `created_at: datetime | None = None`, `suggested_self_term: str | None = None`, `suggested_address_term: str | None = None`. The suggested_* fields are in-memory only (not persisted to DB, per D-31 schema lock).
    - `SeriesBibleDTO` gets `relationship_events: list[RelationshipEventDTO] = []` after `address_map`.

    store.py:
    - Import block extended: `RelationshipEvent` added to models import; `RelationshipEventDTO` added to dto import.
    - `load_series_bible` stmt.options block extended with `selectinload(Series.relationship_events)`; SeriesBibleDTO construction adds `relationship_events=[RelationshipEventDTO.model_validate(e, from_attributes=True) for e in row.relationship_events]`.
    - `record_relationship_event(session_factory, *, series_id, character_a_id, character_b_id, episode_marker, description=None) -> RelationshipEventDTO` added after `upsert_address_pair`. Pattern: `async with session_factory() as session: async with session.begin():` — SELECT for dedup by `(series_id, character_a_id, character_b_id, episode_marker)`, return existing DTO if found, else INSERT + flush + return new DTO. Uses `scalar_one_or_none`. No `locked_fields` column (INSERT-only, no merge path, per D-52).

    config.py:
    - Phase-6 comment header + `enable_relationship_events: bool = True` + `relationship_event_min_confidence: float = 0.0` appended after Phase-5 group.
  </behavior>

  <action>
    Work through each file in order. After each file write, run the quick test command to confirm no import errors before moving on.

    models.py: Add the `relationship_events` SQLAlchemy `relationship` to `Series` (after `address_maps`, per PATTERNS.md §models.py). Add the `series` back-reference to `RelationshipEvent` (after the `description` column). Do NOT modify any existing column or relationship. This is the ONLY change that requires no import modifications — `relationship` is already imported.

    dto.py: Add `RelationshipEventDTO` class after `AddressMapDTO`. Extend `SeriesBibleDTO` with `relationship_events: list[RelationshipEventDTO] = []` field (safe default, additive per D-39). `datetime` is already imported; no new imports needed.

    store.py: Per D-39/Pitfall 5 — `record_relationship_event` MUST live in `store.py`, NOT in any new file. Extend the `from trezarr.bible.models import ...` line to add `RelationshipEvent`. Extend the `from trezarr.bible.dto import ...` line to add `RelationshipEventDTO`. In `load_series_bible`, add `selectinload(Series.relationship_events)` to the stmt.options block and add the list comprehension to `SeriesBibleDTO(...)`. Add `record_relationship_event` as a new public async function after `upsert_address_pair` (PATTERNS.md Pattern 1 is the complete implementation — use it verbatim). Also update the module docstring public API list to include `record_relationship_event`.

    config.py: Append the Phase-6-A block per PATTERNS.md §config.py. Use the exact comment header format `# ── Phase 6: Relationship Evolution + Self-Review (D-51…D-60) ──────────────────`. Add only the two Phase-6-A fields (`enable_relationship_events`, `relationship_event_min_confidence`) — Phase-6-B fields (self-review) belong in plan 06-03.
  </action>

  <verify>
    <automated>uv run pytest tests/bible/test_relationship_events.py::test_relationship_event_written_to_db tests/bible/test_relationship_events.py::test_load_series_bible_includes_events tests/bible/test_relationship_events.py::test_name_matching_case_insensitive -x -q 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/bible/test_relationship_events.py -x -q` exits 0 with all 3 tests PASSING (not xfail — the implementation makes them green).
    - `uv run pytest tests/bible/test_relationship_events.py::test_relationship_event_written_to_db -x` exits 0.
    - `uv run pytest tests/bible/test_relationship_events.py::test_load_series_bible_includes_events -x` exits 0.
    - `uv run pytest tests/bible/test_relationship_events.py::test_name_matching_case_insensitive -x` exits 0.
    - `uv run pytest tests/bible/ -x -q` exits 0 — all Bible tests green.
    - `python -c "from trezarr.bible.dto import RelationshipEventDTO; from trezarr.bible.store import record_relationship_event; print('OK')"` exits 0.
    - `python -c "from trezarr.config import TrezarrSettings; s = TrezarrSettings(llm_api_key='x'); print(s.enable_relationship_events)"` prints `True`.
    - `grep -c "relationship_events" trezarr/bible/models.py` >= 2 (one on Series, one on RelationshipEvent).
    - No SQLAlchemy-related import in any file other than `trezarr/bible/store.py`, `trezarr/bible/models.py` (verified by the existing `tests/bible/test_dto_boundary.py`).
  </acceptance_criteria>

  <done>RelationshipEventDTO, record_relationship_event, load_series_bible extension, and Phase-6-A config fields all functional. BIBLE-07-A, B tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Analyze + Reconcile — RelationshipEventInference, _build_analysis_prompt episode_key, merge_bible_analysis Step 5, _find_transition_for_pair, transition-precedence branch in reconcile_attributions</name>
  <files>
    trezarr/bible/analyze.py,
    trezarr/translate/reconcile.py
  </files>

  <read_first>
    Read each file ONCE in a single pass:

    1. `trezarr/bible/analyze.py` — full file. Extract: `AddressMapInference` model shape (lines 53–59, the direct analog for `RelationshipEventInference`); `BibleAnalysis` model (lines 80–99, `extra="ignore"`, `populate_by_name=True`, field list); `_build_analysis_prompt` signature and body (lines 107–205, specifically the `parts.append(...)` pattern for the [INSTRUCTIONS] block and the `episode_key` injection point at the `return "\n".join(parts)` near line 204); `analyze_file` call site where `_build_analysis_prompt` is called (line ~274); `merge_bible_analysis` body lines 355–487 — the `name_to_id` dict construction (CR-01 pattern at line ~371), the per-row exception handling loop pattern, and the Step 4 end (line ~487). PATTERNS.md §analyze.py has copy-ready code for all changes.

    2. `trezarr/translate/reconcile.py` — full file. Extract: existing imports (TYPE_CHECKING block, lines 21–25); existing helpers before `reconcile_attributions` (lines up to ~122); `KINSHIP_RECIPROCAL` dict (lines 34–53); `get_safe_default` signature; `reconcile_attributions` signature (what parameters it receives — specifically whether it receives `bible`, `episode_key`, `session_factory`, `series_id`, `settings`); the lock-check block (lines 243–254); the survivors gate (lines 208–241); the reciprocal step (lines 263–291). PATTERNS.md §reconcile.py shows `_find_transition_for_pair`, `_derive_transition_terms`, and the for-loop insertion points.

    Cross-reference: `.planning/phases/06-relationship-evolution-self-review/06-RESEARCH.md` §Q3 (concrete precedence flow pseudocode) and §Pitfall 3 (reciprocal coherence) and §Pitfall 6 (forward-only, episode_marker filter).
  </read_first>

  <behavior>
    analyze.py:
    - `RelationshipEventInference(BaseModel)` added after `AddressMapInference`: fields `character_a_name: str`, `character_b_name: str`, `episode_marker: str`, `description: str`, `suggested_self_term: str | None = None`, `suggested_address_term: str | None = None`.
    - `BibleAnalysis` gets `relationship_events: list[RelationshipEventInference] = []` (additive, safe default).
    - `_build_analysis_prompt` gains `episode_key: str = ""` parameter. Appends the `relationship_events` instruction block to `parts` (from PATTERNS.md — "ONLY emit when a relationship has CHANGED", include episode_marker instruction, optional suggested terms).
    - `analyze_file` threads `episode_key=episode_key` to `_build_analysis_prompt` at the call site.
    - `merge_bible_analysis` Step 5 (after Step 4 end): for each `analysis.relationship_events`, resolve `character_a_name` and `character_b_name` via the same `name_to_id` dict using `.strip().lower()` (CR-01 / Pitfall 4). Skip and warn if either name unresolvable. Call `await record_relationship_event(session_factory, series_id=series_id, character_a_id=..., character_b_id=..., episode_marker=episode_key, description=event.description)`. Per-row try/except with warning (WR-06 pattern — no total-failure raise for events, advisory only). Import `record_relationship_event` from `trezarr.bible.store` at the TOP of the file (not inside the function — it is a store module, not a SQLAlchemy model, so D-39 is not violated).

    reconcile.py:
    - `_find_transition_for_pair(relationship_events, spk_id, addr_id, episode_key) -> RelationshipEventDTO | None` added before `reconcile_attributions`. Iterates events, filters to `event.episode_marker == episode_key` (Pitfall 6: forward-only filter — exact match, not <=), matches EITHER direction `(char_a_id==spk_id AND char_b_id==addr_id) OR (char_a_id==addr_id AND char_b_id==spk_id)` (Pitfall 3: undirected events), returns first match or None.
    - `_derive_transition_terms(transition, resolved_map, addr_id, id_to_gender, settings) -> tuple[str, str]` added before `reconcile_attributions`. Precedence: (1) `transition.suggested_self_term AND transition.suggested_address_term` → use them; (2) `get_safe_default(id_to_gender.get(addr_id), settings)` fallback (per D-54 / RESEARCH Q2).
    - `reconcile_attributions` for-loop: insert AFTER lock-check block and BEFORE survivors gate — the transition-precedence branch per D-54: if `settings.enable_relationship_events` and `_find_transition_for_pair(...)` returns a transition → derive new terms, set `resolved_map[pair]`, call `await upsert_address_pair(..., valid_from_episode=episode_key)`, `continue`. The existing Phase-5 lock-check block is PRESERVED unchanged (lock wins over transition per D-34). The transition branch ONLY fires when no lock applies.
    - Reciprocal coherence for transitions: the existing reciprocal step (lines 263–291) already handles this — when `resolved_map[pair]` is set before step (e) runs, `KINSHIP_RECIPROCAL.get((new_self, new_addr))` finds the reverse and writes it for `(addr_id, spk_id)`. No additional code needed (per PATTERNS.md §reconcile.py "Reciprocal coherence for transitions").
    - TYPE_CHECKING block extended with `RelationshipEventDTO` import from `trezarr.bible.dto`.
    - Add `# No asyncio.Semaphore in this module. LLMClient._semaphore is the sole gate (D-06, Pitfall 1).` comment to module header if not already present.
  </behavior>

  <action>
    analyze.py changes are additive — no existing logic is removed or changed. Follow this order:
    1. Add `RelationshipEventInference` model after `AddressMapInference`.
    2. Add `relationship_events: list[RelationshipEventInference] = []` field to `BibleAnalysis`.
    3. Extend `_build_analysis_prompt` signature with `episode_key: str = ""`.
    4. Add the relationship_events instruction block to `_build_analysis_prompt`'s parts list (PATTERNS.md §analyze.py, the multi-line string block with the `IMPORTANT: Do NOT emit` instruction).
    5. Update the `_build_analysis_prompt(...)` call site in `analyze_file` to pass `episode_key=episode_key`.
    6. Add `from trezarr.bible.store import record_relationship_event` to the top-level imports in `analyze.py` (not inside merge_bible_analysis — this avoids a runtime import at call time).
    7. Add Step 5 to `merge_bible_analysis` after Step 4 (PATTERNS.md §analyze.py Step 5 block is copy-ready — use `.strip().lower()` for both name lookups, per-row try/except, no total-failure raise).

    reconcile.py changes — add two helper functions before `reconcile_attributions`, then insert the transition branch inside the for loop:
    1. Extend TYPE_CHECKING import to add `RelationshipEventDTO`.
    2. Add `_find_transition_for_pair` and `_derive_transition_terms` as module-level private functions before `reconcile_attributions` (PATTERNS.md §reconcile.py has both verbatim).
    3. In `reconcile_attributions`, locate the for-loop body. Add the transition-precedence branch AFTER the lock check block and BEFORE the `survivors = [...]` line. The branch structure: `if getattr(settings, "enable_relationship_events", True):` → `transition = _find_transition_for_pair(getattr(bible, "relationship_events", []), spk_id, addr_id, episode_key)` → if found, derive terms, set resolved_map, await upsert_address_pair with `valid_from_episode=episode_key`, `continue`. The `getattr` guard on `bible.relationship_events` allows the function to work with old-shape SeriesBibleDTO (safe degradation per PATTERNS.md).

    CRITICAL invariants to verify before committing:
    - No `asyncio.Semaphore(...)` instantiation in either file.
    - No `from trezarr.bible.models import ...` in `analyze.py` — only `from trezarr.bible.store import record_relationship_event` is acceptable (D-39).
    - The lock-check `continue` is ABOVE the transition branch `continue` (lock wins per D-34/D-54).
    - `_find_transition_for_pair` filters to `event.episode_marker == episode_key` (exact match — Pitfall 6).
    - Both `(spk_id, addr_id)` and `(addr_id, spk_id)` directions are matched (Pitfall 3).
  </action>

  <verify>
    <automated>uv run pytest tests/bible/test_relationship_events.py tests/translate/test_reconcile.py::test_transition_authorizes_terms_change tests/translate/test_reconcile.py::test_lock_beats_transition tests/translate/test_reconcile.py::test_no_transition_no_survivors_safe_default -x -q 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/bible/test_relationship_events.py::test_name_matching_case_insensitive -x` exits 0 (GREEN — BIBLE-07-F confirms CR-01 in merge_bible_analysis).
    - `uv run pytest tests/translate/test_reconcile.py::test_transition_authorizes_terms_change -x` exits 0 (GREEN — BIBLE-07-C).
    - `uv run pytest tests/translate/test_reconcile.py::test_lock_beats_transition -x` exits 0 (GREEN — BIBLE-07-D).
    - `uv run pytest tests/translate/test_reconcile.py::test_no_transition_no_survivors_safe_default -x` exits 0 (GREEN — BIBLE-07-E, Phase-5 invariant preserved).
    - `uv run pytest tests/translate/test_reconcile.py -x -q` exits 0 — ALL existing reconcile tests still pass.
    - `uv run pytest -x -q` (full suite) exits 0 — all 226 pre-existing tests green.
    - `grep -c "asyncio.Semaphore" trezarr/bible/analyze.py trezarr/translate/reconcile.py` = 0 (Pitfall 1 guard).
    - `grep -c "from trezarr.bible.models" trezarr/bible/analyze.py` = 0 (D-39 / Pitfall 5 guard — analyze.py must not import models.py).
    - `uv run pytest tests/bible/test_dto_boundary.py -x -q` exits 0 (D-39 DTO boundary test).
  </acceptance_criteria>

  <done>analyze.py extended with RelationshipEventInference + merge Step 5; reconcile.py extended with _find_transition_for_pair + transition-precedence branch. All BIBLE-07-A through F tests GREEN. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pass-1 LLM response → relationship_event DB row | LLM generates `character_a_name`, `character_b_name`, `description` — these cross the trust boundary into the DB |
| relationship_event.description → bible_event audit log | description is LLM-derived narrative text, not raw subtitle content |
| character name resolution (LLM output → character_id) | name_to_id lookup is the bridge; case-insensitive matching reduces but does not eliminate confusion risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | `relationship_event.description` stored in DB | mitigate | `description` is LLM-derived narrative output, never raw subtitle cue text. `merge_bible_analysis` Step 5 passes only `event.description` (from `RelationshipEventInference`, not source text). Existing `merge_bible_analysis` WR-06 per-row failure handling limits blast radius. |
| T-06-02-02 | Tampering | Character name confusion in relationship_event name resolution | mitigate | CR-01 case-insensitive `.strip().lower()` matching in `merge_bible_analysis` Step 5. Unresolvable names: `logger.warning` + `continue` (skip). Never crash or silently apply to wrong character. |
| T-06-02-03 | Tampering | Transition applied retroactively to wrong episode (Pitfall 6) | mitigate | `_find_transition_for_pair` filters strictly to `event.episode_marker == episode_key` (exact match). v1 is forward-only: past-episode transitions already applied via Address Map row; future events ignored. |
| T-06-02-04 | Elevation of Privilege | Lock bypass via relationship_event transition | mitigate | Transition-precedence branch fires ONLY when no lock applies (lock-check `continue` is above the transition branch in the for loop — D-34 precedence rule is structural). |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Wave 1. All libraries already in pyproject.toml. |
</threat_model>

<verification>
BIBLE-07 tests green (A through F):
`uv run pytest tests/bible/test_relationship_events.py tests/translate/test_reconcile.py::test_transition_authorizes_terms_change tests/translate/test_reconcile.py::test_lock_beats_transition tests/translate/test_reconcile.py::test_no_transition_no_survivors_safe_default -x -q`

D-39 boundary preserved:
`uv run pytest tests/bible/test_dto_boundary.py -x -q`

Full suite:
`uv run pytest -x -q`

No new Semaphore:
`grep -rn "asyncio.Semaphore" trezarr/ | grep -v "llm/client.py"`
(should produce zero output)

No SQLAlchemy leakage:
`grep -n "from trezarr.bible.models import" trezarr/bible/analyze.py trezarr/translate/reconcile.py`
(should produce zero output)
</verification>

<success_criteria>
- BIBLE-07-A: relationship_event row written correctly per D-52 dedup key
- BIBLE-07-B: SeriesBibleDTO.relationship_events list populated by load_series_bible
- BIBLE-07-C: transition-precedence branch applies new terms + bumps valid_from_episode
- BIBLE-07-D: lock beats transition (D-34 precedence structural, not prompt-dependent)
- BIBLE-07-E: Phase-5 safe-default invariant unchanged
- BIBLE-07-F: case-insensitive name matching in merge_bible_analysis (CR-01)
- All 226 pre-existing tests green
- No asyncio.Semaphore outside llm/client.py
- No SQLAlchemy import outside store.py/models.py
</success_criteria>

<output>
Create `.planning/phases/06-relationship-evolution-self-review/06-02-SUMMARY.md` when done.
</output>
