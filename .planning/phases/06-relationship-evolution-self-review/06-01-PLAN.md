---
phase: 06-relationship-evolution-self-review
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bible/test_relationship_events.py
  - tests/translate/test_self_review.py
  - tests/translate/test_reconcile.py
  - tests/bible/conftest.py
autonomous: true
requirements:
  - BIBLE-07
  - ENG-05

must_haves:
  truths:
    - "uv run pytest tests/bible/test_relationship_events.py tests/translate/test_self_review.py tests/translate/test_reconcile.py -x -q exits 0 (xfail stubs pass as XFAIL)"
    - "All 226 pre-existing tests remain green after this plan"
    - "Each of the 10 test node IDs listed in 06-VALIDATION.md is present and collected by pytest"
    - "No new ImportError at collection time in any test file"
  artifacts:
    - path: "tests/bible/test_relationship_events.py"
      provides: "RED stubs for BIBLE-07-A, B, F"
      contains: "test_relationship_event_written_to_db, test_load_series_bible_includes_events, test_name_matching_case_insensitive"
    - path: "tests/translate/test_self_review.py"
      provides: "RED stubs for ENG-05-A, B, C, D"
      contains: "test_pass4_corrects_violation_before_gate, test_pass4_failure_fallback_no_quarantine, test_pass4_disabled_skips_review, test_sentinel_failure_fallback_per_batch"
    - path: "tests/translate/test_reconcile.py"
      provides: "Extended with RED stubs for BIBLE-07-C, D, E"
      contains: "test_transition_authorizes_terms_change, test_lock_beats_transition, test_no_transition_no_survivors_safe_default"
    - path: "tests/bible/conftest.py"
      provides: "DB fixture re-export for bible test directory"
      contains: "from tests.db.conftest import db_engine, session_factory"
  key_links:
    - from: "tests/bible/test_relationship_events.py"
      to: "tests/bible/conftest.py"
      via: "pytest fixture injection"
      pattern: "session_factory"
    - from: "tests/translate/test_reconcile.py"
      to: "tests/translate/conftest.py"
      via: "pytest fixture injection"
      pattern: "session_factory"
---

<objective>
Wave 0 — RED test scaffold for Phase 6.

Creates the three test files that Waves 1 and 2 will turn GREEN. All stubs use the project-standard `pytest.mark.xfail(strict=False, raises=(ImportError, AssertionError, TypeError))` pattern so the full suite stays green before any production code is touched. The 226 pre-existing tests must remain green after this plan.

Purpose: Nyquist compliance — every Phase-6 behavior has a test node that can be run in < 90 s before any production code exists.
Output: 3 test files (2 new, 1 extended), 1 conftest.py, 10 xfail stub test functions mapping to the 10 rows in 06-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-relationship-evolution-self-review/06-VALIDATION.md
@.planning/phases/06-relationship-evolution-self-review/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/bible/test_relationship_events.py (BIBLE-07-A, B, F stubs) + ensure tests/bible/conftest.py re-exports db fixtures</name>
  <files>tests/bible/test_relationship_events.py, tests/bible/conftest.py</files>

  <read_first>
    Read these files before writing anything:
    - `tests/bible/conftest.py` — check if it already exists and has the db_engine/session_factory re-export from tests.db.conftest; create it only if missing or missing the re-export (PATTERNS.md §DB fixture re-export).
    - `tests/db/conftest.py` — confirm the fixture names exported (db_engine, session_factory).
    - `tests/bible/test_address_map.py` — the structural analog for module layout, helper factories, import style, fixture usage.
    - `tests/translate/test_analyze.py` lines 1–55 — AsyncMock LLM pattern + _make_settings helper shape.
    - `.planning/phases/06-relationship-evolution-self-review/06-PATTERNS.md` §tests/bible/test_relationship_events.py — copy-ready imports, helper factories, xfail stub pattern.
    - `.planning/phases/06-relationship-evolution-self-review/06-VALIDATION.md` §Per-Task Verification Map — the exact test node IDs (BIBLE-07-A, B, F rows).
  </read_first>

  <behavior>
    - test_relationship_event_written_to_db (BIBLE-07-A): xfail stub — imports `record_relationship_event` from `trezarr.bible.store` and `RelationshipEventDTO` from `trezarr.bible.dto`; calls `record_relationship_event` with a valid series_id/character_a_id/character_b_id/episode_marker; asserts the returned DTO has `character_a_id` and `character_b_id` matching the inputs.
    - test_load_series_bible_includes_events (BIBLE-07-B): xfail stub — after writing a relationship_event row, calls `load_series_bible` and asserts the returned `SeriesBibleDTO.relationship_events` list has length >= 1.
    - test_name_matching_case_insensitive (BIBLE-07-F): xfail stub — imports `merge_bible_analysis`; feeds it a `BibleAnalysis` with a `RelationshipEventInference` whose `character_a_name` differs in casing from the stored character name; asserts a row is still written (not silently skipped).
  </behavior>

  <action>
    1. Read `tests/bible/conftest.py`. If it does not exist or does not have `from tests.db.conftest import db_engine, session_factory`, create/extend it with exactly that re-export (PATTERNS.md shared pattern "DB fixture re-export via conftest.py"). Add `# noqa: F401` to suppress unused-import lint warnings.

    2. Write `tests/bible/test_relationship_events.py` with:
       - Module docstring listing BIBLE-07-A, B, F coverage.
       - `from __future__ import annotations`
       - Standard imports: `pytest`, `from unittest.mock import AsyncMock`
       - Production imports inside each xfail stub (or at module level with try/except if needed) — `record_relationship_event` from `trezarr.bible.store`; `RelationshipEventDTO` from `trezarr.bible.dto`; `load_series_bible` from `trezarr.bible.store`; `merge_bible_analysis` from `trezarr.bible.analyze`.
       - Module-level helper factories `_create_series` and `_create_characters` (PATTERNS.md analog), using `get_or_create_series` and `upsert_character` from `trezarr.bible.store`.
       - Three async test functions, each decorated with `@pytest.mark.xfail(strict=False, raises=(ImportError, AssertionError, TypeError))` (per D-08 / Phase-5 precedent in STATE.md). Use `raises=(ImportError, AssertionError, TypeError)` — NOT just `ImportError` — because existing modules import cleanly (PATTERNS.md §Critical Implementation Notes #7).
       - Each test body: arrange (create series + characters via fixtures), act (call the function under test), assert (the specific behavior from 06-VALIDATION.md). Bodies can be minimal but must be syntactically valid and make at least one assertion.
       - `asyncio_mode = "auto"` is already set project-wide — no `@pytest.mark.asyncio` needed.
  </action>

  <verify>
    <automated>uv run pytest tests/bible/test_relationship_events.py -x -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/bible/test_relationship_events.py -x -q` exits 0 with 3 xfail results (not FAILED, not ERROR).
    - `uv run pytest tests/bible/test_relationship_events.py --collect-only -q` shows all three node IDs: `test_relationship_event_written_to_db`, `test_load_series_bible_includes_events`, `test_name_matching_case_insensitive`.
    - No `ImportError` at collection time (any import errors land inside the xfail stub body, not at module level).
    - `tests/bible/conftest.py` exists and contains the `db_engine`/`session_factory` re-export.
  </acceptance_criteria>

  <done>tests/bible/test_relationship_events.py created with 3 xfail stubs all XFAIL (not ERROR/FAILED). conftest.py present with fixture re-exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/translate/test_self_review.py (ENG-05-A, B, C, D stubs) and extend tests/translate/test_reconcile.py (BIBLE-07-C, D, E stubs)</name>
  <files>tests/translate/test_self_review.py, tests/translate/test_reconcile.py</files>

  <read_first>
    Read these files before writing anything:
    - `tests/translate/test_reconcile.py` lines 1–137 — the full existing module: current imports, all helper factories (`_make_bible`, `_make_character`, `_make_address_map_entry`, `_make_attribution`, `_make_settings`), and existing test functions. Know the EXACT signatures before adding to this file.
    - `tests/translate/conftest.py` — confirm it re-exports `session_factory` (the BIBLE-07-C/D/E stubs need it).
    - `tests/translate/test_analyze.py` lines 1–55 — AsyncMock LLM pattern, `_make_subdoc`, `_make_settings` shapes for test_self_review.py.
    - `.planning/phases/06-relationship-evolution-self-review/06-PATTERNS.md` §tests/translate/test_self_review.py and §tests/translate/test_reconcile.py — copy-ready helper snippets and xfail stub patterns.
    - `.planning/phases/06-relationship-evolution-self-review/06-VALIDATION.md` §Per-Task Verification Map — exact node IDs for ENG-05-A/B/C/D and BIBLE-07-C/D/E.
  </read_first>

  <behavior>
    test_self_review.py (ENG-05-A, B, C, D):
    - test_pass4_corrects_violation_before_gate (ENG-05-A): xfail stub — mocks `_review_batch` to return corrected Vietnamese text for a known pronoun violation; calls `translate_file` (or `_review_batch` directly for the unit slice); asserts the corrected text appears in the output before `validate_subdoc` runs.
    - test_pass4_failure_fallback_no_quarantine (ENG-05-B): xfail stub — mocks LLM to raise `Exception`; calls `_review_batch`; asserts return value is `None` (not raised, not quarantine-triggering).
    - test_pass4_disabled_skips_review (ENG-05-C): xfail stub — sets `enable_self_review=False` in settings; asserts `_review_batch` is never called (or `translated_doc` is unchanged).
    - test_sentinel_failure_fallback_per_batch (ENG-05-D): xfail stub — mocks LLM to return a response with missing sentinel tokens; asserts `_review_batch` returns `None` for that batch.

    test_reconcile.py extensions (BIBLE-07-C, D, E):
    - test_transition_authorizes_terms_change: xfail stub — builds a bible with a relationship_event for pair (A, B) at current episode; calls `reconcile_attributions`; asserts the pair in resolved_map uses the suggested new terms.
    - test_lock_beats_transition: xfail stub — builds a bible with a locked address_map entry AND a relationship_event for the same pair; calls `reconcile_attributions`; asserts the locked terms win.
    - test_no_transition_no_survivors_safe_default: xfail stub — builds a bible with NO relationship_event and NO high-confidence attributions; calls `reconcile_attributions`; asserts the pair falls back to the safe default (tôi + anh/chị/bạn).
  </behavior>

  <action>
    PART A — tests/translate/test_self_review.py (new file):

    1. Module docstring listing ENG-05-A, B, C, D coverage.
    2. `from __future__ import annotations`, standard pytest + AsyncMock imports.
    3. Production imports from `trezarr.translate.engine` (import `_review_batch`, `build_review_prompt` inside each stub body to avoid collection-time ImportError if symbols not yet defined).
    4. `_make_settings(**kwargs)` helper that constructs a `TrezarrSettings` with `llm_api_key="test-key"`, `enable_self_review=True`, `self_review_context_lines_k=0`, `self_review_max_cues_per_batch=10` as defaults (PATTERNS.md §test_self_review.py helper).
    5. `_make_subdoc(texts)` helper producing a minimal `SubDoc` from a list of text strings (mirror from `test_analyze.py` or `test_engine.py` — read the existing analog to copy exactly).
    6. Four async test functions each with `@pytest.mark.xfail(strict=False, raises=(ImportError, AssertionError, TypeError))`. Stubs must make at least one assertion (even `assert True` is not acceptable — must assert the tested behavior, even if the import fails first).

    PART B — tests/translate/test_reconcile.py (extend existing):

    1. Read the full file first (mandatory). Append AFTER the last existing test function.
    2. Add `_make_relationship_event(id, series_id, char_a_id, char_b_id, episode_marker, suggested_self_term=None, suggested_address_term=None)` helper using `types.SimpleNamespace` (PATTERNS.md §test_reconcile.py extension).
    3. Extend `_make_bible` helper to accept an optional `relationship_events=None` parameter and include it in the SimpleNamespace. If `_make_bible` cannot be modified without breaking existing tests, create a `_make_bible_with_events` variant instead.
    4. Three async xfail stubs for BIBLE-07-C, D, E. Each imports `reconcile_attributions` from `trezarr.translate.reconcile` inside the stub body (or at module level if already imported). Each passes `episode_key` and a `bible` with/without `relationship_events`. Each makes a targeted assertion about `resolved_map` content.
    5. CRITICAL: Do NOT remove or modify any existing test functions or helper factories in `test_reconcile.py`. Addition-only.
  </action>

  <verify>
    <automated>uv run pytest tests/translate/test_self_review.py tests/translate/test_reconcile.py -x -q 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tests/translate/test_self_review.py -x -q` exits 0 with 4 xfail results.
    - `uv run pytest tests/translate/test_reconcile.py -x -q` exits 0; ALL pre-existing reconcile tests still pass (not just xfail); the 3 new stubs show as xfail.
    - `uv run pytest tests/translate/test_self_review.py tests/translate/test_reconcile.py --collect-only -q` shows all 7 new node IDs: `test_pass4_corrects_violation_before_gate`, `test_pass4_failure_fallback_no_quarantine`, `test_pass4_disabled_skips_review`, `test_sentinel_failure_fallback_per_batch`, `test_transition_authorizes_terms_change`, `test_lock_beats_transition`, `test_no_transition_no_survivors_safe_default`.
    - `uv run pytest -x -q` (full suite) exits 0 — all 226 pre-existing tests green plus the 7 new xfail stubs.
  </acceptance_criteria>

  <done>7 xfail stubs across 2 files all XFAIL (not ERROR/FAILED). Full 226-test suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file imports → production modules | Test collection imports production code; malformed imports at module level cause ERROR (not XFAIL). All Phase-6-specific imports must be inside xfail stub bodies or wrapped to avoid collection-time errors on unimplemented symbols. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | xfail stub bodies | accept | Stubs are test-only scaffolding; they touch no production data; risk is zero |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Wave 0 — test files only, no install tasks |
</threat_model>

<verification>
Full suite stays green:
`uv run pytest -x -q` exits 0

Phase-6 test files collected and all xfail:
`uv run pytest tests/bible/test_relationship_events.py tests/translate/test_self_review.py tests/translate/test_reconcile.py -x -q` exits 0 with 10 xfail results (3 + 4 + 3 new stubs)

All 10 node IDs from 06-VALIDATION.md are present:
`uv run pytest tests/bible/test_relationship_events.py tests/translate/test_self_review.py tests/translate/test_reconcile.py --collect-only -q`
</verification>

<success_criteria>
- All 10 Phase-6 test node IDs listed in 06-VALIDATION.md exist and are collected by pytest
- All stubs show XFAIL (not ERROR, not FAILED)
- Full suite (226 + new stubs) exits 0
- `tests/bible/conftest.py` re-exports db_engine and session_factory
</success_criteria>

<output>
Create `.planning/phases/06-relationship-evolution-self-review/06-01-SUMMARY.md` when done.
</output>
