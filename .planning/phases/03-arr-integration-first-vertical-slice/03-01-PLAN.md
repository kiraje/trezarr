---
phase: "03"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/arr/__init__.py
  - tests/arr/test_arr_discovery.py
  - tests/discover/__init__.py
  - tests/discover/test_scan.py
  - tests/test_paths.py
  - tests/test_cli.py
  - tests/output/test_write.py
  - tests/config/test_settings.py
autonomous: true
requirements:
  - INTG-01
  - INTG-03
  - INTG-04
  - AUTO-01
  - AUTO-03
  - AUTO-04

must_haves:
  truths:
    - "All Phase-3 test stubs exist and are RED (xfail strict=False) before any implementation module is written"
    - "pyarr 6.6.x and pytest-httpx 0.36.2 are installed and importable"
    - "The RED suite passes (xfail stubs pass as expected failures; the suite does not error)"
  artifacts:
    - path: "tests/arr/test_arr_discovery.py"
      provides: "RED stubs for INTG-01 Sonarr+Radarr discovery"
      contains: "test_sonarr_discovers_monitored_series"
    - path: "tests/discover/test_scan.py"
      provides: "RED stubs for AUTO-01, AUTO-03, AUTO-04 gap/scan"
      contains: "test_gap_detection"
    - path: "tests/test_paths.py"
      provides: "RED stubs for INTG-03 path mapping + probe + traversal guard"
      contains: "test_path_mapping_replaces_prefix"
    - path: "tests/test_cli.py"
      provides: "RED stubs for D-30 batch-run semantics + exit code"
      contains: "test_batch_run_continues"
    - path: "tests/output/test_write.py"
      provides: "Extended with apply_permissions RED stub for INTG-04"
      contains: "test_apply_permissions"
  key_links:
    - from: "tests/arr/test_arr_discovery.py"
      to: "trezarr/arr/sonarr.py"
      via: "pytest.importorskip"
      pattern: "importorskip.*trezarr.arr.sonarr"
    - from: "tests/discover/test_scan.py"
      to: "trezarr/discover/scan.py"
      via: "pytest.importorskip"
      pattern: "importorskip.*trezarr.discover.scan"
---

## Phase Goal

**As a** self-hosted media user, **I want to** have Trezarr discover real episodes from Sonarr/Radarr and translate them to Vietnamese sidecars automatically, **so that** my media library gets consistent Vietnamese subtitles without manual work.

<objective>
Install the two new dependencies (pyarr, pytest-httpx) and lay down the complete RED test surface for every Phase-3 module before any implementation is written. This is the Nyquist Wave-0 discipline the project already uses (Plan 02-01 established this pattern): the test stubs drive the implementation contract and prevent test-after-the-fact drift.

Purpose: Establish RED-first discipline; unblock all implementation waves; ensure pyarr is importable before Wave-1 code references it.
Output: pyproject.toml updated, uv.lock refreshed, 6 test files (2 new packages + 4 files), 1 extended test file — all stubs marked xfail(strict=False) and confirmed RED.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-RESEARCH.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-PATTERNS.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install pyarr + pytest-httpx; create test package skeletons</name>
  <files>pyproject.toml, uv.lock, tests/arr/__init__.py, tests/discover/__init__.py</files>
  <read_first>
    - pyproject.toml (full file — add pyarr to [project.dependencies] and pytest-httpx to [dependency-groups].dev exactly as existing deps are formatted; confirm Python >=3.12 constraint already present)
    - tests/output/__init__.py (exact empty-file pattern to replicate for tests/arr/__init__.py and tests/discover/__init__.py)
    - 03-RESEARCH.md §"Package Legitimacy Audit" (pyarr [OK], pytest-httpx [OK] — both approved)
    - 03-RESEARCH.md §"Standard Stack" (pyarr==6.6.0, pytest-httpx==0.36.2)
  </read_first>
  <behavior>
    - After install: `python -c "import pyarr; from pyarr import Sonarr, Radarr; print('ok')"` exits 0
    - After install: `python -c "import pytest_httpx; print('ok')"` exits 0
    - tests/arr/__init__.py and tests/discover/__init__.py are empty files (match tests/output/__init__.py pattern)
  </behavior>
  <action>
    Run `uv add pyarr` to add pyarr 6.6.x as a runtime dep and `uv add --dev pytest-httpx` to add pytest-httpx 0.36.2 as a dev dep. Both commands update pyproject.toml and regenerate uv.lock automatically.

    Create tests/arr/__init__.py and tests/discover/__init__.py as empty files (same as tests/output/__init__.py — just a package marker with no content).

    Do NOT add aiopyarr (a different, unmaintained package). Import must be `from pyarr import Sonarr, Radarr` — NOT `SonarrAPI`/`RadarrAPI` (pyarr 6.x only — per 03-RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>uv run python -c "from pyarr import Sonarr, Radarr; import pytest_httpx; print('deps ok')"</automated>
  </verify>
  <done>pyarr importable as `from pyarr import Sonarr, Radarr`; pytest-httpx importable; both __init__.py files exist and are empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED test stubs for all Phase-3 modules</name>
  <files>
    tests/arr/test_arr_discovery.py,
    tests/discover/test_scan.py,
    tests/test_paths.py,
    tests/test_cli.py,
    tests/output/test_write.py,
    tests/config/test_settings.py
  </files>
  <read_first>
    - tests/output/test_write.py (full file — the template: xfail(strict=False) stub pattern, pytest.importorskip, tmp_path, _make_minimal_doc helper; line numbers per 03-PATTERNS.md)
    - tests/output/test_ledger.py (full file — test_foreign_srt_not_clobbered pattern for gap detection stubs; lines 93-117 per 03-PATTERNS.md)
    - tests/translate/test_engine.py (full file — per-item exception pattern for test_cli stubs)
    - tests/config/test_settings.py (full file — env_override, api_key_not_logged, yaml_load patterns; lines per 03-PATTERNS.md)
    - tests/llm/test_client_tiers.py (deferred import pattern per 03-PATTERNS.md)
    - 03-RESEARCH.md §"Validation Architecture / Phase Requirements → Test Map" (all 12 test behaviors to stub)
    - 03-PATTERNS.md §"Test Pattern Assignments" (every test pattern assignment)
    - 03-CONTEXT.md §"Implementation Decisions" (D-21 through D-30 — stub tests must cite these)
  </read_first>
  <behavior>
    - tests/arr/test_arr_discovery.py stubs: test_sonarr_discovers_monitored_series, test_sonarr_filters_unmonitored, test_radarr_discovers_movies, test_radarr_skips_missing_file — all marked @pytest.mark.xfail(strict=False)
    - tests/discover/test_scan.py stubs: test_find_source_sub_priority, test_find_source_sub_none_when_absent, test_gap_detection_no_source, test_gap_detection_eligible_new, test_gap_detection_foreign_vi_skip, test_idempotency_skip_unchanged, test_idempotency_retranslate_on_change, test_foreign_vi (8 stubs)
    - tests/test_paths.py stubs: test_path_mapping_replaces_prefix, test_path_mapping_no_match_passthrough, test_path_mapping_strips_trailing_slash, test_longest_prefix_wins, test_traversal_guard_raises_outside_roots, test_traversal_guard_allows_inside, test_probe_unreadable_root_exits, test_probe_readable_root_passes (8 stubs)
    - tests/test_cli.py stubs: test_batch_run_continues_past_failure, test_batch_exit_zero_all_success, test_batch_exit_nonzero_any_failure, test_batch_summary_printed (4 stubs)
    - tests/output/test_write.py extension: add test_apply_permissions_calls_chown_chmod, test_apply_permissions_chown_permission_error_continues (2 stubs appended after existing tests)
    - tests/config/test_settings.py extension: add test_sonarr_host_env_override, test_sonarr_api_key_not_in_repr, test_path_mappings_yaml_load (3 stubs appended)
    - All stubs use pytest.importorskip("trezarr.arr.sonarr") etc. with deferred imports inside function body — identical to test_write.py line 39 pattern
    - `uv run pytest tests/ -x -q` passes: existing 63 pass, new stubs are xfail (not error)
  </behavior>
  <action>
    Write six test files following the exact stub patterns established in Phase 2 (03-PATTERNS.md §"Test Pattern Assignments").

    CRITICAL CONVENTIONS (from 03-PATTERNS.md §"Shared Patterns"):
    - Every new test file starts with `from __future__ import annotations`
    - Import deferral: all `trezarr.*` imports inside each test function body via `pytest.importorskip`
    - Stub pattern: `@pytest.mark.xfail(strict=False) / def test_X(): pytest.importorskip(...)`
    - No `@pytest.mark.asyncio` decorator — `asyncio_mode = "auto"` is set in pyproject.toml
    - For tests using `httpx_mock` fixture (arr discovery): sync `def` tests, not `async def` (sync Sonarr client per 03-RESEARCH.md Pattern 1)
    - For CLI integration tests: `async def` (the CLI calls asyncio.run internally but tests call `_run_once()` directly)

    tests/arr/test_arr_discovery.py: Use `httpx_mock` fixture (from pytest-httpx). Mock `http://HOST:8989/api/v3/series` and `http://HOST:8989/api/v3/episodefile?seriesId=1` for Sonarr stubs; `http://HOST:7878/api/v3/movie` for Radarr. All stubs are xfail.

    tests/discover/test_scan.py: Use `tmp_path` to create stub SRT files. Define a `_make_minimal_ledger(tmp_path, entries=None)` helper that creates a `Ledger` from `tmp_path / "processed_files.json"` with optional pre-populated entries. Mirror the `_make_minimal_doc` helper pattern from test_write.py.

    tests/test_paths.py: Parametrize `test_path_mapping_replaces_prefix` and siblings with `@pytest.mark.parametrize` over `(api_path, remote, local, expected)` tuples. Use `tmp_path` for the readability probe tests. Use `pytest.raises(SystemExit)` for probe failure test.

    tests/test_cli.py: Patch `trezarr.cli.discover_sonarr_items`, `trezarr.cli.discover_radarr_items`, and `trezarr.cli.translate_file` with `unittest.mock.AsyncMock`. Call `_run_once(config_path=None)` directly (not via argparse). Use `settings_factory` conftest fixture with arr hosts disabled.

    tests/output/test_write.py (extend — append only, do NOT modify existing tests): Add `test_apply_permissions_calls_chown_chmod` and `test_apply_permissions_chown_permission_error_continues` using `unittest.mock.patch("os.chown", side_effect=PermissionError)` pattern. Deferred import of `apply_permissions` via `pytest.importorskip("trezarr.output.write")`.

    tests/config/test_settings.py (extend — append only): Add three stubs following the existing `monkeypatch.setenv` / `TrezarrSettings()` / `assert` pattern.

    After writing all stubs, run the full test suite to confirm existing 63 tests still pass and all new stubs are xfail (not error-fail).
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Full suite shows 63 passed + N xfailed (N = count of new stubs); zero errors; zero unexpected passes. All stubs importable (no ImportError at collection time).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev deps → CI | pytest-httpx installed as dev dep; not present in production image |
| test code → src | Stubs import production modules via pytest.importorskip; no reverse dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | pyarr package install | mitigate | slopcheck [OK] verified in 03-RESEARCH.md Package Legitimacy Audit; package is approved |
| T-03-01-02 | Tampering | pytest-httpx package install | mitigate | slopcheck [OK] verified; 7.5M/month downloads; legitimate source repo |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck confirmed [OK] for all packages; no [ASSUMED] or [SUS] packages — no blocking checkpoint required |
</threat_model>

<verification>
uv run pytest tests/ -x -q — full suite passes (63 old + N xfail new stubs)
uv run python -c "from pyarr import Sonarr, Radarr; print('ok')" — exits 0
uv run python -c "import pytest_httpx; print('ok')" — exits 0
</verification>

<success_criteria>
- pyarr 6.6.x installed and importable as `from pyarr import Sonarr, Radarr`
- pytest-httpx 0.36.2 installed as dev dep
- tests/arr/__init__.py, tests/discover/__init__.py exist (empty)
- All RED stubs exist across 6 test files; `uv run pytest tests/ -x -q` shows xfailed stubs, zero errors
- No existing Phase-1/2 test regressions
</success_criteria>

<output>
Create `.planning/phases/03-arr-integration-first-vertical-slice/03-01-SUMMARY.md` when done.
</output>
