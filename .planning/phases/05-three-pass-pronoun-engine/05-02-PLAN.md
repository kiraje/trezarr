---
phase: 05-three-pass-pronoun-engine
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - trezarr/bible/models.py
  - trezarr/bible/dto.py
  - trezarr/bible/store.py
  - trezarr/config.py
autonomous: true
requirements: [BIBLE-03, ENG-04]

must_haves:
  truths:
    - "Per D-42, Series.address_maps relationship and AddressMap.series back-reference exist in models.py, enabling selectinload(Series.address_maps) in store.py"
    - "Per D-39, AddressMapDTO is defined in dto.py following CharacterDTO/TermDTO ConfigDict(from_attributes=True) pattern"
    - "Per D-39, SeriesBibleDTO gains address_map: list[AddressMapDTO] = [] field and load_series_bible populates it via selectinload"
    - "Per D-42/BIBLE-03, upsert_address_pair and _upsert_address_pair_in_session exist in store.py; identity key is (series_id, speaker_character_id, addressee_character_id); locked_fields respected (lock precedence per D-34)"
    - "Per D-42, load_address_map(session_factory, series_id) -> list[AddressMapDTO] exists in store.py"
    - "Per D-50, Phase-5 TrezarrSettings fields exist in config.py under a '── Phase 5: Three-Pass Pronoun Engine (D-40…D-50)' comment header: enable_pass1_analysis, pass1_max_cues_per_chunk, enable_attribution, attribute_context_lines_k, attribute_max_cues_per_batch, pronoun_confidence_threshold, pronoun_safe_default"
    - "tests/bible/test_address_map.py::test_upsert_address_pair turns GREEN (no longer xfail)"
    - "tests/bible/test_address_map.py::test_locked_pair_not_overwritten turns GREEN (locked row survives upsert)"
    - "No Alembic migration created — address_map table already exists from Phase-4 baseline migration"
  artifacts:
    - path: "trezarr/bible/models.py"
      provides: "Series.address_maps relationship + AddressMap.series back-reference"
      contains: "address_maps: Mapped[list"
    - path: "trezarr/bible/dto.py"
      provides: "AddressMapDTO + SeriesBibleDTO.address_map field"
      contains: "class AddressMapDTO"
    - path: "trezarr/bible/store.py"
      provides: "upsert_address_pair, _upsert_address_pair_in_session, load_address_map, MERGEABLE_FIELDS[address_map], selectinload(Series.address_maps) in load_series_bible"
      contains: "async def upsert_address_pair"
    - path: "trezarr/config.py"
      provides: "Phase-5 TrezarrSettings fields (D-50)"
      contains: "pronoun_confidence_threshold"
  key_links:
    - from: "trezarr/bible/store.py (load_series_bible)"
      to: "trezarr/bible/models.py (Series.address_maps)"
      via: "selectinload(Series.address_maps)"
      pattern: "selectinload.*address_maps"
    - from: "trezarr/bible/store.py (upsert_address_pair)"
      to: "trezarr/bible/models.py (AddressMap)"
      via: "_upsert_address_pair_in_session"
      pattern: "upsert_address_pair"
    - from: "trezarr/bible/dto.py (SeriesBibleDTO)"
      to: "trezarr/bible/dto.py (AddressMapDTO)"
      via: "address_map: list[AddressMapDTO] = []"
      pattern: "address_map.*AddressMapDTO"
---

<objective>
Build the Address Map store foundation: ORM relationship, DTO, store functions, and Phase-5 config fields. This plan delivers the persistence layer that Pass 1 writes to (via upsert_address_pair) and Pass 3 reads from (via load_series_bible which now includes address_map). No migration needed — the address_map table was created empty by Phase-4's baseline migration (D-31).

Purpose: Plans 04 (Pass 1 analyze.py) and 06 (engine.py wiring) both depend on these store functions and DTOs. This is the foundational slice that makes BIBLE-03 addressable.

Output: AddressMapDTO in dto.py, Series.address_maps relationship in models.py, upsert_address_pair + load_address_map + selectinload extension in store.py, Phase-5 settings in config.py. The two address-map test stubs from Plan 01 turn GREEN.
</objective>

<execution_context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/PROJECT.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-RESEARCH.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-PATTERNS.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ORM relationship + DTO extension (models.py + dto.py)</name>

  <read_first>
  - trezarr/bible/models.py (full file — particularly lines 87-92 Series.characters/terms relationships and lines 153-179 AddressMap model for back-reference placement; and the Series class definition to know where to insert address_maps)
  - trezarr/bible/dto.py (full file — CharacterDTO lines 74-96, TermDTO lines 98-118 for ConfigDict pattern; SeriesBibleDTO lines 151-186 to know where to add address_map field)
  - .planning/phases/05-three-pass-pronoun-engine/05-PATTERNS.md (§ trezarr/bible/models.py — ADD Series.address_maps relationship; § trezarr/bible/dto.py — ADD AddressMapDTO)
  </read_first>

  <behavior>
  - models.py: Series.address_maps relationship (Mapped[list["AddressMap"]], back_populates="series", cascade="all, delete-orphan") mirrors Series.characters exactly. AddressMap.series back-reference (Mapped["Series"], back_populates="address_maps") added to AddressMap model. No new columns, no Alembic migration — relationship is ORM-only metadata.
  - dto.py: AddressMapDTO class with ConfigDict(from_attributes=True), fields: id (int), series_id (int), speaker_character_id (int), addressee_character_id (int), self_term (str | None = None), address_term (str | None = None), valid_from_episode (str | None = None), locked_fields (list[str] = []). SeriesBibleDTO gains address_map: list[AddressMapDTO] = [] field.
  </behavior>

  <action>
  In trezarr/bible/models.py: inside the Series class body (after the `terms: Mapped[list["TermDictionary"]] = relationship(...)` line, per the analog at lines 87-92), add:

  `address_maps: Mapped[list["AddressMap"]] = relationship(back_populates="series", cascade="all, delete-orphan")`

  Inside the AddressMap class body (per lines 153-179), add a `series` back-reference after the existing FK columns:

  `series: Mapped["Series"] = relationship(back_populates="address_maps")`

  CRITICAL: Do not add a Alembic migration. The address_map table and FK already exist from Phase-4 baseline migration. Only ORM relationship metadata changes.

  In trezarr/bible/dto.py: add AddressMapDTO class following CharacterDTO (lines 74-96) exactly — same ConfigDict(from_attributes=True), docstring format, field order matching the AddressMap ORM model columns. Place it after TermDTO. The `locked_fields: list[str] = []` default is safe in Pydantic v2 (D-39 / dto.py:14 note).

  Also in SeriesBibleDTO (lines 151-186): add `address_map: list[AddressMapDTO] = []` field. The field must be placed after existing `terms: list[TermDTO] = []` to preserve existing constructor call order.

  No other changes to models.py or dto.py.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/bible/test_models.py tests/db/test_migrations.py -x -q 2>&1 | tail -10</automated>
  </verify>

  <done>
  - grep -n "address_maps" trezarr/bible/models.py returns the Mapped[list] relationship line inside Series and the back-ref inside AddressMap
  - grep -n "class AddressMapDTO" trezarr/bible/dto.py returns a hit
  - grep -n "address_map.*AddressMapDTO" trezarr/bible/dto.py returns the SeriesBibleDTO field
  - Existing tests/bible/test_models.py and tests/db/test_migrations.py pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Address Map store functions + Phase-5 config fields (store.py + config.py), turn test_address_map GREEN</name>

  <read_first>
  - trezarr/bible/store.py (full file — especially: MERGEABLE_FIELDS dict lines ~242; VALID_SOURCES lines ~245; upsert_character lines 631-693; _upsert_character_in_session lines 382-476; load_series_bible lines 194-225 for selectinload extension; imports block lines 65-79)
  - trezarr/bible/models.py (just updated — AddressMap columns: id, series_id, speaker_character_id, addressee_character_id, self_term, address_term, valid_from_episode, locked_fields)
  - trezarr/bible/dto.py (just updated — AddressMapDTO and SeriesBibleDTO.address_map field)
  - trezarr/config.py (lines 66-127 — Phase-3/4 field groups for comment-header pattern; existing field types for convention)
  - .planning/phases/05-three-pass-pronoun-engine/05-PATTERNS.md (§ trezarr/bible/store.py — full store extension snippets; § trezarr/config.py — Phase-5 block)
  - .planning/phases/05-three-pass-pronoun-engine/05-RESEARCH.md (Key Pattern 2 upsert_address_pair; Key Pattern 8 settings fields)
  - tests/bible/test_address_map.py (the two RED stubs to turn GREEN — read their current body to know exactly what to assert)
  </read_first>

  <behavior>
  - store.py MERGEABLE_FIELDS: add "address_map": frozenset({"self_term", "address_term", "valid_from_episode"}) to the dict
  - store.py imports: add AddressMap to the models import line; add AddressMapDTO to the dto import line
  - _upsert_address_pair_in_session: async, takes session + *, series_id, speaker_character_id, addressee_character_id, self_term, address_term, valid_from_episode, episode_key, source. Identity key SELECT by (series_id, speaker_character_id, addressee_character_id). On INSERT: creates AddressMap row + BibleEvent(entity_type="address_map"). On UPDATE: skips locked fields (fields in locked_fields column). Returns (AddressMap, list[BibleEvent]). MUST be called from inside an already-open session.begin() block — does NOT open its own transaction (D-32).
  - upsert_address_pair: public async wrapper; opens `async with session_factory() as session: async with session.begin():` then calls _upsert_address_pair_in_session. Returns (AddressMapDTO, list[BibleEventDTO]) via model_validate(row, from_attributes=True).
  - load_address_map: async, SELECT all AddressMap rows WHERE series_id == series_id, return list[AddressMapDTO].
  - load_series_bible: extend the selectinload() options to add selectinload(Series.address_maps). Add `address_map=[AddressMapDTO.model_validate(a, from_attributes=True) for a in row.address_maps]` to the SeriesBibleDTO constructor call.
  - config.py: add Phase-5 fields block after Phase-4 block. Fields: enable_pass1_analysis (bool = True), pass1_max_cues_per_chunk (int = 400), enable_attribution (bool = True), attribute_context_lines_k (int = 8), attribute_max_cues_per_batch (int = 30), pronoun_confidence_threshold (str = "medium"), pronoun_safe_default (tuple[str, str] | None = Field(default=None)). Comment header: "── Phase 5: Three-Pass Pronoun Engine (D-40…D-50) ──────────────────────────".

  CRITICAL Pitfall C/G: Do NOT route AddressMap writes through merge_inferred() — its isinstance chain does not handle AddressMapDTO (verified in PATTERNS.md § Pitfall G). Write a self-contained _upsert_address_pair_in_session helper.

  CRITICAL Pitfall D (D-39): store.py already imports SQLAlchemy — that is correct. The constraint is that no other module (analyze.py, attribute.py, reconcile.py) imports SQLAlchemy.

  CRITICAL Assumption A8: load_series_bible selectinload(Series.address_maps) requires the ORM relationship from Task 1. If Task 1 is not done, this call will raise AttributeError at import.

  test_upsert_address_pair real body (replace stub): use in-memory SQLite session_factory (from conftest); call upsert_address_pair(session_factory, series_id=1, speaker_character_id=10, addressee_character_id=20, self_term="anh", address_term="em", valid_from_episode="S01E01", episode_key="S01E01", source="inference"); assert dto.self_term == "anh" and dto.address_term == "em".

  test_locked_pair_not_overwritten real body: seed a row with locked_fields=["self_term"]; call upsert_address_pair again with different self_term; assert stored self_term is still the original value.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/bible/test_address_map.py tests/bible/ -x -q 2>&1 | tail -15</automated>
  </verify>

  <done>
  - tests/bible/test_address_map.py::test_upsert_address_pair PASSED (not xfail)
  - tests/bible/test_address_map.py::test_locked_pair_not_overwritten PASSED (not xfail)
  - grep -n "async def upsert_address_pair" trezarr/bible/store.py returns a hit
  - grep -n "selectinload.*address_maps" trezarr/bible/store.py returns a hit
  - grep -n "pronoun_confidence_threshold" trezarr/config.py returns a hit
  - Full bible test suite passes: `uv run pytest tests/bible/ -x -q` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM inference → Address Map INSERT | Inferred self_term/address_term values from LLM written to address_map table via upsert_address_pair |
| locked_fields enforcement | Prevents LLM inferences from overwriting human-locked pairs (D-34) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | _upsert_address_pair_in_session locked_fields bypass | mitigate | Implementation must check `field in locked_fields` before overwriting (mirrors _upsert_character_in_session pattern at store.py lines 429-430). Test: test_locked_pair_not_overwritten asserts this invariant. |
| T-05-02-02 | Tampering | SQL injection via speaker_character_id / addressee_character_id | mitigate | All writes use SQLAlchemy ORM parameterized WHERE/VALUES (select(AddressMap).where(...) with literal int IDs from Pydantic-validated upstream). No string-built SQL. |
| T-05-02-03 | Tampering | pronoun_safe_default env var injection with crafted Vietnamese string | accept | Pydantic validates tuple[str,str]|None shape. Content validation out of v1 scope — operator-supplied config. Residual risk: LOW. |
| T-05-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan. Residual risk: LOW. |

Residual risk after mitigations: LOW. No auth surface; no user-controlled input reaches SQL outside ORM parameterization.
</threat_model>

<verification>
Full suite check:
`uv run pytest tests/bible/ tests/db/ -x -q` exits 0 — all existing bible and DB tests pass plus new address_map tests.
BIBLE-03 store boundary check:
`uv run pytest tests/bible/test_address_map.py -v` shows 2 PASSED (not xfail).
Config field check:
`uv run python -c "from trezarr.config import TrezarrSettings; s = TrezarrSettings(); print(s.pronoun_confidence_threshold, s.enable_pass1_analysis)"` prints "medium True".
</verification>

<success_criteria>
- BIBLE-03: upsert_address_pair inserts directed pair rows with lock precedence respected
- ENG-04 (partial): SeriesBibleDTO now carries address_map list from load_series_bible (foundation for Pass 1 reload after merge)
- 2 address-map tests GREEN; all existing bible tests still pass
- Phase-5 TrezarrSettings fields importable with correct defaults
</success_criteria>

<output>
Create `.planning/phases/05-three-pass-pronoun-engine/05-02-SUMMARY.md` when done.
</output>
