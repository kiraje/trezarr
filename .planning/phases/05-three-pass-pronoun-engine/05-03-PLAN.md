---
phase: 05-three-pass-pronoun-engine
plan: 03
type: execute
wave: 1
depends_on: [05-01, 05-02]
files_modified:
  - trezarr/translate/reconcile.py
  - tests/translate/conftest.py
autonomous: true
requirements: [PRON-02, PRON-03]

must_haves:
  truths:
    - "Per D-44, reconcile_attributions(flat_attributions, bible, session_factory, series_id, episode_key, settings) -> dict[tuple[int,int], tuple[str,str]] implements: first-high-confidence-wins, frequency tie-break on MEDIUM, locked Address Map respected, reciprocal coherence check via KINSHIP_RECIPROCAL"
    - "Per D-45, get_safe_default(addressee_gender, settings) -> tuple[str,str] returns settings.pronoun_safe_default if set, otherwise selects from SAFE_DEFAULT_* constants by gender"
    - "KINSHIP_RECIPROCAL dict is hardcoded in reconcile.py covering the common Vietnamese kinship pairs from RESEARCH.md Key Pattern 5"
    - "No SQLAlchemy imports in reconcile.py — TYPE_CHECKING-only import of async_sessionmaker (D-39, Pitfall D)"
    - "No asyncio.Semaphore in reconcile.py (D-06, Pitfall A)"
    - "tests/translate/test_reconcile.py all four stubs turn GREEN: test_low_confidence_safe_default, test_high_confidence_uses_address_map, test_reciprocal_coherence, test_below_threshold_ignores_address_map"
  artifacts:
    - path: "trezarr/translate/reconcile.py"
      provides: "KINSHIP_RECIPROCAL dict, SAFE_DEFAULT_* constants, reconcile_attributions(), get_safe_default()"
      contains: "KINSHIP_RECIPROCAL"
  key_links:
    - from: "trezarr/translate/reconcile.py (reconcile_attributions)"
      to: "trezarr/bible/store.py (upsert_address_pair)"
      via: "async call to persist reconciled pairs"
      pattern: "upsert_address_pair"
    - from: "trezarr/translate/reconcile.py (get_safe_default)"
      to: "trezarr/config.py (pronoun_safe_default)"
      via: "settings.pronoun_safe_default check"
      pattern: "pronoun_safe_default"
---

<objective>
Implement the deterministic reconciliation module (trezarr/translate/reconcile.py) as a self-contained utility: the Vietnamese kinship-pair reciprocal table, the safe-default selection logic (D-45), and the per-episode pronoun resolution algorithm (D-44). This plan depends on Plan 02 (store foundation) because reconcile.py imports `upsert_address_pair` from `trezarr.bible.store` at module level — a module-level import that does not exist until Plan 02 ships. Running Plan 03 in parallel with Plan 02 would cause an ImportError failing all four test_reconcile stubs. The four reconcile test stubs from Plan 01 turn GREEN here.

Purpose: Plan 06 (engine wiring) needs reconcile_attributions and get_safe_default to orchestrate Pass 3. Implementing reconcile.py independently unblocks Plan 06 without waiting for analyze.py or attribute.py.

Output: reconcile.py with KINSHIP_RECIPROCAL, get_safe_default, reconcile_attributions; four test stubs GREEN.
</objective>

<execution_context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/PROJECT.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-RESEARCH.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-PATTERNS.md
@/Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr/.planning/phases/05-three-pass-pronoun-engine/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reconcile.py — KINSHIP_RECIPROCAL + get_safe_default + reconcile_attributions</name>

  <read_first>
  - trezarr/translate/batching.py (full — the structural analog for a pure-logic synchronous module: imports pattern, module-level constants, TYPE_CHECKING usage, no external I/O)
  - trezarr/translate/engine.py (lines 19-20 — the "No asyncio.Semaphore" module-level comment to copy verbatim to reconcile.py)
  - trezarr/bible/dto.py (SeriesBibleDTO.address_map field, CharacterDTO.gender field — used in get_safe_default and reconcile_attributions name→id lookup)
  - .planning/phases/05-three-pass-pronoun-engine/05-RESEARCH.md (Key Pattern 4 reconcile_attributions algorithm; Key Pattern 5 KINSHIP_RECIPROCAL table + SAFE_DEFAULT_* constants + get_safe_default function)
  - .planning/phases/05-three-pass-pronoun-engine/05-PATTERNS.md (§ trezarr/translate/reconcile.py — imports pattern, module-level constants, function signatures)
  - tests/translate/test_reconcile.py (current content — the four xfail stubs to turn GREEN; read their bodies to understand what assertions are needed)
  </read_first>

  <behavior>
  - KINSHIP_RECIPROCAL constant: dict[tuple[str,str], tuple[str,str]] with entries from RESEARCH.md Key Pattern 5: ("anh","em")→("em","anh"), ("em","anh")→("anh","em"), ("chị","em")→("em","chị"), ("em","chị")→("chị","em"), ("anh","anh")→("anh","anh"), ("chị","chị")→("chị","chị"), ("ông","cháu")→("cháu","ông"), ("bà","cháu")→("cháu","bà"), ("ông","con")→("con","ông"), ("bà","con")→("con","bà"), ("bố","con")→("con","bố"), ("mẹ","con")→("con","mẹ"), ("cha","con")→("con","cha"), ("tôi","bạn")→("bạn","tôi"), ("tôi","anh")→("anh","tôi"), ("tôi","chị")→("chị","tôi"), ("tôi","ông")→("ông","tôi"), ("tôi","bà")→("bà","tôi")
  - SAFE_DEFAULT_SELF = "tôi"; SAFE_DEFAULT_ADDRESS_MALE = "anh"; SAFE_DEFAULT_ADDRESS_FEMALE = "chị"; SAFE_DEFAULT_ADDRESS_NEUTRAL = "bạn"
  - get_safe_default(addressee_gender: str | None, settings: "TrezarrSettings") -> tuple[str, str]: if settings.pronoun_safe_default is not None return settings.pronoun_safe_default; else map gender "male"→SAFE_DEFAULT_ADDRESS_MALE, "female"→SAFE_DEFAULT_ADDRESS_FEMALE, else SAFE_DEFAULT_ADDRESS_NEUTRAL; return (SAFE_DEFAULT_SELF, address)
  - reconcile_attributions(flat_attributions, bible, session_factory, series_id, episode_key, settings) -> dict[tuple[int,int], tuple[str,str]]: (a) build name→char_id index from bible.characters; (b) for each attribution where speaker+addressee matched to IDs, collect by (speaker_id, addressee_id); (c) check existing Address Map for locked rows — return locked pair unchanged; (d) for unlocked, apply the threshold gate: collect all attributions for the ordered pair; filter to those with confidence >= pronoun_confidence_threshold (mapping threshold string "high"|"medium"|"low" to AttributionConfidence enum); if any survive the filter, pick the first HIGH-confidence one (first-seen wins per D-44), with frequency tie-break among MEDIUM if no HIGH survives; if NONE survive the filter (all attributions below threshold), use a LOCKED Address Map entry if present — otherwise SKIP an unlocked Address Map entry and fall through to get_safe_default; (e) call upsert_address_pair for each resolved pair; (f) reciprocal coherence — for each resolved (A→B, self_t, addr_t), look up KINSHIP_RECIPROCAL.get((self_t, addr_t)); if found and (B→A) not yet in resolved_map, call upsert_address_pair with the reciprocal (source="inference"); (g) return resolved_map dict.

  test_low_confidence_safe_default: construct list[LineAttribution] all confidence=LOW, call get_safe_default(None, settings), assert self_term == "tôi"
  test_high_confidence_uses_address_map: construct attribution confidence=HIGH for known pair; call reconcile_attributions with mocked bible + real session_factory (from conftest, Wave 1 dependency); assert resolved_map[(spk_id, addr_id)] == ("anh", "em")
  test_reciprocal_coherence: call reconcile_attributions with one HIGH-confidence A→B attribution; assert B→A row written via upsert_address_pair with ("em", "anh") from KINSHIP_RECIPROCAL
  test_below_threshold_ignores_address_map: threshold=HIGH, attribution confidence=MEDIUM; existing Address Map entry exists unlocked; assert resolved_map still returns safe default (not the Address Map pair) — because MEDIUM is below HIGH threshold
  </behavior>

  <action>
  Create trezarr/translate/reconcile.py. Header comment: "Deterministic pronoun-pair reconciliation for Phase 5 Three-Pass Engine (D-44, D-45)."

  Module-level comment (copy from engine.py pattern per 05-PATTERNS.md § Pattern 2): "No asyncio.Semaphore in this module. LLMClient._semaphore is the sole gate (D-06, Pitfall 1)."

  Imports section mirrors batching.py (lines 1-24 from PATTERNS.md):
  - `from __future__ import annotations`
  - `import logging`
  - `from collections import Counter`
  - `from typing import TYPE_CHECKING`
  - Under TYPE_CHECKING: `from trezarr.config import TrezarrSettings`, `from trezarr.bible.dto import SeriesBibleDTO, CharacterDTO, AddressMapDTO`, `from trezarr.translate.attribute import LineAttribution, AttributionConfidence`, `from sqlalchemy.ext.asyncio import async_sessionmaker`
  - CRITICAL D-39: SQLAlchemy is TYPE_CHECKING-only. Never import select/Session at module level in this file.
  - Runtime import (needed for upsert call): `from trezarr.bible.store import upsert_address_pair` — this is a trezarr.bible import, not a SQLAlchemy import, so it is permitted at module level.

  Define KINSHIP_RECIPROCAL dict with all 18 entries listed in the behavior section above (with inline comment "Standard Vietnamese kinship pronoun reciprocal pairs — extend as needed for niche terms").

  Define SAFE_DEFAULT_* four constants.

  Define get_safe_default(addressee_gender: str | None, settings: "TrezarrSettings") -> tuple[str, str] as described. This is a synchronous pure function.

  Define reconcile_attributions as async (it calls upsert_address_pair which is async). The confidence threshold comparison: `settings.pronoun_confidence_threshold` is a string "high"|"medium"|"low". Map to AttributionConfidence enum: HIGH wins if threshold=="high"; MEDIUM or HIGH wins if threshold=="medium"; any wins if threshold=="low". Per D-44: "first high-confidence wins" means within MEDIUM tier, tie-break by frequency (Counter over MEDIUM attributions for same pair, take most common self_term/address_term pair).

  For test_below_threshold_ignores_address_map: when threshold is "high" and the only attributions are MEDIUM confidence, the filter step produces zero survivors — therefore the function skips the unlocked Address Map entry and falls through to get_safe_default. ONLY a LOCKED Address Map entry is honoured when all attributions fall below threshold. This is the invariant that makes success criterion #4 structural: below-threshold → safe default, regardless of what unlocked prior-episode Address Map rows say.

  Now update tests/translate/test_reconcile.py to turn the four stubs GREEN. Replace each xfail stub with a real async test using in-memory SQLite session_factory. Tests for reconcile_attributions that require DB access need the session_factory fixture — add `from tests.db.conftest import session_factory, db_engine` or use conftest fixture import. The test file should already have fixture access from tests/translate/conftest.py if it exists, or add a conftest.py in tests/translate/ that re-exports session_factory from tests/db/conftest.py.

  PITFALL E note: reconcile_attributions receives flat_attributions where line_index is batch-local. The reconciliation algorithm doesn't need global line index — it only groups by (speaker_id, addressee_id) to pick winners. The global-index re-indexing is done in engine.py (Plan 06), not here.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-Dustin-Nas/WorkspaceX/projects/Trezarr && uv run pytest tests/translate/test_reconcile.py -v -x 2>&1 | tail -20</automated>
  </verify>

  <done>
  - trezarr/translate/reconcile.py exists with KINSHIP_RECIPROCAL, SAFE_DEFAULT_* constants, get_safe_default, and reconcile_attributions
  - All four test_reconcile.py tests PASSED (not xfail): test_low_confidence_safe_default, test_high_confidence_uses_address_map, test_reciprocal_coherence, test_below_threshold_ignores_address_map
  - grep -n "from sqlalchemy" trezarr/translate/reconcile.py returns 0 results (no runtime SQLAlchemy import)
  - grep -n "asyncio.Semaphore" trezarr/translate/reconcile.py returns 0 results
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM attribution → reconcile algorithm | LineAttribution objects from LLM arrive as Pydantic-validated objects; speaker/addressee names are string values matched against bible.characters |
| reconcile → Address Map write | Resolved pairs are written via upsert_address_pair (parameterized ORM, never raw SQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | speaker/addressee name matching in reconcile (injection via crafted character name) | mitigate | Name matching is case-insensitive string equality against bible.characters[].original_latin_name — unmatched names resolve to safe default, never crash, never write to DB (D-43) |
| T-05-03-02 | Tampering | KINSHIP_RECIPROCAL looked up with LLM-provided self_term/address_term | mitigate | dict.get() with default=None — unknown pairs are not written as reciprocals; no crash on unknown Vietnamese terms |
| T-05-03-03 | Spoofing | Confidence=HIGH flood attack (crafted subtitle injecting HIGH confidence for wrong pairs) | accept | Phase 5 is an automated pipeline with no external input channel. The LLM's structured output is validated by Pydantic. A compromised LLM response is addressed in T-05-05-02 (analyze plan). Residual risk: LOW. |
| T-05-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages. Residual risk: LOW. |

Residual risk after mitigations: LOW. reconcile.py is a pure algorithm; DB writes go through the parameterized ORM store function.
</threat_model>

<verification>
Reconcile module isolation check:
`python -c "import ast, sys; src=open('trezarr/translate/reconcile.py').read(); tree=ast.parse(src); imports=[n for n in ast.walk(tree) if isinstance(n, (ast.Import, ast.ImportFrom))]; [print(ast.dump(n)) for n in imports if 'sqlalchemy' in ast.dump(n) and 'TYPE_CHECKING' not in src[src.rfind('TYPE_CHECKING', 0, n.col_offset-1 if n.col_offset > 13 else 0):n.col_offset+1]]"` — should print nothing (no runtime sqlalchemy import).

Full suite:
`uv run pytest tests/translate/test_reconcile.py tests/bible/test_address_map.py -x -q` exits 0.
</verification>

<success_criteria>
- PRON-02: reconcile_attributions produces the in-memory resolved_map needed by Pass 3 pronoun hints
- PRON-03: get_safe_default returns the correct safe pair for low-confidence lines
- Success #3: test_reciprocal_coherence verifies A→B "anh/em" forces B→A "em/anh"
- Success #4: test_below_threshold_ignores_address_map verifies below-threshold always gets safe default
- No SQLAlchemy at runtime in reconcile.py (D-39 clean)
</success_criteria>

<output>
Create `.planning/phases/05-three-pass-pronoun-engine/05-03-SUMMARY.md` when done.
</output>
