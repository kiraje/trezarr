---
phase: 15-reskin-existing-pages
plan: 06
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
  - 15-03
  - 15-04
  - 15-05
files_modified:
  - frontend/src/pages/BibleEditor.tsx
  - frontend/src/components/LockBadge.tsx
  - frontend/src/components/LockToggleButton.tsx
  - frontend/src/components/FieldHistoryPanel.tsx
  - frontend/src/components/PronounCombo.tsx
  - frontend/src/components/ReciprocalSuggestionPanel.tsx
autonomous: true
requirements:
  - RSK-01
  - RSK-02

must_haves:
  truths:
    - "RSK-02/D-02: BibleEditor outer shell — breadcrumb uses text-primary (was text-accent); heading uses text-xl font-semibold text-foreground; loading state uses three Skeleton rows (heading, tab bar, content area); error uses div role=alert className=bg-amber-900/30 border-amber-800 text-amber-400"
    - "RSK-02/D-02: BibleEditor tab bar replaces hand-rolled nav role=tablist / button role=tab with shadcn Tabs/TabsList/TabsTrigger/TabsContent; activeTab state and onValueChange=(v)=>setActiveTab(v as Tab) preserved; five tab values (characters, address_map, terms, register, overrides) unchanged"
    - "RSK-02/D-03: BibleEditor.tsx Toast migration — import { toast } from 'sonner' replaces Toast import; showToast local helper calls toast.success/toast.error; Toast component render removed; ToastState state removed"
    - "RSK-02/D-02: CharactersSection SectionCard → Card/CardHeader/CardTitle/CardContent; raw inputs → Input; add-char form fields → Input; add/save/discard buttons → Button variant=default or variant=ghost; LockBadge and LockToggleButton tokens updated per spec"
    - "RSK-02/D-02: AddressMapSection SectionCard → Card; PronounCombo internal raw input → shadcn Input; ReciprocalSuggestionPanel bg-bg-surface → bg-card border-border; Add pair button → Button variant=outline size=sm"
    - "RSK-02/D-02: TermsSection SectionCard → Card; delete button → Button variant=ghost size=icon; Input for term fields; Add term button → Button variant=outline size=sm"
    - "RSK-02/D-02: RegisterSection and OverridesSection SectionCard → Card; Input/select tokens updated; save buttons → Button variant=default"
    - "RSK-02/D-02: LockBadge locked/human_override provenance maps to bg-primary/20 text-primary border border-primary/40; inferred/llm provenance maps to bg-card text-muted-foreground border border-border; lock icon color → text-primary"
    - "RSK-02/D-02: LockToggleButton text-text-muted hover:text-accent → text-muted-foreground hover:text-primary"
    - "RSK-02/D-02: FieldHistoryPanel bg-bg-surface border-[#2d3148] → bg-card border-border; style={{backgroundColor:'#1e293b',color:'#94a3b8'}} → className=bg-card text-muted-foreground"
    - "RSK-02/D-02: PronounCombo bg-bg-surface border-[#2d3148] text-[#e2e6f0] replaced with shadcn Input; combo logic (list filtering, selection, kinship_reciprocal) UNCHANGED"
    - "RSK-02/D-02: ReciprocalSuggestionPanel bg-bg-surface → bg-card; text-text-muted/text-text-primary → text-muted-foreground/text-foreground; accept/decline logic UNCHANGED"
    - "RSK-02/D-02: BibleEditor 'inferred' chip style={{backgroundColor:'#1e3a5f',color:'#60a5fa'}} → className=bg-primary/15 text-primary; Add action buttons (border-[#3b82f6] hover:bg-[#1e3a5f]) → Button variant=outline size=sm"
    - "RSK-02/D-02: All five section Save buttons (bg-accent hover:bg-[#2563eb]) → Button variant=default className=w-full"
    - "RSK-02/D-05: After this plan, BibleEditor.tsx and all five shared components contain zero occurrences of bg-[#, text-[#, bg-bg-, text-text-, or style={{ with hex values"
    - "RSK-02: NO API calls changed; NO state variables removed or renamed; NO lock/provenance semantics changed; NO tab identifiers changed; FieldHistoryPanel fetch logic UNCHANGED; PronounCombo filtering and selection UNCHANGED; ReciprocalSuggestionPanel accept/decline UNCHANGED"
  artifacts:
    - path: "frontend/src/pages/BibleEditor.tsx"
      provides: "BibleEditor reskinned on shadcn Tabs and primitives; Sonner toast; Skeleton loading"
      contains: "TabsList"
    - path: "frontend/src/components/LockBadge.tsx"
      provides: "LockBadge with purple/neutral token classes"
      contains: "bg-primary/20"
    - path: "frontend/src/components/LockToggleButton.tsx"
      provides: "LockToggleButton with text-muted-foreground hover:text-primary"
      contains: "hover:text-primary"
    - path: "frontend/src/components/FieldHistoryPanel.tsx"
      provides: "FieldHistoryPanel with bg-card border-border"
      contains: "bg-card"
    - path: "frontend/src/components/PronounCombo.tsx"
      provides: "PronounCombo using shadcn Input"
      contains: "Input"
    - path: "frontend/src/components/ReciprocalSuggestionPanel.tsx"
      provides: "ReciprocalSuggestionPanel with CSS-var tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "frontend/src/pages/BibleEditor.tsx"
      to: "frontend/src/components/ui/tabs.tsx"
      via: "Tabs, TabsList, TabsTrigger, TabsContent imports"
      pattern: "TabsList"
    - from: "frontend/src/pages/BibleEditor.tsx"
      to: "sonner"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.success|toast\\.error"
---

<objective>
Reskin BibleEditor.tsx and its five shared components (LockBadge, LockToggleButton,
FieldHistoryPanel, PronounCombo, ReciprocalSuggestionPanel) onto shadcn primitives.
This is the highest-risk surface (90KB, 5 tabs, locks, pronoun combos, address map, overrides).

The reskin follows the UI-SPEC's five sequential sub-task strategy, each ending with a build gate.

RSK-02 NON-NEGOTIABLE: This is a CLASSNAME/STYLE SUBSTITUTION ONLY. Zero behavior changes.
No API calls, state variables, lock semantics, tab identifiers, FieldHistoryPanel fetch logic,
PronounCombo filtering, or ReciprocalSuggestionPanel accept/decline are modified.

Purpose: Final page reskin before the cleanup sweep. All shared components used here are
reskinned in this plan because BibleEditor is their final consumer after plans 01–05.

Output: BibleEditor.tsx and 5 shared components — zero legacy hex. Build green after each task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/src/pages/BibleEditor.tsx
@frontend/src/components/LockBadge.tsx
@frontend/src/components/LockToggleButton.tsx
@frontend/src/components/FieldHistoryPanel.tsx
@frontend/src/components/PronounCombo.tsx
@frontend/src/components/ReciprocalSuggestionPanel.tsx
@frontend/src/components/ui/tabs.tsx
@frontend/src/components/ui/card.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/skeleton.tsx
@frontend/src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin BibleEditor outer shell — heading, breadcrumb, Tabs swap, loading/error, Sonner migration; reskin LockBadge and LockToggleButton</name>
  <files>
    frontend/src/pages/BibleEditor.tsx
    frontend/src/components/LockBadge.tsx
    frontend/src/components/LockToggleButton.tsx
  </files>
  <action>
    Read all three files in full before editing. This task covers:
    (A) BibleEditor outer shell — everything above the section components
    (B) LockBadge and LockToggleButton — reskinned now so CharactersSection (Task 2) can use them

    LOCKBADGE (LockBadge.tsx):
    Read the existing LockBadge to understand its provenance/lock_state rendering. Apply per UI-SPEC:
    - locked / human_override provenance: className="bg-primary/20 text-primary border border-primary/40 text-xs rounded px-1"
    - inferred / llm provenance: className="bg-card text-muted-foreground border border-border text-xs rounded px-1"
    - Lock icon (if present) color: text-primary
    - Provenance labels and lock states are UNCHANGED — only className.
    Import Badge from "../components/ui/badge" if the component currently uses raw spans — use
    Badge with an explicit className (not a variant) so colors are fully controlled.

    LOCKTOGGLEBUTTON (LockToggleButton.tsx):
    Replace: text-text-muted hover:text-accent → text-muted-foreground hover:text-primary
    Keep: all onClick, disabled, aria attributes; keep lucide icon if present.

    BIBLEEDITOR OUTER SHELL (BibleEditor.tsx — outer section only, not section components):

    IMPORTS to add:
    - Tabs, TabsList, TabsTrigger, TabsContent from "../components/ui/tabs"
    - Skeleton from "../components/ui/skeleton"
    - { toast } from "sonner"

    IMPORTS to remove:
    - Toast from "../components/Toast"
    - type { ToastState } from "../components/Toast"

    SONNER MIGRATION (D-03 — final call-site):
    - Remove: const [toast, setToast] = useState<ToastState | null>(null)
    - Remove: const showToast = useCallback(...) that wraps setToast
    - Replace showToast calls in section prop callbacks: showToast({ message, variant }) →
      inline toast.success(message) or toast.error(message). The showToast prop passed to
      section components (CharactersSection, AddressMapSection, etc.) has type
      (t: Omit<ToastState, "id">) => void — after migration, replace this with a prop type
      of (message: string, variant: "success" | "error") => void, OR keep the same prop
      signature but have the outer showToast call toast.success/toast.error based on variant.
      IMPORTANT: The section components call showToast({message, variant}) internally — the
      prop type must remain compatible. Safest approach: keep showToast as a local function
      (not useState-based) that calls toast.success or toast.error:
        const showToast = useCallback((t: Omit<ToastState, "id">) => {
          if (t.variant === "success") toast.success(t.message);
          else toast.error(t.message);
        }, []);
      This keeps section component call-sites unchanged while eliminating the Toast state.
      Remove only the useState<ToastState | null> — keep the showToast callback definition
      using the approach above so section components compile without type changes.
    - Remove: <Toast toast={toast} onDismiss={() => setToast(null)} /> from JSX

    BREADCRUMB: Link to="/bible" text "← Bible" — replace className text-accent with text-primary.

    HEADING: text-lg font-semibold text-text-primary → text-xl font-semibold text-foreground
    Register span: text-xs text-text-muted → text-xs text-muted-foreground

    LOADING STATE: Replace <div><p className="text-sm text-text-muted">Loading…</p></div> with:
      <div className="flex flex-col gap-2">
        <Skeleton className="h-8 w-48" />
        <Skeleton className="h-10 w-full" />
        <Skeleton className="h-64 w-full" />
      </div>

    ERROR/UNREACHABLE BANNER: Replace UnreachableBanner component's inline style with:
    className="w-full mb-4 px-4 py-3 text-sm rounded bg-amber-900/30 border border-amber-800 text-amber-400"

    TAB BAR: Replace the hand-rolled <nav role="tablist">...<button role="tab">...</nav> block with:
      <Tabs value={activeTab} onValueChange={(v) => setActiveTab(v as Tab)}>
        <TabsList>
          <TabsTrigger value="characters">Characters</TabsTrigger>
          <TabsTrigger value="address_map">Address Map</TabsTrigger>
          <TabsTrigger value="terms">Terms</TabsTrigger>
          <TabsTrigger value="register">Register</TabsTrigger>
          <TabsTrigger value="overrides">Overrides</TabsTrigger>
        </TabsList>
        <TabsContent value="characters"><CharactersSection .../></TabsContent>
        <TabsContent value="address_map"><AddressMapSection .../></TabsContent>
        <TabsContent value="terms"><TermsSection .../></TabsContent>
        <TabsContent value="register"><RegisterSection .../></TabsContent>
        <TabsContent value="overrides"><OverridesSection .../></TabsContent>
      </Tabs>
    Remove the <div role="tabpanel"> wrapper (TabsContent handles it). All section component
    props (seriesId, bible, setBible, showToast, pronounsData) are passed unchanged.

    DO NOT touch any section component function bodies in this task.

    Build gate: cd frontend && npm run build must exit 0 before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. BibleEditor uses Tabs primitive for tab bar, Skeleton loading,
    Sonner toast (no Toast component render), text-primary breadcrumb. LockBadge uses
    bg-primary/20 for locked/human_override and bg-card for inferred/llm. LockToggleButton
    uses text-muted-foreground hover:text-primary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reskin CharactersSection in BibleEditor.tsx — Card, Input, Button, FieldHistoryPanel; reskin FieldHistoryPanel.tsx</name>
  <files>
    frontend/src/pages/BibleEditor.tsx
    frontend/src/components/FieldHistoryPanel.tsx
  </files>
  <action>
    Read FieldHistoryPanel.tsx in full. Read the CharactersSection function in BibleEditor.tsx.
    Apply per 15-UI-SPEC.md §BibleEditor — CLASSNAME SUBSTITUTION ONLY.

    FIELDHISTORYPANEL (FieldHistoryPanel.tsx):
    - Container: bg-bg-surface border-[#2d3148] → bg-card border-border
    - Inline style {{backgroundColor:"#1e293b",color:"#94a3b8"}} → className="bg-card text-muted-foreground"
    - Any text-[#6b7280] or text-text-muted → text-muted-foreground
    - Any text-[#e2e6f0] or text-text-primary → text-foreground
    - History fetch logic, field watch prop, expand/collapse behavior: UNCHANGED.

    CHARACTERSSECTION (in BibleEditor.tsx):
    Imports for section: add Card, CardHeader, CardTitle, CardContent from ui/card;
    Input from ui/input; Button from ui/button — these may already be imported at file top;
    no duplicate imports.

    SectionCard wrapper around CharactersSection content:
    - Replace <SectionCard> (the div with bg-bg-surface border-[#2d3148]) with
      <Card><CardHeader><CardTitle>Characters</CardTitle></CardHeader><CardContent className="flex flex-col gap-4">...</CardContent></Card>

    TextField usage inside CharactersSection:
    - Replace TextField component's internal raw input with shadcn Input.
      OR: replace the entire TextField usage with:
        <div className="flex flex-col gap-1">
          <label htmlFor={id} className="text-xs text-muted-foreground">{label}</label>
          <Input id={id} value={value} onChange={e => onChange(e.target.value)} ... />
        </div>
      Either approach is valid as long as Input is used and no hex remains.

    Buttons in CharactersSection:
    - Add character button (text-sm text-accent px-3 py-1 rounded border border-[#3b82f6]
      hover:bg-[#1e3a5f]): replace with Button variant="outline" size="sm"
    - Save character button (bg-accent text-white ... hover:bg-[#2563eb]): replace with
      Button variant="default"
    - Discard/cancel button: Button variant="ghost"
    - Edit pencil icon button: Button variant="ghost" size="icon"
    - History clock icon button: Button variant="ghost" size="icon"

    Inferred chip (style={{backgroundColor:"#1e3a5f",color:"#60a5fa"}}):
    Replace with className="bg-primary/15 text-primary text-xs rounded px-1"

    LockBadge and LockToggleButton: already reskinned in Task 1 — use as-is.
    FieldHistoryPanel: reskinned in this task — passes through cleanly.

    All character state (editingId, editFields, historyOpenId, saving, showAddForm, addFields),
    all API calls (patchCharacter, addCharacter), and all event handlers are UNCHANGED.

    Build gate after this task.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. CharactersSection uses Card wrapper, Input fields, Button primitives.
    FieldHistoryPanel uses bg-card border-border with no hex. Zero hex in FieldHistoryPanel.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reskin AddressMapSection in BibleEditor.tsx — Card, Input, Button; reskin PronounCombo.tsx and ReciprocalSuggestionPanel.tsx</name>
  <files>
    frontend/src/pages/BibleEditor.tsx
    frontend/src/components/PronounCombo.tsx
    frontend/src/components/ReciprocalSuggestionPanel.tsx
  </files>
  <action>
    Read PronounCombo.tsx and ReciprocalSuggestionPanel.tsx in full. Read the AddressMapSection
    function in BibleEditor.tsx. Apply per 15-UI-SPEC.md — CLASSNAME SUBSTITUTION ONLY.

    PRONOUNCOMBO (PronounCombo.tsx):
    - Replace internal raw input element with shadcn Input from "../components/ui/input".
    - Container/dropdown: bg-bg-surface border-[#2d3148] → bg-card border-border
    - Dropdown list items: text-[#e2e6f0] → text-foreground; hover class → hover:bg-accent/50
    - Any text-[#6b7280] → text-muted-foreground
    - The filtering logic (list.filter by input value), selection state, kinship_reciprocal
      behavior, and all props (value, onChange, options, placeholder): UNCHANGED.

    RECIPROCALSUGGESTIONPANEL (ReciprocalSuggestionPanel.tsx):
    - bg-bg-surface → bg-card; border-[#2d3148] → border-border
    - text-text-muted / text-[#6b7280] → text-muted-foreground
    - text-text-primary / text-[#e2e6f0] → text-foreground
    - Accept button: replace raw button with Button variant="default" size="sm" (or variant="outline")
    - Decline button: Button variant="ghost" size="sm"
    - Accept/decline logic, all props, suggestion display: UNCHANGED.

    ADDRESSMAPSECTION (in BibleEditor.tsx):
    - SectionCard → Card/CardHeader/CardTitle/CardContent (same pattern as CharactersSection)
    - CardTitle: "Address Map"
    - Add pair button: Button variant="outline" size="sm"
    - Save/discard buttons: Button variant="default" / Button variant="ghost"
    - Delete pair icon button: Button variant="ghost" size="icon"
    - Lock-with-empty-term hard block: the error message display — if it uses a hex color class,
      replace with text-destructive. BEHAVIOR (the block itself) is UNCHANGED.
    - PronounCombo: already reskinned in this task — passes through cleanly.
    - ReciprocalSuggestionPanel: already reskinned in this task — passes through cleanly.
    - All state (editingPairId, editPairFields, saving, showAddForm, addPairFields),
      API calls (patchAddressMapPair, addAddressMapPair, deleteAddressMapPair): UNCHANGED.

    Build gate after this task.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. PronounCombo uses shadcn Input with no hex. ReciprocalSuggestionPanel
    uses bg-card/border-border with Button accept/decline. AddressMapSection uses Card wrapper.
    Zero hex in PronounCombo.tsx and ReciprocalSuggestionPanel.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 4: Reskin TermsSection, RegisterSection, and OverridesSection in BibleEditor.tsx</name>
  <files>frontend/src/pages/BibleEditor.tsx</files>
  <action>
    Read the TermsSection, RegisterSection, and OverridesSection functions in BibleEditor.tsx.
    Apply per 15-UI-SPEC.md — CLASSNAME SUBSTITUTION ONLY.

    TERMSSECTION:
    - SectionCard → Card/CardHeader/CardTitle("Term Dictionary")/CardContent
    - Input for source term, Vietnamese term fields
    - Delete term button: Button variant="ghost" size="icon" with Trash2 icon (already imported)
    - Add term button: Button variant="outline" size="sm"
    - Save/discard: Button variant="default" / Button variant="ghost"
    - LockBadge and LockToggleButton per row: already reskinned in Task 1
    - All state (editingTermId, editTermFields, saving, showAddTermForm, addTermFields),
      API calls (patchTerm via TermDTO, addTerm, deleteTerm): UNCHANGED.

    REGISTERSECTION:
    - SectionCard → Card/CardHeader/CardTitle("Register")/CardContent
    - The register selector: native <select> is acceptable per UI-SPEC. Add className to match
      shadcn Input appearance: "h-9 w-full rounded-md border border-border bg-background px-3
      text-sm text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
    - Source language priority selector: same styling as register selector
    - Save button: Button variant="default" className="w-full"
    - RestartBanner if present: text-amber-400 className (no inline style)
    - All state (register, sourceLanguagePriority, dirty, saving, restart),
      API call (patchRegister): UNCHANGED.

    OVERRIDESSECTION:
    - SectionCard → Card/CardHeader/CardTitle("Overrides")/CardContent
    - Any text fields: use Input component
    - Save button: Button variant="default" className="w-full"
    - All state and API calls (patchSeriesOverrides): UNCHANGED.

    Do a final pass on ALL remaining legacy tokens in BibleEditor.tsx after the four section
    components are updated. Search the file for any remaining: bg-[#, text-[#, bg-bg-, text-text-,
    style={{, border-[#, focus:outline-[#. Fix any stragglers.

    Also remove the SectionCard and TextField helper components from BibleEditor.tsx if they
    are no longer used after the Card/Input substitutions — replacing them with shadcn primitives
    makes them dead code that will fail tsc noUnusedLocals.

    Build gate after this task.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. TermsSection, RegisterSection, and OverridesSection use Card wrappers,
    Input/select/Button primitives. BibleEditor.tsx has zero hex strings or bridge token classes.
    Dead SectionCard/TextField helpers removed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Final grep pass on BibleEditor and all five shared components; confirm zero hex</name>
  <files>frontend/src/pages/BibleEditor.tsx</files>
  <action>
    Run the cleanup grep on all files touched in this plan:

      grep -n "bg-\[#\|text-\[#\|bg-bg-\|text-text-\|style={{" \
        frontend/src/pages/BibleEditor.tsx \
        frontend/src/components/LockBadge.tsx \
        frontend/src/components/LockToggleButton.tsx \
        frontend/src/components/FieldHistoryPanel.tsx \
        frontend/src/components/PronounCombo.tsx \
        frontend/src/components/ReciprocalSuggestionPanel.tsx

    If ANY line is returned: fix the remaining occurrence in the appropriate file and re-run.
    Do NOT proceed until grep returns zero results.

    Also confirm Toast is removed from BibleEditor:
      grep -n "Toast\|ToastState" frontend/src/pages/BibleEditor.tsx
    Must return zero matches (the showToast callback is ok if it calls toast() from sonner, but
    the Toast component import and ToastState useState must be gone).

    Run the build gate one final time to confirm the complete plan is clean:
      cd frontend && npm run build
    Must exit 0.

    If any fixes were needed during the grep pass, they constitute the "action" of this task.
    If everything was already clean from Tasks 1-4, this task is a verification-only commit.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    grep returns zero results for all six files. npm run build exits 0. BibleEditor and all five
    shared components are fully reskinned with zero legacy hex or bridge tokens.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BibleEditor API calls → backend | All API calls (getSeriesBible, patchCharacter, etc.) are UNCHANGED — this plan only swaps classNames |
| Lock state display | LockBadge shows provenance from API response; color-only change; lock logic in backend is not affected |
| PronounCombo input | User-typed pronoun text is filtered client-side and sent via patchAddressMapPair — Input change does not affect this data path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering (XSS) | BibleEditor text rendering | accept | All bible data (names, terms, register) rendered as JSX text nodes — React auto-escapes; no dangerouslySetInnerHTML |
| T-15-06-02 | Tampering | Lock semantics (BibleEditor) | mitigate | RSK-02 Safety Rules enforced: no API calls, no lock state logic, no hard-block behavior changed — reviewer must confirm before merge |
| T-15-06-03 | Tampering (XSS) | PronounCombo input | accept | Input value is React-controlled state; no innerHTML; same data path as pre-reskin |
| T-15-06-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; all primitives vendored Phase 11; sonner already in package.json |
</threat_model>

<verification>
After Task 5:

grep -rn "bg-\[#\|text-\[#" frontend/src/pages/BibleEditor.tsx frontend/src/components/LockBadge.tsx frontend/src/components/LockToggleButton.tsx frontend/src/components/FieldHistoryPanel.tsx frontend/src/components/PronounCombo.tsx frontend/src/components/ReciprocalSuggestionPanel.tsx
Must return: zero matches.

grep -n "Toast\|ToastState" frontend/src/pages/BibleEditor.tsx
Must return: zero matches.

cd frontend && npm run build
Must: exit 0; tsc -b strict clean; vite build clean.
</verification>

<success_criteria>
- cd frontend && npm run build exits 0 after all five tasks.
- BibleEditor uses shadcn Tabs primitive; Skeleton loading; Sonner toast; Card sections; Button/Input primitives throughout.
- All five tabs (characters, address_map, terms, register, overrides) identifiers unchanged.
- LockBadge uses bg-primary/20 for locked and bg-card for inferred.
- FieldHistoryPanel, PronounCombo, ReciprocalSuggestionPanel use bg-card/border-border.
- Zero hex strings in BibleEditor.tsx and all five shared component files.
- No API calls, state variables, lock semantics, or behavioral logic changed (RSK-02 invariant).
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-06-SUMMARY.md when done.
</output>
