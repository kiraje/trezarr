---
phase: 15-reskin-existing-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Queue.tsx
  - frontend/src/components/JobTable.tsx
autonomous: true
requirements:
  - RSK-01

must_haves:
  truths:
    - "RSK-01/D-01/D-02: Queue heading is text-xl font-semibold text-foreground (was text-lg text-[#e2e6f0]); count span is text-muted-foreground text-xs (was text-[#6b7280])"
    - "RSK-01/D-04: Queue UnreachableBanner is a div role=alert with className bg-amber-900/30 border border-amber-800 text-amber-400 (replaces inline style {{backgroundColor:'#451a03',color:'#fbbf24'}})"
    - "RSK-01/D-04: Queue renders Skeleton rows on first load (jobs.length === 0 and !hasLoaded) instead of empty table immediately; silent re-poll behavior unchanged"
    - "RSK-01/D-04: Queue renders a centered muted paragraph 'Queue is empty. No jobs are currently running.' when jobs is empty and hasLoaded is true and !unreachable"
    - "RSK-01/D-02: JobTable (QueueTable/HistoryTable) table headers use text-muted-foreground text-xs uppercase; row hover uses hover:bg-accent/50; borders use border-border; row text uses text-foreground text-sm; log icon button uses Button variant=ghost size=icon"
    - "RSK-01/D-02: JobTable row hover class hover:bg-[#22263a] is replaced with hover:bg-accent/50 throughout"
    - "RSK-01/D-05: After this plan, Queue.tsx and JobTable.tsx contain zero occurrences of bg-[#, text-[#, bg-bg-, text-text-, or style={{ with hex values"
  artifacts:
    - path: "frontend/src/pages/Queue.tsx"
      provides: "Queue page on shadcn tokens with loading/empty/error states"
      contains: "text-xl font-semibold"
    - path: "frontend/src/components/JobTable.tsx"
      provides: "JobTable with shadcn tokens — shared by Queue and History"
      contains: "hover:bg-accent/50"
  key_links:
    - from: "frontend/src/pages/Queue.tsx"
      to: "frontend/src/components/JobTable.tsx"
      via: "QueueTable import"
      pattern: "QueueTable"
---

<objective>
Reskin Queue.tsx and the shared JobTable.tsx component onto CSS-var tokens and shadcn primitives.
JobTable is reskinned here (alongside Queue, its first consumer) so History.tsx can import the
already-clean component in plan 15-03 without duplication.

Purpose: Second page in D-01 risk sequence. JobTable is a shared component — reskinning it here
means the History page in plan 03 receives the clean component for free.

Output: Queue.tsx and JobTable.tsx — zero legacy hex. Build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/src/pages/Queue.tsx
@frontend/src/components/JobTable.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin JobTable.tsx — token swap on table, headers, rows, log icon button</name>
  <files>frontend/src/components/JobTable.tsx</files>
  <action>
    Read JobTable.tsx in full before editing. Apply substitutions per 15-UI-SPEC.md §Queue:

    TABLE STRUCTURE: Keep plain HTML table (not jolly-ui Table — same rationale as Series.tsx D-01).

    TABLE HEADERS: text-[#6b7280] → text-muted-foreground. Add text-xs uppercase if not present.
    Border on header row: border-[#2d3148] → border-border.

    ROW HOVER: hover:bg-[#22263a] → hover:bg-accent/50. Remove any bg-bg-stripe zebra row class.
    Row border: border-[#2d3148] → border-border.

    ROW TEXT: text-[#e2e6f0] → text-foreground; text-[#6b7280] → text-muted-foreground.
    Cell text size: ensure text-sm on data cells, text-xs text-muted-foreground on secondary
    fields (timestamps, trigger, reason).

    LOG ICON BUTTON: Replace any raw <button> that navigates to logs with Button variant="ghost"
    size="icon" from "../components/ui/button". Keep the existing onClick behavior.

    STATUSBADGE: Already reskinned in plan 01 — no changes needed here.

    All table logic, column definitions, props, and callbacks are UNCHANGED. Only className
    and style attributes change.

    Strict TS: no unused imports. If Button is newly imported, remove stale raw button import
    if any.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. JobTable.tsx contains no hex strings. Table headers are
    text-muted-foreground text-xs. Row hover is hover:bg-accent/50. Log icon uses Button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reskin Queue.tsx — heading, UnreachableBanner, loading/empty states</name>
  <files>frontend/src/pages/Queue.tsx</files>
  <action>
    Read Queue.tsx in full before editing. Apply substitutions per 15-UI-SPEC.md §Queue:

    IMPORTS to add:
    - Skeleton from "../components/ui/skeleton"

    HEADING: text-lg font-semibold text-[#e2e6f0] → text-xl font-semibold text-foreground
    COUNT SPAN: text-xs text-[#6b7280] → text-xs text-muted-foreground

    UNREACHABLE BANNER: Replace the UnreachableBanner component's inline style with:
    className="w-full mb-4 px-4 py-3 text-sm rounded bg-amber-900/30 border border-amber-800
    text-amber-400"
    Remove the style prop entirely. Keep role="alert" and the copy unchanged.

    LOADING STATE: Add a hasLoaded state (boolean, initial false). Set hasLoaded = true after the
    first successful load() call (inside the load callback after setJobs). The loading skeleton
    shows only when jobs.length === 0 && !hasLoaded && !unreachable. After hasLoaded is true,
    re-polls are silent (no skeleton re-shown).

    Loading skeleton markup:
      <div className="flex flex-col gap-1">
        <Skeleton className="h-8 w-32 mb-4" />
        <Skeleton className="h-10 w-full mb-1" />
        <Skeleton className="h-10 w-full mb-1" />
        <Skeleton className="h-10 w-full mb-1" />
        <Skeleton className="h-10 w-full" />
      </div>

    EMPTY STATE: When jobs.length === 0 && hasLoaded && !unreachable, render:
      <div className="py-12 text-center">
        <p className="text-sm text-muted-foreground">
          Queue is empty. No jobs are currently running.
        </p>
      </div>
    Otherwise render QueueTable as before.

    Polling interval and all load/callback logic is UNCHANGED.
    Strict TS: no unused imports.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. Queue.tsx has text-xl heading, amber UnreachableBanner via className
    (no inline style), Skeleton on first load, and muted empty-state paragraph.
    Zero hex strings in either modified file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → JSX render | Queue job fields rendered as React text nodes (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering (XSS) | JobTable cell rendering | accept | All job data (path, status, series names) rendered as JSX text nodes — React auto-escapes |
| T-15-02-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; all primitives vendored Phase 11 |
</threat_model>

<verification>
cd frontend && npm run build

Must: exit 0; tsc -b strict clean; vite build clean.
Spot-check: grep -n "bg-\[#\|text-\[#\|bg-bg-\|text-text-\|style={{" frontend/src/pages/Queue.tsx frontend/src/components/JobTable.tsx
Must return: zero matches.
</verification>

<success_criteria>
- cd frontend && npm run build exits 0.
- JobTable uses text-muted-foreground headers, hover:bg-accent/50 rows, border-border, Button ghost icon for logs.
- Queue has text-xl heading, className-only amber banner, Skeleton on first load, muted empty state.
- Zero hex strings in Queue.tsx and JobTable.tsx.
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-02-SUMMARY.md when done.
</output>
