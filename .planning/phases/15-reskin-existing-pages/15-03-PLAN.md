---
phase: 15-reskin-existing-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/History.tsx
  - frontend/src/components/RetryButton.tsx
autonomous: true
requirements:
  - RSK-01

must_haves:
  truths:
    - "RSK-01/D-01/D-02: History heading is text-xl font-semibold text-foreground; count span is text-muted-foreground text-xs"
    - "RSK-01/D-04: History UnreachableBanner uses className bg-amber-900/30 border border-amber-800 text-amber-400 (no inline style hex)"
    - "RSK-01/D-04: History renders muted empty-state paragraph 'No job history yet.' when jobs is empty and loaded"
    - "RSK-01/D-03: History.tsx imports toast from sonner and calls toast.success(msg)/toast.error(msg) instead of setToast()/Toast component; ToastState import and useState<ToastState> are removed; Toast component render is removed"
    - "RSK-01/D-03: RetryButton.tsx uses Button variant=ghost size=sm with text-primary (was raw button with text-accent); RotateCw icon unchanged"
    - "RSK-01/D-05: After this plan, History.tsx and RetryButton.tsx contain zero occurrences of bg-[#, text-[#, bg-bg-, text-text-, style={{ with hex, or Toast/ToastState imports"
  artifacts:
    - path: "frontend/src/pages/History.tsx"
      provides: "History page on shadcn tokens; Sonner toast migration complete for this page"
      contains: "toast.success"
    - path: "frontend/src/components/RetryButton.tsx"
      provides: "RetryButton using Button primitive with text-primary"
      contains: "text-primary"
  key_links:
    - from: "frontend/src/pages/History.tsx"
      to: "sonner"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.success|toast\\.error"
---

<objective>
Reskin History.tsx and RetryButton.tsx onto shadcn tokens. Migrate History.tsx's toast usage from
the legacy Toast.tsx component to the Sonner API (D-03, first migration step).

Purpose: Third page in D-01 sequence. The Sonner migration starts here — History is the first
call-site. Toast.tsx is NOT deleted yet (Settings and BibleEditor still use it); deletion happens
in plan 15-07 after all call-sites are migrated.

Output: History.tsx and RetryButton.tsx — zero legacy hex; History uses sonner toast().
Build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/src/pages/History.tsx
@frontend/src/components/RetryButton.tsx
@frontend/src/components/Toast.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin RetryButton.tsx — Button primitive with text-primary</name>
  <files>frontend/src/components/RetryButton.tsx</files>
  <action>
    Read RetryButton.tsx in full before editing. Apply per 15-UI-SPEC.md §History:

    Replace the raw button element with Button variant="ghost" size="sm" from
    "../components/ui/button".

    The interactive color: replace text-accent (or any text-[#...]) with text-primary to use
    the purple accent for the action color. Keep the RotateCw lucide icon and its size.

    All onClick/disabled/aria props are UNCHANGED. Only the element and className change.

    Remove any focus:outline-[#...] or hover:bg-[#...] classes (Button applies ring and hover
    automatically for the ghost variant).

    Strict TS: remove stale imports if raw button was the only element.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. RetryButton.tsx uses Button variant="ghost" size="sm" with text-primary.
    No hex strings in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reskin History.tsx — heading, banner, empty state, Sonner migration</name>
  <files>frontend/src/pages/History.tsx</files>
  <action>
    Read History.tsx in full before editing. Apply all substitutions per 15-UI-SPEC.md §History:

    SONNER MIGRATION (D-03 — first call-site):
    - Remove: import Toast from "../components/Toast"
    - Remove: import type { ToastState } from "../components/Toast"
    - Add:    import { toast } from "sonner"
    - Remove: const [toast, setToast] = useState<ToastState | null>(null)
    - In HistoryTable's onToast prop callback: replace setToast({...t, id: Date.now()}) with
      direct toast.success(t.message) or toast.error(t.message) depending on t.variant.
      The onToast prop passes { message, variant } — map variant "success" → toast.success,
      variant "error" → toast.error.
    - Remove: <Toast toast={toast} onDismiss={() => setToast(null)} /> from JSX.
    - Remove the ToastState useState declaration entirely.
    Toast.tsx itself is NOT deleted in this plan (Settings and BibleEditor still use it).

    HEADING: text-lg font-semibold text-[#e2e6f0] → text-xl font-semibold text-foreground
    COUNT SPAN: text-xs text-[#6b7280] → text-xs text-muted-foreground

    UNREACHABLE BANNER: Replace UnreachableBanner's inline style with:
    className="w-full mb-4 px-4 py-3 text-sm rounded bg-amber-900/30 border border-amber-800
    text-amber-400"
    Remove the style prop. Keep role="alert" and the copy unchanged.

    EMPTY STATE: Add hasLoaded state (boolean, initial false). Set hasLoaded = true after first
    successful load. When jobs.length === 0 && hasLoaded && !unreachable, render:
      <div className="py-12 text-center">
        <p className="text-sm text-muted-foreground">No job history yet.</p>
      </div>
    Otherwise render HistoryTable as before.

    Polling, onRefresh, and all load/callback logic are UNCHANGED.
    Strict TS: no unused imports after removing Toast/ToastState.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. History.tsx uses toast from "sonner" — no Toast import, no ToastState
    state. Heading is text-xl. Banner is className-only. Empty state paragraph present.
    Zero hex strings in History.tsx and RetryButton.tsx.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → JSX render | History job fields rendered as React text nodes |
| Sonner toast message | Message strings come from the onToast callback which passes hardcoded strings — no user input injected into toast messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering (XSS) | History.tsx table/toast | accept | Job data rendered as text nodes; toast messages are hardcoded strings not user input |
| T-15-03-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; sonner already installed (v2.0.7 in package.json) |
</threat_model>

<verification>
cd frontend && npm run build

Must: exit 0; tsc -b strict clean; vite build clean.
Spot-check: grep -n "bg-\[#\|text-\[#\|bg-bg-\|text-text-\|style={{" frontend/src/pages/History.tsx frontend/src/components/RetryButton.tsx
Must return: zero matches.
grep -n "Toast\|ToastState" frontend/src/pages/History.tsx
Must return: zero matches (all Toast references removed).
</verification>

<success_criteria>
- cd frontend && npm run build exits 0.
- History.tsx uses import { toast } from "sonner" with no Toast/ToastState references.
- RetryButton.tsx uses Button variant="ghost" size="sm" with text-primary.
- History has text-xl heading, className-only amber banner, empty-state paragraph.
- Zero hex strings in History.tsx and RetryButton.tsx.
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-03-SUMMARY.md when done.
</output>
