---
phase: 15-reskin-existing-pages
plan: 07
type: execute
wave: 3
depends_on:
  - 15-06
files_modified:
  - frontend/tailwind.config.js
  - frontend/src/components/AppShell.tsx
autonomous: true
requirements:
  - RSK-01
  - RSK-02

must_haves:
  truths:
    - "RSK-01/D-05: tailwind.config.js no longer contains any of the five bridge token keys: bg-base, bg-surface, bg-stripe, text-primary, text-muted in the colors.extend section"
    - "RSK-01/D-05: grep -rn 'bg-\\[#\\|text-\\[#' frontend/src/ returns zero results — no raw hex color classes remain in any frontend source file"
    - "RSK-01/D-05: grep -rn 'bg-bg-\\|bg-base\\|bg-surface\\|bg-stripe\\|text-text-primary\\|text-text-muted' frontend/src/ returns zero results — no bridge token class names remain"
    - "RSK-01/D-05: grep -rn 'style={{' frontend/src/ | grep '#' returns zero results — no inline style hex values remain"
    - "RSK-01/D-03: AppShell.tsx imports and renders Toaster from ../components/ui/sonner; <Toaster/> is mounted once inside SidebarProvider (per D-03: <Toaster/> already confirmed absent — mount it now)"
    - "RSK-01/D-03: frontend/src/components/Toast.tsx is DELETED — all call-sites have been migrated to sonner in plans 03, 04, and 06"
    - "RSK-01: cd frontend && npm run build exits 0 after all changes — tsc strict clean, vite build clean"
    - "RSK-01: uv run pytest tests/ -q exits 0 — backend test suite unaffected by frontend reskin"
  artifacts:
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind config with zero legacy bridge tokens"
      contains: "border: 'hsl(var(--border))'"
    - path: "frontend/src/components/AppShell.tsx"
      provides: "AppShell with Toaster mounted"
      contains: "Toaster"
  key_links:
    - from: "frontend/src/components/AppShell.tsx"
      to: "frontend/src/components/ui/sonner.tsx"
      via: "import { Toaster } from './ui/sonner'"
      pattern: "Toaster"
---

<objective>
Phase-closing cleanup: mount the Sonner Toaster in AppShell (required for toast() calls to render),
delete Toast.tsx (all call-sites migrated), remove the five legacy bridge tokens from
tailwind.config.js, and run the full grep/build/pytest acceptance gate.

This is the final task of Phase 15 and of the v1.0 → v1.1 token migration. After this plan:
- D-05 is satisfied (bridge tokens removed, grep gate clean)
- D-03 is fully satisfied (Toast.tsx deleted, Toaster mounted)
- RSK-01 acceptance criteria are met (six pages reskinned, zero visible legacy hex)
- RSK-02 acceptance criteria are met (BibleEditor identical behavior, tests green)

Output: tailwind.config.js and AppShell.tsx modified; Toast.tsx deleted. Grep gate clean.
Build green. pytest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-reskin-existing-pages/15-CONTEXT.md
@.planning/phases/15-reskin-existing-pages/15-UI-SPEC.md
@frontend/tailwind.config.js
@frontend/src/components/AppShell.tsx
@frontend/src/components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount Toaster in AppShell; delete Toast.tsx; remove five bridge tokens from tailwind.config.js</name>
  <files>
    frontend/src/components/AppShell.tsx
    frontend/tailwind.config.js
  </files>
  <action>
    Read AppShell.tsx, tailwind.config.js, and frontend/src/components/ui/sonner.tsx before editing.
    Also verify Toast.tsx has no remaining importers:
      grep -rn "from.*Toast\|import.*Toast" frontend/src/ --include="*.tsx" --include="*.ts"
    If any file still imports Toast (other than Toast.tsx itself): fix that file before deleting.
    All call-sites should have been migrated in plans 03 (History), 04 (Settings), 06 (BibleEditor).

    APPSHELL.tsx — mount Toaster:
    Add: import { Toaster } from "./ui/sonner"
    Add <Toaster /> inside the SidebarProvider, after the <main> element:
      <SidebarProvider>
        <AppSidebar />
        <main className="flex-1 overflow-auto bg-background p-6">
          <Outlet />
        </main>
        <Toaster />
      </SidebarProvider>
    The Toaster is positioned fixed by Sonner internals — placement inside SidebarProvider
    does not affect its DOM position.

    DELETE Toast.tsx:
    Verify zero importers first (grep above). Then delete the file:
      Use Bash to run: rm frontend/src/components/Toast.tsx
    After deletion, run tsc immediately (cd frontend && npm run build) to confirm no dangling
    import references. If any file references Toast and fails tsc, fix it before proceeding.

    TAILWIND.CONFIG.JS — remove five bridge tokens:
    Read the current tailwind.config.js. In the theme.extend.colors section, delete exactly these
    five key-value pairs:
      'bg-base': '#0f1117',
      'bg-surface': '#1a1d27',
      'bg-stripe': '#1e2130',
      'text-primary': '#e2e6f0',
      'text-muted': '#6b7280',
    Leave all other color entries (border, input, ring, background, foreground, primary,
    secondary, destructive, muted, accent, popover, card, sidebar) unchanged.

    After all three changes, run: cd frontend && npm run build
    Must exit 0. If build fails because any file still references a bridge token class name
    (e.g., bg-bg-surface), find it with:
      grep -rn "bg-bg-\|bg-base\|bg-surface\|bg-stripe\|text-text-primary\|text-text-muted" frontend/src/
    Fix the remaining references before the build passes.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm run build exits 0. AppShell mounts Toaster. Toast.tsx is deleted. tailwind.config.js
    contains none of the five bridge token keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full acceptance gate — grep clean, build green, pytest green</name>
  <files>frontend/tailwind.config.js</files>
  <action>
    Run the complete Phase 15 acceptance gate in sequence. Fix any failures before marking done.

    STEP 1 — Raw hex class grep (must return zero):
      grep -rn "bg-\[#\|text-\[#" frontend/src/
    If any result: find the file, apply the correct token substitution from 15-UI-SPEC.md,
    rerun until clean.

    STEP 2 — Bridge token class grep (must return zero):
      grep -rn "bg-bg-\|bg-base\|bg-surface\|bg-stripe\|text-text-primary\|text-text-muted" frontend/src/
    If any result: same fix approach.

    STEP 3 — Inline style hex grep (must return zero):
      grep -rn "style={{" frontend/src/ | grep "#"
    If any result: replace the inline style with the corresponding className from UI-SPEC token map.

    STEP 4 — Bridge tokens removed from tailwind.config.js (must return zero for each):
      grep "bg-base\|bg-surface\|bg-stripe\|text-primary\|text-muted" frontend/tailwind.config.js
    If any result: the token was not removed — re-edit tailwind.config.js.

    STEP 5 — Build gate:
      cd frontend && npm run build
    Must exit 0.

    STEP 6 — Backend test gate:
      uv run pytest tests/ -q
    Must exit 0 with zero failures. The frontend reskin should not have touched Python code,
    but confirm no accidental modification affected Python imports or tests.

    If all six steps pass: the phase is complete. Record results in the SUMMARY.
  </action>
  <verify>
    <automated>grep -rn "bg-\[#\|text-\[#" frontend/src/ ; echo "---" ; cd frontend && npm run build 2>&1 | tail -5 ; cd .. && uv run pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All six grep steps return zero results. cd frontend && npm run build exits 0. uv run pytest
    tests/ -q exits 0 with zero failures. Phase 15 token migration is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sonner Toaster DOM position | Toaster is fixed-position by Sonner; mounting inside SidebarProvider does not expose a new attack surface |
| tailwind.config.js edit | Config-only change; no user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Tampering | tailwind.config.js token removal | accept | Removing unused CSS tokens reduces surface area — no security regression; build gate confirms no reference breakage |
| T-15-07-02 | Denial of Service | Toast.tsx deletion with missed importer | mitigate | grep for remaining Toast importers before delete; tsc build fails immediately if any reference survives |
| T-15-07-SC | Tampering (supply chain) | npm installs | accept | Zero new packages; Toaster is already in sonner.tsx from Phase 11 |
</threat_model>

<verification>
Full acceptance gate (must all pass):

grep -rn "bg-\[#\|text-\[#" frontend/src/
# Expected: (no output)

grep -rn "bg-bg-\|bg-base\|bg-surface\|bg-stripe\|text-text-primary\|text-text-muted" frontend/src/
# Expected: (no output)

grep -rn "style={{" frontend/src/ | grep "#"
# Expected: (no output)

grep "bg-base\|bg-surface\|bg-stripe\|text-primary.*#\|text-muted.*#" frontend/tailwind.config.js
# Expected: (no output)

cd frontend && npm run build
# Expected: exit 0, tsc -b clean, vite build clean

uv run pytest tests/ -q
# Expected: exit 0, 0 failures
</verification>

<success_criteria>
- AppShell.tsx imports and renders Toaster from ./ui/sonner.
- frontend/src/components/Toast.tsx does not exist.
- tailwind.config.js contains none of: bg-base, bg-surface, bg-stripe, text-primary (hex value), text-muted (hex value).
- grep -rn "bg-\[#\|text-\[#" frontend/src/ returns zero results.
- grep -rn "style={{" frontend/src/ | grep "#" returns zero results.
- cd frontend && npm run build exits 0.
- uv run pytest tests/ -q exits 0.
</success_criteria>

<output>
Create .planning/phases/15-reskin-existing-pages/15-07-SUMMARY.md when done.
</output>
