---
phase: 09-multi-format-ass-ssa-vtt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/codec/__init__.py
  - tests/codec/test_ass_roundtrip.py
  - tests/codec/test_vtt_roundtrip.py
  - tests/codec/test_dispatch.py
  - tests/codec/test_karaoke.py
  - tests/codec/test_drawing.py
  - tests/translate/test_timecode.py
  - tests/translate/test_sentinel_ass_vtt.py
  - tests/translate/test_validate_allowlist.py
  - tests/output/test_sidecar_format.py
  - tests/discover/test_gap_format.py
  - tests/fixtures/minimal.ass
  - tests/fixtures/karaoke.ass
  - tests/fixtures/drawing.ass
  - tests/fixtures/pos_an8.ass
  - tests/fixtures/mixed.ass
  - tests/fixtures/crlf.ass
  - tests/fixtures/ssa_v4.ssa
  - tests/fixtures/utf8bom.ass
  - tests/fixtures/minimal.vtt
  - tests/fixtures/cue_settings.vtt
  - tests/fixtures/voices.vtt
  - tests/fixtures/style_region.vtt
  - tests/fixtures/inline_timestamp.vtt
  - tests/fixtures/hourless_tc.vtt
  - tests/fixtures/note_block.vtt
  - tests/fixtures/crlf.vtt
autonomous: true
requirements:
  - FMT-02
  - FMT-03
  - FMT-04

must_haves:
  truths:
    - "All Wave 0 test files exist and fail (xfail or ImportError) before any implementation"
    - "All 16 ASS/SSA/VTT fixture files exist with the structural elements required by research"
    - "pytest tests/codec/ tests/translate/test_timecode.py tests/translate/test_sentinel_ass_vtt.py tests/translate/test_validate_allowlist.py tests/output/test_sidecar_format.py tests/discover/test_gap_format.py -x exits non-zero (RED) with no ERRORS unrelated to missing modules"
  artifacts:
    - path: "tests/codec/test_ass_roundtrip.py"
      provides: "FMT-02 byte-identity test stubs"
    - path: "tests/codec/test_vtt_roundtrip.py"
      provides: "FMT-04 byte-identity test stubs"
    - path: "tests/codec/test_karaoke.py"
      provides: "FMT-03 karaoke verbatim test stubs"
    - path: "tests/codec/test_drawing.py"
      provides: "D-98 drawing-run test stubs"
    - path: "tests/translate/test_timecode.py"
      provides: "D-101 tc_to_ms ASS+VTT test cases"
    - path: "tests/translate/test_sentinel_ass_vtt.py"
      provides: "D-98/D-100 sentinel extension test stubs"
    - path: "tests/translate/test_validate_allowlist.py"
      provides: "D-102 gate allowlist extension test stubs"
    - path: "tests/fixtures/minimal.ass"
      provides: "Baseline ASS fixture"
    - path: "tests/fixtures/karaoke.ass"
      provides: "Karaoke fixture with \\k tags"
    - path: "tests/fixtures/cue_settings.vtt"
      provides: "VTT cue-settings positioning fixture"
  key_links:
    - from: "tests/codec/test_ass_roundtrip.py"
      to: "tests/fixtures/"
      via: "Path(__file__).parent.parent / 'fixtures'"
      pattern: "FIXTURES.*fixtures"
---

<objective>
Wave 0: Create all RED test stubs and real-world fixtures for Phase 9. This plan establishes the Nyquist-compliant test surface before any implementation lands. All tests must be failing (xfail stubs or ImportError) when this plan completes.

Purpose: Prior phases always had a Wave 0 RED scaffold — it is the established Trezarr pattern. The scaffold makes the implementation phases verifiable immediately after each task, with automated sampling latency under 40 seconds.

Output: 10 new test files (some added to existing dirs), 1 new tests/codec/ package, 16 ASS/SSA/VTT fixture files. No production code is created or modified.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-multi-format-ass-ssa-vtt/09-CONTEXT.md
@.planning/phases/09-multi-format-ass-ssa-vtt/09-RESEARCH.md
@.planning/phases/09-multi-format-ass-ssa-vtt/09-VALIDATION.md
@.planning/phases/09-multi-format-ass-ssa-vtt/09-PATTERNS.md
@tests/codec/test_srt_roundtrip.py
@tests/translate/test_sentinel.py
@tests/translate/test_validate.py
@tests/output/test_write.py
@tests/discover/test_scan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test fixture files — 16 ASS/SSA/VTT files in tests/fixtures/</name>
  <files>
    tests/fixtures/minimal.ass
    tests/fixtures/karaoke.ass
    tests/fixtures/drawing.ass
    tests/fixtures/pos_an8.ass
    tests/fixtures/mixed.ass
    tests/fixtures/crlf.ass
    tests/fixtures/ssa_v4.ssa
    tests/fixtures/utf8bom.ass
    tests/fixtures/minimal.vtt
    tests/fixtures/cue_settings.vtt
    tests/fixtures/voices.vtt
    tests/fixtures/style_region.vtt
    tests/fixtures/inline_timestamp.vtt
    tests/fixtures/hourless_tc.vtt
    tests/fixtures/note_block.vtt
    tests/fixtures/crlf.vtt
  </files>
  <read_first>
    @tests/fixtures/minimal.srt — line-ending and encoding conventions for existing fixtures
    @.planning/phases/09-multi-format-ass-ssa-vtt/09-RESEARCH.md §9.2 (ASS fixtures) and §9.3 (VTT fixtures) — exact structural requirements per fixture
    @.planning/phases/09-multi-format-ass-ssa-vtt/09-PATTERNS.md §"No Analog Found" — fixture content specifications
  </read_first>
  <action>
    Create all 16 fixture files in tests/fixtures/ exactly as specified in RESEARCH §9.2 and §9.3. Each fixture must contain the structural elements listed below. Use UTF-8 encoding unless specified. Use LF line endings unless CRLF variant is named.

    ASS fixtures (all in [Script Info] / [V4+ Styles] / [Events] structure):

    minimal.ass — One [Script Info] with ScriptType: v4.00+, one [V4+ Styles] section with one Style line, one [Events] section with one Dialogue: line containing plain text. Plain LF endings.

    karaoke.ass — Same structure; the Dialogue: Text field contains {\k50}syl{\k60}la{\k70}ble karaoke timing. Required to trigger FMT-03 verbatim path.

    drawing.ass — Dialogue: Text contains {\p1}m 0 0 l 100 0 100 100 0 100{\p0} (vector drawing commands). Required to trigger D-98 drawing-run pass-through.

    pos_an8.ass — Dialogue: Text contains {\an8}{\pos(960,50)}Sign text here. Tests tag preservation for positioning overrides.

    mixed.ass — Contains all of the above in one file: a Comment: line in [Events], multiple Style entries in [V4+ Styles], a Dialogue with \N hard break (backslash-capital-N inside text, not in braces), a karaoke Dialogue, a drawing Dialogue, and a normal Dialogue. Tests the integration fixture scenario.

    crlf.ass — Same content as minimal.ass but with CRLF (\r\n) line endings throughout. Required to verify line-ending preservation (Pitfall 8).

    ssa_v4.ssa — Uses [V4 Styles] (no plus), Format line with Marked field: "Format: Marked, Start, End, Style, Name, MarginL, MarginR, MarginV, Effect, Text", and Dialogue lines with "Marked=0" as first field. ScriptType: v4.00 in [Script Info]. Tests SSA variant.

    utf8bom.ass — UTF-8 file starting with BOM byte sequence (EF BB BF). Same content as minimal.ass. Required for BOM round-trip test.

    VTT fixtures:

    minimal.vtt — WEBVTT header line, one blank line, one cue with HH:MM:SS.mmm timecodes and plain text payload.

    cue_settings.vtt — Multiple cues each with different cue settings on the timing line: one with "position:50% align:center", one with "line:90% align:start", one with "size:50%". Required for FMT-04 cue-settings preservation.

    voices.vtt — Cues with <v Speaker Name> inline voice annotation tags in the payload. Required for D-100 sentinel extension.

    style_region.vtt — Has a STYLE block and a REGION block before the first cue. STYLE block contains CSS rules. REGION block defines a positioning region. Both blocks must be preserved verbatim. Required for FMT-04 block preservation.

    inline_timestamp.vtt — Cue payload contains an inline <00:01:23.456> timestamp tag (a VTT timestamp cue tag). Required for D-100 sentinel protection.

    hourless_tc.vtt — All cue timecodes use the hourless MM:SS.mmm format (e.g., 01:23.456 --> 01:25.000). Required for D-101 hourless timecode branch.

    note_block.vtt — Has a NOTE block interspersed between cues. NOTE block content must be preserved verbatim. Required for VTT opaque block preservation.

    crlf.vtt — Same content as minimal.vtt but with CRLF line endings. Required for line-ending preservation.

    Key constraint: All ASS fixture Dialogue: lines must use exactly 9 commas before the Text field (RESEARCH §1.2). The Text field may contain commas freely. No fixture should need pysubs2 to parse — they are written by hand following the spec. Do NOT add any content not specified above; minimal is better for fixture files.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
fixtures = Path('tests/fixtures')
required = ['minimal.ass','karaoke.ass','drawing.ass','pos_an8.ass','mixed.ass','crlf.ass','ssa_v4.ssa','utf8bom.ass','minimal.vtt','cue_settings.vtt','voices.vtt','style_region.vtt','inline_timestamp.vtt','hourless_tc.vtt','note_block.vtt','crlf.vtt']
missing = [f for f in required if not (fixtures / f).exists()]
assert not missing, f'Missing fixtures: {missing}'
# UTF-8 BOM check
bom_bytes = (fixtures / 'utf8bom.ass').read_bytes()
assert bom_bytes[:3] == b'\xef\xbb\xbf', 'utf8bom.ass missing BOM'
# CRLF check
assert b'\r\n' in (fixtures / 'crlf.ass').read_bytes(), 'crlf.ass missing CRLF'
assert b'\r\n' in (fixtures / 'crlf.vtt').read_bytes(), 'crlf.vtt missing CRLF'
# karaoke tag present
assert b'\\\\k' in (fixtures / 'karaoke.ass').read_bytes() or b'{\\\\k' in (fixtures / 'karaoke.ass').read_bytes(), 'karaoke.ass missing karaoke tag'
# drawing tag present
assert b'{\\\\p1}' in (fixtures / 'drawing.ass').read_bytes() or b'{\\p1}' in (fixtures / 'drawing.ass').read_bytes(), 'drawing.ass missing drawing tag'
# VTT WEBVTT header
assert (fixtures / 'minimal.vtt').read_bytes().startswith(b'WEBVTT'), 'minimal.vtt missing WEBVTT header'
print('All fixtures verified')
"
</automated>
  </verify>
  <done>All 16 fixture files exist in tests/fixtures/ with the required structural content: ASS fixtures have correct [Script Info]/[V4+ Styles]/[Events] sections; karaoke.ass has \k tags; drawing.ass has {\p1} drawing commands; mixed.ass has Comment: event, \N break, multiple styles; crlf.* variants use CRLF endings; utf8bom.ass has BOM; ssa_v4.ssa uses [V4 Styles]; VTT fixtures have WEBVTT header; hourless_tc.vtt uses MM:SS.mmm timecodes; style_region.vtt has STYLE and REGION blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 RED test stubs — all codec and translate test files</name>
  <files>
    tests/codec/__init__.py
    tests/codec/test_ass_roundtrip.py
    tests/codec/test_vtt_roundtrip.py
    tests/codec/test_dispatch.py
    tests/codec/test_karaoke.py
    tests/codec/test_drawing.py
    tests/translate/test_timecode.py
    tests/translate/test_sentinel_ass_vtt.py
    tests/translate/test_validate_allowlist.py
    tests/output/test_sidecar_format.py
    tests/discover/test_gap_format.py
  </files>
  <read_first>
    @tests/codec/test_srt_roundtrip.py — the direct fixture-parameterization and byte-identity test template
    @tests/translate/test_sentinel.py — deferred import + pytest.importorskip pattern
    @tests/translate/test_validate.py — _make_line/_make_doc/_settings helper pattern
    @tests/output/test_write.py — derive_vi_sidecar_path test pattern
    @tests/discover/test_scan.py — scan/gap test patterns
    @.planning/phases/09-multi-format-ass-ssa-vtt/09-PATTERNS.md §"Test File Patterns" — concrete test stub templates
    @.planning/phases/09-multi-format-ass-ssa-vtt/09-VALIDATION.md — complete Wave 0 stub list and per-requirement test map
    @.planning/phases/09-multi-format-ass-ssa-vtt/09-RESEARCH.md §9.4 (tc_to_ms test cases), §9.5 (regression test patterns)
  </read_first>
  <action>
    Create tests/codec/__init__.py as an empty file. Then create each test file as xfail stubs following the established Trezarr pattern (xfail with raises=(ImportError, AssertionError, TypeError) for stubs targeting modules not yet created).

    tests/codec/test_ass_roundtrip.py: Parameterize over the 8 ASS fixture names (minimal.ass, karaoke.ass, drawing.ass, pos_an8.ass, mixed.ass, crlf.ass, ssa_v4.ssa, utf8bom.ass). Include xfail stubs for: test_byte_identical_roundtrip (parametrized), test_line_break_preserved (reads mixed.ass, asserts \N in text), test_section_headers_preserved (checks [Script Info] in output bytes), test_comments_verbatim (checks Comment: in output bytes), test_ssa_roundtrip (ssa_v4.ssa fixture), test_malformed_dialogue_preserved_and_flagged (warns UserWarning + raw is not None). Use pytest.importorskip("trezarr.subtitles.ass") pattern from PATTERNS.md. FIXTURES path = Path(__file__).parent.parent / "fixtures".

    tests/codec/test_vtt_roundtrip.py: Parameterize over the 8 VTT fixture names. Include xfail stubs for: test_byte_identical_roundtrip (parametrized), test_cue_settings_preserved (cue_settings.vtt), test_blocks_verbatim (style_region.vtt, note_block.vtt — checks STYLE/REGION/NOTE bytes preserved), test_hourless_timecode_roundtrip (hourless_tc.vtt). Use pytest.importorskip("trezarr.subtitles.vtt").

    tests/codec/test_dispatch.py: Stubs for read_subtitle routing by suffix (.srt→read_srt, .ass→read_ass, .ssa→read_ass, .vtt→read_vtt) and ValueError for unknown suffix. Use pytest.importorskip("trezarr.subtitles.dispatch"). Per D-94.

    tests/codec/test_karaoke.py: Stubs for: test_karaoke_text_is_raw (reads karaoke.ass, asserts sub_line.raw is not None and sub_line.raw == sub_line.text for the karaoke cue); test_karaoke_not_in_llm_batch (karaoke SubLine with raw set should not appear in any translate batch — use batch_subdoc mock). Per D-99 / FMT-03.

    tests/codec/test_drawing.py: Stubs for: test_drawing_run_is_raw (reads drawing.ass, asserts SubLine.raw is not None); test_drawing_run_not_in_llm_batch. Per D-98 / FMT-02.

    tests/translate/test_timecode.py: NOT xfail stubs — this file tests tc_to_ms which ALREADY EXISTS. Write concrete (non-xfail) test functions using pytest.importorskip("trezarr.translate._timecode"). Include the exact assertions from RESEARCH §9.4: tc_to_ms("0:01:23.45") == 83450, tc_to_ms("1:00:00.00") == 3600000, tc_to_ms("01:23.456") == 83456, tc_to_ms("00:01:23.456") == 83456, tc_to_ms("00:00:01,000") == 1000, tc_to_ms("garbage") == 0. Mark the ASS/VTT tests as xfail(strict=False, reason="tc_to_ms ASS/VTT extension not yet implemented") since the extension lands in Wave 1.

    tests/translate/test_sentinel_ass_vtt.py: Stubs for: test_extract_ass_hard_break (\\N is sentinel-protected, not in cleaned text per D-98); test_extract_ass_lowercase_n (\\n is protected); test_extract_ass_h (\\h is protected); test_vtt_voice_tag (the <v Speaker> tag is protected); test_vtt_inline_timestamp (<00:01:23.456> is protected). Use pytest.importorskip("trezarr.translate.sentinel"). Per D-98/D-100.

    tests/translate/test_validate_allowlist.py: Stubs for: test_karaoke_cue_allowlisted (SubLine with raw set skips check 3 gate per D-102 — RESEARCH §7.2 template); test_drawing_cue_allowlisted (same for drawing raw); test_pure_tag_cue_allowlisted (SubLine.text = "<<T0>><<T1>>" is SENTINEL_ONLY_RE match); test_check2_skips_raw_lines (check 2 does not raise for raw-set SubLine with empty-looking text). Copy _make_line/_make_doc/_settings helper functions from test_validate.py. Per D-102.

    tests/output/test_sidecar_format.py: Concrete (non-xfail) test stubs for derive_vi_sidecar_path per PATTERNS.md §"tests/output/": test_sidecar_naming_ass (Show.S01E01.en.ass → Show.S01E01.vi.ass), test_sidecar_naming_ssa (Show.S01E01.ssa → Show.S01E01.vi.ssa), test_sidecar_naming_vtt (Episode.S02E03.vtt → Episode.S02E03.vi.vtt), test_sidecar_naming_srt_unchanged (.srt → .vi.srt, regression guard). Mark ASS/VTT cases xfail(strict=False, reason="derive_vi_sidecar_path not yet generalized"). Per D-95.

    tests/discover/test_gap_format.py: Stubs for: test_foreign_vi_ass_excluded (is_eligible returns False for an item when .vi.ass exists without ledger entry — the AUTO-04 regression guard for ASS); test_foreign_vi_vtt_excluded (same for .vtt). Use the existing gap.py/scan.py test patterns. Per D-96.
  </action>
  <verify>
    <automated>pytest tests/codec/ tests/translate/test_timecode.py tests/translate/test_sentinel_ass_vtt.py tests/translate/test_validate_allowlist.py tests/output/test_sidecar_format.py tests/discover/test_gap_format.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 test files (including tests/codec/__init__.py) exist. pytest exits non-zero (expected failures/xfail or ImportError) with zero unexpected ERRORs. The existing test suite (pytest tests/ -x -q) continues to pass — no regressions introduced by the new stubs. tests/translate/test_timecode.py has concrete assertions for existing SRT behavior passing green; ASS/VTT extension assertions are marked xfail(strict=False).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → test code | Fixture bytes are read from disk and compared; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-SC | Tampering | test fixture files | accept | Fixtures are hand-authored synthetic files; no external downloads; no package installs in this plan |
</threat_model>

<verification>
Run after plan completes:

```
pytest tests/codec/ tests/translate/test_timecode.py tests/translate/test_sentinel_ass_vtt.py tests/translate/test_validate_allowlist.py tests/output/test_sidecar_format.py tests/discover/test_gap_format.py -q 2>&1 | tail -20
```

Expected: a mix of xfail and expected failures. Zero ERRORS unrelated to missing module imports. Existing test suite remains green:
```
pytest tests/ -x -q --ignore=tests/codec/ --ignore=tests/translate/test_timecode.py --ignore=tests/translate/test_sentinel_ass_vtt.py --ignore=tests/translate/test_validate_allowlist.py --ignore=tests/output/test_sidecar_format.py --ignore=tests/discover/test_gap_format.py -q
```
</verification>

<success_criteria>
- 16 fixture files exist in tests/fixtures/ with correct structural content (ASS section headers, karaoke tags, drawing commands, VTT WEBVTT header, cue settings, STYLE/REGION blocks)
- tests/codec/ package exists with __init__.py and 5 test files
- 6 additional test files in existing test dirs cover all Wave 0 gaps from 09-VALIDATION.md
- pytest tests/codec/ -q produces only xfail/expected failures, zero collection ERRORs from syntax
- tests/translate/test_timecode.py SRT assertions pass green (no regression); ASS/VTT assertions xfail
- tests/output/test_sidecar_format.py SRT sidecar regression guard passes green
</success_criteria>

<output>
Create `.planning/phases/09-multi-format-ass-ssa-vtt/09-01-SUMMARY.md` when done.
</output>
