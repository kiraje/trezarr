---
phase: 13-backend-episodes-enrichment
plan: 02
type: execute
wave: 1
depends_on:
  - "13-01"
files_modified:
  - trezarr/translate/engine.py
  - trezarr/output/ledger_sqla.py
autonomous: true
requirements:
  - API-02

must_haves:
  truths:
    - "D-06 (CRITICAL): engine.py Step 11 success-path ledger.record call now includes series_id=_ledger_series_id where _ledger_series_id = str(arr_series_id) if eligible_item is not None and arr_series_id else None — the D-06 engine test turns GREEN after this fix"
    - "D-05 (bulk translated_counts_for_series): ledger_sqla.py has a new module-level async function translated_counts_for_series(session_factory, series_ids) that executes one GROUP BY aggregate query and returns {str(series_id): count} of status='done' ProcessedFile rows — the test_translated_counts_for_series test turns GREEN after this change"
    - "D-06 fix ordering: the engine series_id fix lands in this wave (Wave 1) BEFORE the library.py translated_count work in Wave 2 — translated_count will count future translations correctly after this fix"
    - "API-02 count helper foundation: translated_counts_for_series accepts list[int] series_ids, converts to str_ids = [str(sid) for sid in series_ids], uses ProcessedFile.series_id.in_(str_ids) (parameterized — T-13-02 SQL injection mitigation), groups by ProcessedFile.series_id, returns {} when series_ids is empty"
    - "No backfill: existing ProcessedFile rows with series_id=NULL are not touched — translated_count reflects only future translations after the D-06 fix, as documented per RESEARCH Open Q3"
    - "The three quarantine paths in engine.py (lines ~812, ~911, ~1080) intentionally remain without series_id — they have no eligible_item context; only the success path (Step 11) is fixed"
    - "sqlalchemy func imported: from sqlalchemy import func, select replaces from sqlalchemy import select in ledger_sqla.py"
    - "Full test suite remains green: uv run pytest tests/ -q passes with no regressions"
  artifacts:
    - path: "trezarr/translate/engine.py"
      provides: "D-06 fix: success-path LedgerEntry now carries series_id=str(arr_series_id)"
      contains: "_ledger_series_id"
    - path: "trezarr/output/ledger_sqla.py"
      provides: "bulk translated_counts_for_series aggregate helper"
      contains: "translated_counts_for_series"
  key_links:
    - from: "trezarr/translate/engine.py"
      to: "trezarr/output/ledger.py:LedgerEntry.series_id"
      via: "series_id=_ledger_series_id at Step 11"
      pattern: "_ledger_series_id"
    - from: "trezarr/output/ledger_sqla.py:translated_counts_for_series"
      to: "trezarr/bible/models.py:ProcessedFile"
      via: "select(ProcessedFile.series_id, func.count()).where(...).group_by(...)"
      pattern: "ProcessedFile\\.series_id\\.in_"
---

<objective>
Wave 1: D-06 engine fix + bulk count helper.

Purpose: Fix the load-bearing prerequisite (series_id on the ledger success path) so translated_count is non-zero for future translations, then add the DB aggregate helper that the library.py counts extension will call in Wave 2.

Output: engine.py Step 11 carries series_id; ledger_sqla.py exposes translated_counts_for_series; the D-06 test and count-helper test turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-backend-episodes-enrichment/13-CONTEXT.md
@.planning/phases/13-backend-episodes-enrichment/13-RESEARCH.md
@.planning/phases/13-backend-episodes-enrichment/13-PATTERNS.md
@.planning/phases/13-backend-episodes-enrichment/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-06 engine fix — add series_id to success-path LedgerEntry</name>
  <files>trezarr/translate/engine.py</files>
  <read_first>
    Read trezarr/translate/engine.py lines 780-800 (arr_series_id assignment at line 786: `arr_series_id = getattr(media_item, "series_id", None) or 0`) and lines 1090-1110 (Step 11 ledger.record call at lines 1097-1103 — the exact call site being modified). Also read 13-PATTERNS.md §"trezarr/translate/engine.py — D-06 series_id fix" for the exact delta (two lines to add immediately before the existing ledger.record call).
  </read_first>
  <action>
    At the Step 11 LedgerEntry construction site (engine.py lines ~1097-1103), insert exactly two lines immediately before the `await ledger.record(LedgerEntry(` call:

    ```
    _ledger_series_id: str | None = (
        str(arr_series_id) if eligible_item is not None and arr_series_id else None
    )
    ```

    Then add `series_id=_ledger_series_id,` as a new keyword argument inside the existing LedgerEntry(…) call (after the translated_at= line). No other change to engine.py. No import changes needed — arr_series_id is already in scope at that point (set at line 786: `arr_series_id = getattr(media_item, "series_id", None) or 0`; eligible_item is the function parameter).

    Do NOT touch the three quarantine paths at lines ~812, ~911, ~1080 — they intentionally omit series_id (no eligible_item context at those sites).

    The change is exactly as shown in 13-RESEARCH.md §"D-06 CRITICAL: Exact remediation" and 13-PATTERNS.md §engine.py delta. The full fix is 3 lines total (2 new lines + 1 argument added to the existing call).
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_engine.py::test_ledger_records_series_id_on_success -xvs 2>&1 | tail -15</automated>
  </verify>
  <done>
    test_ledger_records_series_id_on_success turns GREEN (passes, no longer xfail). All other engine tests remain GREEN. `uv run pytest tests/translate/test_engine.py -q` shows all passing, no failures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add translated_counts_for_series bulk helper to ledger_sqla.py</name>
  <files>trezarr/output/ledger_sqla.py</files>
  <read_first>
    Read trezarr/output/ledger_sqla.py lines 1-50 (imports, especially `from sqlalchemy import select` at line ~31) and lines 60-130 (check + check_by_output_path methods to learn the `async with self._session_factory() as session:` + `await session.execute(select(...).where(...))` pattern). Also read 13-PATTERNS.md §"trezarr/output/ledger_sqla.py" for the exact delta: add `func` to the sqlalchemy import line, then append the translated_counts_for_series function at the module bottom.
  </read_first>
  <action>
    Two changes to trezarr/output/ledger_sqla.py:

    1. On the existing `from sqlalchemy import select` line, add `func` so it reads `from sqlalchemy import func, select`. This is the only import change.

    2. Append the following module-level async function at the bottom of the file (after the last existing function, outside the LedgerSQLA class):

    Function name: `translated_counts_for_series`
    Signature: `async def translated_counts_for_series(session_factory: async_sessionmaker, series_ids: list[int]) -> dict[str, int]:`
    Docstring: documents that it returns {str(series_id): count} of status='done' ProcessedFile rows using one GROUP BY aggregate; keys are str because ProcessedFile.series_id is Mapped[str | None]; returns 0 (via missing key) for absent series_ids.
    Body:
    - Early return `{}` if not series_ids
    - `str_ids = [str(sid) for sid in series_ids]`
    - Open session: `async with session_factory() as session:`
    - Execute: `select(ProcessedFile.series_id, func.count().label("n")).where(ProcessedFile.series_id.in_(str_ids), ProcessedFile.status == "done").group_by(ProcessedFile.series_id)`
    - Return: `{row.series_id: row.n for row in result}`

    The session pattern uses `session_factory` as a parameter (not `self._session_factory`) because this is a module-level function, not a method. This matches the 13-PATTERNS.md annotation exactly.

    Security: ProcessedFile.series_id.in_(str_ids) uses SQLAlchemy's parameterized IN clause — T-13-02 SQL injection is mitigated; the list values are never interpolated as raw SQL.
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_library_api.py::test_translated_counts_for_series -xvs 2>&1 | tail -15</automated>
  </verify>
  <done>
    test_translated_counts_for_series turns GREEN. `uv run pytest tests/ -q` shows all tests passing with no regressions. translated_counts_for_series is importable as `from trezarr.output.ledger_sqla import translated_counts_for_series`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web route → DB | translated_counts_for_series receives series_ids from Sonarr API response (server-side ints); they become str_ids for the SQL .in_() clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | Tampering | ledger_sqla.py translated_counts_for_series | mitigate | SQLAlchemy parameterized `.in_(str_ids)` — values are never interpolated as raw SQL; parameterized by the ORM |
| T-13-W1-01 | Tampering | engine.py Step 11 LedgerEntry | accept | series_id is `str(arr_series_id)` derived from server-side Sonarr metadata, not user input; no injection surface |
</threat_model>

<verification>
uv run pytest tests/ -q

Expected: All existing tests pass. test_translated_counts_for_series and test_ledger_records_series_id_on_success turn GREEN. Other Phase-13 stubs remain XFAIL.
</verification>

<success_criteria>
- engine.py Step 11: `_ledger_series_id = str(arr_series_id) if eligible_item is not None and arr_series_id else None` present; `series_id=_ledger_series_id` in LedgerEntry kwargs
- ledger_sqla.py: `translated_counts_for_series` importable at module level; uses `func.count()` + `.in_(str_ids)` + `.group_by(ProcessedFile.series_id)`
- `uv run pytest tests/translate/test_engine.py::test_ledger_records_series_id_on_success -xq` exits 0 GREEN
- `uv run pytest tests/web/test_library_api.py::test_translated_counts_for_series -xq` exits 0 GREEN
- `uv run pytest tests/ -q` no regressions
</success_criteria>

<output>
Create `.planning/phases/13-backend-episodes-enrichment/13-02-SUMMARY.md` when done
</output>
