---
phase: 02-mechanical-translation-core-validation-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/translate/__init__.py
  - tests/translate/test_batching.py
  - tests/translate/test_engine.py
  - tests/translate/test_validate.py
  - tests/translate/test_sentinel.py
  - tests/output/__init__.py
  - tests/output/test_write.py
  - tests/output/test_ledger.py
autonomous: true
requirements: [ENG-02, ENG-03, ENG-06, ENG-07, FMT-05]

must_haves:
  truths:
    - "All Phase 2 test stubs are collected by pytest with no ImportError (modules not yet implemented — stubs use xfail or skip guards)"
    - "Each test file covers its corresponding requirement: test_batching.py→ENG-02, test_engine.py→ENG-03+ENG-06, test_validate.py→ENG-06, test_sentinel.py→D-12, test_write.py→FMT-05, test_ledger.py→ENG-07"
    - "uv run pytest --collect-only tests/translate/ tests/output/ exits 0 (collection succeeds even though implementations are absent)"
  artifacts:
    - path: "tests/translate/__init__.py"
      provides: "translate test package marker"
    - path: "tests/translate/test_batching.py"
      provides: "ENG-02 RED test stubs (scene-gap, token-budget, max-cue fallback)"
    - path: "tests/translate/test_engine.py"
      provides: "ENG-03 + ENG-06 retry/quarantine RED test stubs"
    - path: "tests/translate/test_validate.py"
      provides: "All 7 gate-check RED test stubs (ENG-06)"
    - path: "tests/translate/test_sentinel.py"
      provides: "D-12 sentinel extract/reinsert RED test stubs"
    - path: "tests/output/__init__.py"
      provides: "output test package marker"
    - path: "tests/output/test_write.py"
      provides: "FMT-05 naming + atomic write RED test stubs"
    - path: "tests/output/test_ledger.py"
      provides: "ENG-07 skip/regenerate/foreign/quarantine-retry RED test stubs"
  key_links:
    - from: "tests/translate/test_batching.py"
      to: "trezarr/translate/batching.py"
      via: "deferred import inside test function body (not yet implemented)"
      pattern: "from trezarr.translate.batching import"
    - from: "tests/translate/test_validate.py"
      to: "trezarr/translate/validate.py"
      via: "deferred import"
      pattern: "from trezarr.translate.validate import"
---

## Phase Goal

**As a** Phase 3 integration, **I want to** call `translate_file(path, settings)` and receive a `TranslationResult`, **so that** a discovered SRT is translated and written as a Vietnamese sidecar automatically.

<objective>
Install the complete pytest scaffolding (RED suite) for all Phase 2 modules before any implementation exists. Every test stub uses deferred imports (inside the test function body) so collection succeeds even when the implementation files are absent. This is the Nyquist gate — no later task may be implemented without its corresponding test asserting the correct behavior in the RED state first.

Purpose: Provide automated verification commands for all subsequent plans so each task has an `<automated>` verify command that references a real test file, not a placeholder.
Output: 8 test files (2 package markers + 6 test modules) covering all Phase 2 requirements.
</objective>

<execution_context>
@/Users/dustin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dustin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-CONTEXT.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-VALIDATION.md
@tests/conftest.py
@tests/llm/test_client_tiers.py
@tests/codec/test_srt_model.py
@tests/codec/test_srt_roundtrip.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test package markers and test_batching + test_sentinel stubs</name>
  <files>
    tests/translate/__init__.py
    tests/translate/test_batching.py
    tests/translate/test_sentinel.py
    tests/output/__init__.py
  </files>

  <read_first>
    - tests/conftest.py — settings_factory fixture, deferred-import pattern, asyncio_mode=auto
    - tests/codec/test_srt_model.py — synchronous unit test pattern with deferred imports inside test function body and inline SubLine/SubDoc construction
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md — "Deferred imports inside test functions" section and "Test for pure-Python utilities" section
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md — Q2 batch-packing algorithm, Q4 sentinel format, "Wave 0 Gaps" list
  </read_first>

  <action>
    Create tests/translate/__init__.py and tests/output/__init__.py as empty zero-byte files (matching the pattern of trezarr/subtitles/__init__.py and trezarr/llm/__init__.py — no imports, no __all__).

    Create tests/translate/test_batching.py covering ENG-02. All imports from trezarr.translate.batching must be deferred inside each test function body (not at module top), so pytest collection succeeds before the module is implemented. Use pytest.importorskip or a deferred import guarded with pytest.skip inside the test body.

    Required test function names for test_batching.py (per 02-VALIDATION.md requirement-to-command map):
    - test_token_budget_respected — verifies no batch exceeds the char budget computed from TrezarrSettings defaults; construct a SubDoc with enough SubLines to force multiple batches and assert every batch's total len(cue.text) <= budget
    - test_scene_gap_boundary — verifies a time gap >= translate_scene_gap_ms triggers a batch split; construct two SubLines whose gap_ms >= 2000 and assert they land in different batches
    - test_max_cue_fallback — verifies translate_max_cues_per_batch is enforced when no scene gap exists; construct (max_cues + 1) SubLines with no gaps and assert they produce 2 batches
    - test_single_cue_never_split — verifies a cue that alone exceeds budget is emitted as a one-cue batch (not skipped or raised)
    - test_context_before_after_attached — verifies the Batch objects returned have context_before and context_after lists populated (K = translate_context_lines_k per settings)

    Each test function: import SubLine, SubDoc, TrezarrSettings, and batch_subdoc INSIDE the body. Use pytest.importorskip("trezarr.translate.batching") at the top of each test function body (not module top) — this makes the test skip cleanly when the module does not exist yet (during Wave 0) and will auto-run once the module exists. This matches the xfail(strict=False) stub convention used in Phase 1.

    Create tests/translate/test_sentinel.py covering D-12. Required test functions:
    - test_extract_no_tags — plain text with no tags returns (text, {}) with empty sentinel_map
    - test_extract_italic_tag — text with <i>word</i> extracts the tag into sentinel_map; cleaned text contains <<T0>> not <i>
    - test_extract_multiple_tags — multiple different tags each get a unique <<TN>> key
    - test_reinsert_round_trips — extracted text reinserted returns original text with integrity_ok=True
    - test_reinsert_missing_sentinel — if <<T0>> is removed from translated text before reinsert, integrity_ok=False
    - test_reinsert_orphan_sentinel — if <<T0>> remains in text after reinsert (e.g. sentinel_map is empty), integrity_ok=False
    - test_extract_ass_override_tag — text with {\an8} extracts the ASS override tag correctly

    All imports deferred inside test function bodies. Use pytest.importorskip("trezarr.translate.sentinel") at the start of each test function body.
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && uv run pytest --collect-only tests/translate/test_batching.py tests/translate/test_sentinel.py -q 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - tests/translate/__init__.py exists and is empty (0 bytes)
    - tests/output/__init__.py exists and is empty (0 bytes)
    - pytest --collect-only tests/translate/test_batching.py exits 0 and lists exactly 5 test items (test_token_budget_respected, test_scene_gap_boundary, test_max_cue_fallback, test_single_cue_never_split, test_context_before_after_attached)
    - pytest --collect-only tests/translate/test_sentinel.py exits 0 and lists exactly 7 test items
    - Running uv run pytest tests/translate/test_batching.py tests/translate/test_sentinel.py -q results in all tests skipping (not failing with ImportError) because the module does not exist yet
    - No import statement for trezarr.translate.batching or trezarr.translate.sentinel appears at module top level (outside a test function body) — grep -n "^from trezarr" tests/translate/test_batching.py must return 0 lines
  </acceptance_criteria>

  <done>tests/translate/__init__.py and tests/output/__init__.py are zero-byte files; test_batching.py has 5 test functions and test_sentinel.py has 7 test functions, all collecting cleanly and skipping (not ImportErroring) when the implementation modules are absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test_validate, test_engine, test_write, and test_ledger stubs</name>
  <files>
    tests/translate/test_validate.py
    tests/translate/test_engine.py
    tests/output/test_write.py
    tests/output/test_ledger.py
  </files>

  <read_first>
    - tests/conftest.py — settings_factory fixture, AsyncMock pattern
    - tests/llm/test_client_tiers.py — AsyncMock + patch.object pattern for mocking LLMClient.call()
    - tests/codec/test_srt_roundtrip.py — tmp_path fixture pattern for file I/O tests
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-RESEARCH.md — Q5 validation gate (all 7 checks), Q6 atomic write, Q7 ledger behavior table
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-PATTERNS.md — "Test for pure-Python utilities" and "Test for async engine functions" and "Test for file I/O" sections
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-VALIDATION.md — Wave 0 Gaps list and Per-Task Verification Map
  </read_first>

  <action>
    Create tests/translate/test_validate.py covering all 7 gate checks (ENG-06). All deferred imports (pytest.importorskip("trezarr.translate.validate") inside each test function body). Required test functions matching 02-VALIDATION.md's requirement-to-command map:
    - test_gate_count_mismatch — translated SubDoc has one fewer line than source; assert GateError raised with failure.check == 1
    - test_gate_empty_line — one translated line has text = "   " (whitespace-only); assert GateError with check == 2
    - test_gate_vi_ratio — translated lines are byte-identical to English source (no Vietnamese diacritics); assert GateError with check == 3 (VI diacritic ratio below threshold)
    - test_gate_vi_ratio_allowlist_exempt — lines composed entirely of punctuation/digits/symbols are excluded from the ratio denominator; a file of all-punctuation lines should not trigger check 3
    - test_gate_timecode_mutation — translated SubLine has a different start_tc string; assert GateError with check == 4
    - test_gate_nonmonotonic — two adjacent translated SubLines where start_tc of line[1] < end_tc of line[0]; assert GateError with check == 5
    - test_gate_sentinel_orphan — a translated SubLine.text contains "<<T0>>" (unreinserted sentinel); assert GateError with check == 6
    - test_gate_utf8_invalid — a translated SubLine.text contains a lone surrogate (chr(0xD800)); assert GateError with check == 7
    - test_gate_all_checks_pass — a correctly translated SubDoc (Vietnamese diacritics, same cue count, same timecodes, no sentinels, monotonic) returns None (no exception)

    Create tests/translate/test_engine.py covering ENG-03 and ENG-06 retry/quarantine. Mix of async def (for engine function tests) and sync def (for prompt-building tests). All deferred imports. Required test functions:
    - test_context_window_prompt — call the prompt-building function with context_before, batch_texts, context_after; assert "[CONTEXT" appears before and after "[LINES TO TRANSLATE]"; assert "[context]" prefixes appear on context lines; assert numbered "[1]", "[2]" lines appear in the LINES section
    - test_numbered_line_parse — given a model response string "[1] Xin chào\n[2] Tôi đến đây", the response parser returns ["Xin chào", "Tôi đến đây"]
    - test_batch_retry — async; mock LLMClient.call() to return a bad response (empty line) on attempt 1 and a valid response on attempt 2; assert the translate function retries exactly once and returns the correct translated text; assert BatchValidationError is not raised externally
    - test_quarantine_on_retry_exhaustion — async; mock LLMClient.call() to always return empty lines; assert TranslationError (or equivalent) is raised after exhausting translate_batch_retry_attempts retries
    - test_translate_file_skip_unchanged — async; set up a ledger entry with status="done" and a matching content hash; assert translate_file returns TranslationResult(status="skipped") without calling LLMClient.call()

    Create tests/output/test_write.py covering FMT-05. Synchronous (file I/O, no async). Required test functions:
    - test_sidecar_naming — given source path "Show.S01E01.en.srt", write_vi_sidecar returns a path ending in "Show.S01E01.vi.srt"
    - test_sidecar_naming_no_lang_code — given source "Show.S01E01.srt" (no lang code), returns "Show.S01E01.vi.srt"
    - test_atomic_write_crash_safety — write_vi_sidecar creates the destination file; no .tmp file remains after completion
    - test_utf8_output — output file bytes decode as UTF-8 without error even when source SubDoc encoding is latin-1

    Create tests/output/test_ledger.py covering ENG-07. Mix of sync and async. Required test functions:
    - test_skip_unchanged — ledger has entry with status="done" and correct content_hash; Ledger.check() returns the entry; calling skip-logic with matching hash produces "skipped" outcome
    - test_regenerate_on_hash_change — ledger has status="done" but stored content_hash != current source hash; verify the function proceeds to translation (not skip)
    - test_foreign_srt_not_clobbered — dest .vi.srt exists on disk but source_path is NOT in the ledger; verify the function returns "skipped" and logs (does not attempt to translate or clobber)
    - test_quarantine_retry — ledger entry has status="quarantined"; verify the function proceeds to translation (not skip)
    - test_ledger_persists_atomically — after Ledger.record() is called, the JSON file on disk contains the entry; test that the file is valid JSON
    - test_ledger_corrupt_fallback — write malformed JSON to the ledger path; construct Ledger; verify _data is empty dict and no exception is raised (fallback to empty)

    All test functions use deferred imports. Async test functions use async def (asyncio_mode = "auto" from pyproject.toml — no @pytest.mark.asyncio decorator needed, matching Phase 1 convention).
  </action>

  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && uv run pytest --collect-only tests/translate/ tests/output/ -q 2>&amp;1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - uv run pytest --collect-only tests/translate/ tests/output/ -q exits 0 and lists exactly: 5 items in test_batching, 7 in test_sentinel, 9 in test_validate, 5 in test_engine, 4 in test_write, 6 in test_ledger = 36 total items
    - uv run pytest tests/translate/ tests/output/ -x -q completes without ImportError (all tests skip cleanly because implementation modules do not exist yet)
    - No trezarr.translate.* or trezarr.output.* import appears at module top level in any test file (grep -rn "^from trezarr" tests/translate/ tests/output/ returns 0 lines)
    - Async test functions (test_batch_retry, test_quarantine_on_retry_exhaustion, test_translate_file_skip_unchanged) use async def without @pytest.mark.asyncio decorator
    - uv run pytest -q (full suite including Phase 1 tests) exits 0 — no Phase 1 regressions
  </acceptance_criteria>

  <done>All 36 Phase 2 test stubs are collected cleanly, skip without ImportError when implementation modules are absent, and the full suite (uv run pytest -q) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → test framework | Test collection is local Python execution; no external trust boundary here |
| deferred import → unimplemented module | pytest.importorskip() is the guard that prevents collection failure when modules don't exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-W0-01 | Tampering | test/conftest.py settings_factory | accept | Test-safe API key literal "test-key" is meaningless; SecretStr masking is a Phase-1 control already in place |
| T-02-W0-02 | Information Disclosure | test stubs | accept | Stub files contain no secrets or real paths; hardcoded paths in tests use tmp_path fixture (ephemeral) |
| T-02-SC | Tampering | tenacity (new package install Wave 1) | mitigate | Package Legitimacy Audit in 02-RESEARCH.md confirms tenacity [OK] — no checkpoint needed; slopcheck passed 2026-05-31 |
</threat_model>

<verification>
uv run pytest --collect-only tests/translate/ tests/output/ -q exits 0 with 36 items collected.
uv run pytest tests/translate/ tests/output/ -x -q exits 0 with all tests skipped (no ImportError, no failure).
uv run pytest -q (full suite) exits 0 — no Phase 1 regressions.
</verification>

<success_criteria>
All 36 test stubs are collected by pytest. No test ImportErrors during Wave 0 (all modules use deferred imports). The full Phase 1 suite remains green. Every subsequent plan's automated verify command references a real test file that already exists.
</success_criteria>

<output>
Create `.planning/phases/02-mechanical-translation-core-validation-gate/02-01-SUMMARY.md` when done.
</output>
