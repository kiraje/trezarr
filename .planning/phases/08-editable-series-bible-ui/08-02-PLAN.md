---
phase: 08-editable-series-bible-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - trezarr/translate/reconcile.py
  - trezarr/web/worker.py
  - trezarr/bible/store.py
autonomous: true
requirements:
  - BIBLE-08
  - BIBLE-09

must_haves:
  truths:
    - "reconcile.py exports KNOWN_PRONOUN_TERMS_SELF, KNOWN_PRONOUN_TERMS_ADDRESS, and KINSHIP_RECIPROCAL now contains bác/cháu, chú/cháu, cô/cháu, thầy/em, tao/mày forward + reverse pairs (D-86, D-90)"
    - "worker.py exports get_series_lock(series_id: int) -> asyncio.Lock — lazily creates lock via _series_locks.setdefault (D-81)"
    - "store.py exports apply_human_edit_character, apply_human_edit_address_pair, load_field_history, load_all_series — all four new functions (D-79, D-80, D-82, D-83)"
    - "test_locked_field_survives_merge, test_locked_fields_persisted, test_bible_event_emitted_on_lock pass (GREEN)"
    - "test_locked_pair_survives_reconcile and test_locked_pair_propagates_to_next_episode pass (GREEN — BIBLE-09 proven at store layer)"
    - "test_kinship_reciprocal_bac_chau passes GREEN (D-90 H1 fix confirmed)"
    - "No Alembic migration created (D-79: locked_fields JSON already exists; no schema change needed)"
  artifacts:
    - path: "trezarr/translate/reconcile.py"
      provides: "KNOWN_PRONOUN_TERMS_SELF, KNOWN_PRONOUN_TERMS_ADDRESS constants + KINSHIP_RECIPROCAL D-90 gap fill"
      contains: "KNOWN_PRONOUN_TERMS_SELF"
    - path: "trezarr/web/worker.py"
      provides: "get_series_lock(series_id) public accessor (D-81)"
      exports: ["get_series_lock"]
    - path: "trezarr/bible/store.py"
      provides: "apply_human_edit_character, apply_human_edit_address_pair (D-80), load_field_history (D-82), load_all_series (D-83)"
      exports: ["apply_human_edit_character", "apply_human_edit_address_pair", "load_field_history", "load_all_series"]
  key_links:
    - from: "trezarr/web/routes/bible.py (Plan 08-03)"
      to: "trezarr/bible/store.apply_human_edit_character"
      via: "deferred import inside route handler + get_series_lock acquisition"
      pattern: "async with get_series_lock"
    - from: "trezarr/bible/store.apply_human_edit_character"
      to: "trezarr/bible/models.BibleEvent"
      via: "session.add(BibleEvent(source='lock', ...)) inside session.begin()"
      pattern: "BibleEvent.*source.*lock"
    - from: "trezarr/translate/reconcile.reconcile_attributions"
      to: "KINSHIP_RECIPROCAL"
      via: "reciprocal pass lookup reconcile.py:353-358"
      pattern: "KINSHIP_RECIPROCAL.get"
---

## Phase Goal

**As an** operator, **I want to** have a proven lock-write path (D-80) that atomically sets a Bible field, toggles the lock, and emits an audit event — and a proven `KINSHIP_RECIPROCAL` that covers all relationships the editor exposes — **so that** the REST router and SPA built in Plans 03-04 can trust the store layer is correct.

<objective>
Implement the three server-side engine pieces that Plans 03-04 build on:
1. reconcile.py: KNOWN_PRONOUN_TERMS constants + KINSHIP_RECIPROCAL D-90 additive gap fill (bác/cháu, chú/cháu, cô/cháu, thầy/em, tao/mày).
2. worker.py: get_series_lock(series_id) public accessor exposing _series_locks.
3. store.py: apply_human_edit_character, apply_human_edit_address_pair, load_field_history, load_all_series.

Purpose: All BIBLE-09 propagation tests go GREEN before the REST router exists — proving the lock invariant at the store layer. The REST layer in Plan 03 becomes a thin wiring task.
Output: Modified reconcile.py, worker.py, store.py; GREEN test_human_edit.py + test_kinship_reciprocal_bac_chau.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-editable-series-bible-ui/08-CONTEXT.md
@.planning/phases/08-editable-series-bible-ui/08-PATTERNS.md
@.planning/phases/08-editable-series-bible-ui/08-RESEARCH.md
@trezarr/translate/reconcile.py
@trezarr/web/worker.py
@trezarr/bible/store.py
@tests/bible/test_human_edit.py
@tests/translate/test_reconcile.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill KINSHIP_RECIPROCAL gaps + add KNOWN_PRONOUN_TERMS + expose get_series_lock accessor</name>
  <files>trezarr/translate/reconcile.py, trezarr/web/worker.py</files>
  <read_first>
    - trezarr/translate/reconcile.py:35-54 — existing KINSHIP_RECIPROCAL dict literal; verify exact keys present before adding D-90 gaps; add new keys via .update() after the dict literal, not by mutating the literal itself (regression safety per D-90)
    - trezarr/translate/reconcile.py (full file, top section) — locate the line immediately after the KINSHIP_RECIPROCAL dict where KNOWN_PRONOUN_TERMS_SELF and KNOWN_PRONOUN_TERMS_ADDRESS constants should be inserted (before SAFE_DEFAULT_* per 08-PATTERNS.md §reconcile.py)
    - trezarr/web/worker.py:51 — _series_locks dict declaration; and :412-417 — existing setdefault acquire pattern to copy exactly for get_series_lock
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §reconcile.py (lines 390-445) — exact D-90 additions dict and KNOWN_PRONOUN_TERMS_SELF/ADDRESS constants to add verbatim
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §worker.py (lines 202-234) — get_series_lock function body + note that accessor does NOT use the -1 sentinel (Bible series always have real series_id)
  </read_first>
  <behavior>
    - KINSHIP_RECIPROCAL now round-trips bác/cháu: KINSHIP_RECIPROCAL[("bác","cháu")] == ("cháu","bác") and KINSHIP_RECIPROCAL[("cháu","bác")] == ("bác","cháu")
    - Same for chú/cháu, cô/cháu, thầy/em, tao/mày — all 10 new pairs present (5 relationships × 2 directions)
    - No existing key in KINSHIP_RECIPROCAL is changed (additive-only — regression safety)
    - KNOWN_PRONOUN_TERMS_SELF and KNOWN_PRONOUN_TERMS_ADDRESS are importable from trezarr.translate.reconcile
    - get_series_lock(42) returns an asyncio.Lock; calling it twice with the same id returns the same object (identity check)
  </behavior>
  <action>
    In trezarr/translate/reconcile.py:

    After the KINSHIP_RECIPROCAL dict literal (line ~54), add a KINSHIP_RECIPROCAL.update() call with the D-90 pairs per 08-PATTERNS.md §reconcile.py lines 419-430. The 5 relationships × 2 directions = 10 new entries:
    ("bác","cháu"), ("cháu","bác"), ("chú","cháu"), ("cháu","chú"), ("cô","cháu"), ("cháu","cô"), ("thầy","em"), ("em","thầy"), ("tao","mày"), ("mày","tao").

    Immediately after the .update() call, add KNOWN_PRONOUN_TERMS_SELF and KNOWN_PRONOUN_TERMS_ADDRESS as module-level list constants per 08-PATTERNS.md §reconcile.py lines 433-445. Add a comment: "D-86: single source of truth — exposed via /api/pronouns; never duplicated in the frontend."

    In trezarr/web/worker.py:

    After the _series_locks dict declaration (line 51), add the public function get_series_lock(series_id: int) -> asyncio.Lock per 08-PATTERNS.md §worker.py lines 224-233. Use _series_locks.setdefault(series_id, asyncio.Lock()) — NO -1 sentinel (Bible endpoints always have a real series_id). Add docstring: "Return the per-series asyncio.Lock (D-81). Lazily creates on first call."

    Do NOT modify any other code in either file. The D-90 additions are purely additive; no existing logic is touched.
  </action>
  <verify>
    <automated>uv run pytest tests/translate/test_reconcile.py::test_kinship_reciprocal_bac_chau -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    test_kinship_reciprocal_bac_chau passes GREEN (not xfail). `from trezarr.translate.reconcile import KNOWN_PRONOUN_TERMS_SELF, KNOWN_PRONOUN_TERMS_ADDRESS, KINSHIP_RECIPROCAL` succeeds in a REPL. `from trezarr.web.worker import get_series_lock` succeeds. Full suite still green: `uv run pytest -x -q` reports 0 failures.
  </acceptance_criteria>
  <done>KINSHIP_RECIPROCAL contains all 10 new D-90 pairs. KNOWN_PRONOUN_TERMS_SELF and KNOWN_PRONOUN_TERMS_ADDRESS exported. get_series_lock(series_id) exported from worker.py. test_kinship_reciprocal_bac_chau GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement apply_human_edit_character, apply_human_edit_address_pair, load_field_history, load_all_series in store.py</name>
  <files>trezarr/bible/store.py</files>
  <read_first>
    - trezarr/bible/store.py:259-264 — MERGEABLE_FIELDS whitelist (field allowlist for security validation; human-edit writers must check field against this before any setattr)
    - trezarr/bible/store.py:601-717 — _upsert_address_pair_in_session (the template to mirror for apply_human_edit_address_pair: SELECT by identity, read locked off row, setattr, reassign locked_fields list, session.add BibleEvent)
    - trezarr/bible/store.py:825-838 — public upsert_character transaction-owner wrapper (copy this async with session_factory / async with session.begin() pattern for the new public functions)
    - trezarr/bible/store.py:298-402 — _merge_inferred_in_session: the WR-01 re-read pattern (`fresh_row = await session.get(type(row), row.id)`) to copy for in-txn re-read
    - trezarr/bible/store.py:1007-1023 — load_address_map session shape (mirror for load_field_history: replace AddressMap with BibleEvent, add .where filters, .order_by(BibleEvent.created_at.desc()).limit(limit))
    - trezarr/bible/store.py:210-221 — load_series_bible session shape (mirror for load_all_series: select(Series).order_by(Series.id), SeriesDTO.model_validate(row, from_attributes=True))
    - .planning/phases/08-editable-series-bible-ui/08-PATTERNS.md §store.py (lines 36-200) — full apply_human_edit_character function shape and all key constraints (WR-01, D-80, D-32, D-39)
    - .planning/phases/08-editable-series-bible-ui/08-RESEARCH.md §Pattern 1 — Lock-Aware Store Writer pattern with open question answer: emit BibleEvent even for lock-toggle-without-value-change (old_value == new_value is fine; event signals explicit lock)
    - .planning/phases/08-editable-series-bible-ui/08-CONTEXT.md §D-80 — the four invariants: (1) session.get inside txn (WR-01), (2) setattr new value, (3) locked_fields = [*row.locked_fields, field] REASSIGN not append, (4) BibleEvent(source="lock") same txn, (5) return DTO not ORM row (D-39)
    - .planning/phases/08-editable-series-bible-ui/08-CONTEXT.md §D-83 — address pair add requires existing character_id; apply_human_edit_address_pair edits an existing pair by id; the D-87 non-None guard must be checked here: raise ValueError if lock=True and either self_term or address_term is None/whitespace
    - trezarr/bible/models.py:85,126,154,185 — locked_fields JSON column locations on Series, Character, TermDictionary, AddressMap rows
    - trezarr/bible/models.py:221-264 — BibleEvent model fields: series_id, episode_key, entity_type, entity_id, field, old_value, new_value, source
    - trezarr/bible/dto.py — CharacterDTO, AddressMapDTO, BibleEventDTO, SeriesDTO shapes for return types (D-39: return DTO, never ORM row)
  </read_first>
  <behavior>
    - apply_human_edit_character(session_factory, *, character_id, series_id, field, new_value, lock=False): rejects field not in MERGEABLE_FIELDS["character"] with ValueError before opening session; inside one session.begin(): session.get(Character, character_id) re-reads row (WR-01), setattr(row, field, new_value), reassigns locked_fields as new list (never .append()), session.add(BibleEvent(source="lock", ...)); returns (CharacterDTO, BibleEventDTO)
    - apply_human_edit_address_pair(session_factory, *, address_map_id, series_id, self_term, address_term, lock=False): if lock=True and (not self_term.strip() or not address_term.strip()), raise ValueError("Both self_term and address_term must be non-empty before locking a pair") — the D-87 non-None guard; inside one txn: session.get(AddressMap, address_map_id), set self_term and address_term, reassign locked_fields, emit BibleEvent for each changed field, return (AddressMapDTO, list[BibleEventDTO])
    - load_field_history(session_factory, series_id, entity_type, entity_id, field=None, limit=100): queries BibleEvent filtered by series_id/entity_type/entity_id and optionally field, ordered by created_at desc, LIMIT limit; returns list[BibleEventDTO]
    - load_all_series(session_factory): select(Series).order_by(Series.id); returns list[SeriesDTO]; uses SeriesDTO.model_validate(row, from_attributes=True) (NOT SeriesBibleDTO — no eager-loading for list view)
    - All four functions: never import SQLAlchemy models at the route layer; all models imported within store.py only (D-39 already satisfied by existing store.py imports)
  </behavior>
  <action>
    Add four new public functions to trezarr/bible/store.py after the existing `upsert_address_pair` function (around line 950, before `get_character`). The file already imports all needed models and DTOs; verify imports at lines 66-95 before adding any new imports.

    `apply_human_edit_character`: Pre-session check: `if field not in MERGEABLE_FIELDS.get("character", frozenset()): raise ValueError(...)`. One `async with session_factory() as session: async with session.begin():` block — (1) `row = await session.get(Character, character_id)` (WR-01 re-read inside txn); (2) guard `row is None or row.series_id != series_id → raise ValueError`; (3) `old_value = getattr(row, field); setattr(row, field, new_value)`; (4) build new locked_fields list: `locked_list = list(row.locked_fields or [])`; add or remove field depending on `lock` flag; `row.locked_fields = locked_list` (REASSIGN — never .append()); (5) `evt = BibleEvent(series_id=series_id, episode_key=None, entity_type="character", entity_id=character_id, field=field, old_value=old_value, new_value=new_value, source="lock")` and `session.add(evt)`; (6) after session.begin() exits: return `(CharacterDTO.model_validate(row, from_attributes=True), BibleEventDTO.model_validate(evt, from_attributes=True))`. expire_on_commit=False (already set by build_session_factory) keeps attributes live post-commit.

    `apply_human_edit_address_pair`: Takes address_map_id, series_id, self_term, address_term, lock=False. D-87 pre-check BEFORE opening session: if lock and (not self_term or not self_term.strip() or not address_term or not address_term.strip()): raise ValueError("Both self_term and address_term must be non-empty before locking a pair"). One txn: `row = await session.get(AddressMap, address_map_id)` (WR-01); guard series_id match; strip both terms; collect events list for changed fields; reassign locked_fields if lock=True: lock both "self_term" and "address_term" (they come as a pair per D-87); return (AddressMapDTO.model_validate(row, ...), [BibleEventDTO.model_validate(e, ...) for e in events]).

    `load_field_history`: Mirror load_address_map pattern (store.py:1007-1023). One async with session_factory() as session (no begin() — read-only). stmt = select(BibleEvent).where(BibleEvent.series_id == series_id, BibleEvent.entity_type == entity_type, BibleEvent.entity_id == entity_id); if field is not None: stmt = stmt.where(BibleEvent.field == field); stmt = stmt.order_by(BibleEvent.created_at.desc()).limit(limit); return [BibleEventDTO.model_validate(r, from_attributes=True) for r in rows].

    `load_all_series`: Mirror load_series_bible session shape (store.py:210-221). stmt = select(Series).order_by(Series.id); return [SeriesDTO.model_validate(r, from_attributes=True) for r in rows]. Note: SeriesDTO (not SeriesBibleDTO) — no selectinload needed for list view.
  </action>
  <verify>
    <automated>uv run pytest tests/bible/test_human_edit.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    All 6 tests in test_human_edit.py pass GREEN (no longer xfail). Specifically: test_locked_field_survives_merge, test_locked_fields_persisted, test_bible_event_emitted_on_lock, test_locked_pair_survives_reconcile, test_locked_pair_propagates_to_next_episode all GREEN. Full suite: `uv run pytest -x -q` reports 0 failures. Source check: `grep -c "def apply_human_edit_character\|def apply_human_edit_address_pair\|def load_field_history\|def load_all_series" trezarr/bible/store.py` returns 4.
  </acceptance_criteria>
  <done>4 new store functions implemented. All 6 tests in test_human_edit.py GREEN. BIBLE-09 lock-propagation invariant proven at the store layer before the REST router exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler → store writer | Route passes entity_id, series_id, field, new_value; store writer must not trust these blindly — field validated against MERGEABLE_FIELDS whitelist before setattr |
| store writer → ORM row | WR-01: row re-read from DB inside the transaction; stale DTO never used as the source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | apply_human_edit_character / apply_human_edit_address_pair — field name injection | mitigate | Validate `field` against `MERGEABLE_FIELDS[entity_type]` (frozenset whitelist, store.py:259-264) before any setattr; raise ValueError (HTTP 422 at route layer) for unknown fields |
| T-08-02 | Denial of Service | load_field_history — unbounded history query | mitigate | `LIMIT 100` parameter (default) on all history queries; index `ix_bible_event_series_entity_time` covers the query efficiently |
| T-08-03 | Tampering | apply_human_edit_address_pair — locking pair with empty/None term | mitigate | D-87 pre-check: raise ValueError if lock=True and either term is empty/whitespace; maps to HTTP 422 at route layer; reconcile.py:275 non-None guard is the downstream defense layer |
| T-08-04 | Tampering | JSON locked_fields dirty-tracking bypass | mitigate | REASSIGN locked_fields as new list object (`row.locked_fields = [*row.locked_fields, field]`) — never .append() on plain JSON column (D-80); SQLAlchemy only tracks identity changes on plain JSON columns |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
uv run pytest tests/bible/test_human_edit.py tests/translate/test_reconcile.py::test_kinship_reciprocal_bac_chau -v --tb=short
uv run pytest -x -q
</verification>

<success_criteria>
All 6 test_human_edit.py tests GREEN. test_kinship_reciprocal_bac_chau GREEN. Full suite 0 failures. Four new store functions present in store.py. get_series_lock exported from worker.py. KNOWN_PRONOUN_TERMS_SELF, KNOWN_PRONOUN_TERMS_ADDRESS exported from reconcile.py. No Alembic migration created (D-79).
</success_criteria>

<output>
Create `.planning/phases/08-editable-series-bible-ui/08-02-SUMMARY.md` when done.
</output>
