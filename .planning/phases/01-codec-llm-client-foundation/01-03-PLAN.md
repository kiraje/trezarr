---
phase: 01-codec-llm-client-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - trezarr/config.py
  - trezarr/llm/__init__.py
  - trezarr/llm/client.py
autonomous: true
requirements:
  - ENG-01

must_haves:
  truths:
    - "TrezarrSettings loads values from config.yaml (via YamlConfigSettingsSource) with TREZARR_* env vars taking precedence (D-11)"
    - "API key is a SecretStr — never appears in str(settings), repr(settings), or model_dump() output (D-11)"
    - "LLMClient wraps AsyncOpenAI with max_retries=settings.llm_max_retries (default 4, per D-07) — never uses tenacity"
    - "LLMClient bounds concurrency with asyncio.Semaphore(settings.llm_max_concurrency) (default 4, per D-06)"
    - "Auto-detect tier: attempts json_schema first, falls back to json_object on BadRequestError/UnprocessableEntityError (400/422), then plain text (D-04)"
    - "Pinned mode=json_schema raises BadRequestError on 400 without falling back (D-04)"
  artifacts:
    - path: "trezarr/config.py"
      provides: "TrezarrSettings — pydantic-settings BaseSettings with YamlConfigSettingsSource and SecretStr api_key"
      exports: ["TrezarrSettings", "CONFIG_PATH"]
      contains: "settings_customise_sources"
    - path: "trezarr/llm/client.py"
      provides: "LLMClient — AsyncOpenAI wrapper with semaphore and three-tier structured-output fallback"
      exports: ["LLMClient"]
      contains: "asyncio.Semaphore"
  key_links:
    - from: "trezarr/llm/client.py"
      to: "trezarr/config.py"
      via: "LLMClient.__init__(settings: TrezarrSettings)"
      pattern: "from ..config import TrezarrSettings"
    - from: "trezarr/llm/client.py"
      to: "openai.AsyncOpenAI"
      via: "self._client = AsyncOpenAI(base_url=..., api_key=settings.llm_api_key.get_secret_value(), max_retries=...)"
      pattern: "get_secret_value"
    - from: "tests/config/test_settings.py"
      to: "trezarr/config.py"
      via: "from trezarr.config import TrezarrSettings"
    - from: "tests/llm/test_client_tiers.py"
      to: "trezarr/llm/client.py"
      via: "from trezarr.llm.client import LLMClient"
---

<objective>
Phase 1 — Wave 1 (parallel B): Config module (TrezarrSettings) and async LLM client (LLMClient).

Purpose: Delivers ENG-01. The config module is the single settings object consumed by every future
phase. The LLM client is the sole choke-point for all LLM calls in the project — its retry behavior,
concurrency cap, and structured-output tier logic are foundational correctness properties.

Output:
- trezarr/config.py — TrezarrSettings (pydantic-settings) with YAML source and SecretStr API key
- trezarr/llm/client.py — LLMClient with asyncio.Semaphore, three-tier degradation, no tenacity stacking

This plan runs in parallel with Plan 02 (codec). Zero file overlap — safe to execute concurrently
once Plan 01 (Wave 0) completes.

Phase goal: A standalone subtitle codec that round-trips SRT losslessly and a standalone LLM
client wrapping the user's OpenAI-compatible endpoint — both fully testable without each other.
Decisions honored: D-03 (OpenAI SDK v2, AsyncOpenAI), D-04 (auto-detect degradation tiers),
D-05 (model/base_url/key/context_window all configurable), D-06 (asyncio.Semaphore default 4),
D-07 (SDK max_retries=4, no tenacity stacking), D-11 (pydantic-settings YAML + env, SecretStr).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-codec-llm-client-foundation/01-CONTEXT.md
@.planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md
@.planning/phases/01-codec-llm-client-foundation/01-VALIDATION.md
@.planning/phases/01-codec-llm-client-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrezarrSettings — pydantic-settings config with YAML source and SecretStr (D-11)</name>
  <files>
    trezarr/config.py
  </files>
  <read_first>
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Pattern 4 (complete TrezarrSettings implementation — CRITICAL: yaml_file alone is silently ignored; settings_customise_sources MUST be overridden)
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Pitfall 5 (pydantic-settings YAML silently not loaded without settings_customise_sources)
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Anti-Patterns to Avoid (SecretStr — .get_secret_value() only in LLMClient constructor)
    - tests/config/test_settings.py (all four test functions must pass after this task)
  </read_first>
  <behavior>
    - TrezarrSettings() → llm_max_retries == 4, llm_max_concurrency == 4, llm_structured_output_mode == "auto", llm_context_window == 32768
    - TREZARR_LLM_MODEL=my-model in env → TrezarrSettings().llm_model == "my-model"
    - With yaml_file containing llm_max_retries: 7 → TrezarrSettings(yaml_file=path).llm_max_retries == 7
    - "secret-api-key" NOT in str(TrezarrSettings(llm_api_key="secret-api-key")) (SecretStr masking)
    - "secret-api-key" NOT in repr(settings) or str(settings.model_dump()) (masked at all output points)
    - settings.llm_api_key.get_secret_value() == "secret-api-key" (accessible only via explicit call)
  </behavior>
  <action>
    Create trezarr/config.py implementing TrezarrSettings per research Pattern 4.

    Imports: from pydantic_settings import BaseSettings, SettingsConfigDict, YamlConfigSettingsSource;
    from pydantic import SecretStr, Field; from typing import Tuple, Type, Any; import os.

    CONFIG_PATH = os.environ.get("TREZARR_CONFIG_PATH", "/config/config.yaml")

    class TrezarrSettings(BaseSettings):
      model_config = SettingsConfigDict(
        env_prefix="TREZARR_",
        env_nested_delimiter="__",
      )
      # LLM endpoint (D-03, D-05)
      llm_base_url: str = "http://localhost:1234/v1"
      llm_api_key: SecretStr = SecretStr("not-set")
      llm_model: str = "gpt-4o"
      # Retry + reliability (D-07)
      llm_max_retries: int = 4        # SDK default is 2; D-07 requires 4 explicitly
      llm_request_timeout: float = 120.0
      # Concurrency cap (D-06)
      llm_max_concurrency: int = 4    # asyncio.Semaphore cap
      # Structured output tier (D-04)
      llm_structured_output_mode: str = "auto"  # auto|json_schema|json_object|text
      # Context window (D-05 — used by Phase 2 batching)
      llm_context_window: int = 32768

      @classmethod
      def settings_customise_sources(
        cls,
        settings_cls: Type[BaseSettings],
        init_settings: Any,
        env_settings: Any,
        dotenv_settings: Any,
        file_secret_settings: Any,
      ) -> Tuple:
        return (
          init_settings,
          env_settings,
          YamlConfigSettingsSource(settings_cls, yaml_file=CONFIG_PATH),
          file_secret_settings,
        )

    CRITICAL — the YamlConfigSettingsSource in settings_customise_sources is REQUIRED. Setting yaml_file
    in model_config alone is silently ignored (Pitfall 5). This exact pattern was empirically confirmed.

    Also add a factory function make_settings(**overrides) -> TrezarrSettings that accepts keyword
    args to override fields — used in tests to inject a custom yaml_file path without requiring
    the /config/config.yaml path to exist.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && python -m pytest tests/config/test_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/config/test_settings.py -x -q exits 0 (all 4 tests pass)
    - grep -c "settings_customise_sources" trezarr/config.py returns at least 1
    - grep -c "YamlConfigSettingsSource" trezarr/config.py returns at least 1
    - TrezarrSettings() instantiates with Python-only defaults when no YAML or env vars are set
    - "not-set" (the default api_key placeholder) does NOT appear literally in str(TrezarrSettings())
      because SecretStr masks it to "**********"
    - llm_max_retries defaults to 4 (not 2, which is the SDK default — D-07 requires explicit 4)
  </acceptance_criteria>
  <done>pytest tests/config/test_settings.py -x -q exits 0. SecretStr masking confirmed. YAML source wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLMClient — AsyncOpenAI wrapper with semaphore and three-tier structured output (D-03, D-04, D-06, D-07)</name>
  <files>
    trezarr/llm/__init__.py
    trezarr/llm/client.py
  </files>
  <read_first>
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Pattern 3 (complete LLMClient implementation — exception hierarchy, tier logic, semaphore pattern)
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Pitfall 6 (SDK default max_retries=2; must set 4 explicitly from settings)
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Pitfall 7 (catch ONLY BadRequestError/UnprocessableEntityError, NOT base APIError)
    - .planning/phases/01-codec-llm-client-foundation/01-RESEARCH.md § Anti-Patterns (no tenacity stacking, SecretStr .get_secret_value() ONLY in __init__)
    - tests/llm/test_client_tiers.py (all 4 tier tests must pass)
    - tests/llm/test_client_concurrency.py (semaphore cap test must pass)
    - tests/llm/test_client_no_double_retry.py (no-tenacity and max_retries tests must pass)
    - trezarr/config.py (TrezarrSettings — already created in Task 1)
  </read_first>
  <behavior>
    - LLMClient(settings) with mode="auto": calls self._client.chat.completions.parse() first (json_schema tier)
    - On BadRequestError (400) from .parse(): falls back to .create(response_format={"type":"json_object"}) (json_object tier)
    - On second BadRequestError (400) from .create(json_object): falls back to .create() plain (text tier)
    - On UnprocessableEntityError (422): same fallback behavior as BadRequestError
    - With mode="json_schema": BadRequestError propagates — no fallback (D-04 pinned mode)
    - With mode="json_object": uses .create(json_object) directly, no .parse() attempt
    - With mode="text": uses .create() plain directly
    - asyncio.Semaphore(max_concurrency) acquired before each call, released after
    - self._client = AsyncOpenAI(max_retries=settings.llm_max_retries) — never hardcoded to SDK default 2
    - "tenacity" is NOT imported anywhere in client.py
    - settings.llm_api_key.get_secret_value() called ONLY in __init__, stored nowhere as plain string
  </behavior>
  <action>
    Create trezarr/llm/__init__.py as an empty package marker.

    Create trezarr/llm/client.py implementing LLMClient per research Pattern 3.

    Imports: asyncio; import openai; from openai import AsyncOpenAI; from pydantic import BaseModel;
    from typing import TYPE_CHECKING; if TYPE_CHECKING: from ..config import TrezarrSettings.
    Lazy import TrezarrSettings at runtime to avoid circular imports. Accept settings as Any in
    __init__ signature if needed, or use a Protocol.

    class LLMClient:
      def __init__(self, settings) -> None:
        # D-11: .get_secret_value() called ONLY here — never stored as plain str
        self._client = AsyncOpenAI(
          base_url=settings.llm_base_url,
          api_key=settings.llm_api_key.get_secret_value(),
          max_retries=settings.llm_max_retries,   # D-07: explicit 4, not SDK default 2
          timeout=settings.llm_request_timeout,
        )
        self._semaphore = asyncio.Semaphore(settings.llm_max_concurrency)  # D-06
        self._mode: str = settings.llm_structured_output_mode  # D-04
        self._model: str = settings.llm_model

      async def call(
        self,
        messages: list[dict],
        response_model: type[BaseModel] | None = None,
      ) -> str:
        async with self._semaphore:  # D-06: concurrency cap
          return await self._call_with_fallback(messages, response_model)

      async def _call_with_fallback(self, messages, response_model) -> str:
        mode = self._mode

        # Tier 1: json_schema (strict structured output)
        if response_model and mode in ("auto", "json_schema"):
          try:
            parsed = await self._client.chat.completions.parse(
              model=self._model, messages=messages, response_format=response_model,
            )
            return parsed.choices[0].message.content
          except (openai.BadRequestError, openai.UnprocessableEntityError):
            if mode == "json_schema":
              raise  # D-04: pinned mode never falls back
            # mode == "auto": fall through to Tier 2

        # Tier 2: json_object mode
        if mode in ("auto", "json_object"):
          try:
            resp = await self._client.chat.completions.create(
              model=self._model, messages=messages,
              response_format={"type": "json_object"},
            )
            return resp.choices[0].message.content
          except (openai.BadRequestError, openai.UnprocessableEntityError):
            if mode == "json_object":
              raise  # D-04: pinned mode never falls back
            # mode == "auto": fall through to Tier 3

        # Tier 3: plain text (delimited-text protocol — caller parses; always works)
        resp = await self._client.chat.completions.create(
          model=self._model, messages=messages,
        )
        return resp.choices[0].message.content

    CRITICAL — catch ONLY openai.BadRequestError and openai.UnprocessableEntityError (400/422).
    Do NOT catch openai.APIError (base class) — that would intercept 429/5xx which the SDK retries
    and reroute them to the fallback instead (Pitfall 7).

    Do NOT import tenacity anywhere in this file (Pitfall 6, D-07 double-retry prohibition).
    The SDK handles per-request retries; no additional retry layer is needed or wanted here.
  </action>
  <verify>
    <automated>cd /Users/dustin/Library/CloudStorage/SynologyDrive-DustinNas/WorkspaceX/projects/Trezarr && python -m pytest tests/llm/ -x -q -m "not live"</automated>
  </verify>
  <acceptance_criteria>
    - pytest tests/llm/ -x -q -m "not live" exits 0 (all tier, concurrency, and no-double-retry tests pass)
    - grep -c "tenacity" trezarr/llm/client.py returns 0 (tenacity NOT imported, D-07)
    - grep -c "get_secret_value" trezarr/llm/client.py returns exactly 1 (called only in __init__)
    - grep -c "APIError" trezarr/llm/client.py returns 0 (base APIError NOT caught — Pitfall 7)
    - Semaphore cap test: 5 concurrent calls with max_concurrency=2 shows at most 2 in-flight simultaneously
    - Pinned mode=json_schema + BadRequestError raises (does not silently fall back to text)
    - mode=auto with BadRequestError on both Tier 1 and Tier 2 → Tier 3 plain text call is made
  </acceptance_criteria>
  <done>pytest tests/llm/ -x -q -m "not live" exits 0. All tier, semaphore, and no-tenacity tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/YAML → settings | TREZARR_* env vars and config.yaml flow into TrezarrSettings; API key must never leave SecretStr scope |
| LLMClient → external endpoint | Messages are sent to a user-controlled base_url; SSRF is accepted by design (user's own endpoint) |
| LLM response → caller | Tier 3 plain-text response is unvalidated; caller (Phase 2+) is responsible for parsing and validating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | trezarr/config.py — llm_api_key | mitigate | SecretStr type on llm_api_key; .get_secret_value() called ONLY in LLMClient.__init__; key is never in repr()/str()/model_dump(); verified by test_api_key_not_logged (D-11) |
| T-01-03-02 | Tampering | trezarr/config.py — YAML config load | mitigate | pydantic-settings validates all config values against declared type annotations; PyYAML uses safe_load internally (no arbitrary object deserialization); integer fields reject string injections at validation time |
| T-01-03-03 | Information Disclosure | trezarr/llm/client.py — base_url SSRF | accept | base_url is user-configured and points to the user's own OpenAI-compatible endpoint by design (D-03, project constraint); document that the URL should point to a trusted host; no third-party forwarding occurs |
| T-01-03-04 | DoS | trezarr/llm/client.py — double-retry storm | mitigate | No tenacity import in client.py (grep-verified); SDK handles exponential backoff internally; Tier fallback catches only 400/422 (not 429/5xx); max_retries=4 set explicitly on AsyncOpenAI constructor (D-07) |
| T-01-03-05 | Elevation of Privilege | trezarr/llm/client.py — over-broad exception catch | mitigate | Catch ONLY openai.BadRequestError (400) and openai.UnprocessableEntityError (422); explicitly NOT catching openai.APIError base class; 429/5xx remain in SDK retry queue (Pitfall 7) |
  T-01-03-SC | Tampering | uv dep install | mitigate | All packages pre-audited [OK] in RESEARCH.md; no new installs beyond those declared in pyproject.toml Plan 01 |
</threat_model>

<verification>
After Plan 03 completes (post-Wave 1B):

  pytest tests/config/ tests/llm/ -x -q -m "not live"

Expected: exits 0. All config and LLM tests pass (excluding live endpoint test).

  pytest tests/ -x -q -m "not live"

Expected: exits 0. Full suite (codec + config + llm) green.

Security spot checks:
  python -c "
from trezarr.config import TrezarrSettings
s = TrezarrSettings(llm_api_key='super-secret')
assert 'super-secret' not in str(s), 'KEY LEAKED in str()'
assert 'super-secret' not in repr(s), 'KEY LEAKED in repr()'
assert 'super-secret' not in str(s.model_dump()), 'KEY LEAKED in model_dump()'
print('SecretStr masking OK')
"

  python -c "
import trezarr.llm.client as c
import inspect
src = inspect.getsource(c)
assert 'tenacity' not in src, 'tenacity found in client.py!'
assert src.count('get_secret_value') == 1, 'get_secret_value called more than once!'
print('No tenacity, single get_secret_value OK')
"
</verification>

<success_criteria>
- pytest tests/config/ -x -q exits 0 (YAML+env load, SecretStr masking, defaults all verified)
- pytest tests/llm/ -x -q -m "not live" exits 0 (tier tests, concurrency, no-tenacity)
- pytest tests/ -x -q -m "not live" exits 0 (full suite green)
- API key literal never appears in str(settings) / repr(settings) / model_dump() output
- LLMClient has exactly one call to get_secret_value() — in __init__ only
- tenacity is NOT imported in trezarr/llm/client.py (grep confirms 0 occurrences)
- openai.APIError base class is NOT caught anywhere in client.py (grep confirms)
- asyncio.Semaphore capping confirmed by concurrency test
- max_retries=4 wired from settings to AsyncOpenAI constructor
</success_criteria>

<output>
Create `.planning/phases/01-codec-llm-client-foundation/01-03-SUMMARY.md` when done.
</output>
