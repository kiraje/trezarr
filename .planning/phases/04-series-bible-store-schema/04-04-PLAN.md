---
phase: 04-series-bible-store-schema
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - trezarr/output/ledger.py
  - trezarr/output/ledger_sqla.py
  - trezarr/output/_ledger_protocol.py
  - trezarr/db/migration_runner.py
  - trezarr/translate/engine.py
  - trezarr/cli.py
  - tests/db/test_migrate_json_ledger.py
  - tests/output/test_ledger_sqla.py
  - tests/output/test_ledger.py
  - tests/integration/test_translate_engine_async_ledger.py
  - tests/test_cli.py
autonomous: true
requirements: [BIBLE-01]
tags: [ledger, sqlite, json-to-sqlite-migration, async-ledger, atomic-rename, cli-wiring, regression, ledger-protocol]

must_haves:
  truths:
    - "Per D-37, on engine init the JSON ledger at /config/processed_files.json is migrated into the SQLite processed_file table in one transaction; the JSON file is renamed to .migrated.bak only AFTER the SQLite commit succeeds (Pitfall 5 — commit FIRST, rename SECOND)"
    - "Per D-37, the JSON→SQLite migration is idempotent — re-running with .migrated.bak OR .corrupt.bak present is a no-op (no duplicate rows)"
    - "Per D-37, when a JSON entry's source_path conflicts with an existing SQLite row, the JSON entry is logged-and-skipped (DB is newer truth)"
    - "Per D-37, a corrupt or non-existent JSON ledger is forgiveness-path-handled (log warning, start SQLite empty) — never raises (mirrors trezarr/output/ledger.py:_load Pitfall 4)"
    - "Per the locked planner decision recorded in 04-PATTERNS.md §Pattern 5 Open Question: Ledger.check/record are made ASYNC (Option C). engine.py call sites add await. The 'frozen contract' from D-20 refers to data shape (LedgerEntry field names), NOT sync-vs-async. CLAUDE.md mandates 'Async everywhere — no sync sqlite3 calls'."
    - "Per D-37, LedgerSQLA implements the LedgerProtocol interface (check, record, content_hash) — backed by SQLAlchemy + processed_file table; the LedgerEntry dataclass is reused unchanged (D-20 field-name 1:1 mapping)"
    - "Per Phase-2 regression contract, the entire tests/output/test_ledger.py suite passes against LedgerSQLA via a duplicated tests/output/test_ledger_sqla.py — proves interface preservation"
    - "Per RESEARCH §Open Question 3, run_migrations_to_head is invoked in cli.py AFTER assert_media_roots_configured + probe_media_roots but BEFORE LLMClient construction; migration failure exits non-zero with an actionable message (fail-fast like probe_media_roots)"
    - "Per the cross-AI review HIGH finding, EVERY ledger.check/ledger.record call in trezarr/** is await-ed — enforced by a grep gate, not by line-number listing"
    - "Per the cross-AI review HIGH finding, a LedgerProtocol (typing.Protocol) declares the async contract; both Ledger and LedgerSQLA implement it; engine.py type-annotates the ledger parameter as LedgerProtocol"
    - "Per the cross-AI review HIGH finding, the breaking internal API change (check/record now async) is documented in ledger.py's module docstring, ledger_sqla.py's module docstring, and _ledger_protocol.py's module docstring, with rationale (CLAUDE.md async-everywhere mandate) and scope (internal only — no external API affected)"
    - "Per the cross-AI review MEDIUM finding, corrupt-JSON ledger files rename to processed_files.json.corrupt.bak (not .migrated.bak); the migration idempotency short-circuit checks for EITHER .migrated.bak OR .corrupt.bak presence; the exact backup path is logged in both cases"
    - "Per the cross-AI review MEDIUM finding, the .migrated.bak-presence short-circuit means a deleted DB + existing .migrated.bak loses automatic JSON recovery — this is an accepted v1 trade-off documented in must_haves and in migrate_json_ledger_if_needed's docstring"
    - "Per BIBLE-01 (clean substrate), after this plan no production code reads/writes /config/processed_files.json; the SQLite processed_file table is the single source of truth — this plan is the structural prerequisite that retires the parallel JSON store"
  artifacts:
    - path: "trezarr/output/_ledger_protocol.py"
      provides: "LedgerProtocol (typing.Protocol) declaring async check, async record, static content_hash"
      contains: "class LedgerProtocol(Protocol)"
    - path: "trezarr/output/ledger_sqla.py"
      provides: "LedgerSQLA(session_factory) — async check/record backed by processed_file table; implements LedgerProtocol"
      contains: "class LedgerSQLA"
    - path: "trezarr/db/migration_runner.py"
      provides: "migrate_json_ledger_if_needed(session_factory, settings) added alongside run_migrations_to_head; corrupt JSON → .corrupt.bak; success → .migrated.bak"
      contains: "async def migrate_json_ledger_if_needed"
    - path: "trezarr/cli.py"
      provides: "Step 3.5 wiring — build_engine + run_migrations_to_head + migrate_json_ledger_if_needed + LedgerSQLA construction before Ledger usage"
      contains: "build_engine(settings)"
  key_links:
    - from: "trezarr/cli.py::_run_once"
      to: "build_engine + run_migrations_to_head + migrate_json_ledger_if_needed + LedgerSQLA"
      via: "Step 3.5 inserted between probe_media_roots and discovery"
      pattern: "build_engine\\(settings\\)"
    - from: "trezarr/translate/engine.py (all call sites found by grep, not hardcoded line numbers)"
      to: "await ledger.check / await ledger.record"
      via: "grep gate: rg -n 'ledger\\.(check|record)\\(' trezarr — every match preceded by await"
      pattern: "await ledger\\.(check|record)"
    - from: "trezarr/db/migration_runner.py::migrate_json_ledger_if_needed"
      to: "os.replace(json_path, bak_path) AFTER the SQLite commit"
      via: "commit FIRST, rename SECOND (Pitfall 5); .migrated.bak for success, .corrupt.bak for corrupt JSON"
      pattern: "os\\.replace"
    - from: "trezarr/translate/engine.py::ledger parameter"
      to: "trezarr/output/_ledger_protocol.py::LedgerProtocol"
      via: "type annotation on engine.py's translate_file function ledger parameter"
      pattern: "LedgerProtocol"
---

<objective>
Land the structural prerequisite for BIBLE-01's "Bible persists to SQLite" claim by retiring the Phase-2 JSON ledger as a parallel persistence store. Build `trezarr/output/_ledger_protocol.py` (a typing.Protocol declaring the async contract that both implementations share), make `Ledger.check/record` async (Option C lock-in), build `trezarr/output/ledger_sqla.py` (the SQLAlchemy-backed LedgerSQLA implementing LedgerProtocol), add `migrate_json_ledger_if_needed` to `trezarr/db/migration_runner.py` for the one-shot JSON→SQLite import (commit FIRST, rename SECOND per Pitfall 5; corrupt JSON → `.corrupt.bak`; success → `.migrated.bak`), update ALL `ledger.check`/`ledger.record` call sites in `trezarr/translate/engine.py` to `await` the calls (enforced by a dynamic grep gate, not hardcoded line numbers), and wire the new startup sequence into `trezarr/cli.py`. Document the breaking internal API change explicitly.

Cross-AI review HIGH findings incorporated:
1. Repo-wide ledger-call audit: after editing engine.py, a grep gate asserts EVERY production `ledger.check(...)` / `ledger.record(...)` call in `trezarr/**` is `await`-ed. The grep discovers call sites dynamically — the plan keeps historical line numbers as context only, not as the enforcement mechanism.
2. Breaking internal API change explicitly documented in ledger.py, ledger_sqla.py, and _ledger_protocol.py docstrings. Rationale (CLAUDE.md async-everywhere) and scope (internal only — no external API affected) are stated.
3. `LedgerProtocol` (typing.Protocol) is introduced in `trezarr/output/_ledger_protocol.py` declaring `async def check`, `async def record`, `@staticmethod content_hash`. Both Ledger and LedgerSQLA implement it. `engine.py` type-annotates its `ledger` parameter as `LedgerProtocol`.

Cross-AI review MEDIUM findings incorporated:
4. Corrupt JSON files renamed to `.corrupt.bak` (not `.migrated.bak`). Idempotency short-circuit checks for EITHER suffix. Exact backup path logged in both cases.
5. `.migrated.bak`-presence short-circuit caveat: if the DB is deleted but `.migrated.bak` exists, automatic JSON recovery is lost (the short-circuit fires). This is an accepted v1 trade-off documented in must_haves and in the function docstring.

Purpose: After this plan, the SQLite `processed_file` table is the single source of truth for idempotency tracking — no parallel JSON store, no dual-write debug mode. This is the cleanup phase that makes BIBLE-01's "persists to SQLite" claim unambiguously true.

Output: `trezarr run --once` constructs an `AsyncEngine` + `LedgerSQLA` at startup, migrates any pre-existing JSON ledger into SQLite once (idempotent on re-run), and the translate loop uses `await ledger.check(...)` / `await ledger.record(...)` against the SQLite-backed implementation. After the first run, `/config/processed_files.json` is renamed to `/config/processed_files.json.migrated.bak` (success) or `/config/processed_files.json.corrupt.bak` (corrupt/unreadable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-series-bible-store-schema/04-CONTEXT.md
@.planning/phases/04-series-bible-store-schema/04-RESEARCH.md
@.planning/phases/04-series-bible-store-schema/04-PATTERNS.md
@.planning/phases/04-series-bible-store-schema/04-01-SUMMARY.md
@.planning/phases/04-series-bible-store-schema/04-02-SUMMARY.md
@.planning/phases/04-series-bible-store-schema/04-03-SUMMARY.md
@.planning/phases/02-mechanical-translation-core-validation-gate/02-CONTEXT.md
@CLAUDE.md
@trezarr/output/ledger.py
@trezarr/translate/engine.py
@trezarr/cli.py
@trezarr/paths.py
@trezarr/db/migration_runner.py
@trezarr/bible/models.py
@tests/output/test_ledger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LedgerProtocol + LedgerSQLA (async, SQLAlchemy-backed) + JSON-SQLite migration runner + engine.py await edits (grep-gate enforced)</name>
  <files>
    trezarr/output/_ledger_protocol.py,
    trezarr/output/ledger_sqla.py,
    trezarr/output/ledger.py,
    trezarr/db/migration_runner.py,
    trezarr/translate/engine.py,
    tests/db/test_migrate_json_ledger.py,
    tests/output/test_ledger_sqla.py,
    tests/output/test_ledger.py
  </files>
  <read_first>
    - trezarr/output/ledger.py (full — the Phase-2 Ledger; its public surface is the spec LedgerSQLA must clone; the LedgerEntry dataclass at lines 36-68 is REUSED verbatim — no second definition)
    - trezarr/translate/engine.py lines 1-50 + lines 400-560 (Design decisions block; locate ALL ledger call sites via grep before editing — do NOT rely on stored line numbers as the source of truth since lines shift with edits)
    - tests/output/test_ledger.py (full — the spec; the test names + assertions are duplicated verbatim in tests/output/test_ledger_sqla.py with only construction lines changing)
    - trezarr/bible/models.py (created in 04-01 — ProcessedFile column shape that LedgerSQLA writes against)
    - trezarr/db/migration_runner.py (created in 04-01 — already has run_migrations_to_head; this task ADDS migrate_json_ledger_if_needed alongside it)
    - tests/db/conftest.py (the session_factory fixture)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md sections for ledger.py / ledger_sqla.py / migration_runner.py / test_ledger_sqla.py / test_migrate_json_ledger.py
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Pattern 5: Ledger Backend Swap — Two Viable Shapes" + §"Pitfall 5: JSON-SQLite migration ordering"
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-37 (one-shot migration) / D-38 (DB URL) / D-39 (DTO boundary; LedgerEntry stays a dataclass — it's the call-site API, not a DTO)
    - .planning/phases/02-mechanical-translation-core-validation-gate/02-CONTEXT.md §D-19 (atomic-write via os.replace — mirrored by the rename step) / §D-20 (LedgerEntry field-name 1:1 mapping into processed_file)
  </read_first>
  <behavior>
    - Test 1 (tests/db/test_migrate_json_ledger.py::test_one_shot_migration_happy_path): arrange a JSON ledger at `tmp_path / "processed_files.json"` with 5 entries (covering all status values: done, quarantined, in_progress); run migrations + `migrate_json_ledger_if_needed`; assert SELECT COUNT(*) FROM processed_file == 5; assert each row's columns match the JSON entry's keys 1:1; assert the original JSON file no longer exists; assert the `.migrated.bak` file exists (at the logged path).
    - Test 2 (tests/db/test_migrate_json_ledger.py::test_idempotent_when_migrated_bak_present): pre-create the `.migrated.bak` file; call `migrate_json_ledger_if_needed` — assert SQLite processed_file row count is UNCHANGED; no warning raised; returns without touching the JSON file (if present).
    - Test 3 (tests/db/test_migrate_json_ledger.py::test_idempotent_when_corrupt_bak_present): pre-create the `.corrupt.bak` file; call `migrate_json_ledger_if_needed` — assert also returns as a no-op. Proves the idempotency check covers BOTH suffixes (cross-AI review MEDIUM finding).
    - Test 4 (tests/db/test_migrate_json_ledger.py::test_collision_logged_and_skipped): pre-populate SQLite with a ProcessedFile row for `/data/Show.S01E01.en.srt`; arrange a JSON ledger containing the same source_path with a stale hash; call the migration; assert the existing SQLite row is unchanged (DB is the newer truth per D-37); assert a log warning was emitted naming the source_path; assert `.migrated.bak` rename happened.
    - Test 5 (tests/db/test_migrate_json_ledger.py::test_corrupt_json_renames_to_corrupt_bak_no_raise): arrange a JSON ledger that is corrupt (e.g. `"not valid {{ json"`); call the migration — no exception raised; SQLite processed_file table is empty; log warning emitted; `.corrupt.bak` file exists (NOT `.migrated.bak`). This is the cross-AI review MEDIUM finding — corrupt files get a distinct suffix.
    - Test 6 (tests/db/test_migrate_json_ledger.py::test_missing_json_is_noop): no JSON ledger file exists; call the migration — no exception; no log noise; no `.migrated.bak` OR `.corrupt.bak` created; SQLite empty.
    - Test 7 (tests/db/test_migrate_json_ledger.py::test_commit_before_rename_pitfall5): monkeypatch `os.replace` to raise OSError; call the migration; assert the SQLite rows ARE persisted (commit happened FIRST) AND assert the JSON file is still in place (rename failed AFTER commit). This explicitly tests Pitfall 5's commit-FIRST-rename-SECOND safety.
    - Test 8 (tests/db/test_migrate_json_ledger.py::test_corrupt_bak_path_is_logged): arrange a corrupt JSON ledger; call `migrate_json_ledger_if_needed` with a log capture; assert the log output contains the exact `.corrupt.bak` path string. Ensures operators know where the corrupt file was moved.
    - Test 9 (tests/output/test_ledger_sqla.py — full suite): every test from `tests/output/test_ledger.py` is duplicated here, modified ONLY to: (a) construct `LedgerSQLA(session_factory)` instead of `Ledger(path)`; (b) `await ledger.check(...)` / `await ledger.record(...)`; (c) `async def test_…` instead of `def test_…`. All tests pass — the existing assertions on data shape, status values, and persistence-across-construction are preserved.
    - Test 10 (tests/output/test_ledger.py — updated): every existing test in this file is updated to use `await` on the ledger calls (the Phase-2 Ledger is now async per Option C). The test bodies are otherwise unchanged. All existing assertions still pass.
    - Test 11 (tests/output/test_ledger_sqla.py::test_check_returns_none_for_unknown_path): `await ledger.check("/unknown/path")` returns None.
    - Test 12 (tests/output/test_ledger_sqla.py::test_record_upserts_existing_entry): record an entry with status="in_progress"; record again with the SAME source_path but status="done" + translated_at set; assert SELECT FROM processed_file WHERE source_path=… returns ONE row (UPSERT, not double-INSERT) with status="done" and translated_at populated.
    - Test 13 (tests/output/test_ledger_sqla.py::test_ledger_sqla_implements_ledger_protocol): `from trezarr.output._ledger_protocol import LedgerProtocol; from trezarr.output.ledger_sqla import LedgerSQLA; from trezarr.output.ledger import Ledger`. Assert `issubclass(LedgerSQLA, LedgerProtocol)` is True (runtime structural check). Assert `issubclass(Ledger, LedgerProtocol)` is True. NOTE: typing.Protocol with `runtime_checkable=True` is required for `isinstance`/`issubclass` checks to work at runtime.
    - Test 14 (tests/integration/test_translate_engine_async_ledger.py::test_read_batch_failure_quarantine_records_via_await): construct a minimal translate_file invocation with a source SRT that triggers the read/batch-failure path; use a spy ledger whose `record` is an `AsyncMock`; assert the spy was called at least once after the failure path runs. Without `await`, the coroutine is discarded and the spy counter stays at 0.
    - Test 15 (tests/integration/test_translate_engine_async_ledger.py::test_repo_wide_ledger_await_gate): programmatic enforcement of the repo-wide await rule. Use subprocess to run `rg -n "ledger\.(check|record)\(" trezarr` (or Python's `pathlib.Path.rglob + re.findall` if rg is not available) to find EVERY production call site. For each match, read that line and verify it is preceded by `await ` on the same line (`re.search(r'\bawait\s+ledger\.(check|record)\(', line)`). Assert zero unawated call sites. This test enforces the grep gate from the cross-AI review HIGH finding dynamically — adding a new call site without `await` fails this test immediately.
  </behavior>
  <action>
    Step A — Create LedgerProtocol (cross-AI review HIGH finding):

    Create `trezarr/output/_ledger_protocol.py`. Module docstring:
    `"""LedgerProtocol — typing.Protocol declaring the async Ledger contract (Phase 4).\n\nBREAKING INTERNAL API CHANGE (Phase 4): Ledger.check and Ledger.record were\nsync in Phase 2. As of Phase 4 they are ASYNC in both implementations (Ledger\nand LedgerSQLA). Rationale: CLAUDE.md mandates 'Async everywhere — no sync\nsqlite3 calls in the FastAPI/async event loop'. This is an INTERNAL change\nonly — the Ledger is not a public/external API. No external contract is broken.\n\nBoth Ledger (trezarr/output/ledger.py) and LedgerSQLA (trezarr/output/ledger_sqla.py)\nimplicitly implement this Protocol. Type-check callers by annotating the ledger\nparameter as LedgerProtocol."""`.

    Imports: `from __future__ import annotations`; `from typing import TYPE_CHECKING, Protocol, runtime_checkable`; `if TYPE_CHECKING: from pathlib import Path; from trezarr.output.ledger import LedgerEntry`.

    Define:
    ```
    @runtime_checkable
    class LedgerProtocol(Protocol):
        """Structural protocol for the async idempotency ledger.

        @runtime_checkable so isinstance(obj, LedgerProtocol) works in tests.
        """

        async def check(self, source_path: "str | Path") -> "LedgerEntry | None":
            """Return the existing ledger entry for source_path, or None."""
            ...

        async def record(self, entry: "LedgerEntry") -> None:
            """Persist or update the ledger entry (upsert semantics)."""
            ...

        @staticmethod
        def content_hash(source_bytes: bytes) -> str:
            """Return a short deterministic hash of source_bytes."""
            ...
    ```

    Step B — Make the existing Ledger async (Option C decision) AND document the breaking change:

    Edit `trezarr/output/ledger.py`:
    - Change `def check(self, source_path: str | Path) -> LedgerEntry | None:` to `async def check(...)` — body is unchanged.
    - Change `def record(self, entry: LedgerEntry) -> None:` to `async def record(...)` — body is unchanged.
    - Add import: `from trezarr.output._ledger_protocol import LedgerProtocol  # noqa: F401` (makes the protocol importable via the ledger module for convenience).
    - Add a `BREAKING INTERNAL API CHANGE (Phase 4):` section to the module docstring explicitly stating: (1) check/record are now async; (2) rationale is CLAUDE.md "Async everywhere"; (3) this is internal-only — no external API is affected; (4) all call sites in `trezarr/translate/engine.py` must `await` these methods; (5) a repo-wide grep gate in tests/integration enforces this at test time.

    Step C — Update all engine.py call sites:

    BEFORE editing engine.py, locate ALL current ledger call sites via:
    `grep -n "ledger\.\(check\|record\)(" trezarr/translate/engine.py`

    This grep output — not any stored line numbers — is the canonical list of call sites to update. The historical context from planning identifies approximately 6 call sites (around lines 421, 438, 454, 479, 520, 537 at planning time), but lines shift with edits. Use the grep output to find the current actual locations.

    For each call site found by grep: change `ledger.check(...)` to `await ledger.check(...)` and `ledger.record(...)` to `await ledger.record(...)`.

    Update `trezarr/translate/engine.py` type annotation: add `from trezarr.output._ledger_protocol import LedgerProtocol` to the imports. Update the `ledger` parameter type annotation in `translate_file(...)` (and any other function taking a ledger) from `Ledger` to `LedgerProtocol`.

    Add a single-line entry to the engine.py module docstring's `Design decisions honoured:` block: `D-37  ledger.check/record are async (Phase 4 — BREAKING INTERNAL API CHANGE, see _ledger_protocol.py); type-annotated as LedgerProtocol; all call sites in this file await the calls (enforced by test_repo_wide_ledger_await_gate).`

    Step D — Build LedgerSQLA:

    Create `trezarr/output/ledger_sqla.py`. Module docstring opens with a one-liner ("SQLAlchemy-backed idempotency ledger (D-37 — JSON ledger swap target)."), followed by:
    - `BREAKING INTERNAL API CHANGE (Phase 4):` section mirroring ledger.py (check/record are async; rationale; internal-only scope).
    - `Design decisions honoured:` block citing D-20 (field-name 1:1 mapping), D-37 (backend swap + one-shot import), D-38 (async sessions — no sync sqlite3 calls), D-39 (Phase 5+ never imports SQLAlchemy through this module; the public surface accepts/returns the LedgerEntry dataclass — NOT a Pydantic DTO).
    - `Interface contract:` block listing `await ledger.check(source_path) -> LedgerEntry | None`, `await ledger.record(entry: LedgerEntry) -> None`, `LedgerSQLA.content_hash(source_bytes: bytes) -> str` (`@staticmethod`).
    - `Implements:` `trezarr.output._ledger_protocol.LedgerProtocol (structural/implicit).`

    Imports: `from __future__ import annotations`; `import hashlib`; `import logging`; `from pathlib import Path`; `from sqlalchemy import select`; `from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession`; `from trezarr.bible.models import ProcessedFile`; `from trezarr.output.ledger import LedgerEntry` (REUSE — do NOT redefine).

    Define `class LedgerSQLA:` with:
    - `__init__(self, session_factory: async_sessionmaker[AsyncSession]) -> None:` storing `self._session_factory`.
    - `async def check(self, source_path: str | Path) -> LedgerEntry | None:` — open a session; `select(ProcessedFile).where(ProcessedFile.source_path == str(source_path))`; if `row is None` return None; convert: build a LedgerEntry from row attributes 1:1 (source_path, output_path, status, content_hash, series_id, source_lang, episode_key, translated_at, quarantine_path) and return.
    - `async def record(self, entry: LedgerEntry) -> None:` — UPSERT semantics: `async with self._session_factory() as session: async with session.begin():` — SELECT by source_path; if exists UPDATE all mutable columns; else `session.add(ProcessedFile(...))` with all 9 fields from entry. Use SELECT-then-INSERT/UPDATE (NOT dialect-specific INSERT OR REPLACE) so the upsert works on any SQLAlchemy-supported backend.
    - `@staticmethod content_hash(source_bytes: bytes) -> str:` — returns `hashlib.sha256(source_bytes).hexdigest()[:16]` (identical to ledger.py).

    Module-level `logger = logging.getLogger(__name__)`.

    Step E — Add migrate_json_ledger_if_needed:

    Edit `trezarr/db/migration_runner.py` to ADD alongside `run_migrations_to_head`.

    Add imports near the top: `import json`, `import os`, `from trezarr.bible.models import ProcessedFile`, `from sqlalchemy import select` (if not already imported), `from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession`. Add a TYPE_CHECKING import of TrezarrSettings.

    Update the module docstring to mention the new function, the Pitfall 5 ordering invariant (commit FIRST, rename SECOND — NEVER reverse), and the two backup suffixes: `.migrated.bak` for successful imports, `.corrupt.bak` for corrupt/unreadable JSON.

    Define `async def migrate_json_ledger_if_needed(session_factory: async_sessionmaker[AsyncSession], settings: "TrezarrSettings") -> None:`. Behavior:
    - Resolve `json_path = Path(settings.translate_ledger_path)`.
    - Compute BOTH possible backup paths: `migrated_bak = Path(str(json_path) + ".migrated.bak")` and `corrupt_bak = Path(str(json_path) + ".corrupt.bak")`.
    - Idempotency short-circuit: if EITHER `migrated_bak.exists()` OR `corrupt_bak.exists()`, emit a debug log ("Ledger migration already done (found {bak_path}) — no-op") logging the exact existing backup path, and return immediately. This covers both the success path and the corrupt path from prior runs. NOTE — KNOWN V1 LIMITATION (documented in must_haves.truths): if the SQLite DB is deleted but a `.migrated.bak` exists, the automatic JSON recovery is lost because this short-circuit fires. Operators must manually remove the `.migrated.bak` and restore the JSON to recover. This is accepted for v1 (single-user self-hosted daemon; backup deletion is an explicit operator action).
    - Missing-file no-op: if not `json_path.exists()`, emit a debug log and return.
    - Forgiveness on corrupt JSON: wrap `json.loads(json_path.read_text(encoding="utf-8"))` in `try/except (json.JSONDecodeError, OSError)`. On failure, log a warning("Ledger unreadable — starting SQLite empty. Moving corrupt file to {corrupt_bak}") and then `try: os.replace(json_path, corrupt_bak)` logging the exact corrupt_bak path; if even the rename fails, log error and return. Same handling if the parsed JSON is not a dict.
    - The actual migration in ONE transaction: counters `inserted = 0`, `skipped_collision = 0`, `skipped_malformed = 0`. `async with session_factory() as session: async with session.begin():` — pre-fetch existing source_paths via `(await session.scalars(select(ProcessedFile.source_path))).all()` into a `set`. Then iterate `raw.items()`: validate each entry; if `src_path in existing` log warning and bump `skipped_collision`; else construct `ProcessedFile(...)` and `session.add(row)`. The `with session.begin():` context exit COMMITS (Pitfall 5 step 1).
    - Rename AFTER commit (Pitfall 5 step 2 — NEVER reverse this order): `try: os.replace(json_path, migrated_bak)` logging the exact migrated_bak path on success ("Ledger migration committed ({inserted} rows) — renamed to {migrated_bak}"). On OSError after a successful commit, log error noting that the migration rows ARE in SQLite (commit happened first) but the rename failed; re-run is safe because the collision-skip path handles duplicates.

    Step F — Tests:

    Update `tests/output/test_ledger.py` — for every test function that calls `ledger.check(...)` or `ledger.record(...)`, convert to `async def test_…(...)` and add `await` before the ledger call. Add a comment at the top of each modified test: `# Phase 4 / 04-04: ledger.check/record are now async (Option C in 04-PATTERNS §Pattern 5).`

    Create `tests/output/test_ledger_sqla.py` per PATTERNS §"tests/output/test_ledger_sqla.py" — DUPLICATE every test from tests/output/test_ledger.py, changing only: construction line → `LedgerSQLA(session_factory)`; tests are `async def`; add `await` to ledger calls. For JSON-specific tests (e.g. corrupt JSON fallback), drop them or rewrite as `test_empty_db_check_returns_none`. Add Tests 11, 12, 13 as additional LedgerSQLA-only tests at the end.

    Create `tests/db/test_migrate_json_ledger.py` with Tests 1-8 above. For Test 7 (Pitfall 5 ordering), monkeypatch `os.replace` to raise OSError on its first call ONLY.

    Create `tests/integration/test_translate_engine_async_ledger.py` with Tests 14-15 above. Test 14 uses an `AsyncMock` spy. Test 15 (the grep gate) uses Python's pathlib to walk `trezarr/**/*.py` and assert every line matching `ledger.(check|record)(` is preceded by `await ` on the same line — this is the dynamic enforcement of the cross-AI review HIGH finding.
  </action>
  <verify>
    <automated>uv run pytest tests/output/test_ledger.py tests/output/test_ledger_sqla.py tests/db/test_migrate_json_ledger.py tests/integration/test_translate_engine_async_ledger.py -x &amp;&amp; uv run python -c "from trezarr.output._ledger_protocol import LedgerProtocol; from trezarr.output.ledger_sqla import LedgerSQLA; from trezarr.output.ledger import Ledger; import inspect; assert inspect.iscoroutinefunction(LedgerSQLA.check); assert inspect.iscoroutinefunction(LedgerSQLA.record); assert inspect.iscoroutinefunction(Ledger.check); assert inspect.iscoroutinefunction(Ledger.record); assert issubclass(LedgerSQLA, LedgerProtocol); assert issubclass(Ledger, LedgerProtocol); print('async interface OK; both implement LedgerProtocol')" &amp;&amp; python -c "import pathlib, re; hits = []; [hits.append(f'{f.name}:{i+1}: {line.rstrip()}') for f in pathlib.Path('trezarr').rglob('*.py') for i, line in enumerate(f.read_text().splitlines()) if re.search(r'(?&lt;!await\s)(?&lt;!await  )ledger\.(check|record)\(', line) and not line.lstrip().startswith('#')]; assert not hits, 'Unawaited ledger calls found:\n' + '\n'.join(hits); print('Grep gate passed: all ledger calls are awaited')"</automated>
  </verify>
  <done>
    - `trezarr/output/_ledger_protocol.py` exists with `LedgerProtocol` (`@runtime_checkable`) declaring `async check`, `async record`, `@staticmethod content_hash`. Module docstring explicitly documents the breaking internal API change, rationale, and scope.
    - `trezarr/output/ledger_sqla.py` exists with `LedgerSQLA` exposing async `check`/`record` + static `content_hash`, backed by the `processed_file` table. Module docstring documents the breaking change.
    - `trezarr/output/ledger.py` Ledger.check/record are now async (Option C). Module docstring documents the breaking change.
    - Both `Ledger` and `LedgerSQLA` implement `LedgerProtocol` structurally (runtime_checkable issubclass check passes).
    - `trezarr/translate/engine.py` type-annotates its `ledger` parameter as `LedgerProtocol`; ALL call sites have `await` preceding them. Dynamic grep gate in test_repo_wide_ledger_await_gate enforces this — no hardcoded line numbers.
    - `trezarr/db/migration_runner.py` has `migrate_json_ledger_if_needed` with Pitfall-5-safe ordering, idempotency for BOTH `.migrated.bak` and `.corrupt.bak`, corrupt-JSON handling that renames to `.corrupt.bak` (not `.migrated.bak`), and exact backup path logging.
    - `tests/output/test_ledger.py` updated to async (all existing assertions still pass).
    - `tests/output/test_ledger_sqla.py` duplicates the Phase-2 ledger suite against LedgerSQLA — all tests pass.
    - `tests/db/test_migrate_json_ledger.py` covers happy path, idempotency (BOTH bak suffixes), collision-skip, corrupt-JSON (`.corrupt.bak` suffix), missing-file no-op, Pitfall-5 ordering, and backup-path logging.
    - `tests/integration/test_translate_engine_async_ledger.py` Test 14 proves the quarantine path is awaited. Test 15 (grep gate) enforces the repo-wide await rule dynamically.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cli.py startup wiring — Step 3.5 (engine + migrations + ledger import + LedgerSQLA construction) + end-to-end smoke</name>
  <files>
    trezarr/cli.py,
    tests/test_cli.py
  </files>
  <read_first>
    - trezarr/cli.py (full — the existing 8-step _run_once sequence; the new Step 3.5 inserts between current step 3 probe_media_roots and step 4 discovery, and step 5's Ledger construction changes to LedgerSQLA — per PATTERNS §"trezarr/cli.py")
    - trezarr/paths.py lines 171-220 (probe_media_roots — the fail-fast sys.exit pattern that migration_failure must mirror)
    - tests/test_cli.py (existing — the cli's regression suite; this task adds new tests and updates any existing test that constructs Ledger directly)
    - trezarr/db/migration_runner.py (the run_migrations_to_head + migrate_json_ledger_if_needed functions Task 1 created)
    - trezarr/db/engine.py + trezarr/db/session.py (the build_engine / build_session_factory functions 04-01 created)
    - trezarr/output/ledger_sqla.py (the LedgerSQLA that Task 1 just shipped)
    - trezarr/output/_ledger_protocol.py (LedgerProtocol — the type annotation to use in cli.py)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md §"trezarr/cli.py (MODIFIED — startup wiring point)"
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Open Question 3" (migrations run AFTER probe but BEFORE LLMClient construction)
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-37 + §"code_context" §"Engine startup"
  </read_first>
  <behavior>
    - Test 1 (tests/test_cli.py::test_run_once_builds_engine_and_runs_migrations_before_discovery): patch `build_engine`, `run_migrations_to_head`, `migrate_json_ledger_if_needed` to record call order; patch the *arr discovery calls to return empty lists; call `_run_once(None)`; assert the call order is: assert_media_roots_configured → probe_media_roots → build_engine → run_migrations_to_head → migrate_json_ledger_if_needed → (discovery) → (ledger SQLA construction) → (scan).
    - Test 2 (tests/test_cli.py::test_run_once_constructs_ledger_sqla_not_json_ledger): patch the migration runner functions to no-op; patch discovery to return empty; capture the Ledger instance passed downstream and assert `isinstance(ledger, LedgerSQLA)` (NOT the JSON `Ledger`). Smoke-checks that step 5 was correctly updated.
    - Test 3 (tests/test_cli.py::test_run_once_exits_nonzero_on_migration_failure): patch `run_migrations_to_head` to raise; call `_run_once`; assert the function returns 1 (non-zero exit) AND a logger.error was emitted with an actionable message ("Trezarr cannot start — SQLite migration failed at …"). Mirrors the probe_media_roots fail-fast pattern but returns int (not sys.exit) so it's testable as a coroutine.
    - Test 4 (tests/test_cli.py::test_run_once_logs_but_continues_on_ledger_migration_failure): patch `migrate_json_ledger_if_needed` to raise a non-migration exception; call `_run_once`; assert the run continues to discovery / scan / translate; assert a logger.error was emitted. Asymmetric vs. Test 3: Alembic migration is hard-fatal (DB schema MUST be ready), but the JSON-ledger import is best-effort (D-37 forgiveness).
    - Test 5 (tests/test_cli.py::test_run_once_end_to_end_smoke_with_temp_sqlite): integration — point `bible_db_url` at a temp SQLite, point `translate_ledger_path` at a non-existent path (so the JSON migration is a no-op), patch *arr discovery to return ONE eligible MediaItem with a small valid SRT, patch the LLM client to return a fixed translation; call `_run_once`; assert exit code 0, assert `processed_file` table has one "done" row matching the source path, assert the vi.srt sidecar was written.
  </behavior>
  <action>
    Edit `trezarr/cli.py`:

    Add imports at the top after the existing imports (alphabetical within the trezarr.* group):
    - `from trezarr.db.engine import build_engine`
    - `from trezarr.db.migration_runner import migrate_json_ledger_if_needed, run_migrations_to_head`
    - `from trezarr.db.session import build_session_factory`
    - `from trezarr.output._ledger_protocol import LedgerProtocol`
    - `from trezarr.output.ledger_sqla import LedgerSQLA`

    Update the module docstring's `Design decisions honoured:` block — append `D-37  Step 3.5: build_engine + run_migrations_to_head + migrate_json_ledger_if_needed wire in BEFORE discovery; LedgerSQLA replaces the JSON Ledger from Phase 2. Breaking internal API change: ledger.check/record are async (see _ledger_protocol.py).`

    Insert a NEW Step 3.5 in `_run_once` BETWEEN the existing Step 3 (probe_media_roots) and Step 4 (discovery), per PATTERNS §"trezarr/cli.py". The step has three sub-actions:

    1. Build the engine and run Alembic migrations inside a `try/except`; on any exception, log an error message ("Trezarr cannot start — SQLite migration failed at <bible_db_url>: <exc>") and `return 1` (mirrors probe_media_roots's fail-fast; returns int, not sys.exit, to keep it testable). Guard the migration call with `if settings.bible_db_run_migrations_on_startup:` so an operator can disable startup migrations via config for unusual ops scenarios.
    2. Build the session factory: `session_factory = build_session_factory(engine)`.
    3. Run the JSON-ledger one-shot import inside a separate `try/except`; on exception, log an error noting the run will continue with an empty SQLite ledger (best-effort per D-37 forgiveness). Do NOT return non-zero.

    Update Step 5 (existing `ledger = Ledger(settings.translate_ledger_path)`): replace with `ledger = LedgerSQLA(session_factory)`. Remove the `from trezarr.output.ledger import Ledger` import — verify by searching cli.py for `Ledger(` after the edit; if zero matches besides `LedgerSQLA`, drop the JSON Ledger import.

    Add a comment after step 5: `# Phase 7 daemon will need to call await engine.dispose() on shutdown; one-shot CLI exits the process so dispose is unnecessary here.`

    Update `tests/test_cli.py`:

    Add the 5 new tests above. For Tests 1-4, use `unittest.mock.patch` at the module level to record call order or inject failures. For Test 5 (end-to-end), use the `session_factory` fixture from `tests/db/conftest.py`.

    Audit existing tests in tests/test_cli.py — any test that constructs `Ledger(...)` directly via the cli path must be updated to `LedgerSQLA(session_factory)`. Run `grep -n "Ledger\b" tests/test_cli.py` and update each match. If a test patches `trezarr.cli.Ledger`, repoint it at `trezarr.cli.LedgerSQLA`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/output/test_ledger_sqla.py tests/db -x &amp;&amp; uv run python -c "import inspect; import trezarr.cli as cli; src = inspect.getsource(cli); assert 'build_engine(settings)' in src; assert 'run_migrations_to_head' in src; assert 'migrate_json_ledger_if_needed' in src; assert 'LedgerSQLA(session_factory)' in src; print('cli wiring OK')" &amp;&amp; uv run pytest -x</automated>
  </verify>
  <done>
    - `trezarr/cli.py` has Step 3.5 wired in the correct position (after probe_media_roots, before discovery, before LLMClient construction).
    - Step 5's Ledger construction is `LedgerSQLA(session_factory)`; the legacy JSON `Ledger` import is removed from cli.py.
    - Migration-failure handling is asymmetric and correct: Alembic schema failure → return 1 with actionable error; JSON-ledger import failure → log error and continue with an empty SQLite ledger (D-37 forgiveness).
    - All 5 new cli tests pass; the existing tests/test_cli.py suite (after the Ledger → LedgerSQLA update) continues to pass.
    - `uv run pytest -x` (full project suite) is green — Phase 1/2/3 + 04-01 + 04-02 + 04-03 + 04-04 all pass together.
    - The end-to-end smoke test (Test 5) proves that `trezarr run --once` produces a vi.srt sidecar AND writes a "done" row to the SQLite processed_file table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON ledger file → migrate_json_ledger_if_needed | The JSON file is written by prior Trezarr versions or operators; it may be corrupt — handled with forgiveness path and .corrupt.bak |
| engine.py ledger calls → LedgerSQLA | All call sites must be awaited — enforced by the dynamic grep gate test |
| LedgerProtocol type boundary | Both Ledger and LedgerSQLA implement the protocol; runtime_checkable enables isinstance checks in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | `migrate_json_ledger_if_needed` — commit/rename ordering | mitigate | D-37 / Pitfall 5: commit FIRST, rename SECOND; Test 7 (monkeypatch os.replace) validates this ordering |
| T-04-14 | Denial of Service | Corrupt JSON ledger blocking startup | mitigate | Forgiveness path: corrupt JSON → log warning + rename to .corrupt.bak + continue empty; never raises |
| T-04-15 | Tampering | Unawaited ledger.record() silently drops state transitions | mitigate | Dynamic grep gate in test_repo_wide_ledger_await_gate; Test 14 (AsyncMock spy on quarantine path) |
| T-04-16 | Tampering | .migrated.bak delete + DB delete loses JSON recovery | accept | V1 limitation documented in must_haves.truths and migrate_json_ledger_if_needed docstring; operator must manually restore |
| T-04-17 | Spoofing | LedgerSQLA not implementing LedgerProtocol contract | mitigate | LedgerProtocol is @runtime_checkable; Test 13 asserts issubclass at runtime; coroutine-function check in verify command |
| T-04-SC | Tampering | npm/pip install (no new packages this plan) | accept | No new package installs — inherits from 04-01 |
</threat_model>

<verification>
- `uv run pytest -x` — full project suite passes. This is the load-bearing check for the phase: every Phase-1/2/3 test continues to work after the ledger backend swap.
- `uv run pytest tests/db tests/bible tests/output/test_ledger_sqla.py tests/integration/test_translate_engine_async_ledger.py -x` — Phase 4-specific tests all pass.
- Dynamic grep gate (Test 15 in test_translate_engine_async_ledger.py): asserts ZERO unawaited `ledger.(check|record)(` calls in `trezarr/**/*.py`. This replaces the brittle hardcoded line-number check.
- `uv run python -c "from trezarr.output._ledger_protocol import LedgerProtocol; from trezarr.output.ledger import Ledger; from trezarr.output.ledger_sqla import LedgerSQLA; assert issubclass(Ledger, LedgerProtocol); assert issubclass(LedgerSQLA, LedgerProtocol); print('Both implement LedgerProtocol')"` — protocol check passes.
- `grep -nE "import Ledger\b" trezarr/cli.py` — returns nothing (only LedgerSQLA is imported).
- `grep -n "corrupt.bak\|migrated.bak" trezarr/db/migration_runner.py` — both suffixes are referenced; corrupt JSON uses corrupt.bak, successful migration uses migrated.bak.
- Corrupt-JSON backup path test (Test 8) passes — exact path is logged.
- Idempotency test with `.corrupt.bak` present (Test 3) passes — proves both suffixes short-circuit.
- End-to-end manual smoke (optional, requires LLM endpoint): create a small SRT under a temp /data tree, run `uv run trezarr run --once`, observe that /tmp/test.db is created, the processed_file table has one row, the vi.srt sidecar is written, and /config/processed_files.json is renamed to .migrated.bak.
- Cross-plan invariant: BIBLE-01's "persists to SQLite" claim is now unambiguously true — no JSON file is the source of truth after 04-04 lands.
</verification>

<success_criteria>
- `trezarr/output/_ledger_protocol.py` exists with `@runtime_checkable class LedgerProtocol(Protocol)` declaring `async check`, `async record`, `@staticmethod content_hash`. Module docstring explicitly documents the breaking internal API change, rationale (CLAUDE.md async-everywhere), and scope (internal only).
- `LedgerSQLA` exists in `trezarr/output/ledger_sqla.py` exposing the same interface as Phase-2 Ledger (check/record/content_hash), backed by the SQLAlchemy `processed_file` table. Module docstring documents the breaking change.
- Both `Ledger` and `LedgerSQLA` implement `LedgerProtocol` (runtime_checkable issubclass passes at test time).
- `engine.py` type-annotates its `ledger` parameter as `LedgerProtocol` and has `await` on ALL ledger call sites — enforced by the dynamic grep gate test (Test 15), NOT by hardcoded line numbers.
- `migrate_json_ledger_if_needed` in `trezarr/db/migration_runner.py` implements the D-37 one-shot JSON→SQLite import with Pitfall-5-safe commit-FIRST/rename-SECOND ordering, idempotent on re-run for BOTH `.migrated.bak` and `.corrupt.bak`, corrupt JSON renames to `.corrupt.bak` (not `.migrated.bak`), exact backup path is logged in both cases.
- `.migrated.bak`-presence short-circuit caveat documented: if the DB is deleted but `.migrated.bak` exists, automatic JSON recovery is lost — accepted v1 limitation.
- `trezarr/cli.py` has Step 3.5 wired in the correct position; migration failure is hard-fatal (return 1), JSON-ledger import failure is best-effort (log + continue).
- The full project test suite (`uv run pytest -x`) passes — including all Phase 1/2/3 + 04-01 + 04-02 + 04-03 + 04-04 tests.
- After this plan, no production code reads or writes `/config/processed_files.json` — the SQLite `processed_file` table is the single source of truth.
</success_criteria>

<output>
Create `.planning/phases/04-series-bible-store-schema/04-04-SUMMARY.md` when done.
</output>
