---
phase: 04-series-bible-store-schema
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - trezarr/bible/dto.py
  - trezarr/bible/store.py
  - trezarr/arr/sonarr.py
  - trezarr/arr/radarr.py
  - tests/bible/test_lazy_series_create.py
  - tests/bible/test_arr_metadata_snapshot.py
  - tests/bible/test_dto_boundary.py
autonomous: true
requirements: [BIBLE-01, BIBLE-05]
tags: [bible-store, series, register, arr-metadata, dto-boundary, lazy-creation, mvp-vertical-slice]

must_haves:
  truths:
    - "Per BIBLE-01 + D-36, the first translate of an item with an unknown (arr_kind, arr_instance, arr_series_id) lazily creates a series row and snapshots its arr_metadata — no startup enumeration"
    - "Per BIBLE-01, a subsequent translate of the same item finds the existing series row (idempotent get-or-create)"
    - "Per BIBLE-05 + D-35, series.arr_metadata is a JSON snapshot captured at series-row creation containing arr discovery payload (genres, overview, year, network, runtime)"
    - "Per BIBLE-05 + D-35, series.register is NULL after lazy creation — Phase 5 populates it via merge_inferred (Phase 4 does NOT set it)"
    - "Per D-33, tvdb_id (Sonarr) and tmdb_id (Radarr) are captured at series creation as denormalized columns but are NOT used for lookup"
    - "Per D-39, trezarr.bible.store exposes Pydantic DTOs only — no SQLAlchemy import is reachable through any public name in trezarr.bible.store or trezarr.bible.dto"
    - "Per RESEARCH §Open Question 1, MediaItem is extended with the Phase-4 arr_metadata fields (genres, overview, year, network, runtime, arr_kind, tvdb_id, tmdb_id) at the Phase-3 discovery layer — Phase 5 never re-fetches"
    - "Per the cross-AI review MEDIUM finding, BibleEventDTO.created_at is typed as datetime | None (not Any) — using datetime for correct type contracts; Pydantic v2 coerces ISO-8601 strings automatically"
    - "Per the cross-AI review MEDIUM finding, the arr_metadata size cap uses json.dumps(arr_metadata_snapshot, ensure_ascii=False).encode('utf-8') for the byte count — Vietnamese metadata with non-ASCII characters measures bytes correctly at the UTF-8 level"
    - "Per the cross-AI review MEDIUM finding, processed_file.source_path uniqueness is path-string equality only — case sensitivity depends on the host filesystem (case-insensitive on macOS/Windows, case-sensitive on Linux). This is a known v1 limitation: if the same file is referenced with different casing on a case-insensitive filesystem, a duplicate INSERT will raise IntegrityError rather than finding the existing row. Document this in the store.py docstring as a known limitation to be addressed in a future phase if operators report issues."
  artifacts:
    - path: "trezarr/bible/dto.py"
      provides: "SeriesDTO, SeriesBibleDTO Pydantic models with from_attributes=True"
      contains: "model_config = ConfigDict(from_attributes=True)"
    - path: "trezarr/bible/store.py"
      provides: "get_or_create_series, load_series_bible — Pydantic-typed public API hiding SQLAlchemy"
      contains: "async def get_or_create_series"
    - path: "trezarr/arr/sonarr.py"
      provides: "MediaItem extended with arr_kind/tvdb_id/genres/overview/year/network/runtime fields populated from pyarr series payload"
      contains: "arr_kind: str | None"
  key_links:
    - from: "trezarr/arr/sonarr.py::discover_sonarr_items"
      to: "MediaItem.arr_kind/tvdb_id/genres/overview/year/network/runtime"
      via: "series dict fields from pyarr client.series.get()"
      pattern: "arr_kind=\"sonarr\""
    - from: "trezarr/arr/radarr.py::discover_radarr_items"
      to: "MediaItem.arr_kind/tmdb_id/genres/overview/year/runtime"
      via: "movie dict fields from pyarr client.movie.get()"
      pattern: "arr_kind=\"radarr\""
    - from: "trezarr/bible/store.py::get_or_create_series"
      to: "Series table UNIQUE(arr_kind, arr_instance, arr_series_id)"
      via: "SELECT-then-INSERT under a single session.begin() block"
      pattern: "session\\.begin"
---

<objective>
Deliver the first vertical Bible slice: persistent per-series rows with the arr_metadata snapshot needed by Phase 5's register inference. Extend `MediaItem` with the Phase-4 arr_metadata fields at the discovery layer (Sonarr + Radarr), build the Pydantic DTO boundary (`trezarr/bible/dto.py`), ship `get_or_create_series()` + `load_series_bible()` in `trezarr/bible/store.py`, and add tests that prove lazy creation is idempotent, the arr_metadata JSON round-trips as a dict, and the DTO layer never leaks SQLAlchemy. NO LLM populates anything — Phase 4 just persists what the *arr discovery payload already provides. Register stays NULL (Phase 5 sets it via merge_inferred — that slice arrives in 04-03's merge_inferred implementation).

Purpose: Substrate for BIBLE-01 ("Bible persists per series + loaded as context") and BIBLE-05 ("Register grounded in media metadata"). The arr_metadata snapshot freezes the discovery payload so Phase 5's Pass-1 can ground register inference without re-calling *arr APIs. Lazy creation matches Phase 3's "API for knowledge, filesystem for action" pattern (D-22).

Output: A Series row appears in SQLite the first time any episode of a series is processed; subsequent processings of the same series are idempotent; `load_series_bible(series_id)` returns a `SeriesBibleDTO` carrying register=None, the captured arr_metadata, and empty characters/terms lists. Phase 5's Pass-1 has everything it needs to start inferring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-series-bible-store-schema/04-CONTEXT.md
@.planning/phases/04-series-bible-store-schema/04-RESEARCH.md
@.planning/phases/04-series-bible-store-schema/04-PATTERNS.md
@.planning/phases/04-series-bible-store-schema/04-01-SUMMARY.md
@.planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md
@CLAUDE.md
@trezarr/bible/models.py
@trezarr/arr/sonarr.py
@trezarr/arr/radarr.py
@trezarr/paths.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MediaItem with arr_metadata fields + populate at Sonarr/Radarr discovery</name>
  <files>
    trezarr/arr/sonarr.py,
    trezarr/arr/radarr.py,
    tests/arr/test_media_item_arr_metadata.py
  </files>
  <read_first>
    - trezarr/arr/sonarr.py lines 49-72 (MediaItem dataclass — current shape) AND lines 141-167 (the construction site inside discover_sonarr_items that this task extends)
    - trezarr/arr/radarr.py (full — to mirror the extension and find the equivalent population site)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md §"trezarr/arr/sonarr.py (MODIFIED — extend MediaItem dataclass)"
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Open Question 1" + §"Pattern 6: Lazy Series-Row Creation"
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-33 (tvdb_id/tmdb_id captured at creation) / D-35 (arr_metadata snapshot fields)
    - .planning/phases/03-arr-integration-first-vertical-slice/03-CONTEXT.md §"decisions" D-22 (MediaItem shape — must remain backwards compatible)
  </read_first>
  <behavior>
    - Test 1 (tests/arr/test_media_item_arr_metadata.py::test_sonarr_media_item_carries_arr_metadata_fields): stub the pyarr `Sonarr.series.get()` response to include `tvdbId`, `genres`, `overview`, `year`, `network`, `runtime`; call `discover_sonarr_items(settings)`; the returned MediaItem has `arr_kind="sonarr"`, `tvdb_id=<value>`, `genres=<list>`, `overview=<str>`, `year=<int>`, `network=<str>`, `runtime=<int>`, `tmdb_id=None`.
    - Test 2 (tests/arr/test_media_item_arr_metadata.py::test_radarr_media_item_carries_arr_metadata_fields): stub pyarr `Radarr.movie.get()` with `tmdbId`, `genres`, `overview`, `year`, `runtime` (movies have no network); the returned MediaItem has `arr_kind="radarr"`, `tmdb_id=<value>`, `tvdb_id=None`, `network=None`, plus the populated `genres`/`overview`/`year`/`runtime`.
    - Test 3 (tests/arr/test_media_item_arr_metadata.py::test_media_item_extension_is_backwards_compatible): constructing MediaItem with ONLY the original Phase-3 positional/keyword args (local_path, title, source_type, series_id, season_number) still works — all Phase-4 fields default to None.
    - Test 4 (tests/arr/test_media_item_arr_metadata.py::test_arr_metadata_fields_default_none_when_payload_missing): stub a pyarr payload that omits `genres`/`overview`/`year`/`network`/`runtime` (a sparse *arr install); the MediaItem still constructs with those fields as None — no KeyError.
  </behavior>
  <action>
    Extend the `MediaItem` dataclass in `trezarr/arr/sonarr.py` (lines 49-72) by APPENDING new optional fields per PATTERNS §"trezarr/arr/sonarr.py":
    - Append after the existing fields, under a header comment `# ── Phase 4: arr_metadata snapshot fields (D-33, D-35) ──`:
      - `arr_kind: str | None = None`  (values: "sonarr" | "radarr")
      - `tvdb_id: int | None = None`
      - `tmdb_id: int | None = None`
      - `genres: list[str] | None = None`
      - `overview: str | None = None`
      - `year: int | None = None`
      - `network: str | None = None`
      - `runtime: int | None = None`

    All new fields default to `None` — additive change, never breaks existing constructors (CONTEXT.md §code_context "Established Patterns"). Update the dataclass docstring `Attributes:` block to document each new field (mirror the prose style of existing entries).

    Update the docstring at the top of `trezarr/arr/sonarr.py` to add a `D-35` line under `Design decisions honoured:` noting the arr_metadata snapshot extension.

    Update `discover_sonarr_items` at the construction site (lines 158-167) — augment the existing `MediaItem(...)` keyword arg list to populate the new fields from the already-fetched `series` dict:
    - `arr_kind="sonarr"`
    - `tvdb_id=series.get("tvdbId")`  (note camelCase JSON key from Sonarr API)
    - `genres=series.get("genres")`
    - `overview=series.get("overview")`
    - `year=series.get("year")`
    - `network=series.get("network")`
    - `runtime=series.get("runtime")`

    Do NOT set `tmdb_id` for Sonarr (defaults to None — Sonarr has no TMDB id).

    Mirror the same extension in `trezarr/arr/radarr.py` at the equivalent construction site. For Radarr movies:
    - `arr_kind="radarr"`
    - `tmdb_id=movie.get("tmdbId")`
    - `tvdb_id=movie.get("tvdbId")` — Radarr movies DO carry tvdbId; capture it if present (defensive; defaults None)
    - `genres=movie.get("genres")`
    - `overview=movie.get("overview")`
    - `year=movie.get("year")`
    - `runtime=movie.get("runtime")` — note: movies use `runtime` (minutes) not `runtimeMinutes`
    - Do NOT set `network` (Radarr movies have no network — leave default None)

    Where `radarr.py` re-exports MediaItem from sonarr (per STATE.md "MediaItem dataclass owned by trezarr.arr.sonarr; re-imported by radarr.py"), the import already brings the extended shape — no second dataclass definition needed.

    Create `tests/arr/test_media_item_arr_metadata.py` with the four tests above. Use pytest-httpx (already installed Phase 3) to stub pyarr's HTTP layer; mirror the existing test pattern in `tests/arr/test_sonarr.py` for fixture setup.
  </action>
  <verify>
    <automated>uv run pytest tests/arr/test_media_item_arr_metadata.py tests/arr/ -x &amp;&amp; uv run python -c "from trezarr.arr.sonarr import MediaItem; import inspect; fields = [f.name for f in MediaItem.__dataclass_fields__.values()]; expected = {'local_path','title','source_type','series_id','season_number','arr_kind','tvdb_id','tmdb_id','genres','overview','year','network','runtime'}; assert expected.issubset(set(fields)), 'missing fields: ' + str(expected - set(fields)); print('MediaItem fields:', fields)"</automated>
  </verify>
  <done>
    - MediaItem dataclass carries the 8 new Phase-4 fields, all defaulting to None (backwards-compatible).
    - `discover_sonarr_items` populates `arr_kind="sonarr"`, tvdb_id, genres, overview, year, network, runtime from the pyarr series payload.
    - `discover_radarr_items` populates `arr_kind="radarr"`, tmdb_id, genres, overview, year, runtime (and tvdb_id when present) from the pyarr movie payload.
    - All existing Phase-3 arr tests continue to pass.
    - New test file proves the field shape, arr_kind discrimination, and graceful handling of sparse arr payloads.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bible DTO boundary + store.py with get_or_create_series + load_series_bible</name>
  <files>
    trezarr/bible/dto.py,
    trezarr/bible/store.py,
    tests/bible/test_dto_boundary.py,
    tests/bible/test_lazy_series_create.py,
    tests/bible/test_arr_metadata_snapshot.py
  </files>
  <read_first>
    - trezarr/bible/models.py (created in 04-01 — Series + Character + TermDictionary shape; required to know what to validate from_attributes into)
    - trezarr/paths.py lines 43-56 (PathMapping — the only existing Pydantic BaseModel boundary; mirror its docstring + ConfigDict pattern)
    - trezarr/output/ledger.py lines 71-82 (the small typed public-API style — `Interface contract:` block in the class docstring)
    - tests/db/conftest.py (created in 04-01 — db_engine + session_factory fixtures this plan uses)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md §"trezarr/bible/dto.py" + §"trezarr/bible/store.py" + §"tests/bible/test_lazy_series_create.py"
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Pattern 2: async_sessionmaker + session-per-transaction" + §"Pattern 6: Lazy Series-Row Creation" + §"Code Examples: Loading a series Bible" + §"Code Examples: Get-or-create series" + §"Pitfall 2: expire_on_commit=True deadlocks async sessions" + §"Pitfall 7: session.add() of a Pydantic DTO"
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-33 / D-35 / D-36 / D-39
  </read_first>
  <behavior>
    - Test 1 (tests/bible/test_dto_boundary.py::test_no_sqlalchemy_in_bible_dto_namespace): `import trezarr.bible.dto` succeeds; use `inspect.getsource(trezarr.bible.dto)` and assert the source string contains no `from sqlalchemy` or `import sqlalchemy` lines. This is an import-graph contract test — checking source directly is more robust than inspecting `dir()` names, which can include re-exported symbols that don't reveal the actual import relationship.
    - Test 2 (tests/bible/test_dto_boundary.py::test_series_dto_round_trips_from_sqla_row): create a Series row via the session_factory; `SeriesDTO.model_validate(row, from_attributes=True)` produces a DTO with identical field values; the DTO has `model_config = ConfigDict(from_attributes=True)`.
    - Test 3 (tests/bible/test_lazy_series_create.py::test_first_call_inserts_series_row): call `await get_or_create_series(session_factory, arr_kind="sonarr", arr_instance="default", arr_series_id=42, arr_metadata_snapshot={"genres": ["drama"], "year": 2020})`; SELECT FROM series returns 1 row matching the triple identity; returned SeriesDTO carries register=None, arr_metadata={"genres": ["drama"], "year": 2020}, tvdb_id=None.
    - Test 4 (tests/bible/test_lazy_series_create.py::test_second_call_returns_same_row): two consecutive calls with the same (arr_kind, arr_instance, arr_series_id) return DTOs with the SAME `id`; SELECT COUNT(*) FROM series == 1. Proves D-36 idempotency + D-33 UNIQUE constraint behavior.
    - Test 5 (tests/bible/test_lazy_series_create.py::test_different_arr_kind_creates_separate_rows): `get_or_create_series(arr_kind="sonarr", arr_series_id=42)` and `get_or_create_series(arr_kind="radarr", arr_series_id=42)` produce two distinct rows. Proves the UNIQUE constraint is on the full triple, not just arr_series_id.
    - Test 6 (tests/bible/test_lazy_series_create.py::test_tvdb_tmdb_captured_at_creation): pass `tvdb_id=12345, tmdb_id=None` to `get_or_create_series`; SELECT returns a Series with tvdb_id=12345, tmdb_id=None. Proves D-33 denormalized capture (these are NOT used for lookup in v1 but ARE persisted for the v2 portability hook).
    - Test 7 (tests/bible/test_arr_metadata_snapshot.py::test_arr_metadata_round_trips_as_dict): create a series with `arr_metadata_snapshot={"genres": ["drama", "thriller"], "overview": "A spy story.", "year": 2020, "network": "BBC", "runtime": 60}`; reload via `load_series_bible(series_id)`; the returned `SeriesBibleDTO.arr_metadata` equals the original dict (lists and nested values preserved through JSON round-trip).
    - Test 8 (tests/bible/test_arr_metadata_snapshot.py::test_register_starts_null): newly-created series has `register is None`; Phase 4 NEVER sets it (Phase 5 will via merge_inferred — that arrives in 04-03 as the merge primitive). Document this in the test's docstring.
    - Test 9 (tests/bible/test_lazy_series_create.py::test_get_or_create_uses_single_transaction): use a SQLAlchemy event listener (or check `session.in_transaction()` from inside a patched callback) to assert the SELECT+INSERT happens inside ONE `session.begin()` block — never two transactions. Prevents a race where two concurrent first-translates of the same series would both INSERT (Phase 7 preview from RESEARCH Pitfall 9).
    - Test 10 (tests/bible/test_lazy_series_create.py::test_arr_metadata_size_validation): passing an `arr_metadata_snapshot` larger than 32 KB raises a clear ValueError before INSERT (RESEARCH Security Domain — DoS hardening; cap is a defense-in-depth measure since arr payloads should never approach this). NOTE: the size is measured using `json.dumps(arr_metadata_snapshot, ensure_ascii=False).encode("utf-8")` — this correctly counts UTF-8 bytes for non-ASCII (Vietnamese) values rather than ASCII-escaped character sequences.
    - Test 11 (tests/bible/test_arr_metadata_snapshot.py::test_arr_metadata_utf8_byte_cap_with_vietnamese): create an arr_metadata_snapshot containing Vietnamese text (e.g. `{"overview": "Câu chuyện trinh thám."}`); verify the 32 KB cap calculation uses `ensure_ascii=False` so Vietnamese characters (3 bytes each in UTF-8) count correctly rather than as 6-char `\uXXXX` escape sequences. The test constructs a payload that would exceed 32 KB only if measured with `ensure_ascii=True` but stays under with `ensure_ascii=False` — assert it is accepted.
    - Test 12 (tests/bible/test_dto_boundary.py::test_bible_event_dto_created_at_is_datetime): `BibleEventDTO(id=1, series_id=1, entity_type="character", entity_id=2, field="role", source="inference", created_at="2026-01-01T00:00:00")` — Pydantic v2 coerces the ISO-8601 string to a `datetime`; `isinstance(dto.created_at, datetime)` is True. Proves the `datetime | None` type annotation is correctly handled.
  </behavior>
  <action>
    Create `trezarr/bible/dto.py` per PATTERNS §"trezarr/bible/dto.py":

    Module docstring opens with `"""Pydantic DTOs at the trezarr.bible.store boundary (D-39).\n\nSQLAlchemy models stay inside store.py; everything that crosses out is a DTO.\nField names mirror the SQLAlchemy column names 1:1 so model_validate(row,\nfrom_attributes=True) is a no-op rename."""`.

    Imports (strictly): `from __future__ import annotations`; `from datetime import datetime`; `from typing import Any`; `from pydantic import BaseModel, ConfigDict`. NO SQLAlchemy imports (D-39 — enforced by Test 1).

    Define the following BaseModel classes; each has `model_config = ConfigDict(from_attributes=True)`:
    - `class SeriesDTO(BaseModel):` with fields matching Series model columns: `id: int`, `arr_kind: str`, `arr_instance: str`, `arr_series_id: int`, `tvdb_id: int | None = None`, `tmdb_id: int | None = None`, `register: str | None = None`, `arr_metadata: dict[str, Any] = {}`, `locked_fields: list[str] = []`.
    - `class CharacterDTO(BaseModel):` — fields: `id: int`, `series_id: int`, `original_latin_name: str`, `gender: str | None = None`, `rough_age: str | None = None`, `role: str | None = None`, `locked_fields: list[str] = []`.
    - `class TermDTO(BaseModel):` — fields: `id: int`, `series_id: int`, `source_term: str`, `vietnamese_rendering: str`, `category: str | None = None`, `locked_fields: list[str] = []`.
    - `class BibleEventDTO(BaseModel):` (used by 04-03 but defined here for the DTO boundary): `id: int`, `series_id: int`, `episode_key: str | None = None`, `entity_type: str`, `entity_id: int`, `field: str`, `old_value: Any = None`, `new_value: Any = None`, `source: str`, `created_at: datetime | None = None`. NOTE: `created_at` is typed as `datetime | None` (NOT `Any`) — Pydantic v2 automatically coerces ISO-8601 strings to `datetime` when the field is annotated as `datetime`, so this works correctly whether the value comes from SQLAlchemy (a `datetime` object) or JSON serialization (an ISO-8601 string). This was `Any` in the previous version — changed per cross-AI review MEDIUM finding.
    - `class SeriesBibleDTO(BaseModel):` — the aggregate that `load_series_bible` returns: `id: int`, `arr_kind: str`, `arr_instance: str`, `arr_series_id: int`, `register: str | None = None`, `arr_metadata: dict[str, Any] = {}`, `characters: list[CharacterDTO] = []`, `terms: list[TermDTO] = []`, `locked_fields: list[str] = []`. NOTE: arr_metadata default uses `= {}` literal (Pydantic v2 copies model defaults per-instance per PATTERNS §"trezarr/bible/dto.py"); locked_fields default uses `= []` for the same reason.

    Create `trezarr/bible/store.py` per PATTERNS §"trezarr/bible/store.py" + RESEARCH Code Examples:

    Module docstring opens with `"""Series Bible store — Pydantic-typed API hiding SQLAlchemy internals (D-39).\n\nPublic API (the ONLY surface Phase 5+ may import):\n    load_series_bible(session_factory, series_id) -> SeriesBibleDTO\n    get_or_create_series(session_factory, *, arr_kind, arr_instance, arr_series_id, arr_metadata_snapshot, tvdb_id=None, tmdb_id=None) -> SeriesDTO\n\nInternal:\n    All SQLAlchemy imports stay inside this module. Sessions are opened with\n    `async with session_factory()` and never escape.\n\nKnown limitations:\n    processed_file.source_path uniqueness is path-string equality only. Case\n    sensitivity depends on the host filesystem (case-insensitive on macOS/Windows,\n    case-sensitive on Linux). If the same file is referenced with different path\n    casing on a case-insensitive filesystem, an IntegrityError may be raised\n    rather than finding the existing row. This is an accepted v1 trade-off."""`.

    Add a `Design decisions honoured:` block citing D-32 (atomic txn), D-33 (UNIQUE identity), D-35 (arr_metadata snapshot), D-36 (lazy creation), D-39 (DTO boundary), Pitfall 2 (expire_on_commit=False propagates from the sessionmaker), Pitfall 7 (never `session.add(dto)`).

    Imports: `from __future__ import annotations`; stdlib (`import json`, `import logging`, `from typing import Any`); SQLAlchemy (`from sqlalchemy import select`, `from sqlalchemy.orm import selectinload`, `from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession`); project (`from trezarr.bible.models import Series, Character, TermDictionary`, `from trezarr.bible.dto import SeriesDTO, SeriesBibleDTO, CharacterDTO, TermDTO`).

    Define module-level constant `MAX_ARR_METADATA_BYTES = 32 * 1024  # 32 KB — DoS hardening per RESEARCH §Security Domain`.

    Define `async def get_or_create_series(session_factory: async_sessionmaker[AsyncSession], *, arr_kind: str, arr_instance: str = "default", arr_series_id: int, arr_metadata_snapshot: dict[str, Any], tvdb_id: int | None = None, tmdb_id: int | None = None) -> SeriesDTO:`. Body:
    - Validate `arr_metadata_snapshot` size: `if len(json.dumps(arr_metadata_snapshot, ensure_ascii=False).encode("utf-8")) > MAX_ARR_METADATA_BYTES: raise ValueError(f"arr_metadata snapshot exceeds {MAX_ARR_METADATA_BYTES} bytes")`. NOTE: `ensure_ascii=False` is required so Vietnamese text (3 bytes/char in UTF-8) measures as its actual UTF-8 byte length rather than as `\uXXXX` escape sequences (6 ASCII chars) — per cross-AI review MEDIUM finding.
    - `async with session_factory() as session: async with session.begin():` — single transaction for SELECT-then-INSERT (RESEARCH Code Example "Get-or-create series").
    - Inside the txn: `stmt = select(Series).where(Series.arr_kind == arr_kind, Series.arr_instance == arr_instance, Series.arr_series_id == arr_series_id)`; `row = (await session.execute(stmt)).scalar_one_or_none()`.
    - If `row is None`: construct `row = Series(arr_kind=arr_kind, arr_instance=arr_instance, arr_series_id=arr_series_id, tvdb_id=tvdb_id, tmdb_id=tmdb_id, arr_metadata=arr_metadata_snapshot, register=None, locked_fields=[])`; `session.add(row)`; `await session.flush()` to populate `row.id`.
    - After the `with session.begin()` block exits (auto-commit), return `SeriesDTO.model_validate(row, from_attributes=True)`.
    - Log at info level on creation: `logger.info("Created series row: %s/%s/%s (id=%d)", arr_kind, arr_instance, arr_series_id, row.id)`.

    Define `async def load_series_bible(session_factory: async_sessionmaker[AsyncSession], series_id: int) -> SeriesBibleDTO:` per RESEARCH Code Example "Loading a series Bible":
    - `async with session_factory() as session:`
    - `stmt = select(Series).where(Series.id == series_id).options(selectinload(Series.characters), selectinload(Series.terms))`.
    - `row = (await session.execute(stmt)).scalar_one()` (raises NoResultFound if missing — caller's responsibility to handle).
    - Construct `SeriesBibleDTO(id=row.id, arr_kind=row.arr_kind, arr_instance=row.arr_instance, arr_series_id=row.arr_series_id, register=row.register, arr_metadata=row.arr_metadata, locked_fields=row.locked_fields, characters=[CharacterDTO.model_validate(c, from_attributes=True) for c in row.characters], terms=[TermDTO.model_validate(t, from_attributes=True) for t in row.terms])`.

    Add a module-level `logger = logging.getLogger(__name__)`.

    Create test files:
    - `tests/bible/test_dto_boundary.py` — Tests 1, 2, 12 above. For Test 1: use `inspect.getsource(trezarr.bible.dto)` and assert the source string contains no `from sqlalchemy` or `import sqlalchemy` lines — this is the import-graph contract test (replaces the brittle `dir()` name-inspection). Test 12 validates BibleEventDTO.created_at as `datetime | None`.
    - `tests/bible/test_lazy_series_create.py` — Tests 3, 4, 5, 6, 9, 10 above. Tests are `async def`; use the `session_factory` fixture from `tests/db/conftest.py`.
    - `tests/bible/test_arr_metadata_snapshot.py` — Tests 7, 8, 11 above. Round-trip the dict, verify `register is None`, verify Vietnamese UTF-8 byte cap uses ensure_ascii=False.
  </action>
  <verify>
    <automated>uv run pytest tests/bible/test_dto_boundary.py tests/bible/test_lazy_series_create.py tests/bible/test_arr_metadata_snapshot.py tests/bible/test_models.py -x &amp;&amp; uv run python -c "import inspect, trezarr.bible.dto as dto_mod; src = inspect.getsource(dto_mod); bad = [line for line in src.splitlines() if line.strip().startswith(('from sqlalchemy', 'import sqlalchemy'))]; assert not bad, f'DTO layer leaks SQLA imports: {bad}'; print('DTO boundary clean (import-graph check)')" &amp;&amp; uv run python -c "from trezarr.bible.store import get_or_create_series, load_series_bible; print('store API exports OK')"</automated>
  </verify>
  <done>
    - `trezarr/bible/dto.py` exists with 5 Pydantic BaseModel classes (SeriesDTO, CharacterDTO, TermDTO, BibleEventDTO, SeriesBibleDTO), each with `from_attributes=True`, NO SQLAlchemy imports.
    - `BibleEventDTO.created_at` is typed as `datetime | None` (not `Any`) — per cross-AI review MEDIUM finding.
    - `trezarr/bible/store.py` exposes `get_or_create_series` and `load_series_bible` as the public async API; SQLAlchemy stays inside this module (D-39).
    - `get_or_create_series` enforces D-33 UNIQUE-triple identity, captures D-35 arr_metadata snapshot, persists D-33 tvdb_id/tmdb_id, sets register=None (D-35), and runs SELECT+INSERT in a single `session.begin()` block.
    - arr_metadata size cap uses `json.dumps(..., ensure_ascii=False).encode("utf-8")` for correct Vietnamese byte counting.
    - `processed_file.source_path` case-sensitivity caveat documented in store.py docstring (known v1 limitation).
    - `load_series_bible` eagerly loads characters + terms via `selectinload` and returns a fully-populated `SeriesBibleDTO`.
    - The DTO boundary test uses `inspect.getsource` (import-graph contract test) not brittle `dir()` name inspection.
    - All 12 new tests pass; existing Phase 1/2/3/04-01 tests continue to pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arr API response → MediaItem | pyarr JSON payloads from Sonarr/Radarr; caller is the operator's own *arr instance — low external threat but fields are .get()-accessed defensively |
| arr_metadata_snapshot → SQLite | The 32 KB cap enforced before INSERT guards against oversized payloads from misbehaving *arr instances |
| DTO boundary → callers | SeriesBibleDTO + CharacterDTO etc. cross the store API surface; SQLAlchemy models must not escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | `get_or_create_series` arr_metadata INSERT | mitigate | 32 KB cap via `json.dumps(..., ensure_ascii=False).encode("utf-8")` before INSERT; Vietnamese text measured in UTF-8 bytes |
| T-04-06 | Tampering | `processed_file.source_path` uniqueness | accept | v1 trade-off: case-sensitivity depends on host filesystem; documented in store.py docstring; acceptable for single-user self-hosted operator |
| T-04-07 | Information Disclosure | DTO layer leaking SQLAlchemy types | mitigate | D-39 enforced by import-graph contract test (Test 1 in test_dto_boundary.py) using `inspect.getsource` |
| T-04-SC | Tampering | npm/pip install (no new packages this plan) | accept | No new package installs in this plan — inherits from 04-01 |
</threat_model>

<verification>
- `uv run pytest tests/bible tests/arr tests/db -x` — all tests in these directories pass.
- `uv run pytest -x` — full project suite stays green.
- `uv run python -c "from trezarr.arr.sonarr import MediaItem; m = MediaItem(local_path='/x', title='T', source_type='episode', arr_kind='sonarr', tvdb_id=1, genres=['a']); print(m)"` — MediaItem constructs with the new fields.
- `uv run python -c "import inspect, trezarr.bible.dto as m; src = inspect.getsource(m); assert 'sqlalchemy' not in src.lower(), 'SQLA leak found'; print('DTO clean')"` — D-39 boundary verified via import-graph source check.
- `uv run python -c "from trezarr.bible.dto import BibleEventDTO; from datetime import datetime; dto = BibleEventDTO(id=1, series_id=1, entity_type='character', entity_id=2, field='role', source='inference', created_at='2026-01-01T00:00:00'); assert isinstance(dto.created_at, datetime), type(dto.created_at); print('BibleEventDTO.created_at is datetime:', dto.created_at)"` — created_at coerces to datetime.
- `grep -nE "^(from sqlalchemy|import sqlalchemy)" trezarr/bible/dto.py` returns NOTHING — D-39 boundary verified.
- `grep -nE "^(from trezarr.bible.models|from trezarr.db)" trezarr/arr/` returns NOTHING — the arr/ layer does NOT depend on the bible/ layer.
- `grep -n "ensure_ascii=False" trezarr/bible/store.py` returns one match in `get_or_create_series` — Vietnamese byte cap uses UTF-8 encoding.
</verification>

<success_criteria>
- BIBLE-01 substrate is in place: a Series row persists in SQLite the first time an episode of a series is processed, and is found on subsequent processings (lazy + idempotent per D-36).
- BIBLE-05 substrate is in place: `series.arr_metadata` is populated with the genre/overview/year/network/runtime snapshot from arr discovery (per D-35); register stays NULL waiting for Phase 5.
- MediaItem extension lands at the discovery layer (per RESEARCH Open Question 1 recommendation): Phase 5 reads these fields from MediaItem, never re-fetches.
- `trezarr/bible/dto.py` is a strict Pydantic boundary (D-39): no SQLAlchemy import is reachable through any name in the module (verified via import-graph source inspection, not brittle dir() name-inspection).
- `BibleEventDTO.created_at` is typed as `datetime | None` — Pydantic v2 coerces ISO-8601 strings automatically.
- `get_or_create_series` arr_metadata size cap uses `ensure_ascii=False` for correct Vietnamese UTF-8 byte measurement.
- `processed_file.source_path` case-sensitivity caveat documented in store.py docstring (known v1 limitation).
- `trezarr/bible/store.py` exposes only Pydantic DTOs to callers; SQLAlchemy stays inside this module.
- `get_or_create_series` runs SELECT+INSERT in a single `session.begin()` block (atomic; prevents Phase-7 dual-insert races per RESEARCH Pitfall 9).
- arr_metadata size cap enforces 32 KB upper bound (defense-in-depth per Security Domain).
- All 12 new tests pass; the existing Phase 1/2/3 + 04-01 suites stay green.
</success_criteria>

<output>
Create `.planning/phases/04-series-bible-store-schema/04-02-SUMMARY.md` when done.
</output>
