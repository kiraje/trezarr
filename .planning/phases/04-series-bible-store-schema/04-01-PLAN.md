---
phase: 04-series-bible-store-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - trezarr/db/__init__.py
  - trezarr/db/base.py
  - trezarr/db/engine.py
  - trezarr/db/session.py
  - trezarr/db/migration_runner.py
  - trezarr/bible/__init__.py
  - trezarr/bible/models.py
  - trezarr/config.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_baseline_bible_schema.py
  - tests/db/__init__.py
  - tests/db/conftest.py
  - tests/db/test_engine.py
  - tests/db/test_migrations.py
  - tests/bible/__init__.py
  - tests/bible/test_models.py
autonomous: true
requirements: []
tags: [persistence, sqlite, sqlalchemy, alembic, async, schema]

must_haves:
  truths:
    - "Per D-38, SQLAlchemy 2.0 async + aiosqlite + Alembic is installed and importable"
    - "Per D-38, build_engine(settings) returns an AsyncEngine that applies WAL+FK+synchronous PRAGMAs on EVERY pool connection (not just the first)"
    - "Per D-31, exactly one Alembic baseline migration creates all 7 tables: series, character, term_dictionary, address_map, relationship_event, bible_event, processed_file"
    - "Per D-33, the series table enforces UNIQUE(arr_kind, arr_instance, arr_series_id) and stores tvdb_id/tmdb_id as denormalized nullable columns"
    - "Per D-34, every entity table (series, character, term_dictionary) carries a locked_fields JSON column defaulting to []"
    - "Per D-35, series.arr_metadata is a JSON column and series.register starts NULL"
    - "Per D-32, bible_event records (series_id, episode_key, entity_type, entity_id, field, old_value, new_value, source, created_at) with source ∈ {inference, lock, import, system}"
    - "Per D-39, no test or downstream module imports SQLAlchemy through trezarr.bible — SQLA stays inside store.py (which arrives in 04-02/04-03)"
    - "Test fixtures use temp-file SQLite (tmp_path), never :memory:, so the real Alembic migration runs on every test"
  artifacts:
    - path: "trezarr/db/engine.py"
      provides: "build_engine(settings) -> AsyncEngine with PRAGMA event listener bound to sync_engine"
      contains: "event.listens_for"
    - path: "trezarr/db/session.py"
      provides: "build_session_factory(engine) -> async_sessionmaker with expire_on_commit=False"
      contains: "expire_on_commit=False"
    - path: "trezarr/db/migration_runner.py"
      provides: "run_migrations_to_head(engine) — connection.run_sync(do_upgrade) bridge"
      contains: "run_sync"
    - path: "trezarr/db/base.py"
      provides: "Base(DeclarativeBase) — SQLAlchemy 2.0 typed declarative root"
      contains: "DeclarativeBase"
    - path: "trezarr/bible/models.py"
      provides: "Series, Character, TermDictionary, AddressMap, RelationshipEvent, BibleEvent, ProcessedFile models"
      contains: "class Series(Base):"
    - path: "alembic/env.py"
      provides: "async-aware env.py supporting run_sync injection + offline mode + render_as_batch=True"
      contains: "render_as_batch=True"
    - path: "alembic/versions/0001_baseline_bible_schema.py"
      provides: "Single baseline migration that creates all 7 Bible tables in upgrade() and drops them in downgrade()"
      contains: "create_table('series'"
    - path: "tests/db/conftest.py"
      provides: "db_engine (temp-file SQLite + migrated) and session_factory fixtures"
      contains: "pytest_asyncio.fixture"
  key_links:
    - from: "trezarr/db/engine.py"
      to: "PRAGMA foreign_keys / journal_mode=WAL / synchronous=NORMAL"
      via: "event.listens_for(engine.sync_engine, 'connect')"
      pattern: "event\\.listens_for\\(engine\\.sync_engine"
    - from: "trezarr/db/migration_runner.py"
      to: "alembic.command.upgrade"
      via: "await conn.run_sync(_do_upgrade)"
      pattern: "run_sync"
    - from: "alembic/env.py"
      to: "trezarr.bible.models"
      via: "import-side-effect populates Base.metadata"
      pattern: "from trezarr\\.bible import models"
    - from: "trezarr/config.py"
      to: "trezarr/db/engine.py"
      via: "settings.bible_db_url passed to build_engine"
      pattern: "bible_db_url"
---

<objective>
Stand up the SQLAlchemy 2.0 async + aiosqlite + Alembic persistence backbone for Phase 4. Install the three new packages, build the engine/session/migration scaffolding, hand-author a single baseline Alembic migration that creates all seven long-term Bible tables (D-31), define the SQLAlchemy 2.0 typed models behind a `DeclarativeBase` root, extend `TrezarrSettings` with the Phase-4 DB-URL fields, and create the test fixtures + smoke tests proving the migration produces all 7 tables with the right PRAGMAs enforced per-connection. No store API, no merge logic, no JSON-ledger migration — those are 04-02/03/04. This plan unblocks all of them.

Purpose: Plans 04-02, 04-03, and 04-04 all depend on a working engine + migrated schema + DTO/model layer. Centralising the bootstrapping in one plan keeps the three downstream plans focused on their respective slices (series/register, character/term/merge, ledger swap).

Output: Working `trezarr/db/` + `trezarr/bible/models.py` + Alembic scaffolding + a clean test suite that proves: (1) packages installed, (2) PRAGMAs apply per-connection, (3) baseline migration creates 7 tables, (4) round-tripping a Series/Character/TermDictionary row works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-series-bible-store-schema/04-CONTEXT.md
@.planning/phases/04-series-bible-store-schema/04-RESEARCH.md
@.planning/phases/04-series-bible-store-schema/04-PATTERNS.md
@CLAUDE.md
@trezarr/config.py
@trezarr/output/ledger.py
@trezarr/llm/client.py
@trezarr/paths.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Phase-4 deps + Alembic scaffolding + DB+bible package skeletons</name>
  <files>
    pyproject.toml, uv.lock, .gitignore,
    trezarr/db/__init__.py, trezarr/db/base.py,
    trezarr/db/engine.py, trezarr/db/session.py, trezarr/db/migration_runner.py,
    trezarr/bible/__init__.py,
    trezarr/config.py,
    alembic.ini, alembic/env.py, alembic/script.py.mako,
    tests/db/__init__.py, tests/db/conftest.py, tests/db/test_engine.py
  </files>
  <read_first>
    - pyproject.toml (current dependency manifest — append, do NOT replace)
    - trezarr/config.py (phase-grouped settings; mirror the `# ── Phase N: <topic> (D-XX) ──` convention)
    - trezarr/llm/client.py (closest analog for engine.py: factory-from-settings, TYPE_CHECKING import of TrezarrSettings, module docstring with `Design decisions honoured:` block)
    - trezarr/paths.py lines 171-220 (probe_media_roots — fail-fast sys.exit pattern for run_migrations_to_head failures referenced by 04-04)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md §"trezarr/db/engine.py" + §"trezarr/db/session.py" + §"trezarr/db/migration_runner.py" + §"trezarr/db/base.py" + §"alembic.ini / alembic/env.py / alembic/versions/0001_baseline.py" + §"Module Docstring Convention"
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Pattern 1: SQLAlchemy 2.0 Async Engine + PRAGMAs at Connect" + §"Pattern 2: async_sessionmaker + session-per-transaction" + §"Pattern 4: Alembic Async env.py + Programmatic Upgrade at Startup" + §"Pitfall 1, 2, 4, 6, 8"
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-31 / D-38
    - CLAUDE.md §"Recommended Stack" (verify SQLAlchemy 2.0.x / aiosqlite 0.22.x / alembic 1.18.x version floors)
  </read_first>
  <behavior>
    - Test 1 (tests/db/test_engine.py::test_foreign_keys_enabled_per_connection): opening two distinct connections from the engine pool both report `PRAGMA foreign_keys=1`. Proves D-38 per-connection FK enforcement (Pitfall 4).
    - Test 2 (tests/db/test_engine.py::test_wal_mode_set): `PRAGMA journal_mode` on a fresh DB returns `wal`. Proves D-38 WAL mode.
    - Test 3 (tests/db/test_engine.py::test_synchronous_normal): `PRAGMA synchronous` returns `1` (NORMAL). Proves D-38 synchronous=NORMAL.
    - Test 4 (tests/db/test_engine.py::test_settings_toggles_disable_fk): with `bible_db_enforce_fk=False`, `PRAGMA foreign_keys` returns `0`. Proves the optional toggle wires through.
    - All four tests use a temp-file SQLite (`tmp_path / "trezarr.db"`) via the `bible_db_url=f"sqlite+aiosqlite:///{db_path}"` settings field — never `:memory:` (Pitfall 3).
  </behavior>
  <action>
    Run `uv add "sqlalchemy[asyncio]>=2.0.50,<2.1" "aiosqlite>=0.22.1,<0.23" "alembic>=1.18.4,<1.19"` from the repo root. This updates pyproject.toml `[project.dependencies]` and uv.lock atomically. Verify Python floor 3.12 is unchanged (CLAUDE.md D-01).

    Append to .gitignore (after the existing `*.db` line at line 31): `*.db-wal` and `*.db-shm` on their own lines so WAL sidecar files do not get committed.

    Create `trezarr/db/__init__.py` (empty package marker — single-line docstring `"""SQLAlchemy 2.0 async engine + session + migration runner for Phase 4."""`).

    Create `trezarr/db/base.py` — module docstring with `Design decisions honoured:` block citing D-31 (single baseline) and D-38 (typed declarative); `from __future__ import annotations`; `from sqlalchemy.orm import DeclarativeBase`; single class `Base(DeclarativeBase)` with docstring "Root DeclarativeBase for all Trezarr SQLAlchemy 2.0 models."

    Create `trezarr/db/engine.py` per PATTERNS §"trezarr/db/engine.py":
    - Module docstring with `Design decisions honoured:` block citing D-38 (engine + PRAGMA per-connect) and Pitfall 4 (FK is per-connection — `event.listens_for(engine.sync_engine, "connect")`).
    - `from __future__ import annotations`; stdlib then SA imports per PATTERNS §"Import Order Convention".
    - `if TYPE_CHECKING:` import of `TrezarrSettings` (avoid cycle through config.py).
    - One module-level `logger = logging.getLogger(__name__)`.
    - `def build_engine(settings: "TrezarrSettings") -> AsyncEngine:` — calls `create_async_engine(settings.bible_db_url, echo=False, future=True)`; registers `@event.listens_for(engine.sync_engine, "connect")` callback `_set_sqlite_pragma(dbapi_conn, _connection_record)`. Inside the callback: `cursor = dbapi_conn.cursor()`; if `settings.bible_db_enable_wal`: `cursor.execute("PRAGMA journal_mode=WAL")`; always `cursor.execute("PRAGMA synchronous=NORMAL")`; if `settings.bible_db_enforce_fk`: `cursor.execute("PRAGMA foreign_keys=ON")`; `cursor.close()`. Return the engine.

    Create `trezarr/db/session.py` per PATTERNS §"trezarr/db/session.py":
    - Module docstring with `Design decisions honoured:` D-38 + D-39 + Pitfall 2 (expire_on_commit=False is mandatory; default True deadlocks async sessions post-commit).
    - `from __future__ import annotations`; `from sqlalchemy.ext.asyncio import AsyncEngine, AsyncSession, async_sessionmaker`.
    - `def build_session_factory(engine: AsyncEngine) -> async_sessionmaker[AsyncSession]:` returning `async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`.

    Create `trezarr/db/migration_runner.py` per PATTERNS §"trezarr/db/migration_runner.py" + RESEARCH Pattern 4:
    - Module docstring citing D-31 (baseline migration is source of truth), D-37 (the JSON-ledger migration arrives in 04-04 — leave a `# TODO 04-04` comment placeholder for `migrate_json_ledger_if_needed` but DO NOT implement it here), Pitfall 1 (always use `run_sync` bridge).
    - `from pathlib import Path`; `from alembic import command`; `from alembic.config import Config`; `from sqlalchemy.ext.asyncio import AsyncEngine`.
    - Constant `ALEMBIC_INI = Path(__file__).resolve().parents[2] / "alembic.ini"` (project-root-relative).
    - `def _do_upgrade(connection, cfg: Config) -> None:` — sets `cfg.attributes["connection"] = connection`; calls `command.upgrade(cfg, "head")`.
    - `async def run_migrations_to_head(engine: AsyncEngine) -> None:` — `cfg = Config(str(ALEMBIC_INI))`; `async with engine.begin() as conn: await conn.run_sync(_do_upgrade, cfg)`.

    Create `trezarr/bible/__init__.py` — empty package marker with one-line docstring `"""Series Bible: SQLAlchemy 2.0 typed models + Pydantic DTOs + store API (Phase 4)."""`. Do NOT create models.py here yet — that is Task 2.

    Extend `trezarr/config.py` per PATTERNS §"trezarr/config.py" — append immediately after the existing `# ── Phase 3: Permissions (D-29) ──` block:
    ```
    # ── Phase 4: Series Bible persistence (D-31, D-38) ─────────────────────────
    bible_db_url: str = "sqlite+aiosqlite:////config/trezarr.db"
    bible_db_run_migrations_on_startup: bool = True
    bible_db_enable_wal: bool = True
    bible_db_enforce_fk: bool = True
    ```
    Do NOT wrap `bible_db_url` in `SecretStr` (it is a file path, not a secret). Do NOT add `Field(default_factory=...)` — all four defaults are immutable primitives.

    Run `uv run alembic init alembic` from the repo root to scaffold the `alembic/` directory and `alembic.ini`. Then edit:
    - `alembic.ini`: ensure `script_location = alembic`; DELETE the `sqlalchemy.url = driver://user:pass@localhost/dbname` line (the URL is programmatically injected per RESEARCH Pattern 4).
    - `alembic/env.py`: REPLACE the generated body with RESEARCH Pattern 4's env.py verbatim (the version with `from trezarr.db.base import Base`, `from trezarr.bible import models  # noqa: F401` to populate Base.metadata per Pitfall 8, `target_metadata = Base.metadata`, `render_as_batch=True` per Pitfall 6, async-aware `run_migrations_online` that uses `config.attributes.get("connection")` when injected, and `run_migrations_offline` for `--sql` dry-runs). Note: `trezarr.bible.models` does not exist yet at this point — the noqa F401 import will be valid once Task 2 lands; for Task 1's test pass, comment out the import OR move the env.py wiring to Task 2's commit. Choose: comment it out with `# TODO Task 2: from trezarr.bible import models  # noqa: F401` and uncomment in Task 2.
    - `alembic/script.py.mako`: leave default.

    Create `tests/db/__init__.py` (empty).
    Create `tests/db/conftest.py` per PATTERNS §"tests/db/conftest.py":
    - Module docstring citing CONTEXT.md preference for temp-file SQLite over `:memory:` (Pitfall 3) and asyncio_mode="auto" config.
    - Imports: `import pytest_asyncio`, `from sqlalchemy.ext.asyncio import async_sessionmaker`, `from trezarr.db.engine import build_engine`, `from trezarr.db.migration_runner import run_migrations_to_head`, `from trezarr.config import TrezarrSettings`.
    - `@pytest_asyncio.fixture` `async def db_engine(tmp_path):` — `db_path = tmp_path / "trezarr.db"`; build `TrezarrSettings(bible_db_url=f"sqlite+aiosqlite:///{db_path}", llm_api_key="test-key")`; `engine = build_engine(settings)`; `await run_migrations_to_head(engine)`; `yield engine`; `await engine.dispose()`. NOTE: Task 2 will populate the migration; for Task 1's first commit, the fixture cannot run migrations until the baseline file exists — gate this fixture with a `try/except` that calls `await engine.dispose()` and `pytest.skip("baseline migration not yet created — Task 2")` until Task 2 lands. Alternatively, commit Task 1 + Task 2 together (preferred since they form one unblock).
    - `@pytest_asyncio.fixture` `async def session_factory(db_engine):` returns `async_sessionmaker(db_engine, expire_on_commit=False)`.

    Create `tests/db/test_engine.py` with the four behavior tests above. Each opens its own engine via the conftest `db_engine` fixture, then runs `await engine.connect()` twice (or uses two `async with` blocks) to prove the PRAGMA fires on a SECOND distinct connection (not just the first). For the FK test, the query is `await conn.execute(text("PRAGMA foreign_keys"))` and assert scalar == 1. Tests are `async def test_…` (asyncio_mode="auto" is configured project-wide).
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import sqlalchemy, aiosqlite, alembic; print(sqlalchemy.__version__, aiosqlite.__version__, alembic.__version__)" && uv run python -c "from trezarr.db.engine import build_engine; from trezarr.db.session import build_session_factory; from trezarr.db.migration_runner import run_migrations_to_head; from trezarr.db.base import Base; print('imports OK')" && grep -c '^bible_db_url' trezarr/config.py | grep -v ^# &amp;&amp; grep -q 'event.listens_for' trezarr/db/engine.py &amp;&amp; grep -q 'expire_on_commit=False' trezarr/db/session.py &amp;&amp; grep -q 'run_sync' trezarr/db/migration_runner.py &amp;&amp; grep -q 'render_as_batch=True' alembic/env.py &amp;&amp; grep -q '\*\.db-wal' .gitignore</automated>
  </verify>
  <done>
    - `uv sync` succeeds; the three new packages resolve at the pinned floors.
    - `trezarr.db.{engine,session,migration_runner,base}` and `trezarr.bible` import without error.
    - `trezarr/config.py` has `bible_db_url`, `bible_db_run_migrations_on_startup`, `bible_db_enable_wal`, `bible_db_enforce_fk` fields with correct defaults.
    - `alembic.ini` exists; `alembic/env.py` is async-aware and uses `render_as_batch=True`.
    - `.gitignore` covers `*.db-wal` and `*.db-shm`.
    - `tests/db/test_engine.py` and `tests/db/conftest.py` exist (tests may xfail/skip until Task 2's migration lands — that is intentional and stated in the conftest docstring).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLAlchemy 2.0 typed models + single Alembic baseline migration creating all 7 tables</name>
  <files>
    trezarr/bible/models.py,
    alembic/env.py,
    alembic/versions/0001_baseline_bible_schema.py,
    tests/db/test_migrations.py,
    tests/bible/__init__.py,
    tests/bible/test_models.py
  </files>
  <read_first>
    - trezarr/output/ledger.py lines 36-68 (LedgerEntry dataclass — the field names MUST appear 1:1 as columns on the processed_file table per D-20)
    - trezarr/subtitles/model.py lines 17-46 (rich domain-model docstring convention — apply the `Attributes:` block style to every model class)
    - .planning/phases/04-series-bible-store-schema/04-PATTERNS.md §"trezarr/bible/models.py" + §"Module Docstring Convention" + §"alembic.ini / alembic/env.py / alembic/versions/0001_baseline.py"
    - .planning/phases/04-series-bible-store-schema/04-RESEARCH.md §"Standard Stack" + §"Pattern 1: SQLAlchemy 2.0 Async Engine" + §"Pitfall 6: SQLite ALTER limitations" + §"Pitfall 8: Forgetting to import models in env.py"
    - .planning/phases/04-series-bible-store-schema/04-CONTEXT.md §"decisions" D-31 (full schema baseline) / D-32 (bible_event shape) / D-33 (UNIQUE constraint) / D-34 (locked_fields JSON) / D-35 (arr_metadata JSON, register NULL initially)
    - .planning/REQUIREMENTS.md §"Series Bible" — BIBLE-02 (character: name + gender + rough_age + role), BIBLE-04 (term_dictionary: source_term + vietnamese_rendering + category)
  </read_first>
  <behavior>
    - Test 1 (tests/db/test_migrations.py::test_baseline_creates_all_seven_tables): after `await run_migrations_to_head(engine)` on a fresh temp DB, exactly these 7 tables exist (queried via `sqlite_master`): series, character, term_dictionary, address_map, relationship_event, bible_event, processed_file. Plus the `alembic_version` bookkeeping table. Proves D-31.
    - Test 2 (tests/db/test_migrations.py::test_baseline_downgrade_drops_all_tables): `command.downgrade(cfg, "base")` removes all 7 tables, leaving only `alembic_version`. Proves the migration is reversible (forward-compat for Phase 8 dev workflows).
    - Test 3 (tests/db/test_migrations.py::test_series_unique_constraint_enforced): inserting two `Series` rows with the same `(arr_kind, arr_instance, arr_series_id)` raises `IntegrityError`. Proves D-33.
    - Test 4 (tests/bible/test_models.py::test_series_round_trip): INSERT a Series with `arr_kind="sonarr"`, `arr_instance="default"`, `arr_series_id=42`, `arr_metadata={"genres": ["drama"]}`, `register=None`, `tvdb_id=12345`, `tmdb_id=None`, `locked_fields=[]`; commit; SELECT-by-id returns identical values (`arr_metadata` round-trips as `dict`, `locked_fields` as `list`). Proves D-33/D-34/D-35 column shapes.
    - Test 5 (tests/bible/test_models.py::test_character_round_trip): INSERT a Character with `series_id=<existing>`, `original_latin_name="Mary"`, `gender="female"`, `rough_age="adult"`, `role="detective"`, `locked_fields=["role"]`; commit; reload — all fields including `locked_fields=["role"]` survive. Proves BIBLE-02.
    - Test 6 (tests/bible/test_models.py::test_term_dictionary_round_trip): INSERT a TermDictionary with `series_id=<existing>`, `source_term="King's Landing"`, `vietnamese_rendering="Vương Đô"`, `category="place"`, `locked_fields=[]`; commit; reload — survives. Proves BIBLE-04.
    - Test 7 (tests/bible/test_models.py::test_processed_file_round_trip): INSERT a ProcessedFile with all 9 field names from `LedgerEntry` (source_path, output_path, status, content_hash, series_id, source_lang, episode_key, translated_at, quarantine_path) — proves D-20 schema-compatibility. UNIQUE constraint on `source_path` is enforced (second INSERT with the same `source_path` raises IntegrityError).
    - Test 8 (tests/bible/test_models.py::test_bible_event_round_trip): INSERT a BibleEvent with all 9 fields from D-32 (series_id, episode_key, entity_type, entity_id, field, old_value, new_value, source, created_at); commit; reload — survives. `source ∈ {"inference", "lock", "import", "system"}` validated at insert time.
    - Test 9 (tests/bible/test_models.py::test_no_sqlalchemy_leakage_through_bible_init): `from trezarr.bible import models` succeeds (Pitfall 8 — env.py needs it); `import trezarr.bible` does NOT auto-import SQLAlchemy (no `import sqlalchemy` at __init__.py top — D-39).
  </behavior>
  <action>
    Create `trezarr/bible/models.py` per PATTERNS §"trezarr/bible/models.py":

    Module docstring citing D-31 (single baseline creates all 7 tables), D-32 (bible_event provenance schema), D-33 (UNIQUE identity), D-34 (locked_fields JSON), D-35 (arr_metadata JSON, register NULL), D-39 (SQLA stays inside the bible/ package; downstream imports DTOs only). List the 7 tables created. Note that `address_map` and `relationship_event` ship empty here — Phase 5/6 populate them.

    Imports: `from __future__ import annotations`; stdlib (`from datetime import datetime`, `from typing import Any`); `from sqlalchemy import ForeignKey, JSON, UniqueConstraint, String, Integer, DateTime, Text, func`; `from sqlalchemy.orm import Mapped, mapped_column, relationship`; `from trezarr.db.base import Base`.

    Define 7 classes using SQLAlchemy 2.0 typed-declarative style (`Mapped[T]` + `mapped_column(...)`); each with a rich `Attributes:` docstring per `trezarr/subtitles/model.py` convention:

    1. `class Series(Base):` `__tablename__ = "series"`; `__table_args__ = (UniqueConstraint("arr_kind", "arr_instance", "arr_series_id", name="uq_series_arr_identity"),)`. Columns: `id: Mapped[int] = mapped_column(primary_key=True)`; `arr_kind: Mapped[str]` (sonarr|radarr); `arr_instance: Mapped[str]` (default "default" via Python-side default — NOT a DB-level default, set at insert by the caller); `arr_series_id: Mapped[int]`; `tvdb_id: Mapped[int | None] = mapped_column(default=None)`; `tmdb_id: Mapped[int | None] = mapped_column(default=None)`; `register: Mapped[str | None] = mapped_column(default=None)`; `arr_metadata: Mapped[dict[str, Any]] = mapped_column(JSON, default=dict)`; `locked_fields: Mapped[list[str]] = mapped_column(JSON, default=list)`. Relationships: `characters: Mapped[list["Character"]] = relationship(back_populates="series")`; `terms: Mapped[list["TermDictionary"]] = relationship(back_populates="series")`. Per CONTEXT.md §"specifics" the `locked_fields` default is `list` (mutable callable, NOT `[]` literal — SQLAlchemy needs a callable for mutable defaults too).

    2. `class Character(Base):` `__tablename__ = "character"`. Columns: `id` (PK int); `series_id: Mapped[int] = mapped_column(ForeignKey("series.id"))`; `original_latin_name: Mapped[str]`; `gender: Mapped[str | None] = mapped_column(default=None)`; `rough_age: Mapped[str | None] = mapped_column(default=None)`; `role: Mapped[str | None] = mapped_column(default=None)`; `locked_fields: Mapped[list[str]] = mapped_column(JSON, default=list)`. `series: Mapped["Series"] = relationship(back_populates="characters")`.

    3. `class TermDictionary(Base):` `__tablename__ = "term_dictionary"`. Columns: `id` (PK); `series_id` (FK → series.id); `source_term: Mapped[str]`; `vietnamese_rendering: Mapped[str]`; `category: Mapped[str | None] = mapped_column(default=None)` (proper_noun|title|place|jargon — Python-side enum, no CHECK constraint for v1); `locked_fields: Mapped[list[str]] = mapped_column(JSON, default=list)`. `series: Mapped["Series"] = relationship(back_populates="terms")`.

    4. `class AddressMap(Base):` `__tablename__ = "address_map"` (empty in Phase 4; Phase 5 populates per BIBLE-03). Columns: `id` (PK); `series_id` (FK → series.id); `speaker_character_id: Mapped[int] = mapped_column(ForeignKey("character.id"))`; `addressee_character_id: Mapped[int] = mapped_column(ForeignKey("character.id"))`; `self_term: Mapped[str | None] = mapped_column(default=None)`; `address_term: Mapped[str | None] = mapped_column(default=None)`; `locked_fields: Mapped[list[str]] = mapped_column(JSON, default=list)`; `valid_from_episode: Mapped[str | None] = mapped_column(default=None)`.

    5. `class RelationshipEvent(Base):` `__tablename__ = "relationship_event"` (empty in Phase 4; Phase 6 populates per BIBLE-07). Columns: `id` (PK); `series_id` (FK → series.id); `character_a_id: Mapped[int] = mapped_column(ForeignKey("character.id"))`; `character_b_id: Mapped[int] = mapped_column(ForeignKey("character.id"))`; `episode_marker: Mapped[str]`; `description: Mapped[str | None] = mapped_column(Text, default=None)`; `created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.current_timestamp())`.

    6. `class BibleEvent(Base):` `__tablename__ = "bible_event"` (D-32 append-only audit log). Columns: `id` (PK); `series_id` (FK → series.id); `episode_key: Mapped[str | None] = mapped_column(default=None)`; `entity_type: Mapped[str]` (character|term_dictionary|series); `entity_id: Mapped[int]`; `field: Mapped[str]`; `old_value: Mapped[dict | str | int | None] = mapped_column(JSON, default=None)`; `new_value: Mapped[dict | str | int | None] = mapped_column(JSON, default=None)`; `source: Mapped[str]` (inference|lock|import|system); `created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.current_timestamp())`. NOTE: `old_value`/`new_value` use JSON column (per RESEARCH §"Claude's Discretion" A3 — JSON future-proofs structured fields).

    7. `class ProcessedFile(Base):` `__tablename__ = "processed_file"` (D-37 JSON-ledger swap target — D-20 schema-compat). Columns MUST match `trezarr.output.ledger.LedgerEntry` field names verbatim: `id: Mapped[int] = mapped_column(primary_key=True)` (surrogate PK per RESEARCH A2); `source_path: Mapped[str] = mapped_column(unique=True)` (UNIQUE per RESEARCH A2); `output_path: Mapped[str | None] = mapped_column(default=None)`; `status: Mapped[str]` (done|quarantined|in_progress — Python-level enum); `content_hash: Mapped[str]`; `series_id: Mapped[str | None] = mapped_column(default=None)` (string per LedgerEntry; no FK to series.id — this column is the legacy text identifier, not a relational link); `source_lang: Mapped[str | None] = mapped_column(default=None)`; `episode_key: Mapped[str | None] = mapped_column(default=None)`; `translated_at: Mapped[str | None] = mapped_column(default=None)` (ISO-8601 string per LedgerEntry); `quarantine_path: Mapped[str | None] = mapped_column(default=None)`.

    Now uncomment / write the line `from trezarr.bible import models  # noqa: F401` at the top of `alembic/env.py` (Pitfall 8 — required for `Base.metadata` to be populated).

    Hand-author `alembic/versions/0001_baseline_bible_schema.py`. NOT autogenerate (avoid drift risk on the foundational migration). Structure:
    - `revision = "0001"`; `down_revision = None`; `branch_labels = None`; `depends_on = None`.
    - Module docstring: "Baseline migration: create all 7 long-term Series Bible tables (D-31, D-32, D-33)."
    - `def upgrade() -> None:` — `op.create_table("series", ...)`, `op.create_table("character", ...)`, etc. for all 7 tables in FK-safe creation order: series → character → term_dictionary → address_map → relationship_event → bible_event → processed_file. Use `sa.Column("col", sa.Type, ...)` with explicit `nullable=False/True`, FK constraints, and the `uq_series_arr_identity` UniqueConstraint on the series table. For JSON columns: `sa.Column("arr_metadata", sa.JSON(), nullable=False, server_default="{}"")` for series.arr_metadata; `sa.Column("locked_fields", sa.JSON(), nullable=False, server_default="[]"")` for every entity table.
    - `def downgrade() -> None:` — `op.drop_table(...)` for all 7 in REVERSE order: processed_file → bible_event → relationship_event → address_map → term_dictionary → character → series.

    Create `tests/bible/__init__.py` (empty).
    Create `tests/db/test_migrations.py` with Tests 1-3 above. Use the `db_engine` fixture for Test 1 (migrations have already run); for Test 2, build a fresh engine inside the test, run `run_migrations_to_head`, then call alembic's `command.downgrade(cfg, "base")` via the same `run_sync` bridge; for Test 3, open a session and attempt two duplicate INSERTs, asserting `IntegrityError`.
    Create `tests/bible/test_models.py` with Tests 4-9 above. Use the `session_factory` fixture from `tests/db/conftest.py`. Tests are `async def`.
  </action>
  <verify>
    <automated>uv run pytest tests/db/test_engine.py tests/db/test_migrations.py tests/bible/test_models.py -x &amp;&amp; uv run python -c "from trezarr.db.base import Base; from trezarr.bible import models; tables = sorted(Base.metadata.tables.keys()); assert tables == ['address_map', 'bible_event', 'character', 'processed_file', 'relationship_event', 'series', 'term_dictionary'], tables; print('schema OK:', tables)"</automated>
  </verify>
  <done>
    - All 7 SQLAlchemy 2.0 typed-declarative models exist in `trezarr/bible/models.py` with field shapes matching D-31/D-32/D-33/D-34/D-35 and D-20 LedgerEntry parity.
    - Single Alembic baseline `alembic/versions/0001_baseline_bible_schema.py` creates all 7 tables on `upgrade()` and drops them on `downgrade()`.
    - `alembic/env.py` imports `trezarr.bible.models` so `Base.metadata` is populated for autogenerate compare-checks (Pitfall 8).
    - `tests/db/test_engine.py` (from Task 1) + `tests/db/test_migrations.py` + `tests/bible/test_models.py` all pass: PRAGMAs apply per-connection, baseline produces exactly 7 tables, UNIQUE on series triple enforces, all six core models round-trip correctly.
    - `trezarr.bible.__init__` does NOT import SQLAlchemy (D-39 boundary holds).
  </done>
</task>

</tasks>

<verification>
- Run `uv sync` and verify the three new packages are pinned at the documented floors (`sqlalchemy>=2.0.50,<2.1`, `aiosqlite>=0.22.1,<0.23`, `alembic>=1.18.4,<1.19`).
- Run `uv run pytest tests/db tests/bible -x` — all tests in both directories pass.
- Run `uv run pytest -x` — the FULL existing Phase 1/2/3 suite continues to pass (no regression from config.py extension).
- Run `uv run python -c "from trezarr.db.engine import build_engine; from trezarr.config import TrezarrSettings; e = build_engine(TrezarrSettings(llm_api_key='k', bible_db_url='sqlite+aiosqlite:///:memory:')); print(type(e).__name__)"` → prints `AsyncEngine`.
- Inspect `alembic upgrade head --sql` output (offline mode dry-run via `uv run alembic upgrade head --sql`) and confirm it lists CREATE TABLE for all 7 tables with the expected columns and the UNIQUE constraint on series.
- `grep -n "bible_db_url\|bible_db_run_migrations_on_startup\|bible_db_enable_wal\|bible_db_enforce_fk" trezarr/config.py` — all four fields present, no `SecretStr` wrap on the URL.
</verification>

<success_criteria>
- `uv sync` resolves with SQLAlchemy 2.0.50+, aiosqlite 0.22.1+, alembic 1.18.4+ installed and importable.
- `trezarr.db.engine.build_engine(settings)` returns an `AsyncEngine` that emits WAL + FK=ON + synchronous=NORMAL PRAGMAs on EVERY pool connection (validated by a test that opens two separate connections).
- `trezarr.db.session.build_session_factory(engine)` returns an `async_sessionmaker` with `expire_on_commit=False`.
- `trezarr.db.migration_runner.run_migrations_to_head(engine)` runs the Alembic baseline via the `connection.run_sync(_do_upgrade)` bridge with no event-loop blocking.
- A single Alembic baseline migration file at `alembic/versions/0001_baseline_bible_schema.py` creates all 7 tables (series, character, term_dictionary, address_map, relationship_event, bible_event, processed_file) plus reversible `downgrade()`.
- `trezarr/bible/models.py` defines 7 SQLAlchemy 2.0 typed models with field shapes matching D-31/D-32/D-33/D-34/D-35 and D-20 LedgerEntry parity.
- `tests/db/test_engine.py` (4 PRAGMA tests), `tests/db/test_migrations.py` (3 migration tests), `tests/bible/test_models.py` (6 round-trip + boundary tests) all pass.
- `trezarr/config.py` extends with 4 Phase-4 fields under a `# ── Phase 4: Series Bible persistence (D-31, D-38) ──` header; no other config changes.
- `.gitignore` covers `*.db-wal` and `*.db-shm` (WAL sidecar files).
- Existing Phase 1/2/3 tests continue to pass.
</success_criteria>

<output>
Create `.planning/phases/04-series-bible-store-schema/04-01-SUMMARY.md` when done.
</output>
