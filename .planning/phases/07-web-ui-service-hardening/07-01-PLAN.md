---
phase: 07-web-ui-service-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/web/__init__.py
  - tests/web/test_lifespan.py
  - tests/web/test_settings_api.py
  - tests/web/test_connection_tests.py
  - tests/web/test_jobs_api.py
  - tests/web/test_webhook.py
  - tests/web/test_scheduler.py
  - tests/web/test_worker.py
  - tests/db/test_migrations.py
autonomous: true
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04
  - AUTO-02
  - AUTO-05

must_haves:
  truths:
    - "All Phase-7 test stubs exist as xfail markers — the RED suite runs without import errors"
    - "tests/web/ package is importable and recognized by pytest"
    - "tests/db/test_migrations.py contains a stub for test_0002_migration"
  artifacts:
    - path: "tests/web/__init__.py"
      provides: "Package marker for web tests"
    - path: "tests/web/test_lifespan.py"
      provides: "Stubs for SVC-01 lifespan / engine lifecycle tests"
    - path: "tests/web/test_settings_api.py"
      provides: "Stubs for SVC-02 settings masking / write-back / secret-never-in-response"
    - path: "tests/web/test_connection_tests.py"
      provides: "Stubs for SVC-02 connection-test endpoints"
    - path: "tests/web/test_jobs_api.py"
      provides: "Stubs for SVC-03/SVC-04 queue/history/logs/retry"
    - path: "tests/web/test_webhook.py"
      provides: "Stubs for AUTO-02 webhook handler"
    - path: "tests/web/test_scheduler.py"
      provides: "Stubs for AUTO-02 poll scheduler"
    - path: "tests/web/test_worker.py"
      provides: "Stubs for AUTO-05/D-68 worker per-series lock + no-second-semaphore"
    - path: "tests/db/test_migrations.py"
      provides: "Appended stub for D-69 Alembic 0002 migration"
  key_links:
    - from: "tests/web/test_worker.py"
      to: "trezarr.web.worker"
      via: "xfail import stub"
      pattern: "pytest.mark.xfail.*ImportError"
    - from: "tests/db/test_migrations.py"
      to: "alembic/versions/0002_job_queue.py"
      via: "xfail migration stub"
      pattern: "test_0002_migration"
---

## Phase Goal

**As an** operator, **I want to** run a full Phase-7 test suite that starts RED and turns GREEN incrementally, **so that** every capability wave has immediate automated verification feedback.

<objective>
Create all Wave-0 RED test stubs for Phase 7 per 07-VALIDATION.md. These stubs are xfail markers that give the test infrastructure a Nyquist-compliant feedback surface before any implementation exists. The stubs cover SVC-01, SVC-02, SVC-03, SVC-04, AUTO-02, AUTO-05, and D-68/D-69/D-70.

Purpose: Every subsequent plan has automated verify commands that go GREEN as implementation lands. Zero implementation gaps in the feedback loop.
Output: tests/web/ package (8 test files + __init__.py) and an appended stub in tests/db/test_migrations.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-web-ui-service-hardening/07-VALIDATION.md
@.planning/phases/07-web-ui-service-hardening/07-PATTERNS.md
@tests/db/conftest.py
@tests/db/test_migrations.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/web/ package with lifespan, settings, and connection-test stubs</name>
  <files>
    tests/web/__init__.py,
    tests/web/test_lifespan.py,
    tests/web/test_settings_api.py,
    tests/web/test_connection_tests.py
  </files>
  <read_first>
    - tests/db/__init__.py — empty __init__.py pattern
    - tests/db/test_migrations.py — xfail stub pattern with raises=(ImportError,), reason text citing phase plan number
    - tests/db/conftest.py — db_engine and session_factory fixtures (reused from tests.db.conftest in new web tests)
    - .planning/phases/07-web-ui-service-hardening/07-VALIDATION.md §Wave 0 Requirements — exact test names per row
    - .planning/phases/07-web-ui-service-hardening/07-PATTERNS.md §tests/web/test_lifespan.py, §tests/web/test_settings_api.py, §tests/web/test_connection_tests.py
  </read_first>
  <action>
    Create tests/web/__init__.py as an empty file (same as tests/db/__init__.py).

    Create tests/web/test_lifespan.py with xfail stubs for the following test functions (per 07-VALIDATION.md):
    - test_lifespan_startup_shutdown — xfail(raises=(ImportError, AssertionError), reason="trezarr.web.app not yet created — Plan 07-02")
    - test_engine_disposed_on_shutdown — same xfail
    - test_serve_subcommand_exists — xfail(raises=(ImportError, AssertionError), reason="trezarr.web.app.run_serve not yet created — Plan 07-02"). This one imports from trezarr.cli and asserts a serve subparser exists.

    Import pattern (top of test_lifespan.py): only stdlib + pytest; no trezarr.web imports at module level (they go inside the xfail test body to avoid collection-time errors).

    Create tests/web/test_settings_api.py with xfail stubs for:
    - test_secrets_masked — SVC-02; xfail(raises=(ImportError, AssertionError, TypeError))
    - test_settings_write — SVC-02; xfail same
    - test_secret_never_in_response — D-70; xfail same; docstring: "GET /api/settings must never return raw SecretStr value"

    Create tests/web/test_connection_tests.py with xfail stubs for:
    - test_sonarr_connection_ok — SVC-02; xfail(raises=(ImportError, AssertionError))
    - test_sonarr_connection_error — SVC-02; xfail same
    - test_llm_connection_ok — SVC-02; xfail same
    - test_secret_not_in_connection_error — D-71 security; xfail same; docstring: "Connection test error response must never include the API key value"

    All test files begin with `from __future__ import annotations` and use asyncio_mode=auto (no explicit mark needed). Async test fixtures use @pytest_asyncio.fixture, not @pytest.fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/web/test_lifespan.py tests/web/test_settings_api.py tests/web/test_connection_tests.py -v --tb=short 2>&1 | grep -E "XFAIL|PASSED|FAILED|ERROR|test session"</automated>
  </verify>
  <done>All stubs in the three files report xfail (not FAILED, not ERROR); pytest collects the tests/web/ package without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create jobs, webhook, scheduler, and worker stubs; append migration stub</name>
  <files>
    tests/web/test_jobs_api.py,
    tests/web/test_webhook.py,
    tests/web/test_scheduler.py,
    tests/web/test_worker.py,
    tests/db/test_migrations.py
  </files>
  <read_first>
    - tests/db/test_migrations.py — existing content to append to, not overwrite; pattern for test_baseline_creates_all_seven_tables (the 0001 migration test)
    - tests/output/test_ledger_sqla.py — async DB fixture usage pattern (session_factory from conftest)
    - tests/arr/test_arr_discovery.py — httpx_mock pattern for mocking pyarr; plain def tests (pyarr is sync)
    - .planning/phases/07-web-ui-service-hardening/07-VALIDATION.md §Per-Task Verification Map — exact test names for each row
    - .planning/phases/07-web-ui-service-hardening/07-PATTERNS.md §tests/web/test_jobs_api.py, §tests/web/test_worker.py
  </read_first>
  <action>
    Create tests/web/test_jobs_api.py with xfail stubs for (per 07-VALIDATION.md):
    - test_get_queue — SVC-03; xfail(raises=(ImportError, AssertionError, TypeError))
    - test_get_history — SVC-03; xfail same
    - test_get_job_logs — SVC-03; xfail same; covers GET /api/jobs/{id}/logs
    - test_retry_requeues — SVC-04; xfail same; docstring: "POST /api/jobs/{id}/retry must re-enqueue and set trigger=manual-retry (D-74)"

    Create tests/web/test_webhook.py with xfail stubs for:
    - test_webhook_enqueues — AUTO-02; xfail(raises=(ImportError, AssertionError))
    - test_webhook_returns_200_fast — AUTO-02; xfail same; docstring: "Webhook handler must return 200 without awaiting translation (D-66, Pitfall D)"
    - test_webhook_ignores_payload_content — AUTO-02/D-66; xfail same; docstring: "Handler triggers scan, never trusts payload source_path directly"

    Create tests/web/test_scheduler.py with xfail stubs for:
    - test_poll_enqueues — AUTO-02; xfail(raises=(ImportError, AssertionError))
    - test_poll_deduplicates — AUTO-02/D-66; xfail same; docstring: "Poll must not double-enqueue an already-queued source_path"

    Create tests/web/test_worker.py with xfail stubs for:
    - test_reconcile_in_progress — AUTO-05/D-67; xfail(raises=(ImportError, AssertionError, TypeError))
    - test_per_series_serialization — AUTO-05/D-68; xfail same; docstring: "Two episodes of same series_id must run serially"
    - test_distinct_series_concurrent — AUTO-05/D-68; xfail same
    - test_no_second_semaphore — D-68; xfail same; docstring: "worker must not introduce a second asyncio.Semaphore — only asyncio.Lock per series (D-68/Pitfall C)"

    Append to tests/db/test_migrations.py (do NOT overwrite existing content). Add after the last existing test:
    - test_0002_migration_creates_job_tables — D-69; xfail(raises=(AssertionError,), reason="Alembic 0002 migration not yet authored — Plan 07-02"); docstring: "After run_migrations_to_head, 'job' and 'job_log' tables must exist in sqlite_master"
    The stub body calls assert False to reliably xfail.
  </action>
  <verify>
    <automated>uv run pytest tests/web/ tests/db/test_migrations.py -v --tb=short 2>&1 | grep -E "XFAIL|PASSED|FAILED|ERROR|xfailed|test session"</automated>
  </verify>
  <done>All new stubs report xfail; the existing tests in tests/db/test_migrations.py remain PASSED (not broken by the append); total xfail count in tests/web/ matches the 19 stubs defined in 07-VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → trezarr modules | xfail stubs import trezarr.web.* inside test bodies; collection-time import errors are deliberately avoided |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | npm/pip/cargo installs (none in this plan) | accept | No new packages installed in this plan — test stubs use only stdlib + existing pytest |
</threat_model>

<verification>
Full RED suite passes (all xfail, no FAILED, no ERROR):

```
uv run pytest tests/web/ tests/db/test_migrations.py::test_0002_migration_creates_job_tables -v --tb=short
```

Expected: all stubs xfail, existing tests/db/ tests still pass.
</verification>

<success_criteria>
- tests/web/__init__.py exists (0 bytes)
- 8 test files exist in tests/web/ per 07-VALIDATION.md Wave 0 list
- tests/db/test_migrations.py has test_0002_migration_creates_job_tables appended (xfail)
- `uv run pytest tests/web/ -v` reports 0 FAILED, 0 ERROR — only xfail
- `uv run pytest tests/db/test_migrations.py -v` still passes the existing 0001 tests
</success_criteria>

<output>
Create `.planning/phases/07-web-ui-service-hardening/07-01-SUMMARY.md` when done
</output>
