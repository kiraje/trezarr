---
type: milestone-smoke-test
milestone: v1.0
verified: 2026-06-03
status: passed
target: live deployed container (trezarr:local @ 93454ceffa76), http://localhost:6868
image_built_from: HEAD 3c7f5ae (includes Phase 9 multi-format + Phase 10 source-selection)
db_revision: "0003"
---

# Milestone v1.0 — Live Deployment Smoke Test

End-to-end acceptance run against the **actual deployed artifact** (freshly cold-started
Docker container on the verified HEAD code), not synthetic per-phase scripts. Doubles as
the verify-work "cold-start smoke test."

## Environment

- Container: `trezarr` on image `trezarr:local` (`93454ceffa76`), rebuilt from HEAD `3c7f5ae`.
- Rollback image retained: `trezarr:rollback` (`ca7733289963`, the prior 26h-old build).
- Mounts: `/Users/dustin/trezarr-config → /config`. No `/media` mount; no *arr API keys
  configured (LLM endpoint only) — so discovery/queue are legitimately empty.
- DB: `/config/trezarr.db` migrated `0002 → 0003` on this start (Phase 10 override columns
  the old image never had). `alembic_version = 0003`.

## Results — ALL PASS

| # | Check | Method | Result |
|---|-------|--------|--------|
| 1 | Cold start, no crash loop | `docker inspect` | ✅ `RestartCount=0`, `Health=healthy` |
| 2 | DB migration applied | `alembic_version` | ✅ at head `0003` |
| 3 | SPA served (fresh build) | `GET /` | ✅ `<title>Trezarr</title>` + `index-CAJHVK85.js` (this build's hash) |
| 4 | OpenAPI surface | `GET /openapi.json` | ✅ 24 paths, title=Trezarr v0.1.0 |
| 5 | Health endpoint | `GET /api/health` | ✅ `200 {"status":"ok"}` |
| 6 | Series Bible queryable | `GET /api/series` | ✅ `200 []` (empty; no *arr wired) |
| 7 | Translation queue | `GET /api/queue` | ✅ `200 []` |
| 8 | Job history | `GET /api/jobs` | ✅ `200 []` |
| 9 | Vietnamese pronoun reference (domain core) | `GET /api/pronouns` | ✅ `200`, self/address term sets present (tôi, con, em, anh, chị, ông, bà…) |
| 10 | Settings readable + secret hygiene | `GET /api/settings` | ✅ `200`, `llm_api_key` REDACTED, `is_set:true` |
| 11 | Env-lock design (D-78) | `GET /api/settings/env-locked` | ✅ `["llm_base_url","llm_api_key","llm_model"]` locked from env |
| 12 | LLM connectivity (the one live external dep) | `POST /api/test/llm` (run in-container; key never left the container) | ✅ `200 {"ok":true}` against api.tlemons.com / ds/deepseek-v4-pro |
| 13 | Phase 9 multi-format codec shipped | `docker exec ls/grep` | ✅ `ass.py`, `vtt.py`, `dispatch.py` present; `_SOURCE_SUFFIXES = ("srt","ass","ssa","vtt")` live |

## Scope / Honest limits

- This verifies the **service** works end-to-end: deploy, cold start, DB migration, REST API,
  SPA, config + secret handling, the Vietnamese domain data, and **live LLM connectivity**.
- It does NOT exercise a **full real episode translation**, because no Sonarr/Radarr/Bazarr
  and no `/media` volume are wired into this deployment. A real translation run requires the
  *arr stack + a path-aligned media mount.
- Pre-existing per-phase human-UAT state (separate from this run): phases 4/5/6 `partial`,
  phase 7 `pending`, phases 8/10 `passed`. Phase 9 signed off 2026-06-03 (12/12).

## Verdict

**Milestone v1.0 deployed artifact is verified working.** Ready for `/gsd-complete-milestone`.
